#!/usr/bin/env node
import { createRequire as __cr } from 'node:module'; const require = __cr(import.meta.url);
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var __commonJS = (cb, mod) => function __require2() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/error.js
var require_error = __commonJS({
  "../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/error.js"(exports) {
    var CommanderError2 = class extends Error {
      /**
       * Constructs the CommanderError class
       * @param {number} exitCode suggested exit code which could be used with process.exit
       * @param {string} code an id string representing the error
       * @param {string} message human-readable description of the error
       */
      constructor(exitCode, code, message) {
        super(message);
        Error.captureStackTrace(this, this.constructor);
        this.name = this.constructor.name;
        this.code = code;
        this.exitCode = exitCode;
        this.nestedError = void 0;
      }
    };
    var InvalidArgumentError2 = class extends CommanderError2 {
      /**
       * Constructs the InvalidArgumentError class
       * @param {string} [message] explanation of why argument is invalid
       */
      constructor(message) {
        super(1, "commander.invalidArgument", message);
        Error.captureStackTrace(this, this.constructor);
        this.name = this.constructor.name;
      }
    };
    exports.CommanderError = CommanderError2;
    exports.InvalidArgumentError = InvalidArgumentError2;
  }
});

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/argument.js
var require_argument = __commonJS({
  "../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/argument.js"(exports) {
    var { InvalidArgumentError: InvalidArgumentError2 } = require_error();
    var Argument2 = class {
      /**
       * Initialize a new command argument with the given name and description.
       * The default is that the argument is required, and you can explicitly
       * indicate this with <> around the name. Put [] around the name for an optional argument.
       *
       * @param {string} name
       * @param {string} [description]
       */
      constructor(name, description) {
        this.description = description || "";
        this.variadic = false;
        this.parseArg = void 0;
        this.defaultValue = void 0;
        this.defaultValueDescription = void 0;
        this.argChoices = void 0;
        switch (name[0]) {
          case "<":
            this.required = true;
            this._name = name.slice(1, -1);
            break;
          case "[":
            this.required = false;
            this._name = name.slice(1, -1);
            break;
          default:
            this.required = true;
            this._name = name;
            break;
        }
        if (this._name.length > 3 && this._name.slice(-3) === "...") {
          this.variadic = true;
          this._name = this._name.slice(0, -3);
        }
      }
      /**
       * Return argument name.
       *
       * @return {string}
       */
      name() {
        return this._name;
      }
      /**
       * @package
       */
      _concatValue(value, previous) {
        if (previous === this.defaultValue || !Array.isArray(previous)) {
          return [value];
        }
        return previous.concat(value);
      }
      /**
       * Set the default value, and optionally supply the description to be displayed in the help.
       *
       * @param {*} value
       * @param {string} [description]
       * @return {Argument}
       */
      default(value, description) {
        this.defaultValue = value;
        this.defaultValueDescription = description;
        return this;
      }
      /**
       * Set the custom handler for processing CLI command arguments into argument values.
       *
       * @param {Function} [fn]
       * @return {Argument}
       */
      argParser(fn) {
        this.parseArg = fn;
        return this;
      }
      /**
       * Only allow argument value to be one of choices.
       *
       * @param {string[]} values
       * @return {Argument}
       */
      choices(values) {
        this.argChoices = values.slice();
        this.parseArg = (arg, previous) => {
          if (!this.argChoices.includes(arg)) {
            throw new InvalidArgumentError2(
              `Allowed choices are ${this.argChoices.join(", ")}.`
            );
          }
          if (this.variadic) {
            return this._concatValue(arg, previous);
          }
          return arg;
        };
        return this;
      }
      /**
       * Make argument required.
       *
       * @returns {Argument}
       */
      argRequired() {
        this.required = true;
        return this;
      }
      /**
       * Make argument optional.
       *
       * @returns {Argument}
       */
      argOptional() {
        this.required = false;
        return this;
      }
    };
    function humanReadableArgName(arg) {
      const nameOutput = arg.name() + (arg.variadic === true ? "..." : "");
      return arg.required ? "<" + nameOutput + ">" : "[" + nameOutput + "]";
    }
    exports.Argument = Argument2;
    exports.humanReadableArgName = humanReadableArgName;
  }
});

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/help.js
var require_help = __commonJS({
  "../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/help.js"(exports) {
    var { humanReadableArgName } = require_argument();
    var Help2 = class {
      constructor() {
        this.helpWidth = void 0;
        this.minWidthToWrap = 40;
        this.sortSubcommands = false;
        this.sortOptions = false;
        this.showGlobalOptions = false;
      }
      /**
       * prepareContext is called by Commander after applying overrides from `Command.configureHelp()`
       * and just before calling `formatHelp()`.
       *
       * Commander just uses the helpWidth and the rest is provided for optional use by more complex subclasses.
       *
       * @param {{ error?: boolean, helpWidth?: number, outputHasColors?: boolean }} contextOptions
       */
      prepareContext(contextOptions) {
        this.helpWidth = this.helpWidth ?? contextOptions.helpWidth ?? 80;
      }
      /**
       * Get an array of the visible subcommands. Includes a placeholder for the implicit help command, if there is one.
       *
       * @param {Command} cmd
       * @returns {Command[]}
       */
      visibleCommands(cmd) {
        const visibleCommands = cmd.commands.filter((cmd2) => !cmd2._hidden);
        const helpCommand = cmd._getHelpCommand();
        if (helpCommand && !helpCommand._hidden) {
          visibleCommands.push(helpCommand);
        }
        if (this.sortSubcommands) {
          visibleCommands.sort((a, b) => {
            return a.name().localeCompare(b.name());
          });
        }
        return visibleCommands;
      }
      /**
       * Compare options for sort.
       *
       * @param {Option} a
       * @param {Option} b
       * @returns {number}
       */
      compareOptions(a, b) {
        const getSortKey = (option) => {
          return option.short ? option.short.replace(/^-/, "") : option.long.replace(/^--/, "");
        };
        return getSortKey(a).localeCompare(getSortKey(b));
      }
      /**
       * Get an array of the visible options. Includes a placeholder for the implicit help option, if there is one.
       *
       * @param {Command} cmd
       * @returns {Option[]}
       */
      visibleOptions(cmd) {
        const visibleOptions = cmd.options.filter((option) => !option.hidden);
        const helpOption = cmd._getHelpOption();
        if (helpOption && !helpOption.hidden) {
          const removeShort = helpOption.short && cmd._findOption(helpOption.short);
          const removeLong = helpOption.long && cmd._findOption(helpOption.long);
          if (!removeShort && !removeLong) {
            visibleOptions.push(helpOption);
          } else if (helpOption.long && !removeLong) {
            visibleOptions.push(
              cmd.createOption(helpOption.long, helpOption.description)
            );
          } else if (helpOption.short && !removeShort) {
            visibleOptions.push(
              cmd.createOption(helpOption.short, helpOption.description)
            );
          }
        }
        if (this.sortOptions) {
          visibleOptions.sort(this.compareOptions);
        }
        return visibleOptions;
      }
      /**
       * Get an array of the visible global options. (Not including help.)
       *
       * @param {Command} cmd
       * @returns {Option[]}
       */
      visibleGlobalOptions(cmd) {
        if (!this.showGlobalOptions) return [];
        const globalOptions = [];
        for (let ancestorCmd = cmd.parent; ancestorCmd; ancestorCmd = ancestorCmd.parent) {
          const visibleOptions = ancestorCmd.options.filter(
            (option) => !option.hidden
          );
          globalOptions.push(...visibleOptions);
        }
        if (this.sortOptions) {
          globalOptions.sort(this.compareOptions);
        }
        return globalOptions;
      }
      /**
       * Get an array of the arguments if any have a description.
       *
       * @param {Command} cmd
       * @returns {Argument[]}
       */
      visibleArguments(cmd) {
        if (cmd._argsDescription) {
          cmd.registeredArguments.forEach((argument) => {
            argument.description = argument.description || cmd._argsDescription[argument.name()] || "";
          });
        }
        if (cmd.registeredArguments.find((argument) => argument.description)) {
          return cmd.registeredArguments;
        }
        return [];
      }
      /**
       * Get the command term to show in the list of subcommands.
       *
       * @param {Command} cmd
       * @returns {string}
       */
      subcommandTerm(cmd) {
        const args = cmd.registeredArguments.map((arg) => humanReadableArgName(arg)).join(" ");
        return cmd._name + (cmd._aliases[0] ? "|" + cmd._aliases[0] : "") + (cmd.options.length ? " [options]" : "") + // simplistic check for non-help option
        (args ? " " + args : "");
      }
      /**
       * Get the option term to show in the list of options.
       *
       * @param {Option} option
       * @returns {string}
       */
      optionTerm(option) {
        return option.flags;
      }
      /**
       * Get the argument term to show in the list of arguments.
       *
       * @param {Argument} argument
       * @returns {string}
       */
      argumentTerm(argument) {
        return argument.name();
      }
      /**
       * Get the longest command term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestSubcommandTermLength(cmd, helper) {
        return helper.visibleCommands(cmd).reduce((max, command) => {
          return Math.max(
            max,
            this.displayWidth(
              helper.styleSubcommandTerm(helper.subcommandTerm(command))
            )
          );
        }, 0);
      }
      /**
       * Get the longest option term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestOptionTermLength(cmd, helper) {
        return helper.visibleOptions(cmd).reduce((max, option) => {
          return Math.max(
            max,
            this.displayWidth(helper.styleOptionTerm(helper.optionTerm(option)))
          );
        }, 0);
      }
      /**
       * Get the longest global option term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestGlobalOptionTermLength(cmd, helper) {
        return helper.visibleGlobalOptions(cmd).reduce((max, option) => {
          return Math.max(
            max,
            this.displayWidth(helper.styleOptionTerm(helper.optionTerm(option)))
          );
        }, 0);
      }
      /**
       * Get the longest argument term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestArgumentTermLength(cmd, helper) {
        return helper.visibleArguments(cmd).reduce((max, argument) => {
          return Math.max(
            max,
            this.displayWidth(
              helper.styleArgumentTerm(helper.argumentTerm(argument))
            )
          );
        }, 0);
      }
      /**
       * Get the command usage to be displayed at the top of the built-in help.
       *
       * @param {Command} cmd
       * @returns {string}
       */
      commandUsage(cmd) {
        let cmdName = cmd._name;
        if (cmd._aliases[0]) {
          cmdName = cmdName + "|" + cmd._aliases[0];
        }
        let ancestorCmdNames = "";
        for (let ancestorCmd = cmd.parent; ancestorCmd; ancestorCmd = ancestorCmd.parent) {
          ancestorCmdNames = ancestorCmd.name() + " " + ancestorCmdNames;
        }
        return ancestorCmdNames + cmdName + " " + cmd.usage();
      }
      /**
       * Get the description for the command.
       *
       * @param {Command} cmd
       * @returns {string}
       */
      commandDescription(cmd) {
        return cmd.description();
      }
      /**
       * Get the subcommand summary to show in the list of subcommands.
       * (Fallback to description for backwards compatibility.)
       *
       * @param {Command} cmd
       * @returns {string}
       */
      subcommandDescription(cmd) {
        return cmd.summary() || cmd.description();
      }
      /**
       * Get the option description to show in the list of options.
       *
       * @param {Option} option
       * @return {string}
       */
      optionDescription(option) {
        const extraInfo = [];
        if (option.argChoices) {
          extraInfo.push(
            // use stringify to match the display of the default value
            `choices: ${option.argChoices.map((choice) => JSON.stringify(choice)).join(", ")}`
          );
        }
        if (option.defaultValue !== void 0) {
          const showDefault = option.required || option.optional || option.isBoolean() && typeof option.defaultValue === "boolean";
          if (showDefault) {
            extraInfo.push(
              `default: ${option.defaultValueDescription || JSON.stringify(option.defaultValue)}`
            );
          }
        }
        if (option.presetArg !== void 0 && option.optional) {
          extraInfo.push(`preset: ${JSON.stringify(option.presetArg)}`);
        }
        if (option.envVar !== void 0) {
          extraInfo.push(`env: ${option.envVar}`);
        }
        if (extraInfo.length > 0) {
          return `${option.description} (${extraInfo.join(", ")})`;
        }
        return option.description;
      }
      /**
       * Get the argument description to show in the list of arguments.
       *
       * @param {Argument} argument
       * @return {string}
       */
      argumentDescription(argument) {
        const extraInfo = [];
        if (argument.argChoices) {
          extraInfo.push(
            // use stringify to match the display of the default value
            `choices: ${argument.argChoices.map((choice) => JSON.stringify(choice)).join(", ")}`
          );
        }
        if (argument.defaultValue !== void 0) {
          extraInfo.push(
            `default: ${argument.defaultValueDescription || JSON.stringify(argument.defaultValue)}`
          );
        }
        if (extraInfo.length > 0) {
          const extraDescription = `(${extraInfo.join(", ")})`;
          if (argument.description) {
            return `${argument.description} ${extraDescription}`;
          }
          return extraDescription;
        }
        return argument.description;
      }
      /**
       * Generate the built-in help text.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {string}
       */
      formatHelp(cmd, helper) {
        const termWidth = helper.padWidth(cmd, helper);
        const helpWidth = helper.helpWidth ?? 80;
        function callFormatItem(term, description) {
          return helper.formatItem(term, termWidth, description, helper);
        }
        let output = [
          `${helper.styleTitle("Usage:")} ${helper.styleUsage(helper.commandUsage(cmd))}`,
          ""
        ];
        const commandDescription = helper.commandDescription(cmd);
        if (commandDescription.length > 0) {
          output = output.concat([
            helper.boxWrap(
              helper.styleCommandDescription(commandDescription),
              helpWidth
            ),
            ""
          ]);
        }
        const argumentList = helper.visibleArguments(cmd).map((argument) => {
          return callFormatItem(
            helper.styleArgumentTerm(helper.argumentTerm(argument)),
            helper.styleArgumentDescription(helper.argumentDescription(argument))
          );
        });
        if (argumentList.length > 0) {
          output = output.concat([
            helper.styleTitle("Arguments:"),
            ...argumentList,
            ""
          ]);
        }
        const optionList = helper.visibleOptions(cmd).map((option) => {
          return callFormatItem(
            helper.styleOptionTerm(helper.optionTerm(option)),
            helper.styleOptionDescription(helper.optionDescription(option))
          );
        });
        if (optionList.length > 0) {
          output = output.concat([
            helper.styleTitle("Options:"),
            ...optionList,
            ""
          ]);
        }
        if (helper.showGlobalOptions) {
          const globalOptionList = helper.visibleGlobalOptions(cmd).map((option) => {
            return callFormatItem(
              helper.styleOptionTerm(helper.optionTerm(option)),
              helper.styleOptionDescription(helper.optionDescription(option))
            );
          });
          if (globalOptionList.length > 0) {
            output = output.concat([
              helper.styleTitle("Global Options:"),
              ...globalOptionList,
              ""
            ]);
          }
        }
        const commandList = helper.visibleCommands(cmd).map((cmd2) => {
          return callFormatItem(
            helper.styleSubcommandTerm(helper.subcommandTerm(cmd2)),
            helper.styleSubcommandDescription(helper.subcommandDescription(cmd2))
          );
        });
        if (commandList.length > 0) {
          output = output.concat([
            helper.styleTitle("Commands:"),
            ...commandList,
            ""
          ]);
        }
        return output.join("\n");
      }
      /**
       * Return display width of string, ignoring ANSI escape sequences. Used in padding and wrapping calculations.
       *
       * @param {string} str
       * @returns {number}
       */
      displayWidth(str) {
        return stripColor(str).length;
      }
      /**
       * Style the title for displaying in the help. Called with 'Usage:', 'Options:', etc.
       *
       * @param {string} str
       * @returns {string}
       */
      styleTitle(str) {
        return str;
      }
      styleUsage(str) {
        return str.split(" ").map((word) => {
          if (word === "[options]") return this.styleOptionText(word);
          if (word === "[command]") return this.styleSubcommandText(word);
          if (word[0] === "[" || word[0] === "<")
            return this.styleArgumentText(word);
          return this.styleCommandText(word);
        }).join(" ");
      }
      styleCommandDescription(str) {
        return this.styleDescriptionText(str);
      }
      styleOptionDescription(str) {
        return this.styleDescriptionText(str);
      }
      styleSubcommandDescription(str) {
        return this.styleDescriptionText(str);
      }
      styleArgumentDescription(str) {
        return this.styleDescriptionText(str);
      }
      styleDescriptionText(str) {
        return str;
      }
      styleOptionTerm(str) {
        return this.styleOptionText(str);
      }
      styleSubcommandTerm(str) {
        return str.split(" ").map((word) => {
          if (word === "[options]") return this.styleOptionText(word);
          if (word[0] === "[" || word[0] === "<")
            return this.styleArgumentText(word);
          return this.styleSubcommandText(word);
        }).join(" ");
      }
      styleArgumentTerm(str) {
        return this.styleArgumentText(str);
      }
      styleOptionText(str) {
        return str;
      }
      styleArgumentText(str) {
        return str;
      }
      styleSubcommandText(str) {
        return str;
      }
      styleCommandText(str) {
        return str;
      }
      /**
       * Calculate the pad width from the maximum term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      padWidth(cmd, helper) {
        return Math.max(
          helper.longestOptionTermLength(cmd, helper),
          helper.longestGlobalOptionTermLength(cmd, helper),
          helper.longestSubcommandTermLength(cmd, helper),
          helper.longestArgumentTermLength(cmd, helper)
        );
      }
      /**
       * Detect manually wrapped and indented strings by checking for line break followed by whitespace.
       *
       * @param {string} str
       * @returns {boolean}
       */
      preformatted(str) {
        return /\n[^\S\r\n]/.test(str);
      }
      /**
       * Format the "item", which consists of a term and description. Pad the term and wrap the description, indenting the following lines.
       *
       * So "TTT", 5, "DDD DDDD DD DDD" might be formatted for this.helpWidth=17 like so:
       *   TTT  DDD DDDD
       *        DD DDD
       *
       * @param {string} term
       * @param {number} termWidth
       * @param {string} description
       * @param {Help} helper
       * @returns {string}
       */
      formatItem(term, termWidth, description, helper) {
        const itemIndent = 2;
        const itemIndentStr = " ".repeat(itemIndent);
        if (!description) return itemIndentStr + term;
        const paddedTerm = term.padEnd(
          termWidth + term.length - helper.displayWidth(term)
        );
        const spacerWidth = 2;
        const helpWidth = this.helpWidth ?? 80;
        const remainingWidth = helpWidth - termWidth - spacerWidth - itemIndent;
        let formattedDescription;
        if (remainingWidth < this.minWidthToWrap || helper.preformatted(description)) {
          formattedDescription = description;
        } else {
          const wrappedDescription = helper.boxWrap(description, remainingWidth);
          formattedDescription = wrappedDescription.replace(
            /\n/g,
            "\n" + " ".repeat(termWidth + spacerWidth)
          );
        }
        return itemIndentStr + paddedTerm + " ".repeat(spacerWidth) + formattedDescription.replace(/\n/g, `
${itemIndentStr}`);
      }
      /**
       * Wrap a string at whitespace, preserving existing line breaks.
       * Wrapping is skipped if the width is less than `minWidthToWrap`.
       *
       * @param {string} str
       * @param {number} width
       * @returns {string}
       */
      boxWrap(str, width) {
        if (width < this.minWidthToWrap) return str;
        const rawLines = str.split(/\r\n|\n/);
        const chunkPattern = /[\s]*[^\s]+/g;
        const wrappedLines = [];
        rawLines.forEach((line) => {
          const chunks = line.match(chunkPattern);
          if (chunks === null) {
            wrappedLines.push("");
            return;
          }
          let sumChunks = [chunks.shift()];
          let sumWidth = this.displayWidth(sumChunks[0]);
          chunks.forEach((chunk) => {
            const visibleWidth = this.displayWidth(chunk);
            if (sumWidth + visibleWidth <= width) {
              sumChunks.push(chunk);
              sumWidth += visibleWidth;
              return;
            }
            wrappedLines.push(sumChunks.join(""));
            const nextChunk = chunk.trimStart();
            sumChunks = [nextChunk];
            sumWidth = this.displayWidth(nextChunk);
          });
          wrappedLines.push(sumChunks.join(""));
        });
        return wrappedLines.join("\n");
      }
    };
    function stripColor(str) {
      const sgrPattern = /\x1b\[\d*(;\d*)*m/g;
      return str.replace(sgrPattern, "");
    }
    exports.Help = Help2;
    exports.stripColor = stripColor;
  }
});

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/option.js
var require_option = __commonJS({
  "../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/option.js"(exports) {
    var { InvalidArgumentError: InvalidArgumentError2 } = require_error();
    var Option2 = class {
      /**
       * Initialize a new `Option` with the given `flags` and `description`.
       *
       * @param {string} flags
       * @param {string} [description]
       */
      constructor(flags, description) {
        this.flags = flags;
        this.description = description || "";
        this.required = flags.includes("<");
        this.optional = flags.includes("[");
        this.variadic = /\w\.\.\.[>\]]$/.test(flags);
        this.mandatory = false;
        const optionFlags = splitOptionFlags(flags);
        this.short = optionFlags.shortFlag;
        this.long = optionFlags.longFlag;
        this.negate = false;
        if (this.long) {
          this.negate = this.long.startsWith("--no-");
        }
        this.defaultValue = void 0;
        this.defaultValueDescription = void 0;
        this.presetArg = void 0;
        this.envVar = void 0;
        this.parseArg = void 0;
        this.hidden = false;
        this.argChoices = void 0;
        this.conflictsWith = [];
        this.implied = void 0;
      }
      /**
       * Set the default value, and optionally supply the description to be displayed in the help.
       *
       * @param {*} value
       * @param {string} [description]
       * @return {Option}
       */
      default(value, description) {
        this.defaultValue = value;
        this.defaultValueDescription = description;
        return this;
      }
      /**
       * Preset to use when option used without option-argument, especially optional but also boolean and negated.
       * The custom processing (parseArg) is called.
       *
       * @example
       * new Option('--color').default('GREYSCALE').preset('RGB');
       * new Option('--donate [amount]').preset('20').argParser(parseFloat);
       *
       * @param {*} arg
       * @return {Option}
       */
      preset(arg) {
        this.presetArg = arg;
        return this;
      }
      /**
       * Add option name(s) that conflict with this option.
       * An error will be displayed if conflicting options are found during parsing.
       *
       * @example
       * new Option('--rgb').conflicts('cmyk');
       * new Option('--js').conflicts(['ts', 'jsx']);
       *
       * @param {(string | string[])} names
       * @return {Option}
       */
      conflicts(names) {
        this.conflictsWith = this.conflictsWith.concat(names);
        return this;
      }
      /**
       * Specify implied option values for when this option is set and the implied options are not.
       *
       * The custom processing (parseArg) is not called on the implied values.
       *
       * @example
       * program
       *   .addOption(new Option('--log', 'write logging information to file'))
       *   .addOption(new Option('--trace', 'log extra details').implies({ log: 'trace.txt' }));
       *
       * @param {object} impliedOptionValues
       * @return {Option}
       */
      implies(impliedOptionValues) {
        let newImplied = impliedOptionValues;
        if (typeof impliedOptionValues === "string") {
          newImplied = { [impliedOptionValues]: true };
        }
        this.implied = Object.assign(this.implied || {}, newImplied);
        return this;
      }
      /**
       * Set environment variable to check for option value.
       *
       * An environment variable is only used if when processed the current option value is
       * undefined, or the source of the current value is 'default' or 'config' or 'env'.
       *
       * @param {string} name
       * @return {Option}
       */
      env(name) {
        this.envVar = name;
        return this;
      }
      /**
       * Set the custom handler for processing CLI option arguments into option values.
       *
       * @param {Function} [fn]
       * @return {Option}
       */
      argParser(fn) {
        this.parseArg = fn;
        return this;
      }
      /**
       * Whether the option is mandatory and must have a value after parsing.
       *
       * @param {boolean} [mandatory=true]
       * @return {Option}
       */
      makeOptionMandatory(mandatory = true) {
        this.mandatory = !!mandatory;
        return this;
      }
      /**
       * Hide option in help.
       *
       * @param {boolean} [hide=true]
       * @return {Option}
       */
      hideHelp(hide = true) {
        this.hidden = !!hide;
        return this;
      }
      /**
       * @package
       */
      _concatValue(value, previous) {
        if (previous === this.defaultValue || !Array.isArray(previous)) {
          return [value];
        }
        return previous.concat(value);
      }
      /**
       * Only allow option value to be one of choices.
       *
       * @param {string[]} values
       * @return {Option}
       */
      choices(values) {
        this.argChoices = values.slice();
        this.parseArg = (arg, previous) => {
          if (!this.argChoices.includes(arg)) {
            throw new InvalidArgumentError2(
              `Allowed choices are ${this.argChoices.join(", ")}.`
            );
          }
          if (this.variadic) {
            return this._concatValue(arg, previous);
          }
          return arg;
        };
        return this;
      }
      /**
       * Return option name.
       *
       * @return {string}
       */
      name() {
        if (this.long) {
          return this.long.replace(/^--/, "");
        }
        return this.short.replace(/^-/, "");
      }
      /**
       * Return option name, in a camelcase format that can be used
       * as an object attribute key.
       *
       * @return {string}
       */
      attributeName() {
        if (this.negate) {
          return camelcase(this.name().replace(/^no-/, ""));
        }
        return camelcase(this.name());
      }
      /**
       * Check if `arg` matches the short or long flag.
       *
       * @param {string} arg
       * @return {boolean}
       * @package
       */
      is(arg) {
        return this.short === arg || this.long === arg;
      }
      /**
       * Return whether a boolean option.
       *
       * Options are one of boolean, negated, required argument, or optional argument.
       *
       * @return {boolean}
       * @package
       */
      isBoolean() {
        return !this.required && !this.optional && !this.negate;
      }
    };
    var DualOptions = class {
      /**
       * @param {Option[]} options
       */
      constructor(options) {
        this.positiveOptions = /* @__PURE__ */ new Map();
        this.negativeOptions = /* @__PURE__ */ new Map();
        this.dualOptions = /* @__PURE__ */ new Set();
        options.forEach((option) => {
          if (option.negate) {
            this.negativeOptions.set(option.attributeName(), option);
          } else {
            this.positiveOptions.set(option.attributeName(), option);
          }
        });
        this.negativeOptions.forEach((value, key) => {
          if (this.positiveOptions.has(key)) {
            this.dualOptions.add(key);
          }
        });
      }
      /**
       * Did the value come from the option, and not from possible matching dual option?
       *
       * @param {*} value
       * @param {Option} option
       * @returns {boolean}
       */
      valueFromOption(value, option) {
        const optionKey = option.attributeName();
        if (!this.dualOptions.has(optionKey)) return true;
        const preset = this.negativeOptions.get(optionKey).presetArg;
        const negativeValue = preset !== void 0 ? preset : false;
        return option.negate === (negativeValue === value);
      }
    };
    function camelcase(str) {
      return str.split("-").reduce((str2, word) => {
        return str2 + word[0].toUpperCase() + word.slice(1);
      });
    }
    function splitOptionFlags(flags) {
      let shortFlag;
      let longFlag;
      const shortFlagExp = /^-[^-]$/;
      const longFlagExp = /^--[^-]/;
      const flagParts = flags.split(/[ |,]+/).concat("guard");
      if (shortFlagExp.test(flagParts[0])) shortFlag = flagParts.shift();
      if (longFlagExp.test(flagParts[0])) longFlag = flagParts.shift();
      if (!shortFlag && shortFlagExp.test(flagParts[0]))
        shortFlag = flagParts.shift();
      if (!shortFlag && longFlagExp.test(flagParts[0])) {
        shortFlag = longFlag;
        longFlag = flagParts.shift();
      }
      if (flagParts[0].startsWith("-")) {
        const unsupportedFlag = flagParts[0];
        const baseError = `option creation failed due to '${unsupportedFlag}' in option flags '${flags}'`;
        if (/^-[^-][^-]/.test(unsupportedFlag))
          throw new Error(
            `${baseError}
- a short flag is a single dash and a single character
  - either use a single dash and a single character (for a short flag)
  - or use a double dash for a long option (and can have two, like '--ws, --workspace')`
          );
        if (shortFlagExp.test(unsupportedFlag))
          throw new Error(`${baseError}
- too many short flags`);
        if (longFlagExp.test(unsupportedFlag))
          throw new Error(`${baseError}
- too many long flags`);
        throw new Error(`${baseError}
- unrecognised flag format`);
      }
      if (shortFlag === void 0 && longFlag === void 0)
        throw new Error(
          `option creation failed due to no flags found in '${flags}'.`
        );
      return { shortFlag, longFlag };
    }
    exports.Option = Option2;
    exports.DualOptions = DualOptions;
  }
});

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/suggestSimilar.js
var require_suggestSimilar = __commonJS({
  "../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/suggestSimilar.js"(exports) {
    var maxDistance = 3;
    function editDistance(a, b) {
      if (Math.abs(a.length - b.length) > maxDistance)
        return Math.max(a.length, b.length);
      const d3 = [];
      for (let i = 0; i <= a.length; i++) {
        d3[i] = [i];
      }
      for (let j = 0; j <= b.length; j++) {
        d3[0][j] = j;
      }
      for (let j = 1; j <= b.length; j++) {
        for (let i = 1; i <= a.length; i++) {
          let cost2 = 1;
          if (a[i - 1] === b[j - 1]) {
            cost2 = 0;
          } else {
            cost2 = 1;
          }
          d3[i][j] = Math.min(
            d3[i - 1][j] + 1,
            // deletion
            d3[i][j - 1] + 1,
            // insertion
            d3[i - 1][j - 1] + cost2
            // substitution
          );
          if (i > 1 && j > 1 && a[i - 1] === b[j - 2] && a[i - 2] === b[j - 1]) {
            d3[i][j] = Math.min(d3[i][j], d3[i - 2][j - 2] + 1);
          }
        }
      }
      return d3[a.length][b.length];
    }
    function suggestSimilar(word, candidates) {
      if (!candidates || candidates.length === 0) return "";
      candidates = Array.from(new Set(candidates));
      const searchingOptions = word.startsWith("--");
      if (searchingOptions) {
        word = word.slice(2);
        candidates = candidates.map((candidate) => candidate.slice(2));
      }
      let similar = [];
      let bestDistance = maxDistance;
      const minSimilarity = 0.4;
      candidates.forEach((candidate) => {
        if (candidate.length <= 1) return;
        const distance = editDistance(word, candidate);
        const length = Math.max(word.length, candidate.length);
        const similarity = (length - distance) / length;
        if (similarity > minSimilarity) {
          if (distance < bestDistance) {
            bestDistance = distance;
            similar = [candidate];
          } else if (distance === bestDistance) {
            similar.push(candidate);
          }
        }
      });
      similar.sort((a, b) => a.localeCompare(b));
      if (searchingOptions) {
        similar = similar.map((candidate) => `--${candidate}`);
      }
      if (similar.length > 1) {
        return `
(Did you mean one of ${similar.join(", ")}?)`;
      }
      if (similar.length === 1) {
        return `
(Did you mean ${similar[0]}?)`;
      }
      return "";
    }
    exports.suggestSimilar = suggestSimilar;
  }
});

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/command.js
var require_command = __commonJS({
  "../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/lib/command.js"(exports) {
    var EventEmitter = __require("node:events").EventEmitter;
    var childProcess = __require("node:child_process");
    var path = __require("node:path");
    var fs = __require("node:fs");
    var process2 = __require("node:process");
    var { Argument: Argument2, humanReadableArgName } = require_argument();
    var { CommanderError: CommanderError2 } = require_error();
    var { Help: Help2, stripColor } = require_help();
    var { Option: Option2, DualOptions } = require_option();
    var { suggestSimilar } = require_suggestSimilar();
    var Command2 = class _Command extends EventEmitter {
      /**
       * Initialize a new `Command`.
       *
       * @param {string} [name]
       */
      constructor(name) {
        super();
        this.commands = [];
        this.options = [];
        this.parent = null;
        this._allowUnknownOption = false;
        this._allowExcessArguments = false;
        this.registeredArguments = [];
        this._args = this.registeredArguments;
        this.args = [];
        this.rawArgs = [];
        this.processedArgs = [];
        this._scriptPath = null;
        this._name = name || "";
        this._optionValues = {};
        this._optionValueSources = {};
        this._storeOptionsAsProperties = false;
        this._actionHandler = null;
        this._executableHandler = false;
        this._executableFile = null;
        this._executableDir = null;
        this._defaultCommandName = null;
        this._exitCallback = null;
        this._aliases = [];
        this._combineFlagAndOptionalValue = true;
        this._description = "";
        this._summary = "";
        this._argsDescription = void 0;
        this._enablePositionalOptions = false;
        this._passThroughOptions = false;
        this._lifeCycleHooks = {};
        this._showHelpAfterError = false;
        this._showSuggestionAfterError = true;
        this._savedState = null;
        this._outputConfiguration = {
          writeOut: (str) => process2.stdout.write(str),
          writeErr: (str) => process2.stderr.write(str),
          outputError: (str, write) => write(str),
          getOutHelpWidth: () => process2.stdout.isTTY ? process2.stdout.columns : void 0,
          getErrHelpWidth: () => process2.stderr.isTTY ? process2.stderr.columns : void 0,
          getOutHasColors: () => useColor() ?? (process2.stdout.isTTY && process2.stdout.hasColors?.()),
          getErrHasColors: () => useColor() ?? (process2.stderr.isTTY && process2.stderr.hasColors?.()),
          stripColor: (str) => stripColor(str)
        };
        this._hidden = false;
        this._helpOption = void 0;
        this._addImplicitHelpCommand = void 0;
        this._helpCommand = void 0;
        this._helpConfiguration = {};
      }
      /**
       * Copy settings that are useful to have in common across root command and subcommands.
       *
       * (Used internally when adding a command using `.command()` so subcommands inherit parent settings.)
       *
       * @param {Command} sourceCommand
       * @return {Command} `this` command for chaining
       */
      copyInheritedSettings(sourceCommand) {
        this._outputConfiguration = sourceCommand._outputConfiguration;
        this._helpOption = sourceCommand._helpOption;
        this._helpCommand = sourceCommand._helpCommand;
        this._helpConfiguration = sourceCommand._helpConfiguration;
        this._exitCallback = sourceCommand._exitCallback;
        this._storeOptionsAsProperties = sourceCommand._storeOptionsAsProperties;
        this._combineFlagAndOptionalValue = sourceCommand._combineFlagAndOptionalValue;
        this._allowExcessArguments = sourceCommand._allowExcessArguments;
        this._enablePositionalOptions = sourceCommand._enablePositionalOptions;
        this._showHelpAfterError = sourceCommand._showHelpAfterError;
        this._showSuggestionAfterError = sourceCommand._showSuggestionAfterError;
        return this;
      }
      /**
       * @returns {Command[]}
       * @private
       */
      _getCommandAndAncestors() {
        const result = [];
        for (let command = this; command; command = command.parent) {
          result.push(command);
        }
        return result;
      }
      /**
       * Define a command.
       *
       * There are two styles of command: pay attention to where to put the description.
       *
       * @example
       * // Command implemented using action handler (description is supplied separately to `.command`)
       * program
       *   .command('clone <source> [destination]')
       *   .description('clone a repository into a newly created directory')
       *   .action((source, destination) => {
       *     console.log('clone command called');
       *   });
       *
       * // Command implemented using separate executable file (description is second parameter to `.command`)
       * program
       *   .command('start <service>', 'start named service')
       *   .command('stop [service]', 'stop named service, or all if no name supplied');
       *
       * @param {string} nameAndArgs - command name and arguments, args are `<required>` or `[optional]` and last may also be `variadic...`
       * @param {(object | string)} [actionOptsOrExecDesc] - configuration options (for action), or description (for executable)
       * @param {object} [execOpts] - configuration options (for executable)
       * @return {Command} returns new command for action handler, or `this` for executable command
       */
      command(nameAndArgs, actionOptsOrExecDesc, execOpts) {
        let desc = actionOptsOrExecDesc;
        let opts = execOpts;
        if (typeof desc === "object" && desc !== null) {
          opts = desc;
          desc = null;
        }
        opts = opts || {};
        const [, name, args] = nameAndArgs.match(/([^ ]+) *(.*)/);
        const cmd = this.createCommand(name);
        if (desc) {
          cmd.description(desc);
          cmd._executableHandler = true;
        }
        if (opts.isDefault) this._defaultCommandName = cmd._name;
        cmd._hidden = !!(opts.noHelp || opts.hidden);
        cmd._executableFile = opts.executableFile || null;
        if (args) cmd.arguments(args);
        this._registerCommand(cmd);
        cmd.parent = this;
        cmd.copyInheritedSettings(this);
        if (desc) return this;
        return cmd;
      }
      /**
       * Factory routine to create a new unattached command.
       *
       * See .command() for creating an attached subcommand, which uses this routine to
       * create the command. You can override createCommand to customise subcommands.
       *
       * @param {string} [name]
       * @return {Command} new command
       */
      createCommand(name) {
        return new _Command(name);
      }
      /**
       * You can customise the help with a subclass of Help by overriding createHelp,
       * or by overriding Help properties using configureHelp().
       *
       * @return {Help}
       */
      createHelp() {
        return Object.assign(new Help2(), this.configureHelp());
      }
      /**
       * You can customise the help by overriding Help properties using configureHelp(),
       * or with a subclass of Help by overriding createHelp().
       *
       * @param {object} [configuration] - configuration options
       * @return {(Command | object)} `this` command for chaining, or stored configuration
       */
      configureHelp(configuration) {
        if (configuration === void 0) return this._helpConfiguration;
        this._helpConfiguration = configuration;
        return this;
      }
      /**
       * The default output goes to stdout and stderr. You can customise this for special
       * applications. You can also customise the display of errors by overriding outputError.
       *
       * The configuration properties are all functions:
       *
       *     // change how output being written, defaults to stdout and stderr
       *     writeOut(str)
       *     writeErr(str)
       *     // change how output being written for errors, defaults to writeErr
       *     outputError(str, write) // used for displaying errors and not used for displaying help
       *     // specify width for wrapping help
       *     getOutHelpWidth()
       *     getErrHelpWidth()
       *     // color support, currently only used with Help
       *     getOutHasColors()
       *     getErrHasColors()
       *     stripColor() // used to remove ANSI escape codes if output does not have colors
       *
       * @param {object} [configuration] - configuration options
       * @return {(Command | object)} `this` command for chaining, or stored configuration
       */
      configureOutput(configuration) {
        if (configuration === void 0) return this._outputConfiguration;
        Object.assign(this._outputConfiguration, configuration);
        return this;
      }
      /**
       * Display the help or a custom message after an error occurs.
       *
       * @param {(boolean|string)} [displayHelp]
       * @return {Command} `this` command for chaining
       */
      showHelpAfterError(displayHelp = true) {
        if (typeof displayHelp !== "string") displayHelp = !!displayHelp;
        this._showHelpAfterError = displayHelp;
        return this;
      }
      /**
       * Display suggestion of similar commands for unknown commands, or options for unknown options.
       *
       * @param {boolean} [displaySuggestion]
       * @return {Command} `this` command for chaining
       */
      showSuggestionAfterError(displaySuggestion = true) {
        this._showSuggestionAfterError = !!displaySuggestion;
        return this;
      }
      /**
       * Add a prepared subcommand.
       *
       * See .command() for creating an attached subcommand which inherits settings from its parent.
       *
       * @param {Command} cmd - new subcommand
       * @param {object} [opts] - configuration options
       * @return {Command} `this` command for chaining
       */
      addCommand(cmd, opts) {
        if (!cmd._name) {
          throw new Error(`Command passed to .addCommand() must have a name
- specify the name in Command constructor or using .name()`);
        }
        opts = opts || {};
        if (opts.isDefault) this._defaultCommandName = cmd._name;
        if (opts.noHelp || opts.hidden) cmd._hidden = true;
        this._registerCommand(cmd);
        cmd.parent = this;
        cmd._checkForBrokenPassThrough();
        return this;
      }
      /**
       * Factory routine to create a new unattached argument.
       *
       * See .argument() for creating an attached argument, which uses this routine to
       * create the argument. You can override createArgument to return a custom argument.
       *
       * @param {string} name
       * @param {string} [description]
       * @return {Argument} new argument
       */
      createArgument(name, description) {
        return new Argument2(name, description);
      }
      /**
       * Define argument syntax for command.
       *
       * The default is that the argument is required, and you can explicitly
       * indicate this with <> around the name. Put [] around the name for an optional argument.
       *
       * @example
       * program.argument('<input-file>');
       * program.argument('[output-file]');
       *
       * @param {string} name
       * @param {string} [description]
       * @param {(Function|*)} [fn] - custom argument processing function
       * @param {*} [defaultValue]
       * @return {Command} `this` command for chaining
       */
      argument(name, description, fn, defaultValue) {
        const argument = this.createArgument(name, description);
        if (typeof fn === "function") {
          argument.default(defaultValue).argParser(fn);
        } else {
          argument.default(fn);
        }
        this.addArgument(argument);
        return this;
      }
      /**
       * Define argument syntax for command, adding multiple at once (without descriptions).
       *
       * See also .argument().
       *
       * @example
       * program.arguments('<cmd> [env]');
       *
       * @param {string} names
       * @return {Command} `this` command for chaining
       */
      arguments(names) {
        names.trim().split(/ +/).forEach((detail) => {
          this.argument(detail);
        });
        return this;
      }
      /**
       * Define argument syntax for command, adding a prepared argument.
       *
       * @param {Argument} argument
       * @return {Command} `this` command for chaining
       */
      addArgument(argument) {
        const previousArgument = this.registeredArguments.slice(-1)[0];
        if (previousArgument && previousArgument.variadic) {
          throw new Error(
            `only the last argument can be variadic '${previousArgument.name()}'`
          );
        }
        if (argument.required && argument.defaultValue !== void 0 && argument.parseArg === void 0) {
          throw new Error(
            `a default value for a required argument is never used: '${argument.name()}'`
          );
        }
        this.registeredArguments.push(argument);
        return this;
      }
      /**
       * Customise or override default help command. By default a help command is automatically added if your command has subcommands.
       *
       * @example
       *    program.helpCommand('help [cmd]');
       *    program.helpCommand('help [cmd]', 'show help');
       *    program.helpCommand(false); // suppress default help command
       *    program.helpCommand(true); // add help command even if no subcommands
       *
       * @param {string|boolean} enableOrNameAndArgs - enable with custom name and/or arguments, or boolean to override whether added
       * @param {string} [description] - custom description
       * @return {Command} `this` command for chaining
       */
      helpCommand(enableOrNameAndArgs, description) {
        if (typeof enableOrNameAndArgs === "boolean") {
          this._addImplicitHelpCommand = enableOrNameAndArgs;
          return this;
        }
        enableOrNameAndArgs = enableOrNameAndArgs ?? "help [command]";
        const [, helpName, helpArgs] = enableOrNameAndArgs.match(/([^ ]+) *(.*)/);
        const helpDescription = description ?? "display help for command";
        const helpCommand = this.createCommand(helpName);
        helpCommand.helpOption(false);
        if (helpArgs) helpCommand.arguments(helpArgs);
        if (helpDescription) helpCommand.description(helpDescription);
        this._addImplicitHelpCommand = true;
        this._helpCommand = helpCommand;
        return this;
      }
      /**
       * Add prepared custom help command.
       *
       * @param {(Command|string|boolean)} helpCommand - custom help command, or deprecated enableOrNameAndArgs as for `.helpCommand()`
       * @param {string} [deprecatedDescription] - deprecated custom description used with custom name only
       * @return {Command} `this` command for chaining
       */
      addHelpCommand(helpCommand, deprecatedDescription) {
        if (typeof helpCommand !== "object") {
          this.helpCommand(helpCommand, deprecatedDescription);
          return this;
        }
        this._addImplicitHelpCommand = true;
        this._helpCommand = helpCommand;
        return this;
      }
      /**
       * Lazy create help command.
       *
       * @return {(Command|null)}
       * @package
       */
      _getHelpCommand() {
        const hasImplicitHelpCommand = this._addImplicitHelpCommand ?? (this.commands.length && !this._actionHandler && !this._findCommand("help"));
        if (hasImplicitHelpCommand) {
          if (this._helpCommand === void 0) {
            this.helpCommand(void 0, void 0);
          }
          return this._helpCommand;
        }
        return null;
      }
      /**
       * Add hook for life cycle event.
       *
       * @param {string} event
       * @param {Function} listener
       * @return {Command} `this` command for chaining
       */
      hook(event, listener) {
        const allowedValues = ["preSubcommand", "preAction", "postAction"];
        if (!allowedValues.includes(event)) {
          throw new Error(`Unexpected value for event passed to hook : '${event}'.
Expecting one of '${allowedValues.join("', '")}'`);
        }
        if (this._lifeCycleHooks[event]) {
          this._lifeCycleHooks[event].push(listener);
        } else {
          this._lifeCycleHooks[event] = [listener];
        }
        return this;
      }
      /**
       * Register callback to use as replacement for calling process.exit.
       *
       * @param {Function} [fn] optional callback which will be passed a CommanderError, defaults to throwing
       * @return {Command} `this` command for chaining
       */
      exitOverride(fn) {
        if (fn) {
          this._exitCallback = fn;
        } else {
          this._exitCallback = (err) => {
            if (err.code !== "commander.executeSubCommandAsync") {
              throw err;
            } else {
            }
          };
        }
        return this;
      }
      /**
       * Call process.exit, and _exitCallback if defined.
       *
       * @param {number} exitCode exit code for using with process.exit
       * @param {string} code an id string representing the error
       * @param {string} message human-readable description of the error
       * @return never
       * @private
       */
      _exit(exitCode, code, message) {
        if (this._exitCallback) {
          this._exitCallback(new CommanderError2(exitCode, code, message));
        }
        process2.exit(exitCode);
      }
      /**
       * Register callback `fn` for the command.
       *
       * @example
       * program
       *   .command('serve')
       *   .description('start service')
       *   .action(function() {
       *      // do work here
       *   });
       *
       * @param {Function} fn
       * @return {Command} `this` command for chaining
       */
      action(fn) {
        const listener = (args) => {
          const expectedArgsCount = this.registeredArguments.length;
          const actionArgs = args.slice(0, expectedArgsCount);
          if (this._storeOptionsAsProperties) {
            actionArgs[expectedArgsCount] = this;
          } else {
            actionArgs[expectedArgsCount] = this.opts();
          }
          actionArgs.push(this);
          return fn.apply(this, actionArgs);
        };
        this._actionHandler = listener;
        return this;
      }
      /**
       * Factory routine to create a new unattached option.
       *
       * See .option() for creating an attached option, which uses this routine to
       * create the option. You can override createOption to return a custom option.
       *
       * @param {string} flags
       * @param {string} [description]
       * @return {Option} new option
       */
      createOption(flags, description) {
        return new Option2(flags, description);
      }
      /**
       * Wrap parseArgs to catch 'commander.invalidArgument'.
       *
       * @param {(Option | Argument)} target
       * @param {string} value
       * @param {*} previous
       * @param {string} invalidArgumentMessage
       * @private
       */
      _callParseArg(target, value, previous, invalidArgumentMessage) {
        try {
          return target.parseArg(value, previous);
        } catch (err) {
          if (err.code === "commander.invalidArgument") {
            const message = `${invalidArgumentMessage} ${err.message}`;
            this.error(message, { exitCode: err.exitCode, code: err.code });
          }
          throw err;
        }
      }
      /**
       * Check for option flag conflicts.
       * Register option if no conflicts found, or throw on conflict.
       *
       * @param {Option} option
       * @private
       */
      _registerOption(option) {
        const matchingOption = option.short && this._findOption(option.short) || option.long && this._findOption(option.long);
        if (matchingOption) {
          const matchingFlag = option.long && this._findOption(option.long) ? option.long : option.short;
          throw new Error(`Cannot add option '${option.flags}'${this._name && ` to command '${this._name}'`} due to conflicting flag '${matchingFlag}'
-  already used by option '${matchingOption.flags}'`);
        }
        this.options.push(option);
      }
      /**
       * Check for command name and alias conflicts with existing commands.
       * Register command if no conflicts found, or throw on conflict.
       *
       * @param {Command} command
       * @private
       */
      _registerCommand(command) {
        const knownBy = (cmd) => {
          return [cmd.name()].concat(cmd.aliases());
        };
        const alreadyUsed = knownBy(command).find(
          (name) => this._findCommand(name)
        );
        if (alreadyUsed) {
          const existingCmd = knownBy(this._findCommand(alreadyUsed)).join("|");
          const newCmd = knownBy(command).join("|");
          throw new Error(
            `cannot add command '${newCmd}' as already have command '${existingCmd}'`
          );
        }
        this.commands.push(command);
      }
      /**
       * Add an option.
       *
       * @param {Option} option
       * @return {Command} `this` command for chaining
       */
      addOption(option) {
        this._registerOption(option);
        const oname = option.name();
        const name = option.attributeName();
        if (option.negate) {
          const positiveLongFlag = option.long.replace(/^--no-/, "--");
          if (!this._findOption(positiveLongFlag)) {
            this.setOptionValueWithSource(
              name,
              option.defaultValue === void 0 ? true : option.defaultValue,
              "default"
            );
          }
        } else if (option.defaultValue !== void 0) {
          this.setOptionValueWithSource(name, option.defaultValue, "default");
        }
        const handleOptionValue = (val, invalidValueMessage, valueSource) => {
          if (val == null && option.presetArg !== void 0) {
            val = option.presetArg;
          }
          const oldValue = this.getOptionValue(name);
          if (val !== null && option.parseArg) {
            val = this._callParseArg(option, val, oldValue, invalidValueMessage);
          } else if (val !== null && option.variadic) {
            val = option._concatValue(val, oldValue);
          }
          if (val == null) {
            if (option.negate) {
              val = false;
            } else if (option.isBoolean() || option.optional) {
              val = true;
            } else {
              val = "";
            }
          }
          this.setOptionValueWithSource(name, val, valueSource);
        };
        this.on("option:" + oname, (val) => {
          const invalidValueMessage = `error: option '${option.flags}' argument '${val}' is invalid.`;
          handleOptionValue(val, invalidValueMessage, "cli");
        });
        if (option.envVar) {
          this.on("optionEnv:" + oname, (val) => {
            const invalidValueMessage = `error: option '${option.flags}' value '${val}' from env '${option.envVar}' is invalid.`;
            handleOptionValue(val, invalidValueMessage, "env");
          });
        }
        return this;
      }
      /**
       * Internal implementation shared by .option() and .requiredOption()
       *
       * @return {Command} `this` command for chaining
       * @private
       */
      _optionEx(config, flags, description, fn, defaultValue) {
        if (typeof flags === "object" && flags instanceof Option2) {
          throw new Error(
            "To add an Option object use addOption() instead of option() or requiredOption()"
          );
        }
        const option = this.createOption(flags, description);
        option.makeOptionMandatory(!!config.mandatory);
        if (typeof fn === "function") {
          option.default(defaultValue).argParser(fn);
        } else if (fn instanceof RegExp) {
          const regex = fn;
          fn = (val, def) => {
            const m = regex.exec(val);
            return m ? m[0] : def;
          };
          option.default(defaultValue).argParser(fn);
        } else {
          option.default(fn);
        }
        return this.addOption(option);
      }
      /**
       * Define option with `flags`, `description`, and optional argument parsing function or `defaultValue` or both.
       *
       * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space. A required
       * option-argument is indicated by `<>` and an optional option-argument by `[]`.
       *
       * See the README for more details, and see also addOption() and requiredOption().
       *
       * @example
       * program
       *     .option('-p, --pepper', 'add pepper')
       *     .option('--pt, --pizza-type <TYPE>', 'type of pizza') // required option-argument
       *     .option('-c, --cheese [CHEESE]', 'add extra cheese', 'mozzarella') // optional option-argument with default
       *     .option('-t, --tip <VALUE>', 'add tip to purchase cost', parseFloat) // custom parse function
       *
       * @param {string} flags
       * @param {string} [description]
       * @param {(Function|*)} [parseArg] - custom option processing function or default value
       * @param {*} [defaultValue]
       * @return {Command} `this` command for chaining
       */
      option(flags, description, parseArg, defaultValue) {
        return this._optionEx({}, flags, description, parseArg, defaultValue);
      }
      /**
       * Add a required option which must have a value after parsing. This usually means
       * the option must be specified on the command line. (Otherwise the same as .option().)
       *
       * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space.
       *
       * @param {string} flags
       * @param {string} [description]
       * @param {(Function|*)} [parseArg] - custom option processing function or default value
       * @param {*} [defaultValue]
       * @return {Command} `this` command for chaining
       */
      requiredOption(flags, description, parseArg, defaultValue) {
        return this._optionEx(
          { mandatory: true },
          flags,
          description,
          parseArg,
          defaultValue
        );
      }
      /**
       * Alter parsing of short flags with optional values.
       *
       * @example
       * // for `.option('-f,--flag [value]'):
       * program.combineFlagAndOptionalValue(true);  // `-f80` is treated like `--flag=80`, this is the default behaviour
       * program.combineFlagAndOptionalValue(false) // `-fb` is treated like `-f -b`
       *
       * @param {boolean} [combine] - if `true` or omitted, an optional value can be specified directly after the flag.
       * @return {Command} `this` command for chaining
       */
      combineFlagAndOptionalValue(combine = true) {
        this._combineFlagAndOptionalValue = !!combine;
        return this;
      }
      /**
       * Allow unknown options on the command line.
       *
       * @param {boolean} [allowUnknown] - if `true` or omitted, no error will be thrown for unknown options.
       * @return {Command} `this` command for chaining
       */
      allowUnknownOption(allowUnknown = true) {
        this._allowUnknownOption = !!allowUnknown;
        return this;
      }
      /**
       * Allow excess command-arguments on the command line. Pass false to make excess arguments an error.
       *
       * @param {boolean} [allowExcess] - if `true` or omitted, no error will be thrown for excess arguments.
       * @return {Command} `this` command for chaining
       */
      allowExcessArguments(allowExcess = true) {
        this._allowExcessArguments = !!allowExcess;
        return this;
      }
      /**
       * Enable positional options. Positional means global options are specified before subcommands which lets
       * subcommands reuse the same option names, and also enables subcommands to turn on passThroughOptions.
       * The default behaviour is non-positional and global options may appear anywhere on the command line.
       *
       * @param {boolean} [positional]
       * @return {Command} `this` command for chaining
       */
      enablePositionalOptions(positional = true) {
        this._enablePositionalOptions = !!positional;
        return this;
      }
      /**
       * Pass through options that come after command-arguments rather than treat them as command-options,
       * so actual command-options come before command-arguments. Turning this on for a subcommand requires
       * positional options to have been enabled on the program (parent commands).
       * The default behaviour is non-positional and options may appear before or after command-arguments.
       *
       * @param {boolean} [passThrough] for unknown options.
       * @return {Command} `this` command for chaining
       */
      passThroughOptions(passThrough = true) {
        this._passThroughOptions = !!passThrough;
        this._checkForBrokenPassThrough();
        return this;
      }
      /**
       * @private
       */
      _checkForBrokenPassThrough() {
        if (this.parent && this._passThroughOptions && !this.parent._enablePositionalOptions) {
          throw new Error(
            `passThroughOptions cannot be used for '${this._name}' without turning on enablePositionalOptions for parent command(s)`
          );
        }
      }
      /**
       * Whether to store option values as properties on command object,
       * or store separately (specify false). In both cases the option values can be accessed using .opts().
       *
       * @param {boolean} [storeAsProperties=true]
       * @return {Command} `this` command for chaining
       */
      storeOptionsAsProperties(storeAsProperties = true) {
        if (this.options.length) {
          throw new Error("call .storeOptionsAsProperties() before adding options");
        }
        if (Object.keys(this._optionValues).length) {
          throw new Error(
            "call .storeOptionsAsProperties() before setting option values"
          );
        }
        this._storeOptionsAsProperties = !!storeAsProperties;
        return this;
      }
      /**
       * Retrieve option value.
       *
       * @param {string} key
       * @return {object} value
       */
      getOptionValue(key) {
        if (this._storeOptionsAsProperties) {
          return this[key];
        }
        return this._optionValues[key];
      }
      /**
       * Store option value.
       *
       * @param {string} key
       * @param {object} value
       * @return {Command} `this` command for chaining
       */
      setOptionValue(key, value) {
        return this.setOptionValueWithSource(key, value, void 0);
      }
      /**
       * Store option value and where the value came from.
       *
       * @param {string} key
       * @param {object} value
       * @param {string} source - expected values are default/config/env/cli/implied
       * @return {Command} `this` command for chaining
       */
      setOptionValueWithSource(key, value, source) {
        if (this._storeOptionsAsProperties) {
          this[key] = value;
        } else {
          this._optionValues[key] = value;
        }
        this._optionValueSources[key] = source;
        return this;
      }
      /**
       * Get source of option value.
       * Expected values are default | config | env | cli | implied
       *
       * @param {string} key
       * @return {string}
       */
      getOptionValueSource(key) {
        return this._optionValueSources[key];
      }
      /**
       * Get source of option value. See also .optsWithGlobals().
       * Expected values are default | config | env | cli | implied
       *
       * @param {string} key
       * @return {string}
       */
      getOptionValueSourceWithGlobals(key) {
        let source;
        this._getCommandAndAncestors().forEach((cmd) => {
          if (cmd.getOptionValueSource(key) !== void 0) {
            source = cmd.getOptionValueSource(key);
          }
        });
        return source;
      }
      /**
       * Get user arguments from implied or explicit arguments.
       * Side-effects: set _scriptPath if args included script. Used for default program name, and subcommand searches.
       *
       * @private
       */
      _prepareUserArgs(argv, parseOptions) {
        if (argv !== void 0 && !Array.isArray(argv)) {
          throw new Error("first parameter to parse must be array or undefined");
        }
        parseOptions = parseOptions || {};
        if (argv === void 0 && parseOptions.from === void 0) {
          if (process2.versions?.electron) {
            parseOptions.from = "electron";
          }
          const execArgv = process2.execArgv ?? [];
          if (execArgv.includes("-e") || execArgv.includes("--eval") || execArgv.includes("-p") || execArgv.includes("--print")) {
            parseOptions.from = "eval";
          }
        }
        if (argv === void 0) {
          argv = process2.argv;
        }
        this.rawArgs = argv.slice();
        let userArgs;
        switch (parseOptions.from) {
          case void 0:
          case "node":
            this._scriptPath = argv[1];
            userArgs = argv.slice(2);
            break;
          case "electron":
            if (process2.defaultApp) {
              this._scriptPath = argv[1];
              userArgs = argv.slice(2);
            } else {
              userArgs = argv.slice(1);
            }
            break;
          case "user":
            userArgs = argv.slice(0);
            break;
          case "eval":
            userArgs = argv.slice(1);
            break;
          default:
            throw new Error(
              `unexpected parse option { from: '${parseOptions.from}' }`
            );
        }
        if (!this._name && this._scriptPath)
          this.nameFromFilename(this._scriptPath);
        this._name = this._name || "program";
        return userArgs;
      }
      /**
       * Parse `argv`, setting options and invoking commands when defined.
       *
       * Use parseAsync instead of parse if any of your action handlers are async.
       *
       * Call with no parameters to parse `process.argv`. Detects Electron and special node options like `node --eval`. Easy mode!
       *
       * Or call with an array of strings to parse, and optionally where the user arguments start by specifying where the arguments are `from`:
       * - `'node'`: default, `argv[0]` is the application and `argv[1]` is the script being run, with user arguments after that
       * - `'electron'`: `argv[0]` is the application and `argv[1]` varies depending on whether the electron application is packaged
       * - `'user'`: just user arguments
       *
       * @example
       * program.parse(); // parse process.argv and auto-detect electron and special node flags
       * program.parse(process.argv); // assume argv[0] is app and argv[1] is script
       * program.parse(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
       *
       * @param {string[]} [argv] - optional, defaults to process.argv
       * @param {object} [parseOptions] - optionally specify style of options with from: node/user/electron
       * @param {string} [parseOptions.from] - where the args are from: 'node', 'user', 'electron'
       * @return {Command} `this` command for chaining
       */
      parse(argv, parseOptions) {
        this._prepareForParse();
        const userArgs = this._prepareUserArgs(argv, parseOptions);
        this._parseCommand([], userArgs);
        return this;
      }
      /**
       * Parse `argv`, setting options and invoking commands when defined.
       *
       * Call with no parameters to parse `process.argv`. Detects Electron and special node options like `node --eval`. Easy mode!
       *
       * Or call with an array of strings to parse, and optionally where the user arguments start by specifying where the arguments are `from`:
       * - `'node'`: default, `argv[0]` is the application and `argv[1]` is the script being run, with user arguments after that
       * - `'electron'`: `argv[0]` is the application and `argv[1]` varies depending on whether the electron application is packaged
       * - `'user'`: just user arguments
       *
       * @example
       * await program.parseAsync(); // parse process.argv and auto-detect electron and special node flags
       * await program.parseAsync(process.argv); // assume argv[0] is app and argv[1] is script
       * await program.parseAsync(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
       *
       * @param {string[]} [argv]
       * @param {object} [parseOptions]
       * @param {string} parseOptions.from - where the args are from: 'node', 'user', 'electron'
       * @return {Promise}
       */
      async parseAsync(argv, parseOptions) {
        this._prepareForParse();
        const userArgs = this._prepareUserArgs(argv, parseOptions);
        await this._parseCommand([], userArgs);
        return this;
      }
      _prepareForParse() {
        if (this._savedState === null) {
          this.saveStateBeforeParse();
        } else {
          this.restoreStateBeforeParse();
        }
      }
      /**
       * Called the first time parse is called to save state and allow a restore before subsequent calls to parse.
       * Not usually called directly, but available for subclasses to save their custom state.
       *
       * This is called in a lazy way. Only commands used in parsing chain will have state saved.
       */
      saveStateBeforeParse() {
        this._savedState = {
          // name is stable if supplied by author, but may be unspecified for root command and deduced during parsing
          _name: this._name,
          // option values before parse have default values (including false for negated options)
          // shallow clones
          _optionValues: { ...this._optionValues },
          _optionValueSources: { ...this._optionValueSources }
        };
      }
      /**
       * Restore state before parse for calls after the first.
       * Not usually called directly, but available for subclasses to save their custom state.
       *
       * This is called in a lazy way. Only commands used in parsing chain will have state restored.
       */
      restoreStateBeforeParse() {
        if (this._storeOptionsAsProperties)
          throw new Error(`Can not call parse again when storeOptionsAsProperties is true.
- either make a new Command for each call to parse, or stop storing options as properties`);
        this._name = this._savedState._name;
        this._scriptPath = null;
        this.rawArgs = [];
        this._optionValues = { ...this._savedState._optionValues };
        this._optionValueSources = { ...this._savedState._optionValueSources };
        this.args = [];
        this.processedArgs = [];
      }
      /**
       * Throw if expected executable is missing. Add lots of help for author.
       *
       * @param {string} executableFile
       * @param {string} executableDir
       * @param {string} subcommandName
       */
      _checkForMissingExecutable(executableFile, executableDir, subcommandName) {
        if (fs.existsSync(executableFile)) return;
        const executableDirMessage = executableDir ? `searched for local subcommand relative to directory '${executableDir}'` : "no directory for search for local subcommand, use .executableDir() to supply a custom directory";
        const executableMissing = `'${executableFile}' does not exist
 - if '${subcommandName}' is not meant to be an executable command, remove description parameter from '.command()' and use '.description()' instead
 - if the default executable name is not suitable, use the executableFile option to supply a custom name or path
 - ${executableDirMessage}`;
        throw new Error(executableMissing);
      }
      /**
       * Execute a sub-command executable.
       *
       * @private
       */
      _executeSubCommand(subcommand, args) {
        args = args.slice();
        let launchWithNode = false;
        const sourceExt = [".js", ".ts", ".tsx", ".mjs", ".cjs"];
        function findFile(baseDir, baseName) {
          const localBin = path.resolve(baseDir, baseName);
          if (fs.existsSync(localBin)) return localBin;
          if (sourceExt.includes(path.extname(baseName))) return void 0;
          const foundExt = sourceExt.find(
            (ext) => fs.existsSync(`${localBin}${ext}`)
          );
          if (foundExt) return `${localBin}${foundExt}`;
          return void 0;
        }
        this._checkForMissingMandatoryOptions();
        this._checkForConflictingOptions();
        let executableFile = subcommand._executableFile || `${this._name}-${subcommand._name}`;
        let executableDir = this._executableDir || "";
        if (this._scriptPath) {
          let resolvedScriptPath;
          try {
            resolvedScriptPath = fs.realpathSync(this._scriptPath);
          } catch {
            resolvedScriptPath = this._scriptPath;
          }
          executableDir = path.resolve(
            path.dirname(resolvedScriptPath),
            executableDir
          );
        }
        if (executableDir) {
          let localFile = findFile(executableDir, executableFile);
          if (!localFile && !subcommand._executableFile && this._scriptPath) {
            const legacyName = path.basename(
              this._scriptPath,
              path.extname(this._scriptPath)
            );
            if (legacyName !== this._name) {
              localFile = findFile(
                executableDir,
                `${legacyName}-${subcommand._name}`
              );
            }
          }
          executableFile = localFile || executableFile;
        }
        launchWithNode = sourceExt.includes(path.extname(executableFile));
        let proc;
        if (process2.platform !== "win32") {
          if (launchWithNode) {
            args.unshift(executableFile);
            args = incrementNodeInspectorPort(process2.execArgv).concat(args);
            proc = childProcess.spawn(process2.argv[0], args, { stdio: "inherit" });
          } else {
            proc = childProcess.spawn(executableFile, args, { stdio: "inherit" });
          }
        } else {
          this._checkForMissingExecutable(
            executableFile,
            executableDir,
            subcommand._name
          );
          args.unshift(executableFile);
          args = incrementNodeInspectorPort(process2.execArgv).concat(args);
          proc = childProcess.spawn(process2.execPath, args, { stdio: "inherit" });
        }
        if (!proc.killed) {
          const signals = ["SIGUSR1", "SIGUSR2", "SIGTERM", "SIGINT", "SIGHUP"];
          signals.forEach((signal) => {
            process2.on(signal, () => {
              if (proc.killed === false && proc.exitCode === null) {
                proc.kill(signal);
              }
            });
          });
        }
        const exitCallback = this._exitCallback;
        proc.on("close", (code) => {
          code = code ?? 1;
          if (!exitCallback) {
            process2.exit(code);
          } else {
            exitCallback(
              new CommanderError2(
                code,
                "commander.executeSubCommandAsync",
                "(close)"
              )
            );
          }
        });
        proc.on("error", (err) => {
          if (err.code === "ENOENT") {
            this._checkForMissingExecutable(
              executableFile,
              executableDir,
              subcommand._name
            );
          } else if (err.code === "EACCES") {
            throw new Error(`'${executableFile}' not executable`);
          }
          if (!exitCallback) {
            process2.exit(1);
          } else {
            const wrappedError = new CommanderError2(
              1,
              "commander.executeSubCommandAsync",
              "(error)"
            );
            wrappedError.nestedError = err;
            exitCallback(wrappedError);
          }
        });
        this.runningCommand = proc;
      }
      /**
       * @private
       */
      _dispatchSubcommand(commandName, operands, unknown) {
        const subCommand = this._findCommand(commandName);
        if (!subCommand) this.help({ error: true });
        subCommand._prepareForParse();
        let promiseChain;
        promiseChain = this._chainOrCallSubCommandHook(
          promiseChain,
          subCommand,
          "preSubcommand"
        );
        promiseChain = this._chainOrCall(promiseChain, () => {
          if (subCommand._executableHandler) {
            this._executeSubCommand(subCommand, operands.concat(unknown));
          } else {
            return subCommand._parseCommand(operands, unknown);
          }
        });
        return promiseChain;
      }
      /**
       * Invoke help directly if possible, or dispatch if necessary.
       * e.g. help foo
       *
       * @private
       */
      _dispatchHelpCommand(subcommandName) {
        if (!subcommandName) {
          this.help();
        }
        const subCommand = this._findCommand(subcommandName);
        if (subCommand && !subCommand._executableHandler) {
          subCommand.help();
        }
        return this._dispatchSubcommand(
          subcommandName,
          [],
          [this._getHelpOption()?.long ?? this._getHelpOption()?.short ?? "--help"]
        );
      }
      /**
       * Check this.args against expected this.registeredArguments.
       *
       * @private
       */
      _checkNumberOfArguments() {
        this.registeredArguments.forEach((arg, i) => {
          if (arg.required && this.args[i] == null) {
            this.missingArgument(arg.name());
          }
        });
        if (this.registeredArguments.length > 0 && this.registeredArguments[this.registeredArguments.length - 1].variadic) {
          return;
        }
        if (this.args.length > this.registeredArguments.length) {
          this._excessArguments(this.args);
        }
      }
      /**
       * Process this.args using this.registeredArguments and save as this.processedArgs!
       *
       * @private
       */
      _processArguments() {
        const myParseArg = (argument, value, previous) => {
          let parsedValue = value;
          if (value !== null && argument.parseArg) {
            const invalidValueMessage = `error: command-argument value '${value}' is invalid for argument '${argument.name()}'.`;
            parsedValue = this._callParseArg(
              argument,
              value,
              previous,
              invalidValueMessage
            );
          }
          return parsedValue;
        };
        this._checkNumberOfArguments();
        const processedArgs = [];
        this.registeredArguments.forEach((declaredArg, index) => {
          let value = declaredArg.defaultValue;
          if (declaredArg.variadic) {
            if (index < this.args.length) {
              value = this.args.slice(index);
              if (declaredArg.parseArg) {
                value = value.reduce((processed, v) => {
                  return myParseArg(declaredArg, v, processed);
                }, declaredArg.defaultValue);
              }
            } else if (value === void 0) {
              value = [];
            }
          } else if (index < this.args.length) {
            value = this.args[index];
            if (declaredArg.parseArg) {
              value = myParseArg(declaredArg, value, declaredArg.defaultValue);
            }
          }
          processedArgs[index] = value;
        });
        this.processedArgs = processedArgs;
      }
      /**
       * Once we have a promise we chain, but call synchronously until then.
       *
       * @param {(Promise|undefined)} promise
       * @param {Function} fn
       * @return {(Promise|undefined)}
       * @private
       */
      _chainOrCall(promise, fn) {
        if (promise && promise.then && typeof promise.then === "function") {
          return promise.then(() => fn());
        }
        return fn();
      }
      /**
       *
       * @param {(Promise|undefined)} promise
       * @param {string} event
       * @return {(Promise|undefined)}
       * @private
       */
      _chainOrCallHooks(promise, event) {
        let result = promise;
        const hooks = [];
        this._getCommandAndAncestors().reverse().filter((cmd) => cmd._lifeCycleHooks[event] !== void 0).forEach((hookedCommand) => {
          hookedCommand._lifeCycleHooks[event].forEach((callback) => {
            hooks.push({ hookedCommand, callback });
          });
        });
        if (event === "postAction") {
          hooks.reverse();
        }
        hooks.forEach((hookDetail) => {
          result = this._chainOrCall(result, () => {
            return hookDetail.callback(hookDetail.hookedCommand, this);
          });
        });
        return result;
      }
      /**
       *
       * @param {(Promise|undefined)} promise
       * @param {Command} subCommand
       * @param {string} event
       * @return {(Promise|undefined)}
       * @private
       */
      _chainOrCallSubCommandHook(promise, subCommand, event) {
        let result = promise;
        if (this._lifeCycleHooks[event] !== void 0) {
          this._lifeCycleHooks[event].forEach((hook) => {
            result = this._chainOrCall(result, () => {
              return hook(this, subCommand);
            });
          });
        }
        return result;
      }
      /**
       * Process arguments in context of this command.
       * Returns action result, in case it is a promise.
       *
       * @private
       */
      _parseCommand(operands, unknown) {
        const parsed = this.parseOptions(unknown);
        this._parseOptionsEnv();
        this._parseOptionsImplied();
        operands = operands.concat(parsed.operands);
        unknown = parsed.unknown;
        this.args = operands.concat(unknown);
        if (operands && this._findCommand(operands[0])) {
          return this._dispatchSubcommand(operands[0], operands.slice(1), unknown);
        }
        if (this._getHelpCommand() && operands[0] === this._getHelpCommand().name()) {
          return this._dispatchHelpCommand(operands[1]);
        }
        if (this._defaultCommandName) {
          this._outputHelpIfRequested(unknown);
          return this._dispatchSubcommand(
            this._defaultCommandName,
            operands,
            unknown
          );
        }
        if (this.commands.length && this.args.length === 0 && !this._actionHandler && !this._defaultCommandName) {
          this.help({ error: true });
        }
        this._outputHelpIfRequested(parsed.unknown);
        this._checkForMissingMandatoryOptions();
        this._checkForConflictingOptions();
        const checkForUnknownOptions = () => {
          if (parsed.unknown.length > 0) {
            this.unknownOption(parsed.unknown[0]);
          }
        };
        const commandEvent = `command:${this.name()}`;
        if (this._actionHandler) {
          checkForUnknownOptions();
          this._processArguments();
          let promiseChain;
          promiseChain = this._chainOrCallHooks(promiseChain, "preAction");
          promiseChain = this._chainOrCall(
            promiseChain,
            () => this._actionHandler(this.processedArgs)
          );
          if (this.parent) {
            promiseChain = this._chainOrCall(promiseChain, () => {
              this.parent.emit(commandEvent, operands, unknown);
            });
          }
          promiseChain = this._chainOrCallHooks(promiseChain, "postAction");
          return promiseChain;
        }
        if (this.parent && this.parent.listenerCount(commandEvent)) {
          checkForUnknownOptions();
          this._processArguments();
          this.parent.emit(commandEvent, operands, unknown);
        } else if (operands.length) {
          if (this._findCommand("*")) {
            return this._dispatchSubcommand("*", operands, unknown);
          }
          if (this.listenerCount("command:*")) {
            this.emit("command:*", operands, unknown);
          } else if (this.commands.length) {
            this.unknownCommand();
          } else {
            checkForUnknownOptions();
            this._processArguments();
          }
        } else if (this.commands.length) {
          checkForUnknownOptions();
          this.help({ error: true });
        } else {
          checkForUnknownOptions();
          this._processArguments();
        }
      }
      /**
       * Find matching command.
       *
       * @private
       * @return {Command | undefined}
       */
      _findCommand(name) {
        if (!name) return void 0;
        return this.commands.find(
          (cmd) => cmd._name === name || cmd._aliases.includes(name)
        );
      }
      /**
       * Return an option matching `arg` if any.
       *
       * @param {string} arg
       * @return {Option}
       * @package
       */
      _findOption(arg) {
        return this.options.find((option) => option.is(arg));
      }
      /**
       * Display an error message if a mandatory option does not have a value.
       * Called after checking for help flags in leaf subcommand.
       *
       * @private
       */
      _checkForMissingMandatoryOptions() {
        this._getCommandAndAncestors().forEach((cmd) => {
          cmd.options.forEach((anOption) => {
            if (anOption.mandatory && cmd.getOptionValue(anOption.attributeName()) === void 0) {
              cmd.missingMandatoryOptionValue(anOption);
            }
          });
        });
      }
      /**
       * Display an error message if conflicting options are used together in this.
       *
       * @private
       */
      _checkForConflictingLocalOptions() {
        const definedNonDefaultOptions = this.options.filter((option) => {
          const optionKey = option.attributeName();
          if (this.getOptionValue(optionKey) === void 0) {
            return false;
          }
          return this.getOptionValueSource(optionKey) !== "default";
        });
        const optionsWithConflicting = definedNonDefaultOptions.filter(
          (option) => option.conflictsWith.length > 0
        );
        optionsWithConflicting.forEach((option) => {
          const conflictingAndDefined = definedNonDefaultOptions.find(
            (defined) => option.conflictsWith.includes(defined.attributeName())
          );
          if (conflictingAndDefined) {
            this._conflictingOption(option, conflictingAndDefined);
          }
        });
      }
      /**
       * Display an error message if conflicting options are used together.
       * Called after checking for help flags in leaf subcommand.
       *
       * @private
       */
      _checkForConflictingOptions() {
        this._getCommandAndAncestors().forEach((cmd) => {
          cmd._checkForConflictingLocalOptions();
        });
      }
      /**
       * Parse options from `argv` removing known options,
       * and return argv split into operands and unknown arguments.
       *
       * Side effects: modifies command by storing options. Does not reset state if called again.
       *
       * Examples:
       *
       *     argv => operands, unknown
       *     --known kkk op => [op], []
       *     op --known kkk => [op], []
       *     sub --unknown uuu op => [sub], [--unknown uuu op]
       *     sub -- --unknown uuu op => [sub --unknown uuu op], []
       *
       * @param {string[]} argv
       * @return {{operands: string[], unknown: string[]}}
       */
      parseOptions(argv) {
        const operands = [];
        const unknown = [];
        let dest = operands;
        const args = argv.slice();
        function maybeOption(arg) {
          return arg.length > 1 && arg[0] === "-";
        }
        let activeVariadicOption = null;
        while (args.length) {
          const arg = args.shift();
          if (arg === "--") {
            if (dest === unknown) dest.push(arg);
            dest.push(...args);
            break;
          }
          if (activeVariadicOption && !maybeOption(arg)) {
            this.emit(`option:${activeVariadicOption.name()}`, arg);
            continue;
          }
          activeVariadicOption = null;
          if (maybeOption(arg)) {
            const option = this._findOption(arg);
            if (option) {
              if (option.required) {
                const value = args.shift();
                if (value === void 0) this.optionMissingArgument(option);
                this.emit(`option:${option.name()}`, value);
              } else if (option.optional) {
                let value = null;
                if (args.length > 0 && !maybeOption(args[0])) {
                  value = args.shift();
                }
                this.emit(`option:${option.name()}`, value);
              } else {
                this.emit(`option:${option.name()}`);
              }
              activeVariadicOption = option.variadic ? option : null;
              continue;
            }
          }
          if (arg.length > 2 && arg[0] === "-" && arg[1] !== "-") {
            const option = this._findOption(`-${arg[1]}`);
            if (option) {
              if (option.required || option.optional && this._combineFlagAndOptionalValue) {
                this.emit(`option:${option.name()}`, arg.slice(2));
              } else {
                this.emit(`option:${option.name()}`);
                args.unshift(`-${arg.slice(2)}`);
              }
              continue;
            }
          }
          if (/^--[^=]+=/.test(arg)) {
            const index = arg.indexOf("=");
            const option = this._findOption(arg.slice(0, index));
            if (option && (option.required || option.optional)) {
              this.emit(`option:${option.name()}`, arg.slice(index + 1));
              continue;
            }
          }
          if (maybeOption(arg)) {
            dest = unknown;
          }
          if ((this._enablePositionalOptions || this._passThroughOptions) && operands.length === 0 && unknown.length === 0) {
            if (this._findCommand(arg)) {
              operands.push(arg);
              if (args.length > 0) unknown.push(...args);
              break;
            } else if (this._getHelpCommand() && arg === this._getHelpCommand().name()) {
              operands.push(arg);
              if (args.length > 0) operands.push(...args);
              break;
            } else if (this._defaultCommandName) {
              unknown.push(arg);
              if (args.length > 0) unknown.push(...args);
              break;
            }
          }
          if (this._passThroughOptions) {
            dest.push(arg);
            if (args.length > 0) dest.push(...args);
            break;
          }
          dest.push(arg);
        }
        return { operands, unknown };
      }
      /**
       * Return an object containing local option values as key-value pairs.
       *
       * @return {object}
       */
      opts() {
        if (this._storeOptionsAsProperties) {
          const result = {};
          const len = this.options.length;
          for (let i = 0; i < len; i++) {
            const key = this.options[i].attributeName();
            result[key] = key === this._versionOptionName ? this._version : this[key];
          }
          return result;
        }
        return this._optionValues;
      }
      /**
       * Return an object containing merged local and global option values as key-value pairs.
       *
       * @return {object}
       */
      optsWithGlobals() {
        return this._getCommandAndAncestors().reduce(
          (combinedOptions, cmd) => Object.assign(combinedOptions, cmd.opts()),
          {}
        );
      }
      /**
       * Display error message and exit (or call exitOverride).
       *
       * @param {string} message
       * @param {object} [errorOptions]
       * @param {string} [errorOptions.code] - an id string representing the error
       * @param {number} [errorOptions.exitCode] - used with process.exit
       */
      error(message, errorOptions) {
        this._outputConfiguration.outputError(
          `${message}
`,
          this._outputConfiguration.writeErr
        );
        if (typeof this._showHelpAfterError === "string") {
          this._outputConfiguration.writeErr(`${this._showHelpAfterError}
`);
        } else if (this._showHelpAfterError) {
          this._outputConfiguration.writeErr("\n");
          this.outputHelp({ error: true });
        }
        const config = errorOptions || {};
        const exitCode = config.exitCode || 1;
        const code = config.code || "commander.error";
        this._exit(exitCode, code, message);
      }
      /**
       * Apply any option related environment variables, if option does
       * not have a value from cli or client code.
       *
       * @private
       */
      _parseOptionsEnv() {
        this.options.forEach((option) => {
          if (option.envVar && option.envVar in process2.env) {
            const optionKey = option.attributeName();
            if (this.getOptionValue(optionKey) === void 0 || ["default", "config", "env"].includes(
              this.getOptionValueSource(optionKey)
            )) {
              if (option.required || option.optional) {
                this.emit(`optionEnv:${option.name()}`, process2.env[option.envVar]);
              } else {
                this.emit(`optionEnv:${option.name()}`);
              }
            }
          }
        });
      }
      /**
       * Apply any implied option values, if option is undefined or default value.
       *
       * @private
       */
      _parseOptionsImplied() {
        const dualHelper = new DualOptions(this.options);
        const hasCustomOptionValue = (optionKey) => {
          return this.getOptionValue(optionKey) !== void 0 && !["default", "implied"].includes(this.getOptionValueSource(optionKey));
        };
        this.options.filter(
          (option) => option.implied !== void 0 && hasCustomOptionValue(option.attributeName()) && dualHelper.valueFromOption(
            this.getOptionValue(option.attributeName()),
            option
          )
        ).forEach((option) => {
          Object.keys(option.implied).filter((impliedKey) => !hasCustomOptionValue(impliedKey)).forEach((impliedKey) => {
            this.setOptionValueWithSource(
              impliedKey,
              option.implied[impliedKey],
              "implied"
            );
          });
        });
      }
      /**
       * Argument `name` is missing.
       *
       * @param {string} name
       * @private
       */
      missingArgument(name) {
        const message = `error: missing required argument '${name}'`;
        this.error(message, { code: "commander.missingArgument" });
      }
      /**
       * `Option` is missing an argument.
       *
       * @param {Option} option
       * @private
       */
      optionMissingArgument(option) {
        const message = `error: option '${option.flags}' argument missing`;
        this.error(message, { code: "commander.optionMissingArgument" });
      }
      /**
       * `Option` does not have a value, and is a mandatory option.
       *
       * @param {Option} option
       * @private
       */
      missingMandatoryOptionValue(option) {
        const message = `error: required option '${option.flags}' not specified`;
        this.error(message, { code: "commander.missingMandatoryOptionValue" });
      }
      /**
       * `Option` conflicts with another option.
       *
       * @param {Option} option
       * @param {Option} conflictingOption
       * @private
       */
      _conflictingOption(option, conflictingOption) {
        const findBestOptionFromValue = (option2) => {
          const optionKey = option2.attributeName();
          const optionValue = this.getOptionValue(optionKey);
          const negativeOption = this.options.find(
            (target) => target.negate && optionKey === target.attributeName()
          );
          const positiveOption = this.options.find(
            (target) => !target.negate && optionKey === target.attributeName()
          );
          if (negativeOption && (negativeOption.presetArg === void 0 && optionValue === false || negativeOption.presetArg !== void 0 && optionValue === negativeOption.presetArg)) {
            return negativeOption;
          }
          return positiveOption || option2;
        };
        const getErrorMessage = (option2) => {
          const bestOption = findBestOptionFromValue(option2);
          const optionKey = bestOption.attributeName();
          const source = this.getOptionValueSource(optionKey);
          if (source === "env") {
            return `environment variable '${bestOption.envVar}'`;
          }
          return `option '${bestOption.flags}'`;
        };
        const message = `error: ${getErrorMessage(option)} cannot be used with ${getErrorMessage(conflictingOption)}`;
        this.error(message, { code: "commander.conflictingOption" });
      }
      /**
       * Unknown option `flag`.
       *
       * @param {string} flag
       * @private
       */
      unknownOption(flag) {
        if (this._allowUnknownOption) return;
        let suggestion = "";
        if (flag.startsWith("--") && this._showSuggestionAfterError) {
          let candidateFlags = [];
          let command = this;
          do {
            const moreFlags = command.createHelp().visibleOptions(command).filter((option) => option.long).map((option) => option.long);
            candidateFlags = candidateFlags.concat(moreFlags);
            command = command.parent;
          } while (command && !command._enablePositionalOptions);
          suggestion = suggestSimilar(flag, candidateFlags);
        }
        const message = `error: unknown option '${flag}'${suggestion}`;
        this.error(message, { code: "commander.unknownOption" });
      }
      /**
       * Excess arguments, more than expected.
       *
       * @param {string[]} receivedArgs
       * @private
       */
      _excessArguments(receivedArgs) {
        if (this._allowExcessArguments) return;
        const expected = this.registeredArguments.length;
        const s = expected === 1 ? "" : "s";
        const forSubcommand = this.parent ? ` for '${this.name()}'` : "";
        const message = `error: too many arguments${forSubcommand}. Expected ${expected} argument${s} but got ${receivedArgs.length}.`;
        this.error(message, { code: "commander.excessArguments" });
      }
      /**
       * Unknown command.
       *
       * @private
       */
      unknownCommand() {
        const unknownName = this.args[0];
        let suggestion = "";
        if (this._showSuggestionAfterError) {
          const candidateNames = [];
          this.createHelp().visibleCommands(this).forEach((command) => {
            candidateNames.push(command.name());
            if (command.alias()) candidateNames.push(command.alias());
          });
          suggestion = suggestSimilar(unknownName, candidateNames);
        }
        const message = `error: unknown command '${unknownName}'${suggestion}`;
        this.error(message, { code: "commander.unknownCommand" });
      }
      /**
       * Get or set the program version.
       *
       * This method auto-registers the "-V, --version" option which will print the version number.
       *
       * You can optionally supply the flags and description to override the defaults.
       *
       * @param {string} [str]
       * @param {string} [flags]
       * @param {string} [description]
       * @return {(this | string | undefined)} `this` command for chaining, or version string if no arguments
       */
      version(str, flags, description) {
        if (str === void 0) return this._version;
        this._version = str;
        flags = flags || "-V, --version";
        description = description || "output the version number";
        const versionOption = this.createOption(flags, description);
        this._versionOptionName = versionOption.attributeName();
        this._registerOption(versionOption);
        this.on("option:" + versionOption.name(), () => {
          this._outputConfiguration.writeOut(`${str}
`);
          this._exit(0, "commander.version", str);
        });
        return this;
      }
      /**
       * Set the description.
       *
       * @param {string} [str]
       * @param {object} [argsDescription]
       * @return {(string|Command)}
       */
      description(str, argsDescription) {
        if (str === void 0 && argsDescription === void 0)
          return this._description;
        this._description = str;
        if (argsDescription) {
          this._argsDescription = argsDescription;
        }
        return this;
      }
      /**
       * Set the summary. Used when listed as subcommand of parent.
       *
       * @param {string} [str]
       * @return {(string|Command)}
       */
      summary(str) {
        if (str === void 0) return this._summary;
        this._summary = str;
        return this;
      }
      /**
       * Set an alias for the command.
       *
       * You may call more than once to add multiple aliases. Only the first alias is shown in the auto-generated help.
       *
       * @param {string} [alias]
       * @return {(string|Command)}
       */
      alias(alias) {
        if (alias === void 0) return this._aliases[0];
        let command = this;
        if (this.commands.length !== 0 && this.commands[this.commands.length - 1]._executableHandler) {
          command = this.commands[this.commands.length - 1];
        }
        if (alias === command._name)
          throw new Error("Command alias can't be the same as its name");
        const matchingCommand = this.parent?._findCommand(alias);
        if (matchingCommand) {
          const existingCmd = [matchingCommand.name()].concat(matchingCommand.aliases()).join("|");
          throw new Error(
            `cannot add alias '${alias}' to command '${this.name()}' as already have command '${existingCmd}'`
          );
        }
        command._aliases.push(alias);
        return this;
      }
      /**
       * Set aliases for the command.
       *
       * Only the first alias is shown in the auto-generated help.
       *
       * @param {string[]} [aliases]
       * @return {(string[]|Command)}
       */
      aliases(aliases) {
        if (aliases === void 0) return this._aliases;
        aliases.forEach((alias) => this.alias(alias));
        return this;
      }
      /**
       * Set / get the command usage `str`.
       *
       * @param {string} [str]
       * @return {(string|Command)}
       */
      usage(str) {
        if (str === void 0) {
          if (this._usage) return this._usage;
          const args = this.registeredArguments.map((arg) => {
            return humanReadableArgName(arg);
          });
          return [].concat(
            this.options.length || this._helpOption !== null ? "[options]" : [],
            this.commands.length ? "[command]" : [],
            this.registeredArguments.length ? args : []
          ).join(" ");
        }
        this._usage = str;
        return this;
      }
      /**
       * Get or set the name of the command.
       *
       * @param {string} [str]
       * @return {(string|Command)}
       */
      name(str) {
        if (str === void 0) return this._name;
        this._name = str;
        return this;
      }
      /**
       * Set the name of the command from script filename, such as process.argv[1],
       * or require.main.filename, or __filename.
       *
       * (Used internally and public although not documented in README.)
       *
       * @example
       * program.nameFromFilename(require.main.filename);
       *
       * @param {string} filename
       * @return {Command}
       */
      nameFromFilename(filename) {
        this._name = path.basename(filename, path.extname(filename));
        return this;
      }
      /**
       * Get or set the directory for searching for executable subcommands of this command.
       *
       * @example
       * program.executableDir(__dirname);
       * // or
       * program.executableDir('subcommands');
       *
       * @param {string} [path]
       * @return {(string|null|Command)}
       */
      executableDir(path2) {
        if (path2 === void 0) return this._executableDir;
        this._executableDir = path2;
        return this;
      }
      /**
       * Return program help documentation.
       *
       * @param {{ error: boolean }} [contextOptions] - pass {error:true} to wrap for stderr instead of stdout
       * @return {string}
       */
      helpInformation(contextOptions) {
        const helper = this.createHelp();
        const context = this._getOutputContext(contextOptions);
        helper.prepareContext({
          error: context.error,
          helpWidth: context.helpWidth,
          outputHasColors: context.hasColors
        });
        const text = helper.formatHelp(this, helper);
        if (context.hasColors) return text;
        return this._outputConfiguration.stripColor(text);
      }
      /**
       * @typedef HelpContext
       * @type {object}
       * @property {boolean} error
       * @property {number} helpWidth
       * @property {boolean} hasColors
       * @property {function} write - includes stripColor if needed
       *
       * @returns {HelpContext}
       * @private
       */
      _getOutputContext(contextOptions) {
        contextOptions = contextOptions || {};
        const error = !!contextOptions.error;
        let baseWrite;
        let hasColors;
        let helpWidth;
        if (error) {
          baseWrite = (str) => this._outputConfiguration.writeErr(str);
          hasColors = this._outputConfiguration.getErrHasColors();
          helpWidth = this._outputConfiguration.getErrHelpWidth();
        } else {
          baseWrite = (str) => this._outputConfiguration.writeOut(str);
          hasColors = this._outputConfiguration.getOutHasColors();
          helpWidth = this._outputConfiguration.getOutHelpWidth();
        }
        const write = (str) => {
          if (!hasColors) str = this._outputConfiguration.stripColor(str);
          return baseWrite(str);
        };
        return { error, write, hasColors, helpWidth };
      }
      /**
       * Output help information for this command.
       *
       * Outputs built-in help, and custom text added using `.addHelpText()`.
       *
       * @param {{ error: boolean } | Function} [contextOptions] - pass {error:true} to write to stderr instead of stdout
       */
      outputHelp(contextOptions) {
        let deprecatedCallback;
        if (typeof contextOptions === "function") {
          deprecatedCallback = contextOptions;
          contextOptions = void 0;
        }
        const outputContext = this._getOutputContext(contextOptions);
        const eventContext = {
          error: outputContext.error,
          write: outputContext.write,
          command: this
        };
        this._getCommandAndAncestors().reverse().forEach((command) => command.emit("beforeAllHelp", eventContext));
        this.emit("beforeHelp", eventContext);
        let helpInformation = this.helpInformation({ error: outputContext.error });
        if (deprecatedCallback) {
          helpInformation = deprecatedCallback(helpInformation);
          if (typeof helpInformation !== "string" && !Buffer.isBuffer(helpInformation)) {
            throw new Error("outputHelp callback must return a string or a Buffer");
          }
        }
        outputContext.write(helpInformation);
        if (this._getHelpOption()?.long) {
          this.emit(this._getHelpOption().long);
        }
        this.emit("afterHelp", eventContext);
        this._getCommandAndAncestors().forEach(
          (command) => command.emit("afterAllHelp", eventContext)
        );
      }
      /**
       * You can pass in flags and a description to customise the built-in help option.
       * Pass in false to disable the built-in help option.
       *
       * @example
       * program.helpOption('-?, --help' 'show help'); // customise
       * program.helpOption(false); // disable
       *
       * @param {(string | boolean)} flags
       * @param {string} [description]
       * @return {Command} `this` command for chaining
       */
      helpOption(flags, description) {
        if (typeof flags === "boolean") {
          if (flags) {
            this._helpOption = this._helpOption ?? void 0;
          } else {
            this._helpOption = null;
          }
          return this;
        }
        flags = flags ?? "-h, --help";
        description = description ?? "display help for command";
        this._helpOption = this.createOption(flags, description);
        return this;
      }
      /**
       * Lazy create help option.
       * Returns null if has been disabled with .helpOption(false).
       *
       * @returns {(Option | null)} the help option
       * @package
       */
      _getHelpOption() {
        if (this._helpOption === void 0) {
          this.helpOption(void 0, void 0);
        }
        return this._helpOption;
      }
      /**
       * Supply your own option to use for the built-in help option.
       * This is an alternative to using helpOption() to customise the flags and description etc.
       *
       * @param {Option} option
       * @return {Command} `this` command for chaining
       */
      addHelpOption(option) {
        this._helpOption = option;
        return this;
      }
      /**
       * Output help information and exit.
       *
       * Outputs built-in help, and custom text added using `.addHelpText()`.
       *
       * @param {{ error: boolean }} [contextOptions] - pass {error:true} to write to stderr instead of stdout
       */
      help(contextOptions) {
        this.outputHelp(contextOptions);
        let exitCode = Number(process2.exitCode ?? 0);
        if (exitCode === 0 && contextOptions && typeof contextOptions !== "function" && contextOptions.error) {
          exitCode = 1;
        }
        this._exit(exitCode, "commander.help", "(outputHelp)");
      }
      /**
       * // Do a little typing to coordinate emit and listener for the help text events.
       * @typedef HelpTextEventContext
       * @type {object}
       * @property {boolean} error
       * @property {Command} command
       * @property {function} write
       */
      /**
       * Add additional text to be displayed with the built-in help.
       *
       * Position is 'before' or 'after' to affect just this command,
       * and 'beforeAll' or 'afterAll' to affect this command and all its subcommands.
       *
       * @param {string} position - before or after built-in help
       * @param {(string | Function)} text - string to add, or a function returning a string
       * @return {Command} `this` command for chaining
       */
      addHelpText(position, text) {
        const allowedValues = ["beforeAll", "before", "after", "afterAll"];
        if (!allowedValues.includes(position)) {
          throw new Error(`Unexpected value for position to addHelpText.
Expecting one of '${allowedValues.join("', '")}'`);
        }
        const helpEvent = `${position}Help`;
        this.on(helpEvent, (context) => {
          let helpStr;
          if (typeof text === "function") {
            helpStr = text({ error: context.error, command: context.command });
          } else {
            helpStr = text;
          }
          if (helpStr) {
            context.write(`${helpStr}
`);
          }
        });
        return this;
      }
      /**
       * Output help information if help flags specified
       *
       * @param {Array} args - array of options to search for help flags
       * @private
       */
      _outputHelpIfRequested(args) {
        const helpOption = this._getHelpOption();
        const helpRequested = helpOption && args.find((arg) => helpOption.is(arg));
        if (helpRequested) {
          this.outputHelp();
          this._exit(0, "commander.helpDisplayed", "(outputHelp)");
        }
      }
    };
    function incrementNodeInspectorPort(args) {
      return args.map((arg) => {
        if (!arg.startsWith("--inspect")) {
          return arg;
        }
        let debugOption;
        let debugHost = "127.0.0.1";
        let debugPort = "9229";
        let match;
        if ((match = arg.match(/^(--inspect(-brk)?)$/)) !== null) {
          debugOption = match[1];
        } else if ((match = arg.match(/^(--inspect(-brk|-port)?)=([^:]+)$/)) !== null) {
          debugOption = match[1];
          if (/^\d+$/.test(match[3])) {
            debugPort = match[3];
          } else {
            debugHost = match[3];
          }
        } else if ((match = arg.match(/^(--inspect(-brk|-port)?)=([^:]+):(\d+)$/)) !== null) {
          debugOption = match[1];
          debugHost = match[3];
          debugPort = match[4];
        }
        if (debugOption && debugPort !== "0") {
          return `${debugOption}=${debugHost}:${parseInt(debugPort) + 1}`;
        }
        return arg;
      });
    }
    function useColor() {
      if (process2.env.NO_COLOR || process2.env.FORCE_COLOR === "0" || process2.env.FORCE_COLOR === "false")
        return false;
      if (process2.env.FORCE_COLOR || process2.env.CLICOLOR_FORCE !== void 0)
        return true;
      return void 0;
    }
    exports.Command = Command2;
    exports.useColor = useColor;
  }
});

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/index.js
var require_commander = __commonJS({
  "../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/index.js"(exports) {
    var { Argument: Argument2 } = require_argument();
    var { Command: Command2 } = require_command();
    var { CommanderError: CommanderError2, InvalidArgumentError: InvalidArgumentError2 } = require_error();
    var { Help: Help2 } = require_help();
    var { Option: Option2 } = require_option();
    exports.program = new Command2();
    exports.createCommand = (name) => new Command2(name);
    exports.createOption = (flags, description) => new Option2(flags, description);
    exports.createArgument = (name, description) => new Argument2(name, description);
    exports.Command = Command2;
    exports.Option = Option2;
    exports.Argument = Argument2;
    exports.Help = Help2;
    exports.CommanderError = CommanderError2;
    exports.InvalidArgumentError = InvalidArgumentError2;
    exports.InvalidOptionArgumentError = InvalidArgumentError2;
  }
});

// ../../node_modules/.pnpm/picocolors@1.1.1/node_modules/picocolors/picocolors.js
var require_picocolors = __commonJS({
  "../../node_modules/.pnpm/picocolors@1.1.1/node_modules/picocolors/picocolors.js"(exports, module) {
    var p = process || {};
    var argv = p.argv || [];
    var env = p.env || {};
    var isColorSupported = !(!!env.NO_COLOR || argv.includes("--no-color")) && (!!env.FORCE_COLOR || argv.includes("--color") || p.platform === "win32" || (p.stdout || {}).isTTY && env.TERM !== "dumb" || !!env.CI);
    var formatter = (open, close, replace = open) => (input) => {
      let string = "" + input, index = string.indexOf(close, open.length);
      return ~index ? open + replaceClose(string, close, replace, index) + close : open + string + close;
    };
    var replaceClose = (string, close, replace, index) => {
      let result = "", cursor = 0;
      do {
        result += string.substring(cursor, index) + replace;
        cursor = index + close.length;
        index = string.indexOf(close, cursor);
      } while (~index);
      return result + string.substring(cursor);
    };
    var createColors = (enabled = isColorSupported) => {
      let f = enabled ? formatter : () => String;
      return {
        isColorSupported: enabled,
        reset: f("\x1B[0m", "\x1B[0m"),
        bold: f("\x1B[1m", "\x1B[22m", "\x1B[22m\x1B[1m"),
        dim: f("\x1B[2m", "\x1B[22m", "\x1B[22m\x1B[2m"),
        italic: f("\x1B[3m", "\x1B[23m"),
        underline: f("\x1B[4m", "\x1B[24m"),
        inverse: f("\x1B[7m", "\x1B[27m"),
        hidden: f("\x1B[8m", "\x1B[28m"),
        strikethrough: f("\x1B[9m", "\x1B[29m"),
        black: f("\x1B[30m", "\x1B[39m"),
        red: f("\x1B[31m", "\x1B[39m"),
        green: f("\x1B[32m", "\x1B[39m"),
        yellow: f("\x1B[33m", "\x1B[39m"),
        blue: f("\x1B[34m", "\x1B[39m"),
        magenta: f("\x1B[35m", "\x1B[39m"),
        cyan: f("\x1B[36m", "\x1B[39m"),
        white: f("\x1B[37m", "\x1B[39m"),
        gray: f("\x1B[90m", "\x1B[39m"),
        bgBlack: f("\x1B[40m", "\x1B[49m"),
        bgRed: f("\x1B[41m", "\x1B[49m"),
        bgGreen: f("\x1B[42m", "\x1B[49m"),
        bgYellow: f("\x1B[43m", "\x1B[49m"),
        bgBlue: f("\x1B[44m", "\x1B[49m"),
        bgMagenta: f("\x1B[45m", "\x1B[49m"),
        bgCyan: f("\x1B[46m", "\x1B[49m"),
        bgWhite: f("\x1B[47m", "\x1B[49m"),
        blackBright: f("\x1B[90m", "\x1B[39m"),
        redBright: f("\x1B[91m", "\x1B[39m"),
        greenBright: f("\x1B[92m", "\x1B[39m"),
        yellowBright: f("\x1B[93m", "\x1B[39m"),
        blueBright: f("\x1B[94m", "\x1B[39m"),
        magentaBright: f("\x1B[95m", "\x1B[39m"),
        cyanBright: f("\x1B[96m", "\x1B[39m"),
        whiteBright: f("\x1B[97m", "\x1B[39m"),
        bgBlackBright: f("\x1B[100m", "\x1B[49m"),
        bgRedBright: f("\x1B[101m", "\x1B[49m"),
        bgGreenBright: f("\x1B[102m", "\x1B[49m"),
        bgYellowBright: f("\x1B[103m", "\x1B[49m"),
        bgBlueBright: f("\x1B[104m", "\x1B[49m"),
        bgMagentaBright: f("\x1B[105m", "\x1B[49m"),
        bgCyanBright: f("\x1B[106m", "\x1B[49m"),
        bgWhiteBright: f("\x1B[107m", "\x1B[49m")
      };
    };
    module.exports = createColors();
    module.exports.createColors = createColors;
  }
});

// ../../node_modules/.pnpm/commander@13.1.0/node_modules/commander/esm.mjs
var import_index = __toESM(require_commander(), 1);
var {
  program,
  createCommand,
  createArgument,
  createOption,
  CommanderError,
  InvalidArgumentError,
  InvalidOptionArgumentError,
  // deprecated old name
  Command,
  Argument,
  Option,
  Help
} = import_index.default;

// dist/commands/check.js
var import_picocolors2 = __toESM(require_picocolors(), 1);
import { readFileSync, writeFileSync } from "node:fs";

// ../core/dist/ast.js
function childrenOf(expr) {
  switch (expr.kind) {
    case "money":
    case "int":
    case "bool":
    case "enum":
    case "fact":
    case "rule":
    case "param":
    case "unsupported":
      return [];
    case "mulDiv":
      return [expr.a, expr.b, expr.c];
    case "add":
    case "min":
    case "max":
    case "and":
    case "or":
      return expr.args;
    case "sub":
      return [expr.left, expr.right];
    case "mulRate":
      return [expr.base];
    case "mulInt":
      return [expr.base, expr.count];
    case "max0":
    case "not":
      return [expr.arg];
    case "clamp":
      return [expr.value, expr.lo, expr.hi];
    case "roundToDollar":
    case "stepUnits":
      return [expr.value];
    case "cmp":
      return [expr.left, expr.right];
    case "if":
      return [expr.cond, expr.then, expr.else];
    case "brackets":
      return [expr.base];
    case "match": {
      const kids = [expr.on, ...expr.cases.map((c2) => c2.value)];
      if (expr.else)
        kids.push(expr.else);
      return kids;
    }
  }
}
function walkExpr(expr, visit) {
  visit(expr);
  for (const child of childrenOf(expr))
    walkExpr(child, visit);
}
function valueToJSON(v) {
  switch (v.type) {
    case "money":
      return { type: "money", value: v.cents.toString() };
    case "int":
      return { type: "int", value: v.value.toString() };
    case "bool":
      return { type: "bool", value: v.value };
    case "enum":
      return { type: "enum", value: v.value };
  }
}
function valueFromJSON(tv) {
  switch (tv.type) {
    case "money": {
      if (typeof tv.value !== "string") {
        throw new TypeError("money value must be a string of cents");
      }
      if (!/^-?\d+$/.test(tv.value)) {
        throw new TypeError(`invalid money value: ${JSON.stringify(tv.value)}`);
      }
      return { type: "money", cents: BigInt(tv.value) };
    }
    case "int": {
      if (typeof tv.value !== "string") {
        throw new TypeError("int value must be a decimal string");
      }
      if (!/^-?\d+$/.test(tv.value)) {
        throw new TypeError(`invalid int value: ${JSON.stringify(tv.value)}`);
      }
      return { type: "int", value: BigInt(tv.value) };
    }
    case "bool": {
      if (typeof tv.value !== "boolean") {
        throw new TypeError("bool value must be a boolean");
      }
      return { type: "bool", value: tv.value };
    }
    case "enum": {
      if (typeof tv.value !== "string") {
        throw new TypeError("enum value must be a string");
      }
      return { type: "enum", value: tv.value };
    }
    default:
      throw new TypeError(`unknown value type: ${tv.type}`);
  }
}

// ../core/dist/money.js
function divRound(n, d3, mode) {
  if (d3 === 0n)
    throw new RangeError("division by zero");
  if (d3 < 0n) {
    n = -n;
    d3 = -d3;
  }
  const q = n / d3;
  const r = n % d3;
  if (r === 0n)
    return q;
  const neg = n < 0n;
  const twiceAbsRem = (r < 0n ? -r : r) * 2n;
  switch (mode) {
    case "floor":
      return neg ? q - 1n : q;
    case "ceil":
      return neg ? q : q + 1n;
    case "half-up":
      if (twiceAbsRem >= d3)
        return neg ? q - 1n : q + 1n;
      return q;
    case "half-even":
      if (twiceAbsRem > d3)
        return neg ? q - 1n : q + 1n;
      if (twiceAbsRem < d3)
        return q;
      return q % 2n === 0n ? q : neg ? q - 1n : q + 1n;
  }
}
function mulRate(cents2, rate2, mode) {
  if (rate2.den === 0n)
    throw new RangeError("rate with zero denominator");
  return divRound(cents2 * rate2.num, rate2.den, mode);
}
function max0(cents2) {
  return cents2 < 0n ? 0n : cents2;
}
function roundToDollar(cents2, mode) {
  return divRound(cents2, 100n, mode) * 100n;
}
function stepUnits(value, unit, mode) {
  if (unit <= 0n)
    throw new RangeError("stepUnits requires a positive unit");
  return divRound(value, unit, mode);
}
function parseDollars(input) {
  if (typeof input === "number") {
    if (!Number.isSafeInteger(input)) {
      throw new TypeError(`non-integer dollar amount ${input} must be a string (e.g. "1234.56") so it stays exact`);
    }
    return BigInt(input) * 100n;
  }
  const s = input.trim().replace(/^\$/, "").replace(/,/g, "");
  const m = /^(-?)(\d+)(?:\.(\d{1,2}))?$/.exec(s);
  if (!m) {
    throw new TypeError(`invalid money amount ${JSON.stringify(input)} \u2014 use dollars like 50000, "$50,000", or "1234.56"`);
  }
  const [, sign, whole, frac] = m;
  const cents2 = BigInt(whole) * 100n + BigInt((frac ?? "").padEnd(2, "0") || "0");
  return sign === "-" ? -cents2 : cents2;
}
function parseCents(s) {
  if (!/^-?\d+$/.test(s)) {
    throw new TypeError(`invalid cents literal: ${JSON.stringify(s)}`);
  }
  return BigInt(s);
}
function parseInt_(s) {
  if (!/^-?\d+$/.test(s)) {
    throw new TypeError(`invalid integer literal: ${JSON.stringify(s)}`);
  }
  return BigInt(s);
}

// ../core/dist/canonical.js
var objectCache = /* @__PURE__ */ new WeakMap();
function canonical(value) {
  return serialize(value, "$");
}
function serialize(value, path) {
  if (value === null)
    return "null";
  switch (typeof value) {
    case "boolean":
      return value ? "true" : "false";
    case "string":
      return JSON.stringify(value);
    case "number":
      if (!Number.isSafeInteger(value)) {
        throw new TypeError(`canonical: non-integer number at ${path} (${value}) \u2014 numerics must be decimal strings`);
      }
      return String(value);
    case "bigint":
      throw new TypeError(`canonical: bigint at ${path} \u2014 convert to a decimal string at the boundary`);
    case "undefined":
      throw new TypeError(`canonical: undefined at ${path}`);
    case "object": {
      const cached2 = objectCache.get(value);
      if (cached2 !== void 0)
        return cached2;
      let out;
      if (Array.isArray(value)) {
        out = `[${value.map((v, i) => serialize(v, `${path}[${i}]`)).join(",")}]`;
      } else {
        const obj = value;
        const keys = Object.keys(obj).filter((k) => obj[k] !== void 0).sort();
        const parts = keys.map((k) => `${JSON.stringify(k)}:${serialize(obj[k], `${path}.${k}`)}`);
        out = `{${parts.join(",")}}`;
      }
      objectCache.set(value, out);
      return out;
    }
    default:
      throw new TypeError(`canonical: unsupported ${typeof value} at ${path}`);
  }
}

// ../core/dist/sha256.js
var K = new Uint32Array([
  1116352408,
  1899447441,
  3049323471,
  3921009573,
  961987163,
  1508970993,
  2453635748,
  2870763221,
  3624381080,
  310598401,
  607225278,
  1426881987,
  1925078388,
  2162078206,
  2614888103,
  3248222580,
  3835390401,
  4022224774,
  264347078,
  604807628,
  770255983,
  1249150122,
  1555081692,
  1996064986,
  2554220882,
  2821834349,
  2952996808,
  3210313671,
  3336571891,
  3584528711,
  113926993,
  338241895,
  666307205,
  773529912,
  1294757372,
  1396182291,
  1695183700,
  1986661051,
  2177026350,
  2456956037,
  2730485921,
  2820302411,
  3259730800,
  3345764771,
  3516065817,
  3600352804,
  4094571909,
  275423344,
  430227734,
  506948616,
  659060556,
  883997877,
  958139571,
  1322822218,
  1537002063,
  1747873779,
  1955562222,
  2024104815,
  2227730452,
  2361852424,
  2428436474,
  2756734187,
  3204031479,
  3329325298
]);
var HEX = "0123456789abcdef";
function sha256HexPure(input) {
  const data = new TextEncoder().encode(input);
  const byteLen = data.length;
  const padded = new Uint8Array((byteLen + 8 >> 6) + 1 << 6);
  padded.set(data);
  padded[byteLen] = 128;
  const view = new DataView(padded.buffer);
  const bitLen = BigInt(byteLen) * 8n;
  view.setUint32(padded.length - 8, Number(bitLen >> 32n), false);
  view.setUint32(padded.length - 4, Number(bitLen & 0xffffffffn), false);
  let h0 = 1779033703, h1 = 3144134277, h2 = 1013904242, h3 = 2773480762;
  let h4 = 1359893119, h5 = 2600822924, h6 = 528734635, h7 = 1541459225;
  const w = new Uint32Array(64);
  for (let off = 0; off < padded.length; off += 64) {
    for (let i = 0; i < 16; i++)
      w[i] = view.getUint32(off + i * 4, false);
    for (let i = 16; i < 64; i++) {
      const x = w[i - 15];
      const y = w[i - 2];
      const s0 = (x >>> 7 | x << 25) ^ (x >>> 18 | x << 14) ^ x >>> 3;
      const s1 = (y >>> 17 | y << 15) ^ (y >>> 19 | y << 13) ^ y >>> 10;
      w[i] = w[i - 16] + s0 + w[i - 7] + s1 | 0;
    }
    let a = h0, b = h1, c2 = h2, d3 = h3, e = h4, f = h5, g = h6, h = h7;
    for (let i = 0; i < 64; i++) {
      const S1 = (e >>> 6 | e << 26) ^ (e >>> 11 | e << 21) ^ (e >>> 25 | e << 7);
      const ch = e & f ^ ~e & g;
      const t1 = h + S1 + ch + K[i] + w[i] | 0;
      const S0 = (a >>> 2 | a << 30) ^ (a >>> 13 | a << 19) ^ (a >>> 22 | a << 10);
      const maj = a & b ^ a & c2 ^ b & c2;
      const t2 = S0 + maj | 0;
      h = g;
      g = f;
      f = e;
      e = d3 + t1 | 0;
      d3 = c2;
      c2 = b;
      b = a;
      a = t1 + t2 | 0;
    }
    h0 = h0 + a | 0;
    h1 = h1 + b | 0;
    h2 = h2 + c2 | 0;
    h3 = h3 + d3 | 0;
    h4 = h4 + e | 0;
    h5 = h5 + f | 0;
    h6 = h6 + g | 0;
    h7 = h7 + h | 0;
  }
  let out = "";
  for (const word of [h0, h1, h2, h3, h4, h5, h6, h7]) {
    for (let shift = 28; shift >= 0; shift -= 4) {
      out += HEX[word >>> shift & 15];
    }
  }
  return out;
}

// ../core/dist/hash.js
function sha256Hex(input) {
  return sha256HexPure(input);
}
function hashOf(value) {
  return `sha256:${sha256Hex(canonical(value))}`;
}
function ruleHash(rule) {
  return hashOf(rule);
}
function merkleRoot(rules, facts2 = []) {
  const ordered = [...rules].sort((a, b) => a.id.localeCompare(b.id) || a.version - b.version);
  let level = ordered.map((r) => sha256Hex(canonical(r)));
  if (facts2.length > 0) {
    const orderedFacts = [...facts2].sort((a, b) => a.id.localeCompare(b.id));
    level.push(sha256Hex(canonical({ facts: orderedFacts })));
  }
  if (level.length === 0)
    return `sha256:${sha256Hex("")}`;
  while (level.length > 1) {
    const next = [];
    for (let i = 0; i < level.length; i += 2) {
      const left = level[i];
      const right = i + 1 < level.length ? level[i + 1] : level[i];
      next.push(sha256Hex(`${left}|${right}`));
    }
    level = next;
  }
  return `sha256:${level[0]}`;
}

// ../core/dist/errors.js
var OpenTaxError = class extends Error {
  code;
  constructor(code, message) {
    super(message);
    this.name = new.target.name;
    this.code = code;
  }
  /** Structured, code-specific details. Subclasses override. */
  data() {
    return {};
  }
  /** The whole error as one JSON-safe object. */
  toJSON() {
    return { code: this.code, message: this.message, data: this.data() };
  }
};
var NeedsFactsError = class extends OpenTaxError {
  missing;
  constructor(missing) {
    super("NEEDS_FACTS", `cannot evaluate: missing fact(s): ${missing.map((m) => m.factId).join(", ")}`);
    this.missing = missing;
  }
  data() {
    return { missing: this.missing };
  }
};
var CycleError = class extends OpenTaxError {
  path;
  constructor(path) {
    super("RULE_CYCLE", `rule dependency cycle: ${path.join(" -> ")}`);
    this.path = path;
  }
  data() {
    return { path: this.path };
  }
};
var NoApplicableRuleError = class extends OpenTaxError {
  ruleId;
  asOf;
  knownVersions;
  constructor(ruleId, asOf, knownVersions) {
    super("NO_APPLICABLE_RULE", `no applicable rule "${ruleId}" as of ${asOf}` + (knownVersions.length ? ` (known versions: ${knownVersions.map((v) => `v${v.version} [${v.effectiveFrom}, ${v.effectiveTo ?? "open"})`).join("; ")})` : " (rule id unknown)"));
    this.ruleId = ruleId;
    this.asOf = asOf;
    this.knownVersions = knownVersions;
  }
  data() {
    return {
      ruleId: this.ruleId,
      asOf: this.asOf,
      knownVersions: this.knownVersions
    };
  }
};
var NotModeledError = class extends OpenTaxError {
  ruleId;
  reason;
  constructor(ruleId, reason) {
    super("NOT_MODELED", `rule "${ruleId}" does not model this situation: ${reason} (refusing to guess)`);
    this.ruleId = ruleId;
    this.reason = reason;
  }
  data() {
    return { ruleId: this.ruleId, reason: this.reason };
  }
};
var UnhandledEnumCaseError = class extends OpenTaxError {
  ruleId;
  value;
  constructor(ruleId, value) {
    super("UNHANDLED_ENUM_CASE", `rule "${ruleId}" has no case for value "${value}" \u2014 this input is not covered by the corpus (refusing to guess)`);
    this.ruleId = ruleId;
    this.value = value;
  }
  data() {
    return { ruleId: this.ruleId, value: this.value };
  }
};
var CorpusValidationError = class extends OpenTaxError {
  issues;
  constructor(issues) {
    super("CORPUS_INVALID", `corpus failed validation:
${issues.map((i) => `  - ${i}`).join("\n")}`);
    this.issues = issues;
  }
  data() {
    return { issues: this.issues };
  }
};
var FactValidationError = class extends OpenTaxError {
  constructor(message) {
    super("FACT_INVALID", message);
  }
};
var EngineError = class extends OpenTaxError {
  constructor(message) {
    super("ENGINE_ERROR", message);
  }
};

// ../core/dist/corpus.js
var ISO_DATE = /^\d{4}-\d{2}-\d{2}$/;
function loadCorpus(input) {
  const issues = [];
  const byId = /* @__PURE__ */ new Map();
  const factById = /* @__PURE__ */ new Map();
  const overriders = /* @__PURE__ */ new Map();
  for (const fact44 of input.facts) {
    if (factById.has(fact44.id))
      issues.push(`duplicate fact id "${fact44.id}"`);
    factById.set(fact44.id, fact44);
    if (fact44.type === "enum" && !fact44.enumValues?.length) {
      issues.push(`enum fact "${fact44.id}" must declare enumValues`);
    }
  }
  const seenVersions = /* @__PURE__ */ new Set();
  for (const rule of input.rules) {
    const key = `${rule.id}@${rule.version}`;
    if (seenVersions.has(key))
      issues.push(`duplicate rule version "${key}"`);
    seenVersions.add(key);
    if (!ISO_DATE.test(rule.effectiveFrom)) {
      issues.push(`rule "${key}": invalid effectiveFrom "${rule.effectiveFrom}"`);
    }
    if (rule.effectiveTo !== void 0) {
      if (!ISO_DATE.test(rule.effectiveTo)) {
        issues.push(`rule "${key}": invalid effectiveTo "${rule.effectiveTo}"`);
      } else if (rule.effectiveTo <= rule.effectiveFrom) {
        issues.push(`rule "${key}": empty validity window`);
      }
    }
    const list = byId.get(rule.id) ?? [];
    list.push(rule);
    byId.set(rule.id, list);
    if (rule.overrides) {
      const list2 = overriders.get(rule.overrides.ruleId) ?? [];
      list2.push(rule);
      overriders.set(rule.overrides.ruleId, list2);
      if (rule.overrides.priority <= 0) {
        issues.push(`rule "${key}": override priority must be > 0 (base rule has implicit priority 0)`);
      }
      if (!rule.applicability) {
        issues.push(`rule "${key}": an override must declare an applicability guard`);
      }
    }
    validateExpressions(rule, byIdRefChecker(input), factById, issues);
  }
  for (const [id, versions] of byId) {
    const sorted = [...versions].sort((a, b) => a.effectiveFrom.localeCompare(b.effectiveFrom));
    for (let i = 0; i + 1 < sorted.length; i++) {
      const cur = sorted[i];
      const next = sorted[i + 1];
      if (cur.effectiveTo === void 0 || next.effectiveFrom < cur.effectiveTo) {
        issues.push(`rule "${id}": overlapping validity windows (v${cur.version} and v${next.version})`);
      }
    }
  }
  for (const [targetId, list] of overriders) {
    const targets = byId.get(targetId);
    if (!targets) {
      issues.push(`override target "${targetId}" does not exist`);
      continue;
    }
    const targetType = targets[0].output.type;
    const prios = /* @__PURE__ */ new Map();
    for (const rule of list) {
      if (rule.output.type !== targetType) {
        issues.push(`rule "${rule.id}" overrides "${targetId}" but output type ${rule.output.type} != ${targetType}`);
      }
      const p = rule.overrides.priority;
      const existing = prios.get(p);
      if (existing !== void 0 && existing !== rule.id) {
        issues.push(`rules "${existing}" and "${rule.id}" both override "${targetId}" at priority ${p}`);
      }
      prios.set(p, rule.id);
    }
  }
  if (issues.length > 0)
    throw new CorpusValidationError(issues);
  const ruleHashes = /* @__PURE__ */ new Map();
  for (const rule of input.rules) {
    ruleHashes.set(`${rule.id}@${rule.version}`, ruleHash(rule));
  }
  return {
    name: input.name,
    version: input.version,
    rules: input.rules,
    facts: input.facts,
    merkleRoot: merkleRoot(input.rules, input.facts),
    ruleHashes,
    byId,
    factById,
    overriders
  };
}
function byIdRefChecker(input) {
  return new Set(input.rules.map((r) => r.id));
}
function validateExpressions(rule, knownRuleIds, factById, issues) {
  const key = `${rule.id}@${rule.version}`;
  const exprs = [rule.formula];
  if (rule.applicability)
    exprs.push(rule.applicability);
  for (const root of exprs) {
    walkExpr(root, (e) => {
      switch (e.kind) {
        case "add":
        case "min":
        case "max":
        case "and":
        case "or":
          if (e.args.length === 0) {
            issues.push(`rule "${key}": empty ${e.kind} expression`);
          }
          break;
        case "rule":
          if (!knownRuleIds.has(e.ruleId)) {
            issues.push(`rule "${key}" references unknown rule "${e.ruleId}"`);
          }
          break;
        case "fact":
          if (!factById.has(e.factId)) {
            issues.push(`rule "${key}" references unknown fact "${e.factId}"`);
          }
          break;
        case "param":
          if (!rule.parameters || !(e.name in rule.parameters)) {
            issues.push(`rule "${key}" references unknown parameter "${e.name}"`);
          }
          break;
        case "brackets": {
          if (e.table.length === 0) {
            issues.push(`rule "${key}": empty bracket table`);
            break;
          }
          if (e.table[0].threshold !== "0") {
            issues.push(`rule "${key}": first bracket threshold must be "0" (lower bounds)`);
          }
          for (let i = 0; i + 1 < e.table.length; i++) {
            if (BigInt(e.table[i].threshold) >= BigInt(e.table[i + 1].threshold)) {
              issues.push(`rule "${key}": bracket thresholds must be strictly ascending`);
              break;
            }
          }
          for (const row of e.table) {
            if (BigInt(row.rate.den) === 0n) {
              issues.push(`rule "${key}": bracket rate with zero denominator`);
            }
          }
          break;
        }
        default:
          break;
      }
    });
  }
}
function selectVersion(corpus, ruleId, asOf) {
  const versions = corpus.byId.get(ruleId) ?? [];
  const match = versions.find((r) => r.effectiveFrom <= asOf && (r.effectiveTo === void 0 || asOf < r.effectiveTo));
  if (!match) {
    throw new NoApplicableRuleError(ruleId, asOf, versions.map((v) => ({
      version: v.version,
      effectiveFrom: v.effectiveFrom,
      effectiveTo: v.effectiveTo
    })));
  }
  return match;
}
function selectOverriders(corpus, ruleId, asOf) {
  const list = corpus.overriders.get(ruleId) ?? [];
  return list.filter((r) => r.effectiveFrom <= asOf && (r.effectiveTo === void 0 || asOf < r.effectiveTo)).sort((a, b) => b.overrides.priority - a.overrides.priority);
}

// ../core/dist/proof.js
function artifactBody(artifact) {
  const { schemaVersion, engine, corpus, asOf, target, facts: facts2, assumptions, root } = artifact;
  return { schemaVersion, engine, corpus, asOf, target, facts: facts2, assumptions, root };
}
function computeArtifactHash(artifact) {
  return hashOf(artifactBody(artifact));
}

// ../core/dist/evaluate.js
var ENGINE = { name: "@invaro/opentax-core", version: "0.1.0" };
var POISON = /* @__PURE__ */ Symbol("missing-facts");
function evaluate(corpus, facts2, options) {
  if (!/^\d{4}-\d{2}-\d{2}$/.test(options.asOf)) {
    throw new FactValidationError(`invalid asOf date "${options.asOf}"`);
  }
  const resolved = /* @__PURE__ */ new Map();
  for (const [factId, tv] of Object.entries(facts2)) {
    const spec = corpus.factById.get(factId);
    if (!spec)
      throw new FactValidationError(`unknown fact "${factId}"`);
    const value2 = valueFromJSON(tv);
    if (value2.type !== spec.type) {
      throw new FactValidationError(`fact "${factId}" must be ${spec.type}, got ${value2.type}`);
    }
    if (value2.type === "enum" && spec.enumValues && !spec.enumValues.includes(value2.value)) {
      throw new FactValidationError(`fact "${factId}": "${value2.value}" is not one of ${spec.enumValues.join("|")}`);
    }
    if (value2.type === "money" || value2.type === "int") {
      const n = value2.type === "money" ? value2.cents : value2.value;
      if (spec.min !== void 0 && n < BigInt(spec.min)) {
        throw new FactValidationError(`fact "${factId}": ${n} is below the minimum ${spec.min}`);
      }
      if (spec.max !== void 0 && n > BigInt(spec.max)) {
        throw new FactValidationError(`fact "${factId}": ${n} is above the maximum ${spec.max}`);
      }
    }
    resolved.set(factId, value2);
  }
  const ctx = {
    corpus,
    facts: resolved,
    factsJSON: facts2,
    asOf: options.asOf,
    memo: /* @__PURE__ */ new Map(),
    visiting: [],
    assumptions: [],
    stubs: /* @__PURE__ */ new Map(),
    assumedFactIds: /* @__PURE__ */ new Set(),
    missing: /* @__PURE__ */ new Map(),
    factNodes: /* @__PURE__ */ new Map()
  };
  const { value, node } = evalRule(options.target, ctx);
  if (value === POISON || node === null) {
    throw new NeedsFactsError([...ctx.missing.values()]);
  }
  const body = {
    schemaVersion: "2",
    engine: { ...ENGINE },
    corpus: {
      name: corpus.name,
      version: corpus.version,
      merkleRoot: corpus.merkleRoot
    },
    asOf: options.asOf,
    target: options.target,
    facts: facts2,
    assumptions: ctx.assumptions,
    root: node
  };
  const proof = { ...body, artifactHash: computeArtifactHash(body) };
  return { value, proof };
}
function evalRule(ruleId, ctx) {
  const hit = ctx.memo.get(ruleId);
  if (hit) {
    if (hit.node && hit.node.children?.length) {
      let stub = ctx.stubs.get(ruleId);
      if (!stub) {
        const { children: _omit, ...rest } = hit.node;
        stub = rest;
        ctx.stubs.set(ruleId, stub);
      }
      return { value: hit.value, node: stub };
    }
    return hit;
  }
  if (ctx.visiting.includes(ruleId)) {
    throw new CycleError([...ctx.visiting, ruleId]);
  }
  ctx.visiting.push(ruleId);
  try {
    const base = selectVersion(ctx.corpus, ruleId, ctx.asOf);
    const overs = selectOverriders(ctx.corpus, ruleId, ctx.asOf);
    const children = [];
    const candidates = [];
    let winner = null;
    let poisoned = false;
    for (const cand of overs) {
      const guard = evalExpr(cand.applicability, cand, ctx, children);
      if (guard === POISON) {
        candidates.push(candidateEntry(cand, "unknown"));
        poisoned = true;
        break;
      }
      expectType(guard, "bool", cand, "applicability");
      if (guard.value) {
        candidates.push(candidateEntry(cand, true));
        winner = cand;
        break;
      }
      candidates.push(candidateEntry(cand, false));
    }
    if (!winner && !poisoned) {
      if (base.applicability) {
        const guard = evalExpr(base.applicability, base, ctx, children);
        if (guard === POISON) {
          candidates.push({ ...candidateEntry(base, "unknown"), priority: 0 });
          poisoned = true;
        } else {
          expectType(guard, "bool", base, "applicability");
          if (!guard.value) {
            throw new NoApplicableRuleError(ruleId, ctx.asOf, [
              {
                version: base.version,
                effectiveFrom: base.effectiveFrom,
                effectiveTo: base.effectiveTo
              }
            ]);
          }
          candidates.push({ ...candidateEntry(base, true), priority: 0 });
          winner = base;
        }
      } else {
        if (overs.length > 0) {
          candidates.push({ ...candidateEntry(base, true), priority: 0 });
        }
        winner = base;
      }
    }
    let value = POISON;
    if (winner) {
      value = evalExpr(winner.formula, winner, ctx, children);
    }
    if (value === POISON || winner === null) {
      const entry2 = { value: POISON, node: null };
      ctx.memo.set(ruleId, entry2);
      return entry2;
    }
    if (value.type !== winner.output.type) {
      throw new EngineError(`rule "${winner.id}" declares output ${winner.output.type} but produced ${value.type}`);
    }
    const inputs = {};
    for (const child of children) {
      const key = child.kind === "rule" ? child.ruleId : child.factId;
      inputs[key] = child.value;
    }
    const node = {
      kind: "rule",
      ruleId: winner.id,
      ruleVersion: winner.version,
      ruleHash: ctx.corpus.ruleHashes.get(`${winner.id}@${winner.version}`),
      title: winner.title,
      citation: winner.citation,
      ...winner.id !== ruleId ? { resolvedFrom: ruleId } : {},
      ...overs.length > 0 ? { candidatesConsidered: candidates } : {},
      inputs,
      value: valueToJSON(value),
      children
    };
    const entry = { value, node };
    ctx.memo.set(ruleId, entry);
    return entry;
  } finally {
    ctx.visiting.pop();
  }
}
function candidateEntry(rule, applicable) {
  return {
    ruleId: rule.id,
    version: rule.version,
    priority: rule.overrides?.priority ?? 0,
    applicable
  };
}
function evalExpr(expr, rule, ctx, children) {
  switch (expr.kind) {
    case "money":
      return { type: "money", cents: parseCents(expr.cents) };
    case "int":
      return { type: "int", value: parseInt_(expr.value) };
    case "bool":
      return { type: "bool", value: expr.value };
    case "enum":
      return { type: "enum", value: expr.value };
    case "fact":
      return resolveFact(expr.factId, ctx, children);
    case "rule": {
      const { value, node } = evalRule(expr.ruleId, ctx);
      if (node && !children.includes(node))
        children.push(node);
      return value;
    }
    case "param": {
      const param22 = rule.parameters?.[expr.name];
      if (!param22) {
        throw new EngineError(`rule "${rule.id}" has no parameter "${expr.name}"`);
      }
      return valueFromJSON({ type: param22.type, value: param22.value });
    }
    case "add": {
      const vals = evalAll(expr.args, rule, ctx, children);
      if (vals === POISON)
        return POISON;
      return numericFold(vals, rule, "add", (acc, v) => acc + v);
    }
    case "sub": {
      const vals = evalAll([expr.left, expr.right], rule, ctx, children);
      if (vals === POISON)
        return POISON;
      return numericFold(vals, rule, "sub", (acc, v) => acc - v);
    }
    case "mulRate": {
      const base = evalExpr(expr.base, rule, ctx, children);
      if (base === POISON)
        return POISON;
      expectType(base, "money", rule, "mulRate.base");
      const rate2 = {
        num: parseInt_(expr.rate.num),
        den: parseInt_(expr.rate.den)
      };
      return {
        type: "money",
        cents: mulRate(base.cents, rate2, expr.round)
      };
    }
    case "mulInt": {
      const vals = evalAll([expr.base, expr.count], rule, ctx, children);
      if (vals === POISON)
        return POISON;
      const [base, count] = vals;
      expectType(base, "money", rule, "mulInt.base");
      expectType(count, "int", rule, "mulInt.count");
      return {
        type: "money",
        cents: base.cents * count.value
      };
    }
    case "min":
    case "max": {
      const vals = evalAll(expr.args, rule, ctx, children);
      if (vals === POISON)
        return POISON;
      const pick = expr.kind === "min" ? (a, b) => b < a ? b : a : (a, b) => b > a ? b : a;
      return numericFold(vals, rule, expr.kind, pick);
    }
    case "max0": {
      const v = evalExpr(expr.arg, rule, ctx, children);
      if (v === POISON)
        return POISON;
      expectType(v, "money", rule, "max0");
      return { type: "money", cents: max0(v.cents) };
    }
    case "clamp": {
      const vals = evalAll([expr.value, expr.lo, expr.hi], rule, ctx, children);
      if (vals === POISON)
        return POISON;
      const [v, lo, hi] = vals.map((x) => {
        expectType(x, "money", rule, "clamp");
        return x.cents;
      });
      const clamped = v < lo ? lo : v > hi ? hi : v;
      return { type: "money", cents: clamped };
    }
    case "roundToDollar": {
      const v = evalExpr(expr.value, rule, ctx, children);
      if (v === POISON)
        return POISON;
      expectType(v, "money", rule, "roundToDollar");
      return {
        type: "money",
        cents: roundToDollar(v.cents, expr.mode)
      };
    }
    case "mulDiv": {
      const vals = evalAll([expr.a, expr.b, expr.c], rule, ctx, children);
      if (vals === POISON)
        return POISON;
      const [a, b, c2] = vals.map((x) => {
        expectType(x, "money", rule, "mulDiv");
        return x.cents;
      });
      if (c2 === 0n) {
        throw new EngineError(`rule "${rule.id}": mulDiv division by zero`);
      }
      return { type: "money", cents: divRound(a * b, c2, expr.round) };
    }
    case "unsupported":
      throw new NotModeledError(rule.id, expr.reason);
    case "stepUnits": {
      const v = evalExpr(expr.value, rule, ctx, children);
      if (v === POISON)
        return POISON;
      expectType(v, "money", rule, "stepUnits");
      return {
        type: "int",
        value: stepUnits(v.cents, parseCents(expr.unitCents), expr.mode)
      };
    }
    case "cmp": {
      const vals = evalAll([expr.left, expr.right], rule, ctx, children);
      if (vals === POISON)
        return POISON;
      const [l, r] = vals;
      if (l.type !== r.type) {
        throw new EngineError(`rule "${rule.id}": cmp between ${l.type} and ${r.type}`);
      }
      if (expr.op === "eq" || expr.op === "ne") {
        const eq = canonicalScalar(l) === canonicalScalar(r);
        return { type: "bool", value: expr.op === "eq" ? eq : !eq };
      }
      const ln = numericOf(l, rule, "cmp");
      const rn = numericOf(r, rule, "cmp");
      const result = expr.op === "lt" ? ln < rn : expr.op === "le" ? ln <= rn : expr.op === "gt" ? ln > rn : ln >= rn;
      return { type: "bool", value: result };
    }
    case "and":
    case "or": {
      const decider = expr.kind === "and" ? false : true;
      let sawPoison = false;
      for (const arg of expr.args) {
        const v = evalExpr(arg, rule, ctx, children);
        if (v === POISON) {
          sawPoison = true;
          continue;
        }
        expectType(v, "bool", rule, expr.kind);
        if (v.value === decider) {
          return { type: "bool", value: decider };
        }
      }
      if (sawPoison)
        return POISON;
      return { type: "bool", value: !decider };
    }
    case "not": {
      const v = evalExpr(expr.arg, rule, ctx, children);
      if (v === POISON)
        return POISON;
      expectType(v, "bool", rule, "not");
      return { type: "bool", value: !v.value };
    }
    case "if": {
      const cond = evalExpr(expr.cond, rule, ctx, children);
      if (cond === POISON)
        return POISON;
      expectType(cond, "bool", rule, "if.cond");
      return evalExpr(cond.value ? expr.then : expr.else, rule, ctx, children);
    }
    case "brackets": {
      const base = evalExpr(expr.base, rule, ctx, children);
      if (base === POISON)
        return POISON;
      expectType(base, "money", rule, "brackets.base");
      const amount = base.cents;
      const exact = expr.accumulate === "exact";
      let tax = 0n;
      let exactNum = 0n;
      let exactDen = 1n;
      for (let i = 0; i < expr.table.length; i++) {
        const lower = parseCents(expr.table[i].threshold);
        if (amount <= lower)
          break;
        const upper = i + 1 < expr.table.length ? parseCents(expr.table[i + 1].threshold) : amount;
        const span = (amount < upper ? amount : upper) - lower;
        const rate2 = {
          num: parseInt_(expr.table[i].rate.num),
          den: parseInt_(expr.table[i].rate.den)
        };
        if (exact) {
          if (rate2.den === 0n)
            throw new RangeError("rate with zero denominator");
          exactNum = exactNum * rate2.den + span * rate2.num * exactDen;
          exactDen *= rate2.den;
        } else {
          tax += mulRate(span, rate2, "half-up");
        }
      }
      if (exact) {
        tax = divRound(exactNum, exactDen, "half-up");
      }
      return { type: "money", cents: tax };
    }
    case "match": {
      const on = evalExpr(expr.on, rule, ctx, children);
      if (on === POISON)
        return POISON;
      expectType(on, "enum", rule, "match.on");
      const tag = on.value;
      const found = expr.cases.find((c2) => c2.when === tag);
      if (found)
        return evalExpr(found.value, rule, ctx, children);
      if (expr.else)
        return evalExpr(expr.else, rule, ctx, children);
      throw new UnhandledEnumCaseError(rule.id, tag);
    }
  }
}
function resolveFact(factId, ctx, children) {
  const spec = ctx.corpus.factById.get(factId);
  if (!spec)
    throw new EngineError(`unknown fact "${factId}"`);
  let node = ctx.factNodes.get(factId);
  if (!node) {
    const given = ctx.facts.get(factId);
    if (given !== void 0) {
      node = { kind: "fact", factId, value: valueToJSON(given) };
    } else if (spec.default !== void 0) {
      const value = valueFromJSON({ type: spec.type, value: spec.default.value });
      if (!ctx.assumedFactIds.has(factId)) {
        ctx.assumedFactIds.add(factId);
        ctx.assumptions.push({
          factId,
          value: valueToJSON(value),
          source: "default",
          rationale: spec.default.rationale
        });
      }
      ctx.facts.set(factId, value);
      node = { kind: "assumption", factId, value: valueToJSON(value) };
    } else {
      ctx.missing.set(factId, {
        factId,
        type: spec.type,
        enumValues: spec.enumValues,
        description: spec.description
      });
      return POISON;
    }
    ctx.factNodes.set(factId, node);
  }
  if (!children.includes(node))
    children.push(node);
  return ctx.facts.get(factId);
}
function evalAll(exprs, rule, ctx, children) {
  const out = [];
  let poisoned = false;
  for (const e of exprs) {
    const v = evalExpr(e, rule, ctx, children);
    if (v === POISON)
      poisoned = true;
    else
      out.push(v);
  }
  return poisoned ? POISON : out;
}
function numericFold(vals, rule, op, fold) {
  if (vals.length === 0)
    throw new EngineError(`rule "${rule.id}": empty ${op}`);
  const type = vals[0].type;
  if (type !== "money" && type !== "int") {
    throw new EngineError(`rule "${rule.id}": ${op} over ${type}`);
  }
  let acc = numericOf(vals[0], rule, op);
  for (const v of vals.slice(1)) {
    if (v.type !== type) {
      throw new EngineError(`rule "${rule.id}": ${op} mixes ${type} and ${v.type}`);
    }
    acc = fold(acc, numericOf(v, rule, op));
  }
  return type === "money" ? { type: "money", cents: acc } : { type: "int", value: acc };
}
function numericOf(v, rule, op) {
  if (v.type === "money")
    return v.cents;
  if (v.type === "int")
    return v.value;
  throw new EngineError(`rule "${rule.id}": ${op} expects money|int, got ${v.type}`);
}
function canonicalScalar(v) {
  switch (v.type) {
    case "money":
      return `m:${v.cents}`;
    case "int":
      return `i:${v.value}`;
    case "bool":
      return `b:${v.value}`;
    case "enum":
      return `e:${v.value}`;
  }
}
function expectType(v, type, rule, where) {
  if (v.type !== type) {
    throw new EngineError(`rule "${rule.id}": ${where} expects ${type}, got ${v.type}`);
  }
}
function coerceFacts(corpus, plain) {
  const out = {};
  for (const [factId, raw] of Object.entries(plain)) {
    const spec = corpus.factById.get(factId);
    if (!spec) {
      const known = [...corpus.factById.keys()].join(", ");
      throw new FactValidationError(`unknown fact "${factId}" (known facts: ${known})`);
    }
    switch (spec.type) {
      case "money": {
        if (typeof raw !== "string" && typeof raw !== "number") {
          throw new FactValidationError(`fact "${factId}" must be a dollar amount like 50000 or "1234.56"`);
        }
        try {
          out[factId] = { type: "money", value: parseDollars(raw).toString() };
        } catch (err) {
          throw new FactValidationError(`fact "${factId}": ${err.message}`);
        }
        break;
      }
      case "int": {
        let s;
        if (typeof raw === "string" && /^-?\d+$/.test(raw))
          s = raw;
        else if (typeof raw === "number" && Number.isSafeInteger(raw)) {
          s = String(raw);
        } else {
          throw new FactValidationError(`fact "${factId}" must be a whole number`);
        }
        out[factId] = { type: "int", value: s };
        break;
      }
      case "bool":
        if (typeof raw !== "boolean") {
          throw new FactValidationError(`fact "${factId}" must be true or false`);
        }
        out[factId] = { type: "bool", value: raw };
        break;
      case "enum":
        if (typeof raw !== "string") {
          throw new FactValidationError(`fact "${factId}" must be one of ${spec.enumValues?.join("|")}`);
        }
        out[factId] = { type: "enum", value: raw };
        break;
    }
  }
  return out;
}

// ../core/dist/verify.js
function verifyProof(artifact, corpus) {
  if (artifact?.schemaVersion !== "2" || typeof artifact.artifactHash !== "string" || typeof artifact.target !== "string" || typeof artifact.asOf !== "string" || typeof artifact.engine?.name !== "string" || typeof artifact.corpus?.merkleRoot !== "string" || typeof artifact.facts !== "object" || artifact.facts === null || !Array.isArray(artifact.assumptions) || artifact.root == null || typeof artifact.root !== "object") {
    return {
      ok: false,
      reason: "invalid-artifact",
      message: "artifact is missing required fields or has an unknown schema version"
    };
  }
  let recomputedHash;
  try {
    recomputedHash = computeArtifactHash(artifact);
  } catch (err) {
    return {
      ok: false,
      reason: "invalid-artifact",
      message: `artifact cannot be canonicalized: ${err instanceof Error ? err.message : String(err)}`
    };
  }
  if (recomputedHash !== artifact.artifactHash) {
    return {
      ok: false,
      reason: "artifact-hash-mismatch",
      message: "artifact content does not match its own hash \u2014 the file was altered",
      expected: artifact.artifactHash,
      actual: recomputedHash
    };
  }
  if (artifact.corpus.merkleRoot !== corpus.merkleRoot) {
    return {
      ok: false,
      reason: "corpus-mismatch",
      message: "proof was computed against a different corpus version (Merkle root mismatch)",
      expected: artifact.corpus.merkleRoot,
      actual: corpus.merkleRoot
    };
  }
  let recomputed;
  try {
    recomputed = evaluate(corpus, artifact.facts, {
      asOf: artifact.asOf,
      target: artifact.target
    });
  } catch (err) {
    return {
      ok: false,
      reason: "evaluation-error",
      message: `re-execution failed: ${err instanceof Error ? err.message : String(err)}`
    };
  }
  if (canonical(recomputed.proof.root) !== canonical(artifact.root)) {
    const path = findDivergence(artifact.root, recomputed.proof.root, "root");
    return {
      ok: false,
      reason: "divergence",
      message: `re-execution diverges from the recorded derivation at ${path ?? "root"}`,
      path: path ?? "root"
    };
  }
  if (canonical(recomputed.proof.assumptions) !== canonical(artifact.assumptions)) {
    return {
      ok: false,
      reason: "divergence",
      message: "re-execution produced different assumptions than recorded",
      path: "assumptions"
    };
  }
  return {
    ok: true,
    target: artifact.target,
    value: artifact.root.value.value,
    corpusMerkleRoot: corpus.merkleRoot
  };
}
function findDivergence(a, b, path) {
  if (canonical(a) === canonical(b))
    return null;
  const aKids = a.children ?? [];
  const bKids = b.children ?? [];
  const { children: _a, ...aSelf } = a;
  const { children: _b, ...bSelf } = b;
  if (canonical(aSelf) !== canonical(bSelf))
    return path;
  if (aKids.length !== bKids.length)
    return path;
  for (let i = 0; i < aKids.length; i++) {
    const label = aKids[i].ruleId ?? aKids[i].factId ?? String(i);
    const divergence = findDivergence(aKids[i], bKids[i], `${path}/${label}`);
    if (divergence)
      return divergence;
  }
  return path;
}

// ../corpus-us-federal/dist/occupations.js
var cat = {
  food: "Beverage and Food Service (100s)",
  entertainment: "Entertainment and Events (200s)",
  hospitality: "Hospitality and Guest Services (300s)",
  home: "Home Services (400s)",
  personal: "Personal Services (500s)",
  appearance: "Personal Appearance and Wellness (600s)",
  recreation: "Recreation and Instruction (700s)",
  transport: "Transportation and Delivery (800s)"
};
var slugify = (name) => name.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
function occ(code, name, category) {
  return { code, name, slug: slugify(name), category };
}
var TIPPED_OCCUPATIONS = [
  // Beverage and Food Service (100s)
  occ(101, "Bartenders", cat.food),
  occ(102, "Wait Staff", cat.food),
  occ(103, "Food Servers, Nonrestaurant", cat.food),
  occ(104, "Dining Room and Cafeteria Attendants and Bartender Helpers", cat.food),
  occ(105, "Chefs and Cooks", cat.food),
  occ(106, "Food Preparation Workers", cat.food),
  occ(107, "Fast Food and Counter Workers", cat.food),
  occ(108, "Dishwashers", cat.food),
  occ(109, "Host Staff, Restaurant, Lounge, and Coffee Shop", cat.food),
  occ(110, "Bakers", cat.food),
  // Entertainment and Events (200s)
  occ(201, "Gambling Dealers", cat.entertainment),
  occ(202, "Gambling Change Persons and Booth Cashiers", cat.entertainment),
  occ(203, "Gambling Cage Workers", cat.entertainment),
  occ(204, "Gambling and Sports Book Writers and Runners", cat.entertainment),
  occ(205, "Dancers", cat.entertainment),
  occ(206, "Musicians and Singers", cat.entertainment),
  occ(207, "Disc Jockeys, Except Radio", cat.entertainment),
  occ(208, "Entertainers and Performers", cat.entertainment),
  occ(209, "Digital Content Creators", cat.entertainment),
  occ(210, "Ushers, Lobby Attendants, and Ticket Takers", cat.entertainment),
  occ(211, "Locker Room, Coatroom, and Dressing Room Attendants", cat.entertainment),
  // Hospitality and Guest Services (300s)
  occ(301, "Baggage Porters and Bellhops", cat.hospitality),
  occ(302, "Concierges", cat.hospitality),
  occ(303, "Hotel, Motel, and Resort Desk Clerks", cat.hospitality),
  occ(304, "Maids and Housekeeping Cleaners", cat.hospitality),
  // Home Services (400s)
  occ(401, "Home Maintenance and Repair Workers", cat.home),
  occ(402, "Home Landscaping and Groundskeeping Workers", cat.home),
  occ(403, "Home Electricians", cat.home),
  occ(404, "Home Plumbers", cat.home),
  occ(405, "Home Heating and Air Conditioning Mechanics and Installers", cat.home),
  occ(406, "Home Appliance Installers and Repairers", cat.home),
  occ(407, "Home Cleaning Service Workers", cat.home),
  occ(408, "Locksmiths", cat.home),
  occ(409, "Roadside Assistance Workers", cat.home),
  // Personal Services (500s)
  occ(501, "Personal Care and Service Workers", cat.personal),
  occ(502, "Private Event Planners", cat.personal),
  occ(503, "Private Event and Portrait Photographers", cat.personal),
  occ(504, "Private Event Videographers", cat.personal),
  occ(505, "Event Officiants", cat.personal),
  occ(506, "Pet Caretakers", cat.personal),
  occ(507, "Tutors", cat.personal),
  occ(508, "Nannies and Babysitters", cat.personal),
  occ(null, "Visual Artists", cat.personal),
  // added by the final rule (IR-2026-49)
  occ(null, "Floral Designers", cat.personal),
  // added by the final rule (IR-2026-49)
  // Personal Appearance and Wellness (600s)
  occ(601, "Skincare Specialists", cat.appearance),
  occ(602, "Massage Therapists", cat.appearance),
  occ(603, "Barbers, Hairdressers, Hairstylists, and Cosmetologists", cat.appearance),
  occ(604, "Shampooers", cat.appearance),
  occ(605, "Manicurists and Pedicurists", cat.appearance),
  occ(606, "Makeup Artists", cat.appearance),
  occ(607, "Exercise Trainers and Group Fitness Instructors", cat.appearance),
  occ(608, "Tattoo Artists and Piercers", cat.appearance),
  occ(609, "Tailors", cat.appearance),
  occ(610, "Shoe and Leather Workers and Repairers", cat.appearance),
  occ(611, "Eyebrow Threading and Waxing Technicians", cat.appearance),
  // Recreation and Instruction (700s)
  occ(701, "Golf Caddies", cat.recreation),
  occ(702, "Self-Enrichment Teachers", cat.recreation),
  occ(703, "Sports and Recreation Instructors", cat.recreation),
  occ(704, "Tour Guides", cat.recreation),
  occ(705, "Travel Guides", cat.recreation),
  occ(706, "Recreational and Tour Pilots", cat.recreation),
  // Transportation and Delivery (800s)
  occ(801, "Parking and Valet Attendants", cat.transport),
  occ(802, "Taxi and Rideshare Drivers and Chauffeurs", cat.transport),
  occ(803, "Shuttle Drivers", cat.transport),
  occ(804, "Goods Delivery People", cat.transport),
  occ(805, "Personal Vehicle and Equipment Cleaners", cat.transport),
  occ(806, "Private and Charter Bus Drivers", cat.transport),
  occ(807, "Water Taxi Operators and Charter Boat Workers", cat.transport),
  occ(808, "Rickshaw, Pedicab, and Carriage Drivers", cat.transport),
  occ(809, "Home Movers", cat.transport),
  occ(null, "Gas Pump Attendants", cat.transport)
  // added by the final rule (IR-2026-49)
];
var OCCUPATION_ENUM = [
  ...TIPPED_OCCUPATIONS.map((o) => o.slug),
  "other"
];
var ALIASES = {
  dj: "disc-jockeys-except-radio",
  "disc-jockey": "disc-jockeys-except-radio",
  server: "wait-staff",
  waiter: "wait-staff",
  waitress: "wait-staff",
  cook: "chefs-and-cooks",
  chef: "chefs-and-cooks",
  barista: "fast-food-and-counter-workers",
  hairdresser: "barbers-hairdressers-hairstylists-and-cosmetologists",
  hairstylist: "barbers-hairdressers-hairstylists-and-cosmetologists",
  barber: "barbers-hairdressers-hairstylists-and-cosmetologists",
  cosmetologist: "barbers-hairdressers-hairstylists-and-cosmetologists",
  "nail-tech": "manicurists-and-pedicurists",
  "nail-technician": "manicurists-and-pedicurists",
  masseuse: "massage-therapists",
  "massage-therapist": "massage-therapists",
  "uber-driver": "taxi-and-rideshare-drivers-and-chauffeurs",
  "lyft-driver": "taxi-and-rideshare-drivers-and-chauffeurs",
  "rideshare-driver": "taxi-and-rideshare-drivers-and-chauffeurs",
  "taxi-driver": "taxi-and-rideshare-drivers-and-chauffeurs",
  chauffeur: "taxi-and-rideshare-drivers-and-chauffeurs",
  valet: "parking-and-valet-attendants",
  bellhop: "baggage-porters-and-bellhops",
  porter: "baggage-porters-and-bellhops",
  nanny: "nannies-and-babysitters",
  babysitter: "nannies-and-babysitters",
  caddy: "golf-caddies",
  caddie: "golf-caddies",
  "delivery-driver": "goods-delivery-people",
  doordash: "goods-delivery-people",
  "content-creator": "digital-content-creators",
  influencer: "digital-content-creators",
  streamer: "digital-content-creators",
  concierge: "concierges",
  housekeeper: "maids-and-housekeeping-cleaners",
  maid: "maids-and-housekeeping-cleaners",
  "tattoo-artist": "tattoo-artists-and-piercers",
  "personal-trainer": "exercise-trainers-and-group-fitness-instructors",
  "fitness-instructor": "exercise-trainers-and-group-fitness-instructors",
  "tour-guide": "tour-guides",
  tutor: "tutors",
  musician: "musicians-and-singers",
  singer: "musicians-and-singers",
  dancer: "dancers",
  photographer: "private-event-and-portrait-photographers",
  florist: "floral-designers",
  "gas-station-attendant": "gas-pump-attendants"
};
var stem = (t) => t.replace(/ies$/, "i").replace(/es$/, "").replace(/s$/, "").replace(/y$/, "i");
function matchOccupation(input) {
  const norm = slugify(input);
  if (norm === "other")
    return { slug: "other" };
  if (ALIASES[norm])
    return { slug: ALIASES[norm] };
  const exact = TIPPED_OCCUPATIONS.find((o) => o.slug === norm);
  if (exact)
    return { slug: exact.slug };
  const inputStems = norm.split("-").filter((t) => t.length > 1).map(stem);
  const hits = TIPPED_OCCUPATIONS.filter((o) => {
    if (o.slug.includes(norm))
      return true;
    const slugStems = o.slug.split("-").map(stem);
    return inputStems.length > 0 && inputStems.every((it) => slugStems.some((st) => st === it || st.startsWith(it)));
  });
  if (hits.length === 1)
    return { slug: hits[0].slug };
  return { candidates: hits.map((o) => o.slug) };
}

// ../corpus-us-federal/dist/facts.js
var FILING_STATUSES = [
  "single",
  "mfj",
  // married filing jointly
  "mfs",
  // married filing separately
  "hoh",
  // head of household
  "qss"
  // qualifying surviving spouse
];
var facts = [
  {
    id: "filingStatus",
    type: "enum",
    enumValues: [...FILING_STATUSES],
    description: "Filing status (26 U.S.C. \xA7 1, \xA7 2). One of: single, mfj, mfs, hoh, qss."
    // no default — the engine must never guess a filing status
  },
  {
    id: "wages",
    type: "money",
    min: "0",
    description: 'Wages, salaries, tips (Form W-2 box 1), in dollars (e.g. 50000 or "1234.56").'
    // no default — the engine must never guess income
  },
  {
    id: "taxableInterest",
    type: "money",
    min: "0",
    description: "Taxable interest income, in dollars.",
    default: {
      value: "0",
      rationale: "Assumed no taxable interest absent contrary input"
    }
  },
  {
    id: "isAge65OrOlder",
    type: "bool",
    description: "Taxpayer attained age 65 before the close of the taxable year (26 U.S.C. \xA7 63(f)(1)).",
    default: {
      value: false,
      rationale: "Assumed under 65 absent contrary input"
    }
  },
  {
    id: "isBlind",
    type: "bool",
    description: "Taxpayer is blind at the close of the taxable year (26 U.S.C. \xA7 63(f)(2)).",
    default: { value: false, rationale: "Assumed not blind absent contrary input" }
  },
  {
    id: "spouseIsAge65OrOlder",
    type: "bool",
    description: "Spouse attained age 65 before the close of the taxable year.",
    default: {
      value: false,
      rationale: "Assumed spouse under 65 absent contrary input"
    }
  },
  {
    id: "spouseIsBlind",
    type: "bool",
    description: "Spouse is blind at the close of the taxable year.",
    default: {
      value: false,
      rationale: "Assumed spouse not blind absent contrary input"
    }
  },
  {
    id: "isClaimedAsDependent",
    type: "bool",
    description: "Taxpayer can be claimed as a dependent on another taxpayer's return (26 U.S.C. \xA7 63(c)(5)).",
    default: {
      value: false,
      rationale: "Assumed not claimable as a dependent absent contrary input"
    }
  },
  {
    id: "qualifyingChildren",
    type: "int",
    min: "0",
    description: "Number of qualifying children under age 17 with required SSNs (26 U.S.C. \xA7 24(c), (h)(7)).",
    default: {
      value: "0",
      rationale: "Assumed no qualifying children absent contrary input"
    }
  },
  {
    id: "foreignEarnedIncome",
    type: "money",
    min: "0",
    description: "Foreign earned income qualifying for the \xA7 911 exclusion \u2014 ATTESTS a foreign tax home plus \xA7 911(d)(1) qualification (bona fide residence for an entire taxable year, or 330 full days abroad in 12 months). Include these earnings in the wages/SE inputs too; the exclusion is computed and subtracted. The \xA7 911(c) housing exclusion is not modeled. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no foreign earned income absent contrary input"
    }
  },
  {
    id: "fiduciaryType",
    type: "enum",
    enumValues: ["estate", "simple-trust", "complex-trust"],
    description: "Form 1041 filer type for the \xA7 642(b) exemption: estate ($600), simple trust required to distribute all income currently ($300), or complex trust ($100). Grantor trusts do not file their own tax \u2014 use the grantor's individual return."
    // no default — the engine must never guess the fiduciary type
  },
  {
    id: "fiduciaryIncomeBeforeExemption",
    type: "money",
    min: "0",
    description: "The estate/trust's taxable income BEFORE the \xA7 642(b) exemption but AFTER the \xA7\xA7 651/661 income-distribution deduction (the DNI machinery is attested by this input). In dollars."
    // no default — the engine must never guess fiduciary income
  },
  {
    id: "fiduciaryLongTermGains",
    type: "money",
    min: "0",
    description: "Net long-term capital gain retained by the estate/trust. Any positive amount REFUSES \u2014 the \xA7 1(h) preferential breakpoints for estates and trusts are not modeled. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no retained capital gains absent contrary input"
    }
  },
  {
    id: "householdEmployeeCashWages",
    type: "money",
    min: "0",
    description: "Cash wages paid to ONE household employee this year (Schedule H; nanny, housekeeper, caregiver). The \xA7 3121(b)(3) family exclusions (spouse, child under 21, certain parents, under-18 students) must not be entered. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no household employees absent contrary input"
    }
  },
  {
    id: "householdFutaTestMet",
    type: "bool",
    description: "Total cash wages of $1,000 or more were paid to household employees in some calendar quarter of this or the preceding year (\xA7 3306(c)(2)) AND all state unemployment contributions were paid on time (the full 5.4% FUTA credit) \u2014 attested.",
    default: {
      value: false,
      rationale: "Assumed the FUTA quarter test not met absent contrary input"
    }
  },
  {
    id: "isFarmerOrFisherman",
    type: "bool",
    description: "Gross income from farming or fishing (including oyster farming) is at least 66\u2154% of total gross income for this year or the preceding year (\xA7 6654(i)(2)) \u2014 attested. Switches the estimated-tax safe harbor to 66\u2154% with a single January-15 installment.",
    default: {
      value: false,
      rationale: "Assumed not a qualifying farmer/fisherman absent contrary input"
    }
  },
  {
    id: "homeOfficeSquareFeet",
    type: "int",
    min: "0",
    description: "Square footage of the home used regularly and exclusively for business (\xA7 280A(c)(1) tests attested) \u2014 for the Rev. Proc. 2013-13 simplified method ($5/sq ft, 300 sq ft cap), exposed as a standalone target.",
    default: {
      value: "0",
      rationale: "Assumed no home office absent contrary input"
    }
  },
  {
    id: "corpIsREITorRIC",
    type: "bool",
    description: "The corporation is a real estate investment trust (\xA7 856) or regulated investment company (\xA7 851). REFUSES \u2014 their dividends-paid deduction and distribution requirements are not modeled.",
    default: {
      value: false,
      rationale: "Assumed not a REIT/RIC absent contrary input"
    }
  },
  {
    id: "taxExemptInterest",
    type: "money",
    min: "0",
    description: "Tax-exempt interest (e.g. municipal bonds, Form 1040 line 2a) \u2014 excluded from gross income but counted in the \xA7 86 social-security provisional income and the \xA7 36B premium-tax-credit MAGI. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no tax-exempt interest absent contrary input"
    }
  },
  {
    id: "slcspAnnualPremium",
    type: "money",
    min: "0",
    description: "Annual premium of the second-lowest-cost silver plan (SLCSP) benchmark for the taxpayer's coverage family (Form 1095-A line 33B) \u2014 the \xA7 36B(b)(3)(B) applicable benchmark. Leave 0 if no Marketplace coverage. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no Marketplace coverage absent contrary input"
    }
  },
  {
    id: "marketplacePremiumsPaid",
    type: "money",
    min: "0",
    description: "Annual premiums for the Marketplace qualified health plan(s) actually enrolled in (Form 1095-A line 33A) \u2014 the \xA7 36B(b)(2)(A) ceiling on the premium assistance amount. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no Marketplace premiums absent contrary input"
    }
  },
  {
    id: "advancePTC",
    type: "money",
    min: "0",
    description: "Advance premium tax credit payments made to the insurer during the year (Form 1095-A line 33C) \u2014 reconciled under \xA7 36B(f). MFS RECONCILIATION (\xA7 36B(c)(1)(C)): an MFS filer with no domestic-abuse/abandonment exception is NOT an applicable taxpayer \u2014 PTC = $0 and ALL advance payments are additional tax (Schedule 2 line 2; if the PTC targets refuse the MFS case, add the repayment to line 17 yourself and disclose). The Form 8962 line 28 repayment limitation applies ONLY when household income is under 400% FPL (Table 5 amounts by filing status); at 400% or more line 28 is blank \u2014 repayment is UNCAPPED (2025 Form 8962 instructions, verified). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no advance PTC payments absent contrary input"
    }
  },
  {
    id: "ptcHouseholdSize",
    type: "int",
    min: "1",
    description: "Tax family size for the \xA7 36B poverty-line lookup (\xA7 36B(d)(1): the taxpayer, spouse on a joint return, and dependents). Required when claiming the premium tax credit."
    // no default — the poverty line must never be guessed
  },
  {
    id: "otherDependents",
    type: "int",
    min: "0",
    description: "Number of dependents (\xA7 152) OTHER than CTC qualifying children \u2014 e.g. children 17+, college students, parents \u2014 for the $500 credit for other dependents (\xA7 24(h)(4)); includes qualifying children without the required SSN (\xA7 24(h)(4)(C)).",
    default: {
      value: "0",
      rationale: "Assumed no other dependents absent contrary input"
    }
  },
  {
    id: "dependentCareExpenses",
    type: "money",
    min: "0",
    description: "Employment-related dependent care expenses paid (\xA7 21(b)(2)), NET of any \xA7 129 dependent-care FSA exclusion. Enter only expenses enabling gainful employment for the care of a qualifying individual \u2014 attested. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no dependent care expenses absent contrary input"
    }
  },
  {
    id: "careQualifyingIndividuals",
    type: "int",
    min: "0",
    description: "Number of \xA7 21(b)(1) qualifying individuals for the child and dependent care credit: a dependent under age 13, or a dependent/spouse physically or mentally incapable of self-care living with the taxpayer over half the year.",
    default: {
      value: "0",
      rationale: "Assumed no care-credit qualifying individuals absent contrary input"
    }
  },
  {
    id: "secondaryEarnedIncome",
    type: "money",
    min: "0",
    description: "On a JOINT return: the LOWER-earning spouse's earned income, for the \xA7 21(d)(1)(B) expense limit (the credit cannot exceed either spouse's earned income). The \xA7 21(d)(2) deemed income for a student/incapacitated spouse ($250/$500 per month) is not modeled. In dollars.",
    default: {
      value: "0",
      rationale: "Conservative: absent the lower earner's income a joint return gets no dependent-care credit \u2014 provide it to claim \xA7 21"
    }
  },
  {
    id: "saversContributions",
    type: "money",
    min: "0",
    description: "The taxpayer's qualified retirement savings contributions for \xA7 25B (elective deferrals, IRA contributions, ABLE contributions), NET of \xA7 25B(d)(2) testing-period distributions \u2014 attested. Capped at $2,000 per individual by the rule. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no saver's-credit contributions absent contrary input"
    }
  },
  {
    id: "saversContributionsSpouse",
    type: "money",
    min: "0",
    description: "The spouse's qualified retirement savings contributions for \xA7 25B on a joint return, net of testing-period distributions. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no spousal saver's-credit contributions absent contrary input"
    }
  },
  {
    id: "isFullTimeStudent",
    type: "bool",
    description: "Taxpayer is a full-time student (\xA7 152(f)(2)) \u2014 denies saver's-credit eligibility (\xA7 25B(c)(2)(B)).",
    default: {
      value: false,
      rationale: "Assumed not a full-time student absent contrary input"
    }
  },
  {
    id: "spouseIsFullTimeStudent",
    type: "bool",
    description: "Spouse is a full-time student (\xA7 152(f)(2)) \u2014 denies the spouse's saver's-credit eligibility (\xA7 25B(c)(2)(B)).",
    default: {
      value: false,
      rationale: "Assumed spouse not a full-time student absent contrary input"
    }
  },
  {
    id: "qualifiedAdoptionExpenses",
    type: "money",
    min: "0",
    description: "Qualified adoption expenses paid for a finalized adoption (\xA7 23(d)(1): reasonable adoption fees, court costs, attorney fees \u2014 not for adopting a spouse's child). The multi-year expense-timing rules of \xA7 23(a)(2) are attested by entering the creditable year's amount. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no adoption expenses absent contrary input"
    }
  },
  {
    id: "adoptionIsSpecialNeeds",
    type: "bool",
    description: "The adoption is of a child with special needs, finalized this year (\xA7 23(a)(3), incl. determinations by Indian tribal governments per OBBBA \xA7 70403) \u2014 the maximum credit is deemed paid regardless of actual expenses.",
    default: {
      value: false,
      rationale: "Assumed not a special-needs adoption absent contrary input"
    }
  },
  {
    id: "qualifiedTips",
    type: "money",
    min: "0",
    description: "Qualified tips (26 U.S.C. \xA7 224): voluntary tips received in an occupation on the Treasury tipped-occupation list, already included in wages. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified tips absent contrary input"
    }
  },
  {
    id: "occupation",
    type: "enum",
    enumValues: OCCUPATION_ENUM,
    description: `The taxpayer's tipped occupation, from the Treasury Tipped Occupation list (Treas. Reg. \xA7 1.224-1, final Apr 2026). "other" = not on the list. Only demanded when qualified tips are claimed.`
    // no default — the engine must never guess whether a job is on the list
  },
  {
    id: "tipsWereVoluntary",
    type: "bool",
    description: "Tips were paid voluntarily by customers, determined by the payor, and not subject to negotiation (\xA7 224; Treas. Reg. \xA7 1.224-1). Mandatory service charges do NOT qualify.",
    default: {
      value: true,
      rationale: "Assumed tips were voluntary and non-negotiated absent contrary input \u2014 mandatory service charges would not qualify"
    }
  },
  {
    id: "employerIsSSTB",
    type: "bool",
    description: "The tips were received in the course of an employer's specified service trade or business (\xA7 199A(d)(2)) \u2014 such tips are NOT qualified (\xA7 224).",
    default: {
      value: false,
      rationale: "Assumed the employer is not a specified service trade or business absent contrary input"
    }
  },
  {
    id: "qualifiedOvertimePremium",
    type: "money",
    min: "0",
    description: "Qualified overtime compensation (26 U.S.C. \xA7 225): the FLSA \xA7 7 premium portion in excess of the regular rate, already included in wages. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified overtime premium absent contrary input"
    }
  },
  {
    id: "longTermCapitalGains",
    type: "money",
    min: "0",
    description: "Net long-term capital gains plus qualified dividends (taxed at the \xA7 1(h) preferential rates). Losses are out of scope. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no capital gains or qualified dividends absent contrary input"
    }
  },
  {
    id: "netCapitalLoss",
    type: "money",
    min: "0",
    description: "Net capital LOSS for the year from Schedule D netting, entered as a positive number (26 U.S.C. \xA7 1211(b): up to $3,000/$1,500 MFS against ordinary income; remainder carries over). Mutually exclusive with the gain facts AND with the per-bucket loss facts (shortTermCapitalLoss/longTermCapitalLoss) \u2014 this is the single OVERALL net result; for mixed years (gain in one bucket, loss in the other) use the per-bucket facts and the engine performs the \xA7 1222 netting. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no net capital loss absent contrary input"
    }
  },
  {
    id: "shortTermCapitalLoss",
    type: "money",
    min: "0",
    description: "Net short-term capital LOSS for the year (Schedule D line 7 when negative, entered positive). The ST bucket's single NET result \u2014 mutually exclusive with shortTermCapitalGains and with netCapitalLoss. Combine with longTermCapitalGains for mixed years: the engine performs the \xA7 1222 cross-netting (a net ST loss first reduces net LT gain per \xA7 1222(11); any overall loss is capped by \xA7 1211(b)). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no net short-term capital loss absent contrary input"
    }
  },
  {
    id: "longTermCapitalLoss",
    type: "money",
    min: "0",
    description: "Net long-term capital LOSS for the year (Schedule D line 15 when negative, entered positive). The LT bucket's single NET result \u2014 mutually exclusive with longTermCapitalGains and with netCapitalLoss. Combine with shortTermCapitalGains for mixed years: the engine performs the \xA7 1222 cross-netting (a net LT loss reduces the ordinary-rate net ST gain per \xA7 1222(9); any overall loss is capped by \xA7 1211(b)). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no net long-term capital loss absent contrary input"
    }
  },
  {
    id: "qualifiedDividends",
    type: "money",
    min: "0",
    description: "Qualified dividends (Form 1099-DIV box 1b) as a STANDALONE fact \u2014 use this instead of folding them into longTermCapitalGains whenever a net capital LOSS also exists (dividends are not capital gains: the \xA7 1211(b) loss offset never touches them, and they keep \xA7 1(h) preferential rates). When there is no capital loss, folding qualified dividends into longTermCapitalGains remains equivalent and supported. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified dividends absent contrary input"
    }
  },
  {
    id: "shortTermCapitalGains",
    type: "money",
    min: "0",
    description: "Net short-term capital gain after Schedule D netting, ordinary-rate (\xA7 1222(5)). Counts as \xA7 32(i) disqualified investment income and \xA7 1411 net investment income. Mutually exclusive with netCapitalLoss (provide the single NET Schedule D result: ST gain and/or LT gain, or a net loss \u2014 never both gain and loss). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no net short-term capital gain absent contrary input"
    }
  },
  {
    id: "ordinaryDividends",
    type: "money",
    min: "0",
    description: "Ordinary (non-qualified) dividends, taxed at ordinary rates \u2014 do NOT fold into otherOrdinaryIncome: dividends count as \xA7 32(i) disqualified income and \xA7 1411 net investment income (Form 1099-DIV box 1a minus box 1b). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no ordinary (non-qualified) dividends absent contrary input"
    }
  },
  {
    id: "selfEmploymentNetProfit",
    type: "money",
    min: "0",
    description: "Net profit from self-employment (Schedule C line 31: gross receipts minus ACTUAL Schedule C expenses). Do NOT subtract intake fields like 'W-2 Wages Paid' that exist for the \xA7 199A wage limit \u2014 those belong in qbiW2Wages, not here. A loss year goes in scheduleCNetLoss instead (leave this at 0). GROSS-RECEIPTS COMPOSITION: the intake's own gross-receipts field PLUS every 1099-NEC and 1099-K nested under (or issued to) the business PLUS any 1099-MISC box 3 'other income' issued to a filer who operates a Schedule C business (a payer's MISC box-3 payment to an active sole proprietor is business income, not Schedule 1 line 8z) \u2014 1099-MISC box 1 rents belong on Schedule E, never here. A 1099 whose recipient TIN matches NEITHER spouse is not this return's income: exclude it and disclose. PLACEHOLDER/TEMPLATE DOCUMENTS: a 1099 whose payer block is an unfilled template \u2014 no payer name, address line 'City, AK 99999' or a similar all-9s placeholder \u2014 is an inactive template document, not income: exclude every box on it (income AND withholding) and disclose; a 1099 from a NAMED payer at a real-looking address is live data even when the structured intake has no matching entry. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no self-employment income absent contrary input"
    }
  },
  {
    id: "hasChildUnderSix",
    type: "bool",
    description: "At least one qualifying child was under age six at year end \u2014 gates the California Young Child Tax Credit (R&TC \xA7 17052.1, us.ca.yctc).",
    default: {
      value: "false",
      rationale: "Assumed no child under six absent contrary input"
    }
  },
  {
    id: "caRentedPrincipalResidence",
    type: "bool",
    description: "Rented (did not own) a principal residence in California for at least half of the tax year, was not claimed as a dependent, and the property was not exempt from California property tax \u2014 the eligibility gate for the California nonrefundable renter's credit (FTB 540, us.ca.renters_credit).",
    default: {
      value: "false",
      rationale: "Assumed not a qualifying California renter absent contrary input"
    }
  },
  {
    id: "caAmti",
    type: "money",
    min: "0",
    description: "California alternative minimum taxable income (Schedule P (540) Part I, line 17) \u2014 AGENT-COMPUTED per the R&TC \xA7\xA7 17062(c)-(e), 17062.1 construction rules, then fed to the CA AMT target (us.ca.amt). CONSTRUCTION NOTES (disclose when composing this figure): California individual AMT is FROZEN to IRC \xA7\xA7 55-59 as they read on January 1, 2015 (R&TC \xA7 17062.1, re-enacted by SB 711, Stats. 2025 Ch. 231, WITHOUT moving the freeze forward, even though SB 711 rolled CA's general IRC conformity date to January 1, 2025 for other provisions). Starting from CA taxable income (Form 540 line 19), the STANDARD DEDUCTION IS ADDED BACK (R&TC \xA7 17062(c)(1), denying \xA7 56(b)(1)(E)). The ISO exercise spread follows \xA7 56(b)(3) as modified by R&TC \xA7 17062(c)(2) (plus the California Qualified Stock Option addback, R&TC \xA7 17502). There is NO tax-exempt private-activity-bond-interest preference (R&TC \xA7 17062(d) turns off \xA7 57(a)(5)) and NO AMT foreign tax credit (R&TC \xA7 17062(e) turns off \xA7 59(a)). A taxpayer whose aggregate trade/business gross receipts are under $1,000,000 (a flat threshold, NOT doubled for MFJ) excludes ALL trade/business income, adjustments, and preferences from AMTI (R&TC \xA7 17062(b)(4), the CA small-business AMTI exclusion). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no CA AMT adjustments or preference items absent contrary input"
    }
  },
  {
    id: "caRegularTax",
    type: "money",
    min: "0",
    description: "California 'regular tax' for the Schedule P (540) Part II tentative-minimum-tax comparison (R&TC \xA7 17062(a)(2)) \u2014 the Form 540 line 31 tax as reduced by the exemption credits already claimed, i.e. the figure Schedule P's own worksheet calls the regular tax. The CA AMT target (us.ca.amt) computes AMT = max(0, TMT \u2212 this amount). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no CA regular tax absent contrary input \u2014 note this OVERSTATES computed AMT when a real regular-tax figure exists; supply the actual amount whenever claiming us.ca.amt"
    }
  },
  {
    id: "scheduleCNetLoss",
    type: "money",
    min: "0",
    description: "Schedule C/F net LOSS for the year as a POSITIVE number (gross receipts minus ACTUAL expenses is negative; \xA7 165(c)(1) trade-or-business loss). Reduces gross income AND \xA7 32(c)(2) earned income (EITC/ACTC); no SE tax attaches to a loss year. Schedule C ONLY \u2014 Schedule E losses belong in nonpassiveScheduleELoss or the passive facts. A 'loss' that exists only because the intake's 'W-2 Wages Paid' field was subtracted is a misreading (that field is qbiW2Wages, never an expense). Gross-receipts composition follows selfEmploymentNetProfit's convention (intake receipts + nested 1099-NEC/K + 1099-MISC box 3 issued to the proprietor; MISC box 1 rents \u2192 Schedule E; TIN-mismatched 1099s excluded with disclosure). If QBI-flagged, also enter the same amount in qbiLossOffset. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no self-employment loss absent contrary input"
    }
  },
  {
    id: "k1OrdinaryBusinessIncome",
    type: "money",
    min: "0",
    description: "Ordinary business income from a partnership or S corporation Schedule K-1 (box 1) in which the taxpayer MATERIALLY participates \u2014 QBI-eligible (\xA7 199A) but not subject to SE tax or payroll tax (an S-corp shareholder's share, or a non-SE partnership allocation; a general partner's SE-taxable share belongs in selfEmploymentNetProfit instead). Material participation keeps it out of \xA7 1411 net investment income \u2014 disclosed. Losses out of scope. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no K-1 pass-through business income absent contrary input"
    }
  },
  {
    id: "qbiW2Wages",
    type: "money",
    min: "0",
    description: "W-2 wages PAID BY the qualified trade or business, allocable to QBI (\xA7 199A(b)(4)) \u2014 drives the above-threshold wage limit. Intake forms often label this 'W-2 Wages Paid' under the business section: it is a \xA7 199A LIMIT input, NOT a Schedule C expense \u2014 never subtract it from selfEmploymentNetProfit. Defaults to $0, which can only understate the deduction (conservative). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed the business paid no W-2 wages absent contrary input (conservative above the threshold)"
    }
  },
  {
    id: "qbiUBIA",
    type: "money",
    min: "0",
    description: "Unadjusted basis immediately after acquisition of the business's qualified property (\xA7 199A(b)(6)) \u2014 the 2.5% prong of the wage limit. Defaults to $0 (conservative). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified-property UBIA absent contrary input (conservative)"
    }
  },
  {
    id: "businessIsSSTB",
    type: "bool",
    description: "The trade or business is a specified service trade or business (\xA7 199A(d)(2): health, law, accounting, consulting, financial services, athletics, performing arts, \u2026) \u2014 above the threshold an SSTB's deduction phases to zero."
    // no default — only demanded above the § 199A threshold, where it
    // decides everything; the engine must never guess it
  },
  {
    id: "sepContribution",
    type: "money",
    min: "0",
    description: "Employer contribution to the self-employed taxpayer's own SEP-IRA or solo-401(k) profit-sharing (\xA7 404(h): up to 20% of net SE earnings after \xBD SE tax, capped at the \xA7 415(c) limit \u2014 $70,000 for 2025, $72,000 for 2026). In dollars. DEDUCTIBLE-LIMIT CONVENTION: when a documented SEP/Keogh contribution exceeds the limit, the deducted amount = 20% \xD7 (0.9235 \xD7 total Schedule C net profit \u2212 the \xBD-SE-tax deduction), rounded half-up \u2014 the \xA7 401(c)(2)/\xA7 1402(a)(12) reading that applies BOTH the 92.35% factor AND the \xBD-SE subtraction. (Note: Pub. 560's worksheet applies only the \xBD-SE subtraction; this engine follows the statutory \xA7 1402(a)(12) reduction.) Pass the documented contribution and apply this cap when self-computing the Schedule 1 line 16 amount.",
    default: {
      value: "0",
      rationale: "Assumed no self-employed retirement contribution absent contrary input"
    }
  },
  {
    id: "selfEmployedHealthPremiums",
    type: "money",
    min: "0",
    description: "Health insurance premiums paid by a self-employed taxpayer for themselves, spouse, and dependents (\xA7 162(l)) \u2014 deductible up to the business's earned income; no employer-subsidized coverage available (assumed, disclosed). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no self-employed health insurance premiums absent contrary input"
    }
  },
  {
    id: "employeeAnnualWages",
    type: "money",
    min: "0",
    description: "One employee's annual wages, for the employer-side payroll-tax target (\xA7 3111 FICA + FUTA). In dollars."
    // no default — only demanded by the employer payroll target
  },
  {
    id: "qreCurrentYear",
    type: "money",
    min: "0",
    description: "Qualified research expenses for the current year (\xA7 41(b); \xA7 41(d) qualification attested). In dollars."
    // no default — only demanded by the research-credit target
  },
  {
    id: "qreAvgPrior3Years",
    type: "money",
    min: "0",
    description: "Average annual qualified research expenses over the 3 preceding years \u2014 0 means no prior QREs (the 6% startup rate of \xA7 41(c)(4)(B) applies). In dollars."
    // no default — the rate regime turns on it; the engine must not guess
  },
  {
    id: "hsaContribution",
    type: "money",
    min: "0",
    description: "Personal HSA contributions (not through payroll) under 26 U.S.C. \xA7 223; HDHP coverage assumed. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no HSA contributions absent contrary input"
    }
  },
  {
    id: "hsaCoverage",
    type: "enum",
    enumValues: ["self", "family"],
    description: "HDHP coverage type for HSA limits (\xA7 223(b)(2))."
    // no default — only demanded when hsaContribution > 0
  },
  {
    id: "isAge55OrOlder",
    type: "bool",
    description: "Taxpayer is 55 or older (HSA catch-up contribution, \xA7 223(b)(3)).",
    default: {
      value: false,
      rationale: "Assumed under 55 absent contrary input (denies the HSA catch-up \u2014 conservative)"
    }
  },
  {
    id: "studentLoanInterest",
    type: "money",
    min: "0",
    description: "Interest paid on qualified education loans (26 U.S.C. \xA7 221). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no student loan interest absent contrary input"
    }
  },
  {
    id: "carLoanInterest",
    type: "money",
    min: "0",
    description: "Interest paid on a qualified passenger vehicle loan (26 U.S.C. \xA7 163(h)(4)): new US-assembled personal-use vehicle purchased after 2024. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified vehicle loan interest absent contrary input"
    }
  },
  {
    id: "charitableCashContributions",
    type: "money",
    min: "0",
    description: "Cash contributions to qualifying charities (26 U.S.C. \xA7 170(p); DAFs and certain private foundations excluded). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no charitable cash contributions absent contrary input"
    }
  },
  {
    id: "stateAndLocalTaxesPaid",
    type: "money",
    min: "0",
    description: "State and local taxes paid that Schedule A can count (26 U.S.C. \xA7 164(a)): state/local income (or elected general sales) taxes plus real and personal property taxes, personal portion only. Subject to the OBBBA \xA7 164(b)(6) cap. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no deductible state and local taxes absent contrary input"
    }
  },
  {
    id: "mortgageInterestPaid",
    type: "money",
    min: "0",
    description: "Home mortgage interest paid on acquisition debt secured by a qualified residence (26 U.S.C. \xA7 163(h)(3)); post-Dec-15-2017 origination assumed. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no home mortgage interest absent contrary input"
    }
  },
  {
    id: "mortgageAverageBalance",
    type: "money",
    min: "0",
    description: "Average balance of home acquisition debt for the year (Publication 936 method) \u2014 interest is prorated when this exceeds the $750,000 ($375,000 MFS) limit of \xA7 163(h)(3)."
    // no default — only demanded when mortgageInterestPaid > 0; the engine
    // must never guess whether the $750k limit binds
  },
  {
    id: "medicalExpenses",
    type: "money",
    min: "0",
    description: "Unreimbursed medical and dental care expenses (26 U.S.C. \xA7 213) \u2014 deductible over 7.5% of AGI when itemizing. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no unreimbursed medical expenses absent contrary input"
    }
  },
  {
    id: "casualtyFederalDisasterLosses",
    type: "money",
    min: "0",
    description: "Personal casualty losses attributable to FEDERALLY DECLARED disasters that are NOT qualified disaster losses (\xA7 165(h)(5)): the SUM over events of max(0, min(FMV decline, adjusted basis) \u2212 insurance \u2212 $100 per casualty) \u2014 the per-event Form 4684 lines 1\u201311 arithmetic is attested by the input. The 10%-of-AGI floor (\xA7 165(h)(2)) is applied by the engine; deductible only when itemizing. Non-declared personal casualty losses are nondeductible \u2014 do not enter them. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no federally-declared-disaster casualty losses absent contrary input"
    }
  },
  {
    id: "casualtyQualifiedDisasterLosses",
    type: "money",
    min: "0",
    description: "Net QUALIFIED disaster losses (Form 4684 line 15): the SUM over qualified-disaster events of max(0, min(FMV decline, adjusted basis) \u2212 insurance \u2212 $500 per casualty) \u2014 per-event arithmetic attested. NOT subject to the 10%-of-AGI floor, and allowed even WITHOUT itemizing by increasing the standard deduction (2025 Form 4684 instructions). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified disaster losses absent contrary input"
    }
  },
  {
    id: "passiveActivityIncome",
    type: "money",
    min: "0",
    description: "Net INCOME from passive activities this year (\xA7 469(c): rentals without real-estate-professional status, passive royalties, passive K-1 income) \u2014 absorbs passive losses dollar-for-dollar on Form 8582. Transcribe the TOTAL including any self-rental income items, and ALSO flag the self-rental subset in selfRentalNetIncome \u2014 the rule itself excludes that subset from the absorption pool (Reg. \xA7 1.469-2(f)(6)); do not pre-net. For the standalone target us.federal.passive_loss_allowed. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no passive-activity income absent contrary input"
    }
  },
  {
    id: "selfRentalNetIncome",
    type: "money",
    min: "0",
    description: "The SELF-RENTAL subset of passiveActivityIncome: net rental income items from property rented for use in a trade or business in which the taxpayer MATERIALLY participates (Reg. \xA7 1.469-2(f)(6) recharacterizes each such item's net income as NOT from a passive activity, so it cannot absorb passive losses). Item by item, INCOME items only \u2014 a self-rented property that nets a LOSS stays passive (leave its loss in passiveActivityLosses; add nothing here for it). Report the income itself as nonpassive Schedule E income (k1OrdinaryBusinessIncome if QBI-eligible, else scheduleENetIncome); it is also OUT of net investment income for NIIT (Reg. \xA7 1.1411-4(g)(6)(i) deems it ordinary-course). COMPOSING SCHEDULE E AFTERWARD: net the allowed passive loss against the QBI-flagged bucket FIRST \u2014 k1OrdinaryBusinessIncome = QBI-eligible income minus allowed QBI-flagged losses; only the non-QBI remainder goes in scheduleENetIncome (e.g. self-rental 10,000 QBI + royalty 2,000 non-QBI with 2,000 allowed QBI-flagged loss \u2192 k1 = 8,000, scheduleENetIncome = 2,000). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no self-rental income absent contrary input"
    }
  },
  {
    id: "passiveActivityLosses",
    type: "money",
    min: "0",
    description: "Total passive-activity losses claimed this year as a POSITIVE number (current-year passive losses plus prior-year suspended carryovers being applied). For the standalone target us.federal.passive_loss_allowed. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no passive-activity losses absent contrary input"
    }
  },
  {
    id: "rentalActiveParticipationLosses",
    type: "money",
    min: "0",
    description: "The portion of passiveActivityLosses from rental real estate in which the taxpayer ACTIVELY PARTICIPATED (\xA7 469(i)(1) \u2014 a lower bar than material participation: bona fide management decisions with \u226510% ownership, attested). Only this portion can use the \xA7 469(i) $25,000 special allowance. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no active-participation rental losses absent contrary input"
    }
  },
  {
    id: "mfsConsideredUnmarried",
    type: "bool",
    description: "MFS filer meets the \xA7 32(d)(2) separation requirements: a qualifying child lived with them for more than half the year, they furnished over half the cost of that household, AND either they are legally separated under a decree or the spouse was not a household member during the last 6 months of the year. Unlocks the EITC for an MFS filer (\xA7 32(d)(1)); without it MFS receives $0.",
    default: {
      value: false,
      rationale: "Assumed the \xA7 32(d)(2) separation tests are not met (conservative: MFS gets no EITC)"
    }
  },
  {
    id: "mfsLivedApartAllYear",
    type: "bool",
    description: "MFS filer lived apart from the spouse AT ALL TIMES during the year \u2014 \xA7 469(i)(5) (halves the special allowance to $12,500/$50,000; living together at any time = NO allowance) AND \xA7 219(g)(4) (an MFS filer who lived apart all year is NOT treated as married for the IRA-deduction phaseout: the single ranges apply and no spousal-coverage attribution occurs).",
    default: {
      value: false,
      rationale: "Assumed the spouses lived together at some point (zero MFS allowance \u2014 conservative)"
    }
  },
  {
    id: "scheduleENetIncome",
    type: "money",
    min: "0",
    description: "Net Schedule E rental/royalty income AFTER \xA7 469 netting (compute allowed passive losses with the standalone target us.federal.passive_loss_allowed, then enter the net POSITIVE result here) that is NOT QBI-eligible. NONPASSIVE Schedule E losses (\xA7 469(c)(7) real-estate professional) are not enterable here: net them against k1OrdinaryBusinessIncome when QBI-eligible (they reduce the QBI base too), else disclose a wage-substitution workaround and keep medicareWages/socialSecurityWages at their true W-2 values \u2014 QBI-eligible pass-through income belongs in k1OrdinaryBusinessIncome instead. Treated as ordinary income with no SE tax; NOT counted as \xA7 1411 investment income by this corpus (nonpassive/self-rental character attested \u2014 passive rental income that IS investment income would be missed by the NIIT rule: disclosed limitation). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no non-QBI Schedule E income absent contrary input"
    }
  },
  {
    id: "pensionGrossPayments",
    type: "money",
    min: "0",
    description: "TOTAL pension/annuity payments received this year from a qualified-plan annuity with after-tax cost basis (1099-R box 1) \u2014 the \xA7 72(d) Simplified Method computes the taxable part. Use taxablePensionsAndAnnuities instead (or additionally, for other pensions) when the taxable amount is already known. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no simplified-method annuity absent contrary input"
    }
  },
  {
    id: "pensionCostBasis",
    type: "money",
    min: "0",
    description: "Investment in the contract as of the annuity starting date \u2014 employee after-tax contributions (1099-R box 9b). The \xA7 72(d)(1)(B) monthly exclusion is this divided by the anticipated-payments table number; lifetime recovery is capped at this amount. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed zero after-tax basis (fully taxable pension) absent contrary input"
    }
  },
  {
    id: "pensionAgeAtStart",
    type: "int",
    min: "0",
    max: "120",
    description: "Primary annuitant's age at the ANNUITY STARTING DATE (not current age) \u2014 picks the \xA7 72(d)(1)(B)(iii) single-life anticipated-payments band (\u226455: 360, 56-60: 310, 61-65: 260, 66-70: 210, 71+: 160). Required when pensionCostBasis is set and the annuity is single-life.",
    default: {
      value: "0",
      rationale: "Unset; the simplified-method rule refuses rather than assume an age band"
    }
  },
  {
    id: "pensionIsJointAndSurvivor",
    type: "bool",
    description: "True for an annuity payable over more than one life with a starting date after 1997 \u2014 the \xA7 72(d)(1)(B)(iv) COMBINED-ages table applies (set pensionCombinedAgesAtStart).",
    default: {
      value: false,
      rationale: "Assumed a single-life annuity absent contrary input"
    }
  },
  {
    id: "pensionCombinedAgesAtStart",
    type: "int",
    min: "0",
    max: "240",
    description: "Combined ages of both annuitants at the annuity starting date, for joint-and-survivor annuities (\xA7 72(d)(1)(B)(iv): \u2264110: 410, 111-120: 360, 121-130: 310, 131-140: 260, 141+: 210).",
    default: {
      value: "0",
      rationale: "Unset; the simplified-method rule refuses rather than assume a band"
    }
  },
  {
    id: "pensionMonthsThisYear",
    type: "int",
    min: "1",
    max: "12",
    description: "Number of monthly annuity payments received this tax year (Simplified Method Worksheet line 4 multiplier). 12 for a full year.",
    default: {
      value: "12",
      rationale: "Assumed a full year of monthly payments absent contrary input"
    }
  },
  {
    id: "pensionBasisPreviouslyRecovered",
    type: "money",
    min: "0",
    description: "Cost basis already recovered tax-free in prior years (Simplified Method Worksheet line 6) \u2014 lifetime recovery cannot exceed pensionCostBasis for post-1986 starting dates. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed the first year of the annuity absent contrary input"
    }
  },
  {
    id: "rentalDepreciableBasis",
    type: "money",
    min: "0",
    description: "Depreciable basis of residential rental BUILDING(S) \u2014 cost basis excluding land (land never depreciates, \xA7 167; Pub 527 ch. 2). Drives the \xA7 168 GDS 27.5-year straight-line mid-month depreciation (Pub 946 Table A-6). Set rentalPlacedInServiceMonth for a first-year property; leave it 0 for property in service the whole year. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no depreciable rental property absent contrary input"
    }
  },
  {
    id: "rentalPlacedInServiceMonth",
    type: "int",
    min: "0",
    max: "12",
    description: "Month (1-12) the residential rental property was placed in service THIS tax year \u2014 first-year depreciation uses the \xA7 168(d)(2) mid-month convention (Pub 946 Table A-6 row 1). 0 (the default) = placed in service in a PRIOR year: the steady-state 3.636% year applies.",
    default: {
      value: "0",
      rationale: "Assumed the property was in service before this year (steady-state 3.636% year) absent contrary input"
    }
  },
  {
    id: "rentalIncomeBeforeDepreciation",
    type: "money",
    min: "0",
    description: "Schedule E rental result BEFORE depreciation: rents received minus cash operating expenses (management, repairs, insurance, taxes, mortgage interest\u2026). The engine subtracts the computed \xA7 168 depreciation to reach net rental income. Use scheduleENetIncome instead when you already have the after-depreciation net; the two inputs add (separate properties). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no rental activity reported through the depreciation path absent contrary input"
    }
  },
  {
    id: "businessMilesDriven",
    type: "int",
    min: "0",
    description: "Business miles driven for the optional standard mileage rate (70\xA2/mile for 2025 per Notice 2025-5; 72.5\xA2/mile for 2026 per Notice 2026-10) \u2014 the standalone target us.federal.vehicle_standard_mileage computes the deduction; subtract it from your Schedule C profit yourself.",
    default: {
      value: "0",
      rationale: "Assumed no business mileage absent contrary input"
    }
  },
  {
    id: "isRetiredOnTotalDisability",
    type: "bool",
    description: "Retired on permanent and total disability (\xA7 22(e)(3)) \u2014 a \xA7 22(b) qualified individual even under age 65. Set this whenever the taxpayer is documented as retired on permanent and total disability. The physician's statement (Schedule R Part II) is a KEEP-FOR-RECORDS substantiation requirement, not filed with the return \u2014 an intake answer that no statement is on file yet does NOT zero the credit; disclose the substantiation gap instead.",
    default: {
      value: false,
      rationale: "Assumed not retired on total disability absent contrary input"
    }
  },
  {
    id: "scheduleRDisabilityIncome",
    type: "money",
    min: "0",
    description: "Taxable disability income under \xA7\xA7 72/105(a) (e.g. a code-3 1099-R reported as wages before minimum retirement age) \u2014 caps the \xA7 22(c)(2) initial amount for an under-65 disabled individual. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no \xA7 72/105(a) disability income absent contrary input"
    }
  },
  {
    id: "nontaxableBenefitsForScheduleR",
    type: "money",
    min: "0",
    description: "Nontaxable social security, railroad retirement, VA pensions and other excluded disability benefits (\xA7 22(c)(3)) \u2014 reduce the \xA7 22 amount dollar-for-dollar. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no \xA7 22(c)(3) nontaxable benefits absent contrary input"
    }
  },
  {
    id: "qbiLossOffset",
    type: "money",
    min: "0",
    description: "QBI-eligible LOSSES (positive number) from activities NOT in selfEmploymentNetProfit/k1OrdinaryBusinessIncome \u2014 e.g. an allowed nonpassive \xA7 469(c)(7) rental loss flagged as QBI. Reduces the \xA7 199A QBI base only (Reg. \xA7 1.199A-1(d)(2)(iii)); enter the income-side effect via nonpassiveScheduleELoss. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no external QBI losses absent contrary input"
    }
  },
  {
    id: "nonpassiveScheduleELoss",
    type: "money",
    min: "0",
    description: "NONPASSIVE Schedule E losses as a positive number (\xA7 469(c)(7) real-estate-professional rentals with material participation, or allowed \xA7 469 losses not netted elsewhere) \u2014 subtracts from gross income. Schedule E ONLY: never route a Schedule C amount here. A Schedule C 'loss' that exists only because the intake's 'W-2 Wages Paid' field was subtracted is a misreading \u2014 that field is the \xA7 199A wage-limit input (qbiW2Wages), never an expense; Schedule C profit = gross receipts minus ACTUAL Schedule C expenses. If the activity is QBI-flagged, also enter the same amount in qbiLossOffset. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no nonpassive Schedule E losses absent contrary input"
    }
  },
  {
    id: "medicareWages",
    type: "money",
    min: "0",
    description: "W-2 box 5 Medicare wages and tips \u2014 the Form 8959 Additional-Medicare-Tax base. Often HIGHER than box 1 (pre-tax 401(k) deferrals reduce box 1 but not box 5). Leave 0 to approximate with box 1 wages (understates the tax when box 5 > box 1 \u2014 disclosed). In dollars.",
    default: {
      value: "0",
      rationale: "Approximated by box 1 wages absent contrary input (Form 8959 wants box 5)"
    }
  },
  {
    id: "niitInvestmentExpenses",
    type: "money",
    min: "0",
    description: "Deductions properly allocable to investment income for the \xA7 1411 net investment income tax (Form 8960 lines 9\u201310: investment interest expense, allocable state income tax, and other modifications). Subtracted from gross investment income. COORDINATION: if you applied a \xA7 163(d)(4)(B) election by moving elected capital gain from longTermCapitalGains to otherOrdinaryIncome, that gain already left the NII base \u2014 do NOT also enter the matching investment interest here (it would double-reduce NII). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no properly-allocable investment deductions absent contrary input"
    }
  },
  {
    id: "investmentInterestDeduction",
    type: "money",
    min: "0",
    description: "Investment interest expense DEDUCTIBLE this year (\xA7 163(d), Form 4952 line 8) \u2014 AFTER the net-investment-income limit and any \xA7 163(d)(4)(B) election, which are attested by the input (the engine does not run Form 4952). RUN THE LIMIT FIRST: with no net investment income (taxable interest + ordinary dividends + elected gains), the deductible amount is $0 and the whole expense carries forward \u2014 enter 0, not the amount paid. An itemized deduction; excess carries forward outside this corpus. If an election treated capital gain as investment income, also move that amount from longTermCapitalGains to otherOrdinaryIncome yourself. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no deductible investment interest absent contrary input"
    }
  },
  {
    id: "gamblingLossesItemized",
    type: "money",
    min: "0",
    description: "Gambling losses deductible as an itemized deduction (\xA7 165(d), Schedule A line 16) \u2014 LIMITED TO GAMBLING WINNINGS included in income: enter min(losses paid, winnings reported), attested by the input (the engine does not track the per-session limit). TY2025: the full limited amount deducts; the OBBBA 90%-of-losses haircut (\xA7 165(d) as amended) applies only to taxable years beginning after 12/31/2025. Itemized-only; never reduces winnings directly. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no deductible gambling losses absent contrary input"
    }
  },
  {
    id: "otherItemizedDeductions",
    type: "money",
    min: "0",
    description: "Other itemized deductions (Schedule A line 16, attested): impairment-related work expenses of a disabled individual (\xA7 67(d) \u2014 NOT subject to the suspended 2% floor), federal estate tax on income in respect of a decedent, amortizable bond premium, and the other line-16 write-ins. Gambling losses have their own fact (gamblingLossesItemized). Enter the documented total \u2014 deductible in full. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no Schedule A line-16 write-in deductions absent contrary input"
    }
  },
  {
    id: "noncashCharitableContributions",
    type: "money",
    min: "0",
    description: "Noncash charitable contributions deductible this year (\xA7 170, Form 8283) \u2014 the 20/30/50%-of-AGI property limits and appraisal requirements are ATTESTED by the input (enter the allowed amount, INCLUDING prior-year carryovers used this year). Itemized-only; does not qualify for \xA7 170(p). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no noncash charitable contributions absent contrary input"
    }
  },
  {
    id: "socialSecurityWages",
    type: "money",
    min: "0",
    description: "W-2 box 3 social security wages for the SE-tax OASDI wage-base coordination (Schedule SE line 8a). PER PERSON: Schedule SE belongs to ONE individual \u2014 on a joint return enter ONLY the SE-earner's own box 3, never the couple's sum (summing can wrongly zero the OASDI portion). Leave 0 to approximate with box 1 wages. In dollars.",
    default: {
      value: "0",
      rationale: "Approximated by box 1 wages absent contrary input (Schedule SE line 8a wants box 3)"
    }
  },
  {
    id: "stateTaxableIncome",
    type: "money",
    min: "0",
    description: "STATE taxable income (e.g. CA Form 540 line 19, VA Form 760 line 15, IL net income line 11) \u2014 computed by the preparer under state law, then fed to the state tax targets (us.ca.income_tax, us.va.income_tax, us.il.income_tax) so the rate-schedule arithmetic is engine-pinned instead of recalled. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no state taxable income absent contrary input"
    }
  },
  {
    id: "nyYonkersBase",
    type: "money",
    min: "0",
    description: "Yonkers resident surcharge base (2025 IT-201-I Yonkers worksheet, line m) \u2014 NOT the same as IT-201 line 46 ('Total New York State taxes'). It is line 46 MINUS a specific credit bundle: the Empire State child credit (IT-213 line 9) + real property tax credit (IT-214 line 20) + child/dependent care credit (IT-216 line 14) + earned income credit (IT-215 line 16) + noncustodial parent EIC (IT-209 line 32/42) + college tuition credit (IT-272 line 5/7) + IT-201 lines 69/69a + IT-201-ATT line 13 other credits, net of a STAR reconciliation add-back (Form IT-119 line 3) \u2014 agent-composed per the worksheet, then fed to us.ny.yonkers_surcharge (16.75% x this base, feeds IT-201 LINE 55, not line 54 which is MCTMT). Only relevant when IT-201 line 46 is more than $0 and the taxpayer was a Yonkers resident. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no Yonkers resident surcharge base absent contrary input (not a Yonkers resident, or line 46 is $0)"
    }
  },
  {
    id: "nyIt214Fagi",
    type: "money",
    min: "0",
    description: "Federal adjusted gross income for the NY IT-214 Real Property Tax Credit (Form IT-214 line 8) \u2014 gates AND buckets the flat Table A/Table B lookup (us.ny.it214). Over $18,000 disqualifies the credit entirely ('If line 8 is more than $18,000, stop; you do not qualify'). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no IT-214 FAGI absent contrary input"
    }
  },
  {
    id: "nyIt214Eligible",
    type: "bool",
    description: "Attests the NY IT-214 NON-ARITHMETIC eligibility gates (2025 Form IT-214 Step 2, lines 1-6): full-year New York State resident; occupied the same residence 6 or more months; did NOT own real property with a market value over $85,000; NOT claimable as a dependent on another taxpayer's federal return; did NOT reside in public housing or a completely property-tax-exempt residence; nursing-home residency disclosed per the instructions. Does NOT include the arithmetic gates \u2014 the $18,000 FAGI cap, the $450 average-monthly-adjusted-rent test (printed line-12 percentage menu), and the line 18/19 comparison are all COMPUTED by us.ny.it214 from nyIt214Fagi/nyIt214TotalRent/nyIt214RentPercent/nyIt214MonthsPaid/nyIt214HomeownerTaxes.",
    default: {
      value: false,
      rationale: "Assumed the IT-214 Step 2 eligibility gates (residency, occupancy, property value, dependent status, exempt housing) are not met absent contrary attestation"
    }
  },
  {
    id: "nyIt214TotalRent",
    type: "money",
    min: "0",
    description: "Form IT-214 line 11 (2025): total rent paid during the year for the qualifying NY residence, EXCLUDING any subsidized part of the rental charge. The engine applies the printed line-12 percentage menu (nyIt214RentPercent) and the $450 line-13 average-monthly test itself. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no rent paid absent contrary input (homeowner or non-claimant)"
    }
  },
  {
    id: "nyIt214RentPercent",
    type: "int",
    min: "0",
    description: "Form IT-214 line 12 (2025) printed adjusted-rent percentage, chosen from the form's own menu by which charges the rent INCLUDES: heat+gas+electricity+furnishings+board \u2192 50; heat+gas+electricity+furnishings \u2192 75; heat+gas+electricity \u2192 80; heat (or heat+gas) \u2192 85; none of the above \u2192 100. These are the only five values the printed form allows \u2014 never subtract actual utility dollar amounts.",
    default: {
      value: "100",
      rationale: "Assumed rent includes no heat/gas/electricity/furnishings/board charges absent contrary input (100% per the printed menu)"
    }
  },
  {
    id: "nyIt214MonthsPaid",
    type: "int",
    min: "1",
    description: "Form IT-214 line 13 divisor (2025): the number of months rent was paid during the year (the printed instruction divides line 12 adjusted rent by this to get average monthly adjusted rent, tested against the $450 cap).",
    default: {
      value: "12",
      rationale: "Assumed rent was paid for the full 12 months absent contrary input"
    }
  },
  {
    id: "nyIt214HomeownerTaxes",
    type: "money",
    min: "0",
    description: "Form IT-214 line 17 (2025): homeowners' real property taxes paid during the year (line 15) plus special assessments (line 16) on the qualifying residence. A part-year owner/renter supplies both this and the rent facts; the engine sums both buckets into line 18. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no homeowner real property taxes absent contrary input (renter or non-claimant)"
    }
  },
  {
    id: "nyIt216StateCredit",
    type: "money",
    min: "0",
    description: "New York State child and dependent care credit as computed on Form IT-216, line 14 (2025) \u2014 qualified expenses (net of excluded W-2 box 10 dependent care benefits) x the federal line-10 decimal (FAGI-keyed, .35 down to .20) x the NYS line-13 limitation factor (NYAGI-keyed sawtooth; both tables quoted verbatim in us.ny.parameters). This is Worksheet 2 line 1 \u2014 the STARTING amount the NYC child and dependent care credit prorates (us.ny.nyc_cdcc). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no New York State child and dependent care credit absent contrary input"
    }
  },
  {
    id: "nyIt216Under4Expenses",
    type: "money",
    min: "0",
    description: "Form IT-216 line 23 (2025): total qualified child/dependent care expenses paid for qualifying persons who were UNDER 4 years old on December 31 and listed on IT-216 line 3. Numerator of the Worksheet 2 line 4 proration ratio (us.ny.nyc_cdcc). Use the same gross/net basis as nyIt216TotalExpenses so the ratio is consistent. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualifying persons under age 4 absent contrary input"
    }
  },
  {
    id: "nyIt216TotalExpenses",
    type: "money",
    min: "0",
    description: "Form IT-216 line 3a total (2025): total qualified child/dependent care expenses for ALL qualifying persons. Denominator of the Worksheet 2 line 4 proration ratio (us.ny.nyc_cdcc); the ratio is capped at 100%. Use the same gross/net basis as nyIt216Under4Expenses. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified child/dependent care expenses absent contrary input"
    }
  },
  {
    id: "forceItemized",
    type: "bool",
    description: "Force the \xA7 63(e) election to itemize even when the standard deduction (plus \xA7 170(p)) is larger \u2014 e.g. an MFS filer coordinating with an itemizing spouse, or an explicit client instruction. Default false = the engine takes the larger bundle automatically.",
    default: {
      value: false,
      rationale: "Assumed the tax-minimizing standard-or-itemized election absent contrary input"
    }
  },
  {
    id: "educatorExpenses",
    type: "money",
    min: "0",
    description: "TAXPAYER's eligible educator classroom expenses (\xA7 62(a)(2)(D)) \u2014 capped at $300 for 2025 (indexed). K-12 teachers/counselors/aides with 900+ hours (attested). A spouse who is ALSO an eligible educator gets a separate $300 cap: put the spouse's expenses in spouseEducatorExpenses, never summed here. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no educator expenses absent contrary input"
    }
  },
  {
    id: "spouseEducatorExpenses",
    type: "money",
    min: "0",
    description: "SPOUSE's eligible educator classroom expenses (\xA7 62(a)(2)(D)) \u2014 separately capped at $300 for 2025 (per-educator cap; an MFJ couple of two educators deducts up to $600 total). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed the spouse is not an eligible educator absent contrary input"
    }
  },
  {
    id: "mfsAbuseOrAbandonmentException",
    type: "bool",
    description: "MFS filer qualifies for the Reg. \xA7 1.36B-2(b)(2) domestic-abuse or spousal-abandonment exception (attested on Form 8962 by checking the 'relief' box) \u2014 the \xA7 36B(c)(1)(C) joint-filing requirement is waived and the premium tax credit computes normally; excess-APTC repayment is then subject to the ordinary \xA7 36B(f)(2)(B) caps. Available for at most 3 consecutive years.",
    default: { value: false, rationale: "Assumed no abuse/abandonment relief absent contrary attestation (conservative: MFS gets no PTC and repays APTC uncapped)" }
  },
  {
    id: "hsaDistributions",
    type: "money",
    min: "0",
    description: "HSA distributions received (Form 1099-SA box 1, all accounts summed). The portion NOT spent on qualified medical expenses (hsaQualifiedMedicalExpenses) is gross income (\xA7 223(f)(2)) and draws the 20% additional tax (\xA7 223(f)(4)) unless hsaDistributionPenaltyExempt. In dollars.",
    default: { value: "0", rationale: "Assumed no HSA distributions absent contrary input" }
  },
  {
    id: "hsaQualifiedMedicalExpenses",
    type: "money",
    min: "0",
    description: "Unreimbursed qualified medical expenses paid from HSA distributions (Form 8889 line 15) \u2014 ONLY the amount the intake EXPLICITLY attests was spent on qualified medical expenses. Absent an explicit attestation, leave this 0 and the full distribution is taxable (\xA7 223(f)) \u2014 never assume qualified use from silence; the taxpayer bears the burden of substantiating qualified use. In dollars.",
    default: { value: "0", rationale: "Assumed distributions were not for qualified medical expenses absent contrary input (conservative: taxable)" }
  },
  {
    id: "hsaDistributionPenaltyExempt",
    type: "bool",
    description: "The \xA7 223(f)(4)(B)-(C) exceptions apply to any taxable HSA distribution: made after age 65, after death, or attributable to disability \u2014 the 20% additional tax does not apply (the income inclusion still does).",
    default: { value: false, rationale: "Assumed no penalty exception absent contrary input" }
  },
  {
    id: "alimonyPaidPre2019",
    type: "money",
    min: "0",
    description: "Alimony/separate maintenance PAID under an instrument executed BEFORE 2019 (and not modified to adopt TCJA treatment) \u2014 deductible above the line under pre-TCJA \xA7 215/\xA7 62(a)(10). Post-2018-instrument alimony is NOT deductible federally (do not enter it here; note some states, e.g. NY, decoupled). In dollars.",
    default: { value: "0", rationale: "Assumed no pre-2019 alimony paid absent contrary input" }
  },
  {
    id: "alimonyReceivedPre2019",
    type: "money",
    min: "0",
    description: "Alimony/separate maintenance RECEIVED under a pre-2019 instrument \u2014 includible in gross income under pre-TCJA \xA7 71. Post-2018-instrument alimony received is NOT federal income (exclude it here). In dollars.",
    default: { value: "0", rationale: "Assumed no pre-2019 alimony received absent contrary input" }
  },
  {
    id: "federalEstimatedPayments",
    type: "money",
    min: "0",
    description: "Federal estimated tax payments for the year plus any prior-year overpayment applied (Form 1040 line 26) \u2014 its OWN line, never folded into withholding (line 25d is withholding only). In dollars.",
    default: { value: "0", rationale: "Assumed no estimated payments absent contrary input" }
  },
  {
    id: "federalExtensionPayment",
    type: "money",
    min: "0",
    description: "Payment made with the federal extension request (Form 4868) \u2014 a Form 1040 line 31 payment via Schedule 3 line 13a; counted in total payments, NEVER folded into withholding or estimated payments. In dollars.",
    default: { value: "0", rationale: "Assumed no extension payment absent contrary input" }
  },
  {
    id: "earlyWithdrawalPenaltyPaid",
    type: "money",
    min: "0",
    description: "Penalty on early withdrawal of savings (1099-INT box 2 / 1099-OID box 3) \u2014 deductible above the line in full (\xA7 62(a)(9); Schedule 1 line 18). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no early-withdrawal penalty absent contrary input"
    }
  },
  {
    id: "otherAdjustments",
    type: "money",
    min: "0",
    description: "Other above-the-line adjustments from Schedule 1 lines 24a-24z, ATTESTED as the documented total: jury duty pay given to the employer (24a), reforestation amortization (24d), \xA7 3402 supplemental-unemployment repayments (24e), \xA7 501(c)(18)(D) pension contributions (24f, W-2 box 12 code H), attorney fees for unlawful-discrimination claims (24h) and IRS whistleblower awards (24i), and similar write-in adjustments. Enter the SUM of the documented amounts \u2014 each is deductible in full; never fold these into income as negative amounts. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no Schedule 1 line-24 write-in adjustments absent contrary input"
    }
  },
  {
    id: "iraContribution",
    type: "money",
    min: "0",
    description: "Traditional IRA contributions for the year (26 U.S.C. \xA7 219). Roth contributions are not deductible and do not belong here. In dollars. GATE-vs-DOLLAR: an intake screener like 'Made contributions to a traditional or Roth IRA = false' does NOT nullify a populated traditional-IRA dollar field \u2014 IRA contributions are a DEDUCTION item, so the documented dollar controls (transcribe it; any \xA7 219(g) phase-out still applies, including the \xA7 219(g)(4) lived-apart-all-year MFS carve-out). The boolean-gate-controls rule is for PAYMENT fields only.",
    default: {
      value: "0",
      rationale: "Assumed no traditional IRA contributions absent contrary input"
    }
  },
  {
    id: "isActivePlanParticipant",
    type: "bool",
    description: "Taxpayer is an active participant in an employer retirement plan (W-2 box 13 'Retirement plan') \u2014 triggers the \xA7 219(g) deduction phase-out."
    // no default — only demanded when iraContribution > 0; the engine must
    // never guess plan coverage (it decides the whole phase-out)
  },
  {
    id: "spouseIsActivePlanParticipant",
    type: "bool",
    description: "Spouse is an active participant in an employer retirement plan (\xA7 219(g)(7) spousal phase-out)."
    // no default — only demanded for a married non-participant with an IRA
  },
  {
    id: "isAge50OrOlder",
    type: "bool",
    description: "Taxpayer attained age 50 before the close of the taxable year (IRA catch-up, \xA7 219(b)(5)(B)).",
    default: {
      value: false,
      rationale: "Assumed under 50 absent contrary input (denies the IRA catch-up \u2014 conservative)"
    }
  },
  {
    id: "aotcExpensesStudent1",
    type: "money",
    min: "0",
    description: "Qualified tuition and related expenses for the FIRST eligible student (26 U.S.C. \xA7 25A(b)) \u2014 half-time enrollment in one of the first 4 postsecondary years, no 4 prior AOTC claims, no felony drug conviction, and SSN/EIN requirements assumed satisfied. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no American Opportunity credit expenses absent contrary input"
    }
  },
  {
    id: "aotcExpensesStudent2",
    type: "money",
    min: "0",
    description: "Qualified expenses for a SECOND eligible AOTC student, if any (same \xA7 25A(b) conditions assumed). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no second AOTC student absent contrary input"
    }
  },
  {
    id: "aotcExpensesStudent3",
    type: "money",
    min: "0",
    description: "Qualified expenses for a THIRD eligible AOTC student, if any (same \xA7 25A(b) conditions assumed; a fourth simultaneous student is not representable). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no third AOTC student absent contrary input"
    }
  },
  {
    id: "llcQualifiedExpenses",
    type: "money",
    min: "0",
    description: "Qualified tuition and related expenses for the Lifetime Learning Credit (26 U.S.C. \xA7 25A(c)) \u2014 per return, and not the same expenses claimed for the AOTC (\xA7 25A(c)(2)(A)). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no Lifetime Learning Credit expenses absent contrary input"
    }
  },
  {
    id: "isSubjectToKiddieTax",
    type: "bool",
    description: "Taxpayer is a child to whom 26 U.S.C. \xA7 1(g) applies (under 18, or 18 / full-time student under 24 with earned income \u2264 half their support, a living parent, no joint return). Triggers the Form 8615 tax and denies the refundable AOTC (\xA7 25A(i)).",
    default: {
      value: false,
      rationale: "Assumed not a kiddie-tax child absent contrary input (the claimant is typically the parent)"
    }
  },
  {
    id: "parentTaxableIncome",
    type: "money",
    min: "0",
    description: "The parent's taxable income (Form 8615 line 6) \u2014 needed to compute the allocable parental tax at the parent's rates (\xA7 1(g)(3))."
    // no default — the engine must never guess the parent's income
  },
  {
    id: "parentFilingStatus",
    type: "enum",
    enumValues: [...FILING_STATUSES],
    description: "The parent's filing status for the Form 8615 recomputation (\xA7 1(g))."
    // no default
  },
  {
    id: "parentHasPreferentialIncome",
    type: "bool",
    description: "The parent's taxable income includes net capital gain or qualified dividends (preferential rates) \u2014 the Form 8615 recomputation then needs the parent's QDCGT worksheet, which is not modeled (refuses)."
    // no default — deciding it silently would change the parent-rate math
  },
  {
    id: "socialSecurityBenefits",
    type: "money",
    min: "0",
    description: "Social security (and tier-1 railroad retirement) benefits received \u2014 box 5 of SSA-1099; includible per the \xA7 86 0/50/85% formula. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no social security benefits absent contrary input"
    }
  },
  {
    id: "taxableIraDistributions",
    type: "money",
    min: "0",
    description: "TAXABLE IRA distributions (\xA7 408(d); Form 8606 basis already applied). When nondeductible-contribution basis exists, compute the taxable amount with the standalone target us.federal.ira8606.taxable_amount (per individual) and enter the result here. ROLLOVERS are excluded entirely: when an interview/confirmation field like 'distributions less rollovers' shows 0, the 1099-R was rolled over and contributes NOTHING here regardless of its box 2a or distribution code \u2014 payer forms cannot see 60-day rollovers. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no IRA distributions absent contrary input"
    }
  },
  {
    id: "ira8606Basis",
    type: "money",
    min: "0",
    description: "Form 8606 line 2: this individual's total basis in traditional IRAs from earlier years (the most recent prior Form 8606 line 14). PER INDIVIDUAL \u2014 spouses file separate Forms 8606; run the ira8606 target once per spouse. In dollars."
    // no default \u2014 the 8606 targets are meaningless without an explicit basis
  },
  {
    id: "ira8606NondeductibleContributions",
    type: "money",
    min: "0",
    description: "Form 8606 line 1: nondeductible traditional IRA contributions FOR this year, including those made through the following April 15 (the line 4 timing split is not modeled \u2014 enter the full year's amount). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no current-year nondeductible contributions absent contrary input"
    }
  },
  {
    id: "ira8606YearEndValue",
    type: "money",
    min: "0",
    description: "Form 8606 line 6: value of ALL this individual's traditional, SEP, and SIMPLE IRAs as of December 31, plus any outstanding rollovers (\xA7 408(d)(2)(C)). In dollars."
    // no default \u2014 a guessed $0 would silently inflate the nontaxable ratio
  },
  {
    id: "ira8606Distributions",
    type: "money",
    min: "0",
    description: "Form 8606 line 7: this year's distributions from this individual's traditional/SEP/SIMPLE IRAs, EXCLUDING rollovers, Roth conversions, qualified charitable distributions, and returned contributions (exclusions attested by the input). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no non-conversion IRA distributions absent contrary input"
    }
  },
  {
    id: "ira8606Conversions",
    type: "money",
    min: "0",
    description: "Form 8606 line 8: net amount this individual converted from traditional/SEP/SIMPLE IRAs to Roth IRAs this year. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no Roth conversions absent contrary input"
    }
  },
  {
    id: "taxablePensionsAndAnnuities",
    type: "money",
    min: "0",
    description: "TAXABLE pensions and annuities (\xA7 72 exclusion ratio already applied \u2014 1099-R box 2a). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no pension or annuity income absent contrary input"
    }
  },
  {
    id: "unemploymentCompensation",
    type: "money",
    min: "0",
    description: "Unemployment compensation (\xA7 85, fully includible). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no unemployment compensation absent contrary input"
    }
  },
  {
    id: "otherOrdinaryIncome",
    type: "money",
    min: "0",
    description: "Other fully-includible ordinary income reported on Schedule 1 line 8 \u2014 gambling winnings (W-2G, line 8b), prizes and awards, Alaska Permanent Fund dividends (line 8g), jury duty pay, etc. (\xA7 61(a)). NOT earned income, NOT \xA7 1411 net investment income, no SE tax. Do not enter income another fact covers. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no other Schedule 1 income absent contrary input"
    }
  },
  {
    id: "eitcAdditionalQualifyingChildren",
    type: "int",
    min: "0",
    description: "EIC qualifying children NOT already counted in qualifyingChildren \u2014 \xA7 32(c)(3) uses the \xA7 152(c) age rules (under 19, under 24 if a full-time student, or ANY age if permanently and totally disabled), which are wider than the CTC's under-17 cut. A 17-year-old, a 20-year-old student, or a disabled adult child goes here. Counted only for the earned income credit.",
    default: {
      value: "0",
      rationale: "Assumed no EIC-only qualifying children absent contrary input (the CTC child count is the floor)"
    }
  },
  {
    id: "earlyDistributionSubjectToPenalty",
    type: "money",
    min: "0",
    description: "The portion of retirement distributions subject to the \xA7 72(t) 10% additional tax \u2014 enter only the amount to which NO exception (59\xBD, disability, SEPP, first home, \xA7 72(t)(2)) applies. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no penalized early distributions absent contrary input"
    }
  },
  {
    id: "amtIsoExerciseSpread",
    type: "money",
    min: "0",
    description: "Bargain element on incentive stock options exercised and NOT sold in the same year (\xA7 56(b)(3) AMT adjustment). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no ISO exercises absent contrary input"
    }
  },
  {
    id: "amtOtherPreferences",
    type: "money",
    min: "0",
    description: "Other Form 6251 Part I adjustments and preferences: tax-exempt private-activity-bond interest, depreciation adjustments, depletion, intangible drilling costs, etc. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no other AMT preference items absent contrary input"
    }
  },
  {
    id: "federalTaxWithheld",
    type: "money",
    min: "0",
    description: "Federal income tax withheld so far this year (W-2 box 2 + estimated payments) \u2014 credited against the liability under 26 U.S.C. \xA7 31. INCLUDE Form 8959 Part IV excess Medicare withholding: any W-2 box 6 amount over 1.45% of box 5 is Additional-Medicare-Tax withholding that belongs here (otherwise the 0.9% tax is paid twice). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no withholding reported absent contrary input"
    }
  },
  {
    id: "useFormulaMethod",
    type: "bool",
    description: "Compute with exact continuous formulas instead of the printed-form methods (analysis/comparison mode \u2014 filed returns use the forms). Affects: the IRS Tax Table below $100,000, the \xA7 32(f) EIC Table ($50-bracket midpoints), and Schedule SE's per-line whole-dollar rounding.",
    default: {
      value: false,
      rationale: "The printed-form methods (Tax Table, EIC Table, Schedule SE rounding) are how filed returns compute"
    }
  },
  // ── § 6654 estimated-tax safe harbor ──────────────────────────────────
  {
    id: "priorYearTax",
    type: "money",
    min: "0",
    description: "Total tax shown on last year's return (\xA7 6654(d)(1)(B)(ii)). In dollars."
    // no default — the engine must never guess a prior-year liability
  },
  {
    id: "priorYearAGI",
    type: "money",
    min: "0",
    description: "AGI shown on last year's return \u2014 over $150,000 ($75,000 MFS) triggers the 110% safe-harbor prong (\xA7 6654(d)(1)(C)). In dollars."
    // no default
  },
  // ── business-entity classification & entity-level tax ────────────────
  {
    id: "entityLegalForm",
    type: "enum",
    enumValues: ["llc", "corporation"],
    description: "The business's state-law legal form: a limited liability company, or a state-law corporation (a per-se corporation under Treas. Reg. \xA7 301.7701-2(b)(1))."
    // no default — the engine must never guess what was formed
  },
  {
    id: "llcMemberCount",
    type: "int",
    min: "1",
    description: "Number of members (owners) of the LLC \u2014 one member defaults to disregarded-entity treatment, two or more to partnership (Treas. Reg. \xA7 301.7701-3(b)(1))."
    // no default — only demanded for an LLC with no corporate election
  },
  {
    id: "filedForm8832CorpElection",
    type: "bool",
    description: "The entity filed a Form 8832 election to be classified as an association taxable as a corporation (Treas. Reg. \xA7 301.7701-3(c)).",
    default: {
      value: false,
      rationale: "Assumed no Form 8832 election absent contrary input"
    }
  },
  {
    id: "filedForm2553SElection",
    type: "bool",
    description: "The entity filed a timely Form 2553 S election under \xA7 1362(a)(1) (for an eligible entity this also deems association classification, Reg. \xA7 301.7701-3(c)(1)(v)(C)).",
    default: {
      value: false,
      rationale: "Assumed no S election absent contrary input"
    }
  },
  {
    id: "sCorpShareholderCount",
    type: "int",
    min: "1",
    description: "Number of shareholders, counting married couples and \xA7 1361(c)(1) family members as one (\xA7 1361(b)(1)(A): may not exceed 100)."
    // no default — only demanded when an S election exists
  },
  {
    id: "sCorpHasIneligibleShareholder",
    type: "bool",
    description: "Any shareholder is ineligible under \xA7 1361(b)(1)(B)\u2013(C): a nonresident alien, or an entity other than an estate or eligible trust/exempt organization."
    // no default — only demanded when an S election exists
  },
  {
    id: "sCorpHasMultipleStockClasses",
    type: "bool",
    description: "The corporation has more than one class of stock (\xA7 1361(b)(1)(D); differences in voting rights alone do not create a second class, \xA7 1361(c)(4))."
    // no default — only demanded when an S election exists
  },
  {
    id: "corpTaxableIncome",
    type: "money",
    min: "0",
    description: "The C corporation's taxable income BEFORE the \xA7 250 deduction, if already computed \u2014 used as-is when provided. Leave at 0 to have the engine compute it from corpGrossIncome and the deduction components (charitable/DRD/NOL machinery). CONTRACT: the \xA7 250 deduction is computed separately from corpFDDEI/corpNCTI and subtracted by the tax rule \u2014 an AS-FILED Form 1120 line 30 already nets out \xA7 250, so when providing corpFDDEI/corpNCTI enter the pre-\xA7 250 amount here (line 30 plus the \xA7 250 deduction as filed), never the net. In dollars.",
    default: {
      value: "0",
      rationale: "No line-30 amount provided \u2014 taxable income computed from the gross-income and deduction components"
    }
  },
  {
    id: "corpGrossIncome",
    type: "money",
    min: "0",
    description: "The corporation's gross income (\xA7 61), INCLUDING any dividends received, any \xA7 951 subpart F and \xA7 951A NCTI/GILTI inclusions with their \xA7 78 gross-ups (\xA7 951A(a) is a gross-income INCLUSION \u2014 the \xA7 250 deduction is computed separately from corpNCTI), and any \xA7 245A-eligible foreign-sub dividends. In dollars."
    // no default — the engine must never guess income
  },
  {
    id: "corpOrdinaryDeductions",
    type: "money",
    min: "0",
    description: "Ordinary business deductions (salaries, rents, prior-year amortization, \u2026) \u2014 everything EXCEPT charitable contributions, the dividends-received deduction, and NOLs, which have their own limited rules. Enter compensation already limited by \xA7 162(m) (no deduction for a covered employee's remuneration over $1,000,000 at a publicly held corporation \u2014 not modeled, attested). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no ordinary deductions absent contrary input"
    }
  },
  {
    id: "corpCharitableContributions",
    type: "money",
    min: "0",
    description: "The corporation's charitable contributions \u2014 current-year gifts plus allowable prior-year \xA7 170(d)(2) carryovers being used (both subject to the same ceiling and, from 2026, the OBBBA floor). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no corporate charitable contributions absent contrary input"
    }
  },
  {
    id: "corpDividendsReceived",
    type: "money",
    min: "0",
    description: "Dividends received from other taxable domestic corporations (\xA7 243 DRD; must also be included in corpGrossIncome). Enter only dividends on stock meeting the \xA7 246(c) holding period (held more than 45 days during the 91-day window around the ex-dividend date; 90/181 for certain preferred) \u2014 attested; \xA7 1059 extraordinary-dividend basis reduction not modeled. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no intercorporate dividends absent contrary input"
    }
  },
  {
    id: "corpDrdOwnershipTier",
    type: "enum",
    enumValues: ["under-20", "20-to-80", "80-plus"],
    description: "Ownership of the dividend-paying corporation: under 20% (50% DRD), 20\u201380% (65% DRD), or 80%+ affiliated (100% DRD, \xA7 243(a)(3))."
    // no default — only demanded when dividends were received
  },
  {
    id: "corpNOLCarryforward",
    type: "money",
    min: "0",
    description: "Net operating loss carryforward available this year (\xA7 172: deduction limited to 80% of taxable income before the NOL; post-TCJA, no carrybacks). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no NOL carryforward absent contrary input"
    }
  },
  {
    id: "corpPriorYearTax",
    type: "money",
    min: "0",
    description: "Tax shown on the corporation's preceding-year return (\xA7 6655(d) prior-year prong; unavailable if that year showed zero tax or was short). In dollars."
    // no default — only demanded for corporate estimated-tax questions
  },
  {
    id: "corpIsLargeCorporation",
    type: "bool",
    description: "The corporation had taxable income of $1,000,000 or more in any of the 3 preceding taxable years (\xA7 6655(g)(2) 'large corporation' \u2014 may not use the prior-year safe harbor)."
    // no default — only demanded for corporate estimated-tax questions
  },
  {
    id: "corpFDDEI",
    type: "money",
    min: "0",
    description: "TY2025: foreign-derived intangible income (FDII); TY2026+: foreign-derived deduction eligible income (FDDEI, OBBBA \u2014 QBAI abolished). The \xA7 250 deduction applies 37.5% (2025) / 33.34% (2026+). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no foreign-derived eligible income absent contrary input"
    }
  },
  {
    id: "corpNCTI",
    type: "money",
    min: "0",
    description: "TY2025: the \xA7 951A GILTI inclusion (with its \xA7 78 gross-up); TY2026+: net CFC tested income (NCTI, OBBBA). The \xA7 250 deduction applies 50% (2025) / 40% (2026+). The inclusion itself is GROSS INCOME (\xA7 951A(a)) \u2014 it must also be in corpGrossIncome with its \xA7 78 gross-up; this fact drives only the \xA7 250 deduction and FTC basket. The per-CFC tested-income aggregation is not modeled. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no CFC inclusion absent contrary input"
    }
  },
  {
    id: "generalBusinessCredits",
    type: "money",
    min: "0",
    description: "Aggregate current-year \xA7 38(b) general business credits (e.g. the \xA7 41 research credit target's result) \u2014 limited under \xA7 38(c). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no general business credits absent contrary input"
    }
  },
  {
    id: "aggregateBusinessLoss",
    type: "money",
    min: "0",
    description: "The noncorporate taxpayer's net aggregate business LOSS for the year, entered as a positive number (\xA7 461(l): the excess over $313k/$626k in 2025, $256k/$512k in 2026, is disallowed and becomes an NOL). In dollars."
    // no default — only demanded by the excess-business-loss target
  },
  {
    id: "corpForeignTaxesGeneral",
    type: "money",
    min: "0",
    description: "Creditable foreign income taxes in the \xA7 904(d) GENERAL basket. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no general-basket foreign taxes absent contrary input"
    }
  },
  {
    id: "corpForeignSourceIncomeGeneral",
    type: "money",
    min: "0",
    description: "Foreign-source taxable income in the general basket (\xA7 904 limitation numerator; \xA7 861 expense allocation attested). In dollars."
    // no default — only demanded when general-basket foreign taxes exist
  },
  {
    id: "corpForeignTaxesNCTI",
    type: "money",
    min: "0",
    description: "Foreign taxes attributable to the \xA7 951A basket (GILTI/NCTI) \u2014 the \xA7 960(d) deemed-paid credit takes the 80% (2025) / 90% (2026+, OBBBA) allowance, no carryovers. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no CFC-basket foreign taxes absent contrary input"
    }
  },
  {
    id: "corpBaseErosionTestMet",
    type: "bool",
    description: "The corporation's base erosion percentage is 3% or more (2% for banks/securities dealers) \u2014 one of the two \xA7 59A applicable-taxpayer tests. BEAT applies only to $500M+ multinationals.",
    default: {
      value: false,
      rationale: "Assumed the 3% base-erosion test is not met absent contrary input (BEAT reaches only large multinationals with substantial related-party deductions)"
    }
  },
  {
    id: "corpBaseErosionTaxBenefits",
    type: "money",
    min: "0",
    description: "Base erosion tax benefits for the year (\xA7 59A(c)(2)) \u2014 include the base-erosion percentage of any NOL deduction (\xA7 59A(c)(1)(B)). Added back to reach modified taxable income. In dollars."
    // no default — only demanded when the base-erosion test is met
  },
  {
    id: "corpCapitalGains",
    type: "money",
    min: "0",
    description: "The corporation's capital gains for the year (\xA7 1211(a): losses offset only these; net gain is ordinary-rate income for a corporation). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no corporate capital gains absent contrary input"
    }
  },
  {
    id: "corpCapitalLosses",
    type: "money",
    min: "0",
    description: "The corporation's capital losses for the year, including prior-year \xA7 1212(a) carryovers being used (allowed only to the extent of capital gains). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no corporate capital losses absent contrary input"
    }
  },
  {
    id: "corpPortfolioDebtFinancedPercent",
    type: "int",
    min: "0",
    description: "Average indebtedness percentage (0-100) of debt-financed portfolio stock (\xA7 246A) \u2014 reduces the 50%/65% DRD proportionally; 0 = not debt-financed.",
    default: {
      value: "0",
      rationale: "Assumed the dividend-paying stock is not debt-financed absent contrary input"
    }
  },
  {
    id: "corpFilesConsolidatedReturn",
    type: "bool",
    description: "The corporation joins a consolidated return (\xA7\xA7 1501-1504) \u2014 intercompany eliminations and SRLY rules are not modeled, so this refuses.",
    default: {
      value: false,
      rationale: "Assumed a separate (non-consolidated) return absent contrary input"
    }
  },
  {
    id: "corpTIThrough3Months",
    type: "money",
    min: "0",
    description: "Corporate taxable income for the first 3 months (\xA7 6655(e) annualization, installments 1-2). In dollars."
  },
  {
    id: "corpTIThrough6Months",
    type: "money",
    min: "0",
    description: "Corporate taxable income for the first 6 months (\xA7 6655(e) annualization, installment 3). In dollars."
  },
  {
    id: "corpTIThrough9Months",
    type: "money",
    min: "0",
    description: "Corporate taxable income for the first 9 months (\xA7 6655(e) annualization, installment 4). In dollars."
  },
  {
    id: "sCorpHasAccumulatedEandP",
    type: "bool",
    description: "The S corporation has accumulated earnings and profits from C-corporation years at the close of the year (\xA7 1375 applies only then).",
    default: {
      value: false,
      rationale: "Assumed no C-year accumulated E&P absent contrary input (an always-S corporation has none)"
    }
  },
  {
    id: "sCorpPassiveInvestmentIncome",
    type: "money",
    min: "0",
    description: "The S corporation's passive investment income \u2014 royalties, rents, dividends, interest, annuities (\xA7 1375(b)(3)). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no passive investment income absent contrary input"
    }
  },
  {
    id: "sCorpGrossReceipts",
    type: "money",
    min: "0",
    description: "The S corporation's gross receipts for the year (\xA7 1375). In dollars."
    // no default — only demanded when the § 1375 branch is reachable
  },
  {
    id: "sCorpNetPassiveIncome",
    type: "money",
    min: "0",
    description: "Passive investment income net of directly-connected deductions (\xA7 1375(b)(2)). In dollars."
    // no default — only demanded when the § 1375 branch is reachable
  },
  {
    id: "sCorpTaxableIncomeAsC",
    type: "money",
    min: "0",
    description: "The S corporation's taxable income computed as if it were a C corporation (\xA7\xA7 1374(b)(1)/1375(b)(1)(B) cap). In dollars."
    // no default — only demanded when §§ 1374/1375 are reachable
  },
  {
    id: "sCorpRecognizedBuiltInGain",
    type: "money",
    min: "0",
    description: "Net recognized built-in gain during the \xA7 1374(d)(7) 5-year recognition period after a C-to-S conversion (0 if the period has passed or there was no conversion). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no recognized built-in gain absent contrary input (no C-to-S conversion in the recognition period)"
    }
  },
  {
    id: "corpAvgAdjustedFinancialStatementIncome",
    type: "money",
    min: "0",
    description: "3-year-average adjusted financial statement income (\xA7 56A) \u2014 over $1 billion triggers the corporate AMT, which this engine refuses to approximate. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed 3-year-average AFSI at or below the $1 billion CAMT threshold absent contrary input"
    }
  },
  {
    id: "corpEquipmentPurchases",
    type: "money",
    min: "0",
    description: "Cost of qualified \xA7 168(k) property acquired AND placed in service this year (acquired after January 19, 2025 \u2014 100% bonus depreciation, OBBBA-permanent). EXCLUDE passenger automobiles (the \xA7 280F luxury-auto caps are not modeled) and anything entered in corpSection179Cost. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no qualified property placed in service absent contrary input"
    }
  },
  {
    id: "corpSection179Cost",
    type: "money",
    min: "0",
    description: "Cost of \xA7 179 property the corporation elects to expense \u2014 including qualified real property (roofs, HVAC, fire/security systems on nonresidential real property, \xA7 179(d)(1)(B)(ii)) that \xA7 168(k) cannot reach. Must NOT also be in corpEquipmentPurchases; EXCLUDE passenger automobiles and sport utility vehicles (the \xA7 280F caps and the \xA7 179(b)(5) SUV cap are not modeled). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no \xA7 179 election absent contrary input"
    }
  },
  {
    id: "corpFiscalYearFiler",
    type: "bool",
    description: "The corporation uses a FISCAL taxable year (or files a \xA7 443 short-period return). Fiscal and short years are not modeled \u2014 the OBBBA parameters (\xA7 250 rates, \xA7 59A 10.5%, \xA7 960(d) 90%, \xA7 448(c) $32M, the \xA7 170(b)(2) 1% floor) apply by the taxable year's BEGINNING date, and \xA7 443(b) annualization / \xA7 15 proration are not encoded.",
    default: {
      value: false,
      rationale: "Calendar taxable year assumed \u2014 fiscal-year and short-period returns refuse"
    }
  },
  {
    id: "corpIsCoveredCorporation",
    type: "bool",
    description: "The corporation is a 'covered corporation' for the \xA7 4501 stock-repurchase excise tax: a domestic corporation whose stock is traded on an established securities market (\xA7 4501(b)).",
    default: {
      value: false,
      rationale: "Assumed not a listed domestic corporation absent contrary input"
    }
  },
  {
    id: "corpStockRepurchasedFMV",
    type: "money",
    min: "0",
    description: "Fair market value of the corporation's own stock repurchased (\xA7 317(b) redemptions and economically similar transactions) during the taxable year, for the \xA7 4501 excise. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no stock repurchases absent contrary input"
    }
  },
  {
    id: "corpStockIssuedFMV",
    type: "money",
    min: "0",
    description: "Fair market value of stock issued by the corporation during the taxable year (including to employees) \u2014 netted against repurchases under \xA7 4501(c)(3). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no stock issuances absent contrary input"
    }
  },
  {
    id: "corpSection245ADividends",
    type: "money",
    min: "0",
    description: "Foreign-source portion of dividends received from specified 10-percent-owned foreign corporations, eligible for the \xA7 245A participation-exemption DRD (100%). Must also be included in corpGrossIncome. Attested by entry: US-shareholder status, NOT a \xA7 245A(e) hybrid dividend, and the \xA7 246(c)(5) 365-day holding period met; no foreign tax credit is allowed for the deducted portion (\xA7 245A(d)) \u2014 keep these taxes out of the FTC inputs. In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no \xA7 245A-eligible foreign-sub dividends absent contrary input"
    }
  },
  {
    id: "corpDomesticResearch",
    type: "money",
    min: "0",
    description: "Domestic research or experimental expenditures \u2014 currently deductible under \xA7 174A (OBBBA, permanent from 2025). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no domestic research expenditures absent contrary input"
    }
  },
  {
    id: "corpForeignResearch",
    type: "money",
    min: "0",
    description: "FOREIGN research or experimental expenditures paid this year \u2014 capitalized and amortized over 15 years (\xA7 174; first-year deduction is 1/30 under the midpoint convention). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no foreign research expenditures absent contrary input"
    }
  },
  {
    id: "corpBusinessInterestExpense",
    type: "money",
    min: "0",
    description: "Business interest expense (\xA7 163(j): limited to 30% of EBITDA-based ATI unless the \xA7 448(c) gross-receipts test is met). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no business interest expense absent contrary input"
    }
  },
  {
    id: "corpAvgGrossReceipts3yr",
    type: "money",
    min: "0",
    description: "3-year-average annual gross receipts (\xA7 448(c) test: $31M for 2025, $32M for 2026 \u2014 at or below it the \xA7 163(j) limit does not apply). In dollars."
    // no default — only demanded when there is business interest expense
  },
  {
    id: "corpDividendsPaid",
    type: "money",
    min: "0",
    description: "Dividends paid during the year (the \xA7 561 dividends-paid deduction for the accumulated-earnings computation). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no dividends paid absent contrary input"
    }
  },
  {
    id: "corpAccumulatedEandPStart",
    type: "money",
    min: "0",
    description: "Accumulated earnings and profits at the close of the PRECEDING year (\xA7 535(c)(2) minimum-credit offset). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no prior accumulated earnings and profits absent contrary input"
    }
  },
  {
    id: "corpReasonableNeedsRetention",
    type: "money",
    min: "0",
    description: "Earnings retained for the reasonable needs of the business (\xA7\xA7 535(c)(1), 537 \u2014 documented needs; part of the accumulated earnings credit). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no documented reasonable-needs retention absent contrary input"
    }
  },
  {
    id: "corpIsPersonalServiceCorp",
    type: "bool",
    description: "The corporation's principal function is services in health, law, engineering, architecture, accounting, actuarial science, performing arts, or consulting (\xA7 535(c)(2)(B): $150,000 minimum credit instead of $250,000).",
    default: {
      value: false,
      rationale: "Assumed not a listed service corporation absent contrary input"
    }
  },
  {
    id: "corpPHCIncome",
    type: "money",
    min: "0",
    description: "Personal holding company income (\xA7 543: dividends, interest, royalties, annuities, certain rents). In dollars."
    // no default — only demanded for PHC determinations
  },
  {
    id: "corpAdjustedOrdinaryGrossIncome",
    type: "money",
    min: "0",
    description: "Adjusted ordinary gross income (\xA7 543(b)(2)) \u2014 the 60% test base. In dollars."
    // no default — only demanded for PHC determinations
  },
  {
    id: "corpOwnedByFiveOrFewer",
    type: "bool",
    description: "More than 50% of the stock's value was owned (directly or via \xA7 544 attribution) by 5 or fewer individuals during the last half of the year (\xA7 542(a)(2))."
    // no default — only demanded for PHC determinations
  },
  {
    id: "corpUndistributedPHCIncome",
    type: "money",
    min: "0",
    description: "Undistributed personal holding company income (\xA7 545: taxable income adjusted, less federal taxes and the dividends-paid deduction). In dollars."
    // no default — only demanded when the PHC tax is computed
  },
  {
    id: "qsbsGain",
    type: "money",
    min: "0",
    description: "Gain on a sale of qualified small business stock (\xA7 1202) \u2014 original issuance, active-business, and gross-asset tests assumed satisfied (disclosed). In dollars.",
    default: {
      value: "0",
      rationale: "Assumed no QSBS gain absent contrary input"
    }
  },
  {
    id: "qsbsAcquiredAfterJuly2025",
    type: "bool",
    description: "The stock was acquired AFTER July 4, 2025 (the OBBBA tiered regime: 50/75/100% at 3/4/5 years, $15M cap) rather than under prior law ($10M cap, 100% at 5 years)."
    // no default — the acquisition date decides the whole regime
  },
  {
    id: "qsbsAcquiredAfterSept2010",
    type: "bool",
    description: "For prior-law stock: acquired after September 27, 2010 (the 100%-exclusion vintage; earlier vintages refuse)."
    // no default — only demanded for pre-OBBBA stock held 5+ years
  },
  {
    id: "qsbsHoldingPeriodYears",
    type: "int",
    min: "0",
    description: "Whole years the QSBS was held before the sale (\xA7 1202(a)/(b) holding tiers)."
    // no default — only demanded when there is QSBS gain
  },
  // ── § 6654(d)(2) annualized-income installments (cumulative periods) ──
  {
    id: "wagesThroughMar31",
    type: "money",
    min: "0",
    description: "Wages received January 1 through March 31 (Form 2210 Schedule AI column (a)). Only demanded when the year has wages. In dollars."
  },
  {
    id: "wagesThroughMay31",
    type: "money",
    min: "0",
    description: "Wages received January 1 through May 31 (Schedule AI column (b)). In dollars."
  },
  {
    id: "wagesThroughAug31",
    type: "money",
    min: "0",
    description: "Wages received January 1 through August 31 (Schedule AI column (c)). In dollars."
  },
  {
    id: "seProfitThroughMar31",
    type: "money",
    min: "0",
    description: "Self-employment net profit earned January 1 through March 31 (Schedule AI column (a)). Only demanded when the year has SE profit. In dollars."
  },
  {
    id: "seProfitThroughMay31",
    type: "money",
    min: "0",
    description: "Self-employment net profit January 1 through May 31 (Schedule AI column (b)). In dollars."
  },
  {
    id: "seProfitThroughAug31",
    type: "money",
    min: "0",
    description: "Self-employment net profit January 1 through August 31 (Schedule AI column (c)). In dollars."
  },
  // ── § 152 dependent determination (about ONE candidate person) ───────
  {
    id: "depRelationshipChild",
    type: "bool",
    description: "Candidate is the taxpayer's child, stepchild, foster child, sibling, step-sibling, or a descendant of any of them (\xA7 152(c)(2))."
  },
  {
    id: "depAge",
    type: "int",
    min: "0",
    description: "Candidate's age at the end of the year (\xA7 152(c)(3))."
  },
  {
    id: "depIsFullTimeStudent",
    type: "bool",
    description: "Candidate was a full-time student for at least 5 months (\xA7 152(f)(2)).",
    default: {
      value: false,
      rationale: "Assumed not a full-time student absent contrary input"
    }
  },
  {
    id: "depPermanentlyDisabled",
    type: "bool",
    description: "Candidate is permanently and totally disabled (\xA7 152(c)(3)(B)).",
    default: {
      value: false,
      rationale: "Assumed not permanently and totally disabled absent contrary input"
    }
  },
  {
    id: "depYoungerThanTaxpayer",
    type: "bool",
    description: "Candidate is younger than the taxpayer (\xA7 152(c)(3)(A)).",
    default: {
      value: true,
      rationale: "Assumed the candidate is younger than the taxpayer absent contrary input"
    }
  },
  {
    id: "depLivedWithTaxpayerOverHalfYear",
    type: "bool",
    description: "Candidate had the same principal residence as the taxpayer for more than half the year (\xA7 152(c)(1)(B))."
    // no default — a core test, always asked
  },
  {
    id: "depProvidedOwnSupportOverHalf",
    type: "bool",
    description: "Candidate provided more than half of their own support (\xA7 152(c)(1)(D)).",
    default: {
      value: false,
      rationale: "Assumed the candidate did not provide over half their own support"
    }
  },
  {
    id: "depFilesJointReturn",
    type: "bool",
    description: "Candidate files a joint return with a spouse (other than a refund-only claim) (\xA7 152(c)(1)(E)).",
    default: {
      value: false,
      rationale: "Assumed the candidate does not file a joint return"
    }
  },
  {
    id: "depRelationshipRelative",
    type: "bool",
    description: "Candidate bears a \xA7 152(d)(2) relationship to the taxpayer (parent, grandparent, sibling, in-law, etc.) or lived in the household all year."
    // no default
  },
  {
    id: "depGrossIncome",
    type: "money",
    min: "0",
    description: "Candidate's gross income for the year (\xA7 152(d)(1)(B) limit: $5,200 TY2025 / $5,300 TY2026). In dollars."
    // no default
  },
  {
    id: "taxpayerProvidedOverHalfSupport",
    type: "bool",
    description: "The taxpayer provided more than half of the candidate's support (\xA7 152(d)(1)(C))."
    // no default
  },
  {
    id: "depIsQualifyingChildOfAnother",
    type: "bool",
    description: "Candidate is the qualifying child of the taxpayer or any other taxpayer (\xA7 152(d)(1)(D)).",
    default: {
      value: false,
      rationale: "Assumed not anyone's qualifying child absent contrary input"
    }
  },
  {
    id: "hasMultipleSupportAgreement",
    type: "bool",
    description: "A \xA7 152(d)(3) multiple-support agreement is in place for the candidate: the group together provided over half the support, no one person provided over half, each member could otherwise claim the candidate, and every other over-10% contributor signed a Form 2120 waiver.",
    default: {
      value: false,
      rationale: "Assumed no multiple-support agreement absent contrary input"
    }
  },
  {
    id: "taxpayerProvidedOver10PercentSupport",
    type: "bool",
    description: "The taxpayer contributed over 10 percent of the candidate's support (\xA7 152(d)(3)(D) \u2014 the support test under a multiple-support agreement)."
    // no default — only demanded when a multiple-support agreement exists
  },
  {
    id: "depDivorcedParentsRule",
    type: "bool",
    description: "\xA7 152(e) applies to the candidate child: the parents are divorced, separated, or lived apart the last 6 months of the year; the child received over half their support from the parents and was in their custody over half the year.",
    default: {
      value: false,
      rationale: "Assumed the divorced-parents rule does not apply absent contrary input"
    }
  },
  {
    id: "taxpayerIsCustodialParent",
    type: "bool",
    description: "The taxpayer is the custodial parent (the parent with whom the child resided the greater number of nights, \xA7 152(e)(4)(A))."
    // no default — only demanded when § 152(e) applies; the engine must not
    // guess which side of the release the taxpayer is on
  },
  {
    id: "custodialParentReleasedClaim",
    type: "bool",
    description: "The custodial parent signed a written declaration (Form 8332) releasing the claim to the child for this year (\xA7 152(e)(2))."
    // no default — only demanded when § 152(e) applies
  },
  {
    id: "isAtLeastAge25",
    type: "bool",
    description: "Taxpayer attained age 25 before the close of the taxable year (26 U.S.C. \xA7 32(c)(1)(A)(ii)(II), childless EITC age floor)."
    // no default — only demanded when the childless-EITC branch is actually
    // reachable; the engine must not guess an eligibility age
  },
  {
    id: "spouseItemizes",
    type: "bool",
    description: "Spouse itemizes deductions on a separate return (26 U.S.C. \xA7 63(c)(6)(A)) \u2014 if so, an MFS filer's standard deduction is zero."
    // no default — only demanded for married-filing-separately evaluations
  },
  // ---- Pennsylvania deep pack (state-pa.ts) -------------------------------
  // PA classes are TRANSCRIBED separately from the federal facts: PA
  // classification diverges (capital-gain distributions are dividends,
  // 401(k) deferrals are taxable compensation, eligible retirement income is
  // exempt), so nothing here is auto-mapped from taxableInterest etc.
  {
    id: "paCompensationAdjustment",
    type: "money",
    description: "PA compensation adjustment: W-2 Box 16 total MINUS Box 1 total (PA taxes 401(k)/elective deferrals as compensation; active-duty pay outside PA and other exempt items reduce it). May be negative. Default 0 assumes Box 16 = Box 1. Taxable early retirement-plan/IRA distributions under PA's cost-recovery method also go here. In dollars.",
    default: { value: "0", rationale: "PA W-2 Box 16 assumed equal to federal Box 1 wages absent contrary input" }
  },
  {
    id: "paUnreimbursedBusinessExpenses",
    type: "money",
    min: "0",
    description: "PA Schedule UE unreimbursed employee business expenses (PA-40 line 1b) \u2014 a compensation-class expense, not a line-10 deduction. In dollars.",
    default: { value: "0", rationale: "No unreimbursed employee business expenses claimed absent contrary input" }
  },
  {
    id: "paInterestIncome",
    type: "money",
    min: "0",
    description: "PA-taxable interest income (PA-40 line 2, gross class \u2014 no expenses; forfeited-interest penalties may offset within the class before entry). Includes commercial-annuity income taxable as PA interest. In dollars.",
    default: { value: "0", rationale: "No PA interest income absent contrary input" }
  },
  {
    id: "paDividendIncome",
    type: "money",
    min: "0",
    description: "PA-taxable dividend income (PA-40 line 3, gross class) INCLUDING mutual-fund capital gains distributions, which PA classifies as dividends. In dollars.",
    default: { value: "0", rationale: "No PA dividend income absent contrary input" }
  },
  {
    id: "paBusinessNetIncome",
    type: "money",
    description: "Taxpayer's own net income or LOSS from business/profession/farm (PA-40 line 4 class, after within-class netting of the taxpayer's own activities). Negative allowed; a loss never offsets other classes or the spouse. In dollars.",
    default: { value: "0", rationale: "No business income or loss absent contrary input" }
  },
  {
    id: "paSpouseBusinessNetIncome",
    type: "money",
    description: "Spouse's own net income or LOSS from business/profession/farm (PA-40 line 4 class). Negative allowed; never netted against the taxpayer's. In dollars.",
    default: { value: "0", rationale: "No spouse business income or loss absent contrary input" }
  },
  {
    id: "paPropertyGainNet",
    type: "money",
    description: "Taxpayer's own net gain or LOSS from sale/exchange/disposition of property (PA-40 line 5 class). Negative allowed; no carryover, no cross-class or spousal offset. In dollars.",
    default: { value: "0", rationale: "No property gain or loss absent contrary input" }
  },
  {
    id: "paSpousePropertyGainNet",
    type: "money",
    description: "Spouse's own net gain or LOSS from disposition of property (PA-40 line 5 class). Negative allowed; never netted against the taxpayer's. In dollars.",
    default: { value: "0", rationale: "No spouse property gain or loss absent contrary input" }
  },
  {
    id: "paRentRoyaltyNet",
    type: "money",
    description: "Taxpayer's own net income or LOSS from rents/royalties/patents/copyrights (PA-40 line 6 class). Negative allowed; short-term rentals (<30 days) belong in the business class instead. In dollars.",
    default: { value: "0", rationale: "No rent/royalty income or loss absent contrary input" }
  },
  {
    id: "paSpouseRentRoyaltyNet",
    type: "money",
    description: "Spouse's own net income or LOSS from rents/royalties/patents/copyrights (PA-40 line 6 class). Negative allowed; never netted against the taxpayer's. In dollars.",
    default: { value: "0", rationale: "No spouse rent/royalty income or loss absent contrary input" }
  },
  {
    id: "paEstateTrustIncome",
    type: "money",
    min: "0",
    description: "PA-taxable income from estates or trusts (PA-40 line 7, Schedule J) \u2014 an estate or trust cannot distribute a loss, so this is never negative. In dollars.",
    default: { value: "0", rationale: "No estate/trust income absent contrary input" }
  },
  {
    id: "paGamblingWinnings",
    type: "money",
    min: "0",
    description: "PA-taxable gambling and lottery winnings (PA-40 line 8, Schedule T) net of wager costs (PA Lottery ticket costs deductible only for tickets bought on/after 1/1/2016); noncash PA Lottery prizes are exempt. In dollars.",
    default: { value: "0", rationale: "No gambling winnings absent contrary input" }
  },
  {
    id: "pa529Contributions",
    type: "money",
    min: "0",
    description: "PA Schedule O \xA7 529 tuition-program contribution deduction, ALREADY capped by the caller at $19,000 per beneficiary per taxpayer-spouse (2025) \u2014 the corpus cannot see per-beneficiary detail. No deduction for 529-to-529 rollovers or beneficiary changes. In dollars.",
    default: { value: "0", rationale: "No 529 contributions claimed absent contrary input" }
  },
  {
    id: "paAbleContributions",
    type: "money",
    min: "0",
    description: "PA Schedule O \xA7 529A ABLE contribution deduction, capped by the caller at the annual federal gift-tax exclusion ($19,000 for 2025). In dollars.",
    default: { value: "0", rationale: "No ABLE contributions claimed absent contrary input" }
  },
  {
    id: "paMsaHsaContributions",
    type: "money",
    min: "0",
    description: "PA Schedule O Medical Savings Account + Health Savings Account contribution deductions \u2014 limited to the amounts allowed for FEDERAL income tax purposes (supply the federal-allowed total). In dollars.",
    default: { value: "0", rationale: "No MSA/HSA deduction claimed absent contrary input" }
  },
  {
    id: "paSpDependentChildren",
    type: "int",
    min: "0",
    description: "PA Schedule SP dependent CHILDREN count (natural/adopted/step; grandchild of a grandparent; foster child of a foster parent \u2014 never aunts/uncles/unrelated persons) claimable as federal dependents; adult qualifying children count. Raises the Tax Forgiveness eligibility-income threshold $9,500 each.",
    default: {
      value: "0",
      rationale: "Conservative: no SP dependent children assumed \u2014 understates the forgiveness threshold, never overstates the credit"
    }
  },
  {
    id: "paEligibilityIncomeAddbacks",
    type: "money",
    min: "0",
    description: "PA Schedule SP Section III nontaxable add-backs to eligibility income: nontaxable interest/dividends/gains, alimony received, insurance proceeds and inheritances (incl. 1099-R code-4 box 1), gifts/awards/prizes (incl. noncash PA Lottery), non-PA income, nontaxable military pay (not combat), excluded home-sale gain, nontaxable educational assistance, outside cash support. Do NOT add Social Security/RRB, eligible retirement benefits, child support, military pensions, workers' comp, personal-injury damages, or sick/disability pay. In dollars.",
    default: { value: "0", rationale: "No nontaxable eligibility-income add-backs absent contrary input" }
  },
  {
    id: "paResidentCredit",
    type: "money",
    min: "0",
    description: "PA resident credit for income tax paid to other states (PA-40 line 22, Schedule G-L; not allowed for reciprocal-state compensation: IN, MD, NJ, OH, VA, WV). Subtracts from tax BEFORE Tax Forgiveness per Schedule SP Section IV. In dollars.",
    default: { value: "0", rationale: "No other-state tax credit claimed absent contrary input" }
  }
];

// ../corpus-us-federal/dist/rules/adjustments.js
var fact = (factId) => ({ kind: "fact", factId });
var money = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef = (ruleId) => ({ kind: "rule", ruleId });
var param = (name) => ({ kind: "param", name });
var zero = money("0");
var isStatus = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact("filingStatus"),
  right: { kind: "enum", value: status }
});
function hsaRule(version, effectiveFrom, effectiveTo, selfCents, familyCents, source, yearLabel) {
  return {
    id: "us.federal.hsa_deduction",
    version,
    jurisdiction: "us.federal",
    title: `HSA deduction (TY${yearLabel}; HDHP eligibility assumed)`,
    citation: {
      source: `26 U.S.C. \xA7 223; ${source}`,
      section: "\xA7 223(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/223",
      excerpt: `Deduction for HSA contributions limited to the annual limitation by coverage type, plus the $1,000 additional amount for individuals who have attained age 55 (\xA7 223(b)(3)). Excess contributions are not deducted (and their 6% excise tax is not modeled).`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      limitSelf: { value: selfCents, type: "money" },
      limitFamily: { value: familyCents, type: "money" },
      catchUp: { value: "100000", type: "money" }
      // $1,000, statutory
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "gt", left: fact("hsaContribution"), right: zero },
      then: {
        kind: "min",
        args: [
          fact("hsaContribution"),
          {
            kind: "add",
            args: [
              {
                kind: "match",
                on: fact("hsaCoverage"),
                cases: [
                  { when: "self", value: param("limitSelf") },
                  { when: "family", value: param("limitFamily") }
                ]
              },
              {
                kind: "if",
                cond: fact("isAge55OrOlder"),
                then: param("catchUp"),
                else: zero
              }
            ]
          }
        ]
      },
      else: zero
    }
  };
}
function studentLoanRule(version, effectiveFrom, effectiveTo, jointThresholdCents, jointRangeCents, source, yearLabel) {
  const magi2 = ruleRef("us.federal.agi_before_student_loan");
  const threshold2 = {
    kind: "if",
    cond: isStatus("mfj"),
    then: param("thresholdJoint"),
    else: param("threshold")
  };
  const range = {
    kind: "if",
    cond: isStatus("mfj"),
    then: param("rangeJoint"),
    else: param("range")
  };
  const capped = {
    kind: "min",
    args: [fact("studentLoanInterest"), param("cap")]
  };
  return {
    id: "us.federal.student_loan_interest_deduction",
    version,
    jurisdiction: "us.federal",
    title: `Student loan interest deduction (TY${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 221; ${source}`,
      section: "\xA7 221(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/221",
      excerpt: `$2,500 maximum, reduced by the amount which bears the same ratio as MAGI in excess of the threshold bears to the phase-out range. Married taxpayers filing separately may not claim it (\xA7 221(e)(2)). MAGI computed before this deduction (\xA7 221(b)(2)(C)).`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      cap: { value: "250000", type: "money" },
      // $2,500, not indexed
      threshold: { value: "8500000", type: "money" },
      // $85,000
      range: { value: "1500000", type: "money" },
      // $15,000
      thresholdJoint: { value: jointThresholdCents, type: "money" },
      rangeJoint: { value: jointRangeCents, type: "money" }
    },
    formula: {
      kind: "if",
      cond: isStatus("mfs"),
      then: zero,
      else: {
        kind: "if",
        cond: { kind: "cmp", op: "gt", left: fact("studentLoanInterest"), right: zero },
        then: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: capped,
            right: {
              // capped × min(excess, range) / range — the § 221(b)(2) ratio
              kind: "mulDiv",
              a: capped,
              b: {
                kind: "min",
                args: [
                  { kind: "max0", arg: { kind: "sub", left: magi2, right: threshold2 } },
                  range
                ]
              },
              c: range,
              round: "half-up"
            }
          }
        },
        else: zero
      }
    }
  };
}
function sepRule(version, effectiveFrom, effectiveTo, limitCents, source, yearLabel, limitLabel) {
  return {
    id: "us.federal.sep_deduction",
    version,
    jurisdiction: "us.federal",
    title: `SEP / solo-401(k) employer contribution deduction (TY${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7\xA7 404(a)(8), (h), 415(c); ${source}`,
      section: "\xA7 404(h); \xA7 415(c)(1)(A)",
      url: "https://www.law.cornell.edu/uscode/text/26/404",
      excerpt: `Employer contributions for a self-employed individual are deductible up to 25 percent of net earnings computed AFTER the \xBD-SE-tax deduction and after the contribution itself \u2014 algebraically 20 percent of (net profit \u2212 \xBD SE tax) \u2014 and never more than the \xA7 415(c) annual addition limit (${limitLabel} for ${yearLabel}, per the notice). [Employee elective deferrals to a solo 401(k) are not separately modeled; excess contributions are not deducted and the 10% \xA7 4972 excise is not modeled \u2014 disclosed.]`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      annualAdditionLimit: { value: limitCents, type: "money" }
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "gt", left: fact("sepContribution"), right: zero },
      then: {
        kind: "min",
        args: [
          fact("sepContribution"),
          // 25% of (profit − ½SE − contribution) ⇒ 20% of (profit − ½SE)
          {
            kind: "mulRate",
            base: {
              kind: "max0",
              arg: {
                kind: "sub",
                left: fact("selfEmploymentNetProfit"),
                right: ruleRef("us.federal.se_tax_half_deduction")
              }
            },
            rate: { num: "20", den: "100" },
            round: "half-up"
          },
          param("annualAdditionLimit")
        ]
      },
      else: zero
    }
  };
}
var adjustmentRules = [
  hsaRule(1, "2025-01-01", "2026-01-01", "430000", "855000", "Rev. Proc. 2024-25", "2025"),
  hsaRule(2, "2026-01-01", "2027-01-01", "440000", "875000", "Rev. Proc. 2025-19", "2026"),
  sepRule(1, "2025-01-01", "2026-01-01", "7000000", "Notice 2024-80", "2025", "$70,000"),
  sepRule(2, "2026-01-01", "2027-01-01", "7200000", "Notice 2025-67", "2026", "$72,000"),
  {
    id: "us.federal.sehi_deduction",
    version: 1,
    jurisdiction: "us.federal",
    title: "Self-employed health insurance deduction (\xA7 162(l))",
    citation: {
      source: "26 U.S.C. \xA7 162(l)",
      section: "\xA7 162(l)(1), (2)",
      url: "https://www.law.cornell.edu/uscode/text/26/162",
      excerpt: "\u2026there shall be allowed as a deduction\u2026 the amount paid during the taxable year for insurance which constitutes medical care for the taxpayer, the taxpayer's spouse, and dependents \u2014 not to exceed the earned income derived from the trade or business (here: net profit \u2212 \xBD SE tax \u2212 the SEP deduction). [Assumed and disclosed: not eligible for any month to participate in an employer-subsidized plan (\xA7 162(l)(2)(B)); the >2%-S-corporation-shareholder W-2 route is not modeled \u2014 this rule keys on self-employment profit.]"
    },
    effectiveFrom: "2025-01-01",
    // statutory, unindexed: open-ended
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "gt",
        left: fact("selfEmployedHealthPremiums"),
        right: zero
      },
      then: {
        kind: "min",
        args: [
          fact("selfEmployedHealthPremiums"),
          {
            kind: "max0",
            arg: {
              kind: "sub",
              left: fact("selfEmploymentNetProfit"),
              right: {
                kind: "add",
                args: [
                  ruleRef("us.federal.se_tax_half_deduction"),
                  ruleRef("us.federal.sep_deduction")
                ]
              }
            }
          }
        ]
      },
      else: zero
    }
  },
  studentLoanRule(
    1,
    "2025-01-01",
    "2026-01-01",
    "17000000",
    "3000000",
    // $170,000 threshold, $30,000 range
    "Rev. Proc. 2024-40 \xA7 2.30",
    "2025"
  ),
  studentLoanRule(
    2,
    "2026-01-01",
    "2027-01-01",
    "17500000",
    "3000000",
    // $175,000 threshold, $30,000 range ($175k–$205k)
    "Rev. Proc. 2025-32 \xA7 4.29",
    "2026"
  )
];

// ../corpus-us-federal/dist/rules/income-tax.js
var fact2 = (factId) => ({ kind: "fact", factId });
var money2 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef2 = (ruleId) => ({ kind: "rule", ruleId });
var RATES = ["10", "12", "22", "24", "32", "35", "37"];
function table(lowerBoundsDollars) {
  return lowerBoundsDollars.map((dollars2, i) => ({
    threshold: String(BigInt(dollars2) * 100n),
    rate: { num: RATES[i], den: "100" }
  }));
}
var d = (dollars2) => money2(String(BigInt(dollars2) * 100n));
var B25 = {
  single: table([0, 11925, 48475, 103350, 197300, 250525, 626350]),
  mfj: table([0, 23850, 96950, 206700, 394600, 501050, 751600]),
  hoh: table([0, 17e3, 64850, 103350, 197300, 250500, 626350]),
  mfs: table([0, 11925, 48475, 103350, 197300, 250525, 375800])
};
var TY2025 = {
  single: { brackets: B25.single, zeroMax: 48350, fifteenMax: 533400 },
  mfj: { brackets: B25.mfj, zeroMax: 96700, fifteenMax: 600050 },
  qss: { brackets: B25.mfj, zeroMax: 96700, fifteenMax: 600050 },
  hoh: { brackets: B25.hoh, zeroMax: 64750, fifteenMax: 566700 },
  mfs: { brackets: B25.mfs, zeroMax: 48350, fifteenMax: 3e5 }
};
var B26 = {
  single: table([0, 12400, 50400, 105700, 201775, 256225, 640600]),
  mfj: table([0, 24800, 100800, 211400, 403550, 512450, 768700]),
  hoh: table([0, 17700, 67450, 105700, 201750, 256200, 640600]),
  mfs: table([0, 12400, 50400, 105700, 201775, 256225, 384350])
};
var TY2026 = {
  single: { brackets: B26.single, zeroMax: 49450, fifteenMax: 545500 },
  mfj: { brackets: B26.mfj, zeroMax: 98900, fifteenMax: 613700 },
  qss: { brackets: B26.mfj, zeroMax: 98900, fifteenMax: 613700 },
  hoh: { brackets: B26.hoh, zeroMax: 66200, fifteenMax: 579600 },
  mfs: { brackets: B26.mfs, zeroMax: 49450, fifteenMax: 306850 }
};
var taxable = ruleRef2("us.federal.taxable_income");
var ordinary = ruleRef2("us.federal.ordinary_taxable_income");
function statusTax(sy) {
  const pref = { kind: "sub", left: taxable, right: ordinary };
  const gain0 = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: { kind: "min", args: [d(sy.zeroMax), taxable] },
      right: ordinary
    }
  };
  const gain15 = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: {
        kind: "sub",
        left: { kind: "min", args: [d(sy.fifteenMax), taxable] },
        right: ordinary
      },
      right: gain0
    }
  };
  const gain20 = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: { kind: "sub", left: pref, right: gain0 },
      right: gain15
    }
  };
  return {
    kind: "add",
    args: [
      { kind: "brackets", base: ordinary, table: sy.brackets },
      { kind: "mulRate", base: gain15, rate: { num: "15", den: "100" }, round: "half-up" },
      { kind: "mulRate", base: gain20, rate: { num: "20", den: "100" }, round: "half-up" }
    ]
  };
}
function incomeTaxRule(version, effectiveFrom, effectiveTo, tables, yearLabel, citation) {
  return {
    id: "us.federal.income_tax_before_credits",
    version,
    jurisdiction: "us.federal",
    title: `Income tax before credits (${yearLabel} rate tables + capital gains stack)`,
    citation,
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    formula: {
      kind: "match",
      on: fact2("filingStatus"),
      cases: ["single", "mfj", "hoh", "mfs", "qss"].map((s) => ({
        when: s,
        value: statusTax(tables[s])
      }))
    }
  };
}
function bandMidpoint(o) {
  const lt = (cents2) => ({
    kind: "cmp",
    op: "lt",
    left: o,
    right: money2(cents2)
  });
  const banded = (unit, half) => ({
    kind: "add",
    args: [
      {
        kind: "mulInt",
        base: money2(unit),
        count: { kind: "stepUnits", value: o, unitCents: unit, mode: "floor" }
      },
      money2(half)
    ]
  });
  return {
    kind: "if",
    cond: lt("500"),
    // under $5
    then: money2("250"),
    else: {
      kind: "if",
      cond: lt("1500"),
      // $5–15
      then: money2("1000"),
      else: {
        kind: "if",
        cond: lt("2500"),
        // $15–25
        then: money2("2000"),
        else: {
          kind: "if",
          cond: lt("300000"),
          // $25 bands to $3,000
          then: banded("2500", "1250"),
          else: banded("5000", "2500")
          // $50 bands to $100,000
        }
      }
    }
  };
}
function statusTableTax(sy) {
  const pref = { kind: "sub", left: taxable, right: ordinary };
  const gain0 = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: { kind: "min", args: [d(sy.zeroMax), taxable] },
      right: ordinary
    }
  };
  const gain15 = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: {
        kind: "sub",
        left: { kind: "min", args: [d(sy.fifteenMax), taxable] },
        right: ordinary
      },
      right: gain0
    }
  };
  const gain20 = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: { kind: "sub", left: pref, right: gain0 },
      right: gain15
    }
  };
  return {
    kind: "add",
    args: [
      {
        kind: "roundToDollar",
        value: { kind: "brackets", base: bandMidpoint(ordinary), table: sy.brackets },
        mode: "half-up"
      },
      { kind: "mulRate", base: gain15, rate: { num: "15", den: "100" }, round: "half-up" },
      { kind: "mulRate", base: gain20, rate: { num: "20", den: "100" }, round: "half-up" }
    ]
  };
}
function taxTableRule(version, effectiveFrom, effectiveTo, tables, yearLabel, methodNote) {
  return {
    id: "us.federal.income_tax_before_credits.tax_table",
    version,
    jurisdiction: "us.federal",
    title: `IRS Tax Table method for taxable income under $100,000 (${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 3(a); ${yearLabel} Form 1040 instructions (Tax Table)`,
      section: "\xA7 3(a); Tax Table",
      url: "https://www.irs.gov/pub/irs-pdf/p1040.pdf",
      excerpt: `Filers with taxable income under $100,000 must use the Tax Table, whose entries equal the \xA7 1 bracket formula evaluated at each income band's midpoint ($0\u20135, $5\u201315, $15\u201325, $25 bands to $3,000, then $50 bands), rounded to the nearest dollar (halves up). Capital gains keep the exact QDCGT-worksheet percentages. ${methodNote}`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    applicability: {
      kind: "and",
      args: [
        { kind: "cmp", op: "lt", left: taxable, right: money2("10000000") },
        // < $100,000
        { kind: "not", arg: fact2("useFormulaMethod") }
      ]
    },
    formula: {
      kind: "match",
      on: fact2("filingStatus"),
      cases: ["single", "mfj", "hoh", "mfs", "qss"].map((s) => ({
        when: s,
        value: statusTableTax(tables[s])
      }))
    },
    overrides: { ruleId: "us.federal.income_tax_before_credits", priority: 10 }
  };
}
function ordinaryRateTaxExpr(base, statusFactId, year) {
  const tables = year === "2025" ? TY2025 : TY2026;
  const perStatus = (sy) => ({
    kind: "if",
    cond: { kind: "cmp", op: "lt", left: base, right: money2("10000000") },
    // < $100,000
    then: {
      kind: "roundToDollar",
      value: { kind: "brackets", base: bandMidpoint(base), table: sy.brackets },
      mode: "half-up"
    },
    else: { kind: "brackets", base, table: sy.brackets }
  });
  return {
    kind: "match",
    on: fact2(statusFactId),
    cases: ["single", "mfj", "hoh", "mfs", "qss"].map((s) => ({
      when: s,
      value: perStatus(tables[s])
    }))
  };
}
var incomeTaxRules = [
  {
    id: "us.federal.ordinary_taxable_income",
    version: 3,
    // v2 read the raw longTermCapitalGains fact; the § 1(h) amount is now the § 1222(11) netted result from schedule_d
    jurisdiction: "us.federal",
    title: "Taxable income taxed at ordinary rates (excludes preferential gains and qualified dividends)",
    citation: {
      source: "26 U.S.C. \xA7 1(h)(1)(A), (h)(11)",
      section: "\xA7 1(h)(1)(A), (h)(11)",
      url: "https://www.law.cornell.edu/uscode/text/26/1",
      excerpt: "\u2026a tax computed at the rates\u2026 on the greater of taxable income reduced by the net capital gain\u2026 \xA7 1(h)(11)(A): qualified dividend income shall be treated as net capital gain for purposes of this subsection."
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: taxable,
        right: {
          kind: "add",
          args: [ruleRef2("us.federal.schedule_d.preferential_lt_gain"), fact2("qualifiedDividends")]
        }
      }
    }
  },
  incomeTaxRule(3, "2025-01-01", "2026-01-01", TY2025, "2025", {
    source: "26 U.S.C. \xA7 1(h), (j); Rev. Proc. 2024-40",
    section: "\xA7 1(j)(2), (j)(5); Rev. Proc. 2024-40 \xA7\xA7 2.01, 2.03",
    url: "https://www.irs.gov/pub/irs-drop/rp-24-40.pdf",
    excerpt: "2025 ordinary rate tables (10\u201337%); maximum zero rate amounts $48,350/$96,700/$64,750/$48,350 and maximum 15% rate amounts $533,400/$600,050/$566,700/$300,000 by filing status."
  }),
  incomeTaxRule(4, "2026-01-01", "2027-01-01", TY2026, "2026", {
    source: "26 U.S.C. \xA7 1(h), (j); Rev. Proc. 2025-32",
    section: "\xA7 1(j)(2), (j)(5); Rev. Proc. 2025-32 \xA7\xA7 4.01, 4.03",
    url: "https://www.irs.gov/pub/irs-drop/rp-25-32.pdf",
    excerpt: "2026 ordinary rate tables (10\u201337%); maximum zero rate amounts $49,450/$98,900/$66,200/$49,450 and maximum 15% rate amounts $545,500/$613,700/$579,600/$306,850 by filing status."
  }),
  taxTableRule(1, "2025-01-01", "2026-01-01", TY2025, "2025", "Sampled entries verified against the printed 2025 Publication 1040 table."),
  taxTableRule(2, "2026-01-01", "2027-01-01", TY2026, "2026", "The official 2026 table publishes with the 2026 filing season; entries here are computed by the same published method over the 2026 brackets.")
];

// ../corpus-us-federal/dist/rules/amt.js
var J = "us.federal";
var fact3 = (factId) => ({ kind: "fact", factId });
var money3 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef3 = (ruleId) => ({ kind: "rule", ruleId });
var param2 = (name) => ({ kind: "param", name });
var d2 = (dollars2) => money3(String(BigInt(dollars2) * 100n));
var taxable2 = ruleRef3("us.federal.taxable_income");
var regOrdinary = ruleRef3("us.federal.ordinary_taxable_income");
function amtRule(version, from, to, year, p, cg, source, amountsNote) {
  const byStatus = (single, joint, mfs) => ({
    kind: "match",
    on: fact3("filingStatus"),
    cases: [
      { when: "single", value: param2(single) },
      { when: "hoh", value: param2(single) },
      { when: "mfj", value: param2(joint) },
      { when: "qss", value: param2(joint) },
      { when: "mfs", value: param2(mfs) }
    ]
  });
  const itemizing = {
    kind: "cmp",
    op: "gt",
    left: ruleRef3("us.federal.itemized_deductions"),
    right: {
      kind: "add",
      args: [
        ruleRef3("us.federal.standard_deduction"),
        ruleRef3("us.federal.charitable_deduction_nonitemizer")
      ]
    }
  };
  const amti = {
    kind: "add",
    args: [
      taxable2,
      {
        kind: "if",
        cond: itemizing,
        then: ruleRef3("us.federal.salt_deduction"),
        else: ruleRef3("us.federal.standard_deduction")
      },
      ruleRef3("us.federal.senior_deduction"),
      fact3("amtIsoExerciseSpread"),
      fact3("amtOtherPreferences")
    ]
  };
  const exemptionPhased = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: byStatus("exemptSingle", "exemptJoint", "exemptMfs"),
      right: {
        kind: "mulRate",
        base: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: amti,
            right: byStatus("threshSingle", "threshJoint", "threshMfs")
          }
        },
        rate: p.phaseRate,
        round: "half-up"
      }
    }
  };
  const exemption2 = {
    kind: "if",
    cond: fact3("isSubjectToKiddieTax"),
    then: {
      kind: "min",
      args: [
        exemptionPhased,
        { kind: "add", args: [fact3("wages"), param2("kiddiePlus")] }
        // earned ≈ wages
      ]
    },
    else: exemptionPhased
  };
  const base = { kind: "max0", arg: { kind: "sub", left: amti, right: exemption2 } };
  const prefRegular = { kind: "sub", left: taxable2, right: regOrdinary };
  const amtOrdinary = { kind: "max0", arg: { kind: "sub", left: base, right: prefRegular } };
  const break28 = {
    kind: "if",
    cond: {
      kind: "cmp",
      op: "eq",
      left: fact3("filingStatus"),
      right: { kind: "enum", value: "mfs" }
    },
    then: param2("break28Mfs"),
    else: param2("break28")
  };
  const tmt26_28 = {
    kind: "add",
    args: [
      {
        kind: "mulRate",
        base: { kind: "min", args: [amtOrdinary, break28] },
        rate: { num: "26", den: "100" },
        round: "half-up"
      },
      {
        kind: "mulRate",
        base: { kind: "max0", arg: { kind: "sub", left: amtOrdinary, right: break28 } },
        rate: { num: "28", den: "100" },
        round: "half-up"
      }
    ]
  };
  const gainSlices = (sy) => {
    const gain0 = {
      kind: "max0",
      arg: {
        kind: "sub",
        left: { kind: "min", args: [d2(sy.zeroMax), taxable2] },
        right: regOrdinary
      }
    };
    const gain15 = {
      kind: "max0",
      arg: {
        kind: "sub",
        left: {
          kind: "sub",
          left: { kind: "min", args: [d2(sy.fifteenMax), taxable2] },
          right: regOrdinary
        },
        right: gain0
      }
    };
    const gain20 = {
      kind: "max0",
      arg: {
        kind: "sub",
        left: { kind: "sub", left: prefRegular, right: gain0 },
        right: gain15
      }
    };
    return {
      kind: "add",
      args: [
        { kind: "mulRate", base: gain15, rate: { num: "15", den: "100" }, round: "half-up" },
        { kind: "mulRate", base: gain20, rate: { num: "20", den: "100" }, round: "half-up" }
      ]
    };
  };
  const gainsTax = {
    kind: "match",
    on: fact3("filingStatus"),
    cases: ["single", "mfj", "hoh", "mfs", "qss"].map((st) => ({
      when: st,
      value: gainSlices(cg[st])
    }))
  };
  return {
    id: "us.federal.amt",
    version,
    jurisdiction: J,
    title: `Alternative minimum tax \u2014 Form 6251 (TY${year})`,
    citation: {
      source: `26 U.S.C. \xA7\xA7 55\u201359; ${source}`,
      section: "\xA7 55(b), (d); \xA7\xA7 56\u201359; \xA7 59(j)",
      url: "https://www.law.cornell.edu/uscode/text/26/55",
      excerpt: `The excess of the tentative minimum tax \u2014 26 percent of the taxable excess up to the 28-percent breakpoint and 28 percent above, with capital gains keeping their \xA71(h) rates (Form 6251 Part III) \u2014 over the regular tax. ${amountsNote} AMTI adds back the SALT deduction when itemizing, the standard deduction otherwise (\xA7 56(b)(1)), the OBBBA senior deduction (2025 Form 6251 instructions), the \xA7 56(b)(3) ISO exercise spread, and entered preference items; the \xA7\xA7 224/225/163(h)(4)/170(p) deductions and \xA7 199A carry no addback. A \xA7 1(g) child's exemption is capped at earned income (\u2248 wages) plus the \xA7 59(j) amount. [Not modeled, disclosed: the \xA7 55(d)(3) MFS add-back, AMT credit carryforward (\xA7 53), and the new-\xA7 68 unwind question.]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: {
      exemptSingle: { value: p.exemptSingle, type: "money" },
      exemptJoint: { value: p.exemptJoint, type: "money" },
      exemptMfs: { value: p.exemptMfs, type: "money" },
      threshSingle: { value: p.threshSingle, type: "money" },
      threshJoint: { value: p.threshJoint, type: "money" },
      threshMfs: { value: p.threshMfs, type: "money" },
      break28: { value: p.break28, type: "money" },
      break28Mfs: { value: p.break28Mfs, type: "money" },
      kiddiePlus: { value: p.kiddiePlus, type: "money" }
    },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: { kind: "add", args: [tmt26_28, gainsTax] },
        right: ruleRef3("us.federal.income_tax_before_credits")
      }
    }
  };
}
var amtRules = [
  amtRule(1, "2025-01-01", "2026-01-01", "2025", {
    exemptSingle: "8810000",
    exemptJoint: "13700000",
    exemptMfs: "6850000",
    threshSingle: "62635000",
    threshJoint: "125270000",
    threshMfs: "62635000",
    phaseRate: { num: "25", den: "100" },
    break28: "23910000",
    break28Mfs: "11955000",
    kiddiePlus: "955000"
  }, TY2025, "Rev. Proc. 2024-40 \xA7\xA7 2.11\u20132.12", "For 2025: exemptions $88,100 ($137,000 joint/QSS; $68,500 MFS) phasing out at 25% of AMTI over $626,350 ($1,252,700 joint); the 28% rate applies above $239,100 ($119,550 MFS); the \xA7 59(j) kiddie amount is $9,550."),
  amtRule(2, "2026-01-01", "2027-01-01", "2026", {
    exemptSingle: "9010000",
    exemptJoint: "14020000",
    exemptMfs: "7010000",
    threshSingle: "50000000",
    threshJoint: "100000000",
    threshMfs: "50000000",
    phaseRate: { num: "50", den: "100" },
    break28: "24450000",
    break28Mfs: "12225000",
    kiddiePlus: "975000"
  }, TY2026, "Rev. Proc. 2025-32 \xA7\xA7 3.10\u20133.11; Pub. L. 119-21 (OBBBA)", "For 2026: exemptions $90,100 ($140,200 joint/QSS; $70,100 MFS) \u2014 but the OBBBA RESET the phase-out thresholds to $500,000 ($1,000,000 joint) at a DOUBLED 50% rate (the printed complete-phase-out amounts $680,200/$1,280,400 confirm it arithmetically); the 28% rate applies above $244,500 ($122,250 MFS); the \xA7 59(j) kiddie amount is $9,750.")
];

// ../corpus-us-federal/dist/rules/annualized-installments.js
var J2 = "us.federal";
var fact4 = (factId) => ({ kind: "fact", factId });
var money4 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef4 = (ruleId) => ({ kind: "rule", ruleId });
var zero2 = money4("0");
var gt0 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero2 });
var OUT_OF_SCOPE = {
  kind: "or",
  args: [
    gt0(fact4("taxableInterest")),
    gt0(fact4("longTermCapitalGains")),
    gt0(fact4("netCapitalLoss")),
    gt0(fact4("shortTermCapitalLoss")),
    gt0(fact4("longTermCapitalLoss")),
    gt0(fact4("qualifiedTips")),
    gt0(fact4("qualifiedOvertimePremium")),
    gt0(fact4("carLoanInterest")),
    gt0(fact4("charitableCashContributions")),
    gt0(fact4("stateAndLocalTaxesPaid")),
    gt0(fact4("mortgageInterestPaid")),
    gt0(fact4("medicalExpenses")),
    gt0(fact4("hsaContribution")),
    gt0(fact4("studentLoanInterest")),
    gt0(fact4("iraContribution")),
    gt0(fact4("aotcExpensesStudent1")),
    gt0(fact4("aotcExpensesStudent2")),
    gt0(fact4("aotcExpensesStudent3")),
    gt0(fact4("llcQualifiedExpenses")),
    {
      kind: "cmp",
      op: "gt",
      left: fact4("qualifyingChildren"),
      right: { kind: "int", value: "0" }
    },
    fact4("isAge65OrOlder"),
    fact4("isBlind"),
    fact4("isClaimedAsDependent"),
    fact4("isSubjectToKiddieTax")
  ]
};
var QUARTERS = [
  { n: 1, num: "4", den: "1", pct: { num: "225", den: "1000" }, oasdi: { num: "496", den: "1000" }, medicare: { num: "116", den: "1000" }, wagesFact: "wagesThroughMar31", seFact: "seProfitThroughMar31" },
  { n: 2, num: "12", den: "5", pct: { num: "45", den: "100" }, oasdi: { num: "2976", den: "10000" }, medicare: { num: "696", den: "10000" }, wagesFact: "wagesThroughMay31", seFact: "seProfitThroughMay31" },
  { n: 3, num: "3", den: "2", pct: { num: "675", den: "1000" }, oasdi: { num: "186", den: "1000" }, medicare: { num: "435", den: "10000" }, wagesFact: "wagesThroughAug31", seFact: "seProfitThroughAug31" },
  { n: 4, num: "1", den: "1", pct: { num: "90", den: "100" }, oasdi: { num: "124", den: "1000" }, medicare: { num: "29", den: "1000" }, wagesFact: null, seFact: null }
];
function cumulative(yearFactId, periodFactId) {
  if (periodFactId === null)
    return fact4(yearFactId);
  return {
    kind: "if",
    cond: gt0(fact4(yearFactId)),
    then: fact4(periodFactId),
    else: zero2
  };
}
function quarterRule(q, year, from, to, proratedLimitCents, proratedLabel, withQbiMinimum) {
  const cumWages = cumulative("wages", q.wagesFact);
  const cumSE = cumulative("selfEmploymentNetProfit", q.seFact);
  const netEarn = {
    kind: "mulRate",
    base: cumSE,
    rate: { num: "9235", den: "10000" },
    round: "half-up"
  };
  const ssRoom = {
    kind: "max0",
    arg: { kind: "sub", left: money4(proratedLimitCents), right: cumWages }
  };
  const seTax = {
    kind: "add",
    args: [
      {
        kind: "mulRate",
        base: { kind: "min", args: [netEarn, ssRoom] },
        rate: q.oasdi,
        round: "half-up"
      },
      { kind: "mulRate", base: netEarn, rate: q.medicare, round: "half-up" }
    ]
  };
  const halfSePeriod = {
    kind: "mulDiv",
    a: seTax,
    b: money4(String(BigInt(q.den))),
    // × den
    c: money4(String(2n * BigInt(q.num))),
    // ÷ 2·num
    round: "half-up"
  };
  const factorRate = { num: q.num, den: q.den };
  const agiPeriod = {
    kind: "sub",
    left: { kind: "add", args: [cumWages, cumSE] },
    right: halfSePeriod
  };
  const annualizedAgi = {
    kind: "mulRate",
    base: agiPeriod,
    rate: factorRate,
    round: "half-up"
  };
  const tiBeforeQbi = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: annualizedAgi,
      right: ruleRef4("us.federal.standard_deduction")
      // line 7: in FULL
    }
  };
  const qbiBase = {
    kind: "mulRate",
    base: { kind: "max0", arg: { kind: "sub", left: cumSE, right: halfSePeriod } },
    rate: factorRate,
    round: "half-up"
  };
  const qbiCore = {
    kind: "min",
    args: [
      { kind: "mulRate", base: qbiBase, rate: { num: "20", den: "100" }, round: "half-up" },
      { kind: "mulRate", base: tiBeforeQbi, rate: { num: "20", den: "100" }, round: "half-up" }
    ]
  };
  const qbi2 = withQbiMinimum ? {
    kind: "if",
    cond: { kind: "cmp", op: "ge", left: qbiBase, right: money4("100000") },
    then: { kind: "max", args: [qbiCore, money4("40000")] },
    // OBBBA $400 min
    else: qbiCore
  } : qbiCore;
  const ti = { kind: "max0", arg: { kind: "sub", left: tiBeforeQbi, right: qbi2 } };
  const line17 = {
    kind: "add",
    args: [ordinaryRateTaxExpr(ti, "filingStatus", year), seTax]
  };
  const line21 = { kind: "mulRate", base: line17, rate: q.pct, round: "half-up" };
  const line24 = {
    kind: "mulRate",
    base: ruleRef4("us.federal.estimated.required_annual_payment"),
    rate: { num: "25", den: "100" },
    round: "half-up"
  };
  const priorSum = q.n === 1 ? zero2 : {
    kind: "add",
    args: Array.from({ length: q.n - 1 }, (_, j) => ruleRef4(`us.federal.estimated.annualized_installment_q${j + 1}`))
  };
  const line27 = {
    kind: "min",
    args: [
      { kind: "max0", arg: { kind: "sub", left: line21, right: priorSum } },
      // line 23
      {
        // line 26, telescoped: column# × 25%RAP − Σ prior installments
        kind: "sub",
        left: { kind: "mulInt", base: line24, count: { kind: "int", value: String(q.n) } },
        right: priorSum
      }
    ]
  };
  return {
    id: `us.federal.estimated.annualized_installment_q${q.n}`,
    version: year === "2025" ? 3 : 4,
    // v1/v2 predated the per-bucket capital-loss facts in the scope guard
    jurisdiction: J2,
    title: `Annualized-income required installment #${q.n} (\xA7 6654(d)(2), TY${year})`,
    citation: {
      source: `26 U.S.C. \xA7 6654(d)(2); ${year} Form 2210 Schedule AI`,
      section: "\xA7 6654(d)(2); Schedule AI",
      url: "https://www.irs.gov/forms-pubs/about-form-2210",
      excerpt: `Installment #${q.n}: annualize the period's income by ${q.num === q.den ? "1" : Number(q.num) / Number(q.den)} (Schedule AI line 2), standard deduction in full (line 7), annualized SE tax per Part II (prorated social security limit ${proratedLabel}; printed factor-embedded rates), take ${Number(q.pct.num) / (Number(q.pct.den) / 100)}% of the resulting tax (line 20), subtract earlier columns' installments, and cap at 25% of the required annual payment plus amounts saved in earlier columns (lines 22\u201327). [Scope: the wage + self-employment core; anything else refuses. Per-column credits (line 18) not modeled \u2014 can only overstate an installment, conservative. Exact cents; the paper form's whole-dollar rounding may differ by small amounts. Cumulative period facts are demanded only for nonzero year amounts.]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: OUT_OF_SCOPE,
      then: {
        kind: "unsupported",
        reason: "the annualized-income installment method is modeled for the wage + self-employment core only \u2014 remove the other income/deduction/credit facts or use the even quarterly_payment"
      },
      else: line27
    }
  };
}
var P2025 = ["4402500", "7337500", "11740000", "17610000"];
var L2025 = ["$44,025", "$73,375", "$117,400", "$176,100"];
var P2026 = ["4612500", "7687500", "12300000", "18450000"];
var L2026 = ["$46,125 (from the $184,500 base)", "$76,875", "$123,000", "$184,500"];
var annualizedInstallmentRules = [
  ...QUARTERS.map((q) => quarterRule(q, "2025", "2025-01-01", "2026-01-01", P2025[q.n - 1], L2025[q.n - 1], false)),
  ...QUARTERS.map((q) => quarterRule(q, "2026", "2026-01-01", "2027-01-01", P2026[q.n - 1], L2026[q.n - 1], true))
];

// ../corpus-us-federal/dist/rules/capital-losses.js
var J3 = "us.federal";
var FROM = { effectiveFrom: "2025-01-01" };
var fact5 = (factId) => ({ kind: "fact", factId });
var money5 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef5 = (ruleId) => ({ kind: "rule", ruleId });
var param3 = (name) => ({ kind: "param", name });
var zero3 = money5("0");
var CITE = {
  source: "26 U.S.C. \xA7\xA7 1211(b), 1212(b)",
  url: "https://www.law.cornell.edu/uscode/text/26/1211"
};
var capitalLossRules = [
  {
    id: "us.federal.capital_loss_ordinary_offset",
    version: 3,
    // v2 predated schedule_d netting: the loss is now the § 1222(10) netted result (legacy netCapitalLoss passes through; mixed-bucket years compute), and the contradiction refusal lives in the schedule_d rules
    jurisdiction: J3,
    title: "Capital loss allowed against ordinary income ($3,000 / $1,500 MFS limit)",
    citation: {
      ...CITE,
      section: "\xA7 1211(b)",
      excerpt: "\u2026losses from sales or exchanges of capital assets shall be allowed only to the extent of the gains from such sales or exchanges, plus (if such losses exceed such gains) the lower of $3,000 ($1,500 in the case of a married individual filing a separate return), or the excess of such losses over such gains. [The loss here is the \xA7 1222(10) net capital loss after Schedule D netting \u2014 computed by us.federal.schedule_d.net_loss from the per-bucket facts, or passed through from the legacy overall netCapitalLoss fact; contradictory fact combinations refuse there.]"
    },
    ...FROM,
    output: { type: "money" },
    parameters: {
      limit: { value: "300000", type: "money" },
      // $3,000
      limitMfs: { value: "150000", type: "money" }
      // $1,500
    },
    formula: {
      kind: "min",
      args: [
        ruleRef5("us.federal.schedule_d.net_loss"),
        {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "eq",
            left: fact5("filingStatus"),
            right: { kind: "enum", value: "mfs" }
          },
          then: param3("limitMfs"),
          else: param3("limit")
        }
      ]
    }
  },
  {
    id: "us.federal.capital_loss_carryover",
    version: 2,
    // v1 read the legacy netCapitalLoss fact directly; the netted schedule_d result also covers mixed-bucket years
    jurisdiction: J3,
    title: "Capital loss carryover to the following year (simplified)",
    citation: {
      ...CITE,
      section: "\xA7 1212(b)",
      excerpt: "\u2026the excess of the net capital loss over the amount allowed under \xA7 1211(b) shall be treated as a capital loss in the succeeding taxable year. [Not modeled, disclosed: the \xA7 1212(b)(2) adjustment that preserves carryover when taxable income is below the allowed amount (this rule never OVERstates the carryover), and the short-term/long-term character split.]"
    },
    ...FROM,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: ruleRef5("us.federal.schedule_d.net_loss"),
        right: ruleRef5("us.federal.capital_loss_ordinary_offset")
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/car-loan.js
var fact6 = (factId) => ({ kind: "fact", factId });
var money6 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef6 = (ruleId) => ({ kind: "rule", ruleId });
var param4 = (name) => ({ kind: "param", name });
var zero4 = money6("0");
var isStatus2 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact6("filingStatus"),
  right: { kind: "enum", value: status }
});
var carLoanRules = [
  {
    id: "us.federal.car_loan_interest_deduction",
    version: 1,
    jurisdiction: "us.federal",
    title: "Qualified passenger vehicle loan interest deduction (OBBBA; simplified: MAGI approximated as AGI)",
    citation: {
      source: "26 U.S.C. \xA7 163(h)(4), added by Pub. L. 119-21 (OBBBA)",
      section: "\xA7 163(h)(4)",
      url: "https://www.law.cornell.edu/uscode/text/26/163",
      excerpt: "The amount of interest taken into account\u2026 shall not exceed $10,000\u2026 reduced (but not below zero) by $200 for each $1,000 (or portion thereof) by which the modified adjusted gross income of the taxpayer exceeds $100,000 ($200,000 in the case of a joint return). [TY2025\u20132028. Vehicle conditions (new, US final assembly, personal use, post-2024 purchase, VIN reported) assumed satisfied \u2014 disclosed. Married-filing-separately encoded conservatively as $0 per IRS guidance on married filers.]"
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2029-01-01",
    output: { type: "money" },
    parameters: {
      cap: { value: "1000000", type: "money" },
      // $10,000
      magiThreshold: { value: "10000000", type: "money" },
      // $100,000
      magiThresholdJoint: { value: "20000000", type: "money" }
      // $200,000
    },
    formula: {
      kind: "if",
      cond: isStatus2("mfs"),
      then: zero4,
      else: {
        kind: "if",
        cond: { kind: "cmp", op: "gt", left: fact6("carLoanInterest"), right: zero4 },
        then: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: { kind: "min", args: [fact6("carLoanInterest"), param4("cap")] },
            right: {
              kind: "mulInt",
              base: money6("20000"),
              // $200 per step
              count: {
                kind: "stepUnits",
                value: {
                  kind: "max0",
                  arg: {
                    kind: "sub",
                    left: ruleRef6("us.federal.agi"),
                    right: {
                      kind: "if",
                      cond: isStatus2("mfj"),
                      then: param4("magiThresholdJoint"),
                      else: param4("magiThreshold")
                    }
                  }
                },
                unitCents: "100000",
                // $1,000
                mode: "ceil"
                // "or portion thereof" — a CEILING, unlike §§ 224/225
              }
            }
          }
        },
        else: zero4
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/charity.js
var fact7 = (factId) => ({ kind: "fact", factId });
var money7 = (cents2) => ({ kind: "money", cents: cents2 });
var CITE2 = {
  source: "26 U.S.C. \xA7 170(p), as amended by Pub. L. 119-21 (OBBBA)",
  section: "\xA7 170(p)",
  url: "https://www.law.cornell.edu/uscode/text/26/170"
};
var charityRules = [
  {
    id: "us.federal.charitable_deduction_nonitemizer",
    version: 1,
    jurisdiction: "us.federal",
    title: "Charitable deduction for non-itemizers (not yet effective in 2025)",
    citation: {
      ...CITE2,
      excerpt: "The OBBBA's permanent non-itemizer charitable deduction applies to taxable years beginning after December 31, 2025 \u2014 for 2025 the amount is $0."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    formula: money7("0")
  },
  {
    id: "us.federal.charitable_deduction_nonitemizer",
    version: 2,
    jurisdiction: "us.federal",
    title: "Charitable deduction for non-itemizers (OBBBA, permanent from 2026)",
    citation: {
      ...CITE2,
      excerpt: "\u2026a deduction for charitable cash contributions of up to $1,000 ($2,000 in the case of a joint return) is allowed to individuals who do not itemize, for taxable years beginning after December 31, 2025; amounts are not indexed. [Cash to qualifying organizations only \u2014 donor-advised funds and certain private foundations excluded; assumed satisfied, disclosed.]"
    },
    effectiveFrom: "2026-01-01",
    // permanent: open-ended
    output: { type: "money" },
    parameters: {
      cap: { value: "100000", type: "money" },
      // $1,000
      capJoint: { value: "200000", type: "money" }
      // $2,000 (joint return)
    },
    formula: {
      kind: "min",
      args: [
        fact7("charitableCashContributions"),
        {
          kind: "match",
          on: fact7("filingStatus"),
          cases: [
            { when: "mfj", value: { kind: "param", name: "capJoint" } },
            { when: "single", value: { kind: "param", name: "cap" } },
            { when: "hoh", value: { kind: "param", name: "cap" } },
            { when: "mfs", value: { kind: "param", name: "cap" } },
            { when: "qss", value: { kind: "param", name: "cap" } }
          ]
        }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/corporate.js
var J4 = "us.federal";
var FROM2 = { effectiveFrom: "2025-01-01" };
var fact8 = (factId) => ({ kind: "fact", factId });
var money8 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef7 = (ruleId) => ({ kind: "rule", ruleId });
var enumLit = (value) => ({ kind: "enum", value });
var zero5 = money8("0");
var S_ELIGIBLE = {
  kind: "and",
  args: [
    {
      kind: "cmp",
      op: "le",
      left: fact8("sCorpShareholderCount"),
      right: { kind: "int", value: "100" }
    },
    { kind: "not", arg: fact8("sCorpHasIneligibleShareholder") },
    { kind: "not", arg: fact8("sCorpHasMultipleStockClasses") }
  ]
};
var INEFFECTIVE_S = {
  kind: "unsupported",
  reason: "the S election appears ineffective \u2014 \xA7 1361(b) eligibility failed (over 100 shareholders, an ineligible shareholder, or a second class of stock); \xA7 1362(f) inadvertent-election relief is not modeled"
};
var corporateRules = [
  {
    id: "us.federal.corp.entity_classification",
    version: 1,
    jurisdiction: J4,
    title: "Federal tax classification of the business entity (check-the-box)",
    citation: {
      source: "Treas. Reg. \xA7 301.7701-3; 26 U.S.C. \xA7\xA7 1361(b), 1362; Forms 8832 & 2553",
      section: "Reg. \xA7 301.7701-3(a), (b)(1), (c)(1)(v)(C)",
      url: "https://www.law.cornell.edu/cfr/text/26/301.7701-3",
      excerpt: "A domestic eligible entity that does not elect otherwise is classified as a partnership 'if it has two or more members' or 'disregarded as an entity separate from its owner' if it has a single owner; an eligible entity may elect association (corporate) classification on Form 8832; and 'an eligible entity that timely elects to be an S corporation under section 1362(a)(1) is treated as having made an election\u2026 to be classified as an association.' A state-law corporation is a per-se corporation (Reg. \xA7 301.7701-2(b)(1)). S status requires \xA7 1361(b): no more than 100 shareholders (family aggregation per \xA7 1361(c)(1)), no ineligible or nonresident-alien shareholders, one class of stock. [An ineffective S election REFUSES \u2014 \xA7 1362(f) relief not modeled.]"
    },
    ...FROM2,
    output: { type: "enum" },
    formula: {
      kind: "match",
      on: fact8("entityLegalForm"),
      cases: [
        {
          when: "corporation",
          value: {
            kind: "if",
            cond: fact8("filedForm2553SElection"),
            then: {
              kind: "if",
              cond: S_ELIGIBLE,
              then: enumLit("s-corporation"),
              else: INEFFECTIVE_S
            },
            else: enumLit("c-corporation")
          }
        },
        {
          when: "llc",
          value: {
            kind: "if",
            cond: fact8("filedForm2553SElection"),
            then: {
              kind: "if",
              cond: S_ELIGIBLE,
              then: enumLit("s-corporation"),
              else: INEFFECTIVE_S
            },
            else: {
              kind: "if",
              cond: fact8("filedForm8832CorpElection"),
              then: enumLit("c-corporation"),
              else: {
                kind: "if",
                cond: {
                  kind: "cmp",
                  op: "ge",
                  left: fact8("llcMemberCount"),
                  right: { kind: "int", value: "2" }
                },
                then: enumLit("partnership"),
                else: enumLit("disregarded-entity")
              }
            }
          }
        }
      ]
    }
  },
  {
    id: "us.federal.corp.income_tax",
    version: 4,
    // v3's excerpt predated the 1120 machinery; v4 also guards the line-30 × § 250 double-count
    jurisdiction: J4,
    title: "C corporation income tax \u2014 flat 21% of taxable income less the \xA7 250 deduction (\xA7 11)",
    citation: {
      source: "26 U.S.C. \xA7 11(b); \xA7 250; \xA7\xA7 55\u201356A (CAMT, guarded)",
      section: "\xA7 11(a)\u2013(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/11",
      excerpt: "A tax is hereby imposed for each taxable year on the taxable income of every corporation\u2026 The amount of the tax imposed by subsection (a) shall be 21 percent of taxable income. [Flat since the TCJA; confirmed unchanged by Pub. L. 119-21 (2025). Taxable income is the corpTaxableIncome input if given \u2014 which must be PRE-\xA7 250 (the \xA7 250 deduction is computed and subtracted HERE from corpFDDEI/corpNCTI; an as-filed line 30 already nets it out and must not be combined with those facts) \u2014 or computed by the modeled 1120 machinery (charitable limit, DRD/\xA7 245A, NOL, \xA7 168(k)/\xA7 179/\xA7 174A/\xA7 163(j)). REFUSES for corporations whose 3-year-average adjusted financial statement income exceeds $1,000,000,000 \u2014 the \xA7\xA7 55\u201356A corporate alternative minimum tax (NOT repealed by the OBBBA; interim guidance through Notice 2026-7) is not modeled. The \xA7 531 accumulated-earnings, \xA7 541 PHC, and \xA7 6655 estimated taxes are modeled as their own targets.]"
    },
    ...FROM2,
    output: { type: "money" },
    parameters: {
      camtThreshold: { value: "100000000000", type: "money" }
      // $1,000,000,000
    },
    formula: {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "gt",
        left: fact8("corpAvgAdjustedFinancialStatementIncome"),
        right: { kind: "param", name: "camtThreshold" }
      },
      then: {
        kind: "unsupported",
        reason: "3-year-average adjusted financial statement income exceeds $1 billion \u2014 the \xA7\xA7 55\u201356A corporate alternative minimum tax applies and is not modeled"
      },
      else: {
        kind: "mulRate",
        base: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: ruleRef7("us.federal.corp.taxable_income"),
            right: ruleRef7("us.federal.corp.section250_deduction")
          }
        },
        rate: { num: "21", den: "100" },
        round: "half-up"
      }
    }
  },
  {
    id: "us.federal.corp.stock_buyback_excise",
    version: 1,
    jurisdiction: J4,
    title: "Stock repurchase excise tax \u2014 1% of net repurchases (\xA7 4501)",
    citation: {
      source: "26 U.S.C. \xA7 4501 (added by Pub. L. 117-169; effective repurchases after 2022)",
      section: "\xA7 4501(a), (c)(3), (e)(3)",
      url: "https://www.law.cornell.edu/uscode/text/26/4501",
      excerpt: "There is hereby imposed on each covered corporation a tax equal to 1 percent of the fair market value of any stock of the corporation which is repurchased by such corporation during the taxable year. The amount taken into account\u2026 shall be reduced by the fair market value of any stock issued by the covered corporation during the taxable year (\xA7 4501(c)(3), the netting rule); no tax applies where the total value of the stock repurchased during the taxable year does not exceed $1,000,000 (\xA7 4501(e)(3), tested against GROSS repurchases). [A chapter-37 excise reported on Forms 720/7208 \u2014 not part of the \xA7 11 income tax and not deductible; the \xA7 4501(c)(2) affiliate rules, (d) foreign-corporation rules, and the (e)(1)\u2013(2), (4)\u2013(6) exceptions (reorganizations, ESOP contributions, RIC/REIT, dealers, \xA7 302-dividend treatment) are attested by the repurchase input.]"
    },
    ...FROM2,
    output: { type: "money" },
    parameters: {
      deMinimis: { value: "100000000", type: "money" }
      // $1,000,000
    },
    formula: {
      kind: "if",
      cond: {
        kind: "and",
        args: [
          fact8("corpIsCoveredCorporation"),
          {
            kind: "cmp",
            op: "gt",
            left: fact8("corpStockRepurchasedFMV"),
            right: { kind: "param", name: "deMinimis" }
          }
        ]
      },
      then: {
        kind: "mulRate",
        base: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: fact8("corpStockRepurchasedFMV"),
            right: fact8("corpStockIssuedFMV")
          }
        },
        rate: { num: "1", den: "100" },
        round: "half-up"
      },
      else: zero5
    }
  },
  {
    id: "us.federal.corp.entity_level_income_tax",
    version: 3,
    // v2 predated § 59A BEAT in the C-corporation branch
    jurisdiction: J4,
    title: "Entity-level federal income tax by classification (pass-throughs: $0, taxed on the owners' returns)",
    citation: {
      source: "26 U.S.C. \xA7 11; \xA7 701; \xA7 1363(a); Treas. Reg. \xA7 301.7701-3",
      section: "\xA7 701; \xA7 1363(a); \xA7 11",
      url: "https://www.law.cornell.edu/uscode/text/26/701",
      excerpt: "A partnership as such shall not be subject to the income tax imposed by this chapter \u2014 persons carrying on business as partners are liable in their separate capacities (\xA7 701); an S corporation is generally not subject to chapter-1 taxes (\xA7 1363(a)) EXCEPT the \xA7 1374 built-in-gains and \xA7 1375 excess-net-passive-income entity taxes, which are computed here; a disregarded entity's activity is reported on its owner's return (this corpus's individual chain \u2014 e.g. selfEmploymentNetProfit or k1OrdinaryBusinessIncome); a C corporation pays the flat 21% under \xA7 11."
    },
    ...FROM2,
    output: { type: "money" },
    formula: {
      kind: "match",
      on: ruleRef7("us.federal.corp.entity_classification"),
      cases: [
        { when: "disregarded-entity", value: zero5 },
        { when: "partnership", value: zero5 },
        { when: "s-corporation", value: ruleRef7("us.federal.corp.s_corp_entity_taxes") },
        {
          when: "c-corporation",
          value: {
            kind: "add",
            args: [
              ruleRef7("us.federal.corp.income_tax"),
              ruleRef7("us.federal.corp.beat")
              // § 59A: "in addition to" (zero unless attested applicable)
            ]
          }
        }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/corporate-1120.js
var J5 = "us.federal";
var FROM3 = { effectiveFrom: "2025-01-01" };
var fact9 = (factId) => ({ kind: "fact", factId });
var money9 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef8 = (ruleId) => ({ kind: "rule", ruleId });
var zero6 = money9("0");
var gt02 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero6 });
var pct = (num, base) => ({
  kind: "mulRate",
  base,
  rate: { num, den: "100" },
  round: "half-up"
});
var netCapitalGainIncluded = {
  kind: "max0",
  arg: { kind: "sub", left: fact9("corpCapitalGains"), right: fact9("corpCapitalLosses") }
};
var ati163j = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: { kind: "add", args: [fact9("corpGrossIncome"), netCapitalGainIncluded] },
    right: {
      kind: "add",
      args: [fact9("corpOrdinaryDeductions"), fact9("corpDomesticResearch")]
    }
  }
};
var active179Base = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: fact9("corpGrossIncome"),
    right: {
      kind: "add",
      args: [
        fact9("corpOrdinaryDeductions"),
        ruleRef8("us.federal.corp.depreciation_deduction"),
        ruleRef8("us.federal.corp.research_deduction"),
        ruleRef8("us.federal.corp.interest_deduction")
      ]
    }
  }
};
var preSpecialBase = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: { kind: "add", args: [fact9("corpGrossIncome"), netCapitalGainIncluded] },
    right: {
      kind: "add",
      args: [
        fact9("corpOrdinaryDeductions"),
        ruleRef8("us.federal.corp.depreciation_deduction"),
        ruleRef8("us.federal.corp.section179_deduction"),
        ruleRef8("us.federal.corp.research_deduction"),
        ruleRef8("us.federal.corp.interest_deduction")
      ]
    }
  }
};
function section179Rule(version, from, to, capCents, thresholdCents, capLabel, thresholdLabel, yearLabel, sourceLabel) {
  const dollarLimited = {
    kind: "min",
    args: [
      fact9("corpSection179Cost"),
      {
        kind: "max0",
        arg: {
          kind: "sub",
          left: money9(capCents),
          right: {
            kind: "max0",
            arg: {
              kind: "sub",
              left: fact9("corpSection179Cost"),
              right: money9(thresholdCents)
            }
          }
        }
      }
    ]
  };
  return {
    id: "us.federal.corp.section179_deduction",
    version,
    jurisdiction: J5,
    title: `\xA7 179 expensing \u2014 ${capLabel} limit, ${thresholdLabel} phase-out (${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 179, as amended by Pub. L. 119-21 (OBBBA \xA7 70306); ${sourceLabel}`,
      section: "\xA7 179(b)(1)\u2013(3)",
      url: "https://www.law.cornell.edu/uscode/text/26/179",
      excerpt: `"The aggregate cost which may be taken into account under subsection (a) for any taxable year shall not exceed ${capLabel}" \u2014 "reduced (but not below zero) by the amount by which the cost of section 179 property placed in service during such taxable year exceeds ${thresholdLabel}" (${yearLabel}; the OBBBA amounts apply to taxable years beginning after December 31, 2024). The \xA7 179(b)(3)(A) limit caps the deduction at taxable income from the active conduct of a trade or business computed without \xA7 179 \u2014 approximated here as gross income less ordinary/depreciation/research/interest deductions, capital gains excluded (enter active amounts only if this limit binds); the excess carries forward (\xA7 179(b)(3)(B), its own target). [The \xA7 179(b)(5) SUV cap ($32,000 for 2026, Rev. Proc. 2025-32 \xA7 3.24) and the \xA7 280F passenger-auto interaction are not modeled \u2014 exclude vehicles; primary use: qualified real property that \xA7 168(k) cannot reach.]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: gt02(fact9("corpSection179Cost")),
      then: { kind: "min", args: [dollarLimited, active179Base] },
      else: zero6
    }
  };
}
function section179CarryforwardRule(version, from, to, capCents, thresholdCents, yearLabel) {
  const dollarLimited = {
    kind: "min",
    args: [
      fact9("corpSection179Cost"),
      {
        kind: "max0",
        arg: {
          kind: "sub",
          left: money9(capCents),
          right: {
            kind: "max0",
            arg: {
              kind: "sub",
              left: fact9("corpSection179Cost"),
              right: money9(thresholdCents)
            }
          }
        }
      }
    ]
  };
  return {
    id: "us.federal.corp.section179_carryforward",
    version,
    jurisdiction: J5,
    title: `\xA7 179 deduction carried forward \u2014 the \xA7 179(b)(3)(B) income-limit excess (${yearLabel})`,
    citation: {
      source: "26 U.S.C. \xA7 179(b)(3)(B)",
      section: "\xA7 179(b)(3)(B)",
      url: "https://www.law.cornell.edu/uscode/text/26/179",
      excerpt: "The amount allowable as a deduction\u2026 for any taxable year shall be increased by the lesser of\u2014 (i) the aggregate amount disallowed under subparagraph (A) for all prior taxable years (to the extent not previously allowed\u2026), or (ii) the excess (if any) of\u2014 (I) the limitation of paragraphs (1) and (2)\u2026 over (II) the amount allowable as a deduction\u2026 \u2014 the INCOME-limited excess carries forward; cost above the \xA7 179(b)(1)\u2013(2) dollar limitation does not (it is simply not elected). [This target reports the carryforward generated this year; prior-year carryforwards are not tracked.]"
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: gt02(fact9("corpSection179Cost")),
      then: {
        kind: "max0",
        arg: {
          kind: "sub",
          left: dollarLimited,
          right: ruleRef8("us.federal.corp.section179_deduction")
        }
      },
      else: zero6
    }
  };
}
function corpCharitableRule(version, from, to, withFloor, yearLabel) {
  const capped = {
    kind: "min",
    args: [fact9("corpCharitableContributions"), pct("10", preSpecialBase)]
  };
  return {
    id: "us.federal.corp.charitable_deduction",
    version,
    jurisdiction: J5,
    title: `Corporate charitable deduction \u2014 10% ceiling${withFloor ? " + the OBBBA 1% floor" : ""} (${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 170(b)(2)${withFloor ? ", as amended by Pub. L. 119-21 (OBBBA)" : ""}`,
      section: "\xA7 170(b)(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/170",
      excerpt: withFloor ? "For taxable years beginning after December 31, 2025, a corporation's charitable contributions are deductible only to the extent the aggregate exceeds 1 percent of taxable income and does not exceed 10 percent of taxable income \u2014 taxable income computed without this section, part VIII (except \xA7 248) [the DRD], any NOL carryback, any capital loss carryback, and \xA7 199A(g). [Below-floor amounts are lost unless the 10% ceiling also binds; the 5-year carryover (\xA7 170(d)(2)) is not tracked \u2014 disclosed. A return with BOTH an NOL carryforward and contributions refuses: the carryforward belongs in the base and the \xA7 172 limit depends on post-charitable income \u2014 circular.]" : "A corporation's charitable deduction cannot exceed 10 percent of taxable income computed without this section, part VIII (except \xA7 248) [the DRD], any NOL carryback, any capital loss carryback, and \xA7 199A(g). No floor applies to taxable years beginning before 2026. [5-year carryover (\xA7 170(d)(2)) not tracked; a return with BOTH an NOL carryforward and contributions refuses \u2014 circular base interaction.]"
    },
    effectiveFrom: from,
    ...to ? { effectiveTo: to } : {},
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "and",
        args: [gt02(fact9("corpCharitableContributions")), gt02(fact9("corpNOLCarryforward"))]
      },
      then: {
        kind: "unsupported",
        reason: "an NOL carryforward and charitable contributions interact circularly through the \xA7 170(b)(2) base and the \xA7 172 80% limit \u2014 not modeled together"
      },
      else: withFloor ? { kind: "max0", arg: { kind: "sub", left: capped, right: pct("1", preSpecialBase) } } : capped
    }
  };
}
var base246 = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: preSpecialBase,
    right: ruleRef8("us.federal.corp.charitable_deduction")
  }
};
var drdExpr = (ratePct) => {
  const keepShareCents = {
    kind: "sub",
    left: money9("100"),
    right: {
      kind: "mulInt",
      base: money9("1"),
      count: fact9("corpPortfolioDebtFinancedPercent")
    }
  };
  const debtAdjustedDividends = {
    kind: "mulDiv",
    a: fact9("corpDividendsReceived"),
    b: keepShareCents,
    c: money9("100"),
    round: "half-up"
  };
  const tentative = pct(ratePct, debtAdjustedDividends);
  return {
    kind: "if",
    // § 246(b)(2): if base − tentative < 0 the limitation does not apply
    cond: { kind: "cmp", op: "lt", left: base246, right: tentative },
    then: tentative,
    else: { kind: "min", args: [tentative, pct(ratePct, base246)] }
  };
};
function interestRule(version, from, to, thresholdCents, thresholdLabel, yearLabel) {
  return {
    id: "us.federal.corp.interest_deduction",
    version,
    jurisdiction: J5,
    title: `Business interest deduction \u2014 \xA7 163(j) 30%-of-ATI limit, EBITDA base (${yearLabel})`,
    citation: {
      source: "26 U.S.C. \xA7 163(j), as amended by Pub. L. 119-21 (OBBBA); \xA7 448(c)",
      section: "\xA7 163(j)(1), (3), (8); \xA7 448(c)",
      url: "https://www.law.cornell.edu/uscode/text/26/163",
      excerpt: `Business interest deduction limited to 30 percent of adjusted taxable income \u2014 computed, permanently for taxable years beginning after 2024 under the OBBBA, WITHOUT depreciation or amortization (the EBITDA base). The limitation does not apply to a taxpayer meeting the \xA7 448(c) gross-receipts test: 3-year-average gross receipts of ${thresholdLabel} or less for ${yearLabel}. [Disallowed interest carries forward indefinitely (\xA7 163(j)(2)) \u2014 not tracked; business interest income and floor-plan financing are not modeled; a limited taxpayer WITH charitable contributions REFUSES \u2014 the \xA7 170(b)(2) base and ATI interact circularly.]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: {
      grossReceiptsThreshold: { value: thresholdCents, type: "money" }
    },
    formula: {
      kind: "if",
      cond: gt02(fact9("corpBusinessInterestExpense")),
      then: {
        kind: "if",
        cond: {
          kind: "cmp",
          op: "le",
          left: fact9("corpAvgGrossReceipts3yr"),
          right: { kind: "param", name: "grossReceiptsThreshold" }
        },
        then: fact9("corpBusinessInterestExpense"),
        // small-business exemption
        else: {
          kind: "if",
          cond: gt02(fact9("corpCharitableContributions")),
          then: {
            kind: "unsupported",
            reason: "the \xA7 163(j) ATI and the \xA7 170(b)(2) charitable base interact circularly \u2014 a limited taxpayer with charitable contributions is not modeled"
          },
          else: {
            kind: "min",
            args: [fact9("corpBusinessInterestExpense"), pct("30", ati163j)]
          }
        }
      },
      else: zero6
    }
  };
}
var corporate1120Rules = [
  {
    id: "us.federal.corp.depreciation_deduction",
    version: 1,
    jurisdiction: J5,
    title: "Bonus depreciation \u2014 100% expensing (\xA7 168(k), OBBBA-permanent)",
    citation: {
      source: "26 U.S.C. \xA7 168(k), as amended by Pub. L. 119-21 (OBBBA)",
      section: "\xA7 168(k)(1), (6)",
      url: "https://www.law.cornell.edu/uscode/text/26/168",
      excerpt: "The applicable percentage is 100 percent, permanently, for qualified property ACQUIRED after January 19, 2025 (the OBBBA struck the TCJA phase-down; property acquired January 1\u201319, 2025 kept the 40% rate \u2014 acquisition after January 19, 2025 is assumed and disclosed). At 100% expensing the \xA7 179 election is redundant for this property class. [Qualified-property conditions (\u226420-year recovery, original-use/used-acquisition rules) assumed satisfied \u2014 disclosed.]"
    },
    ...FROM3,
    output: { type: "money" },
    formula: fact9("corpEquipmentPurchases")
  },
  {
    id: "us.federal.corp.research_deduction",
    version: 1,
    jurisdiction: J5,
    title: "Research deduction \u2014 \xA7 174A domestic expensing + \xA7 174 foreign 15-year amortization",
    citation: {
      source: "26 U.S.C. \xA7 174A (added by Pub. L. 119-21 (OBBBA)); \xA7 174; Rev. Proc. 2025-28",
      section: "\xA7 174A(a); \xA7 174(a)(2)(B)",
      url: "https://www.law.cornell.edu/uscode/text/26/174A",
      excerpt: "Domestic research or experimental expenditures are deductible currently (\xA7 174A, permanent for taxable years beginning after 2024); FOREIGN research remains capitalized under \xA7 174 and amortized over 15 years beginning with the midpoint of the taxable year \u2014 the first-year deduction is 1/30 of the cost. [Prior-year foreign amortization belongs in ordinary deductions \u2014 the schedule is not tracked; the small-business retroactive catch-up elections of Rev. Proc. 2025-28 are not modeled \u2014 disclosed.]"
    },
    ...FROM3,
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        fact9("corpDomesticResearch"),
        {
          kind: "mulDiv",
          a: fact9("corpForeignResearch"),
          b: money9("1"),
          c: money9("30"),
          round: "half-up"
        }
      ]
    }
  },
  interestRule(1, "2025-01-01", "2026-01-01", "3100000000", "$31,000,000", "2025"),
  interestRule(2, "2026-01-01", "2027-01-01", "3200000000", "$32,000,000", "2026"),
  section179Rule(1, "2025-01-01", "2026-01-01", "250000000", "400000000", "$2,500,000", "$4,000,000", "TY2025", "Rev. Proc. 2025-32 \xA7 2.10 (OBBBA statutory amounts)"),
  section179Rule(2, "2026-01-01", "2027-01-01", "256000000", "409000000", "$2,560,000", "$4,090,000", "TY2026", "Rev. Proc. 2025-32 \xA7 3.24, verbatim"),
  section179CarryforwardRule(1, "2025-01-01", "2026-01-01", "250000000", "400000000", "TY2025"),
  section179CarryforwardRule(2, "2026-01-01", "2027-01-01", "256000000", "409000000", "TY2026"),
  corpCharitableRule(7, "2025-01-01", "2026-01-01", false, "TY2025"),
  corpCharitableRule(8, "2026-01-01", void 0, true, "TY2026+, permanent"),
  {
    id: "us.federal.corp.dividends_received_deduction",
    version: 4,
    // v3's excerpt misstated § 246A as unmodeled; v4 adds the § 246(c) attestation and the § 179-aware base
    jurisdiction: J5,
    title: "Dividends-received deduction (50%/65%/100% by ownership tier, \xA7 246(b) limit, \xA7 246A reduction)",
    citation: {
      source: "26 U.S.C. \xA7\xA7 243, 246(b), (c), 246A",
      section: "\xA7 243(a), (c); \xA7 246(b), (c); \xA7 246A",
      url: "https://www.law.cornell.edu/uscode/text/26/243",
      excerpt: "50 percent of dividends received (65 percent from a 20-percent-owned corporation; 100 percent for qualifying dividends from an affiliated group member, \xA7 243(a)(3)). The 50%/65% deductions may not exceed the same percentage of taxable income computed without the DRD, \xA7 172 NOL, \xA7 199A, \xA7 250, and capital loss carrybacks (\xA7 246(b)(1)) \u2014 but the limitation 'shall not apply for any taxable year for which there is a net operating loss' (\xA7 246(b)(2)): if the full deduction drives the base below zero it is allowed in full. The 100% DRD is never limited. The 50%/65% percentages ARE reduced by the average-indebtedness percentage of debt-financed portfolio stock (\xA7 246A, the corpPortfolioDebtFinancedPercent input). \xA7 246(c): no deduction for stock held 45 days or less in the 91-day ex-dividend window (90/181 for certain preferred) \u2014 the holding period is ATTESTED by the dividends input. [Dividends must be included in corpGrossIncome; \xA7 1059 extraordinary-dividend basis adjustments are not modeled \u2014 disclosed.]"
    },
    ...FROM3,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: gt02(fact9("corpDividendsReceived")),
      then: {
        kind: "match",
        on: fact9("corpDrdOwnershipTier"),
        cases: [
          { when: "under-20", value: drdExpr("50") },
          { when: "20-to-80", value: drdExpr("65") },
          { when: "80-plus", value: pct("100", fact9("corpDividendsReceived")) }
        ]
      },
      else: zero6
    }
  },
  {
    id: "us.federal.corp.nol_deduction",
    version: 4,
    // v3 predated § 179 in the base and § 245A in the 80%-limit subtraction
    jurisdiction: J5,
    title: "Net operating loss deduction (80% of taxable income, \xA7 172)",
    citation: {
      source: "26 U.S.C. \xA7 172(a)(2)",
      section: "\xA7 172(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/172",
      excerpt: "\u2026the aggregate of the net operating loss carryovers\u2026 shall not exceed 80 percent of taxable income computed without regard to the deduction allowable under this section (post-TCJA, permanent; carryforwards indefinite, no carrybacks). [The charitable interaction refuses in the charitable rule; the carryforward's remaining balance is not tracked \u2014 disclosed.]"
    },
    ...FROM3,
    output: { type: "money" },
    formula: {
      // guard first: a return with no carryforward must not demand the
      // component facts of the 80%-limit base
      kind: "if",
      cond: gt02(fact9("corpNOLCarryforward")),
      then: {
        kind: "min",
        args: [
          fact9("corpNOLCarryforward"),
          pct("80", {
            kind: "max0",
            arg: {
              kind: "sub",
              left: base246,
              right: {
                kind: "add",
                args: [
                  ruleRef8("us.federal.corp.dividends_received_deduction"),
                  ruleRef8("us.federal.corp.section245a_drd")
                ]
              }
            }
          })
        ]
      },
      else: zero6
    }
  },
  {
    id: "us.federal.corp.taxable_income",
    version: 4,
    // v3 predated the fiscal-year guard, § 179, and the § 245A DRD
    jurisdiction: J5,
    title: "Corporate taxable income (Form 1120 line 30 \u2014 given, or computed)",
    citation: {
      source: "26 U.S.C. \xA7 63(a); \xA7\xA7 15, 443 (guarded); Form 1120 lines 28\u201330",
      section: "\xA7 63(a); Form 1120",
      url: "https://www.irs.gov/forms-pubs/about-form-1120",
      excerpt: "Taxable income = gross income minus deductions: here, gross income minus ordinary business deductions, depreciation/\xA7 179/research/interest, the (limited) charitable deduction, the special deductions (\xA7 243 DRD and the \xA7 245A participation exemption), and the NOL deduction. If corpTaxableIncome (line 30) is provided directly it is used as-is. FISCAL-YEAR and \xA7 443 short-period returns REFUSE: the OBBBA parameters (\xA7 250 rates, \xA7 59A 10.5%, \xA7 960(d) 90%, \xA7 448(c) $32M, the \xA7 170(b)(2) 1% floor, \xA7 179 amounts) apply to 'taxable years beginning after' their effective dates, while this corpus selects rules by calendar date \u2014 and \xA7 443(b) annualization ('placed on an annual basis by multiplying\u2026 by 12, dividing the result by the number of months') and \xA7 15 proration are not encoded."
    },
    ...FROM3,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: fact9("corpIsREITorRIC"),
      then: {
        kind: "unsupported",
        reason: "REITs (\xA7 857) and RICs (\xA7 852) are taxed on income NET of the dividends-paid deduction with 90% distribution requirements \u2014 ordinary Form 1120 logic would be silently wrong, so these returns refuse"
      },
      else: {
        kind: "if",
        cond: fact9("corpFiscalYearFiler"),
        then: {
          kind: "unsupported",
          reason: "fiscal-year and short-period returns are not modeled \u2014 the OBBBA parameters apply by the taxable year's beginning date and \xA7\xA7 15/443 annualization is not encoded; provide a calendar taxable year"
        },
        else: {
          kind: "if",
          cond: fact9("corpFilesConsolidatedReturn"),
          then: {
            kind: "unsupported",
            reason: "consolidated returns (\xA7\xA7 1501\u20131504: intercompany eliminations, SRLY, investment adjustments) are not modeled \u2014 evaluate each member separately or provide the consolidated line 30 with corpFilesConsolidatedReturn unset only if you accept single-entity treatment"
          },
          else: {
            kind: "if",
            cond: gt02(fact9("corpTaxableIncome")),
            then: fact9("corpTaxableIncome"),
            else: {
              kind: "max0",
              arg: {
                kind: "sub",
                left: base246,
                right: {
                  kind: "add",
                  args: [
                    ruleRef8("us.federal.corp.dividends_received_deduction"),
                    ruleRef8("us.federal.corp.section245a_drd"),
                    ruleRef8("us.federal.corp.nol_deduction")
                  ]
                }
              }
            }
          }
        }
      }
    }
  },
  {
    id: "us.federal.corp.capital_loss_carryover_generated",
    version: 1,
    jurisdiction: J5,
    title: "Corporate capital loss carryover generated (\xA7 1212(a))",
    citation: {
      source: "26 U.S.C. \xA7\xA7 1211(a), 1212(a)",
      section: "\xA7 1211(a); \xA7 1212(a)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/1211",
      excerpt: "\u2026losses from sales or exchanges of capital assets shall be allowed only to the extent of gains from such sales or exchanges (\xA7 1211(a)); the excess is a capital loss carryback to each of the 3 preceding years and a carryover to the 5 succeeding years, treated as short-term (\xA7 1212(a)). [The 3-year carryback and multi-year tracking are not modeled \u2014 this target reports the excess generated; prior-year carryovers belong in the corpCapitalLosses input.]"
    },
    ...FROM3,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: { kind: "sub", left: fact9("corpCapitalLosses"), right: fact9("corpCapitalGains") }
    }
  },
  {
    id: "us.federal.corp.estimated.required_annual_payment",
    version: 2,
    // v1 used the gross § 11 tax; § 6655(g)(1) includes § 59A and subtracts credits
    jurisdiction: J5,
    title: "Corporate required annual payment (\xA7 6655(d); tax per \xA7 6655(g)(1) \u2014 after credits, plus BEAT)",
    citation: {
      source: "26 U.S.C. \xA7 6655(d), (g)(1), (g)(2)",
      section: "\xA7 6655(d)(1)\u2013(2); \xA7 6655(g)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/6655",
      excerpt: "\u2026the lesser of 100 percent of the tax shown on the return for the taxable year, or 100 percent of the tax shown on the return for the preceding taxable year \u2014 except that the preceding-year prong 'shall not apply in the case of a large corporation' (taxable income of $1,000,000 or more in any of the 3 preceding taxable years, \xA7 6655(g)(2)). 'Tax' means 'the sum of\u2014 (i) the tax imposed by section 11\u2026, (ii) the tax imposed by section 55, (iii) the tax imposed by section 59A, plus (iv) the tax imposed by section 887, over\u2026 the credits against tax provided by part IV' (\xA7 6655(g)(1)) \u2014 computed here as the income tax after the FTC and general business credit, plus BEAT (\xA7 55 CAMT already refuses above $1B AFSI; \xA7 887 not modeled). [The large corporation's first-installment exception (\xA7 6655(d)(2)(B)) is not modeled \u2014 conservative; the \xA7 6655(g)(4) S-corporation modifications are not modeled \u2014 disclosed.]"
    },
    ...FROM3,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: fact9("corpIsLargeCorporation"),
      then: {
        kind: "add",
        args: [
          ruleRef8("us.federal.corp.income_tax_after_credits"),
          ruleRef8("us.federal.corp.beat")
        ]
      },
      else: {
        kind: "min",
        args: [
          {
            kind: "add",
            args: [
              ruleRef8("us.federal.corp.income_tax_after_credits"),
              ruleRef8("us.federal.corp.beat")
            ]
          },
          fact9("corpPriorYearTax")
        ]
      }
    }
  },
  {
    id: "us.federal.corp.estimated.quarterly_payment",
    version: 2,
    // v1's de-minimis test used the gross § 11 tax; § 6655(f) keys to the § 6655(g)(1) tax
    jurisdiction: J5,
    title: "Corporate required quarterly installment (25%; none if tax under $500)",
    citation: {
      source: "26 U.S.C. \xA7 6655(d)(1)(A), (f), (g)(1)",
      section: "\xA7 6655(d)(1)(A), (f)",
      url: "https://www.law.cornell.edu/uscode/text/26/6655",
      excerpt: "\u202625 percent of the required annual payment; no addition to tax applies if the tax shown on the return (the \xA7 6655(g)(1) tax \u2014 after credits, including \xA7 59A) is less than $500. [The \xA7 6655(e) annualized/adjusted-seasonal methods are modeled as their own targets.]"
    },
    ...FROM3,
    output: { type: "money" },
    parameters: { deMinimis: { value: "50000", type: "money" } },
    // $500
    formula: {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "lt",
        left: {
          kind: "add",
          args: [
            ruleRef8("us.federal.corp.income_tax_after_credits"),
            ruleRef8("us.federal.corp.beat")
          ]
        },
        right: { kind: "param", name: "deMinimis" }
      },
      then: zero6,
      else: pct("25", ruleRef8("us.federal.corp.estimated.required_annual_payment"))
    }
  },
  {
    id: "us.federal.corp.s_corp_entity_taxes",
    version: 1,
    jurisdiction: J5,
    title: "S corporation entity-level taxes: \xA7 1375 excess net passive income + \xA7 1374 built-in gains",
    citation: {
      source: "26 U.S.C. \xA7\xA7 1374, 1375",
      section: "\xA7 1374(a)\u2013(b); \xA7 1375(a)\u2013(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/1375",
      excerpt: "\xA7 1375: if an S corporation has accumulated earnings and profits [from C years] at the close of the year and passive investment income exceeds 25 percent of gross receipts, a tax at the highest \xA7 11(b) rate (21%) is imposed on excess net passive income \u2014 net passive income \xD7 (PII \u2212 25% of gross receipts) / PII, not exceeding taxable income computed as if a C corporation. \xA7 1374: 21% of net recognized built-in gain during the 5-year recognition period after a C\u2192S conversion (the period condition is attested by the built-in-gain fact), likewise capped at as-if-C taxable income. [The \xA7 1366(f)(2)\u2013(3) reductions of shareholder pass-through amounts for these taxes are not modeled \u2014 disclosed.]"
    },
    ...FROM3,
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        // § 1374 built-in gains
        {
          kind: "if",
          cond: gt02(fact9("sCorpRecognizedBuiltInGain")),
          then: pct("21", {
            kind: "min",
            args: [fact9("sCorpRecognizedBuiltInGain"), fact9("sCorpTaxableIncomeAsC")]
          }),
          else: zero6
        },
        // § 1375 excess net passive income
        {
          kind: "if",
          cond: {
            kind: "and",
            args: [
              fact9("sCorpHasAccumulatedEandP"),
              gt02(fact9("sCorpPassiveInvestmentIncome")),
              {
                kind: "cmp",
                op: "gt",
                left: fact9("sCorpPassiveInvestmentIncome"),
                right: pct("25", fact9("sCorpGrossReceipts"))
              }
            ]
          },
          then: pct("21", {
            kind: "min",
            args: [
              {
                kind: "mulDiv",
                a: fact9("sCorpNetPassiveIncome"),
                b: {
                  kind: "sub",
                  left: fact9("sCorpPassiveInvestmentIncome"),
                  right: pct("25", fact9("sCorpGrossReceipts"))
                },
                c: fact9("sCorpPassiveInvestmentIncome"),
                round: "half-up"
              },
              fact9("sCorpTaxableIncomeAsC")
            ]
          }),
          else: zero6
        }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/corporate-annualized.js
var J6 = "us.federal";
var fact10 = (factId) => ({ kind: "fact", factId });
var money10 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef9 = (ruleId) => ({ kind: "rule", ruleId });
var QUARTERS2 = [
  { n: 1, tiFact: "corpTIThrough3Months", factor: { num: "4", den: "1" }, pct: { num: "25", den: "100" }, periodLabel: "first 3 months \xD74, 25%" },
  { n: 2, tiFact: "corpTIThrough3Months", factor: { num: "4", den: "1" }, pct: { num: "50", den: "100" }, periodLabel: "first 3 months \xD74, 50%" },
  { n: 3, tiFact: "corpTIThrough6Months", factor: { num: "2", den: "1" }, pct: { num: "75", den: "100" }, periodLabel: "first 6 months \xD72, 75%" },
  { n: 4, tiFact: "corpTIThrough9Months", factor: { num: "4", den: "3" }, pct: { num: "100", den: "100" }, periodLabel: "first 9 months \xD74/3, 100%" }
];
var corporateAnnualizedRules = QUARTERS2.map((q) => {
  const annualizedTax = {
    kind: "mulRate",
    base: {
      kind: "mulRate",
      base: fact10(q.tiFact),
      rate: q.factor,
      round: "half-up"
    },
    rate: { num: "21", den: "100" },
    round: "half-up"
  };
  const line = {
    kind: "mulRate",
    base: annualizedTax,
    rate: q.pct,
    round: "half-up"
  };
  const regular25 = {
    kind: "mulRate",
    base: ruleRef9("us.federal.corp.estimated.required_annual_payment"),
    rate: { num: "25", den: "100" },
    round: "half-up"
  };
  const priorSum = q.n === 1 ? money10("0") : {
    kind: "add",
    args: Array.from({ length: q.n - 1 }, (_, j) => ruleRef9(`us.federal.corp.annualized_installment_q${j + 1}`))
  };
  return {
    id: `us.federal.corp.annualized_installment_q${q.n}`,
    version: 1,
    jurisdiction: J6,
    title: `Corporate annualized required installment #${q.n} (\xA7 6655(e): ${q.periodLabel})`,
    citation: {
      source: "26 U.S.C. \xA7 6655(e); Form 1120-W (historical) method",
      section: "\xA7 6655(e)(1), (2)(A)",
      url: "https://www.law.cornell.edu/uscode/text/26/6655",
      excerpt: `Installment #${q.n}: annualize taxable income for the ${q.periodLabel.split(",")[0]} period, take 21 percent, apply the ${q.pct.num}% applicable percentage, subtract earlier installments, and cap at installment# \xD7 25% of the required annual payment minus earlier installments (the \xA7 6655(e)(1)(B) recapture, telescoped). [The \xA7 250 deduction and credits are not annualized; the elective alternative monthly periods of \xA7 6655(e)(2)(C) are not modeled \u2014 disclosed. Exact cents.]`
    },
    effectiveFrom: "2025-01-01",
    // statutory method: open-ended
    output: { type: "money" },
    formula: {
      kind: "min",
      args: [
        { kind: "max0", arg: { kind: "sub", left: line, right: priorSum } },
        {
          kind: "sub",
          left: {
            kind: "mulInt",
            base: regular25,
            count: { kind: "int", value: String(q.n) }
          },
          right: priorSum
        }
      ]
    }
  };
});

// ../corpus-us-federal/dist/rules/corporate-penalty-taxes.js
var J7 = "us.federal";
var FROM4 = { effectiveFrom: "2025-01-01" };
var fact11 = (factId) => ({ kind: "fact", factId });
var money11 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef10 = (ruleId) => ({ kind: "rule", ruleId });
var pct20 = (base) => ({
  kind: "mulRate",
  base,
  rate: { num: "20", den: "100" },
  round: "half-up"
});
var corporatePenaltyTaxRules = [
  {
    id: "us.federal.corp.accumulated_earnings_tax",
    version: 1,
    jurisdiction: J7,
    title: "Accumulated earnings tax \u2014 20% of accumulated taxable income (\xA7 531)",
    citation: {
      source: "26 U.S.C. \xA7\xA7 531, 535",
      section: "\xA7 531; \xA7 535(a)\u2013(c)",
      url: "https://www.law.cornell.edu/uscode/text/26/531",
      excerpt: "\u2026a tax equal to 20 percent of the accumulated taxable income: taxable income adjusted under \xA7 535(b) \u2014 federal income taxes accrued are subtracted, and the special deductions of part VIII [the DRD] and the \xA7 172 NOL deduction are not allowed (added back) \u2014 minus the dividends paid deduction and the accumulated earnings credit. The credit is not 'less than the amount by which $250,000 exceeds the accumulated earnings and profits\u2026 at the close of the preceding taxable year' ($150,000 for health, law, engineering, architecture, accounting, actuarial-science, performing-arts, or consulting service corporations), and not less than amounts retained for the reasonable needs of the business (\xA7 537 \u2014 attested by the retention fact). [Not modeled, disclosed: the \xA7 535(b)(2) unlimited-charitable and (b)(5)\u2013(6) capital gain/loss adjustments; \xA7 532(b)(1) \u2014 this tax does NOT apply to a personal holding company.]"
    },
    ...FROM4,
    output: { type: "money" },
    parameters: {
      minimumCredit: { value: "25000000", type: "money" },
      // $250,000
      minimumCreditService: { value: "15000000", type: "money" }
      // $150,000
    },
    formula: pct20({
      kind: "max0",
      arg: {
        kind: "sub",
        left: {
          // § 535(b): TI + DRD addback + NOL addback − federal income tax
          kind: "sub",
          left: {
            kind: "add",
            args: [
              ruleRef10("us.federal.corp.taxable_income"),
              ruleRef10("us.federal.corp.dividends_received_deduction"),
              ruleRef10("us.federal.corp.nol_deduction")
            ]
          },
          right: ruleRef10("us.federal.corp.income_tax")
        },
        right: {
          kind: "add",
          args: [
            fact11("corpDividendsPaid"),
            {
              // accumulated earnings credit: greater of reasonable-needs
              // retention or the $250k/$150k minimum less starting E&P
              kind: "max",
              args: [
                fact11("corpReasonableNeedsRetention"),
                {
                  kind: "max0",
                  arg: {
                    kind: "sub",
                    left: {
                      kind: "if",
                      cond: fact11("corpIsPersonalServiceCorp"),
                      then: { kind: "param", name: "minimumCreditService" },
                      else: { kind: "param", name: "minimumCredit" }
                    },
                    right: fact11("corpAccumulatedEandPStart")
                  }
                }
              ]
            }
          ]
        }
      }
    })
  },
  {
    id: "us.federal.corp.is_personal_holding_company",
    version: 1,
    jurisdiction: J7,
    title: "Personal holding company determination (\xA7 542 income + ownership tests)",
    citation: {
      source: "26 U.S.C. \xA7 542(a)",
      section: "\xA7 542(a)(1)\u2013(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/542",
      excerpt: "\u2026'personal holding company' means any corporation if (1) at least 60 percent of its adjusted ordinary gross income\u2026 is personal holding company income [dividends, interest, royalties, annuities, certain rents \u2014 \xA7 543], and (2) at any time during the last half of the taxable year more than 50 percent in value of its outstanding stock is owned, directly or indirectly, by or for not more than 5 individuals. [The \xA7 542(c) excluded classes (banks, insurance\u2026) and \xA7 544 constructive-ownership attribution are attested by the input facts \u2014 disclosed.]"
    },
    ...FROM4,
    output: { type: "bool" },
    formula: {
      kind: "and",
      args: [
        {
          kind: "cmp",
          op: "ge",
          left: fact11("corpPHCIncome"),
          right: {
            kind: "mulRate",
            base: fact11("corpAdjustedOrdinaryGrossIncome"),
            rate: { num: "60", den: "100" },
            round: "half-up"
          }
        },
        fact11("corpOwnedByFiveOrFewer")
      ]
    }
  },
  {
    id: "us.federal.corp.phc_tax",
    version: 1,
    jurisdiction: J7,
    title: "Personal holding company tax \u2014 20% of undistributed PHC income (\xA7 541)",
    citation: {
      source: "26 U.S.C. \xA7\xA7 541, 545",
      section: "\xA7 541; \xA7 545",
      url: "https://www.law.cornell.edu/uscode/text/26/541",
      excerpt: "\u2026a personal holding company tax equal to 20 percent of the undistributed personal holding company income \u2014 taxable income adjusted under \xA7 545 (federal taxes and the dividends-paid deduction subtracted; provide the adjusted amount as the input fact). Zero when the \xA7 542 tests fail. [In addition to the \xA7 11 tax; the AET never stacks with it (\xA7 532(b)(1)).]"
    },
    ...FROM4,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: ruleRef10("us.federal.corp.is_personal_holding_company"),
      then: pct20(fact11("corpUndistributedPHCIncome")),
      else: money11("0")
    }
  }
];

// ../corpus-us-federal/dist/rules/qsbs.js
var fact12 = (factId) => ({ kind: "fact", factId });
var money12 = (cents2) => ({ kind: "money", cents: cents2 });
var param5 = (name) => ({ kind: "param", name });
var years = fact12("qsbsHoldingPeriodYears");
var atLeast = (n) => ({
  kind: "cmp",
  op: "ge",
  left: years,
  right: { kind: "int", value: n }
});
var cappedGain = (capParam) => ({
  kind: "min",
  args: [fact12("qsbsGain"), param5(capParam)]
});
var share = (num, capParam) => ({
  kind: "mulRate",
  base: cappedGain(capParam),
  rate: { num, den: "100" },
  round: "half-up"
});
var tiers = {
  kind: "if",
  cond: fact12("qsbsAcquiredAfterJuly2025"),
  then: {
    kind: "if",
    cond: atLeast("5"),
    then: share("100", "capNew"),
    else: {
      kind: "if",
      cond: atLeast("4"),
      then: share("75", "capNew"),
      else: {
        kind: "if",
        cond: atLeast("3"),
        then: share("50", "capNew"),
        else: money12("0")
        // under 3 years: no exclusion yet
      }
    }
  },
  else: {
    kind: "if",
    cond: atLeast("5"),
    then: {
      kind: "if",
      cond: fact12("qsbsAcquiredAfterSept2010"),
      then: share("100", "capOld"),
      else: {
        kind: "unsupported",
        reason: "stock acquired on or before September 27, 2010 falls in the legacy 50%/75% regimes (\xA7 1(h)(4) 28%-rate gain + 7% AMT preference) \u2014 not modeled"
      }
    },
    else: money12("0")
    // prior-law stock under 5 years: nothing excludable
  }
};
var qsbsRules = [
  {
    id: "us.federal.qsbs_exclusion",
    version: 1,
    jurisdiction: "us.federal",
    title: "QSBS gain exclusion \u2014 OBBBA tiers (50/75/100% at 3/4/5 years) or prior-law 100% at 5 years",
    citation: {
      source: "26 U.S.C. \xA7 1202, as amended by Pub. L. 119-21 (OBBBA)",
      section: "\xA7 1202(a), (b)",
      url: "https://www.law.cornell.edu/uscode/text/26/1202",
      excerpt: "For qualified small business stock acquired after July 4, 2025: 50 percent of gain excluded after a 3-year holding period, 75 percent after 4 years, 100 percent after 5 years; per-issuer limit the greater of $15,000,000 (indexed after 2026) or 10 times basis [the 10\xD7-basis alternative is not modeled \u2014 the flat cap is conservative]; issuer aggregate gross assets must not exceed $75,000,000. For stock acquired after September 27, 2010 and on or before July 4, 2025: 100 percent excluded after 5 years, $10,000,000 cap. [Assumed and disclosed: original issuance for money/services, active qualified trade or business, and the asset test; earlier-vintage stock (50%/75% legacy with \xA7 1(h)(4) 28%-rate gain and the 7% AMT preference) REFUSES. The non-excluded remainder of the new 50%/75% tiers is likewise 28%-rate gain \u2014 do not enter it as longTermCapitalGains; this target answers the exclusion amount only.]"
    },
    effectiveFrom: "2025-01-01",
    // statutory: open-ended
    output: { type: "money" },
    parameters: {
      capNew: { value: "1500000000", type: "money" },
      // $15,000,000
      capOld: { value: "1000000000", type: "money" }
      // $10,000,000
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "eq", left: fact12("qsbsGain"), right: money12("0") },
      then: money12("0"),
      else: tiers
    }
  }
];

// ../corpus-us-federal/dist/rules/credits.js
var J8 = "us.federal";
var FROM5 = { effectiveFrom: "2025-01-01" };
var fact13 = (factId) => ({ kind: "fact", factId });
var money13 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef11 = (ruleId) => ({ kind: "rule", ruleId });
var param6 = (name) => ({ kind: "param", name });
var zero7 = money13("0");
var gt03 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero7 });
var isStatus3 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact13("filingStatus"),
  right: { kind: "enum", value: status }
});
var limitBase = {
  kind: "add",
  args: [
    ruleRef11("us.federal.income_tax_before_credits"),
    ruleRef11("us.federal.amt")
  ]
};
var earnedIncome = {
  kind: "add",
  args: [
    fact13("wages"),
    {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact13("selfEmploymentNetProfit"),
        right: ruleRef11("us.federal.se_tax_half_deduction")
      }
    }
  ]
};
var stepPoints = (over, unitCents) => ({
  kind: "mulInt",
  base: money13("1"),
  count: {
    kind: "stepUnits",
    value: { kind: "max0", arg: over },
    unitCents,
    mode: "ceil"
    // "or fraction thereof"
  }
});
var cdccExpenseBase = {
  kind: "min",
  args: [
    fact13("dependentCareExpenses"),
    {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "ge",
        left: fact13("careQualifyingIndividuals"),
        right: { kind: "int", value: "2" }
      },
      then: money13("600000"),
      // $6,000
      else: {
        kind: "if",
        cond: {
          kind: "cmp",
          op: "ge",
          left: fact13("careQualifyingIndividuals"),
          right: { kind: "int", value: "1" }
        },
        then: money13("300000"),
        // $3,000
        else: zero7
      }
    },
    earnedIncome,
    {
      kind: "if",
      cond: isStatus3("mfj"),
      then: fact13("secondaryEarnedIncome"),
      else: earnedIncome
    }
  ]
};
var pctOf = (base, pctCents) => ({
  kind: "mulDiv",
  a: base,
  b: pctCents,
  c: money13("100"),
  round: "half-up"
});
function cdccTentativeRule(version, from, to, pctExpr, yearLabel, mechanicsExcerpt) {
  return {
    id: "us.federal.cdcc.tentative",
    version,
    jurisdiction: J8,
    title: `Child and dependent care credit before the liability limit (${yearLabel})`,
    citation: {
      source: version > 1 ? "26 U.S.C. \xA7 21, as amended by Pub. L. 119-21 (OBBBA) \xA7 70405" : "26 U.S.C. \xA7 21 (pre-OBBBA text, per the \xA7 70405(b) effective date)",
      section: "\xA7 21(a)\u2013(e)",
      url: "https://www.law.cornell.edu/uscode/text/26/21",
      excerpt: mechanicsExcerpt
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "or",
        args: [
          isStatus3("mfs"),
          { kind: "not", arg: gt03(fact13("dependentCareExpenses")) }
        ]
      },
      then: zero7,
      else: pctOf(cdccExpenseBase, pctExpr)
    }
  };
}
var pct2025 = {
  kind: "max",
  args: [
    money13("20"),
    {
      kind: "sub",
      left: money13("35"),
      right: stepPoints({ kind: "sub", left: ruleRef11("us.federal.agi"), right: money13("1500000") }, "200000")
    }
  ]
};
var pct2026 = {
  kind: "max",
  args: [
    money13("20"),
    {
      kind: "sub",
      left: {
        kind: "max",
        args: [
          money13("35"),
          {
            kind: "sub",
            left: money13("50"),
            right: stepPoints({
              kind: "sub",
              left: ruleRef11("us.federal.agi"),
              right: money13("1500000")
            }, "200000")
          }
        ]
      },
      right: {
        kind: "if",
        cond: isStatus3("mfj"),
        then: stepPoints({
          kind: "sub",
          left: ruleRef11("us.federal.agi"),
          right: money13("15000000")
          // $150,000 joint
        }, "400000"),
        else: stepPoints({
          kind: "sub",
          left: ruleRef11("us.federal.agi"),
          right: money13("7500000")
          // $75,000
        }, "200000")
      }
    }
  ]
};
var saversTier = (mfjCents, hohCents, otherCents) => ({
  kind: "match",
  on: fact13("filingStatus"),
  cases: [
    { when: "mfj", value: money13(mfjCents) },
    { when: "hoh", value: money13(hohCents) }
  ],
  else: money13(otherCents)
});
function saversTentativeRule(version, from, to, tiers2, yearLabel, noticeLabel, tableExcerpt) {
  const eligibleContributions = {
    kind: "add",
    args: [
      {
        kind: "if",
        cond: {
          kind: "or",
          args: [fact13("isFullTimeStudent"), fact13("isClaimedAsDependent")]
        },
        then: zero7,
        else: { kind: "min", args: [fact13("saversContributions"), money13("200000")] }
      },
      {
        kind: "if",
        cond: {
          kind: "and",
          args: [isStatus3("mfj"), { kind: "not", arg: fact13("spouseIsFullTimeStudent") }]
        },
        then: {
          kind: "min",
          args: [fact13("saversContributionsSpouse"), money13("200000")]
        },
        else: zero7
      }
    ]
  };
  const agi2 = ruleRef11("us.federal.agi");
  return {
    id: "us.federal.savers.tentative",
    version,
    jurisdiction: J8,
    title: `Saver's credit before the liability limit (\xA7 25B, ${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 25B; ${noticeLabel}`,
      section: "\xA7 25B(a)\u2013(c)",
      url: "https://www.law.cornell.edu/uscode/text/26/25B",
      excerpt: `A credit equal to the applicable percentage of so much of the qualified retirement savings contributions of the eligible individual\u2026 as do not exceed $2,000 \u2014 per individual (spouse separately on a joint return). ${tableExcerpt} Head-of-household tiers are 75 percent and all other statuses 50 percent of the joint amounts (\xA7 25B(b)(2)) \u2014 a surviving-spouse return is not a joint return, so the "all other" tiers apply. Ineligible: full-time students and claimed dependents (\xA7 25B(c)(2)); attainment of age 18 (\xA7 25B(c)(1)) is assumed \u2014 disclosed. Contributions are net of \xA7 25B(d)(2) testing-period distributions \u2014 attested by the input. [For taxable years after 2026 the credit is replaced by the \xA7 6433 Saver's Match (SECURE 2.0 \xA7 103; OBBBA \xA7 70116).]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "le", left: agi2, right: tiers2.t50 },
      then: {
        kind: "mulRate",
        base: eligibleContributions,
        rate: { num: "50", den: "100" },
        round: "half-up"
      },
      else: {
        kind: "if",
        cond: { kind: "cmp", op: "le", left: agi2, right: tiers2.t20 },
        then: {
          kind: "mulRate",
          base: eligibleContributions,
          rate: { num: "20", den: "100" },
          round: "half-up"
        },
        else: {
          kind: "if",
          cond: { kind: "cmp", op: "le", left: agi2, right: tiers2.t10 },
          then: {
            kind: "mulRate",
            base: eligibleContributions,
            rate: { num: "10", den: "100" },
            round: "half-up"
          },
          else: zero7
        }
      }
    }
  };
}
function adoptionAllowedRule(version, from, to, maxCents, thresholdCents, yearLabel, amountsExcerpt) {
  const deemedExpenses = {
    kind: "if",
    cond: fact13("adoptionIsSpecialNeeds"),
    then: param6("maxCredit"),
    else: { kind: "min", args: [fact13("qualifiedAdoptionExpenses"), param6("maxCredit")] }
  };
  const phaseFraction = {
    kind: "min",
    args: [
      {
        kind: "max0",
        arg: {
          kind: "sub",
          left: ruleRef11("us.federal.agi"),
          right: param6("phaseoutStart")
        }
      },
      param6("phaseoutRange")
    ]
  };
  return {
    id: "us.federal.adoption.allowed",
    version,
    jurisdiction: J8,
    title: `Adoption credit after the MAGI phase-out (\xA7 23, ${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 23, as amended by Pub. L. 119-21 (OBBBA) \xA7 70402; ${yearLabel === "TY2025" ? "Rev. Proc. 2024-40 \xA7 2.04" : "Rev. Proc. 2025-32 \xA7 3.04"}`,
      section: "\xA7 23(a)\u2013(b), (f)",
      url: "https://www.law.cornell.edu/uscode/text/26/23",
      excerpt: `${amountsExcerpt} A special-needs adoption is treated as having paid the maximum regardless of actual expenses (\xA7 23(a)(3)). The credit is reduced by the ratio of the MAGI excess over the threshold to $40,000 (\xA7 23(b)(2)(A) \u2014 the $40,000 span is NOT indexed; MAGI is AGI without \xA7\xA7 911/931/933, \xA7 23(b)(2)(B) \u2014 MAGI \u2248 AGI here, disclosed). Married taxpayers only on a joint return (\xA7 23(f)(1), rules similar to \xA7 21(e)(2)) \u2014 MFS: $0; the living-apart exception is not modeled (conservative). The \xA7 23(a)(2) expense-timing rules (year after payment for pre-final years; foreign adoptions only when final) are attested by the input.`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: {
      maxCredit: { value: maxCents, type: "money" },
      phaseoutStart: { value: thresholdCents, type: "money" },
      phaseoutRange: { value: "4000000", type: "money" }
      // $40,000, unindexed
    },
    formula: {
      kind: "if",
      cond: {
        kind: "or",
        args: [
          isStatus3("mfs"),
          {
            kind: "not",
            arg: {
              kind: "or",
              args: [gt03(fact13("qualifiedAdoptionExpenses")), fact13("adoptionIsSpecialNeeds")]
            }
          }
        ]
      },
      then: zero7,
      else: {
        kind: "max0",
        arg: {
          kind: "sub",
          left: deemedExpenses,
          right: {
            kind: "mulDiv",
            a: deemedExpenses,
            b: phaseFraction,
            c: param6("phaseoutRange"),
            round: "half-up"
          }
        }
      }
    }
  };
}
function adoptionRefundableRule(version, from, to, capCents, capLabel, sourceLabel) {
  return {
    id: "us.federal.adoption.refundable",
    version,
    jurisdiction: J8,
    title: `Refundable portion of the adoption credit (\xA7 23(a)(4), ${capLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 23(a)(4) (added by Pub. L. 119-21 (OBBBA) \xA7 70402, effective TY2025); ${sourceLabel}`,
      section: "\xA7 23(a)(4)",
      url: "https://www.law.cornell.edu/uscode/text/26/23",
      excerpt: `So much of the credit allowed under paragraph (1) as does not exceed ${capLabel} shall be treated as a credit allowed under subpart C [refundable] and not as a credit allowed under this subpart. [Indexed after 2025 per \xA7 23(h)(3); only the NONREFUNDABLE remainder carries forward under \xA7 23(c) as conformed by OBBBA \xA7 70402(c).]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: { refundableCap: { value: capCents, type: "money" } },
    formula: {
      kind: "min",
      args: [ruleRef11("us.federal.adoption.allowed"), param6("refundableCap")]
    }
  };
}
var creditBlockRules = [
  cdccTentativeRule(1, "2025-01-01", "2026-01-01", pct2025, "TY2025: 35% \u2192 20%", "The applicable percentage is '35 percent reduced (but not below 20 percent) by 1 percentage point for each $2,000 (or fraction thereof) by which the taxpayer's adjusted gross income for the taxable year exceeds $15,000' (pre-OBBBA text, in force for taxable years beginning before 2026 per OBBBA \xA7 70405(b)). Expenses capped at $3,000 (one qualifying individual) / $6,000 (two or more), \xA7 21(c), net of the \xA7 129 exclusion (netted by the input); limited to earned income \u2014 on a joint return, to the lesser spouse's earned income (\xA7 21(d)(1)). Married only on a joint return (\xA7 21(e)(2)) \u2014 MFS: $0; the \xA7 21(e)(4) living-apart exception and \xA7 21(d)(2) deemed student-spouse income are not modeled (conservative). Qualifying-individual status (\xA7 21(b)(1): dependent under 13, or incapable of self-care) is attested by the count input."),
  cdccTentativeRule(2, "2026-01-01", "2027-01-01", pct2026, "TY2026: OBBBA 50% \u2192 35% \u2192 20%", "OBBBA \xA7 70405 (taxable years beginning after December 31, 2025): the applicable percentage 'means 50 percent\u2014 (A) reduced (but not below 35 percent) by 1 percentage point for each $2,000 or fraction thereof by which the taxpayer's adjusted gross income for the taxable year exceeds $15,000, and (B) further reduced (but not below 20 percent) by 1 percentage point for each $2,000 ($4,000 in the case of a joint return) or fraction thereof by which the taxpayer's adjusted gross income for the taxable year exceeds $75,000 ($150,000 in the case of a joint return).' Expense caps $3,000/$6,000 (\xA7 21(c), unindexed, unchanged by the OBBBA), limited to earned income \u2014 joint returns to the lesser spouse's earned income (\xA7 21(d)(1)). Married only on a joint return (\xA7 21(e)(2)) \u2014 MFS: $0; \xA7 21(e)(4) and \xA7 21(d)(2) not modeled (conservative). Qualifying-individual status attested by the count input."),
  {
    id: "us.federal.cdcc",
    version: 1,
    jurisdiction: J8,
    title: "Child and dependent care credit, limited to tax (\xA7 26(a))",
    citation: {
      source: "26 U.S.C. \xA7 21; \xA7 26(a); Form 2441",
      section: "\xA7 21(a); \xA7 26(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/21",
      excerpt: "The \xA7 21 credit is a nonrefundable personal credit limited to the \xA7 26(a) tax liability (Form 2441 Credit Limit Worksheet) \u2014 the first credit in this corpus's ordering, so limited to the full regular tax + AMT."
    },
    ...FROM5,
    output: { type: "money" },
    formula: {
      kind: "min",
      args: [ruleRef11("us.federal.cdcc.tentative"), { kind: "max0", arg: limitBase }]
    }
  },
  saversTentativeRule(1, "2025-01-01", "2026-01-01", {
    t50: saversTier("4750000", "3562500", "2375000"),
    t20: saversTier("5100000", "3825000", "2550000"),
    t10: saversTier("7900000", "5925000", "3950000")
  }, "TY2025", "Notice 2024-80", "For 2025 (Notice 2024-80): joint 50% to $47,500, 20% to $51,000, 10% to $79,000; head of household $35,625/$38,250/$59,250; all other statuses $23,750/$25,500/$39,500 \u2014 zero above."),
  saversTentativeRule(2, "2026-01-01", "2027-01-01", {
    t50: saversTier("4850000", "3637500", "2425000"),
    t20: saversTier("5250000", "3937500", "2625000"),
    t10: saversTier("8050000", "6037500", "4025000")
  }, "TY2026", "Notice 2025-67", "For 2026 (Notice 2025-67): joint 50% to $48,500, 20% to $52,500, 10% to $80,500; head of household $36,375/$39,375/$60,375; all other statuses $24,250/$26,250/$40,250 \u2014 zero above."),
  {
    id: "us.federal.savers_credit",
    version: 1,
    jurisdiction: J8,
    title: "Saver's credit, limited to remaining tax (\xA7 26(a) after CDCC + education)",
    citation: {
      source: "26 U.S.C. \xA7 25B; \xA7 26(a); Form 8880 Credit Limit Worksheet",
      section: "\xA7 25B(a); \xA7 26(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/25B",
      excerpt: "Nonrefundable, limited to the tax remaining after the credits the Form 8880 worksheet subtracts first \u2014 here the \xA7 21 dependent-care and \xA7 25A education credits."
    },
    ...FROM5,
    output: { type: "money" },
    formula: {
      kind: "min",
      args: [
        ruleRef11("us.federal.savers.tentative"),
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: limitBase,
            right: {
              kind: "add",
              args: [
                ruleRef11("us.federal.cdcc"),
                ruleRef11("us.federal.education.nonrefundable")
              ]
            }
          }
        }
      ]
    }
  },
  adoptionAllowedRule(1, "2025-01-01", "2026-01-01", "1728000", "25919000", "TY2025", "For taxable years beginning in 2025, the credit allowed for an adoption of a child with special needs is $17,280; the maximum credit allowed for other adoptions is the amount of qualified adoption expenses up to $17,280; phase-out begins at modified adjusted gross income in excess of $259,190 and is complete at $299,190 (Rev. Proc. 2024-40 \xA7 2.04, verbatim)."),
  adoptionAllowedRule(2, "2026-01-01", "2027-01-01", "1767000", "26508000", "TY2026", "For taxable years beginning in 2026, the credit allowed for an adoption of a child with special needs is $17,670; the maximum credit allowed for other adoptions is the amount of qualified adoption expenses up to $17,670; phase-out begins at modified adjusted gross income in excess of $265,080 and is complete at $305,080 (Rev. Proc. 2025-32 \xA7 3.04, verbatim)."),
  adoptionRefundableRule(1, "2025-01-01", "2026-01-01", "500000", "$5,000", "statutory, unindexed for 2025"),
  adoptionRefundableRule(2, "2026-01-01", "2027-01-01", "512000", "$5,120", "Rev. Proc. 2025-32 \xA7 3.04(3)"),
  {
    id: "us.federal.adoption.nonrefundable",
    version: 1,
    jurisdiction: J8,
    title: "Nonrefundable adoption credit, limited to remaining tax (\xA7 26(a) after CDCC + education + saver's)",
    citation: {
      source: "26 U.S.C. \xA7 23(a), (b)(4); \xA7 26(a); Form 8839",
      section: "\xA7 23(b)(4); \xA7 26(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/23",
      excerpt: "The credit allowed net of its \xA7 23(a)(4) refundable portion, limited to the tax remaining after the credits the Form 8839 worksheet subtracts first \u2014 here the \xA7\xA7 21, 25A, and 25B credits."
    },
    ...FROM5,
    output: { type: "money" },
    formula: {
      kind: "min",
      args: [
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: ruleRef11("us.federal.adoption.allowed"),
            right: ruleRef11("us.federal.adoption.refundable")
          }
        },
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: limitBase,
            right: {
              kind: "add",
              args: [
                ruleRef11("us.federal.cdcc"),
                ruleRef11("us.federal.education.nonrefundable"),
                ruleRef11("us.federal.savers_credit")
              ]
            }
          }
        }
      ]
    }
  },
  {
    id: "us.federal.adoption.carryforward",
    version: 1,
    jurisdiction: J8,
    title: "Adoption credit carryforward generated (\xA7 23(c), nonrefundable portion only)",
    citation: {
      source: "26 U.S.C. \xA7 23(c), as conformed by Pub. L. 119-21 (OBBBA) \xA7 70402(c)",
      section: "\xA7 23(c)(1)\u2013(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/23",
      excerpt: "The nonrefundable portion in excess of the \xA7 26(a) limitation carries to the succeeding taxable year \u2014 'No credit may be carried forward under this subsection to any taxable year following the fifth taxable year after the taxable year in which the credit arose.' The refundable \xA7 23(a)(4) portion never carries. [This target reports the carryforward generated this year; prior-year carryforwards are not tracked.]"
    },
    ...FROM5,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: ruleRef11("us.federal.adoption.allowed"),
            right: ruleRef11("us.federal.adoption.refundable")
          }
        },
        right: ruleRef11("us.federal.adoption.nonrefundable")
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/ctc.js
var FROM6 = { effectiveFrom: "2025-01-01" };
var TY25_26 = { effectiveFrom: "2025-01-01", effectiveTo: "2027-01-01" };
var J9 = "us.federal";
var fact14 = (factId) => ({ kind: "fact", factId });
var money14 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef12 = (ruleId) => ({ kind: "rule", ruleId });
var ctcRules = [
  {
    id: "us.federal.ctc.tentative",
    version: 3,
    // v2 predated the § 24(h)(4) $500 credit for other dependents
    jurisdiction: J9,
    title: "Child tax credit + credit for other dependents, before phase-out (as amended by OBBBA)",
    citation: {
      source: "26 U.S.C. \xA7 24(a), (h)(4), as amended by Pub. L. 119-21 (OBBBA) \xA7 70104; Rev. Proc. 2025-32 \xA7 4.05",
      section: "\xA7 24(a), (h)(2), (h)(4)",
      url: "https://www.law.cornell.edu/uscode/text/26/24",
      excerpt: "The One Big Beautiful Bill Act (enacted July 4, 2025) raised the child tax credit to $2,200 per qualifying child for taxable years beginning in 2025; Rev. Proc. 2025-32 \xA7 4.05 confirms $2,200 for 2026. \xA7 24(h)(4): 'The credit determined under subsection (a) (after the application of paragraph (2)) shall be increased by $500 for each dependent of the taxpayer (as defined in section 152) other than a qualifying child' \u2014 unindexed, never refundable (the \xA7 24(d)(1)(A) ACTC cross-reference excludes it via (h)(5)'s per-child cap), subject to the same \xA7 24(b) phase-out as part of the combined credit (Schedule 8812 line 8). [\xA7 24(h)(4)(B) noncitizen exception and the dependent's TIN requirement (\xA7 24(e)) attested by the count.]"
    },
    ...TY25_26,
    output: { type: "money" },
    parameters: {
      perChild: { value: "220000", type: "money" },
      // $2,200
      perOtherDependent: { value: "50000", type: "money" }
      // $500, unindexed
    },
    formula: {
      kind: "add",
      args: [
        {
          kind: "mulInt",
          base: { kind: "param", name: "perChild" },
          count: fact14("qualifyingChildren")
        },
        {
          kind: "mulInt",
          base: { kind: "param", name: "perOtherDependent" },
          count: fact14("otherDependents")
        }
      ]
    }
  },
  {
    id: "us.federal.ctc.phaseout_threshold",
    version: 1,
    jurisdiction: J9,
    title: "CTC phase-out threshold by filing status",
    citation: {
      source: "26 U.S.C. \xA7 24(h)(3) (made permanent by Pub. L. 119-21 \xA7 70104)",
      section: "\xA7 24(h)(3)",
      url: "https://www.law.cornell.edu/uscode/text/26/24",
      excerpt: "\u2026subsection (b)(2) shall be applied by substituting '$400,000' for '$110,000' [joint returns] and '$200,000' for the [other] amounts\u2026"
    },
    ...FROM6,
    output: { type: "money" },
    formula: {
      kind: "match",
      on: fact14("filingStatus"),
      cases: [
        // $400,000 applies only "in the case of a joint return" — every
        // other status (including QSS and MFS) gets $200,000.
        { when: "mfj", value: money14("40000000") },
        // $400,000
        { when: "single", value: money14("20000000") },
        // $200,000
        { when: "hoh", value: money14("20000000") },
        // $200,000
        { when: "mfs", value: money14("20000000") },
        // $200,000
        { when: "qss", value: money14("20000000") }
        // $200,000
      ]
    }
  },
  {
    id: "us.federal.ctc.reduction",
    version: 1,
    jurisdiction: J9,
    title: "CTC phase-out reduction",
    citation: {
      source: "26 U.S.C. \xA7 24(b)(1), (2)",
      section: "\xA7 24(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/24",
      excerpt: "\u2026reduced (but not below zero) by $50 for each $1,000 (or fraction thereof) by which the taxpayer's modified adjusted gross income exceeds the threshold amount."
    },
    ...FROM6,
    output: { type: "money" },
    parameters: {
      perStep: { value: "5000", type: "money" }
      // $50
    },
    formula: {
      kind: "mulInt",
      base: { kind: "param", name: "perStep" },
      count: {
        kind: "stepUnits",
        value: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: ruleRef12("us.federal.agi"),
            right: ruleRef12("us.federal.ctc.phaseout_threshold")
          }
        },
        unitCents: "100000",
        // $1,000
        mode: "ceil"
        // "or fraction thereof"
      }
    }
  },
  {
    id: "us.federal.ctc.after_phaseout",
    version: 1,
    jurisdiction: J9,
    title: "Child tax credit after phase-out (before the liability limit)",
    citation: {
      source: "26 U.S.C. \xA7 24(a), (b)",
      section: "\xA7 24(a)\u2013(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/24",
      excerpt: "\u2026credit\u2026 reduced (but not below zero) by $50 for each $1,000 (or fraction thereof) by which\u2026 modified adjusted gross income exceeds the threshold amount."
    },
    ...FROM6,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: ruleRef12("us.federal.ctc.tentative"),
        right: ruleRef12("us.federal.ctc.reduction")
      }
    }
  },
  {
    id: "us.federal.ctc",
    version: 6,
    // v5 predated the Schedule R credit ahead of it in the ordering
    jurisdiction: J9,
    title: "Child tax credit + ODC (nonrefundable portion, after other Schedule 3 credits)",
    citation: {
      source: "26 U.S.C. \xA7 24; \xA7 26(a); Schedule 8812 Credit Limit Worksheet",
      section: "\xA7 24(a)\u2013(b); \xA7 26(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/24",
      excerpt: "credit\u2026 limited to tax liability under \xA7 26(a) reduced by the other nonrefundable credits (the Schedule 8812 Credit Limit Worksheet subtracts Schedule 3 credits \u2014 here the \xA7 21 dependent-care, \xA7 25A education, \xA7 25B saver's, \xA7 23 adoption, and \xA7 22 Schedule R credits \u2014 before limiting the CTC); the refundable additional child tax credit (\xA7 24(d)) is computed separately."
    },
    ...FROM6,
    output: { type: "money" },
    formula: {
      kind: "min",
      args: [
        ruleRef12("us.federal.ctc.after_phaseout"),
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: {
              kind: "add",
              args: [
                ruleRef12("us.federal.income_tax_before_credits"),
                ruleRef12("us.federal.amt")
              ]
            },
            right: {
              kind: "add",
              args: [
                ruleRef12("us.federal.cdcc"),
                ruleRef12("us.federal.education.nonrefundable"),
                ruleRef12("us.federal.savers_credit"),
                ruleRef12("us.federal.adoption.nonrefundable"),
                ruleRef12("us.federal.schedule_r_credit")
              ]
            }
          }
        }
      ]
    }
  },
  {
    id: "us.federal.actc",
    version: 3,
    // v2 predated the Schedule C net-loss reduction of earned income
    jurisdiction: J9,
    title: "Additional child tax credit \u2014 refundable (simplified: 3+-child SS-tax alternative not modeled)",
    citation: {
      source: "26 U.S.C. \xA7 24(d), (h)(5); Rev. Proc. 2024-40 \xA7 2.05 / Rev. Proc. 2025-32 \xA7 4.05",
      section: "\xA7 24(d)(1); \xA7 24(h)(5)",
      url: "https://www.law.cornell.edu/uscode/text/26/24",
      excerpt: "The credit is refundable up to $1,700 per qualifying child (2025 and 2026, as adjusted), limited to 15 percent of so much of earned income as exceeds $2,500, and to the portion of the credit not used against tax. [The \xA7 24(d)(1)(B)(ii) alternative for taxpayers with 3+ children (social security taxes minus EIC) is NOT modeled \u2014 it can only increase the refund at very low earned income; disclosed. Earned income approximated as wages.]"
    },
    ...FROM6,
    output: { type: "money" },
    parameters: {
      refundableCapPerChild: { value: "170000", type: "money" },
      // $1,700
      earnedIncomeFloor: { value: "250000", type: "money" }
      // $2,500
    },
    formula: {
      kind: "min",
      args: [
        // the part of the credit the liability limit left unused
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: ruleRef12("us.federal.ctc.after_phaseout"),
            right: ruleRef12("us.federal.ctc")
          }
        },
        // refundable cap: $1,700 per qualifying child
        {
          kind: "mulInt",
          base: { kind: "param", name: "refundableCapPerChild" },
          count: fact14("qualifyingChildren")
        },
        // 15% of earned income (wages + SE earnings net of ½ SE tax) above $2,500
        {
          kind: "mulRate",
          base: {
            kind: "max0",
            arg: {
              kind: "sub",
              left: {
                kind: "add",
                args: [
                  fact14("wages"),
                  {
                    kind: "max0",
                    arg: {
                      kind: "sub",
                      left: fact14("selfEmploymentNetProfit"),
                      right: ruleRef12("us.federal.se_tax_half_deduction")
                    }
                  }
                ]
              },
              right: {
                kind: "add",
                args: [
                  { kind: "param", name: "earnedIncomeFloor" },
                  // Schedule C net loss reduces § 24(d) earned income too
                  fact14("scheduleCNetLoss")
                ]
              }
            }
          },
          rate: { num: "15", den: "100" },
          round: "half-up"
        }
      ]
    }
  },
  {
    id: "us.federal.income_tax_after_credits",
    version: 5,
    // v4 predated the Schedule R credit
    jurisdiction: J9,
    title: "Income tax after nonrefundable credits (CDCC + education + saver's + adoption + Schedule R + CTC/ODC)",
    citation: {
      source: "26 U.S.C. \xA7 26(a)",
      section: "\xA7 26(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/26",
      excerpt: "The aggregate amount of credits allowed by this subpart\u2026 shall not exceed the taxpayer's regular tax liability for the taxable year\u2026 [modeled nonrefundable credits, in the forms' worksheet order: \xA7 21 dependent care, \xA7 25A education, \xA7 25B saver's, \xA7 23 adoption (nonrefundable portion), \xA7 24 child tax credit + credit for other dependents]."
    },
    ...FROM6,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: {
          kind: "add",
          args: [
            ruleRef12("us.federal.income_tax_before_credits"),
            ruleRef12("us.federal.amt")
            // Schedule 2 line 1 joins line 16 before credits
          ]
        },
        right: {
          kind: "add",
          args: [
            ruleRef12("us.federal.cdcc"),
            ruleRef12("us.federal.education.nonrefundable"),
            ruleRef12("us.federal.savers_credit"),
            ruleRef12("us.federal.adoption.nonrefundable"),
            ruleRef12("us.federal.schedule_r_credit"),
            ruleRef12("us.federal.ctc")
          ]
        }
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/dependents.js
var fact15 = (factId) => ({ kind: "fact", factId });
var intLit = (n) => ({ kind: "int", value: n });
var FROM7 = { effectiveFrom: "2025-01-01" };
var CITE_URL = "https://www.law.cornell.edu/uscode/text/26/152";
var ageTest = {
  kind: "or",
  args: [
    fact15("depPermanentlyDisabled"),
    {
      kind: "and",
      args: [
        fact15("depYoungerThanTaxpayer"),
        {
          kind: "or",
          args: [
            { kind: "cmp", op: "lt", left: fact15("depAge"), right: intLit("19") },
            {
              kind: "and",
              args: [
                fact15("depIsFullTimeStudent"),
                { kind: "cmp", op: "lt", left: fact15("depAge"), right: intLit("24") }
              ]
            }
          ]
        }
      ]
    }
  ]
};
function qualifyingRelativeRule(version, effectiveFrom, effectiveTo, limitCents, yearLabel, revProc) {
  return {
    id: "us.federal.dependent.qualifying_relative",
    version,
    jurisdiction: "us.federal",
    title: `Candidate is a qualifying relative (\xA7 152(d), TY${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 152(d); ${revProc}`,
      section: "\xA7 152(d)(1)",
      url: CITE_URL,
      excerpt: `\u2026an individual who bears a \xA7 152(d)(2) relationship to the taxpayer (or is a full-year member of the household), whose gross income for the calendar year is less than the exemption amount (TY${yearLabel}: as adjusted), with respect to whom the taxpayer provides over one-half of the individual's support, and who is not a qualifying child of the taxpayer or any other taxpayer. [Multiple-support agreements (\xA7 152(d)(3)) not modeled.]`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "bool" },
    parameters: {
      grossIncomeLimit: { value: limitCents, type: "money" }
    },
    formula: {
      kind: "and",
      args: [
        { kind: "not", arg: fact15("depIsQualifyingChildOfAnother") },
        fact15("depRelationshipRelative"),
        {
          kind: "cmp",
          op: "lt",
          left: fact15("depGrossIncome"),
          right: { kind: "param", name: "grossIncomeLimit" }
        },
        fact15("taxpayerProvidedOverHalfSupport")
      ]
    }
  };
}
var dependentRules = [
  {
    id: "us.federal.dependent.qualifying_child",
    version: 1,
    jurisdiction: "us.federal",
    title: "Candidate is a qualifying child (\xA7 152(c))",
    citation: {
      source: "26 U.S.C. \xA7 152(c)",
      section: "\xA7 152(c)(1)\u2013(3)",
      url: CITE_URL,
      excerpt: "\u2026a child of the taxpayer or a descendant of such a child, or a brother, sister, stepbrother, or stepsister\u2026 or a descendant; who has the same principal place of abode as the taxpayer for more than one-half of the taxable year; who is younger than the taxpayer and has not attained age 19 (24 if a full-time student), or is permanently and totally disabled; who has not provided over one-half of such individual's own support; and who has not filed a joint return. [Tie-breakers (\xA7 152(c)(4)) and divorced-parent rules (\xA7 152(e)) not modeled.]"
    },
    ...FROM7,
    output: { type: "bool" },
    formula: {
      kind: "and",
      args: [
        fact15("depRelationshipChild"),
        ageTest,
        fact15("depLivedWithTaxpayerOverHalfYear"),
        { kind: "not", arg: fact15("depProvidedOwnSupportOverHalf") },
        { kind: "not", arg: fact15("depFilesJointReturn") }
      ]
    }
  },
  qualifyingRelativeRule(1, "2025-01-01", "2026-01-01", "520000", "2025", "Rev. Proc. 2024-40 \xA7 2.24 ($5,200)"),
  qualifyingRelativeRule(2, "2026-01-01", "2027-01-01", "530000", "2026", "Rev. Proc. 2025-32 \xA7 4.23 ($5,300)"),
  ...[
    { version: 1, from: "2025-01-01", to: "2026-01-01", limit: "520000", year: "2025", revProc: "Rev. Proc. 2024-40 \xA7 2.24 ($5,200)" },
    { version: 2, from: "2026-01-01", to: "2027-01-01", limit: "530000", year: "2026", revProc: "Rev. Proc. 2025-32 \xA7 4.23 ($5,300)" }
  ].map(({ version, from, to, limit, year, revProc }) => ({
    id: "us.federal.dependent.qualifying_relative.multiple_support",
    version,
    jurisdiction: "us.federal",
    title: `Multiple-support agreement: over-10% contributor treated as providing over half (\xA7 152(d)(3), TY${year})`,
    citation: {
      source: `26 U.S.C. \xA7 152(d)(3); Form 2120; ${revProc}`,
      section: "\xA7 152(d)(3)",
      url: CITE_URL,
      excerpt: "\u2026the taxpayer shall be treated as having contributed over one-half of the support of an individual if no one person contributed over one-half, over one-half was received from two or more persons each of whom (but for the support test) could claim the individual, the taxpayer contributed over 10 percent, and every other over-10% contributor files a written declaration waiving the claim (Form 2120). [The group conditions and waivers are attested by the hasMultipleSupportAgreement fact \u2014 disclosed.]"
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "bool" },
    applicability: fact15("hasMultipleSupportAgreement"),
    parameters: {
      grossIncomeLimit: { value: limit, type: "money" }
    },
    formula: {
      kind: "and",
      args: [
        { kind: "not", arg: fact15("depIsQualifyingChildOfAnother") },
        fact15("depRelationshipRelative"),
        {
          kind: "cmp",
          op: "lt",
          left: fact15("depGrossIncome"),
          right: { kind: "param", name: "grossIncomeLimit" }
        },
        // § 152(d)(3) replaces the over-half support test with over-10%
        fact15("taxpayerProvidedOver10PercentSupport")
      ]
    },
    overrides: { ruleId: "us.federal.dependent.qualifying_relative", priority: 10 }
  })),
  {
    id: "us.federal.dependent.qualifying_child.divorced_release",
    version: 1,
    jurisdiction: "us.federal",
    title: "Divorced or separated parents: the Form 8332 release reallocates the qualifying child (\xA7 152(e))",
    citation: {
      source: "26 U.S.C. \xA7 152(e); Form 8332",
      section: "\xA7 152(e)(1), (2)",
      url: CITE_URL,
      excerpt: "\u2026a child shall be treated as the qualifying child of the NONCUSTODIAL parent if the custodial parent signs a written declaration that such parent will not claim such child as a dependent (Form 8332) \u2014 provided the parents are divorced, separated, or lived apart during the last 6 months, the child received over one-half of support from the parents, and was in the custody of one or both for more than one-half of the year (attested by the depDivorcedParentsRule fact). The releasing CUSTODIAL parent correspondingly cannot claim the child. [\xA7 152(e) moves the DEPENDENCY (CTC); the EITC residency test is unaffected by a release \u2014 disclosed.]"
    },
    ...FROM7,
    output: { type: "bool" },
    applicability: fact15("depDivorcedParentsRule"),
    formula: {
      kind: "if",
      cond: fact15("taxpayerIsCustodialParent"),
      // custodial parent: normal tests, but a signed release forfeits the claim
      then: {
        kind: "and",
        args: [
          { kind: "not", arg: fact15("custodialParentReleasedClaim") },
          fact15("depRelationshipChild"),
          ageTest,
          fact15("depLivedWithTaxpayerOverHalfYear"),
          { kind: "not", arg: fact15("depProvidedOwnSupportOverHalf") },
          { kind: "not", arg: fact15("depFilesJointReturn") }
        ]
      },
      // noncustodial parent: the release stands in for the residency test
      else: {
        kind: "and",
        args: [
          fact15("custodialParentReleasedClaim"),
          fact15("depRelationshipChild"),
          ageTest,
          { kind: "not", arg: fact15("depFilesJointReturn") }
        ]
      }
    },
    overrides: { ruleId: "us.federal.dependent.qualifying_child", priority: 10 }
  },
  {
    id: "us.federal.dependent.is_dependent",
    version: 1,
    jurisdiction: "us.federal",
    title: "Candidate can be claimed as the taxpayer's dependent (\xA7 152(a))",
    citation: {
      source: "26 U.S.C. \xA7 152(a)",
      section: "\xA7 152(a)",
      url: CITE_URL,
      excerpt: "\u2026the term 'dependent' means a qualifying child, or a qualifying relative. [\xA7 152(b) taxpayer-side limits (dependents of dependents, married dependents filing jointly, non-citizens) not modeled \u2014 disclosed.]"
    },
    ...FROM7,
    output: { type: "bool" },
    formula: {
      // qualifying-child first: if it holds, none of the relative-test
      // facts (income, support) are ever demanded
      kind: "or",
      args: [
        { kind: "rule", ruleId: "us.federal.dependent.qualifying_child" },
        { kind: "rule", ruleId: "us.federal.dependent.qualifying_relative" }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/education-credits.js
var J10 = "us.federal";
var FROM8 = { effectiveFrom: "2025-01-01" };
var fact16 = (factId) => ({ kind: "fact", factId });
var money15 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef13 = (ruleId) => ({ kind: "rule", ruleId });
var param7 = (name) => ({ kind: "param", name });
var zero8 = money15("0");
var isStatus4 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact16("filingStatus"),
  right: { kind: "enum", value: status }
});
var CITE3 = {
  source: "26 U.S.C. \xA7 25A (SSN/EIN requirements as amended by Pub. L. 119-21)",
  url: "https://www.law.cornell.edu/uscode/text/26/25A"
};
var perStudent = (factId) => ({
  kind: "add",
  args: [
    { kind: "min", args: [fact16(factId), money15("200000")] },
    {
      kind: "mulRate",
      base: {
        kind: "min",
        args: [
          { kind: "max0", arg: { kind: "sub", left: fact16(factId), right: money15("200000") } },
          money15("200000")
        ]
      },
      rate: { num: "25", den: "100" },
      round: "half-up"
    }
  ]
});
function phased(tentative) {
  const threshold2 = {
    kind: "if",
    cond: isStatus4("mfj"),
    then: param7("thresholdJoint"),
    else: param7("threshold")
  };
  const range = {
    kind: "if",
    cond: isStatus4("mfj"),
    then: param7("rangeJoint"),
    else: param7("range")
  };
  return {
    kind: "if",
    cond: isStatus4("mfs"),
    then: zero8,
    else: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: tentative,
        right: {
          kind: "mulDiv",
          a: tentative,
          b: {
            kind: "min",
            args: [
              {
                kind: "max0",
                arg: {
                  kind: "sub",
                  left: ruleRef13("us.federal.agi"),
                  right: threshold2
                }
              },
              range
            ]
          },
          c: range,
          round: "half-up"
        }
      }
    }
  };
}
var PHASE_PARAMS = {
  threshold: { value: "8000000", type: "money" },
  // $80,000
  thresholdJoint: { value: "16000000", type: "money" },
  // $160,000
  range: { value: "1000000", type: "money" },
  // $10,000
  rangeJoint: { value: "2000000", type: "money" }
  // $20,000
};
var educationCreditRules = [
  {
    id: "us.federal.education.aotc_tentative",
    version: 1,
    jurisdiction: J10,
    title: "American Opportunity Tax Credit before phase-out (per student, up to 3 students)",
    citation: {
      ...CITE3,
      section: "\xA7 25A(b)(1)",
      excerpt: "Per eligible student: 100 percent of so much of the qualified tuition and related expenses\u2026 as does not exceed $2,000, plus 25 percent of such expenses\u2026 as exceeds $2,000 but does not exceed $4,000 (maximum $2,500/student). [Assumed satisfied per student with expenses entered, and disclosed: enrolled at least half-time in one of the first 4 years of postsecondary education, AOTC not claimed for 4 prior years, no felony drug conviction, and the \xA7 25A(g) SSN/EIN requirements (as amended by Pub. L. 119-21). A fourth simultaneous AOTC student is not representable \u2014 conservative.]"
    },
    ...FROM8,
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        perStudent("aotcExpensesStudent1"),
        perStudent("aotcExpensesStudent2"),
        perStudent("aotcExpensesStudent3")
      ]
    }
  },
  {
    id: "us.federal.education.aotc",
    version: 1,
    jurisdiction: J10,
    title: "American Opportunity Tax Credit after the MAGI phase-out",
    citation: {
      ...CITE3,
      section: "\xA7 25A(d), (g)(6)",
      excerpt: "The credit is reduced by the amount which bears the same ratio as the excess of MAGI over $80,000 ($160,000 joint) bears to $10,000 ($20,000 joint) \u2014 amounts NOT indexed (post-2020 consolidation). In the case of a married individual, allowed only on a joint return (MFS: $0); a surviving-spouse return is not a joint return, so the single amounts apply. Exact statutory ratio (Form 8863 rounds the ratio to 3 decimals; sub-dollar difference). MAGI \u2248 AGI \u2014 disclosed."
    },
    ...FROM8,
    output: { type: "money" },
    parameters: { ...PHASE_PARAMS },
    formula: phased(ruleRef13("us.federal.education.aotc_tentative"))
  },
  {
    id: "us.federal.education.aotc_refundable",
    version: 1,
    jurisdiction: J10,
    title: "Refundable portion of the AOTC (40%; none for a kiddie-tax child)",
    citation: {
      ...CITE3,
      section: "\xA7 25A(i)",
      excerpt: "40 percent of so much of the credit allowed under subsection (a) as is attributable to the American Opportunity Tax Credit\u2026 shall be treated as a credit allowable under subpart C [refundable \u2014 effectively up to $1,000 per student]. The preceding sentence shall not apply\u2026 if such taxpayer is a child to whom subsection (g) of section 1 applies [the kiddie tax]."
    },
    ...FROM8,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: fact16("isSubjectToKiddieTax"),
      then: zero8,
      else: {
        kind: "mulRate",
        base: ruleRef13("us.federal.education.aotc"),
        rate: { num: "40", den: "100" },
        round: "half-up"
      }
    }
  },
  {
    id: "us.federal.education.llc",
    version: 1,
    jurisdiction: J10,
    title: "Lifetime Learning Credit after the MAGI phase-out (per return)",
    citation: {
      ...CITE3,
      section: "\xA7 25A(c), (d), (g)(6)",
      excerpt: "20 percent of so much of the qualified tuition and related expenses paid\u2026 as does not exceed $10,000 \u2014 per RETURN, not per student; phased out over the same unindexed $80,000/$160,000 MAGI range; married taxpayers only on a joint return. [Disclosed: \xA7 25A(c)(2)(A) \u2014 expenses used for the AOTC cannot also be taken into account here; the two expense inputs are assumed to be for different students/expenses.]"
    },
    ...FROM8,
    output: { type: "money" },
    parameters: { ...PHASE_PARAMS, cap: { value: "1000000", type: "money" } },
    // $10,000
    formula: phased({
      kind: "mulRate",
      base: { kind: "min", args: [fact16("llcQualifiedExpenses"), param7("cap")] },
      rate: { num: "20", den: "100" },
      round: "half-up"
    })
  },
  {
    id: "us.federal.education.nonrefundable",
    version: 3,
    // v2 predated the § 21 CDCC ahead of it in the credit ordering
    jurisdiction: J10,
    title: "Nonrefundable education credits (60% of the AOTC + all of the LLC), limited to remaining tax",
    citation: {
      source: "26 U.S.C. \xA7 25A(i); \xA7 26(a); Schedule 3 (Form 1040) line 3; Form 8863 Credit Limit Worksheet",
      section: "\xA7 25A(i); \xA7 26(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/25A",
      excerpt: "The AOTC net of its 40% refundable portion, plus the Lifetime Learning Credit, allowed as nonrefundable credits limited to the regular tax liability (\xA7 26(a)) remaining after the \xA7 21 dependent-care credit (the Form 8863 worksheet subtracts it first)."
    },
    ...FROM8,
    output: { type: "money" },
    formula: {
      kind: "min",
      args: [
        {
          kind: "add",
          args: [
            {
              kind: "max0",
              arg: {
                kind: "sub",
                left: ruleRef13("us.federal.education.aotc"),
                right: ruleRef13("us.federal.education.aotc_refundable")
              }
            },
            ruleRef13("us.federal.education.llc")
          ]
        },
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: {
              kind: "add",
              args: [
                ruleRef13("us.federal.income_tax_before_credits"),
                ruleRef13("us.federal.amt")
              ]
            },
            right: ruleRef13("us.federal.cdcc")
          }
        }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/eitc.js
var fact17 = (factId) => ({ kind: "fact", factId });
var money16 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef14 = (ruleId) => ({ kind: "rule", ruleId });
var param8 = (name) => ({ kind: "param", name });
var intLit2 = (n) => ({ kind: "int", value: n });
var zero9 = money16("0");
var isStatus5 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact17("filingStatus"),
  right: { kind: "enum", value: status }
});
var PHASE_IN = [
  { num: "765", den: "10000" },
  { num: "34", den: "100" },
  { num: "40", den: "100" },
  { num: "45", den: "100" }
];
var PHASE_OUT = [
  { num: "765", den: "10000" },
  { num: "1598", den: "10000" },
  { num: "2106", den: "10000" },
  { num: "2106", den: "10000" }
];
var TY20252 = {
  eia: [8490, 12730, 17880, 17880],
  max: [649, 4328, 7152, 8046],
  thresholdOther0: 10620,
  thresholdMFJ0: 17730,
  thresholdOtherKids: 23350,
  thresholdMFJKids: 30470,
  completedOther0: 19104,
  completedMFJ0: 26214,
  investmentLimit: 11950
};
var TY20262 = {
  eia: [8680, 13020, 18290, 18290],
  max: [664, 4427, 7316, 8231],
  thresholdOther0: 10860,
  thresholdMFJ0: 18140,
  thresholdOtherKids: 23890,
  thresholdMFJKids: 31160,
  completedOther0: 19540,
  completedMFJ0: 26820,
  investmentLimit: 12200
};
var dollars = (d3) => (BigInt(d3) * 100n).toString();
var earned = {
  // A Schedule C net loss reduces § 32(c)(2) earned income (Pub. 596 worksheet:
  // combine ALL Schedule C profits and losses), floored at zero.
  kind: "max0",
  arg: {
    kind: "sub",
    left: {
      kind: "add",
      args: [
        fact17("wages"),
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: fact17("selfEmploymentNetProfit"),
            right: ruleRef14("us.federal.se_tax_half_deduction")
          }
        }
      ]
    },
    right: fact17("scheduleCNetLoss")
  }
};
var phaseoutIncome = {
  kind: "max",
  args: [ruleRef14("us.federal.agi"), earned]
};
function bracketMidpoint(x) {
  return {
    kind: "add",
    args: [
      {
        kind: "mulInt",
        base: money16("5000"),
        // $50
        count: { kind: "stepUnits", value: x, unitCents: "5000", mode: "floor" }
      },
      money16("2500")
      // $25
    ]
  };
}
function statutoryAmount(kids, threshold2, x) {
  return {
    kind: "max0",
    arg: {
      kind: "sub",
      left: {
        kind: "min",
        args: [
          { kind: "mulRate", base: x, rate: PHASE_IN[kids], round: "half-up" },
          param8(`max${kids}`)
        ]
      },
      right: {
        kind: "mulRate",
        base: { kind: "max0", arg: { kind: "sub", left: x, right: threshold2 } },
        rate: PHASE_OUT[kids],
        round: "half-up"
      }
    }
  };
}
function tableLookup(kids, threshold2, x) {
  return {
    kind: "roundToDollar",
    value: statutoryAmount(kids, threshold2, bracketMidpoint(x)),
    mode: "half-up"
  };
}
function creditFormula(kids, threshold2) {
  const continuous = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: {
        kind: "min",
        args: [
          { kind: "mulRate", base: earned, rate: PHASE_IN[kids], round: "half-up" },
          param8(`max${kids}`)
        ]
      },
      right: {
        kind: "mulRate",
        base: { kind: "max0", arg: { kind: "sub", left: phaseoutIncome, right: threshold2 } },
        rate: PHASE_OUT[kids],
        round: "half-up"
      }
    }
  };
  const table2 = {
    kind: "if",
    cond: { kind: "cmp", op: "gt", left: earned, right: zero9 },
    // table starts at $1
    then: {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "ge",
        left: ruleRef14("us.federal.agi"),
        right: threshold2
      },
      then: {
        kind: "min",
        args: [
          tableLookup(kids, threshold2, earned),
          tableLookup(kids, threshold2, ruleRef14("us.federal.agi"))
        ]
      },
      else: tableLookup(kids, threshold2, earned)
    },
    else: zero9
  };
  return { kind: "if", cond: fact17("useFormulaMethod"), then: continuous, else: table2 };
}
function eitcRule(version, effectiveFrom, effectiveTo, p, yearLabel, revProc, opts = {}) {
  const ruleId = opts.ruleId ?? "us.federal.eitc";
  const ageGate = opts.ageGate ?? true;
  const parameters = {
    investmentLimit: { value: dollars(p.investmentLimit), type: "money" },
    thresholdOther0: { value: dollars(p.thresholdOther0), type: "money" },
    thresholdMFJ0: { value: dollars(p.thresholdMFJ0), type: "money" },
    thresholdOtherKids: { value: dollars(p.thresholdOtherKids), type: "money" },
    thresholdMFJKids: { value: dollars(p.thresholdMFJKids), type: "money" },
    completedOther0: { value: dollars(p.completedOther0), type: "money" },
    completedMFJ0: { value: dollars(p.completedMFJ0), type: "money" }
  };
  for (const k of [0, 1, 2, 3]) {
    parameters[`eia${k}`] = { value: dollars(p.eia[k]), type: "money" };
    parameters[`max${k}`] = { value: dollars(p.max[k]), type: "money" };
  }
  const kids = {
    kind: "add",
    args: [fact17("qualifyingChildren"), fact17("eitcAdditionalQualifyingChildren")]
  };
  const kidsEq = (n) => ({
    kind: "cmp",
    op: "eq",
    left: kids,
    right: intLit2(n)
  });
  const investmentOK = {
    kind: "cmp",
    op: "le",
    left: {
      kind: "add",
      args: [
        fact17("taxableInterest"),
        ruleRef14("us.federal.schedule_d.preferential_lt_gain"),
        fact17("qualifiedDividends"),
        ruleRef14("us.federal.schedule_d.ordinary_st_gain"),
        fact17("ordinaryDividends")
      ]
    },
    right: param8("investmentLimit")
  };
  const notDependent = { kind: "not", arg: fact17("isClaimedAsDependent") };
  const thresholdKids = {
    kind: "if",
    cond: isStatus5("mfj"),
    then: param8("thresholdMFJKids"),
    else: param8("thresholdOtherKids")
  };
  const threshold0 = {
    kind: "if",
    cond: isStatus5("mfj"),
    then: param8("thresholdMFJ0"),
    else: param8("thresholdOther0")
  };
  const completed0 = {
    kind: "if",
    cond: isStatus5("mfj"),
    then: param8("completedMFJ0"),
    else: param8("completedOther0")
  };
  const childlessBranch = {
    kind: "if",
    cond: {
      kind: "and",
      args: [
        // income disqualification FIRST — high earners are never asked
        // the childless age question (and() short-circuits on false)
        { kind: "cmp", op: "lt", left: phaseoutIncome, right: completed0 },
        notDependent,
        // § 32(c)(1)(A)(ii)(II) childless age floor/ceiling — OMITTED when
        // ageGate is false (us.federal.eitc_no_age_gate, for state decoupling
        // statutes like 35 ILCS 5/212(b-5)/(b-10) that recompute the federal
        // credit "without regard to" this age requirement). Omitting the
        // fact references entirely (not just short-circuiting on a default)
        // means the engine never demands isAge65OrOlder/isAtLeastAge25 to
        // evaluate this variant.
        ...ageGate ? [
          { kind: "not", arg: fact17("isAge65OrOlder") },
          // must be under 65
          fact17("isAtLeastAge25")
          // must be 25+
        ] : [],
        investmentOK
      ]
    },
    then: creditFormula(0, threshold0),
    else: zero9
  };
  const kidsBranch = {
    kind: "if",
    cond: { kind: "and", args: [notDependent, investmentOK] },
    then: {
      kind: "if",
      cond: kidsEq("1"),
      then: creditFormula(1, thresholdKids),
      else: {
        kind: "if",
        cond: kidsEq("2"),
        then: creditFormula(2, thresholdKids),
        else: creditFormula(3, thresholdKids)
        // 3 or more
      }
    },
    else: zero9
  };
  return {
    id: ruleId,
    version,
    jurisdiction: "us.federal",
    title: opts.titleSuffix ?? `Earned income credit (TY${yearLabel}; EIC Table method \u2014 \xA7 32(f) $50 brackets at midpoints; \xA7 32(d) separated spouses not modeled)`,
    citation: {
      source: `26 U.S.C. \xA7 32; ${revProc}; Form 1040 instructions (EIC Worksheet and EIC Table)`,
      section: `\xA7 32(a)-(b), (f), (i); ${revProc}`,
      url: "https://www.law.cornell.edu/uscode/text/26/32",
      excerpt: `Credit = phase-in percentage of earned income up to the maximum, reduced by the phase-out percentage of AGI (or, if greater, earned income) above the threshold; denied entirely if investment income exceeds the \xA7 32(i) limit. 'The amount of the credit allowed by this section shall be determined under tables prescribed by the Secretary\u2026 [which] shall reflect the provisions of subsections (a) and (b) and shall have income brackets of not greater than $50 each' (\xA7 32(f), verbatim) \u2014 the DEFAULT here is that table method (statutory amounts computed at each $50 bracket's midpoint, whole dollars; the EIC Worksheet looks up earned income, and also AGI when AGI reaches the phase-out threshold, taking the smaller); useFormulaMethod opts back to the continuous formula. Married taxpayers must file jointly (\xA7 32(d)) \u2014 UNLESS the MFS filer meets the \xA7 32(d)(2) separation requirements (set mfsConsideredUnmarried: qualifying child in the home > half year, > half household cost furnished, legally separated or spouse absent the last 6 months), in which case the credit computes normally. Earned income approximated as wages + SE net earnings; \xA7 32(c)(3) qualifying children = the CTC child count plus eitcAdditionalQualifyingChildren (children under 19, students under 24, or permanently and totally disabled at ANY age, who miss the CTC's under-17 cut). TY${yearLabel} dollar amounts per ${revProc}.${opts.extraExcerpt ?? ""}`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters,
    formula: {
      kind: "if",
      // § 32(d): married must file jointly — EXCEPT an MFS filer meeting the
      // § 32(d)(2) separation requirements (considered unmarried), who qualifies.
      cond: {
        kind: "and",
        args: [isStatus5("mfs"), { kind: "not", arg: fact17("mfsConsideredUnmarried") }]
      },
      then: zero9,
      else: {
        kind: "if",
        cond: kidsEq("0"),
        then: childlessBranch,
        else: kidsBranch
      }
    }
  };
}
var NO_AGE_GATE_EXCERPT = " NO AGE GATE VARIANT (us.federal.eitc_no_age_gate): recomputes this SAME \xA7 32(a)-(b), (f), (i) credit with the \xA7 32(c)(1)(A)(ii) childless-filer age requirement (25-64) removed entirely \u2014 i.e. the isAge65OrOlder/isAtLeastAge25 facts are never consulted, so a childless filer of ANY age reaches the ordinary table/formula math. Exists solely to feed STATE statutes that expressly decouple from the federal age floor, e.g. Illinois (35 ILCS 5/212(b-5) for 18-24, (b-10) for 65+, P.A. 102-700: 'based on the federal tax credit for which the taxpayer would have been eligible without regard to any age requirements... in Section 32(c)(1)(A)(ii)'). This rule's own federal answer is NOT a valid federal EITC \u2014 it is an intermediate for us.il.eitc (and any other state with an identical decoupling statute) and must never be substituted for us.federal.eitc on the federal return itself.";
var eitcRules = [
  // v5/v6: table method default (§ 32(f)) + eitcAdditionalQualifyingChildren
  // v8/v9: § 32(i) investment-income limit also counts the standalone qualifiedDividends fact
  // v10/v11: § 32(i) investment-income limit also counts shortTermCapitalGains/ordinaryDividends
  // v12/v13 read the raw gain facts; the § 32(i) test now takes the schedule_d
  // netted results ("capital gain net income", § 32(i)(1)(C))
  eitcRule(14, "2025-01-01", "2026-01-01", TY20252, "2025", "Rev. Proc. 2024-40 \xA7 2.06"),
  eitcRule(15, "2026-01-01", "2027-01-01", TY20262, "2026", "Rev. Proc. 2025-32 \xA7 4.06"),
  // us.federal.eitc_no_age_gate (new): the same § 32(a)-(b),(f),(i) formula with
  // the childless § 32(c)(1)(A)(ii) age gate removed, for state EITC statutes
  // that decouple from it (35 ILCS 5/212(b-5)/(b-10), IL P.A. 102-700) — see
  // us.il.eitc, which is 20% of THIS rule's answer, not us.federal.eitc's.
  eitcRule(3, "2025-01-01", "2026-01-01", TY20252, "2025", "Rev. Proc. 2024-40 \xA7 2.06", {
    ruleId: "us.federal.eitc_no_age_gate",
    ageGate: false,
    titleSuffix: "Earned income credit, \xA7 32(c)(1)(A)(ii) childless age gate REMOVED (TY2025; for state decoupling statutes only)",
    extraExcerpt: NO_AGE_GATE_EXCERPT
  }),
  eitcRule(4, "2026-01-01", "2027-01-01", TY20262, "2026", "Rev. Proc. 2025-32 \xA7 4.06", {
    ruleId: "us.federal.eitc_no_age_gate",
    ageGate: false,
    titleSuffix: "Earned income credit, \xA7 32(c)(1)(A)(ii) childless age gate REMOVED (TY2026; for state decoupling statutes only)",
    extraExcerpt: NO_AGE_GATE_EXCERPT
  })
];

// ../corpus-us-federal/dist/rules/feie.js
var J11 = "us.federal";
var fact18 = (factId) => ({ kind: "fact", factId });
var money17 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef15 = (ruleId) => ({ kind: "rule", ruleId });
var param9 = (name) => ({ kind: "param", name });
var zero10 = money17("0");
var gt04 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero10 });
var exclusion = ruleRef15("us.federal.feie.exclusion");
function feieExclusionRule(version, from, to, limitCents, limitLabel, sourceLabel) {
  return {
    id: "us.federal.feie.exclusion",
    version,
    jurisdiction: J11,
    title: `Foreign earned income exclusion (\xA7 911, ${limitLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 911(a), (b)(2)(D), (d); ${sourceLabel}`,
      section: "\xA7 911(a)(1), (b)(2)(D)(i)",
      url: "https://www.law.cornell.edu/uscode/text/26/911",
      excerpt: `"the foreign earned income exclusion amount under \xA7 911(b)(2)(D)(i) is ${limitLabel}" (${sourceLabel}, verbatim). Qualification is attested by the input: a tax home in a foreign country AND either bona fide residence for an uninterrupted period including an entire taxable year (\xA7 911(d)(1)(A)) or presence in a foreign country during at least 330 full days in 12 consecutive months (\xA7 911(d)(1)(B)). The \xA7 911(c) housing exclusion is NOT modeled (its cap is adjusted by annual per-locality IRS Notices) \u2014 conservative. Foreign earned income must be INCLUDED in the wages/SE inputs; this exclusion subtracts from gross income.`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: { limit: { value: limitCents, type: "money" } },
    formula: {
      kind: "min",
      args: [fact18("foreignEarnedIncome"), param9("limit")]
    }
  };
}
function feieStackingRule(version, from, to, year) {
  const withExcluded = ordinaryRateTaxExpr({
    kind: "add",
    args: [ruleRef15("us.federal.taxable_income"), exclusion]
  }, "filingStatus", year);
  const onExcluded = ordinaryRateTaxExpr(exclusion, "filingStatus", year);
  return {
    id: `us.federal.income_tax_before_credits.feie`,
    version,
    jurisdiction: J11,
    title: `\xA7 911(f) stacking: tax at the rates that would apply with the excluded income (TY${year})`,
    citation: {
      source: "26 U.S.C. \xA7 911(f)(1); Foreign Earned Income Tax Worksheet (Form 1040 instructions)",
      section: "\xA7 911(f)(1)(A)",
      url: "https://www.law.cornell.edu/uscode/text/26/911",
      excerpt: "If, for any taxable year, any amount is excluded from gross income of a taxpayer under subsection (a)\u2026 the tax imposed by section 1\u2026 shall be equal to the excess (if any) of\u2014 (i) the tax which would be imposed by section 1\u2026 if the taxpayer's taxable income were increased by the amount excluded under subsection (a)\u2026, over (ii) the tax which would be imposed by section 1\u2026 if the taxpayer's taxable income were equal to the amount excluded under subsection (a)\u2026 [Both terms use the Tax Table method below $100,000 per the Foreign Earned Income Tax Worksheet. REFUSES with preferential-rate income (the \xA7 911(f)(2) capital-gains coordination is not modeled) and with nonzero AMT (the \xA7 911(f)(1)(B) AMT stacking is not modeled).]"
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    applicability: gt04(exclusion),
    formula: {
      kind: "if",
      // amt itself references income_tax_before_credits (§ 55 compares TMT
      // to the regular tax), so the guard keys on the AMT-triggering INPUT
      // facts rather than the amt rule — referencing it here would cycle
      cond: {
        kind: "or",
        args: [
          gt04(fact18("longTermCapitalGains")),
          gt04(fact18("qualifiedDividends")),
          gt04(fact18("amtIsoExerciseSpread")),
          gt04(fact18("amtOtherPreferences"))
        ]
      },
      then: {
        kind: "unsupported",
        reason: "the \xA7 911(f)(2) preferential-rate coordination and the \xA7 911(f)(1)(B) AMT stacking are not modeled \u2014 a \xA7 911 return with capital gains/qualified dividends or entered AMT preference items refuses"
      },
      else: {
        kind: "max0",
        arg: { kind: "sub", left: withExcluded, right: onExcluded }
      }
    },
    overrides: {
      ruleId: "us.federal.income_tax_before_credits",
      priority: 15
      // above the base; below the kiddie override (20) — a kiddie child abroad is the rarer, stricter case
    }
  };
}
var feieRules = [
  feieExclusionRule(1, "2025-01-01", "2026-01-01", "13000000", "$130,000", "Rev. Proc. 2024-40 \xA7 2.39"),
  feieExclusionRule(2, "2026-01-01", "2027-01-01", "13290000", "$132,900", "Rev. Proc. 2025-32 \xA7 4.39"),
  // v3/v4: the preferential-income refusal guard also catches the standalone qualifiedDividends fact
  feieStackingRule(3, "2025-01-01", "2026-01-01", "2025"),
  feieStackingRule(4, "2026-01-01", "2027-01-01", "2026"),
  {
    id: "us.federal.eitc.feie_denial",
    version: 1,
    jurisdiction: J11,
    title: "\xA7 911 claimants get no earned income credit",
    citation: {
      source: "26 U.S.C. \xA7 32(c)(1)(C)",
      section: "\xA7 32(c)(1)(C)",
      url: "https://www.law.cornell.edu/uscode/text/26/32",
      excerpt: "The term 'eligible individual' does not include any individual who claims the benefits of section 911 (relating to citizens or residents living abroad) for the taxable year."
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    applicability: gt04(exclusion),
    formula: zero10,
    overrides: {
      ruleId: "us.federal.eitc",
      priority: 30
    }
  }
];

// ../corpus-us-federal/dist/rules/fiduciary.js
var J12 = "us.federal";
var fact19 = (factId) => ({ kind: "fact", factId });
var money18 = (cents2) => ({ kind: "money", cents: cents2 });
var zero11 = money18("0");
var gt05 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero11 });
var exemption = {
  kind: "match",
  on: fact19("fiduciaryType"),
  cases: [
    { when: "estate", value: money18("60000") },
    { when: "simple-trust", value: money18("30000") },
    { when: "complex-trust", value: money18("10000") }
  ]
};
function fiduciaryRule(version, from, to, b1, b2, b3, tableLabel, sourceLabel) {
  return {
    id: "us.federal.fiduciary.income_tax",
    version,
    jurisdiction: J12,
    title: `Estate/trust income tax \u2014 \xA7 1(e) compressed brackets (${tableLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 1(e), (j)(2)(E); \xA7 642(b); ${sourceLabel}`,
      section: "\xA7 1(j)(2)(E); \xA7 642(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/1",
      excerpt: `${tableLabel} rate schedule for estates and trusts (${sourceLabel}, Table 5, verbatim thresholds): 10% to ${fmt(b1)}, 24% to ${fmt(b2)}, 35% to ${fmt(b3)}, 37% above. The \xA7 642(b) exemption is deducted in lieu of \xA7 151: 'An estate shall be allowed a deduction of $600'; a trust 'required to distribute all of its income currently' $300; any other trust $100 (statutory, unindexed). [Input is taxable income BEFORE the exemption, AFTER the \xA7\xA7 651/661 income-distribution deduction \u2014 the DNI machinery is attested. Preferential-rate income REFUSES (the \xA7 1(h) trust breakpoints are not modeled); the estate/trust AMT and \xA7 642(b)(2)(C) qualified disability trusts are not modeled \u2014 disclosed. Grantor trusts are not taxed here at all \u2014 their items belong on the grantor's return.]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: gt05(fact19("fiduciaryLongTermGains")),
      then: {
        kind: "unsupported",
        reason: "preferential-rate income in an estate/trust is not modeled \u2014 the \xA7 1(h) breakpoints for estates and trusts and the Schedule D Tax Worksheet stacking refuse rather than tax gains at ordinary rates"
      },
      else: {
        kind: "brackets",
        base: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: fact19("fiduciaryIncomeBeforeExemption"),
            right: exemption
          }
        },
        table: [
          { threshold: "0", rate: { num: "10", den: "100" } },
          { threshold: b1, rate: { num: "24", den: "100" } },
          { threshold: b2, rate: { num: "35", den: "100" } },
          { threshold: b3, rate: { num: "37", den: "100" } }
        ]
      }
    }
  };
}
function fmt(cents2) {
  const d3 = BigInt(cents2) / 100n;
  return `$${d3.toLocaleString("en-US")}`;
}
var fiduciaryRules = [
  fiduciaryRule(1, "2025-01-01", "2026-01-01", "315000", "1145000", "1565000", "TY2025", "Rev. Proc. 2024-40 \xA7 2.01"),
  fiduciaryRule(2, "2026-01-01", "2027-01-01", "330000", "1170000", "1600000", "TY2026", "Rev. Proc. 2025-32 \xA7 4.01")
];

// ../corpus-us-federal/dist/rules/ptc.js
var J13 = "us.federal";
var fact20 = (factId) => ({ kind: "fact", factId });
var money19 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef16 = (ruleId) => ({ kind: "rule", ruleId });
var param10 = (name) => ({ kind: "param", name });
var zero12 = money19("0");
var gt06 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero12 });
var isStatus6 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact20("filingStatus"),
  right: { kind: "enum", value: status }
});
var householdIncome = {
  kind: "add",
  args: [
    ruleRef16("us.federal.agi"),
    ruleRef16("us.federal.feie.exclusion"),
    fact20("taxExemptInterest"),
    {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact20("socialSecurityBenefits"),
        right: ruleRef16("us.federal.taxable_social_security")
      }
    }
  ]
};
var povertyLine = {
  kind: "add",
  args: [
    param10("fplFirstPerson"),
    {
      kind: "mulInt",
      base: param10("fplPerAdditional"),
      count: {
        kind: "sub",
        left: fact20("ptcHouseholdSize"),
        right: { kind: "int", value: "1" }
      }
    }
  ]
};
var fplPercentCents = {
  kind: "mulDiv",
  a: householdIncome,
  b: money19("10000"),
  c: povertyLine,
  round: "floor"
};
function tierChain(fplPct, tiers2, above400) {
  let chain = above400;
  for (let i = tiers2.length - 1; i >= 0; i--) {
    const t = tiers2[i];
    const width = String(Number(t.hi) - Number(t.lo));
    const span = String(Number(t.final) - Number(t.initial));
    const interpolated = t.initial === t.final ? money19(t.initial) : {
      kind: "add",
      args: [
        money19(t.initial),
        {
          kind: "mulDiv",
          a: money19(span),
          b: {
            kind: "sub",
            left: fplPct,
            right: money19(t.lo)
          },
          c: money19(width),
          round: "half-up"
        }
      ]
    };
    chain = {
      kind: "if",
      cond: { kind: "cmp", op: "lt", left: fplPct, right: money19(t.hi) },
      then: interpolated,
      else: chain
    };
  }
  return chain;
}
var contribution = (pctCents) => ({
  kind: "mulDiv",
  a: householdIncome,
  b: pctCents,
  c: money19("10000"),
  round: "half-up"
});
var assistanceAmount = (pctCents) => ({
  kind: "min",
  args: [
    fact20("marketplacePremiumsPaid"),
    {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact20("slcspAnnualPremium"),
        right: contribution(pctCents)
      }
    }
  ]
});
function ptcRule(version, from, to, fplFirstPersonCents, fplPerAdditionalCents, tiers2, above400, yearLabel, excerpt) {
  const inRange = above400 === "cliff" ? {
    kind: "if",
    cond: {
      kind: "cmp",
      op: "gt",
      left: fplPercentCents,
      right: money19("40000")
      // 400.00%
    },
    then: zero12,
    else: assistanceAmount(tierChain(
      fplPercentCents,
      tiers2,
      zero12
      /* unreachable */
    ))
  } : assistanceAmount(tierChain(fplPercentCents, tiers2, above400));
  const core = {
    kind: "if",
    // § 36B(c)(1)(A): household income must be at least 100% of the
    // poverty line — below it, not an applicable taxpayer
    cond: { kind: "cmp", op: "lt", left: fplPercentCents, right: money19("10000") },
    then: zero12,
    else: inRange
  };
  return {
    id: "us.federal.ptc",
    version,
    jurisdiction: J13,
    title: `Premium tax credit (\xA7 36B, ${yearLabel})`,
    citation: {
      source: version > 1 ? "26 U.S.C. \xA7 36B (post-ARPA-sunset); Rev. Proc. 2025-25 \xA7 3.01; 90 FR 5917 (2025 HHS poverty guidelines)" : "26 U.S.C. \xA7 36B(b)(3)(A)(iii), (c)(1)(E) (ARPA/IRA, sunset before 2026); Rev. Proc. 2024-35 \xA7 2.01; 89 FR 2961 (2024 HHS poverty guidelines)",
      section: "\xA7 36B(b), (c)(1), (d)",
      url: "https://www.law.cornell.edu/uscode/text/26/36B",
      excerpt
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: {
      fplFirstPerson: { value: fplFirstPersonCents, type: "money" },
      fplPerAdditional: { value: fplPerAdditionalCents, type: "money" }
    },
    formula: {
      kind: "if",
      cond: { kind: "not", arg: gt06(fact20("slcspAnnualPremium")) },
      then: {
        kind: "if",
        cond: gt06(fact20("advancePTC")),
        then: {
          kind: "unsupported",
          reason: "advance PTC was paid but no SLCSP benchmark premium was provided \u2014 the \xA7 36B(f) reconciliation cannot be computed without slcspAnnualPremium (Form 1095-A line 33B)"
        },
        else: zero12
      },
      else: {
        kind: "if",
        cond: {
          kind: "and",
          args: [
            isStatus6("mfs"),
            // Reg. § 1.36B-2(b)(2): abuse/abandonment relief waives § 36B(c)(1)(C)
            { kind: "not", arg: fact20("mfsAbuseOrAbandonmentException") }
          ]
        },
        then: zero12,
        // § 36B(c)(1)(C): MFS without the relief attestation gets no PTC
        else: core
      }
    }
  };
}
var TIERS_2025 = [
  { lo: "10000", hi: "15000", initial: "0", final: "0" },
  { lo: "15000", hi: "20000", initial: "0", final: "200" },
  { lo: "20000", hi: "25000", initial: "200", final: "400" },
  { lo: "25000", hi: "30000", initial: "400", final: "600" },
  { lo: "30000", hi: "40000", initial: "600", final: "850" }
];
var TIERS_2026 = [
  { lo: "10000", hi: "13300", initial: "210", final: "210" },
  { lo: "13300", hi: "15000", initial: "314", final: "419" },
  { lo: "15000", hi: "20000", initial: "419", final: "660" },
  { lo: "20000", hi: "25000", initial: "660", final: "844" },
  { lo: "25000", hi: "30000", initial: "844", final: "996" },
  { lo: "30000", hi: "40001", initial: "996", final: "996" }
  // "but not more than 400%" — inclusive
];
function repayment2025() {
  const capFor = (unmarriedCents, otherCents) => ({
    kind: "if",
    cond: isStatus6("single"),
    then: money19(unmarriedCents),
    else: money19(otherCents)
  });
  const cappedBand = {
    kind: "if",
    cond: { kind: "cmp", op: "lt", left: fplPercentCents, right: money19("20000") },
    then: capFor("37500", "75000"),
    else: {
      kind: "if",
      cond: { kind: "cmp", op: "lt", left: fplPercentCents, right: money19("30000") },
      then: capFor("97500", "195000"),
      else: capFor("162500", "325000")
    }
  };
  const excess = {
    kind: "max0",
    arg: {
      kind: "sub",
      left: fact20("advancePTC"),
      right: ruleRef16("us.federal.ptc")
    }
  };
  return {
    id: "us.federal.ptc.excess_aptc_repayment",
    version: 1,
    jurisdiction: J13,
    title: "Excess advance PTC repayment, limited (\xA7 36B(f)(2)(B), TY2025)",
    citation: {
      source: "26 U.S.C. \xA7 36B(f)(2) (2024 ed.); Rev. Proc. 2024-40 \xA7 2.07",
      section: "\xA7 36B(f)(2)(B)",
      url: "https://www.law.cornell.edu/uscode/text/26/36B",
      excerpt: "Advance payments in excess of the allowed credit increase the tax imposed \u2014 but for a taxpayer whose household income is less than 400 percent of the poverty line, the increase is capped by the applicable dollar amount: for 2025, $750 (under 200% FPL), $1,950 (200\u2013300%), $3,250 (300\u2013400%), 'one-half of such amount in the case of a taxpayer whose tax is determined under section 1(c)' [unmarried other than surviving spouses and heads of household] (Rev. Proc. 2024-40 \xA7 2.07: $375/$975/$1,625). At or above 400% FPL the excess is repaid in full."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      // the same TY2025 poverty-line parameters as the credit rule —
      // the FPL band selects the repayment cap
      fplFirstPerson: { value: "1506000", type: "money" },
      // $15,060
      fplPerAdditional: { value: "538000", type: "money" }
      // $5,380
    },
    formula: {
      kind: "if",
      cond: gt06(fact20("advancePTC")),
      then: {
        kind: "if",
        cond: { kind: "cmp", op: "lt", left: fplPercentCents, right: money19("40000") },
        then: { kind: "min", args: [excess, cappedBand] },
        else: excess
      },
      else: zero12
    }
  };
}
var ptcRules = [
  ptcRule(
    2,
    "2025-01-01",
    "2026-01-01",
    "1506000",
    // $15,060 (2024 guidelines, used for TY2025)
    "538000",
    // $5,380 per additional person
    TIERS_2025,
    // 400%+ in 2025: 8.5%, no cliff (§ 36B(c)(1)(E))
    money19("850"),
    "TY2025: ARPA/IRA 0\u20138.5%, no 400% cliff",
    `For taxable years beginning after December 31, 2020, and before January 1, 2026, the enhanced table applies (\xA7 36B(b)(3)(A)(iii); Rev. Proc. 2024-35: 0% to 150% FPL, rising linearly to 8.5% at 300\u2013400%) and subparagraph (A) is applied 'without regard to "but does not exceed 400 percent"' (\xA7 36B(c)(1)(E)) \u2014 8.5% with NO income cliff. Premium assistance is the lesser of enrollment premiums or the excess of the SLCSP benchmark over the applicable percentage of household income (\xA7 36B(b)(2)). Household income = MAGI + tax-exempt interest + the \xA7 911 exclusion + nontaxable social security (\xA7 36B(d)(2)(B)); poverty line per the 2024 HHS guidelines (89 FR 2961: $15,060 + $5,380/person, 48 contiguous states \u2014 Alaska/Hawaii's higher guidelines not modeled, conservative). Below 100% FPL: not an applicable taxpayer \u2014 $0 (\xA7 36B(c)(1)(A); the (c)(1)(B) immigrant special rule not modeled, conservative). Married filing separately: $0 (\xA7 36B(c)(1)(C); the abuse/abandonment exception not modeled). [Monthly computation annualized; dependents' MAGI and the \xA7 36B(c)(2) employer-coverage/affordability disqualifications attested by the inputs; Form 8962's whole-percent and 4-decimal table rounding may differ sub-dollar \u2014 statutory linear interpolation encoded.]`
  ),
  ptcRule(
    3,
    "2026-01-01",
    "2027-01-01",
    "1565000",
    // $15,650 (2025 guidelines, used for TY2026)
    "550000",
    // $5,500 per additional person
    TIERS_2026,
    // the 2026 cliff: over 400% FPL → not an applicable taxpayer → $0 credit
    "cliff",
    "TY2026: indexed statutory table, the 400% cliff returns",
    "For taxable years beginning in 2026 the ARPA/IRA enhancements have sunset by their own terms: the applicable percentage comes from the indexed statutory table (Rev. Proc. 2025-25 \xA7 3.01: 2.10% under 133% FPL; 3.14\u21924.19 at 133\u2013150; 4.19\u21926.60 at 150\u2013200; 6.60\u21928.44 at 200\u2013250; 8.44\u21929.96 at 250\u2013300; 9.96 at 300\u2013400), and the term 'applicable taxpayer' again requires household income that 'does not exceed 400 percent of\u2026 the poverty line' (\xA7 36B(c)(1)(A)) \u2014 one dollar past 400% FPL destroys the entire credit. Premium assistance is the lesser of enrollment premiums or the SLCSP benchmark minus the applicable percentage of household income (\xA7 36B(b)(2)). Household income = MAGI + tax-exempt interest + the \xA7 911 exclusion + nontaxable social security (\xA7 36B(d)(2)(B)); poverty line per the 2025 HHS guidelines (90 FR 5917: $15,650 + $5,500/person, 48 contiguous states \u2014 AK/HI not modeled, conservative). Below 100% FPL: $0 (\xA7 36B(c)(1)(A); OBBBA \xA7 71302 struck the (c)(1)(B) special rule). MFS: $0 (\xA7 36B(c)(1)(C)). [Monthly computation annualized; dependents' MAGI and employer-coverage disqualifications attested; Form 8962 rounding may differ sub-dollar.]"
  ),
  {
    id: "us.federal.ptc.net",
    version: 1,
    jurisdiction: J13,
    title: "Net premium tax credit (credit in excess of advance payments)",
    citation: {
      source: "26 U.S.C. \xA7 36B(f)(1); Schedule 3 (Form 1040) line 9",
      section: "\xA7 36B(f)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/36B",
      excerpt: "The credit allowed is reduced (but not below zero) by the advance payments made on the taxpayer's behalf \u2014 the remainder is the refundable net premium tax credit; advance payments in excess of the credit are an addition to tax (the excess-APTC repayment target)."
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: ruleRef16("us.federal.ptc"),
        right: fact20("advancePTC")
      }
    }
  },
  repayment2025(),
  {
    id: "us.federal.ptc.excess_aptc_repayment",
    version: 2,
    jurisdiction: J13,
    title: "Excess advance PTC repayment \u2014 UNCAPPED (OBBBA repealed \xA7 36B(f)(2)(B), TY2026+)",
    citation: {
      source: "26 U.S.C. \xA7 36B(f)(2), as amended by Pub. L. 119-21 (OBBBA) \xA7 71305; Rev. Proc. 2025-32 \xA7 2.04",
      section: "\xA7 36B(f)(2); OBBBA \xA7 71305",
      url: "https://www.law.cornell.edu/uscode/text/26/36B",
      excerpt: "Section 71305 of the OBBBA removes \xA7 36B(f)(2)(B), which limited the tax increase from excess advance payments for certain households, effective for taxable years beginning after December 31, 2025 (Rev. Proc. 2025-32 \xA7 2.04, verbatim) \u2014 the FULL excess of advance payments over the allowed credit is repaid regardless of income."
    },
    effectiveFrom: "2026-01-01",
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact20("advancePTC"),
        right: ruleRef16("us.federal.ptc")
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/residual.js
var J14 = "us.federal";
var fact21 = (factId) => ({ kind: "fact", factId });
var money20 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef17 = (ruleId) => ({ kind: "rule", ruleId });
var param11 = (name) => ({ kind: "param", name });
var zero13 = money20("0");
function householdRule(version, from, to, thresholdCents, thresholdLabel, sourceLabel) {
  return {
    id: "us.federal.household_employment_taxes",
    version,
    jurisdiction: J14,
    title: `Household employment taxes \u2014 Schedule H (${thresholdLabel} FICA threshold)`,
    citation: {
      source: `26 U.S.C. \xA7\xA7 3101, 3111, 3121(a)(7)(B), (x), 3306(c)(2); ${sourceLabel}`,
      section: "\xA7 3121(a)(7)(B), (x); \xA7 3306(c)(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/3121",
      excerpt: `Cash remuneration for domestic service in the employer's private home is excluded from FICA wages 'if the cash remuneration paid in such year\u2026 is less than the applicable dollar threshold' \u2014 ${thresholdLabel} for this year (${sourceLabel}); at or above it, the combined employer and employee FICA (12.4% OASDI + 2.9% Medicare = 15.3%) applies to ALL the wages. FUTA applies if $1,000+ was paid in any calendar quarter of this or the prior year (\xA7 3306(c)(2), attested by the flag): 6.0% of the first $7,000, net 0.6% with the full 5.4% state-contribution credit assumed \u2014 attested. [ONE household employee modeled; the \xA7 3121(b)(3) family exclusions (spouse, child under 21, certain parents, under-18 students) and the OASDI wage base are attested/assumed by the input; income-tax withholding for the employee is not modeled.]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: {
      ficaThreshold: { value: thresholdCents, type: "money" },
      futaWageCap: { value: "700000", type: "money" }
      // $7,000
    },
    formula: {
      kind: "add",
      args: [
        {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "ge",
            left: fact21("householdEmployeeCashWages"),
            right: param11("ficaThreshold")
          },
          then: {
            kind: "mulRate",
            base: fact21("householdEmployeeCashWages"),
            rate: { num: "153", den: "1000" },
            round: "half-up"
          },
          else: zero13
        },
        {
          kind: "if",
          cond: fact21("householdFutaTestMet"),
          then: {
            kind: "mulRate",
            base: {
              kind: "min",
              args: [fact21("householdEmployeeCashWages"), param11("futaWageCap")]
            },
            rate: { num: "6", den: "1000" },
            // net 0.6% (full state credit attested)
            round: "half-up"
          },
          else: zero13
        }
      ]
    }
  };
}
var residualRules = [
  householdRule(1, "2025-01-01", "2026-01-01", "280000", "$2,800", "89 FR 85276; 2025 Instructions for Schedule H"),
  householdRule(2, "2026-01-01", "2027-01-01", "300000", "$3,000", "90 FR 49047"),
  {
    id: "us.federal.estimated.required_annual_payment.farmer",
    version: 1,
    jurisdiction: J14,
    title: "Required annual payment \u2014 farmers and fishermen (66\u2154%, \xA7 6654(i))",
    citation: {
      source: "26 U.S.C. \xA7 6654(i)",
      section: "\xA7 6654(i)(1)\u2013(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/6654",
      excerpt: "If an individual is a farmer or fisherman\u2026 there shall be only 1 required installment\u2026 the due date\u2026 shall be January 15 of the following taxable year\u2026 the amount\u2026 shall be equal to the required annual payment determined under subsection (d)(1)(B) by substituting '66\u2154 percent' for '90 percent' and without regard to subparagraph (C) of subsection (d)(1) [the 110% high-AGI prong]. Qualification \u2014 gross income from farming or fishing (including oyster farming) at least 66\u2154 percent of total gross income this year or the preceding year \u2014 is ATTESTED by the isFarmerOrFisherman flag. [Filing and paying in full by March 1 avoids the addition entirely (\xA7 6654(i)(1)(D)); the single installment equals this full amount, not 25% of it.]"
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    applicability: fact21("isFarmerOrFisherman"),
    formula: {
      kind: "min",
      args: [
        {
          kind: "mulRate",
          base: ruleRef17("us.federal.net_tax"),
          rate: { num: "2", den: "3" },
          // 66⅔ percent, exact
          round: "half-up"
        },
        {
          kind: "mulRate",
          base: fact21("priorYearTax"),
          rate: { num: "1", den: "1" },
          round: "half-up"
        }
      ]
    },
    overrides: {
      ruleId: "us.federal.estimated.required_annual_payment",
      priority: 20
      // beats the 110% high-AGI override — § 6654(i)(1)(C) disregards (d)(1)(C)
    }
  },
  {
    id: "us.federal.home_office_simplified",
    version: 1,
    jurisdiction: J14,
    title: "Home office deduction \u2014 simplified safe harbor ($5/sq ft, 300 sq ft cap)",
    citation: {
      source: "26 U.S.C. \xA7 280A(c); Rev. Proc. 2013-13",
      section: "Rev. Proc. 2013-13 \xA7 4.01",
      url: "https://www.irs.gov/pub/irs-drop/rp-13-13.pdf",
      excerpt: "The allowable square footage is the portion of a home used in a qualified business use of the home, but not to exceed 300 square feet\u2026 The prescribed rate is $5.00 (Rev. Proc. 2013-13 \xA7 4.01, verbatim). Limited to gross income from the business use (\xA7 280A(c)(5) \u2014 approximated by the SE net profit input; no carryover under the safe harbor). [A STANDALONE determination: subtract the result from your --se-profit yourself \u2014 it changes SE tax and \xA7 199A QBI. Exclusive-and-regular use and the \xA7 280A(c)(1) principal-place-of-business tests are attested by the input; the actual-expense method (depreciation, allocation, carryovers) is not modeled.]"
    },
    effectiveFrom: "2025-01-01",
    // Rev. Proc. 2013-13 rate, unchanged
    output: { type: "money" },
    parameters: {
      ratePerSqFt: { value: "500", type: "money" },
      // $5.00
      sqFtCap: { value: "300", type: "int" }
    },
    formula: {
      kind: "if",
      cond: gt0AsInt(fact21("homeOfficeSquareFeet")),
      then: {
        kind: "min",
        args: [
          {
            kind: "mulInt",
            base: param11("ratePerSqFt"),
            count: {
              kind: "min",
              args: [fact21("homeOfficeSquareFeet"), param11("sqFtCap")]
            }
          },
          fact21("selfEmploymentNetProfit")
        ]
      },
      else: zero13
    }
  }
];
function gt0AsInt(e) {
  return { kind: "cmp", op: "gt", left: e, right: { kind: "int", value: "0" } };
}

// ../corpus-us-federal/dist/rules/employer.js
var J15 = "us.federal";
var fact22 = (factId) => ({ kind: "fact", factId });
var money21 = (cents2) => ({ kind: "money", cents: cents2 });
var param12 = (name) => ({ kind: "param", name });
var rate = (num, den, base) => ({
  kind: "mulRate",
  base,
  rate: { num, den },
  round: "half-up"
});
function payrollRule(version, from, to, wageBaseCents, yearLabel, baseLabel) {
  return {
    id: "us.federal.employer.payroll_tax",
    version,
    jurisdiction: J15,
    title: `Employer payroll tax per employee (FICA + FUTA, TY${yearLabel})`,
    citation: {
      source: "26 U.S.C. \xA7\xA7 3111(a)\u2013(b), 3301, 3302; SSA wage base announcement",
      section: "\xA7 3111; \xA7\xA7 3301\u20133302",
      url: "https://www.law.cornell.edu/uscode/text/26/3111",
      excerpt: `The employer share: 6.2 percent of wages up to the ${baseLabel} contribution and benefit base for ${yearLabel}, plus 1.45 percent of all wages (the employer does not match the employee's 0.9% Additional Medicare), plus FUTA at 6.0 percent of the first $7,000 less the 5.4 percent credit for timely state unemployment contributions \u2014 net 0.6 percent ($42 maximum; full credit and no \xA7 3302(c) credit-reduction state assumed, disclosed).`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: {
      ssWageBase: { value: wageBaseCents, type: "money" },
      futaWageBase: { value: "700000", type: "money" }
      // $7,000 (since 1983)
    },
    formula: {
      kind: "add",
      args: [
        rate("62", "1000", {
          kind: "min",
          args: [fact22("employeeAnnualWages"), param12("ssWageBase")]
        }),
        rate("145", "10000", fact22("employeeAnnualWages")),
        rate("6", "1000", {
          kind: "min",
          args: [fact22("employeeAnnualWages"), param12("futaWageBase")]
        })
      ]
    }
  };
}
var employerRules = [
  payrollRule(1, "2025-01-01", "2026-01-01", "17610000", "2025", "$176,100"),
  payrollRule(2, "2026-01-01", "2027-01-01", "18450000", "2026", "$184,500"),
  {
    id: "us.federal.credit.research_asc",
    version: 1,
    jurisdiction: J15,
    title: "Research credit \u2014 alternative simplified credit (\xA7 41(c)(4))",
    citation: {
      source: "26 U.S.C. \xA7 41(c)(4); Form 6765",
      section: "\xA7 41(c)(4)(A)\u2013(B)",
      url: "https://www.law.cornell.edu/uscode/text/26/41",
      excerpt: "\u2026the alternative simplified credit equals 14 percent of so much of the qualified research expenses\u2026 as exceeds 50 percent of the average qualified research expenses for the 3 taxable years preceding \u2014 or, if the taxpayer has no qualified research expenses in any one of the 3 preceding taxable years, 6 percent of the current year's QREs. [This is the GROSS credit: the \xA7 38 general-business-credit limitation and the \xA7 280C(c) interaction with the \xA7 174A deduction (reduced-credit election) are not modeled \u2014 disclosed. Qualified-research (\xA7 41(d)) qualification of the expenses is attested by the inputs.]"
    },
    effectiveFrom: "2025-01-01",
    // statutory, unindexed: open-ended
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "eq",
        left: fact22("qreAvgPrior3Years"),
        right: money21("0")
      },
      then: rate("6", "100", fact22("qreCurrentYear")),
      else: rate("14", "100", {
        kind: "max0",
        arg: {
          kind: "sub",
          left: fact22("qreCurrentYear"),
          right: rate("50", "100", fact22("qreAvgPrior3Years"))
        }
      })
    }
  }
];

// ../corpus-us-federal/dist/rules/estimated-tax.js
var fact23 = (factId) => ({ kind: "fact", factId });
var ruleRef18 = (ruleId) => ({ kind: "rule", ruleId });
var FROM9 = { effectiveFrom: "2025-01-01" };
var isStatus7 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact23("filingStatus"),
  right: { kind: "enum", value: status }
});
var estimatedTaxRules = [
  {
    id: "us.federal.estimated.required_annual_payment",
    version: 1,
    jurisdiction: "us.federal",
    title: "Required annual payment (\xA7 6654 safe harbor)",
    citation: {
      source: "26 U.S.C. \xA7 6654(d)(1)(B), (C)",
      section: "\xA7 6654(d)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/6654",
      excerpt: "\u2026the lesser of (i) 90 percent of the tax shown on the return for the taxable year\u2026 or (ii) 100 percent of the tax shown on the return of the individual for the preceding taxable year. If the adjusted gross income\u2026 for the preceding taxable year exceeds $150,000 [$75,000 married filing separately], clause (ii) shall be applied by substituting '110 percent'."
    },
    ...FROM9,
    output: { type: "money" },
    parameters: {
      highAGIThreshold: { value: "15000000", type: "money" },
      // $150,000
      highAGIThresholdMFS: { value: "7500000", type: "money" }
      // $75,000
    },
    formula: {
      kind: "min",
      args: [
        // 90% of this year's tax (income + SE/NIIT/Add'l Medicare, net of credits)
        {
          kind: "mulRate",
          base: ruleRef18("us.federal.net_tax"),
          rate: { num: "90", den: "100" },
          round: "half-up"
        },
        // 100%/110% of last year's tax
        {
          kind: "mulRate",
          base: fact23("priorYearTax"),
          rate: { num: "1", den: "1" },
          round: "half-up"
        }
      ]
    }
  },
  {
    // the 110% substitution as an OVERRIDE — the statute's own structure
    id: "us.federal.estimated.required_annual_payment.high_agi",
    version: 1,
    jurisdiction: "us.federal",
    title: "Required annual payment \u2014 110% prong for prior-year AGI over $150,000",
    citation: {
      source: "26 U.S.C. \xA7 6654(d)(1)(C)",
      section: "\xA7 6654(d)(1)(C)",
      url: "https://www.law.cornell.edu/uscode/text/26/6654",
      excerpt: "\u2026clause (ii) of subparagraph (B) shall be applied by substituting '110 percent' for '100 percent'. \u2026'$75,000' [is substituted for '$150,000'] in the case of a married individual filing separately."
    },
    ...FROM9,
    output: { type: "money" },
    applicability: {
      kind: "cmp",
      op: "gt",
      left: fact23("priorYearAGI"),
      right: {
        kind: "if",
        cond: isStatus7("mfs"),
        then: { kind: "param", name: "highAGIThresholdMFS" },
        else: { kind: "param", name: "highAGIThreshold" }
      }
    },
    parameters: {
      highAGIThreshold: { value: "15000000", type: "money" },
      highAGIThresholdMFS: { value: "7500000", type: "money" }
    },
    formula: {
      kind: "min",
      args: [
        {
          kind: "mulRate",
          base: ruleRef18("us.federal.net_tax"),
          rate: { num: "90", den: "100" },
          round: "half-up"
        },
        {
          kind: "mulRate",
          base: fact23("priorYearTax"),
          rate: { num: "110", den: "100" },
          round: "half-up"
        }
      ]
    },
    overrides: {
      ruleId: "us.federal.estimated.required_annual_payment",
      priority: 10
    }
  },
  {
    id: "us.federal.estimated.quarterly_payment",
    version: 1,
    jurisdiction: "us.federal",
    title: "Required quarterly estimated payment (even installments, withholding deemed even)",
    citation: {
      source: "26 U.S.C. \xA7 6654(d)(1)(A); \xA7 6654(g)(1)",
      section: "\xA7 6654(d)(1)(A), (g)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/6654",
      excerpt: "\u2026the amount of any required installment shall be 25 percent of the required annual payment; \u2026an equal part of [withheld tax] shall be deemed paid on each due date. [Annualized-income method for uneven income not modeled.]"
    },
    ...FROM9,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "mulRate",
        base: {
          kind: "sub",
          left: ruleRef18("us.federal.estimated.required_annual_payment"),
          right: fact23("federalTaxWithheld")
        },
        rate: { num: "25", den: "100" },
        round: "half-up"
      }
    }
  },
  {
    id: "us.federal.estimated.safe_harbor_met",
    version: 1,
    jurisdiction: "us.federal",
    title: "Withholding alone already satisfies \xA7 6654 (no estimated payments needed)",
    citation: {
      source: "26 U.S.C. \xA7 6654(e)(1); \xA7 6654(d)(1)",
      section: "\xA7 6654(e)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/6654",
      excerpt: "No addition to tax shall be imposed\u2026 if the tax shown on the return\u2026, reduced by the credit allowable under section 31 [withholding], is less than $1,000 \u2014 or if withholding alone reaches the required annual payment."
    },
    ...FROM9,
    output: { type: "bool" },
    parameters: {
      deMinimis: { value: "100000", type: "money" }
      // $1,000
    },
    formula: {
      kind: "or",
      args: [
        // ordered: the de-minimis test needs NO prior-year facts — a person
        // whose balance is tiny gets a definitive yes without more questions
        {
          kind: "cmp",
          op: "lt",
          left: ruleRef18("us.federal.balance_due"),
          right: { kind: "param", name: "deMinimis" }
        },
        {
          kind: "cmp",
          op: "ge",
          left: fact23("federalTaxWithheld"),
          right: ruleRef18("us.federal.estimated.required_annual_payment")
        }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/income.js
var J16 = "us.federal";
var fact24 = (factId) => ({ kind: "fact", factId });
var ruleRef19 = (ruleId) => ({ kind: "rule", ruleId });
var FROM10 = { effectiveFrom: "2025-01-01" };
var incomeRules = [
  {
    id: "us.federal.gross_income",
    version: 15,
    // v14 read the raw gain facts; capital gains now enter via schedule_d netting (mixed gain/loss years compute instead of refusing), and rental income can enter pre-depreciation via us.federal.rental.net_income
    jurisdiction: J16,
    title: "Gross income (simplified: wages + interest + capital gains (long/short-term) + qualified/ordinary dividends + SE net profit + K-1 pass-through income \u2212 allowed capital loss \u2212 \xA7 911 exclusion)",
    citation: {
      source: "26 U.S.C. \xA7 61(a); \xA7\xA7 701, 1366(a); \xA7 1211(b); \xA7 1222(5)",
      section: "\xA7 61(a)(1)\u2013(4); \xA7 1366(a); \xA7 1211(b); \xA7 1222(5)",
      url: "https://www.law.cornell.edu/uscode/text/26/61",
      excerpt: "gross income means all income from whatever source derived, including\u2026 compensation for services\u2026 gains derived from dealings in property\u2026 interest\u2026 dividends\u2026 annuities\u2026 pensions [and] distributions (\xA7\xA7 61(a), 72, 85, 408(d)); net short-term capital gain (\xA7 1222(5)) and ordinary (non-qualified) dividends are taxed at ordinary rates, unlike net long-term capital gain/qualified dividends; other fully-includible ordinary income \u2014 gambling winnings, prizes, Alaska Permanent Fund dividends, jury duty pay (Schedule 1 line 8) \u2014 enters through the otherOrdinaryIncome fact; a partner's or S-corporation shareholder's share passes through under \xA7\xA7 701\u2013702 and 1366(a); social security benefits are included per the \xA7 86 formula \u2014 reduced by the capital loss allowed against ordinary income under \xA7 1211(b) (up to $3,000/$1,500 MFS) and by the \xA7 911(a) foreign earned income exclusion (foreign earnings stay IN the wages/SE inputs; the exclusion subtracts here \u2014 the \xA7 911(f) stacking override taxes the remainder at the right rates). [IRA/pension inputs are the TAXABLE amounts \u2014 Form 8606 basis and \xA7 72 exclusion-ratio machinery attested.]"
    },
    ...FROM10,
    output: { type: "money" },
    formula: {
      kind: "sub",
      left: {
        kind: "add",
        args: [
          fact24("wages"),
          fact24("taxableInterest"),
          ruleRef19("us.federal.schedule_d.preferential_lt_gain"),
          fact24("qualifiedDividends"),
          ruleRef19("us.federal.schedule_d.ordinary_st_gain"),
          fact24("ordinaryDividends"),
          fact24("selfEmploymentNetProfit"),
          fact24("k1OrdinaryBusinessIncome"),
          fact24("taxableIraDistributions"),
          fact24("taxablePensionsAndAnnuities"),
          ruleRef19("us.federal.pension.simplified_method_taxable"),
          fact24("unemploymentCompensation"),
          fact24("otherOrdinaryIncome"),
          fact24("alimonyReceivedPre2019"),
          // § 223(f)(2): HSA distributions beyond qualified medical expenses
          {
            kind: "max0",
            arg: {
              kind: "sub",
              left: fact24("hsaDistributions"),
              right: fact24("hsaQualifiedMedicalExpenses")
            }
          },
          fact24("scheduleENetIncome"),
          ruleRef19("us.federal.rental.net_income"),
          ruleRef19("us.federal.taxable_social_security")
        ]
      },
      right: {
        kind: "add",
        args: [
          ruleRef19("us.federal.capital_loss_ordinary_offset"),
          ruleRef19("us.federal.feie.exclusion"),
          fact24("nonpassiveScheduleELoss"),
          fact24("scheduleCNetLoss")
        ]
      }
    }
  },
  {
    id: "us.federal.above_the_line_adjustments",
    version: 5,
    // v4 lacked the Schedule 1 line-24 write-in adjustments (jury pay, reforestation, SUB repayments, § 501(c)(18)(D), discrimination/whistleblower attorney fees) — otherAdjustments, attested
    jurisdiction: J16,
    title: "Above-the-line adjustments before the student-loan deduction (\xBD SE tax + HSA + SEP + SE health insurance + educator + early-withdrawal penalty)",
    citation: {
      source: "26 U.S.C. \xA7 62(a)(1), (a)(2)(D), (a)(6), (a)(9), (a)(17), (a)(19)",
      section: "\xA7 62(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/62",
      excerpt: "adjusted gross income means gross income minus\u2026 the deduction allowed by section 164(f) [half of SE tax]\u2026 the deduction allowed by section 223 [HSA]\u2026 the deductions allowed by section 404 [self-employed retirement plans]\u2026 the deduction allowed by section 162(l) [self-employed health insurance]\u2026 the deductions\u2026 [for] expenses\u2026 of eligible educators (\xA7 62(a)(2)(D) \u2014 $300 cap PER ELIGIBLE EDUCATOR for 2025 (an MFJ couple of two educators deducts up to $600: use educatorExpenses for the taxpayer and spouseEducatorExpenses for the spouse, each capped separately), the 2025 indexed amount)\u2026 [and] penalties forfeited because of premature withdrawal of funds from time savings accounts or deposits (\xA7 62(a)(9), uncapped)."
    },
    ...FROM10,
    output: { type: "money" },
    parameters: {
      educatorCap: { value: "30000", type: "money" }
      // $300 (2025)
    },
    formula: {
      kind: "add",
      args: [
        ruleRef19("us.federal.se_tax_half_deduction"),
        ruleRef19("us.federal.hsa_deduction"),
        ruleRef19("us.federal.sep_deduction"),
        ruleRef19("us.federal.sehi_deduction"),
        {
          kind: "min",
          args: [fact24("educatorExpenses"), { kind: "param", name: "educatorCap" }]
        },
        // § 62(a)(2)(D): the $300 cap is PER eligible educator — a spouse who
        // is also an eligible educator gets their own cap
        {
          kind: "min",
          args: [fact24("spouseEducatorExpenses"), { kind: "param", name: "educatorCap" }]
        },
        fact24("earlyWithdrawalPenaltyPaid"),
        // pre-TCJA § 215/§ 62(a)(10): pre-2019-instrument alimony paid
        fact24("alimonyPaidPre2019"),
        // Schedule 1 lines 24a-z write-in adjustments (attested total)
        fact24("otherAdjustments")
      ]
    }
  },
  {
    id: "us.federal.agi_before_student_loan",
    version: 2,
    // v1 predated the § 219 IRA deduction
    jurisdiction: J16,
    title: "AGI before the student-loan deduction (\xA7 221(b)(2)(C) MAGI base)",
    citation: {
      source: "26 U.S.C. \xA7 221(b)(2)(C); \xA7 62(a)(7)",
      section: "\xA7 221(b)(2)(C)",
      url: "https://www.law.cornell.edu/uscode/text/26/221",
      excerpt: "'modified adjusted gross income' means adjusted gross income determined\u2026 without regard to this section\u2026 [The \xA7 219 IRA deduction IS subtracted here \u2014 \xA7 221 disregards only itself; \xA7 219's own MAGI is computed one step earlier, without regard to both \xA7\xA7 219 and 221.]"
    },
    ...FROM10,
    output: { type: "money" },
    formula: {
      kind: "sub",
      left: ruleRef19("us.federal.gross_income"),
      right: {
        kind: "add",
        args: [
          ruleRef19("us.federal.above_the_line_adjustments"),
          ruleRef19("us.federal.ira_deduction")
        ]
      }
    }
  },
  {
    id: "us.federal.agi",
    version: 2,
    // v1 had no above-the-line adjustments
    jurisdiction: J16,
    title: "Adjusted gross income",
    citation: {
      source: "26 U.S.C. \xA7 62(a)",
      section: "\xA7 62(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/62",
      excerpt: "the term 'adjusted gross income' means, in the case of an individual, gross income minus the following deductions\u2026"
    },
    ...FROM10,
    output: { type: "money" },
    formula: {
      kind: "sub",
      left: ruleRef19("us.federal.agi_before_student_loan"),
      right: ruleRef19("us.federal.student_loan_interest_deduction")
    }
  },
  {
    id: "us.federal.taxable_income_before_qbi",
    version: 2,
    // v1 was non-itemizer only (no Schedule A, no election)
    jurisdiction: J16,
    title: "Taxable income before \xA7 199A (standard-or-itemized election + OBBBA senior/tips/overtime/car-loan deductions)",
    citation: {
      source: "26 U.S.C. \xA7 63(a), (b), as amended by Pub. L. 119-21 (OBBBA)",
      section: "\xA7 63(a), (b)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "A non-itemizer's taxable income is adjusted gross income minus the standard deduction and the deductions provided by \xA7\xA7 151, 170(p), 199A, 224, 225, and 163(h)(4) (\xA7 63(b)); an electing itemizer subtracts itemized deductions in place of the standard deduction and \xA7 170(p) (\xA7 63(a), (d), (e)). Computed here without the \xA7 199A deduction (which \xA7 199A applies against taxable income determined without regard to it)."
    },
    ...FROM10,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: ruleRef19("us.federal.agi"),
        right: {
          kind: "add",
          args: [
            ruleRef19("us.federal.deduction_election"),
            ruleRef19("us.federal.senior_deduction"),
            ruleRef19("us.federal.tips_deduction"),
            ruleRef19("us.federal.overtime_deduction"),
            ruleRef19("us.federal.car_loan_interest_deduction")
          ]
        }
      }
    }
  },
  {
    id: "us.federal.taxable_income",
    version: 5,
    // v4 predated the § 199A deduction
    jurisdiction: J16,
    title: "Taxable income",
    citation: {
      source: "26 U.S.C. \xA7 63(b); \xA7 199A(a)",
      section: "\xA7 63(b)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "\u2026taxable income equals the amount computed under \xA7 63(b) less the deduction allowable under \xA7 199A for qualified business income."
    },
    ...FROM10,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: ruleRef19("us.federal.taxable_income_before_qbi"),
        right: ruleRef19("us.federal.qbi_deduction")
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/international.js
var J17 = "us.federal";
var fact25 = (factId) => ({ kind: "fact", factId });
var money22 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef20 = (ruleId) => ({ kind: "rule", ruleId });
var param13 = (name) => ({ kind: "param", name });
var zero14 = money22("0");
var gt07 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero14 });
function section250Rule(version, from, to, fdRate, cfcRate, yearLabel, excerpt) {
  const total = { kind: "add", args: [fact25("corpFDDEI"), fact25("corpNCTI")] };
  const ti = ruleRef20("us.federal.corp.taxable_income");
  const scaled = (amount) => ({
    kind: "if",
    cond: { kind: "cmp", op: "gt", left: total, right: ti },
    then: { kind: "mulDiv", a: amount, b: ti, c: total, round: "half-up" },
    else: amount
  });
  return {
    id: "us.federal.corp.section250_deduction",
    version,
    jurisdiction: J17,
    title: `\xA7 250 deduction (${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 250${version > 1 ? ", as amended by Pub. L. 119-21 (OBBBA)" : ""}`,
      section: "\xA7 250(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/250",
      excerpt
    },
    effectiveFrom: from,
    ...to ? { effectiveTo: to } : {},
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: { kind: "or", args: [gt07(fact25("corpFDDEI")), gt07(fact25("corpNCTI"))] },
      then: {
        kind: "add",
        args: [
          { kind: "mulRate", base: scaled(fact25("corpFDDEI")), rate: fdRate, round: "half-up" },
          { kind: "mulRate", base: scaled(fact25("corpNCTI")), rate: cfcRate, round: "half-up" }
        ]
      },
      else: zero14
    }
  };
}
function eblRule(version, from, to, singleCents, jointCents, source, yearLabel, note) {
  return {
    id: "us.federal.excess_business_loss_disallowed",
    version,
    jurisdiction: J17,
    title: `Excess business loss disallowed (\xA7 461(l), TY${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 461(l), as made permanent by Pub. L. 119-21 (OBBBA); ${source}`,
      section: "\xA7 461(l)(1), (3)",
      url: "https://www.law.cornell.edu/uscode/text/26/461",
      excerpt: `A noncorporate taxpayer's aggregate business deductions in excess of aggregate business gross income plus the threshold \u2014 ${note} \u2014 are disallowed and treated as an NOL carryforward to the following year (\xA7 461(l)(2)). The OBBBA made the limitation permanent and re-based its indexing: the 2026 thresholds are LOWER than 2025's. [Standalone determination: this corpus's income facts are non-negative, so the disallowed amount is answered as its own target; enter the net aggregate business LOSS as a positive number.]`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    parameters: {
      threshold: { value: singleCents, type: "money" },
      thresholdJoint: { value: jointCents, type: "money" }
    },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact25("aggregateBusinessLoss"),
        right: {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "eq",
            left: fact25("filingStatus"),
            right: { kind: "enum", value: "mfj" }
          },
          then: param13("thresholdJoint"),
          else: param13("threshold")
        }
      }
    }
  };
}
var tiAfter250 = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: ruleRef20("us.federal.corp.taxable_income"),
    right: ruleRef20("us.federal.corp.section250_deduction")
  }
};
var basketLimit = (basketIncome) => ({
  kind: "mulDiv",
  a: ruleRef20("us.federal.corp.income_tax"),
  b: { kind: "min", args: [basketIncome, tiAfter250] },
  c: tiAfter250,
  round: "half-up"
});
function ftcRule(version, from, to, haircut, netOf250, yearLabel, haircutLabel) {
  const cfcBasketIncome = {
    kind: "mulRate",
    base: fact25("corpNCTI"),
    rate: netOf250,
    round: "half-up"
  };
  return {
    id: "us.federal.corp.ftc",
    version,
    jurisdiction: J17,
    title: `Corporate foreign tax credit \u2014 \xA7 904 basket limitation (${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7\xA7 901, 904, 960(d)${version > 1 ? ", as amended by Pub. L. 119-21 (OBBBA)" : ""}`,
      section: "\xA7 904(a), (d); \xA7 960(d)",
      url: "https://www.law.cornell.edu/uscode/text/26/904",
      excerpt: `Foreign taxes credited up to the \xA7 904 limitation per basket \u2014 the US tax multiplied by the basket's foreign-source taxable income over total taxable income (net of \xA7 250). The \xA7 951A-basket deemed-paid credit is limited to ${haircutLabel} of the foreign taxes (\xA7 960(d)), and that basket's income is net of its allocable \xA7 250 deduction; no carryovers exist in the \xA7 951A basket, and the general basket's 1-back/10-forward carryovers are not tracked \u2014 disclosed. [Two baskets modeled (general + CFC); the foreign-branch and passive baskets, \xA7 861 expense allocation, and \xA7 78 fine structure are attested by the inputs.]`
    },
    effectiveFrom: from,
    ...to ? { effectiveTo: to } : {},
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        {
          kind: "if",
          cond: gt07(fact25("corpForeignTaxesGeneral")),
          then: {
            kind: "min",
            args: [
              fact25("corpForeignTaxesGeneral"),
              basketLimit(fact25("corpForeignSourceIncomeGeneral"))
            ]
          },
          else: zero14
        },
        {
          kind: "if",
          cond: gt07(fact25("corpForeignTaxesNCTI")),
          then: {
            kind: "min",
            args: [
              { kind: "mulRate", base: fact25("corpForeignTaxesNCTI"), rate: haircut, round: "half-up" },
              basketLimit(cfcBasketIncome)
            ]
          },
          else: zero14
        }
      ]
    }
  };
}
function beatRule(version, from, to, rate2, yearLabel, rateLabel) {
  return {
    id: "us.federal.corp.beat",
    version,
    jurisdiction: J17,
    title: `Base erosion and anti-abuse tax (\xA7 59A, ${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 59A${version > 1 ? ", as amended by Pub. L. 119-21 (OBBBA)" : ""}`,
      section: "\xA7 59A(a)\u2013(c), (e)",
      url: "https://www.law.cornell.edu/uscode/text/26/59A",
      excerpt: `An applicable taxpayer \u2014 a corporation with 3-year-average gross receipts of at least $500,000,000 whose base erosion percentage is 3 percent or higher (2% for banks/dealers; attested by the test fact) \u2014 pays the excess of ${rateLabel} of modified taxable income over the regular tax liability. ${version > 1 ? "The OBBBA fixed the rate permanently at 10.5% for taxable years beginning after December 31, 2025 (repealing the scheduled 12.5% and the add-back of all credits)" : "10% for taxable years beginning in 2025"}; general business credits (incl. \xA7 41) remain protected from the regular-tax reduction \u2014 modeled by subtracting only the foreign tax credit. [MTI = taxable income (net of \xA7 250) plus base erosion tax benefits \u2014 include the \xA7 59A(c)(1)(B) base-erosion percentage of any NOL deduction in the benefits input; disclosed.]`
    },
    effectiveFrom: from,
    ...to ? { effectiveTo: to } : {},
    output: { type: "money" },
    parameters: {
      receiptsThreshold: { value: "50000000000", type: "money" }
      // $500,000,000
    },
    formula: {
      kind: "if",
      cond: fact25("corpBaseErosionTestMet"),
      then: {
        kind: "if",
        cond: {
          kind: "cmp",
          op: "lt",
          left: fact25("corpAvgGrossReceipts3yr"),
          right: { kind: "param", name: "receiptsThreshold" }
        },
        then: zero14,
        // under $500M: not an applicable taxpayer
        else: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: {
              kind: "mulRate",
              base: {
                kind: "add",
                args: [tiAfter250, fact25("corpBaseErosionTaxBenefits")]
              },
              rate: rate2,
              round: "half-up"
            },
            right: {
              kind: "max0",
              arg: {
                kind: "sub",
                left: ruleRef20("us.federal.corp.income_tax"),
                right: ruleRef20("us.federal.corp.ftc")
              }
            }
          }
        }
      },
      else: zero14
    }
  };
}
var netIncomeTax = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: ruleRef20("us.federal.corp.income_tax"),
    right: ruleRef20("us.federal.corp.ftc")
  }
};
var internationalAndRemainderRules = [
  ftcRule(1, "2025-01-01", "2026-01-01", { num: "80", den: "100" }, { num: "50", den: "100" }, "TY2025: 80% GILTI-basket haircut", "80 percent"),
  ftcRule(2, "2026-01-01", void 0, { num: "90", den: "100" }, { num: "60", den: "100" }, "TY2026+: the OBBBA 90% NCTI-basket allowance", "90 percent"),
  beatRule(1, "2025-01-01", "2026-01-01", { num: "10", den: "100" }, "TY2025", "10 percent"),
  beatRule(2, "2026-01-01", void 0, { num: "105", den: "1000" }, "TY2026+, permanent", "10.5 percent"),
  section250Rule(1, "2025-01-01", "2026-01-01", { num: "375", den: "1000" }, { num: "50", den: "100" }, "TY2025: 37.5% of FDII + 50% of GILTI", "For taxable years beginning in 2025 (prior law): a deduction of 37.5 percent of foreign-derived intangible income plus 50 percent of the \xA7 951A GILTI inclusion (and the \xA7 78 gross-up attributable to it \u2014 include it in the input amount), limited by \xA7 250(a)(2): when the sum exceeds taxable income (computed without this section), each amount is reduced pro-rata. [FDII/GILTI amounts (QBAI-based under prior law) are taken as computed inputs; foreign tax credits and \xA7 59A BEAT are not modeled \u2014 disclosed.]"),
  section250Rule(2, "2026-01-01", void 0, { num: "3334", den: "10000" }, { num: "40", den: "100" }, "TY2026+: 33.34% of FDDEI + 40% of NCTI (OBBBA)", "For taxable years beginning after December 31, 2025 (OBBBA): FDII becomes FOREIGN-DERIVED DEDUCTION ELIGIBLE INCOME and GILTI becomes NET CFC TESTED INCOME, QBAI (the 10% deemed tangible return) is ABOLISHED, and the deduction is 33.34 percent of FDDEI plus 40 percent of NCTI (effective rates 14% and 12.6\u201314%), limited by \xA7 250(a)(2) pro-rata scaling against taxable income computed without this section. [Include the \xA7 78 gross-up in the NCTI input; the 90% NCTI foreign-tax-credit allowance and \xA7 59A BEAT (10.5% from 2026) are not modeled \u2014 disclosed.]"),
  eblRule(1, "2025-01-01", "2026-01-01", "31300000", "62600000", "2025 Instructions for Form 461", "2025", "$313,000 ($626,000 for joint returns) for 2025"),
  eblRule(2, "2026-01-01", "2027-01-01", "25600000", "51200000", "Rev. Proc. 2025-32 \xA7 3.31", "2026", '"$256,000 ($512,000 for joint returns)" for 2026 (Rev. Proc. 2025-32 \xA7 3.31, verbatim \u2014 a DECREASE from 2025)'),
  {
    id: "us.federal.corp.gbc_allowed",
    version: 2,
    // v1 predated the foreign tax credit in "net income tax"
    jurisdiction: J17,
    title: "General business credit allowed (\xA7 38(c) limitation)",
    citation: {
      source: "26 U.S.C. \xA7 38(c); \xA7 38(c)(6)(E)",
      section: "\xA7 38(c)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/38",
      excerpt: "The credit allowed\u2026 shall not exceed the excess of the taxpayer's net income tax over the greater of the tentative minimum tax or 25 percent of so much of the net regular tax liability as exceeds $25,000. For a corporation that is not a CAMT applicable corporation, the tentative minimum tax is zero (\xA7 38(c)(6)(E)) \u2014 applicable corporations already refuse at this corpus's $1B-AFSI guard. [Input the aggregate current-year \xA7 38(b) credits \u2014 e.g. the \xA7 41 research credit target's result; the \xA7 39 1-back/20-forward carryover is exposed as its own target, not tracked.]"
    },
    effectiveFrom: "2025-01-01",
    // statutory, unindexed: open-ended
    output: { type: "money" },
    parameters: {
      floor: { value: "2500000", type: "money" }
      // $25,000
    },
    formula: {
      kind: "min",
      args: [
        fact25("generalBusinessCredits"),
        {
          // "net income tax" = tax after the foreign tax credit (§ 38(c)(1))
          kind: "max0",
          arg: {
            kind: "sub",
            left: netIncomeTax,
            right: {
              kind: "mulRate",
              base: {
                kind: "max0",
                arg: { kind: "sub", left: netIncomeTax, right: param13("floor") }
              },
              rate: { num: "25", den: "100" },
              round: "half-up"
            }
          }
        }
      ]
    }
  },
  {
    id: "us.federal.corp.income_tax_after_credits",
    version: 2,
    // v1 predated the foreign tax credit
    jurisdiction: J17,
    title: "Corporate income tax after the general business credit",
    citation: {
      source: "26 U.S.C. \xA7 38(a); \xA7 11",
      section: "\xA7 38(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/38",
      excerpt: "The \xA7 901 foreign tax credit and the \xA7 38 general business credit (limited under \xA7 38(c)) are allowed against the \xA7 11 tax \u2014 FTC first, then the GBC against the net income tax."
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: netIncomeTax,
        right: ruleRef20("us.federal.corp.gbc_allowed")
      }
    }
  },
  {
    id: "us.federal.corp.gbc_carryforward",
    version: 1,
    jurisdiction: J17,
    title: "General business credit carryforward generated (\xA7 39)",
    citation: {
      source: "26 U.S.C. \xA7 39(a)",
      section: "\xA7 39(a)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/39",
      excerpt: "\u2026the excess credit is carried back 1 year and forward 20. [The 1-year carryback and multi-year tracking are not modeled \u2014 this target reports the excess generated this year.]"
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact25("generalBusinessCredits"),
        right: ruleRef20("us.federal.corp.gbc_allowed")
      }
    }
  },
  {
    id: "us.federal.corp.section245a_drd",
    version: 1,
    jurisdiction: J17,
    title: "\xA7 245A participation-exemption DRD (100% of eligible foreign-sub dividends)",
    citation: {
      source: "26 U.S.C. \xA7 245A; \xA7 246(c)(5)",
      section: "\xA7 245A(a), (d), (e); \xA7 246(c)(5)",
      url: "https://www.law.cornell.edu/uscode/text/26/245A",
      excerpt: "In the case of any dividend received from a specified 10-percent owned foreign corporation by a domestic corporation which is a United States shareholder with respect to such foreign corporation, there shall be allowed as a deduction an amount equal to the foreign-source portion of such dividend. [Attested by the input: NOT a \xA7 245A(e) hybrid dividend, and the \xA7 246(c)(5) holding period met ('365 days' during the '731-day period' around the ex-dividend date). No foreign tax credit or deduction is allowed for taxes attributable to the deducted portion (\xA7 245A(d)) \u2014 keep them out of the FTC inputs. Never subject to the \xA7 246(b) taxable-income limit. The dividend must be included in corpGrossIncome.]"
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: fact25("corpSection245ADividends")
  },
  {
    id: "us.federal.corp.charitable_carryover",
    version: 2,
    // v1 predated § 179 in the § 170(b)(2) base
    jurisdiction: J17,
    title: "Corporate charitable contribution carryover generated (\xA7 170(d)(2))",
    citation: {
      source: "26 U.S.C. \xA7 170(d)(2), as amended by Pub. L. 119-21 (OBBBA)",
      section: "\xA7 170(d)(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/170",
      excerpt: "Contributions in excess of the 10% ceiling carry to the 5 succeeding taxable years \u2014 and under the OBBBA, when the ceiling binds, the 1%-floored amount carries over as well (contributions lost to the floor alone do NOT carry). Encoded as contributions minus the allowed deduction whenever the ceiling binds, zero otherwise. [The 5-year expiry and re-application of the floor to carried amounts are not tracked \u2014 this target reports the carryover generated this year.]"
    },
    effectiveFrom: "2025-01-01",
    // references the year-versioned deduction rule
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "gt",
        left: fact25("corpCharitableContributions"),
        right: {
          // the ceiling from the SAME § 170(b)(2) base the deduction uses
          kind: "mulRate",
          base: preSpecialBase,
          rate: { num: "10", den: "100" },
          round: "half-up"
        }
      },
      // ceiling binds: everything not deducted this year carries
      then: {
        kind: "max0",
        arg: {
          kind: "sub",
          left: fact25("corpCharitableContributions"),
          right: ruleRef20("us.federal.corp.charitable_deduction")
        }
      },
      else: zero14
    }
  },
  {
    id: "us.federal.corp.interest_carryforward",
    version: 1,
    jurisdiction: J17,
    title: "Disallowed business interest carried forward (\xA7 163(j)(2))",
    citation: {
      source: "26 U.S.C. \xA7 163(j)(2)",
      section: "\xA7 163(j)(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/163",
      excerpt: "The amount of any business interest not allowed as a deduction for any taxable year shall be treated as business interest paid or accrued in the succeeding taxable year \u2014 carried forward indefinitely. [This target reports the carryforward generated this year; prior-year carryforwards belong in the interest-expense input.]"
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact25("corpBusinessInterestExpense"),
        right: ruleRef20("us.federal.corp.interest_deduction")
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/investment-taxes.js
var fact26 = (factId) => ({ kind: "fact", factId });
var money23 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef21 = (ruleId) => ({ kind: "rule", ruleId });
var FROM11 = { effectiveFrom: "2025-01-01" };
var threshold = {
  kind: "match",
  on: fact26("filingStatus"),
  cases: [
    { when: "mfj", value: money23("25000000") },
    // $250,000
    { when: "qss", value: money23("25000000") },
    // $250,000 (§ 1411(b): surviving spouse)
    { when: "mfs", value: money23("12500000") },
    // $125,000
    { when: "single", value: money23("20000000") },
    // $200,000
    { when: "hoh", value: money23("20000000") }
    // $200,000
  ]
};
var amThreshold = {
  kind: "match",
  on: fact26("filingStatus"),
  cases: [
    { when: "mfj", value: money23("25000000") },
    { when: "qss", value: money23("20000000") },
    { when: "mfs", value: money23("12500000") },
    { when: "single", value: money23("20000000") },
    { when: "hoh", value: money23("20000000") }
  ]
};
var investmentTaxRules = [
  {
    id: "us.federal.niit",
    version: 6,
    // v5 read the raw gain facts; § 1411 net investment income now takes the schedule_d netted results (losses reduce gains before NIIT)
    jurisdiction: "us.federal",
    title: "Net investment income tax (investment income = interest + capital gains (LT/ST) + dividends (qualified/ordinary) \u2212 allocable deductions; MAGI \u2248 AGI)",
    citation: {
      source: "26 U.S.C. \xA7 1411",
      section: "\xA7 1411(a)(1), (b), (c)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/1411",
      excerpt: "\u2026a tax equal to 3.8 percent of the lesser of (A) net investment income\u2026 or (B) the excess (if any) of modified adjusted gross income\u2026 over the threshold amount [$250,000 joint return or surviving spouse; $125,000 married filing separately; $200,000 otherwise \u2014 not indexed]. Net investment income is the excess of the gross investment income items 'over (B) the deductions allowed by this subtitle which are properly allocable to such gross income or net gain' (\xA7 1411(c)(1), verbatim) \u2014 investment interest expense, allocable state tax, and other Form 8960 line 9\u201310 amounts enter through niitInvestmentExpenses."
    },
    ...FROM11,
    output: { type: "money" },
    formula: (() => {
      const tax = {
        kind: "mulRate",
        base: {
          kind: "min",
          args: [
            {
              kind: "max0",
              arg: {
                kind: "sub",
                left: {
                  kind: "add",
                  args: [
                    fact26("taxableInterest"),
                    ruleRef21("us.federal.schedule_d.preferential_lt_gain"),
                    fact26("qualifiedDividends"),
                    ruleRef21("us.federal.schedule_d.ordinary_st_gain"),
                    fact26("ordinaryDividends")
                  ]
                },
                right: fact26("niitInvestmentExpenses")
              }
            },
            {
              kind: "max0",
              arg: { kind: "sub", left: ruleRef21("us.federal.agi"), right: threshold }
            }
          ]
        },
        rate: { num: "38", den: "1000" },
        round: "half-up"
      };
      return {
        kind: "if",
        cond: fact26("useFormulaMethod"),
        then: tax,
        else: { kind: "roundToDollar", value: tax, mode: "half-up" }
        // Form 8960 line 17, whole dollars
      };
    })()
  },
  {
    id: "us.federal.additional_medicare_tax",
    version: 3,
    // v2 rounded once combined; the printed Form 8959 rounds Parts I and II per line
    jurisdiction: "us.federal",
    title: "Additional Medicare Tax (0.9%; Form 8959 uses medicareWages box 5 when given, else box-1 wages)",
    citation: {
      source: "26 U.S.C. \xA7 3101(b)(2); \xA7 1401(b)(2); Form 8959",
      section: "\xA7 3101(b)(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/3101",
      excerpt: "\u2026an additional tax equal to 0.9 percent of wages\u2026 in excess of $250,000 in the case of a joint return, $125,000 in the case of a married taxpayer filing separately, [and] $200,000 in any other case [not indexed; SE earnings coordinate via \xA7 1401(b)(2)]. Form 8959's wage base is W-2 BOX 5 Medicare wages \u2014 approximated by box 1 when medicareWages is not supplied (box 5 is often higher: pre-tax 401(k) deferrals reduce box 1 only). Reconciled on Form 8959 as part of total tax; employer-withheld Additional Medicare Tax (box 6 over 1.45% of box 5) belongs in withholding via Form 8959 Part IV."
    },
    ...FROM11,
    output: { type: "money" },
    formula: (() => {
      const mw = {
        kind: "if",
        cond: { kind: "cmp", op: "gt", left: fact26("medicareWages"), right: money23("0") },
        then: fact26("medicareWages"),
        else: fact26("wages")
      };
      const pct09 = (base) => ({
        kind: "mulRate",
        base,
        rate: { num: "9", den: "1000" },
        round: "half-up"
      });
      const wagesPart = pct09({ kind: "max0", arg: { kind: "sub", left: mw, right: amThreshold } });
      const sePart = pct09({
        kind: "max0",
        arg: {
          kind: "sub",
          left: ruleRef21("us.federal.se_net_earnings"),
          right: { kind: "max0", arg: { kind: "sub", left: amThreshold, right: mw } }
        }
      });
      const combined = pct09({
        kind: "max0",
        arg: {
          kind: "sub",
          left: { kind: "add", args: [mw, ruleRef21("us.federal.se_net_earnings")] },
          right: amThreshold
        }
      });
      return {
        kind: "if",
        cond: fact26("useFormulaMethod"),
        then: combined,
        else: {
          // printed Form 8959: Parts I and II each entered in whole dollars
          kind: "add",
          args: [
            { kind: "roundToDollar", value: wagesPart, mode: "half-up" },
            { kind: "roundToDollar", value: sePart, mode: "half-up" }
          ]
        }
      };
    })()
  },
  {
    id: "us.federal.other_taxes",
    version: 4,
    // v3 predated the § 223(f) HSA distribution taxes
    jurisdiction: "us.federal",
    title: "Other taxes (SE tax + Additional Medicare + NIIT + \xA7 72(t) + excess APTC + Schedule H)",
    citation: {
      source: "26 U.S.C. \xA7\xA7 1401, 1411, 3101(b)(2); Form 1040 Schedule 2",
      section: "Schedule 2, Part II",
      url: "https://www.irs.gov/forms-pubs/about-schedule-2-form-1040",
      excerpt: "Self-employment tax, Additional Medicare Tax (Form 8959), net investment income tax (Form 8960), and the \xA7 72(t) 10-percent additional tax on early distributions (exceptions \u2014 age 59\xBD, disability, SEPP, \xA7 72(t)(2) list \u2014 attested by entering only the penalized amount) , the \xA7 36B(f)(2) excess advance premium tax credit repayment (Schedule 2 line 2), household employment taxes (Schedule H, Schedule 2 line 9), and the \xA7 223(f)(4) 20-percent additional tax on nonqualified HSA distributions (Form 8889 Part II; waived at 65+/death/disability) are added to income tax in computing total tax."
    },
    ...FROM11,
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        ruleRef21("us.federal.se_tax"),
        ruleRef21("us.federal.additional_medicare_tax"),
        ruleRef21("us.federal.niit"),
        // § 223(f)(4): 20% additional tax on nonqualified HSA distributions
        // (waived after 65 / death / disability via hsaDistributionPenaltyExempt)
        {
          kind: "if",
          cond: fact26("hsaDistributionPenaltyExempt"),
          then: { kind: "money", cents: "0" },
          else: {
            kind: "mulRate",
            base: {
              kind: "max0",
              arg: {
                kind: "sub",
                left: fact26("hsaDistributions"),
                right: fact26("hsaQualifiedMedicalExpenses")
              }
            },
            rate: { num: "20", den: "100" },
            round: "half-up"
          }
        },
        {
          kind: "mulRate",
          base: fact26("earlyDistributionSubjectToPenalty"),
          rate: { num: "10", den: "100" },
          round: "half-up"
        },
        ruleRef21("us.federal.ptc.excess_aptc_repayment"),
        ruleRef21("us.federal.household_employment_taxes")
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/ira.js
var J18 = "us.federal";
var fact27 = (factId) => ({ kind: "fact", factId });
var money24 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef22 = (ruleId) => ({ kind: "rule", ruleId });
var param14 = (name) => ({ kind: "param", name });
var zero15 = money24("0");
var magi = {
  kind: "sub",
  left: ruleRef22("us.federal.gross_income"),
  right: ruleRef22("us.federal.above_the_line_adjustments")
};
var compensation = {
  kind: "add",
  args: [
    fact27("wages"),
    {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact27("selfEmploymentNetProfit"),
        right: ruleRef22("us.federal.se_tax_half_deduction")
      }
    }
  ]
};
function iraRule(version, effectiveFrom, effectiveTo, yearLabel, p, source, amountsNote) {
  const limit = {
    kind: "add",
    args: [
      param14("base"),
      { kind: "if", cond: fact27("isAge50OrOlder"), then: param14("catchUp"), else: zero15 }
    ]
  };
  const phasedLimit = (threshold2, range) => {
    const excess = {
      kind: "max0",
      arg: { kind: "sub", left: magi, right: threshold2 }
    };
    const reduction = {
      // floor to the next lowest $10: floor-÷$10, then × $10
      kind: "mulInt",
      base: money24("1000"),
      // $10
      count: {
        kind: "stepUnits",
        value: { kind: "mulDiv", a: limit, b: excess, c: range, round: "floor" },
        unitCents: "1000",
        mode: "floor"
      }
    };
    return {
      kind: "if",
      cond: { kind: "cmp", op: "ge", left: excess, right: range },
      then: zero15,
      // fully phased: the $200 floor does not apply
      else: {
        kind: "max",
        args: [money24("20000"), { kind: "sub", left: limit, right: reduction }]
        // $200 floor
      }
    };
  };
  const rangeJoint = money24("2000000");
  const range10 = money24("1000000");
  const coveredLimit = {
    kind: "match",
    on: fact27("filingStatus"),
    cases: [
      { when: "single", value: phasedLimit(param14("thresholdSingle"), range10) },
      { when: "hoh", value: phasedLimit(param14("thresholdSingle"), range10) },
      { when: "mfj", value: phasedLimit(param14("thresholdJoint"), rangeJoint) },
      // Pub 590-A groups QSS with MFJ for this range (the § 219(g)(3)(B)(i)
      // text says "joint return"; IRS guidance controls filing practice —
      // disclosed in the excerpt)
      { when: "qss", value: phasedLimit(param14("thresholdJoint"), rangeJoint) },
      {
        when: "mfs",
        value: {
          kind: "if",
          // § 219(g)(4): lived apart at all times -> NOT treated as married;
          // the single phase-out range applies instead of $0-$10,000
          cond: fact27("mfsLivedApartAllYear"),
          then: phasedLimit(param14("thresholdSingle"), range10),
          else: phasedLimit(zero15, range10)
          // $0–$10,000
        }
      }
    ]
  };
  const notCoveredLimit = {
    kind: "match",
    on: fact27("filingStatus"),
    cases: [
      { when: "single", value: limit },
      { when: "hoh", value: limit },
      { when: "qss", value: limit },
      {
        when: "mfj",
        value: {
          kind: "if",
          cond: fact27("spouseIsActivePlanParticipant"),
          then: phasedLimit(param14("thresholdSpousal"), range10),
          // § 219(g)(7)
          else: limit
        }
      },
      {
        when: "mfs",
        value: {
          kind: "if",
          // § 219(g)(4): lived apart all year -> not married, no spousal attribution
          cond: fact27("mfsLivedApartAllYear"),
          then: limit,
          else: {
            kind: "if",
            cond: fact27("spouseIsActivePlanParticipant"),
            then: phasedLimit(zero15, range10),
            // § 219(g)(3)(B)(iii): $0
            else: limit
          }
        }
      }
    ]
  };
  return {
    id: "us.federal.ira_deduction",
    version,
    jurisdiction: J18,
    title: `Traditional IRA deduction (TY${yearLabel})`,
    citation: {
      source: `26 U.S.C. \xA7 219; ${source}`,
      section: "\xA7 219(b), (g)",
      url: "https://www.law.cornell.edu/uscode/text/26/219",
      excerpt: `${amountsNote} Phase-out per \xA7 219(g)(2): the limit is reduced by the amount bearing the same ratio to it as excess MAGI bears to $10,000 ($20,000 joint), the reduction "rounded to the next lowest $10"; the limit is not reduced "below $200 unless\u2026 reduced to zero." MAGI is AGI without regard to \xA7\xA7 221 and 219 (\xA7 219(g)(3)(A)). Deduction capped at includible compensation (\xA7 219(b)(1)(B)) \u2014 compensation \u2248 wages + SE earnings net of \xBD SE tax, and the \xA7 219(c) spousal-compensation rule is assumed satisfied on joint returns; disclosed. Pub 590-A groups qualifying surviving spouses with joint filers for the covered range (the statutory text says "joint return" \u2014 IRS guidance controls filing practice; disclosed). Not modeled, disclosed: the \xA7 219(g)(4) living-apart-all-year MFS exception (conservative), nondeductible-contribution basis (Form 8606), Roth IRAs, and the 6% excess-contribution excise.`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      base: { value: p.base, type: "money" },
      catchUp: { value: p.catchUp, type: "money" },
      thresholdSingle: { value: p.thresholdSingle, type: "money" },
      thresholdJoint: { value: p.thresholdJoint, type: "money" },
      thresholdSpousal: { value: p.thresholdSpousal, type: "money" }
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "gt", left: fact27("iraContribution"), right: zero15 },
      then: {
        kind: "min",
        args: [
          fact27("iraContribution"),
          {
            kind: "if",
            cond: fact27("isActivePlanParticipant"),
            then: coveredLimit,
            else: notCoveredLimit
          },
          compensation
        ]
      },
      else: zero15
    }
  };
}
var iraRules = [
  iraRule(2, "2025-01-01", "2026-01-01", "2025", {
    base: "700000",
    catchUp: "100000",
    // $7,000 + $1,000
    thresholdSingle: "7900000",
    // $79,000
    thresholdJoint: "12600000",
    // $126,000
    thresholdSpousal: "23600000"
    // $236,000
  }, "Notice 2024-80; Pub 590-A (2025)", "For 2025 the deductible amount is $7,000 ($8,000 if age 50+); active-participant phase-out ranges $79,000\u2013$89,000 (single/HoH), $126,000\u2013$146,000 (joint/QSS), $0\u2013$10,000 (MFS \u2014 but \xA7 219(g)(4): an MFS filer who lived apart from the spouse at ALL times during the year is not treated as married, so the single range applies and no spousal attribution occurs; set mfsLivedApartAllYear); $236,000\u2013$246,000 where only the spouse participates."),
  iraRule(3, "2026-01-01", "2027-01-01", "2026", {
    base: "750000",
    catchUp: "110000",
    // $7,500 + $1,100 (Notice 2025-67)
    thresholdSingle: "8100000",
    // $81,000
    thresholdJoint: "12900000",
    // $129,000
    thresholdSpousal: "24200000"
    // $242,000
  }, "Notice 2025-67", "For 2026 the deductible amount rises to $7,500 ($8,600 if age 50+ \u2014 the $1,100 catch-up is the first indexed increase under \xA7 219(b)(5)(C)); phase-out ranges $81,000\u2013$91,000 (single/HoH), $129,000\u2013$149,000 (joint/QSS), $0\u2013$10,000 (MFS \u2014 but \xA7 219(g)(4): an MFS filer who lived apart from the spouse at ALL times during the year is not treated as married, so the single range applies and no spousal attribution occurs; set mfsLivedApartAllYear); $242,000\u2013$252,000 where only the spouse participates.")
];

// ../corpus-us-federal/dist/rules/ira-8606.js
var J19 = "us.federal";
var fact28 = (factId) => ({ kind: "fact", factId });
var money25 = (cents2) => ({ kind: "money", cents: cents2 });
var zero16 = money25("0");
var gt08 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero16 });
var line3 = {
  kind: "add",
  args: [fact28("ira8606NondeductibleContributions"), fact28("ira8606Basis")]
};
var line9 = {
  kind: "add",
  args: [
    fact28("ira8606YearEndValue"),
    fact28("ira8606Distributions"),
    fact28("ira8606Conversions")
  ]
};
function nontaxablePortion(amount) {
  return {
    kind: "if",
    cond: gt08(amount),
    then: {
      kind: "min",
      args: [
        amount,
        {
          kind: "roundToDollar",
          value: { kind: "mulDiv", a: amount, b: line3, c: line9, round: "half-up" },
          mode: "half-up"
        }
      ]
    },
    else: zero16
  };
}
var line11 = nontaxablePortion(fact28("ira8606Conversions"));
var line12 = nontaxablePortion(fact28("ira8606Distributions"));
var CITATION_EXCERPT = `Distributions are included in gross income 'in the manner provided under section 72'; for that purpose 'all individual retirement plans shall be treated as 1 contract, all distributions during any taxable year shall be treated as 1 distribution, and the value of the contract, income on the contract, and investment in the contract shall be computed as of the close of the calendar year in which the taxable year begins' (\xA7 408(d)(1)\u2013(2), verbatim). The 2025 Form 8606 mechanizes this: line 6 'Enter the value of all your traditional IRAs as of December 31, 2025, plus any outstanding rollovers'; line 7 'Enter your distributions from traditional IRAs in 2025. Do not include rollovers\u2026. Also, do not include qualified charitable distributions' [exclusions attested by the input]; line 8 'Enter the net amount you converted from traditional IRAs to Roth IRAs in 2025'; line 10 'Divide line 5 by line 9. Enter the result as a decimal rounded to at least 3 places. If the result is 1.000 or more, enter "1.000"' [full-precision integer arithmetic satisfies 'at least 3 places'; the cap is the min against the amount]; line 11 'Multiply line 8 by line 10. This is the nontaxable portion of the amount you converted to Roth IRAs'; line 12 'Multiply line 7 by line 10. This is the nontaxable portion of your distributions that you did not convert to a Roth IRA'; line 18 'Taxable amount. Subtract line 17 from line 16\u2026 include this amount on 2025 Form 1040\u2026 line 4b.' [PER INDIVIDUAL \u2014 spouses file separate Forms 8606; enter the result into taxableIraDistributions yourself (a STANDALONE determination, like the home-office safe harbor). SEP/SIMPLE values belong in line 6. Line 4 (contributions made January\u2013April for the prior year), line 15b qualified disaster distributions, inherited-IRA basis, and qualified charitable distributions are not modeled \u2014 disclosed. PRIORITY WHEN SOURCES CONFLICT: if the interview or source documents already STATE this year's taxable amount, that documented figure controls the FACTS \u2014 transcribe it into taxableIraDistributions and disclose the pro-rata dissent; run this target only when basis exists and no taxable amount is documented.]`;
var ira8606Rules = [
  {
    id: "us.federal.ira8606.taxable_amount",
    version: 1,
    jurisdiction: J19,
    title: "Taxable IRA distributions and Roth conversions with basis \u2014 Form 8606 Part I pro-rata (\xA7 408(d))",
    citation: {
      source: "26 U.S.C. \xA7 408(d)(1)\u2013(2); 2025 Form 8606 Part I",
      section: "\xA7 408(d)(2); Form 8606 lines 6\u201318",
      url: "https://www.law.cornell.edu/uscode/text/26/408",
      excerpt: CITATION_EXCERPT
    },
    effectiveFrom: "2025-01-01",
    // statutory mechanics, un-indexed
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        // line 15a/15c: taxable non-converted distributions (15b disaster
        // repayments not modeled)
        { kind: "max0", arg: { kind: "sub", left: fact28("ira8606Distributions"), right: line12 } },
        // line 18: taxable conversions = line 16 − line 17
        { kind: "max0", arg: { kind: "sub", left: fact28("ira8606Conversions"), right: line11 } }
      ]
    }
  },
  {
    id: "us.federal.ira8606.basis_carryforward",
    version: 1,
    jurisdiction: J19,
    title: "Traditional IRA basis carried to next year \u2014 Form 8606 line 14",
    citation: {
      source: "26 U.S.C. \xA7 408(d)(2); 2025 Form 8606 Part I",
      section: "Form 8606 lines 13\u201314",
      url: "https://www.law.cornell.edu/uscode/text/26/408",
      excerpt: "Line 13: 'Add lines 11 and 12. This is the nontaxable portion of all your distributions'; line 14: 'Subtract line 13 from line 3. This is your total basis in traditional IRAs for 2025 and earlier years' (2025 Form 8606, verbatim). [This target reports next year's line 2 for the SAME individual; prior-year basis is an input, not tracked across years.]"
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: line3,
        right: { kind: "add", args: [line11, line12] }
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/itemized.js
var J20 = "us.federal";
var fact29 = (factId) => ({ kind: "fact", factId });
var money26 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef23 = (ruleId) => ({ kind: "rule", ruleId });
var param15 = (name) => ({ kind: "param", name });
var zero17 = money26("0");
var isStatus8 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact29("filingStatus"),
  right: { kind: "enum", value: status }
});
var mfsHalf = (full, half) => ({
  kind: "if",
  cond: isStatus8("mfs"),
  then: param15(half),
  else: param15(full)
});
var agi = ruleRef23("us.federal.agi");
function saltRule(version, effectiveFrom, effectiveTo, amounts, label) {
  return {
    id: "us.federal.salt_deduction",
    version,
    jurisdiction: J20,
    title: `State and local tax deduction with the OBBBA cap (TY${label.year}: ${label.cap}, 30% phase-down over ${label.threshold} MAGI, $10,000 floor)`,
    citation: {
      source: "26 U.S.C. \xA7 164(b)(6)\u2013(7), as amended by Pub. L. 119-21 (OBBBA) \xA7 70120",
      section: "\xA7 164(b)(6), (b)(7)",
      url: "https://www.law.cornell.edu/uscode/text/26/164",
      excerpt: `State and local income (or sales) and property taxes are capped at the applicable limitation amount \u2014 ${label.cap} for taxable years beginning in ${label.year} (halved for a married individual filing separately) \u2014 "reduced by 30 percent of the excess (if any) of the taxpayer's modified adjusted gross income over the threshold amount" of ${label.threshold} (halved MFS); the reduction "shall not result in the applicable limitation amount being less than $10,000" ($5,000 MFS). Both the cap and the threshold escalate 1% per year through 2029, then revert to a flat $10,000 in 2030. [MAGI \u2248 AGI: the \xA7 911/931/933 foreign-exclusion add-backs are not modeled \u2014 disclosed.]`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      cap: { value: amounts.cap, type: "money" },
      capMfs: { value: amounts.capMfs, type: "money" },
      threshold: { value: amounts.threshold, type: "money" },
      thresholdMfs: { value: amounts.thresholdMfs, type: "money" },
      floor: { value: "1000000", type: "money" },
      // $10,000
      floorMfs: { value: "500000", type: "money" }
      // $5,000
    },
    formula: {
      kind: "min",
      args: [
        fact29("stateAndLocalTaxesPaid"),
        {
          // applicable limitation amount = max(floor, cap − 30% × excess MAGI)
          kind: "max",
          args: [
            mfsHalf("floor", "floorMfs"),
            {
              kind: "sub",
              left: mfsHalf("cap", "capMfs"),
              right: {
                kind: "mulRate",
                base: {
                  kind: "max0",
                  arg: {
                    kind: "sub",
                    left: agi,
                    right: mfsHalf("threshold", "thresholdMfs")
                  }
                },
                rate: { num: "30", den: "100" },
                round: "half-up"
              }
            }
          ]
        }
      ]
    }
  };
}
var charityCashLimited = {
  kind: "min",
  args: [
    fact29("charitableCashContributions"),
    { kind: "mulRate", base: agi, rate: { num: "60", den: "100" }, round: "half-up" }
  ]
};
var CHARITY_CITE = {
  source: "26 U.S.C. \xA7 170(b)(1)(G), (b)(1)(I), as amended by Pub. L. 119-21 (OBBBA) \xA7\xA7 70424\u201370425",
  section: "\xA7 170(b)(1)(G), (b)(1)(I)",
  url: "https://www.law.cornell.edu/uscode/text/26/170"
};
var itemizedRules = [
  saltRule(1, "2025-01-01", "2026-01-01", { cap: "4000000", capMfs: "2000000", threshold: "50000000", thresholdMfs: "25000000" }, { year: "2025", cap: "$40,000", threshold: "$500,000" }),
  saltRule(2, "2026-01-01", "2027-01-01", { cap: "4040000", capMfs: "2020000", threshold: "50500000", thresholdMfs: "25250000" }, { year: "2026", cap: "$40,400", threshold: "$505,000" }),
  {
    id: "us.federal.mortgage_interest_deduction",
    version: 1,
    jurisdiction: J20,
    title: "Home mortgage interest on acquisition debt up to $750,000 ($375,000 MFS)",
    citation: {
      source: "26 U.S.C. \xA7 163(h)(3), as amended by Pub. L. 119-21 (OBBBA)",
      section: "\xA7 163(h)(3)(B)(ii), (h)(3)(F)",
      url: "https://www.law.cornell.edu/uscode/text/26/163",
      excerpt: "Qualified residence interest on acquisition indebtedness, applying the $750,000 ($375,000 in the case of a married individual filing a separate return) limitation \u2014 made permanent by the OBBBA (the before-January-1-2026 sunset struck from \xA7 163(h)(3)(F)). When the average acquisition-debt balance exceeds the limit, deductible interest is prorated by limit/balance (the Publication 936 method). [Assumed and disclosed: debt incurred after December 15, 2017 and secured by a qualified residence (the grandfathered $1,000,000 pre-2018 limit is NOT modeled \u2014 conservative); home-equity interest is not deductible (\xA7 163(h)(3)(F)(i)(I)); mortgage insurance premiums treated as qualified residence interest from 2026 (OBBBA) are NOT modeled \u2014 conservative.]"
    },
    effectiveFrom: "2025-01-01",
    // limit permanent under OBBBA: open-ended
    output: { type: "money" },
    parameters: {
      limit: { value: "75000000", type: "money" },
      // $750,000
      limitMfs: { value: "37500000", type: "money" }
      // $375,000
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "gt", left: fact29("mortgageInterestPaid"), right: zero17 },
      then: {
        kind: "if",
        cond: {
          kind: "cmp",
          op: "le",
          left: fact29("mortgageAverageBalance"),
          right: mfsHalf("limit", "limitMfs")
        },
        then: fact29("mortgageInterestPaid"),
        else: {
          kind: "mulDiv",
          a: fact29("mortgageInterestPaid"),
          b: mfsHalf("limit", "limitMfs"),
          c: fact29("mortgageAverageBalance"),
          round: "half-up"
        }
      },
      else: zero17
    }
  },
  {
    id: "us.federal.charitable_deduction_itemizer",
    version: 1,
    jurisdiction: J20,
    title: "Charitable deduction for itemizers (TY2025: cash, 60% of AGI limit)",
    citation: {
      ...CHARITY_CITE,
      excerpt: "Cash contributions to public charities are deductible up to 60 percent of the taxpayer's contribution base (\u2248 AGI) \u2014 the 60% limit is permanent (OBBBA \xA7 70424 struck the pre-2026 sunset from \xA7 170(b)(1)(G)). The \xA7 170(b)(1)(I) 0.5% floor does not apply to taxable years beginning before 2026. [Modeled and disclosed: CASH gifts to 60%-limit organizations only; excess-contribution carryovers (\xA7 170(d)) are not modeled \u2014 conservative for the current year.]"
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    formula: charityCashLimited
  },
  {
    id: "us.federal.charitable_deduction_itemizer",
    version: 2,
    jurisdiction: J20,
    title: "Charitable deduction for itemizers (TY2026+: 60% of AGI limit, then the OBBBA 0.5% floor)",
    citation: {
      ...CHARITY_CITE,
      excerpt: `For taxable years beginning after December 31, 2025 (OBBBA \xA7 70425), \xA7 170(b)(1)(I) provides: "Any charitable contribution otherwise allowable (without regard to this subparagraph) as a deduction under this section shall be allowed only to the extent that the aggregate of such contributions exceeds 0.5 percent of the taxpayer's contribution base for the taxable year." Encoded as the otherwise-allowable amount (cash capped at 60% of AGI, \xA7 170(b)(1)(G), permanent) minus 0.5% of AGI. [Modeled and disclosed: CASH gifts to 60%-limit organizations only; \xA7 170(d) carryovers and the (b)(1)(I)(ii)\u2013(iii) carryover coordination are not modeled \u2014 conservative for the current year.]`
    },
    effectiveFrom: "2026-01-01",
    // permanent: open-ended
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: charityCashLimited,
        right: {
          kind: "mulRate",
          base: agi,
          rate: { num: "5", den: "1000" },
          // 0.5%
          round: "half-up"
        }
      }
    }
  },
  {
    id: "us.federal.medical_expense_deduction",
    version: 1,
    jurisdiction: J20,
    title: "Medical expense deduction (expenses over 7.5% of AGI)",
    citation: {
      source: "26 U.S.C. \xA7 213(a)",
      section: "\xA7 213(a)",
      url: "https://www.law.cornell.edu/uscode/text/26/213",
      excerpt: "There shall be allowed as a deduction the expenses paid during the taxable year, not compensated for by insurance or otherwise, for medical care of the taxpayer, his spouse, or a dependent\u2026 to the extent that such expenses exceed 7.5 percent of adjusted gross income. [The 7.5% floor is permanent \u2014 P.L. 116-260 struck the temporary \xA7 213(f).]"
    },
    effectiveFrom: "2025-01-01",
    // permanent: open-ended
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact29("medicalExpenses"),
        right: {
          kind: "mulRate",
          base: agi,
          rate: { num: "75", den: "1000" },
          // 7.5%
          round: "half-up"
        }
      }
    }
  },
  {
    id: "us.federal.casualty_loss_deduction",
    version: 1,
    jurisdiction: J20,
    title: "Personal casualty losses \u2014 federally declared disasters (10% AGI floor) + qualified disaster losses (no AGI floor)",
    citation: {
      source: "26 U.S.C. \xA7 165(h); 2025 Instructions for Form 4684",
      section: "\xA7 165(h)(1), (2), (5)",
      url: "https://www.law.cornell.edu/uscode/text/26/165",
      excerpt: "A personal casualty loss 'shall be allowed as a deduction under subsection (a) only to the extent it is attributable to a Federally declared disaster\u2026 or a State declared disaster' (\xA7 165(h)(5), verbatim \u2014 non-declared losses are nondeductible and must not be entered); each casualty is reduced by the \xA7 165(h)(1) floor and the excess of losses over gains is 'allowed\u2026 only to the extent\u2026 such excess\u2026 exceeds 10 percent of the adjusted gross income of the individual' (\xA7 165(h)(2)(A), verbatim). Qualified disaster losses differ: 'Personal casualty and theft losses attributable to a qualified disaster loss are not subject to the 10% of the AGI reduction and the $100 reduction is increased to $500' (2025 Instructions for Form 4684, verbatim). [Per-event floors and insurance netting are attested by the two inputs; personal casualty GAINS are not modeled \u2014 enter net losses only. The qualified portion also increases the STANDARD deduction when not itemizing \u2014 see the deduction election.]"
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: fact29("casualtyFederalDisasterLosses"),
            right: {
              kind: "mulRate",
              base: ruleRef23("us.federal.agi"),
              rate: { num: "10", den: "100" },
              round: "half-up"
            }
          }
        },
        fact29("casualtyQualifiedDisasterLosses")
      ]
    }
  },
  {
    id: "us.federal.itemized_deductions_before_limitation",
    version: 4,
    // v2 lacked investment interest (§ 163(d)) and noncash charitable; v4 adds gambling losses (§ 165(d), Schedule A line 16) as an attested-limited fact
    jurisdiction: J20,
    title: "Itemized deductions before the \xA7 68 overall limitation (SALT + mortgage + charitable cash/noncash + medical + casualty + investment interest + gambling losses)",
    citation: {
      source: "26 U.S.C. \xA7 63(d)",
      section: "\xA7 63(d)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "'Itemized deductions' means the deductions allowable under this chapter other than (1) the deductions allowable in arriving at adjusted gross income, and (2) any deduction referred to in any paragraph of subsection (b). [Modeled Schedule A lines: taxes (\xA7 164), home mortgage interest (\xA7 163(h)), gifts to charity (\xA7 170) \u2014 cash via the limited charitable rule, noncash as the attested-allowed Form 8283 amount \u2014 medical (\xA7 213), casualty losses (\xA7 165(h), Form 4684), investment interest (\xA7 163(d)) as the attested post-Form-4952 allowed amount, and gambling losses (\xA7 165(d), Schedule A line 16) as the attested winnings-limited amount (TY2025: 100% of the limited amount; the OBBBA 90% haircut begins TY2026). Other miscellaneous itemized deductions are not modeled \u2014 conservative.]"
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        ruleRef23("us.federal.salt_deduction"),
        ruleRef23("us.federal.mortgage_interest_deduction"),
        ruleRef23("us.federal.charitable_deduction_itemizer"),
        fact29("noncashCharitableContributions"),
        ruleRef23("us.federal.medical_expense_deduction"),
        ruleRef23("us.federal.casualty_loss_deduction"),
        fact29("investmentInterestDeduction"),
        fact29("gamblingLossesItemized"),
        fact29("otherItemizedDeductions")
      ]
    }
  },
  {
    id: "us.federal.itemized_deductions",
    version: 1,
    jurisdiction: J20,
    title: "Itemized deductions (TY2025: no overall limitation \u2014 \xA7 68 suspended)",
    citation: {
      source: "26 U.S.C. \xA7 68(f) (as in effect for 2025; P.L. 115-97 \xA7 11046)",
      section: "\xA7 68(f)",
      url: "https://www.law.cornell.edu/uscode/text/26/68",
      excerpt: "Section 68 shall not apply to any taxable year beginning after December 31, 2017, and before January 1, 2026 \u2014 for 2025, itemized deductions carry no overall (Pease) limitation."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    formula: ruleRef23("us.federal.itemized_deductions_before_limitation")
  },
  {
    id: "us.federal.itemized_deductions",
    version: 2,
    jurisdiction: J20,
    title: "Itemized deductions after the \xA7 68 overall limitation (TY2026+: 2/37 of the amount over the 37% bracket)",
    citation: {
      source: "26 U.S.C. \xA7 68, as amended by Pub. L. 119-21 (OBBBA) \xA7 70111",
      section: "\xA7 68(a), (b)",
      url: "https://www.law.cornell.edu/uscode/text/26/68",
      excerpt: "For taxable years beginning after December 31, 2025, itemized deductions are reduced by 2/37 of the lesser of (1) such amount of itemized deductions, or (2) so much of the taxable income of the taxpayer for the taxable year (determined without regard to this section and increased by such amount of itemized deductions) as exceeds the dollar amount at which the 37 percent rate bracket under section 1 begins (Rev. Proc. 2025-32 \xA7 4.01: $640,600 single/HoH, $768,700 MFJ/QSS, $384,350 MFS for 2026); applied after the application of any other limitation (\xA7 68(b)). [The taxable-income term nets out every deduction except the itemized deductions themselves; the \xA7 199A interaction is NOT modeled \u2014 when both would apply, this rule refuses rather than guess.]"
    },
    effectiveFrom: "2026-01-01",
    effectiveTo: "2027-01-01",
    // the 37%-bracket start is indexed — TY2027 needs a new version
    output: { type: "money" },
    formula: (() => {
      const itemizedPre = ruleRef23("us.federal.itemized_deductions_before_limitation");
      const tiIncreased = {
        kind: "max0",
        arg: {
          kind: "sub",
          left: agi,
          right: {
            kind: "add",
            args: [
              ruleRef23("us.federal.senior_deduction"),
              ruleRef23("us.federal.tips_deduction"),
              ruleRef23("us.federal.overtime_deduction"),
              ruleRef23("us.federal.car_loan_interest_deduction")
            ]
          }
        }
      };
      const bracket37Start = {
        kind: "match",
        on: fact29("filingStatus"),
        cases: [
          { when: "single", value: money26("64060000") },
          // $640,600
          { when: "hoh", value: money26("64060000") },
          // $640,600
          { when: "mfj", value: money26("76870000") },
          // $768,700
          { when: "qss", value: money26("76870000") },
          // $768,700
          { when: "mfs", value: money26("38435000") }
          // $384,350
        ]
      };
      const reductionBase = {
        kind: "min",
        args: [
          itemizedPre,
          { kind: "max0", arg: { kind: "sub", left: tiIncreased, right: bracket37Start } }
        ]
      };
      return {
        kind: "if",
        cond: {
          kind: "and",
          args: [
            {
              kind: "cmp",
              op: "gt",
              left: fact29("selfEmploymentNetProfit"),
              right: zero17
            },
            { kind: "cmp", op: "gt", left: reductionBase, right: zero17 }
          ]
        },
        then: {
          kind: "unsupported",
          reason: "the \xA7 68 overall limitation and the \xA7 199A deduction interact through the taxable-income term \u2014 this combination is not modeled yet"
        },
        else: {
          kind: "sub",
          left: itemizedPre,
          right: {
            kind: "mulRate",
            base: reductionBase,
            rate: { num: "2", den: "37" },
            round: "half-up"
          }
        }
      };
    })()
  },
  {
    id: "us.federal.deduction_election",
    version: 3,
    // v2 lacked the qualified-disaster standard-deduction increase
    jurisdiction: J20,
    title: "Standard-or-itemized election (greater bundle by default; qualified disaster losses increase the standard side; forceItemized honors an explicit \xA7 63(e) election)",
    citation: {
      source: "26 U.S.C. \xA7 63(a), (b), (d), (e); \xA7 170(p); \xA7 165(h)(5); 2025 Instructions for Form 4684",
      section: "\xA7 63(b), (e); \xA7 165(h)(5)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "A non-itemizer's taxable income is adjusted gross income minus the standard deduction and the \xA7 170(p) non-itemizer charitable deduction (\xA7 63(b)); an individual who elects to itemize (\xA7 63(e)) subtracts itemized deductions instead and \u2014 because \xA7 170(p) applies only to 'an individual who does not elect to itemize deductions' \u2014 forgoes \xA7 170(p). 'You can deduct qualified disaster losses without itemizing other deductions on Schedule A' (2025 Instructions for Form 4684, verbatim) \u2014 the net qualified disaster loss increases the STANDARD side of the election. Encoded as the larger bundle (the election that minimizes tax) UNLESS forceItemized attests an explicit \xA7 63(e) election to itemize regardless \u2014 then itemized deductions apply even when smaller. The \xA7\xA7 151(d)(5)/224/225/163(h)(4) and \xA7 199A deductions are excluded from 'itemized deductions' by \xA7 63(d)(2) and apply on both sides. The \xA7 63(c)(6)(A) zero standard deduction for an MFS filer whose spouse itemizes flows through the standard-deduction term."
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: fact29("forceItemized"),
      then: ruleRef23("us.federal.itemized_deductions"),
      else: {
        kind: "max",
        args: [
          {
            kind: "add",
            args: [
              ruleRef23("us.federal.standard_deduction"),
              ruleRef23("us.federal.charitable_deduction_nonitemizer"),
              fact29("casualtyQualifiedDisasterLosses")
            ]
          },
          ruleRef23("us.federal.itemized_deductions")
        ]
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/kiddie.js
var J21 = "us.federal";
var fact30 = (factId) => ({ kind: "fact", factId });
var money27 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef24 = (ruleId) => ({ kind: "rule", ruleId });
var param16 = (name) => ({ kind: "param", name });
var zero18 = money27("0");
var CITE4 = {
  source: "26 U.S.C. \xA7 1(g); Form 8615",
  url: "https://www.law.cornell.edu/uscode/text/26/1"
};
var nui = ruleRef24("us.federal.kiddie.net_unearned_income");
function kiddieTaxRule(version, from, to, year) {
  const childRegular = ordinaryRateTaxExpr(ruleRef24("us.federal.taxable_income"), "filingStatus", year);
  const childReduced = ordinaryRateTaxExpr({
    kind: "max0",
    arg: { kind: "sub", left: ruleRef24("us.federal.taxable_income"), right: nui }
  }, "filingStatus", year);
  const parentWith = ordinaryRateTaxExpr({ kind: "add", args: [fact30("parentTaxableIncome"), nui] }, "parentFilingStatus", year);
  const parentWithout = ordinaryRateTaxExpr(fact30("parentTaxableIncome"), "parentFilingStatus", year);
  return {
    id: "us.federal.kiddie_tax",
    version,
    jurisdiction: J21,
    title: `Kiddie tax \u2014 the Form 8615 greater-of computation (TY${year})`,
    citation: {
      ...CITE4,
      section: "\xA7 1(g)(1), (3)",
      excerpt: "\u2026the greater of (A) the tax imposed\u2026 without regard to this subsection, or (B) the tax which would be imposed\u2026 if the taxable income of such child\u2026 were reduced by the net unearned income of such child, plus such child's share of the allocable parental tax \u2014 the excess of the tax on the parent's taxable income including the children's net unearned income over the tax on it alone. [Assumed and disclosed: this child is the parent's only \xA7 1(g) child; child and parent preferential-rate income REFUSE rather than skip the capital-gains worksheets; Tax Table method used below $100,000 per the Form 8615 instructions.]"
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: { kind: "not", arg: fact30("isSubjectToKiddieTax") },
      then: {
        kind: "unsupported",
        reason: "the kiddie_tax target applies only to a child subject to \xA7 1(g) \u2014 pass isSubjectToKiddieTax=true (or evaluate the normal net-tax target)"
      },
      else: {
        kind: "if",
        cond: {
          kind: "or",
          args: [
            { kind: "cmp", op: "gt", left: fact30("longTermCapitalGains"), right: zero18 },
            { kind: "cmp", op: "gt", left: fact30("qualifiedDividends"), right: zero18 },
            { kind: "cmp", op: "gt", left: fact30("shortTermCapitalGains"), right: zero18 },
            { kind: "cmp", op: "gt", left: fact30("ordinaryDividends"), right: zero18 },
            { kind: "cmp", op: "gt", left: fact30("shortTermCapitalLoss"), right: zero18 },
            { kind: "cmp", op: "gt", left: fact30("longTermCapitalLoss"), right: zero18 },
            { kind: "cmp", op: "gt", left: fact30("netCapitalLoss"), right: zero18 }
          ]
        },
        then: {
          kind: "unsupported",
          reason: "a \xA7 1(g) child with capital gains/losses, qualified dividends, or ordinary dividends needs the Form 8615 capital-gains worksheets, and/or is unearned income this corpus's 'unearned income \u2248 taxable interest' approximation would silently drop \u2014 not modeled"
        },
        else: {
          kind: "if",
          cond: fact30("parentHasPreferentialIncome"),
          then: {
            kind: "unsupported",
            reason: "the parent's taxable income includes preferential-rate income \u2014 the Form 8615 recomputation would need the parent's QDCGT worksheet, not modeled"
          },
          else: {
            kind: "max",
            args: [
              childRegular,
              {
                kind: "add",
                args: [
                  childReduced,
                  { kind: "max0", arg: { kind: "sub", left: parentWith, right: parentWithout } }
                ]
              }
            ]
          }
        }
      }
    }
  };
}
var kiddieRules = [
  {
    id: "us.federal.kiddie.net_unearned_income",
    version: 1,
    jurisdiction: J21,
    title: "Net unearned income of a \xA7 1(g) child",
    citation: {
      ...CITE4,
      section: "\xA7 1(g)(4)",
      excerpt: "\u2026the excess of the portion of adjusted gross income\u2026 not attributable to earned income, over the sum of the amount in effect under section 63(c)(5)(A) ($1,350 for taxable years beginning in 2025 and 2026) plus the greater of that amount or the itemized deductions directly connected \u2014 $2,700 with none such (child itemized deductions not modeled); shall not exceed the child's taxable income. [Unearned income \u2248 taxable interest \u2014 the child's capital gains refuse in the tax rule; disclosed.]"
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2027-01-01",
    // pinned to the years the $1,350 amount is verified for
    output: { type: "money" },
    parameters: {
      threshold: { value: "270000", type: "money" }
      // $2,700 = 2 × $1,350
    },
    formula: {
      kind: "min",
      args: [
        {
          kind: "max0",
          arg: { kind: "sub", left: fact30("taxableInterest"), right: param16("threshold") }
        },
        ruleRef24("us.federal.taxable_income")
      ]
    }
  },
  // v3/v4: the § 1(g) capital-gains refusal guard also catches the standalone qualifiedDividends fact
  // v5/v6: the refusal guard also catches shortTermCapitalGains/ordinaryDividends (unearned income
  // the "unearned income ≈ taxable interest" approximation in net_unearned_income would otherwise drop)
  // v5/v6: the refusal guard predated the per-bucket capital-loss facts and netCapitalLoss
  kiddieTaxRule(7, "2025-01-01", "2026-01-01", "2025"),
  kiddieTaxRule(8, "2026-01-01", "2027-01-01", "2026"),
  {
    id: "us.federal.income_tax_before_credits.kiddie",
    version: 1,
    jurisdiction: J21,
    title: "\xA7 1(g) override: a kiddie child's income tax is the Form 8615 tax",
    citation: {
      ...CITE4,
      section: "\xA7 1(g)(1)",
      excerpt: "In the case of any child to whom this subsection applies, the tax imposed by this section shall be equal to the greater of [the regular tax or the Form 8615 sum] \u2014 this override replaces the ordinary income-tax computation (including the Tax Table method, which the Form 8615 computation applies internally)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2027-01-01",
    output: { type: "money" },
    applicability: fact30("isSubjectToKiddieTax"),
    formula: ruleRef24("us.federal.kiddie_tax"),
    overrides: { ruleId: "us.federal.income_tax_before_credits", priority: 20 }
  }
];

// ../corpus-us-federal/dist/rules/net-tax.js
var ruleRef25 = (ruleId) => ({ kind: "rule", ruleId });
var FROM12 = { effectiveFrom: "2025-01-01" };
var netTaxRules = [
  {
    id: "us.federal.refundable_credits",
    version: 3,
    // v2 predated the refundable adoption credit and the net premium tax credit
    jurisdiction: "us.federal",
    title: "Refundable credits (EITC + ACTC + 40% AOTC + refundable adoption + net PTC)",
    citation: {
      source: "26 U.S.C. \xA7\xA7 23(a)(4), 24(d), 25A(i), 32, 36B",
      section: "\xA7\xA7 23(a)(4), 24(d), 25A(i), 32, 36B",
      url: "https://www.law.cornell.edu/uscode/text/26/32",
      excerpt: "Refundable credits are allowed against tax and, to the extent they exceed it, treated as an overpayment (\xA7 6401(b)) \u2014 the EITC, the additional child tax credit, the 40% refundable portion of the American Opportunity Tax Credit, the \xA7 23(a)(4) refundable adoption portion (OBBBA), and the \xA7 36B net premium tax credit (in excess of advance payments)."
    },
    ...FROM12,
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        ruleRef25("us.federal.eitc"),
        ruleRef25("us.federal.actc"),
        ruleRef25("us.federal.education.aotc_refundable"),
        ruleRef25("us.federal.adoption.refundable"),
        ruleRef25("us.federal.ptc.net")
      ]
    }
  },
  {
    id: "us.federal.net_tax",
    version: 2,
    // v1 predated SE tax / NIIT / Additional Medicare
    jurisdiction: "us.federal",
    title: "Total federal tax net of refundable credits (income + SE + NIIT + Add'l Medicare; negative = refund)",
    citation: {
      source: "26 U.S.C. \xA7 6401(b); Form 1040 lines 22\u201324",
      section: "\xA7 6401(b)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/6401",
      excerpt: "Total tax includes income tax after nonrefundable credits plus other taxes (Schedule 2); refundable credits in excess of it are treated as an overpayment (\xA7 6401(b))."
    },
    ...FROM12,
    output: { type: "money" },
    formula: {
      kind: "sub",
      left: {
        kind: "add",
        args: [
          ruleRef25("us.federal.income_tax_after_credits"),
          ruleRef25("us.federal.other_taxes")
        ]
      },
      right: ruleRef25("us.federal.refundable_credits")
    }
  },
  {
    id: "us.federal.balance_due",
    version: 1,
    jurisdiction: "us.federal",
    title: "Balance due after withholding (negative = refund expected at filing)",
    citation: {
      source: "26 U.S.C. \xA7 31(a)",
      section: "\xA7 31(a)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/31",
      excerpt: "The amount withheld as tax under chapter 24 shall be allowed to the recipient of the income as a credit against the tax imposed by this subtitle. [The mid-year withholding checkup: liability minus what has already been paid in.]"
    },
    ...FROM12,
    output: { type: "money" },
    formula: {
      kind: "sub",
      left: ruleRef25("us.federal.net_tax"),
      right: { kind: "fact", factId: "federalTaxWithheld" }
    }
  }
];

// ../corpus-us-federal/dist/rules/passive-losses.js
var J22 = "us.federal";
var fact31 = (factId) => ({ kind: "fact", factId });
var money28 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef26 = (ruleId) => ({ kind: "rule", ruleId });
var zero19 = money28("0");
var isMfs = {
  kind: "cmp",
  op: "eq",
  left: fact31("filingStatus"),
  right: { kind: "enum", value: "mfs" }
};
var absorptionPool = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: fact31("passiveActivityIncome"),
    right: fact31("selfRentalNetIncome")
  }
};
var absorbed = {
  kind: "min",
  args: [fact31("passiveActivityLosses"), absorptionPool]
};
var remainingRentalActive = {
  kind: "min",
  args: [
    fact31("rentalActiveParticipationLosses"),
    { kind: "max0", arg: { kind: "sub", left: fact31("passiveActivityLosses"), right: absorbed } }
  ]
};
function allowanceCap(capCents, phaseStartCents) {
  return {
    kind: "max0",
    arg: {
      kind: "sub",
      left: money28(capCents),
      right: {
        kind: "mulRate",
        base: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: ruleRef26("us.federal.agi"),
            right: money28(phaseStartCents)
          }
        },
        rate: { num: "50", den: "100" },
        round: "half-up"
      }
    }
  };
}
var specialAllowance = {
  kind: "if",
  cond: isMfs,
  then: {
    kind: "if",
    cond: fact31("mfsLivedApartAllYear"),
    then: {
      kind: "min",
      args: [remainingRentalActive, allowanceCap("1250000", "5000000")]
      // $12,500 / $50,000
    },
    else: zero19
    // § 469(i)(5)(B): lived together at any time → no allowance
  },
  else: {
    kind: "min",
    args: [remainingRentalActive, allowanceCap("2500000", "10000000")]
    // $25,000 / $100,000
  }
};
var CITATION = {
  source: "26 U.S.C. \xA7 469(a), (b), (i); 26 CFR \xA7 1.469-2(f)(6)",
  section: "\xA7 469(i)(1)\u2013(3), (5); Reg. \xA7 1.469-2(f)(6)",
  url: "https://www.law.cornell.edu/uscode/text/26/469",
  excerpt: "Passive activity losses are disallowed (\xA7 469(a)(1)) and 'treated as a deduction or credit allocable to such activity in the next taxable year' (\xA7 469(b), verbatim) \u2014 losses apply against passive income first, then: 'In the case of any natural person, subsection (a) shall not apply to that portion of the passive activity loss\u2026 which is attributable to all rental real estate activities with respect to which such individual actively participated' (\xA7 469(i)(1)), capped at $25,000 (\xA7 469(i)(2)), 'reduced (but not below zero) by 50 percent of the amount by which the adjusted gross income of the taxpayer for the taxable year exceeds $100,000' (\xA7 469(i)(3)(A)); for a married individual filing separately, 'substituting \u201C$12,500\u201D for \u201C$25,000\u201D\u2026 [and] \u201C$50,000\u201D for \u201C$100,000\u201D' if living apart at all times, and NO allowance for one who 'does not live apart from his spouse at all times during such taxable year' (\xA7 469(i)(5), verbatim). SELF-RENTAL RECHARACTERIZATION: 'An amount of the taxpayer's gross rental activity income for the taxable year from an item of property equal to the net rental activity income for the year from that item of property is treated as not from a passive activity if the property\u2014 (i) Is rented for use in a trade or business activity\u2026 in which the taxpayer materially participates\u2026 for the taxable year; and (ii) Is not described in \xA7 1.469-2T(f)(5)' (Reg. \xA7 1.469-2(f)(6), verbatim) \u2014 this rule excludes selfRentalNetIncome from the absorption pool; that income is also deemed derived in the ordinary course of a trade or business for \xA7 1411 (Reg. \xA7 1.1411-4(g)(6)(i)), so it is NOT net investment income. [STANDALONE determination: net the result into scheduleENetIncome / k1OrdinaryBusinessIncome yourself, and reduce the QBI base by allowed QBI-flagged losses. \xA7 469(i)(3)(E) MAGI add-backs approximated by AGI over the other supplied facts; the real-estate-professional escape (\xA7 469(c)(7)) makes those rentals NONpassive \u2014 exclude them from these inputs entirely; per-activity pro-rata allocation, the (f)(6) disposition-income arm, and the (i)(6) credit rules are not modeled.]"
};
var passiveLossRules = [
  {
    id: "us.federal.passive_loss_allowed",
    version: 2,
    // v2: § 1.469-2(f)(6) self-rental income excluded from the pool
    jurisdiction: J22,
    title: "Passive losses allowed this year \u2014 Form 8582 core (\xA7 469: income absorption + the \xA7 469(i) $25,000 rental allowance)",
    citation: CITATION,
    effectiveFrom: "2025-01-01",
    // statutory, un-indexed since 1986
    output: { type: "money" },
    formula: { kind: "add", args: [absorbed, specialAllowance] }
  },
  {
    id: "us.federal.passive_loss_suspended",
    version: 2,
    // v2: follows passive_loss_allowed v2
    jurisdiction: J22,
    title: "Passive losses suspended to next year (\xA7 469(b) carryforward)",
    citation: CITATION,
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact31("passiveActivityLosses"),
        right: ruleRef26("us.federal.passive_loss_allowed")
      }
    }
  }
];
function mileageRule(version, from, to, rateNum, rateLabel, noticeLabel) {
  return {
    id: "us.federal.vehicle_standard_mileage",
    version,
    jurisdiction: J22,
    title: `Business standard mileage deduction (${rateLabel}/mile, ${noticeLabel})`,
    citation: {
      source: `${noticeLabel}; Rev. Proc. 2019-46`,
      section: `${noticeLabel} \xA7 3`,
      url: "https://www.irs.gov/pub/irs-drop/n-26-10.pdf",
      excerpt: `The standard mileage rate for transportation or travel expenses is ${rateLabel} per mile for all miles of business use (${noticeLabel}, verbatim rate). [A STANDALONE determination in lieu of actual vehicle expenses (Rev. Proc. 2019-46 rules attested): subtract the result from your Schedule C profit yourself \u2014 it changes SE tax and QBI. Depreciation-component basis tracking and the actual-expense method are not modeled.] ADD-ON EXPENSES (Pub. 463 ch. 4): the mileage rate covers depreciation/gas/repairs ONLY. PARKING FEES and TOLLS for business trips deduct IN FULL in addition. The vehicle's PERSONAL PROPERTY TAX and (for the self-employed) CAR LOAN INTEREST deduct in addition but ONLY at the BUSINESS-USE PERCENTAGE = business miles \xF7 TOTAL miles driven (business + commuting + other/personal), computed and rounded (half-up to the dollar) PER VEHICLE: per-vehicle deduction = miles \xD7 rate + parking + tolls + businessPct \xD7 (property tax + loan interest). A vehicle with no commuting/other miles listed is 100% business use (add-ons in full).`
    },
    effectiveFrom: from,
    effectiveTo: to,
    output: { type: "money" },
    formula: {
      // miles × $1.00, scaled by the cents-per-mile rate (handles half-cent rates)
      kind: "mulRate",
      base: { kind: "mulInt", base: money28("100"), count: fact31("businessMilesDriven") },
      rate: { num: rateNum, den: "1000" },
      round: "half-up"
    }
  };
}
var mileageRules = [
  mileageRule(1, "2025-01-01", "2026-01-01", "700", "70 cents", "Notice 2025-5"),
  mileageRule(2, "2026-01-01", "2027-01-01", "725", "72.5 cents", "Notice 2026-10")
];

// ../corpus-us-federal/dist/rules/pension.js
var J23 = "us.federal";
var FROM13 = { effectiveFrom: "2025-01-01" };
var fact32 = (factId) => ({ kind: "fact", factId });
var money29 = (cents2) => ({ kind: "money", cents: cents2 });
var int = (value) => ({ kind: "int", value });
var ruleRef27 = (ruleId) => ({ kind: "rule", ruleId });
var zero20 = money29("0");
var cost = fact32("pensionCostBasis");
var months = fact32("pensionMonthsThisYear");
var annualAt = (n) => ({
  kind: "mulInt",
  base: { kind: "mulRate", base: cost, rate: { num: "1", den: n }, round: "half-up" },
  count: months
});
var ladder = (on, bands, last) => {
  let expr = annualAt(last);
  for (let i = bands.length - 1; i >= 0; i--) {
    const [limit, n] = bands[i];
    expr = {
      kind: "if",
      cond: { kind: "cmp", op: "le", left: on, right: int(limit) },
      then: annualAt(n),
      else: expr
    };
  }
  return expr;
};
var needsAge = (ageFact) => ({
  kind: "cmp",
  op: "eq",
  left: fact32(ageFact),
  right: int("0")
});
var pensionRules = [
  {
    id: "us.federal.pension.simplified_method_exclusion",
    version: 1,
    jurisdiction: J23,
    title: "Simplified Method tax-free portion (\xA7 72(d): cost \xF7 anticipated payments \xD7 months, capped at unrecovered cost)",
    citation: {
      source: "26 U.S.C. \xA7 72(d); IRS Pub 575 (Simplified Method Worksheet, Tables 1-2)",
      section: "\xA7 72(d)(1)(B)(iii)-(iv)",
      url: "https://www.law.cornell.edu/uscode/text/26/72",
      excerpt: "Gross income shall not include so much of any monthly annuity payment\u2026 as does not exceed the amount obtained by dividing (I) the investment in the contract (as of the annuity starting date), by (II) the number of anticipated payments\u2026 Age at annuity starting date: not more than 55 \u2192 360; more than 55 but not more than 60 \u2192 310; more than 60 but not more than 65 \u2192 260; more than 65 but not more than 70 \u2192 210; more than 70 \u2192 160. Combined ages (more than one life): not more than 110 \u2192 410; \u2026120 \u2192 360; \u2026130 \u2192 310; \u2026140 \u2192 260; more than 140 \u2192 210. [Lifetime recovery capped at total cost (Pub 575); monthly division keeps cents per the Pub 575 worked example. The rule refuses when the basis is set but the required age fact is not.]"
    },
    ...FROM13,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "eq", left: cost, right: zero20 },
      then: zero20,
      else: {
        kind: "if",
        cond: fact32("pensionIsJointAndSurvivor"),
        then: {
          kind: "if",
          cond: needsAge("pensionCombinedAgesAtStart"),
          then: {
            kind: "unsupported",
            reason: "a joint-and-survivor simplified-method annuity needs pensionCombinedAgesAtStart (combined ages at the annuity starting date) \u2014 refusing rather than assume a \xA7 72(d)(1)(B)(iv) band"
          },
          else: {
            kind: "min",
            args: [
              ladder(fact32("pensionCombinedAgesAtStart"), [
                ["110", "410"],
                ["120", "360"],
                ["130", "310"],
                ["140", "260"]
              ], "210"),
              { kind: "max0", arg: { kind: "sub", left: cost, right: fact32("pensionBasisPreviouslyRecovered") } }
            ]
          }
        },
        else: {
          kind: "if",
          cond: needsAge("pensionAgeAtStart"),
          then: {
            kind: "unsupported",
            reason: "a simplified-method annuity needs pensionAgeAtStart (age at the annuity starting date, NOT current age) \u2014 refusing rather than assume a \xA7 72(d)(1)(B)(iii) band"
          },
          else: {
            kind: "min",
            args: [
              ladder(fact32("pensionAgeAtStart"), [
                ["55", "360"],
                ["60", "310"],
                ["65", "260"],
                ["70", "210"]
              ], "160"),
              { kind: "max0", arg: { kind: "sub", left: cost, right: fact32("pensionBasisPreviouslyRecovered") } }
            ]
          }
        }
      }
    }
  },
  {
    id: "us.federal.pension.simplified_method_taxable",
    version: 1,
    jurisdiction: J23,
    title: "Taxable pension under the Simplified Method (gross payments minus the \xA7 72(d) exclusion)",
    citation: {
      source: "26 U.S.C. \xA7 72(a), (d); IRS Pub 575",
      section: "\xA7 72(a), (d)(1)(B)(i)",
      url: "https://www.law.cornell.edu/uscode/text/26/72",
      excerpt: "Except as otherwise provided in this chapter, gross income includes any amount received as an annuity\u2026 gross income shall not include so much of any monthly annuity payment\u2026 as does not exceed [the \xA7 72(d) exclusion]. [Feeds gross income alongside the already-taxable taxablePensionsAndAnnuities fact \u2014 the two inputs are additive for separate pensions.]"
    },
    ...FROM13,
    output: { type: "money" },
    formula: {
      kind: "max0",
      arg: {
        kind: "sub",
        left: fact32("pensionGrossPayments"),
        right: ruleRef27("us.federal.pension.simplified_method_exclusion")
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/rental.js
var J24 = "us.federal";
var FROM14 = { effectiveFrom: "2025-01-01" };
var fact33 = (factId) => ({ kind: "fact", factId });
var money30 = (cents2) => ({ kind: "money", cents: cents2 });
var int2 = (value) => ({ kind: "int", value });
var ruleRef28 = (ruleId) => ({ kind: "rule", ruleId });
var zero21 = money30("0");
var YEAR1_RATE_PER_100K = [
  "3485",
  // Jan
  "3182",
  // Feb
  "2879",
  // Mar
  "2576",
  // Apr
  "2273",
  // May
  "1970",
  // Jun
  "1667",
  // Jul
  "1364",
  // Aug
  "1061",
  // Sep
  "758",
  // Oct
  "455",
  // Nov
  "152"
  // Dec
];
var STEADY_RATE_PER_100K = "3636";
var rateOf = (per100k) => ({
  kind: "mulRate",
  base: fact33("rentalDepreciableBasis"),
  rate: { num: per100k, den: "100000" },
  round: "half-up"
});
var monthTable = () => {
  let expr = rateOf(STEADY_RATE_PER_100K);
  for (let m = 12; m >= 1; m--) {
    expr = {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "eq",
        left: fact33("rentalPlacedInServiceMonth"),
        right: int2(String(m))
      },
      then: rateOf(YEAR1_RATE_PER_100K[m - 1]),
      else: expr
    };
  }
  return expr;
};
var CITE_BASE = {
  source: "26 U.S.C. \xA7 168; IRS Pub 946 Table A-6; IRS Pub 527 ch. 2",
  url: "https://www.irs.gov/publications/p946"
};
var rentalRules = [
  {
    id: "us.federal.rental.depreciation",
    version: 1,
    jurisdiction: J24,
    title: "Residential rental depreciation (\xA7 168 GDS: 27.5-yr straight line, mid-month)",
    citation: {
      ...CITE_BASE,
      section: "\xA7 168(b)(3)(B), (c), (d)(2)",
      excerpt: "In the case of residential rental property\u2026 the applicable depreciation method is the straight line method\u2026 the applicable recovery period is 27.5 years\u2026 the applicable convention is the mid-month convention. [Pub 946 Table A-6 year-1 percentages by placed-in-service month: 3.485, 3.182, 2.879, 2.576, 2.273, 1.970, 1.667, 1.364, 1.061, 0.758, 0.455, 0.152; full years thereafter 3.636 \u2014 the printed three-decimal table, which is what filed returns use. Basis is the BUILDING only (land never depreciates, \xA7 167). Not modeled, disclosed: ADS elections, separately-dated improvements, the final recovery year's stub, and mid-year dispositions.]"
    },
    ...FROM14,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "eq", left: fact33("rentalDepreciableBasis"), right: zero21 },
      then: zero21,
      else: monthTable()
    }
  },
  {
    id: "us.federal.rental.net_income",
    version: 1,
    jurisdiction: J24,
    title: "Net rental income: Schedule E result before depreciation minus \xA7 168 depreciation",
    citation: {
      ...CITE_BASE,
      section: "\xA7 62(a)(4); Schedule E (Form 1040) line 18",
      excerpt: "Deductions attributable to rents\u2026 are allowed in arriving at adjusted gross income (\xA7 62(a)(4)); Schedule E line 18: 'Depreciation expense or depletion.' [When the computed depreciation EXCEEDS the pre-depreciation rental result, the year is a rental LOSS governed by the \xA7 469 passive-activity rules \u2014 enter it through rentalActiveParticipationLosses (the $25,000 allowance machinery) instead; this rule refuses rather than route a loss around \xA7 469.]"
    },
    ...FROM14,
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "gt",
        left: ruleRef28("us.federal.rental.depreciation"),
        right: fact33("rentalIncomeBeforeDepreciation")
      },
      then: {
        kind: "if",
        cond: { kind: "cmp", op: "eq", left: fact33("rentalIncomeBeforeDepreciation"), right: zero21 },
        then: zero21,
        // depreciation facts present but no income routed through this path: nothing to net
        else: {
          kind: "unsupported",
          reason: "depreciation exceeds the pre-depreciation rental result \u2014 this year is a rental LOSS subject to the \xA7 469 passive-activity limits: enter it via rentalActiveParticipationLosses (the $25,000 active-participation allowance applies) rather than through this income path"
        }
      },
      else: {
        kind: "sub",
        left: fact33("rentalIncomeBeforeDepreciation"),
        right: ruleRef28("us.federal.rental.depreciation")
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/schedule-d.js
var J25 = "us.federal";
var FROM15 = { effectiveFrom: "2025-01-01" };
var fact34 = (factId) => ({ kind: "fact", factId });
var money31 = (cents2) => ({ kind: "money", cents: cents2 });
var zero22 = money31("0");
var gt09 = (e) => ({ kind: "cmp", op: "gt", left: e, right: zero22 });
var max02 = (arg) => ({ kind: "max0", arg });
var sub = (left, right) => ({ kind: "sub", left, right });
var add = (...args) => ({ kind: "add", args });
var and = (...args) => ({ kind: "and", args });
var or = (...args) => ({ kind: "or", args });
var stG = fact34("shortTermCapitalGains");
var stL = fact34("shortTermCapitalLoss");
var ltG = fact34("longTermCapitalGains");
var ltL = fact34("longTermCapitalLoss");
var legacyLoss = fact34("netCapitalLoss");
var CONTRADICTORY = or(and(gt09(stG), gt09(stL)), and(gt09(ltG), gt09(ltL)), and(gt09(legacyLoss), or(gt09(stG), gt09(ltG), gt09(stL), gt09(ltL))));
var refuse = (computation) => ({
  kind: "if",
  cond: CONTRADICTORY,
  then: {
    kind: "unsupported",
    reason: "contradictory Schedule D facts \u2014 each capital fact is a NET result: a bucket takes a gain OR a loss (never both), and the overall netCapitalLoss cannot be combined with per-bucket facts"
  },
  else: computation
});
var CITE5 = {
  source: "26 U.S.C. \xA7 1222; 2025 Schedule D (Form 1040)",
  url: "https://www.law.cornell.edu/uscode/text/26/1222"
};
var scheduleDRules = [
  {
    id: "us.federal.schedule_d.preferential_lt_gain",
    version: 1,
    jurisdiction: J25,
    title: "Net capital gain (\xA7 1222(11)): net LT gain reduced by any net ST loss",
    citation: {
      ...CITE5,
      section: "\xA7 1222(11)",
      excerpt: "The term 'net capital gain' means the excess of the net long-term capital gain for the taxable year over the net short-term capital loss for such year. [Schedule D: line 16 combines line 7 (net short-term) and line 15 (net long-term); when line 7 is a loss and line 15 a gain, the remainder keeps long-term character and prices through the \xA7 1(h) stack. Inputs are per-bucket NET results; contradictory combinations refuse.]"
    },
    ...FROM15,
    output: { type: "money" },
    formula: refuse(max02(sub(sub(ltG, ltL), max02(sub(stL, stG)))))
  },
  {
    id: "us.federal.schedule_d.ordinary_st_gain",
    version: 1,
    jurisdiction: J25,
    title: "Net short-term capital gain surviving netting (\xA7 1222(5), (9)): ordinary-rate",
    citation: {
      ...CITE5,
      section: "\xA7 1222(5), (9)",
      excerpt: "'Net short-term capital gain' means the excess of short-term capital gains for the taxable year over the short-term capital losses for such year\u2026 'net long-term capital loss' means the excess of long-term capital losses\u2026 over the long-term capital gains. [Schedule D line 16: a net LT loss reduces the net ST gain; whatever short-term gain survives is ordinary income \u2014 \xA7 1(h) prices only the \xA7 1222(11) net capital gain.]"
    },
    ...FROM15,
    output: { type: "money" },
    formula: refuse(max02(sub(sub(stG, stL), max02(sub(ltL, ltG)))))
  },
  {
    id: "us.federal.schedule_d.net_loss",
    version: 1,
    jurisdiction: J25,
    title: "Net capital loss after netting (\xA7 1222(10)), feeding the \xA7 1211(b) offset",
    citation: {
      ...CITE5,
      section: "\xA7 1222(10)",
      excerpt: "The term 'net capital loss' means the excess of the losses from sales or exchanges of capital assets over the sum allowed under section 1211. [Computed as the overall negative of Schedule D line 16, entered here positive: the legacy netCapitalLoss fact passes through unchanged, or the per-bucket facts net to a loss when bucket losses exceed bucket gains. \xA7 1211(b) caps the ordinary-income offset; \xA7 1212(b) carries the rest forward.]"
    },
    ...FROM15,
    output: { type: "money" },
    formula: refuse(add(legacyLoss, max02(sub(add(stL, ltL), add(stG, ltG)))))
  }
];

// ../corpus-us-federal/dist/rules/schedule-r.js
var J26 = "us.federal";
var fact35 = (factId) => ({ kind: "fact", factId });
var money32 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef29 = (ruleId) => ({ kind: "rule", ruleId });
var zero23 = money32("0");
var isStatus9 = (v) => ({
  kind: "cmp",
  op: "eq",
  left: fact35("filingStatus"),
  right: { kind: "enum", value: v }
});
var qualified = {
  kind: "or",
  args: [fact35("isAge65OrOlder"), fact35("isRetiredOnTotalDisability")]
};
var initialByStatus = {
  kind: "if",
  cond: isStatus9("mfs"),
  then: money32("375000"),
  else: {
    kind: "if",
    cond: {
      kind: "and",
      args: [
        isStatus9("mfj"),
        fact35("spouseIsAge65OrOlder")
        // spouse-disabled qualification not modeled — disclosed
      ]
    },
    then: money32("750000"),
    else: money32("500000")
  }
};
var initial = {
  kind: "if",
  cond: fact35("isAge65OrOlder"),
  then: initialByStatus,
  else: { kind: "min", args: [initialByStatus, fact35("scheduleRDisabilityIncome")] }
};
var agiThreshold = {
  kind: "if",
  cond: isStatus9("mfj"),
  then: money32("1000000"),
  // $10,000
  else: {
    kind: "if",
    cond: isStatus9("mfs"),
    then: money32("500000"),
    // $5,000
    else: money32("750000")
    // $7,500
  }
};
var section22Amount = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: initial,
    right: {
      kind: "add",
      args: [
        fact35("nontaxableBenefitsForScheduleR"),
        {
          kind: "mulRate",
          base: {
            kind: "max0",
            arg: { kind: "sub", left: ruleRef29("us.federal.agi"), right: agiThreshold }
          },
          rate: { num: "50", den: "100" },
          round: "half-up"
        }
      ]
    }
  }
};
var CITATION2 = {
  source: "26 U.S.C. \xA7 22; Schedule R (Form 1040)",
  section: "\xA7 22(a)-(e)",
  url: "https://www.law.cornell.edu/uscode/text/26/22",
  excerpt: "A credit equal to '15 percent of such individual's section 22 amount for such taxable year' (\xA7 22(a), verbatim) for a qualified individual \u2014 one 'who has attained age 65 before the close of the taxable year, or\u2026 who retired on disability\u2026 and who, when he retired, was permanently and totally disabled' (\xA7 22(b), verbatim; the physician's-statement substantiation is ATTESTED by isRetiredOnTotalDisability). Initial amounts: '$5,000 in the case of a single individual' or a joint return with one qualified spouse, '$7,500 in the case of a joint return where both spouses are qualified individuals', '$3,750 in the case of a married individual filing a separate return' (\xA7 22(c)(2)) \u2014 for an under-65 disabled individual, limited to \xA7\xA7 72/105(a) disability income; reduced by \xA7 22(c)(3) nontaxable social security, railroad retirement and VA benefits, and by 'one-half of the excess' of AGI over $7,500 / $10,000 joint / $5,000 MFS (\xA7 22(d)). [MFS living with the spouse at any time: no credit (\xA7 22(e)(1)) \u2014 gated on mfsLivedApartAllYear. A joint-return spouse qualifying by DISABILITY (rather than age 65) is not modeled for the $7,500 tier \u2014 conservative. Schedule R credit joins the \xA7 26(a) cascade before the CTC.]"
};
var scheduleRRules = [
  {
    id: "us.federal.schedule_r.tentative",
    version: 1,
    jurisdiction: J26,
    title: "Credit for the elderly or the disabled \u2014 tentative (\xA7 22, Schedule R)",
    citation: CITATION2,
    effectiveFrom: "2025-01-01",
    // statutory, unindexed
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "and",
        args: [
          qualified,
          {
            kind: "or",
            args: [{ kind: "not", arg: isStatus9("mfs") }, fact35("mfsLivedApartAllYear")]
          }
        ]
      },
      then: {
        kind: "roundToDollar",
        value: {
          kind: "mulRate",
          base: section22Amount,
          rate: { num: "15", den: "100" },
          round: "half-up"
        },
        mode: "half-up"
      },
      else: zero23
    }
  },
  {
    id: "us.federal.schedule_r_credit",
    version: 1,
    jurisdiction: J26,
    title: "Schedule R credit, limited to remaining tax (\xA7 26(a) after CDCC + education + saver's + adoption)",
    citation: CITATION2,
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "min",
      args: [
        ruleRef29("us.federal.schedule_r.tentative"),
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: {
              kind: "add",
              args: [
                ruleRef29("us.federal.income_tax_before_credits"),
                ruleRef29("us.federal.amt")
              ]
            },
            right: {
              kind: "add",
              args: [
                ruleRef29("us.federal.cdcc"),
                ruleRef29("us.federal.education.nonrefundable"),
                ruleRef29("us.federal.savers_credit"),
                ruleRef29("us.federal.adoption.nonrefundable")
              ]
            }
          }
        }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/state-helpers.js
var fact36 = (factId) => ({ kind: "fact", factId });
var money33 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef30 = (ruleId) => ({ kind: "rule", ruleId });
var isStatus10 = (v) => ({
  kind: "cmp",
  op: "eq",
  left: fact36("filingStatus"),
  right: { kind: "enum", value: v }
});
var printedSchedule = (base, rows) => {
  const rowExpr = (r) => ({
    kind: "add",
    args: [
      money33(r.fixedCents),
      {
        kind: "mulRate",
        base: { kind: "sub", left: base, right: money33(r.thresholdCents) },
        rate: r.rate,
        round: "half-up"
      }
    ]
  });
  let expr = rowExpr(rows[rows.length - 1]);
  for (let i = rows.length - 2; i >= 0; i--) {
    expr = {
      kind: "if",
      cond: { kind: "cmp", op: "le", left: base, right: money33(rows[i + 1].thresholdCents) },
      then: rowExpr(rows[i]),
      else: expr
    };
  }
  return expr;
};

// ../corpus-us-federal/dist/rules/state-il.js
var ilRules = [
  {
    id: "us.il.income_tax",
    version: 3,
    // v1 lacked the age/blind exemptions, AGI caps, ICR/1299-C credit parameters, and line 9/11 floors; v2 (unlabeled) preceded this comment; v3 cross-references us.il.eitc for the 18-24/65+ childless age-decoupling (35 ILCS 5/212(b-5)/(b-10)) — the flat "20% of federal EITC" line below is INCOMPLETE for that population
    jurisdiction: "us.il",
    title: "Illinois income tax \u2014 flat 4.95% of net income (IL-1040 line 12)",
    citation: {
      source: "35 ILCS 5/201(b)(5.4); 2025 Form IL-1040 instructions",
      section: "IL-1040 line 12",
      url: "https://tax.illinois.gov/content/dam/soi/en/web/tax/forms/incometax/documents/currentyear/individual/il-1040-instr.pdf",
      excerpt: "Residents: Multiply Line 11 by 4.95% (.0495). Cannot be less than zero (2025 IL-1040 line 12, verbatim). LINE FLOORS (2025 instructions): line 9 base income AND line 11 net income 'may not be less than zero \u2014 if the result is a negative number, enter 0'. Related 2025 parameters (IDOR 2025 IL-1040 + Schedule ICR + Schedule 1299-C instructions + Pub-132, web-verified July 2026): exemption allowance $2,850 per exemption PLUS $1,000 per age-65+ box and $1,000 per blind box (taxpayer/spouse, per box \u2014 65 AND blind = $2,000); the ENTIRE line 10 allowance is $0 if federal AGI exceeds $500,000 MFJ / $250,000 all others. Schedule ICR (nonrefundable, total cannot exceed tax due, same AGI caps): property tax credit = 5% of IL property tax on the principal residence (exclude business-use portion); K-12 education expense credit = 25% of qualified expenses OVER $250, max $750 \u2014 HOME-SCHOOL expenses are NOT qualified (35 ILCS 5/201(m): public/nonpublic school tuition, book and lab fees only; exclude school_type home entries before summing). Schedule 1299-C K-12 Instructional Materials & Supplies credit (900+ hour educators): up to $500 ($1,000 MFJ both educators). Illinois EITC = 20% of the federal EITC (refundable) \u2014 BUT for a taxpayer age 18-24 or 65+ with NO qualifying children, 35 ILCS 5/212(b-5)/(b-10) (P.A. 102-700) decouples from the federal \xA7 32(c)(1)(A)(ii) childless age floor: use the us.il.eitc target (20% of us.federal.eitc_no_age_gate) instead of 20% x the ordinary federal EITC, which is correctly $0 for that population and would understate line 29. Illinois Child Tax Credit (TY2025) = 40% of the Illinois EITC for taxpayers with a dependent child under 12. [Input: IL net income (line 11 \u2014 base income minus exemptions), computed by the preparer; worked example: 30,485 \xD7 4.95% = 1,509.] ESTIMATED TAX (IL-2210, 2025): required if IL tax after withholding/credits will exceed $1,000. SCHEDULE NR: part-year and nonresidents apportion base income on Schedule NR \u2014 this target computes RESIDENT tax on already-apportioned net income only; the Schedule NR apportionment worksheet itself is not modeled (refuses/agent-composed)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      exemptionAllowance: { value: "285000", type: "money" },
      // $2,850 (2025)
      exemptionAge65: { value: "100000", type: "money" },
      // $1,000 per box
      exemptionBlind: { value: "100000", type: "money" },
      // $1,000 per box
      exemptionAgiCapJoint: { value: "50000000", type: "money" },
      // $500,000
      exemptionAgiCapOther: { value: "25000000", type: "money" },
      // $250,000
      propertyTaxCreditPct: { value: "5", type: "int" },
      k12CreditPct: { value: "25", type: "int" },
      k12ExpenseFloor: { value: "25000", type: "money" },
      // first $250 excluded
      k12CreditMax: { value: "75000", type: "money" },
      // $750
      teacherMaterialsCreditMax: { value: "50000", type: "money" },
      // $500 ($1,000 MFJ both)
      eicPctOfFederal: { value: "20", type: "int" },
      ctcPctOfIlEic: { value: "40", type: "int" },
      estimatedTaxThreshold: { value: "100000", type: "money" }
      // $1,000 (IL-2210)
    },
    formula: {
      kind: "mulRate",
      base: { kind: "max0", arg: fact36("stateTaxableIncome") },
      rate: { num: "495", den: "10000" },
      round: "half-up"
    }
  },
  {
    id: "us.il.eitc",
    version: 1,
    jurisdiction: "us.il",
    title: "Illinois Earned Income Credit \u2014 age-decoupled from the federal childless age gate (35 ILCS 5/212(a)(vi), (b-5), (b-10))",
    citation: {
      source: "35 ILCS 5/212; 2025 Schedule IL-E/EITC instructions (R-12/25); Illinois Pub. 132 (2025)",
      section: "35 ILCS 5/212(a)(vi), (b-5), (b-10)",
      url: "https://www.ilga.gov/legislation/ilcs/fulltext.asp?DocName=003500050K212",
      excerpt: "35 ILCS 5/212(a)(vi), verbatim: each individual taxpayer is entitled to a credit of '20% of the federal tax credit for each taxable year beginning on or after January 1, 2023.' DECOUPLING (verbatim, unmodified since P.A. 102-700, eff. 4-19-22 \u2014 confirmed current for TY2025, R-12/25 revision stamp): '(b-5) ... each individual taxpayer who has attained the age of 18 ... but has not yet attained the age of 25 is entitled to the credit under paragraph (a) based on the federal tax credit for which the taxpayer would have been eligible without regard to any age requirements that would otherwise apply to individuals without a qualifying child in Section 32(c)(1)(A)(ii) of the federal Internal Revenue Code.' '(b-10) ... each individual taxpayer who has attained the age of 65 or older ... is entitled to the credit under paragraph (a) based on the federal tax credit for which the taxpayer would have been eligible without regard to any age requirements ... in Section 32(c)(1)(A)(ii).' MECHANICS (Schedule IL-E/EITC 2025, Step 4 / Illinois Expanded EITC Worksheet Part 6, verbatim): 'Multiply the amount on Line 6 by 20% (0.2)' where Line 6 is 'the amount of federal Earned Income Tax Credit ... or the amount from the Illinois Expanded EITC Worksheet' \u2014 for an 18-24 or 65+ CHILDLESS filer, Line 6 is NOT the (correctly $0) ordinary federal EITC but the EIC-Table/AGI 'smaller of' amount the filer WOULD have received absent the \xA7 32(c)(1)(A)(ii) age floor, i.e. this rule = 20% x us.federal.eitc_no_age_gate. A filer WITH qualifying children, or a childless filer already 25-64, gets the identical answer either way (the no-age-gate rule and the ordinary federal EITC agree outside the decoupled population) \u2014 this target is safe to use for ALL Illinois EITC claimants, not just the 18-24/65+ childless group. Nonresident/part-year apportionment (Schedule IL-E/EITC line 8 decimal) is NOT applied here (parameters-only elsewhere); apply it to this rule's answer if claiming a part-year/nonresident return."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      pctOfFederal: { value: "20", type: "int" }
    },
    formula: {
      kind: "roundToDollar",
      value: {
        kind: "mulRate",
        base: ruleRef30("us.federal.eitc_no_age_gate"),
        rate: { num: "20", den: "100" },
        round: "half-up"
      },
      mode: "half-up"
    }
  },
  {
    id: "us.il.subtractions",
    version: 1,
    jurisdiction: "us.il",
    title: "Illinois Schedule M \u2014 retirement-income subtraction (line 5) and related AGI modifications (35 ILCS 5/203(a)(2))",
    citation: {
      source: "35 ILCS 5/203(a)(2); 2025 Form IL-1040 instructions (Schedule M line instructions); Publication 120, Retirement Income",
      section: "IL-1040 lines 2, 5, 6, 7; Schedule M line 22",
      url: "https://tax.illinois.gov/content/dam/soi/en/web/tax/research/publications/pubs/documents/pub-120.pdf",
      excerpt: "LINE 5 (verbatim, 2025 IL-1040 instructions), 'Social Security benefits and certain retirement plans': 'Enter the amount of federally taxed Social Security and retirement income included in your adjusted gross income on Form IL-1040, Line 1 that you received from qualified employee benefit plans (including railroad retirement and 401(K) plans) and Individual Retirement Accounts or self-employed retirement plans reported on federal Form 1040 or 1040-SR, Line 4b and 5b; Social Security and railroad retirement benefits reported on federal Form 1040 or 1040-SR, Line 6b; government retirement and government disability plans and group term life insurance premiums paid by a qualified retirement plan reported as wages\u2026 Line 1z; state or local government deferred compensation plans\u2026 Line 1z and 5b; certain capital gains on employer securities\u2026 Line 7a.' SCOPE: ALL federally-taxed retirement income is subtracted, Social Security INCLUDED inside line 5 (not a separate line) \u2014 exclusions per Publication 120: third-party sick pay, non-government disability income, and Form 4972 lump-sum-distribution averaging amounts are NOT subtractable. LINE 2 addition (verbatim): 'Enter the amount of federally tax-exempt interest and dividend income reported on federal Form 1040\u2026 Line 2a.' LINE 6 CORRECTION: line 6 is the ILLINOIS income tax overpayment (a subtraction of last year's state refund), NOT US-government interest \u2014 US Treasury/agency obligation interest is a DIFFERENT item, entered on Schedule M line 22 and carried to IL-1040 LINE 7 'other subtractions' (35 ILCS 5/203(a)(2)(N)); do not put US-govt interest on line 6. 2025 Schedule M NEW line 18: medical debt relief subtraction (medical debt cancelled/forgiven and included in federal AGI). Parameters-only: no oracle formula \u2014 these are Schedule M/1040-line transcription facts, agent-composed onto IL-1040 lines 2/5/7."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    formula: {
      kind: "unsupported",
      reason: "parameters-only rule: IL Schedule M subtractions/additions are agent-transcribed from federal Form 1040/Schedule 1 line references cited above \u2014 no formula to evaluate; use lookup_tax_parameter / read the citation"
    }
  },
  {
    id: "us.il.use_tax",
    version: 1,
    jurisdiction: "us.il",
    title: "Illinois Use Tax (UT) Table \u2014 estimated liability by AGI when no purchase records exist (IL-1040 line 21)",
    citation: {
      source: "35 ILCS 105/3-10; 2025 Form IL-1040 instructions, Use Tax (UT) Table",
      section: "IL-1040 line 21; UT Table",
      url: "https://tax.illinois.gov/content/dam/soi/en/web/tax/forms/incometax/documents/currentyear/individual/il-1040-instr.pdf",
      excerpt: "'If you had no major purchases and you do not have receipts to figure your purchases, use this table to estimate your annual Illinois Use Tax liability.' AGI (from Form IL-1040, Line 1) \u2192 Use Tax (2025 UT Table, verbatim): $0\u2013$10,000 \u2192 $3; $10,001\u2013$20,000 \u2192 $8; $20,001\u2013$30,000 \u2192 $13; $30,001\u2013$40,000 \u2192 $18; $40,001\u2013$50,000 \u2192 $23; $50,001\u2013$75,000 \u2192 $31; $75,001\u2013$100,000 \u2192 $44; above $100,000 \u2192 AGI \xD7 0.05% (0.0005). This is the NO-RECEIPTS ESTIMATE only \u2014 a taxpayer with actual purchase records instead completes the UT Worksheet (6.25% general merchandise / 1% qualifying food-drug-medical, less other-state sales tax paid), which is a different (higher-precision) computation not modeled here. Note: if annual use tax liability exceeds $600 ($1,200 MFJ), Form ST-44 must be filed separately (the $1,000-flat rows below the ST-44 threshold are unaffected by this rule)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      tier1Max: { value: "1000000", type: "money" },
      // $10,000
      tier2Max: { value: "2000000", type: "money" },
      // $20,000
      tier3Max: { value: "3000000", type: "money" },
      // $30,000
      tier4Max: { value: "4000000", type: "money" },
      // $40,000
      tier5Max: { value: "5000000", type: "money" },
      // $50,000
      tier6Max: { value: "7500000", type: "money" },
      // $75,000
      tier7Max: { value: "10000000", type: "money" },
      // $100,000
      tier1Tax: { value: "300", type: "money" },
      // $3
      tier2Tax: { value: "800", type: "money" },
      // $8
      tier3Tax: { value: "1300", type: "money" },
      // $13
      tier4Tax: { value: "1800", type: "money" },
      // $18
      tier5Tax: { value: "2300", type: "money" },
      // $23
      tier6Tax: { value: "3100", type: "money" },
      // $31
      tier7Tax: { value: "4400", type: "money" }
      // $44
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "le", left: ruleRef30("us.federal.agi"), right: { kind: "param", name: "tier1Max" } },
      then: { kind: "param", name: "tier1Tax" },
      else: {
        kind: "if",
        cond: { kind: "cmp", op: "le", left: ruleRef30("us.federal.agi"), right: { kind: "param", name: "tier2Max" } },
        then: { kind: "param", name: "tier2Tax" },
        else: {
          kind: "if",
          cond: { kind: "cmp", op: "le", left: ruleRef30("us.federal.agi"), right: { kind: "param", name: "tier3Max" } },
          then: { kind: "param", name: "tier3Tax" },
          else: {
            kind: "if",
            cond: { kind: "cmp", op: "le", left: ruleRef30("us.federal.agi"), right: { kind: "param", name: "tier4Max" } },
            then: { kind: "param", name: "tier4Tax" },
            else: {
              kind: "if",
              cond: { kind: "cmp", op: "le", left: ruleRef30("us.federal.agi"), right: { kind: "param", name: "tier5Max" } },
              then: { kind: "param", name: "tier5Tax" },
              else: {
                kind: "if",
                cond: { kind: "cmp", op: "le", left: ruleRef30("us.federal.agi"), right: { kind: "param", name: "tier6Max" } },
                then: { kind: "param", name: "tier6Tax" },
                else: {
                  kind: "if",
                  cond: { kind: "cmp", op: "le", left: ruleRef30("us.federal.agi"), right: { kind: "param", name: "tier7Max" } },
                  then: { kind: "param", name: "tier7Tax" },
                  else: {
                    kind: "roundToDollar",
                    value: { kind: "mulRate", base: ruleRef30("us.federal.agi"), rate: { num: "5", den: "10000" }, round: "half-up" },
                    mode: "half-up"
                  }
                }
              }
            }
          }
        }
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/state-va.js
var vaRules = [
  {
    id: "us.va.income_tax",
    version: 3,
    // v1 used the raw rate schedule with half-up rounding; v2 encodes the VA Tax Table convention; v3 bounds the table at its printed $100,000 ceiling — at/above it the rate schedule (half-up) applies per the form's own method instruction
    jurisdiction: "us.va",
    title: "Virginia income tax \u2014 Va. Code \xA7 58.1-320 via the VA Tax Table convention (Form 760 line 16)",
    citation: {
      source: "Va. Code \xA7 58.1-320; 2025 Form 760 instructions (tax.virginia.gov PDF)",
      section: "\xA7 58.1-320; Form 760 line 16",
      url: "https://www.tax.virginia.gov/sites/default/files/vatax-pdf/2025-760-instructions.pdf",
      excerpt: "2% on the first $3,000; 3% over $3,000 to $5,000; 5% over $5,000 to $17,000; 5.75% over $17,000 (Va. Code \xA7 58.1-320, single schedule for all statuses). THE TABLE CONVENTION (this rule's default): the published VA Tax Table's rows are back-solved from clean whole-dollar tax increments \u2014 $2 per row above $5,000 on an ODD-dollar grid \u2014 so table tax = the nearest odd dollar to the rate-schedule value (ties up); verified against the printed row 44,026\u201344,061 \u2192 $2,275 (2024/2023 booklets; the 2025 booklet points to the same online table). useFormulaMethod=true gives the raw schedule with half-up rounding instead (the 2025 instructions' own $90,000 example: 4,917.50 \u2192 4,918). Related 2025 parameters (2025 Form 760 instructions, web-verified July 2026): standard deduction $8,750 single/MFS / $17,500 MFJ (TY2025\u20132026, see us.va.standard_deduction for the enacted TY2027+ windows); exemption $930/person + $800 each age-65-or-blind (taxpayer/spouse only); AGE DEDUCTION (Va. Code \xA7 58.1-322.03(2)): $12,000 per person 65+, UNREDUCED if born on/before 1/1/1939, else reduced $1-for-$1 by AFAGI over $50,000 single / $75,000 married (joint AFAGI); Credit for Low Income Individuals: $300 \xD7 (personal + dependent exemptions, EXCLUDING 65/blind extras), nonrefundable, gated on family VAGI \u2264 poverty guideline (2025: $15,650 for 1 + $5,500 each additional person) \u2014 mutually exclusive with the Virginia EITC; Virginia EITC 2025: 20% of federal EITC, REFUNDABLE at 20% for TY2025\u20132029 (Va. Code \xA7 58.1-339.8, as amended by the 2026 Appropriation Act \u2014 the refundable 20% rate was extended through TY2029, superseding the earlier TY2026 sunset; do not use the stale 15% or a TY2026 cutoff) or nonrefundable 20%. VA has no HOH status: federal HOH files as single-column. NONRESIDENT/PART-YEAR RETURNS: Form 763 (nonresident) and 760PY (part-year) compute Virginia-source-income allocation on their own worksheets \u2014 NOT modeled here (refuses/agent-composed); this target is the RESIDENT Form 760 computation on already-determined Virginia taxable income only. [Input: Virginia taxable income (Form 760 line 15); worked examples: tax(44,060) = 2,275 (table); tax(34,030) = 1,699 (both methods agree).]"
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      standardDeductionSingle: { value: "875000", type: "money" },
      // $8,750
      standardDeductionJoint: { value: "1750000", type: "money" },
      // $17,500
      exemptionPerPerson: { value: "93000", type: "money" },
      // $930
      exemptionAge65OrBlind: { value: "80000", type: "money" },
      // $800
      ageDeductionBase: { value: "1200000", type: "money" },
      // $12,000
      ageDeductionThresholdSingle: { value: "5000000", type: "money" },
      // $50,000 AFAGI
      ageDeductionThresholdMarried: { value: "7500000", type: "money" },
      // $75,000 AFAGI
      lowIncomeCreditPerExemption: { value: "30000", type: "money" },
      // $300
      povertyGuidelineFirstPerson: { value: "1565000", type: "money" },
      // $15,650 (2025)
      povertyGuidelinePerAdditional: { value: "550000", type: "money" },
      // $5,500
      eicPctOfFederal: { value: "20", type: "int" },
      eicRefundablePctOfFederal: { value: "20", type: "int" }
      // TY2025-2029 (was 15%, extended by 2026 Appropriation Act)
    },
    formula: {
      kind: "if",
      cond: fact36("useFormulaMethod"),
      then: {
        kind: "roundToDollar",
        value: {
          kind: "brackets",
          base: { kind: "max0", arg: fact36("stateTaxableIncome") },
          table: [
            { threshold: "0", rate: { num: "2", den: "100" } },
            { threshold: "300000", rate: { num: "3", den: "100" } },
            { threshold: "500000", rate: { num: "5", den: "100" } },
            { threshold: "1700000", rate: { num: "575", den: "10000" } }
          ]
        },
        mode: "half-up"
      },
      else: {
        kind: "if",
        // $1-step table rows below $5,000 taxable income: nearest dollar
        cond: {
          kind: "cmp",
          op: "le",
          left: { kind: "max0", arg: fact36("stateTaxableIncome") },
          right: { kind: "money", cents: "500000" }
        },
        then: {
          kind: "roundToDollar",
          value: {
            kind: "brackets",
            base: { kind: "max0", arg: fact36("stateTaxableIncome") },
            table: [
              { threshold: "0", rate: { num: "2", den: "100" } },
              { threshold: "300000", rate: { num: "3", den: "100" } }
            ]
          },
          mode: "half-up"
        },
        // the printed VA Tax Table ends at $100,000 taxable income — at or
        // above it the form's own instruction is the RATE SCHEDULE, exact and
        // rounded to the nearest dollar (worked example: 440,857 →
        // 25,092, the half-up schedule value, NOT the $2-grid's 25,091)
        else: {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "ge",
            left: { kind: "max0", arg: fact36("stateTaxableIncome") },
            right: { kind: "money", cents: "10000000" }
            // $100,000
          },
          then: {
            kind: "roundToDollar",
            value: {
              kind: "brackets",
              base: { kind: "max0", arg: fact36("stateTaxableIncome") },
              table: [
                { threshold: "0", rate: { num: "2", den: "100" } },
                { threshold: "300000", rate: { num: "3", den: "100" } },
                { threshold: "500000", rate: { num: "5", den: "100" } },
                { threshold: "1700000", rate: { num: "575", den: "10000" } }
              ]
            },
            mode: "half-up"
          },
          // $2-step table rows $5,000-$100,000 on the odd-dollar grid: T = 2·⌊u/2⌋ + 1
          else: {
            kind: "add",
            args: [
              {
                kind: "mulInt",
                base: { kind: "money", cents: "200" },
                // $2
                count: {
                  kind: "stepUnits",
                  value: {
                    kind: "brackets",
                    base: { kind: "max0", arg: fact36("stateTaxableIncome") },
                    table: [
                      { threshold: "0", rate: { num: "2", den: "100" } },
                      { threshold: "300000", rate: { num: "3", den: "100" } },
                      { threshold: "500000", rate: { num: "5", den: "100" } },
                      { threshold: "1700000", rate: { num: "575", den: "10000" } }
                    ]
                  },
                  unitCents: "200",
                  // $2 grid
                  mode: "floor"
                }
              },
              { kind: "money", cents: "100" }
              // + $1 → odd-dollar grid
            ]
          }
        }
      }
    }
  },
  {
    id: "us.va.subtractions",
    version: 1,
    jurisdiction: "us.va",
    title: "Virginia Form 760 subtractions (Va. Code \xA7 58.1-322.02) \u2014 state refund, US govt interest, Social Security, 529, military, National Guard",
    citation: {
      source: "Va. Code \xA7 58.1-322.02; 2025 Form 760 instructions",
      section: "\xA7 58.1-322.02; Form 760 lines 6-8",
      url: "https://www.tax.virginia.gov/sites/default/files/vatax-pdf/2025-760-instructions.pdf",
      excerpt: "2025 Virginia subtractions (\xA7 58.1-322.02, web-verified July 2026): state income tax refund (included in federal AGI, not taxable to VA); interest on US government obligations (Treasury bills/notes/bonds \u2014 federal law bars state taxation); Social Security and Tier 1 railroad retirement benefits, FULLY (\xA7 58.1-322.02(1) \u2014 VA does not conform to the federal 0/50/85% inclusion, all of it is subtracted); Virginia529/ABLE contributions up to $4,000 PER ACCOUNT per year (excess carries forward indefinitely), UNLIMITED (no $4,000 cap) if the account owner is age 70 or older; military benefits subtraction up to $40,000 (2025 and later \u2014 the age-55-or-older gate that applied through TY2023 was repealed; available regardless of age); Virginia National Guard income subtraction = the LESSER of (a) basic pay for up to 39 calendar days of service, or (b) $5,500, available only to a member whose grade is O-6 (colonel) or below. Parameters-only: agent-transcribed onto Form 760 lines 6-8 (Schedule ADJ where applicable) from the federal-return line references above."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      section529PerAccount: { value: "400000", type: "money" },
      // $4,000/account (unlimited if owner 70+)
      militaryBenefitsMax: { value: "4000000", type: "money" },
      // $40,000 (2025+, no age gate)
      nationalGuardMaxDays: { value: "39", type: "int" },
      nationalGuardMax: { value: "550000", type: "money" }
      // $5,500
    },
    formula: {
      kind: "unsupported",
      reason: "parameters-only rule: VA Form 760 subtractions (state refund, US govt interest, Social Security, 529, military, National Guard) are agent-transcribed per the citation above \u2014 no formula to evaluate; use lookup_tax_parameter"
    }
  },
  {
    id: "us.va.spouse_tax_adjustment",
    version: 2,
    // v1 stated only the gating conditions and the $259 cap, disclosing the per-spouse allocation as agent-composed/form-published with no further prescription; v2 carries the COMPLETE verbatim worksheet (Parts 1-2 of the STA worksheet, the Separate-VAGI worksheet steps 1-2, and the age-deduction 50/50 split rule) so the allocation is fully prescribed income-source attribution, not an agent-invented ratio — 2025 Form 760 instructions p.12-13, web-verified July 2026
    jurisdiction: "us.va",
    title: "Virginia Spouse Tax Adjustment (Va. Code \xA7 58.1-320, STA worksheet) \u2014 up to $259, MFJ only",
    citation: {
      source: "Va. Code \xA7 58.1-320; 2025 Form 760 instructions, Spouse Tax Adjustment worksheet (p.12-13)",
      section: "Form 760 line 17",
      url: "https://www.tax.virginia.gov/sites/default/files/vatax-pdf/2025-760-instructions.pdf",
      excerpt: "2025 Spouse Tax Adjustment (STA): available ONLY on a joint (MFJ) return where BOTH spouses have income and their COMBINED Virginia taxable income exceeds $3,000; maximum credit $259 (footer statement + Line 5 shortcut, both confirmed in the TY2025 booklet). NARRATIVE (p.13, verbatim): 'Couples filing jointly under Filing Status 2 may reduce their tax by up to $259 with the STA if both have taxable income to report and their combined taxable income on Line 15 is more than $3,000 ... Using the STA, couples filing joint returns will not pay higher taxes than if they had filed separate returns.' HOW IT WORKS (verbatim): 'Virginia tax rates increase with income: 2% up to $3,000; 3% from $3,001 to $5,000; 5% from $5,001 to $17,000; and 5.75% for income over $17,000. The STA lets both incomes reported on jointly filed returns benefit from the lower tax rates.'\n\nTHE ALLOCATION IS PRESCRIBED, NOT DISCRETIONARY \u2014 built on income-SOURCE attribution, never a proportional wage-ratio or a benefit-maximizing choice. VERBATIM WORKSHEET, PART 1: SEPARATE YOUR INCOME AND EXEMPTIONS (per spouse): '1. Enter the portion of the Virginia Adjusted Gross Income (VAGI) on Line 9 of Form 760 that is related to each spouse. Use the worksheet at the bottom of the page to compute the separate VAGI for each spouse.' '2. Enter separate personal exemption amounts. Enter a 1 in the boxes that apply and multiply the total by $800. Add $930 to the total to compute the personal exemptions for you and spouse. You: 65 or over + Blind = Total x $800 + $930 = ___. Spouse: 65 or over + Blind = Total x $800 + $930 = ___.' (i.e., PER SPOUSE: $930 + $800 x that spouse's own 65-or-over/blind box count \u2014 a fixed formula, never proportional to income.) '3. Subtract Line 2 from Line 1. If either amount is 0 or less, stop here; you do not qualify for this credit.'\n\nVERBATIM WORKSHEET, PART 2: CALCULATE YOUR TAX ADJUSTMENT: '4. Enter the taxable income from Line 15 on Form 760.' '5. Enter the smaller amount from Line 3 above. If this amount is larger than $17,000 and Line 4 is larger than $34,000, skip to Line 12 and enter $259 as the credit.' '6. Subtract Line 5 from Line 4 (if $0 or less, enter $0).' '7. Divide the amount on Line 4 by 2.' '8. Enter the tax on the smaller amount from Line 5 or Line 7. Refer to the tax table or rate schedule.' '9. Enter the tax on the larger amount from Line 6 or Line 7. Refer to the tax table or rate schedule.' (Lines 8-9 use us.va.income_tax's own rate schedule/table on the stated amount \u2014 NOT a fresh computation.) '10. Add Lines 8 and 9.' '11. Enter the tax from Line 16 on Form 760.' '12. TAX ADJUSTMENT: Subtract Line 10 from Line 11. Enter this amount on Line 17 of Form 760.' 'The Spouse Tax Adjustment cannot exceed $259.'\n\nVERBATIM 'WORKSHEET FOR DETERMINING SEPARATE VIRGINIA ADJUSTED GROSS INCOME' (bottom of p.12) \u2014 feeds Part 1 Line 1 above: STEP 1 - Determine Separate Federal Adjusted Gross Income (You / Spouse columns, LITERAL income-source attribution \u2014 each spouse's OWN items, never a ratio): '1. Wages, salaries, etc. 2. Taxable interest and dividend income. 3. Taxable refunds, adjustments, or offsets of state and local income tax. 4. Business income. 5. Capital gains/losses and other gains/losses. 6. Taxable pensions, annuities, and IRA distributions. 7. Rents, royalties, partnerships, estates, trusts, etc. 8. Other income (farm income, taxable social security, etc.). 9. Gross income - add Lines 1 through 8. 10. Adjustments to gross income. 11. FAGI - subtract Line 10 from Line 9. (The total of both columns should equal your joint FAGI reported on your 1040)' STEP 2 - Determine Separate Virginia Adjusted Gross Income: '12. Total additions to FAGI (Form 760, Line 2). 13. Subtotal - add Lines 11 and 12. 14. Age Deduction (Form 760, Line 4). 15. Social Security Act and Tier 1 Railroad Retirement Act Benefits (Form 760, Line 5). 16. State income tax refund or overpayment credit reported as income on your federal return (Form 760, Line 6). 17. Other Subtractions (Form 760, Line 7). 18. Total Subtractions from FAGI - add Lines 14, 15, 16, and 17. 19. Subtract Line 18 from Line 13. These are your separate VAGI amounts to be used on Line 1 of the Spouse Tax Adjustment Worksheet. (The total of both columns should equal your combined VAGI reported on Line 9 of your 760)'\n\nAGE DEDUCTION 50/50 SPLIT RULE (feeds Step 2 Line 14 above \u2014 the ONE genuinely joint item on this worksheet, verbatim): 'If both spouses are claiming an income-based age deduction, regardless of whether filing jointly or separately, the married taxpayers must compute a joint age deduction first, then allocate half of the joint deduction to each spouse.' (Compute the joint age deduction once via us.va.income_tax's ageDeductionBase/threshold parameters on COMBINED AFAGI, then split it exactly 50/50 \u2014 not proportional to either spouse's income or age-deduction eligibility share.)\n\nOWNERSHIP ATTRIBUTION FOR STEP 1: a document belongs to the spouse the STRUCTURED INTAKE's ownership tag names (who_applies_to / whose-account fields: taxpayer | spouse | joint) \u2014 the intake tag CONTROLS over the name printed on the PDF (a 1099-B may print only the primary's name yet be intake-tagged joint). taxpayer/spouse-tagged items go WHOLE to that spouse; joint-tagged items (interest, dividends INCLUDING their capital-gain distributions, jointly-tagged 1099-Bs) split 50/50 with the ODD DOLLAR TO THE PRIMARY. Adjustments follow their owner: each spouse's own \xBD-SE-tax (from their own Schedule SE), own educator cap, and the student-loan interest to the named 1098-E borrower. (Worked example: primary = SchC 16,391 + own crypto LT 999 + joint splits 2,778 + 426 + 44 + 425 \u2212 \xBDSE 1,158 \u2212 SL 2,500 \u2212 educator 300 = 17,105; spouse = 55,123; STA = 245.)\n\nSUMMARY OF WHAT IS AND ISN'T DISCRETIONARY: (1) Separate-VAGI Step 1 items are attributed by literal ownership (each spouse's own wages/interest/business/capital-gain/pension/rent/other-income line, cross-checked to equal the joint FAGI). (2) Personal exemptions use the fixed $930+$800-per-box formula, per spouse \u2014 NOT proportional to income. (3) The age deduction (a genuinely joint item) is computed once, jointly, then split EXACTLY 50/50 \u2014 not proportional to either spouse's income or share of the joint deduction. (4) The standard/itemized deduction is NEVER split on this worksheet \u2014 VAGI is computed pre-deduction, and Part 2's Lines 4-12 arithmetic (min/max sequence below) handles the deduction's effect implicitly through the FIXED sequence: Line 5 = smaller of the two Line-3 (exemption-adjusted VAGI) amounts; Line 6 = Line 4 minus Line 5 (floored at 0); Line 7 = Line 4 divided by 2; tax is computed on min(Line 5, Line 7) and max(Line 6, Line 7) via us.va.income_tax and summed (Line 10), then subtracted from the actual joint tax (Line 11 = Form 760 Line 16) for the STA (Line 12), capped at $259. There is NO discretionary maximization step anywhere in this sequence \u2014 given the source-attributed Step-1/Step-2 inputs, the worksheet produces exactly one number. DISCLOSED SCOPE: this rule still does not COMPUTE the worksheet end-to-end (the per-spouse Separate-VAGI Step-1 income-source figures are themselves agent-transcribed facts, not derived by this corpus) \u2014 it now states the COMPLETE prescription (every line, every allocation rule) rather than only the gating conditions and cap, so an agent following it mechanically reaches the one correct number instead of inventing a wage-ratio or benefit-maximizing allocation."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      maxCredit: { value: "25900", type: "money" },
      // $259
      combinedTaxableIncomeFloor: { value: "300000", type: "money" },
      // $3,000
      line5ShortcutThreshold: { value: "1700000", type: "money" },
      // $17,000 (Line 5 shortcut test)
      line4ShortcutThreshold: { value: "3400000", type: "money" },
      // $34,000 (Line 5 shortcut test)
      personalExemptionBase: { value: "93000", type: "money" },
      // $930 (Part 1 Line 2, per spouse)
      personalExemptionPerBox: { value: "80000", type: "money" }
      // $800 per 65-or-over/blind box, per spouse
    },
    formula: {
      kind: "unsupported",
      reason: "Spouse Tax Adjustment worksheet (2025 Form 760 instructions p.12-13): MFJ only, combined VA taxable income > $3,000, credit capped at $259. The full worksheet is now stated verbatim in this rule's citation (Separate-VAGI Steps 1-2 income-source attribution, the $930+$800-per-box exemption formula per spouse, the age-deduction 50/50 split, and the Lines 4-12 min/max tax sequence) \u2014 but the per-spouse Separate-VAGI Step-1 income figures are inherently agent-attested facts (this corpus has no per-spouse income split), so the worksheet is not executed as a formula here; follow the citation's prescription exactly and disclose the per-spouse inputs used."
    }
  },
  {
    id: "us.va.standard_deduction",
    version: 1,
    // TY2025-2026: unchanged from the long-standing $8,750/$17,500 (matches us.va.income_tax's documentation parameters)
    jurisdiction: "us.va",
    title: "Virginia standard deduction by filing status (Va. Code \xA7 58.1-322.03(1)) \u2014 TY2025-2026",
    citation: {
      source: "Va. Code \xA7 58.1-322.03(1); 2025 Form 760 instructions",
      section: "\xA7 58.1-322.03(1); Form 760 line 11",
      url: "https://www.tax.virginia.gov/sites/default/files/vatax-pdf/2025-760-instructions.pdf",
      excerpt: "For taxable years 2025 and 2026, the Virginia standard deduction is $8,750 for single filers/MFS and $17,500 for married filing jointly (VA has no separate HOH column \u2014 federal HOH files the single column). See us.va.standard_deduction v2/v3 for the TY2027+ enacted increases (2026 Appropriation Act)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2027-01-01",
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "or",
        args: [
          { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "mfj" } },
          { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "qss" } }
        ]
      },
      then: { kind: "money", cents: "1750000" },
      // $17,500
      else: { kind: "money", cents: "875000" }
      // $8,750
    }
  },
  {
    id: "us.va.standard_deduction",
    version: 2,
    jurisdiction: "us.va",
    title: "Virginia standard deduction by filing status (Va. Code \xA7 58.1-322.03(1), as amended) \u2014 TY2027 enacted increase",
    citation: {
      source: "Va. Code \xA7 58.1-322.03(1), as amended by the 2026 Appropriation Act (HB30, Sp. Sess. I, c. 1); 2026 Legislative Summary",
      section: "2026 Legislative Summary, 'Standard Deduction' (Item 4-14)",
      url: "https://www.tax.virginia.gov/sites/default/files/inline-files/2026-legislative-summary.pdf",
      excerpt: "The third enactment clause of the 2026 Appropriation Act (House Bill 30, Special Session I, Chapter 1) increases the standard deduction from $8,750 to $9,200 for single filers and from $17,500 to $18,400 for married filers filing jointly, effective for taxable years beginning on and after January 1, 2027, but before January 1, 2028. NOTE: the 2025 Form 760 booklet's prose ('the increased amount sunsets after 2026') is STALE relative to this later enactment \u2014 the statute now controls a TY2027 increase, not a TY2026 sunset. VA has no HOH column (single applies)."
    },
    effectiveFrom: "2027-01-01",
    effectiveTo: "2028-01-01",
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "or",
        args: [
          { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "mfj" } },
          { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "qss" } }
        ]
      },
      then: { kind: "money", cents: "1840000" },
      // $18,400
      else: { kind: "money", cents: "920000" }
      // $9,200
    }
  },
  {
    id: "us.va.standard_deduction",
    version: 3,
    jurisdiction: "us.va",
    title: "Virginia standard deduction by filing status (Va. Code \xA7 58.1-322.03(1), as amended) \u2014 TY2028-2029 enacted increase",
    citation: {
      source: "Va. Code \xA7 58.1-322.03(1), as amended by the 2026 Appropriation Act (HB30, Sp. Sess. I, c. 1); 2026 Legislative Summary",
      section: "2026 Legislative Summary, 'Standard Deduction' (Item 4-14)",
      url: "https://www.tax.virginia.gov/sites/default/files/inline-files/2026-legislative-summary.pdf",
      excerpt: "The same enactment further increases the standard deduction to $9,300 for single filers and $18,600 for married filers filing jointly, effective for taxable years beginning on and after January 1, 2028, but before January 1, 2030. SUNSET (not encoded \u2014 out of this window): the increased amounts are scheduled to sunset after TY2029 and revert to the PRE-2019 standard deduction of $3,000 single / $6,000 MFJ starting TY2030; this rule does not cover TY2030+ (refuses/no applicable version beyond 2030-01-01)."
    },
    effectiveFrom: "2028-01-01",
    effectiveTo: "2030-01-01",
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: {
        kind: "or",
        args: [
          { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "mfj" } },
          { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "qss" } }
        ]
      },
      then: { kind: "money", cents: "1860000" },
      // $18,600
      else: { kind: "money", cents: "930000" }
      // $9,300
    }
  }
];

// ../corpus-us-federal/dist/rules/state-ca.js
var caRules = [
  {
    id: "us.ca.income_tax",
    version: 5,
    // v2 refused MFJ/QSS entirely; v3 encodes Schedule Y per-bracket (not by doubling Schedule X's rounded constants); v4 discloses the Schedule CA social security exclusion alongside unemployment compensation; v5 encodes the FTB Tax Table convention (midpoint of the $100-wide bracket, sub-$100,000) as the default method, per the VA/NY table pattern
    jurisdiction: "us.ca",
    title: "California income tax \u2014 2025 rate schedules X (single/MFS), Y (MFJ/QSS), and Z (HOH), Form 540 line 31",
    citation: {
      source: "2025 California Tax Rate Schedules (FTB); 2025 California Tax Table (FTB 540 booklet); Rev. & Tax. Code \xA7 17041",
      section: "FTB 2025 Schedule X / Schedule Y / Schedule Z; 2025 California Tax Table",
      url: "https://www.ftb.ca.gov/forms/2025/2025-540-tax-rate-schedules.pdf",
      excerpt: `2025 Schedule Z (head of household), FTB: 1% to $22,173; 2% to $52,530; 4% to $67,716; 6% to $83,805; 8% to $98,990; 9.3% to $505,208 \u2014 income above $505,208 REFUSES here (higher 10.3/11.3/12.3% tiers and the 1% MHSA surcharge not yet encoded from source). Schedule X (single/MFS), 2025 FTB, web-verified July 2026: 1% to $11,079; 2% to $26,264; 4% to $41,452; 6% to $57,542; 8% to $72,724; 9.3% above (same $505,208 shared refusal boundary as this rule's HOH branch, applied before differentiating single from HOH) \u2014 corrected in v5 from stale/mistyped thresholds that broke the Schedule-Y-is-2x-Schedule-X identity below. SCHEDULE Y (married filing jointly / qualifying surviving spouse, 2025, FTB): thresholds are EXACTLY 2\xD7 Schedule X's \u2014 $0/$22,158/$52,528/$82,904/$115,084/$145,448/$742,958 at 1/2/4/6/8/9.3/10.3/11.3/12.3% \u2014 encoded PER-BRACKET (marginal accumulation over the doubled thresholds) rather than by doubling Schedule X's own printed (already-rounded) fixed-dollar constants, because independently rounding at each schedule drifts by pennies in the upper brackets; income above $742,958 REFUSES here on the same basis as X/Z (10.3%+ tiers not yet encoded). CALIFORNIA TAX TABLE CONVENTION (2025 FTB 540 booklet, 'California Tax Table', pp.68-75; DEFAULT method for CA taxable income under $100,000, per Form 540/540NR line 19 instructions: 'Read down the column labeled "If Your Taxable Income Is..." to find the range that includes your taxable income... Read across the columns labeled "The Tax For Filing Status"...'): rows are $1-$50 as a SPECIAL FIRST ROW (tax $0 for every filing status), then $100-WIDE brackets thereafter (51-150, 151-250, ..., 99,951-100,000 \u2014 the last row is only $50 wide because the table truncates exactly at $100,000). Table tax = apply the Schedule X/Y/Z marginal formula above at the bracket's MIDPOINT, rounded to the nearest dollar (the SAME midpoint-of-bracket convention already used for the VA and NY tax tables \u2014 decisively confirmed for CA too, not merely assumed). Verified 9-for-9 exact against three incomes \xD7 three filing-status columns (FTB 2025 tax table + rate schedules, web-verified July 2026): row 40,051-40,150 (mid $40,100.50) \u2192 Single $968 / MFJ $580 / HOH $580; row 70,051-70,150 (mid $70,100.50) \u2192 Single $2,992 / MFJ $1,532 / HOH $1,579; row 95,051-95,150 (mid $95,100.50) \u2192 Single $5,283 / MFJ $2,776 / HOH $3,305. The truncated last row (99,951-100,000) uses its OWN true midpoint $99,975.50 (not the phantom $100,000.50 the general $100-bracket formula would otherwise produce) \u2014 verified: Single at that row = $5,736. CA taxable income of $100,000 OR ABOVE uses the RATE SCHEDULE directly (unrounded to the cent, per the schedule's own marginal math) \u2014 the table stops at exactly $100,000. useFormulaMethod=true bypasses the table entirely and uses the raw rate schedule at every income level (matches the pre-v5 default behavior). Related 2025 parameters (FTB, web-verified July 2026): standard deduction $5,706 single/MFS, $11,412 MFJ/HOH/QSS; personal exemption CREDIT $153; dependent exemption credit $475. EXEMPTION CREDIT PHASEOUT (R&TC \xA7 17054(a), (b); 2025 FTB Schedule CA/540 instructions): credits (personal + dependent, but NOT senior) phase out $6 for each $2,500 (or fraction thereof \u2014 the quotient is ROUNDED UP) of federal AGI over $252,203 single/MFS ($1,250 divisor increments instead of $2,500 for MFS \u2014 i.e. the SAME $6 reduction per HALF the increment), $504,411 MFJ/QSS, $378,310 HOH; fully phased out credits are not reduced below $0. [Input: California taxable income (Form 540 line 19). SCHEDULE CA (2025 instructions, line 7): California EXCLUDES unemployment compensation (and CA PFL benefits) from taxable income \u2014 subtract it from federal AGI when composing CA AGI. SCHEDULE CA (2025 instructions, line 6, column B): California ALSO EXCLUDES social security benefits \u2014 the federally-taxable amount (the \xA7 86 includible amount, us.federal.taxable_social_security) is likewise subtracted from federal AGI when composing CA AGI. CalEITC \u2192 us.ca.caleitc; Young Child Tax Credit \u2192 us.ca.yctc; renter's credit \u2192 us.ca.renters_credit; CDCC \u2192 us.ca.cdcc; misc. parameters (use tax, SDI) \u2192 us.ca.parameters.]`
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      standardDeductionSingleMfs: { value: "570600", type: "money" },
      // $5,706
      standardDeductionJointHohQss: { value: "1141200", type: "money" },
      // $11,412
      personalExemptionCredit: { value: "15300", type: "money" },
      // $153
      dependentExemptionCredit: { value: "47500", type: "money" },
      // $475
      exemptionPhaseoutStep: { value: "600", type: "money" },
      // $6 reduction
      exemptionPhaseoutIncrementSingleMfs: { value: "125000", type: "money" },
      // $1,250 (MFS uses this half-increment)
      exemptionPhaseoutIncrementOther: { value: "250000", type: "money" },
      // $2,500
      exemptionPhaseoutThresholdSingleMfs: { value: "25220300", type: "money" },
      // $252,203
      exemptionPhaseoutThresholdMfj: { value: "50441100", type: "money" },
      // $504,411
      exemptionPhaseoutThresholdHoh: { value: "37831000", type: "money" },
      // $378,310
      tableThreshold: { value: "10000000", type: "money" },
      // $100,000 — table below, schedule at/above
      tableFirstRowCeiling: { value: "5000", type: "money" },
      // $50 — special first row, tax $0
      tableLastRowFloor: { value: "9995100", type: "money" },
      // $99,951 — start of the truncated last row
      tableLastRowMidpoint: { value: "9997550", type: "money" }
      // $99,975.50 — the truncated row's own true midpoint
    },
    formula: (() => {
      const scheduleFor = (base2) => ({
        kind: "if",
        cond: { kind: "or", args: [isStatus10("mfj"), isStatus10("qss")] },
        then: {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "gt",
            left: base2,
            right: money33("74295800")
            // $742,958 — Schedule Y's 9.3% band ends here
          },
          then: {
            kind: "unsupported",
            reason: "CA taxable income above $742,958 (Schedule Y) reaches the 10.3%+ tiers and the 1% MHSA surcharge, not yet encoded from the FTB schedule \u2014 compute yourself and disclose"
          },
          else: {
            // Schedule Y: MFJ & QSS (2025) — thresholds exactly 2x Schedule X, encoded per-bracket
            kind: "brackets",
            base: { kind: "max0", arg: base2 },
            table: [
              { threshold: "0", rate: { num: "1", den: "100" } },
              { threshold: "2215800", rate: { num: "2", den: "100" } },
              { threshold: "5252800", rate: { num: "4", den: "100" } },
              { threshold: "8290400", rate: { num: "6", den: "100" } },
              { threshold: "11508400", rate: { num: "8", den: "100" } },
              { threshold: "14544800", rate: { num: "93", den: "1000" } }
            ]
          }
        },
        else: {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "gt",
            left: base2,
            right: money33("50520800")
            // $505,208 — encoded bands end here
          },
          then: {
            kind: "unsupported",
            reason: "CA taxable income above $505,208 reaches the 10.3%+ tiers and the 1% MHSA surcharge, not yet encoded from the FTB schedule \u2014 compute yourself and disclose"
          },
          else: {
            kind: "if",
            cond: isStatus10("hoh"),
            then: {
              kind: "brackets",
              base: { kind: "max0", arg: base2 },
              table: [
                { threshold: "0", rate: { num: "1", den: "100" } },
                { threshold: "2217300", rate: { num: "2", den: "100" } },
                { threshold: "5253000", rate: { num: "4", den: "100" } },
                { threshold: "6771600", rate: { num: "6", den: "100" } },
                { threshold: "8380500", rate: { num: "8", den: "100" } },
                { threshold: "9899000", rate: { num: "93", den: "1000" } }
              ]
            },
            else: {
              // Schedule X: single & MFS (2025). Thresholds corrected in v5 to
              // $11,079/$26,264/$41,452/$57,542/$72,724 (2025 FTB Schedule X,
              // web-verified July 2026; these are EXACTLY half of Schedule
              // Y's already-correct $22,158/$52,528/$82,904/$115,084/$145,448
              // — the pre-v5 table used stale/mistyped thresholds that were
              // NOT half of Y's, silently breaking the "Y = 2x X" identity
              // this rule's own citation has always claimed).
              kind: "brackets",
              base: { kind: "max0", arg: base2 },
              table: [
                { threshold: "0", rate: { num: "1", den: "100" } },
                { threshold: "1107900", rate: { num: "2", den: "100" } },
                { threshold: "2626400", rate: { num: "4", den: "100" } },
                { threshold: "4145200", rate: { num: "6", den: "100" } },
                { threshold: "5754200", rate: { num: "8", den: "100" } },
                { threshold: "7272400", rate: { num: "93", den: "1000" } }
              ]
            }
          }
        }
      });
      const base = { kind: "max0", arg: fact36("stateTaxableIncome") };
      const lower = {
        kind: "add",
        args: [
          money33("5100"),
          // $51
          {
            kind: "mulInt",
            base: money33("10000"),
            // $100
            count: {
              kind: "stepUnits",
              value: { kind: "sub", left: base, right: money33("5100") },
              unitCents: "10000",
              mode: "floor"
            }
          }
        ]
      };
      const generalMidpoint = { kind: "add", args: [lower, money33("4950")] };
      const midpoint = {
        kind: "if",
        cond: {
          kind: "cmp",
          op: "gt",
          left: base,
          right: { kind: "param", name: "tableLastRowFloor" }
          // > $99,951
        },
        then: { kind: "param", name: "tableLastRowMidpoint" },
        // $99,975.50
        else: generalMidpoint
      };
      return {
        kind: "if",
        cond: {
          kind: "or",
          args: [
            fact36("useFormulaMethod"),
            {
              kind: "cmp",
              op: "ge",
              left: base,
              right: { kind: "param", name: "tableThreshold" }
              // >= $100,000
            }
          ]
        },
        then: scheduleFor(base),
        else: {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "le",
            left: base,
            right: { kind: "param", name: "tableFirstRowCeiling" }
            // <= $50
          },
          then: money33("0"),
          else: { kind: "roundToDollar", value: scheduleFor(midpoint), mode: "half-up" }
        }
      };
    })()
  },
  {
    id: "us.ca.caleitc",
    version: 5,
    // v3's phase-in kept the floor-aligned midpoint, misplacing $50-multiple incomes into the next row; v4 aligns the phase-in to the table's true upper-bound rows (mid = ceil(x/50)*50 - 24.50), matching the post-peak segments; v5 recalibrates the 0-kid GENTLE phase-out anchor from an unverified construction ($216 @ mid $9,475.50, rate -43/4680, "unverified-but-consistent") to a printed-table-confirmed anchor ($183 @ mid $12,975.50 = bracket 12,951-13,000, rate -9/1000 = -0.9%/$1) — the prior anchor produced $184 for that row, one dollar off the 2025 FTB 3514 booklet's printed credit
    jurisdiction: "us.ca",
    title: "California Earned Income Tax Credit (R&TC \xA7 17052, FTB 3514) \u2014 2025, full range",
    citation: {
      source: "R&TC \xA7 17052; 2025 FTB 3514 booklet (EITC table + General Information)",
      section: "R&TC \xA7 17052",
      url: "https://www.ftb.ca.gov/forms/2025/2025-3514-booklet.pdf",
      excerpt: "2025 CalEITC: income ceiling $32,900 (all family sizes, both earned income AND federal AGI must be under $32,901 or the credit is $0); maximum credit $302 (0 kids, peak bracket $4,651-$4,700) / $2,016 (1 kid, peak $6,951-$7,000) / $3,339 (2 kids, peak $9,801-$9,850) / $3,756 (3+ kids, same $9,801-$9,850 peak). DECODED STRUCTURE (July 2026 web-verification: the printed table was parsed in full, all 658 $50-wide brackets from $1-$32,900, `pdftotext -layout`, and formula-fit): no plateau \u2014 every column peaks at a single $50 bracket and immediately begins declining. The decline is NOT one straight line: it has a KINK where the phase-out slope changes from steep to gentle, at 0 kids $5,450/$5,451, 1 kid $11,800/$11,801, 2 kids $17,800/$17,801, 3+ kids $18,000/$18,001 \u2014 confirmed by row-to-row deltas dropping from roughly $17-19 per $50 bracket (steep) to roughly $2-3 per bracket (gentle) exactly at each boundary. THIS RULE NOW COMPUTES ALL THREE SEGMENTS: (1) PHASE-IN (unchanged from v1): credit = round(bracket-midpoint \xD7 combined rate), combined rate = federal \xA7 32(b) phase-in rate \xD7 the 85% \xA7 17052(c) adjustment factor (7.65/34/40/45% \xD7 85% = 6.5025/28.90/34.00/38.25%), using the floor-aligned $50-bracket midpoint \u2014 verified against the printed row 2,201-2,250 \u2192 851 for 3+ children (2,225.50 \xD7 45% \xD7 85% = 851.25 \u2192 851). (2) STEEP PHASE-OUT (peak to kink): the printed table declines at (within rounding) the SAME rate magnitude as the phase-in line (a symmetric V through the peak) \u2014 credit = round(anchor value \u2212 combined phase-in rate \xD7 (bracket-midpoint \u2212 anchor-bracket-midpoint)), using the TRUE (upper-bound-aligned) $50-bracket midpoint. For 1/2/3+ kids the line is anchored to an actual printed table row (1 kid: bracket 9,451-9,500, $1,306; 2 kids and 3+ kids: bracket 9,951-10,000, $3,288/$3,699) rather than the theoretical peak itself \u2014 the peak POINT sits fractionally off this line for 1 kid specifically (a peak-anchored construction understates by roughly $13 there), so a real table row is used everywhere data exists. Verified EXACT against every printed row in this zone for 1/2/3+ kids (e.g. 2 kids: $10,000 \u2192 3,288 exact, $16,500 \u2192 1,078 exact, $17,500 \u2192 738 exact; 3+ kids: $16,500 \u2192 1,213 exact; 1 kid: $9,500 \u2192 1,306 exact, $11,000 \u2192 873 exact). (3) GENTLE PHASE-OUT (kink to ceiling): a SEPARATE, shallower line anchored to a printed table row just past the kink, at the independently-fit rate (0 kids = \u22120.9%/$1 i.e. \u22129/1000 per $1 of midpoint, CONFIRMED against the printed 2025 FTB 3514 booklet row $12,951-$13,000 \u2192 $183 exact, web-verified July 2026 \u2014 corrects a prior unverified anchor that produced $184 for that row; 1 kid \u2248 \u22123.01%/$1 i.e. \u2212541/18000, 2 kids \u2248 \u22124.19%/$1 i.e. \u221225/596, 3+ kids \u2248 \u22124.26%/$1 i.e. \u2212613/14400) \u2014 verified within \u2264$1 across every printed row from the kink to $32,900 (e.g. 0 kids: $17,000 \u2192 147 exact; 2 kids: $18,000 \u2192 626 exact, $25,000 \u2192 333 vs. computed 332, $30,000 \u2192 123 vs. computed 123). Max residual across all 658 rows, all four columns: \u2264 $2 pre-rounding (a single pre-existing phase-in-region anomaly just below the 2/3+-kid peak, at $9,500, runs to $17-19 \u2014 inherited from the v1 floor-aligned phase-in formula, unchanged here per the phase-in region's own design, and confined to that one boundary row). The 0-kid STEEP segment (peak to $5,450) has no printed table data in the range this session could source ($9,500+) to verify against; it uses the peak-anchor construction (disclosed as unverified-but-consistent with the confirmed symmetric-V structure elsewhere) \u2014 only the GENTLE segment's 0-kid anchor is table-confirmed as of v5. Earned income here = wages (line 1z, INCLUDING any taxable dependent-care benefits on line 1e) + max(0, SE profit \u2212 \xBDSE tax) \u2212 Schedule C loss, same composition as the federal \xA7 32(c)(2) input. CAUTION: when a spouse's own Schedule C loss makes that spouse's Form 2441 earned income zero or negative, the \xA7 129 exclusion for W-2 box 10 dependent care benefits is $0 \u2014 the FULL box-10 amount becomes taxable wages (line 1e), raising both federal AGI and this credit's earned income (federal earned 10,500 + 6,000 fully-taxable DCB \u2192 CA earned 16,500 \u2192 credit 1,078 from the printed row). Kids = qualifyingChildren + eitcAdditionalQualifyingChildren."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      ceiling: { value: "3290000", type: "money" },
      // $32,900
      max0kids: { value: "30200", type: "money" },
      // $302 (also the steep segment's peak anchor)
      max1kid: { value: "201600", type: "money" },
      // $2,016
      max2kids: { value: "333900", type: "money" },
      // $3,339
      max3kids: { value: "375600", type: "money" },
      // $3,756
      adjustmentFactorPct: { value: "85", type: "int" },
      peakUpper0kids: { value: "470000", type: "money" },
      // $4,700 (last dollar of the peak $50-bracket)
      peakUpper1kid: { value: "700000", type: "money" },
      // $7,000
      peakUpper2kids: { value: "985000", type: "money" },
      // $9,850
      peakUpper3kids: { value: "985000", type: "money" },
      // $9,850 (same bracket as 2 kids)
      peakMid0kids: { value: "467550", type: "money" },
      // $4,675.50 true midpoint of $4,651-$4,700
      peakMid1kid: { value: "697550", type: "money" },
      // $6,975.50 true midpoint of $6,951-$7,000
      peakMid2kids3kids: { value: "982550", type: "money" },
      // $9,825.50 true midpoint of $9,801-$9,850
      // STEEP segment anchors: a printed table row's (true bracket midpoint,
      // credit) pair, NOT the theoretical peak — for 1/2/3+ kids the peak
      // point itself sits fractionally off the steep line's true regression
      // (verified: the peak-anchor construction is off by ~$13 for 1 kid),
      // so the steep line is anchored to real table data instead. 0 kids has
      // no printed steep-zone data in the sourced range and keeps the
      // peak-anchor (peakMid0kids/max0kids) construction, disclosed above.
      steepAnchorMid1kid: { value: "947550", type: "money" },
      // bracket 9,451-9,500, mid $9,475.50
      steepAnchorValue1kid: { value: "130600", type: "money" },
      // $1,306 printed
      steepAnchorMid2kids: { value: "997550", type: "money" },
      // bracket 9,951-10,000, mid $9,975.50
      steepAnchorValue2kids: { value: "328800", type: "money" },
      // $3,288 printed
      steepAnchorMid3kids: { value: "997550", type: "money" },
      // bracket 9,951-10,000, mid $9,975.50
      steepAnchorValue3kids: { value: "369900", type: "money" },
      // $3,699 printed
      kinkUpper0kids: { value: "545000", type: "money" },
      // $5,450 (last dollar of the last steep bracket)
      kinkUpper1kid: { value: "1180000", type: "money" },
      // $11,800
      kinkUpper2kids: { value: "1780000", type: "money" },
      // $17,800
      kinkUpper3kids: { value: "1800000", type: "money" },
      // $18,000
      // gentle-segment anchor: (true bracket midpoint, printed credit) of one
      // printed row just past the kink, used to continue the gentle line
      gentleAnchorMid0kids: { value: "1297550", type: "money" },
      // bracket 12,951-13,000, mid $12,975.50 (2025 FTB 3514 booklet, confirmed printed row)
      gentleAnchorValue0kids: { value: "18300", type: "money" },
      // $183 printed (confirmed row 12,951-13,000)
      gentleAnchorMid1kid: { value: "1197550", type: "money" },
      // bracket 11,951-12,000, mid $11,975.50
      gentleAnchorValue1kid: { value: "62900", type: "money" },
      // $629 printed
      gentleAnchorMid2kids: { value: "1797550", type: "money" },
      // bracket 17,951-18,000, mid $17,975.50
      gentleAnchorValue2kids: { value: "62600", type: "money" },
      // $626 printed
      gentleAnchorMid3kids: { value: "1847550", type: "money" },
      // bracket 18,451-18,500, mid $18,475.50
      gentleAnchorValue3kids: { value: "61400", type: "money" }
      // $614 printed
    },
    formula: (() => {
      const earned2 = {
        kind: "max0",
        arg: {
          kind: "sub",
          left: {
            kind: "add",
            args: [
              fact36("wages"),
              {
                kind: "max0",
                arg: {
                  kind: "sub",
                  left: fact36("selfEmploymentNetProfit"),
                  right: { kind: "rule", ruleId: "us.federal.se_tax_half_deduction" }
                }
              }
            ]
          },
          right: fact36("scheduleCNetLoss")
        }
      };
      const midCeil = {
        kind: "sub",
        left: {
          kind: "mulInt",
          base: money33("5000"),
          count: { kind: "stepUnits", value: earned2, unitCents: "5000", mode: "ceil" }
        },
        right: money33("2450")
        // $24.50
      };
      const kids = {
        kind: "add",
        args: [fact36("qualifyingChildren"), fact36("eitcAdditionalQualifyingChildren")]
      };
      const kidsEq = (n) => ({
        kind: "cmp",
        op: "eq",
        left: kids,
        right: { kind: "int", value: n }
      });
      const tentative = (num, den) => ({
        kind: "roundToDollar",
        value: {
          kind: "mulRate",
          // v4: the FTB table's rows are upper-bound-aligned everywhere —
          // "at least X, but not over Y" with Y a multiple of $50 — so the
          // phase-in uses the same TRUE midpoint as the post-peak segments
          // (the v2/v3 floor-aligned midpoint misplaced $50-multiple incomes
          // into the NEXT row: earned 5,500 belongs to 5,451-5,500, mid
          // 5,475.50, not 5,501-5,550). max0 guards the earned=0 corner.
          base: { kind: "max0", arg: midCeil },
          rate: { num, den },
          round: "half-up"
        },
        mode: "half-up"
      });
      const linear = (anchorMid, anchorValue, num, den) => ({
        kind: "roundToDollar",
        value: {
          kind: "add",
          args: [
            anchorValue,
            {
              kind: "mulRate",
              base: { kind: "sub", left: midCeil, right: anchorMid },
              rate: { num, den },
              round: "half-up"
            }
          ]
        },
        mode: "half-up"
      });
      const param22 = (name) => ({ kind: "param", name });
      const segment = (peakUpperParam, kinkUpperParam, phaseIn, steep, gentle) => ({
        kind: "if",
        cond: { kind: "cmp", op: "le", left: earned2, right: param22(peakUpperParam) },
        then: phaseIn,
        else: {
          kind: "if",
          cond: { kind: "cmp", op: "le", left: earned2, right: param22(kinkUpperParam) },
          then: steep,
          else: gentle
        }
      });
      const col0 = segment(
        "peakUpper0kids",
        "kinkUpper0kids",
        tentative("65025", "1000000"),
        // 7.65% × 85%
        linear(param22("peakMid0kids"), param22("max0kids"), "-2601", "40000"),
        // mirror of the phase-in rate
        linear(param22("gentleAnchorMid0kids"), param22("gentleAnchorValue0kids"), "-9", "1000")
      );
      const col1 = segment(
        "peakUpper1kid",
        "kinkUpper1kid",
        tentative("2890", "10000"),
        // 34% × 85%
        linear(param22("steepAnchorMid1kid"), param22("steepAnchorValue1kid"), "-289", "1000"),
        linear(param22("gentleAnchorMid1kid"), param22("gentleAnchorValue1kid"), "-541", "18000")
      );
      const col2 = segment(
        "peakUpper2kids",
        "kinkUpper2kids",
        tentative("3400", "10000"),
        // 40% × 85%
        linear(param22("steepAnchorMid2kids"), param22("steepAnchorValue2kids"), "-17", "50"),
        linear(param22("gentleAnchorMid2kids"), param22("gentleAnchorValue2kids"), "-25", "596")
      );
      const col3 = segment(
        "peakUpper3kids",
        "kinkUpper3kids",
        tentative("3825", "10000"),
        // 45% × 85%
        linear(param22("steepAnchorMid3kids"), param22("steepAnchorValue3kids"), "-153", "400"),
        linear(param22("gentleAnchorMid3kids"), param22("gentleAnchorValue3kids"), "-613", "14400")
      );
      return {
        kind: "if",
        // BOTH earned income AND federal AGI must be under the ceiling
        // (R&TC § 17052 / FTB 3514: "Both your earned income and federal AGI
        // must be less than $32,901") — either at/over the ceiling → $0
        cond: {
          kind: "or",
          args: [
            {
              kind: "cmp",
              op: "ge",
              left: earned2,
              right: { kind: "param", name: "ceiling" }
            },
            {
              kind: "cmp",
              op: "ge",
              left: ruleRef30("us.federal.agi"),
              right: { kind: "param", name: "ceiling" }
            }
          ]
        },
        then: money33("0"),
        else: {
          kind: "if",
          cond: kidsEq("0"),
          then: col0,
          else: {
            kind: "if",
            cond: kidsEq("1"),
            then: col1,
            else: {
              kind: "if",
              cond: kidsEq("2"),
              then: col2,
              else: col3
            }
          }
        }
      };
    })()
  },
  {
    id: "us.ca.yctc",
    version: 1,
    jurisdiction: "us.ca",
    title: "California Young Child Tax Credit (R&TC \xA7 17052.1, FTB 3514) \u2014 2025",
    citation: {
      source: "R&TC \xA7 17052.1; 2025 FTB 3514 booklet, General Information",
      section: "R&TC \xA7 17052.1",
      url: "https://www.ftb.ca.gov/forms/2025/2025-3514-booklet.pdf",
      excerpt: "2025 YCTC (verbatim from the FTB 3514 booklet): 'the maximum amount of credit allowable for a qualified taxpayer is $1,189 and the credit amount phases out as earned income exceeds the threshold amount of $27,425, and completely phases out at $32,901.' Requires CalEITC eligibility and a qualifying child UNDER SIX at year end (hasChildUnderSix). This rule returns the full $1,189 at or below the $27,425 threshold and $0 at or above $32,901; the phase-out band between them refuses (the per-$100 reduction schedule is table-published, not formula-published \u2014 read FTB 3514 and disclose)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      maxCredit: { value: "118900", type: "money" },
      // $1,189
      phaseOutStart: { value: "2742500", type: "money" },
      // $27,425
      phaseOutComplete: { value: "3290100", type: "money" }
      // $32,901
    },
    formula: (() => {
      const earned2 = {
        kind: "max0",
        arg: {
          kind: "sub",
          left: {
            kind: "add",
            args: [
              fact36("wages"),
              {
                kind: "max0",
                arg: {
                  kind: "sub",
                  left: fact36("selfEmploymentNetProfit"),
                  right: { kind: "rule", ruleId: "us.federal.se_tax_half_deduction" }
                }
              }
            ]
          },
          right: fact36("scheduleCNetLoss")
        }
      };
      return {
        kind: "if",
        cond: { kind: "not", arg: fact36("hasChildUnderSix") },
        then: money33("0"),
        else: {
          kind: "if",
          cond: { kind: "cmp", op: "ge", left: earned2, right: { kind: "param", name: "phaseOutComplete" } },
          then: money33("0"),
          else: {
            kind: "if",
            cond: { kind: "cmp", op: "le", left: earned2, right: { kind: "param", name: "phaseOutStart" } },
            then: { kind: "param", name: "maxCredit" },
            else: {
              kind: "unsupported",
              reason: "YCTC phase-out band ($27,425\u2013$32,901 earned income): the per-increment reduction is table-published, not formula-published \u2014 read the 2025 FTB 3514 worksheet and disclose"
            }
          }
        }
      };
    })()
  },
  {
    id: "us.ca.renters_credit",
    version: 1,
    jurisdiction: "us.ca",
    title: "California nonrefundable renter's credit (R&TC \xA7 17053.5, FTB 540)",
    citation: {
      source: "R&TC \xA7 17053.5; 2025 FTB 540 booklet (Nonrefundable Renter's Credit qualification record)",
      section: "R&TC \xA7 17053.5",
      url: "https://www.ftb.ca.gov/forms/2025/2025-540-booklet.pdf",
      excerpt: "2025 nonrefundable renter's credit: $60 for single or married/RDP filing separately with AGI $53,994 or less; $120 for married/RDP filing jointly, head of household, or qualifying surviving spouse with AGI $107,987 or less \u2014 $0 (no credit) once AGI exceeds the applicable threshold (a CLIFF, not a phase-out). NONREFUNDABLE. Eligibility (attested by caRentedPrincipalResidence): rented property in California as a principal residence for at least half of 2025, was not claimed as a dependent by another taxpayer, and the property was not exempt from California property tax. AB 130 (2025) authorized higher $250/$500 amounts CONTINGENT on a future appropriation that has not been made as of this web-verification (July 2026) \u2014 this rule defaults to the base $60/$120 statutory amounts, not the contingent AB 130 figures."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      creditSingleMfs: { value: "6000", type: "money" },
      // $60
      creditJointHohQss: { value: "12000", type: "money" },
      // $120
      agiCeilingSingleMfs: { value: "5399400", type: "money" },
      // $53,994
      agiCeilingJointHohQss: { value: "10798700", type: "money" }
      // $107,987
    },
    formula: {
      kind: "if",
      cond: { kind: "not", arg: fact36("caRentedPrincipalResidence") },
      then: money33("0"),
      else: {
        kind: "if",
        cond: {
          kind: "or",
          args: [isStatus10("mfj"), isStatus10("hoh"), isStatus10("qss")]
        },
        then: {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "le",
            left: ruleRef30("us.federal.agi"),
            right: { kind: "param", name: "agiCeilingJointHohQss" }
          },
          then: { kind: "param", name: "creditJointHohQss" },
          else: money33("0")
        },
        else: {
          kind: "if",
          cond: {
            kind: "cmp",
            op: "le",
            left: ruleRef30("us.federal.agi"),
            right: { kind: "param", name: "agiCeilingSingleMfs" }
          },
          then: { kind: "param", name: "creditSingleMfs" },
          else: money33("0")
        }
      }
    }
  },
  {
    id: "us.ca.cdcc",
    version: 1,
    jurisdiction: "us.ca",
    title: "California child and dependent care expenses credit (R&TC \xA7 17052.6, FTB 3506) \u2014 2025",
    citation: {
      source: "R&TC \xA7 17052.6; 2025 FTB 3506 (Child and Dependent Care Expenses Credit)",
      section: "R&TC \xA7 17052.6",
      url: "https://www.ftb.ca.gov/forms/2025/2025-3506.pdf",
      excerpt: "2025 CA CDCC: a PERCENTAGE of the FEDERAL child and dependent care credit (us.federal.cdcc), keyed to federal AGI: 50% if AGI \u2264 $40,000; 43% if AGI is over $40,000 but not over $70,000; 34% if AGI is over $70,000 but not over $100,000; $0 (no credit) if AGI exceeds $100,000 \u2014 a CLIFF at each breakpoint, not a smooth phase-down. NONREFUNDABLE. Requires the same earned-income-of-both-spouses gate as the federal credit (both spouses must have earned income on a joint return, per instructions). Parameters-only: the federal CDCC base (us.federal.cdcc) is the oracle target for the underlying credit; this rule documents the CA percentage table \u2014 apply the percentage to that result and disclose."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      pctAgiUpTo40k: { value: "50", type: "int" },
      pctAgi40kTo70k: { value: "43", type: "int" },
      pctAgi70kTo100k: { value: "34", type: "int" },
      agiTier1Max: { value: "4000000", type: "money" },
      // $40,000
      agiTier2Max: { value: "7000000", type: "money" },
      // $70,000
      agiTier3Max: { value: "10000000", type: "money" }
      // $100,000
    },
    formula: {
      kind: "unsupported",
      reason: "parameters-only rule: California's CDCC is a % of the federal CDCC (us.federal.cdcc) selected by the AGI tier documented in this rule's parameters \u2014 evaluate us.federal.cdcc, apply the tier percentage, and disclose"
    }
  },
  {
    id: "us.ca.amt",
    version: 1,
    jurisdiction: "us.ca",
    title: "California alternative minimum tax \u2014 Schedule P (540), Part II (2025)",
    citation: {
      source: "R&TC \xA7\xA7 17062, 17062.1; 2025 FTB Schedule P (540) instructions",
      section: "R&TC \xA7 17062(a)-(e); \xA7 17062.1",
      url: "https://www.ftb.ca.gov/forms/2025/2025-540-p-instructions.html",
      excerpt: "THIS RULE TAKES caAmti AS AN INPUT \u2014 it applies the exemption/TMT/comparison mechanics only; it does not construct AMTI itself. CONSTRUCTION OF caAmti (disclosed on the fact, repeated here): California individual AMT is FROZEN to IRC \xA7\xA7 55-59 as they read on January 1, 2015 (R&TC \xA7 17062.1, re-enacted by SB 711 (Stats. 2025, Ch. 231) WITHOUT moving the freeze forward, even though SB 711 rolled CA's general IRC conformity date to January 1, 2025 for other provisions). Starting from CA taxable income (Form 540 line 19), the STANDARD DEDUCTION IS ADDED BACK (R&TC \xA7 17062(c)(1), denying \xA7 56(b)(1)(E)). The ISO exercise spread follows \xA7 56(b)(3) as modified by R&TC \xA7 17062(c)(2) (plus the California Qualified Stock Option addback, R&TC \xA7 17502). There is NO tax-exempt private-activity-bond-interest preference (R&TC \xA7 17062(d) turns off \xA7 57(a)(5)) and NO AMT foreign tax credit (R&TC \xA7 17062(e) turns off \xA7 59(a)). A taxpayer with aggregate trade/business gross receipts under $1,000,000 (a flat threshold, NOT doubled for MFJ) excludes ALL trade/business income, adjustments, and preferences from AMTI (R&TC \xA7 17062(b)(4), the CA small-business AMTI exclusion). 2025 exemption/phase-out amounts (Exemption Worksheet, 2025 FTB Schedule P (540) instructions, web-verified July 2026): exemption $92,749 single/HOH, $123,667 MFJ/QSS, $61,830 MFS; phased out 25 cents per dollar of caAmti over $347,808 single/HOH, $463,745 MFJ/QSS, $231,868 MFS, floored at $0. Rate \u2014 7% (R&TC \xA7 17062(b)(3)(A)(iv)) of caAmti minus the exemption = the tentative minimum tax. AMT (R&TC \xA7 17062(a)) = the excess, if any, of the tentative minimum tax over the regular CA tax (caRegularTax \u2014 Schedule P (540) line 25: the Form 540 LINE 31 regular tax BEFORE exemption credits; worked example: TMT 25,484 \u2212 line-31 12,038 = AMT 13,446 \u2014 passing the after-credits line 48 amount overstates AMT by exactly the exemption credits). MFS AMTI STACKING ADD-BACK: a caAmti above $479,188 for an MFS filer requires adding to AMTI, before this rule's own exemption/phase-out math, the lesser of 25% of the excess over $479,188 or $61,830 (Schedule P (540) 'Line 21' worksheet) \u2014 NOT modeled here; this rule REFUSES for MFS above that threshold rather than silently omitting the add-back. Also not modeled: the \xA7 59(j) certain-children-under-24 exemption cap, and Part III credit ordering (e.g. the PTE elective tax credit's below-TMT exception)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      exemptSingleHoh: { value: "9274900", type: "money" },
      // $92,749
      exemptMfj: { value: "12366700", type: "money" },
      // $123,667
      exemptMfs: { value: "6183000", type: "money" },
      // $61,830
      threshSingleHoh: { value: "34780800", type: "money" },
      // $347,808
      threshMfj: { value: "46374500", type: "money" },
      // $463,745
      threshMfs: { value: "23186800", type: "money" },
      // $231,868
      mfsStackingThreshold: { value: "47918800", type: "money" }
      // $479,188
    },
    formula: (() => {
      const byStatus = (single, joint, mfs) => ({
        kind: "match",
        on: fact36("filingStatus"),
        cases: [
          { when: "single", value: { kind: "param", name: single } },
          { when: "hoh", value: { kind: "param", name: single } },
          { when: "mfj", value: { kind: "param", name: joint } },
          { when: "qss", value: { kind: "param", name: joint } },
          { when: "mfs", value: { kind: "param", name: mfs } }
        ]
      });
      const exemption2 = byStatus("exemptSingleHoh", "exemptMfj", "exemptMfs");
      const threshold2 = byStatus("threshSingleHoh", "threshMfj", "threshMfs");
      const exemptionPhased = {
        kind: "max0",
        arg: {
          kind: "sub",
          left: exemption2,
          right: {
            kind: "mulRate",
            base: { kind: "max0", arg: { kind: "sub", left: fact36("caAmti"), right: threshold2 } },
            rate: { num: "25", den: "100" },
            round: "half-up"
          }
        }
      };
      const tmt = {
        kind: "mulRate",
        base: { kind: "max0", arg: { kind: "sub", left: fact36("caAmti"), right: exemptionPhased } },
        rate: { num: "7", den: "100" },
        round: "half-up"
      };
      const amt = { kind: "max0", arg: { kind: "sub", left: tmt, right: fact36("caRegularTax") } };
      return {
        kind: "if",
        cond: {
          kind: "and",
          args: [
            isStatus10("mfs"),
            {
              kind: "cmp",
              op: "gt",
              left: fact36("caAmti"),
              right: { kind: "param", name: "mfsStackingThreshold" }
            }
          ]
        },
        then: {
          kind: "unsupported",
          reason: "MFS caAmti above $479,188 requires the Schedule P (540) Line 21 stacking add-back (the lesser of 25% of the excess over $479,188 or $61,830) applied to caAmti BEFORE this rule's exemption/phase-out math \u2014 not modeled; recompute caAmti with that add-back and re-run, or work Schedule P (540) directly"
        },
        else: amt
      };
    })()
  },
  {
    id: "us.ca.bhst",
    version: 1,
    jurisdiction: "us.ca",
    title: "California Behavioral Health Services Tax (formerly Mental Health Services Tax) \u2014 Form 540 line 62 (2025)",
    citation: {
      source: 'R&TC \xA7 17043; 2025 FTB Form 540 instructions, "Line 62 \u2013 Behavioral Health Services Tax"',
      section: "R&TC \xA7 17043",
      url: "https://www.ftb.ca.gov/forms/2025/2025-540-instructions.html",
      excerpt: "Flat 1% tax on the portion of CA taxable income (Form 540 line 19) over $1,000,000 (R&TC \xA7 17043(a)) \u2014 renamed the 'Behavioral Health Services Tax' by Prop 1 (2024)'s restructuring of the Mental Health Services Act into the Behavioral Health Services Act; same statute, same $1,000,000 threshold, unchanged since operative January 1, 2005. The $1,000,000 threshold is FLAT for every filing status \u2014 R&TC \xA7 17043(c) turns off \xA7 17041 filing-status bracket recomputation and \xA7 17045 joint-return combining, but does NOT halve the threshold for MFS. The commonly-repeated '$500,000 MFS' figure belongs to a DIFFERENT rule entirely \u2014 the FTB 5805 high-income estimated-tax safe harbor \u2014 and must not be applied to this tax. No credits reduce this tax (\xA7 17043(c)(1) turns off \xA7 17039 generally). Computed directly on Form 540 line 62, not on Schedule P \u2014 independent of the AMT computation (us.ca.amt), though both feed the estimated-tax base."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      threshold: { value: "100000000", type: "money" }
      // $1,000,000
    },
    formula: {
      kind: "mulRate",
      base: {
        kind: "max0",
        arg: { kind: "sub", left: fact36("stateTaxableIncome"), right: { kind: "param", name: "threshold" } }
      },
      rate: { num: "1", den: "100" },
      round: "half-up"
    }
  },
  {
    id: "us.ca.parameters",
    version: 1,
    jurisdiction: "us.ca",
    title: "California 2025 miscellaneous parameters \u2014 use tax table, SDI rate, TY2026 enacted changes",
    citation: {
      source: "Rev. & Tax. Code \xA7\xA7 6452, 17041; 2025 FTB 540 use tax worksheet; EDD SDI rate notice; SB 951",
      section: "Use Tax Worksheet; SDI rate",
      url: "https://www.ftb.ca.gov/forms/2025/2025-540-booklet.pdf",
      excerpt: "USE TAX (2025 FTB 540 use tax lookup table, no-receipts estimate keyed to CA AGI, illustrative low/high rows): under $10,000 \u2192 $1 \u2026 $150,000\u2013$174,999 \u2192 $17, $175,000\u2013$199,999 \u2192 $19; above $199,999, multiply CA AGI by 0.010% and round to the nearest whole dollar. SDI (State Disability Insurance, EDD, SB 951): 2025 rate is 1.2% of ALL wages with NO taxable wage cap (SB 951 removed the cap starting 2024) \u2014 the pre-2024 'excess SDI' credit line no longer applies (no wage base is ever exceeded, so no excess withholding can occur). TY2026 ENACTED CHANGES (not encoded this pass \u2014 informational only): SDI rate rises to 1.3% for 2026; the 2026 CPI inflation adjustment factor is 2.971% (exact 2026 dollar brackets for Schedules X/Y/Z and the standard deduction/exemption credits were not yet confirmed from FTB as of this July 2026 web-verification \u2014 do not extrapolate them)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      sdiRatePct: { value: "12", type: "int" },
      // 1.2% (×10 to keep as int; see sdiRateDen)
      sdiRateDen: { value: "1000", type: "int" }
    },
    formula: {
      kind: "unsupported",
      reason: "parameters-only rule: California use-tax lookup table and SDI rate are documented in this rule's citation for lookup_tax_parameter \u2014 no single oracle formula (use tax is agent-composed from the FTB table; SDI is withheld, not computed on the return)"
    }
  }
];

// ../corpus-us-federal/dist/rules/state-ny.js
var nyRules = [
  {
    id: "us.ny.parameters",
    version: 7,
    // v2 had the credit/modification set with an IT-216 shorthand and only tables 1/2/5; v3 adds the full six household-credit tables, the verbatim IT-216 factor tables, IT-272, 529, pension exclusion, § 685(c), and IT-196; v4 wrongly claimed IT-214's adjusted-rent line has no fixed percentage table; v6 replaces the ambiguous NYC child-care proration shorthand with the verbatim Worksheet 2 order and points at us.ny.nyc_cdcc; v7 CORRECTS IT-214 line 12 to the printed form's FIXED PERCENTAGE MENU (50/75/80/85/100 by included services) and routes the whole Step 3-5 arithmetic to us.ny.it214 v2; v5 had corrected the flat Table A/B credit lookup mechanic (Form IT-214 (2025), IT-214-I (2025), web-verified July 2026): the credit is NOT 25%-of-rent scaled into a dollar amount — lines 8-19 (FAGI cap, line-9 rate x line-10, adjusted rent, the line-14 25% conversion, $450/month cap) are an ELIGIBILITY GATE ONLY; the credit itself (line 20) is a FLAT lookup from Table A (65+) / Table B (under 65) by FAGI bracket, now its own computable target us.ny.it214
    jurisdiction: "us.ny",
    title: "New York 2025 standard deductions (Tax Law \xA7 614 \u2014 statutory, unindexed)",
    citation: {
      source: "N.Y. Tax Law \xA7 614; 2025 Form IT-201 instructions",
      section: "Tax Law \xA7 614",
      url: "https://www.tax.ny.gov/pit/file/standard_deductions.htm",
      excerpt: "Standard deductions (Tax Law \xA7 614, 2025-confirmed): $8,000 single; $16,050 MFJ/QSS; $11,200 HOH; $8,000 MFS; $3,100 dependent filer. Dependent exemptions $1,000 each (IT-201 line 36). CREDITS & MODIFICATIONS (all web-verified from the 2025 IT-201-I / IT-213-I / IT-215-I / IT-216-I / IT-214-I / IT-225-I / IT-272-I / IT-196-I): NYS EIC = 30% of federal EIC, reduced by the household credit ACTUALLY ABSORBED against NYS tax (min(line 39 tax, household credit) \u2014 when tax is $0 the reduction is $0 and the full 30% is refundable; IT-215 worksheet). NYC EIC = federal EIC \xD7 a sliding NYAGI rate: 30% \u2264 $7,500; 25% flat $7,500\u2013$15,000; phasing 25\u219220% to $17,500; 20% to $20,000; phasing 20\u219215% to $22,500; 15% to $40,000; phasing 15\u219210% to $42,500; 10% above. EMPIRE STATE CHILD CREDIT 2025 (IT-213, decoupled from federal): $1,000 per qualifying child UNDER FOUR + $330 per child 4\u201316 (TY2025; rises to $500 for the under-4 group's OLDER siblings \u2014 the ESCC ages-4-16 amount is $500 for TY2026-27 only, not yet in effect for TY2025), reduced $16.50 per $1,000 of FAGI (rounded DOWN to the nearest $1,000) over $110,000 MFJ / $75,000 single-HOH-QSS / $55,000 MFS.\n\nNYS HOUSEHOLD CREDIT (IT-201 line 40, IT-201-I p.13, keyed to FEDERAL AGI = Form IT-201 line 19) \u2014 THREE TABLES, verbatim 2025:\nTable 1 (single only): FAGI over \u2013$5,000 \u2192 $75; $5,000\u2013$6,000 \u2192 $60; $6,000\u2013$7,000 \u2192 $50; $7,000\u2013$20,000 \u2192 $45; $20,000\u2013$25,000 \u2192 $40; $25,000\u2013$28,000 \u2192 $20; over $28,000 \u2192 no credit.\nTable 2 (MFJ/HOH/QSS), by (dependents + 1 for filer + 1 for spouse if MFJ) = 1/2/3/4/5/6/7/each-over-7: FAGI \u2013$5,000 \u2192 90/105/120/135/150/165/180/+15; $5,000\u2013$6,000 \u2192 75/90/105/120/135/150/165/+15; $6,000\u2013$7,000 \u2192 65/80/95/110/125/140/155/+15; $7,000\u2013$20,000 \u2192 60/75/90/105/120/135/150/+15; $20,000\u2013$22,000 \u2192 60/70/80/90/100/110/120/+10; $22,000\u2013$25,000 \u2192 50/60/70/80/90/100/110/+10; $25,000\u2013$28,000 \u2192 40/45/50/55/60/65/70/+5; $28,000\u2013$32,000 \u2192 20/25/30/35/40/45/50/+5; over $32,000 \u2192 no credit.\nTable 3 (MFS only, dependents from BOTH returns + 1 for self + 1 for spouse), same 1-7/+over-7 columns: FAGI \u2013$5,000 \u2192 45/53/60/68/75/83/90/+8; $5,000\u2013$6,000 \u2192 38/45/53/60/68/75/83/+8; $6,000\u2013$7,000 \u2192 33/40/48/55/63/70/78/+8; $7,000\u2013$20,000 \u2192 30/38/45/53/60/68/75/+8; $20,000\u2013$22,000 \u2192 30/35/40/45/50/55/60/+5; $22,000\u2013$25,000 \u2192 25/30/35/40/45/50/55/+5; $25,000\u2013$28,000 \u2192 20/23/25/28/30/33/35/+3; $28,000\u2013$32,000 \u2192 10/13/15/18/20/23/25/+3; over $32,000 \u2192 no credit (table 3 amounts are pre-rounded per the form's Note 5).\n\nNYC HOUSEHOLD CREDIT (line 48, IT-201-I p.14) \u2014 THREE TABLES, verbatim 2025:\nTable 4 (single only): FAGI \u2013$10,000 \u2192 $15; $10,000\u2013$12,500 \u2192 $10; over $12,500 \u2192 no credit.\nTable 5 (MFJ/HOH/QSS), 1/2/3/4/5/6/7/each-over-7: FAGI \u2013$15,000 \u2192 30/60/90/120/150/180/210/+30; $15,000\u2013$17,500 \u2192 25/50/75/100/125/150/175/+25; $17,500\u2013$20,000 \u2192 15/30/45/60/75/90/105/+15; $20,000\u2013$22,500 \u2192 10/20/30/40/50/60/70/+10; over $22,500 \u2192 no credit.\nTable 6 (MFS only, dependents both returns + self + spouse), 1/2/3/4/5/6/7/each-over-7: FAGI \u2013$15,000 \u2192 15/30/45/60/75/90/105/+15; $15,000\u2013$17,500 \u2192 13/25/38/50/63/75/88/+13; $17,500\u2013$20,000 \u2192 8/15/23/30/38/45/53/+8; $20,000\u2013$22,500 \u2192 5/10/15/20/25/30/35/+5; over $22,500 \u2192 no credit (pre-rounded, Note 5).\n\nIT-216 CDCC (2025 IT-216-I, verbatim factor tables \u2014 replaces the earlier '1.10 \u2264 $25k' shorthand): base = qualifying expenses NET of employer dependent-care benefits excluded on W-2 box 10. NYS LINE-13 FACTOR (NYAGI-keyed, NON-MONOTONIC SAWTOOTH, IT-216-I p.7 'credit limitation table', $200-wide rows \u2014 segment anchors verbatim): flat 1.100 for NYAGI \u2013$25,000; descends in small ($0.001\u2013$0.002-per-$200) steps from 1.099 at $25,000 down to 1.000 by $40,000; FLAT 1.000 for $40,000\u2013$50,000; JUMPS to 1.162 at $50,000 and descends to 1.000 by $52,600\u2013$52,800, continuing down to 0.601 by $59,800\u2013$60,000; JUMPS AGAIN to 1.070 at $60,000 and descends to 0.476 by $64,800\u2013$65,000; FLAT 0.600 for $65,000\u2013$150,000; FLAT 0.200 above $150,000. NYS LINE-10 FACTOR (separate table, FAGI-keyed, monotonic): .35 for FAGI \u2013$15,000, stepping down by .01 per $2,000 of FAGI to .20 at $43,000 and above (verbatim anchors: $15,000\u2013$17,000 \u2192 .34 \u2026 $41,000\u2013$43,000 \u2192 .21; $43,000+ \u2192 .20). NYC CHILD CARE CREDIT (Worksheet 2 \u2014 now a standalone computable target, us.ny.nyc_cdcc): requires a qualifying child UNDER 4 and FAGI $30,000 or less; the worksheet PRORATES THE NYS CREDIT ITSELF (IT-216 line 14 \u2192 Worksheet 2 line 1) by the under-4 expense share (line 23 \xF7 line 3a, capped at 1.0000), THEN multiplies by the line-6 FAGI factor (0.750 for FAGI \u2013$25,000, descending 0.030 per $200 to 0.000 at $30,000). NEVER prorate the raw expenses and re-apply the line-10/line-13 factors \u2014 that understates the credit; feed nyIt216StateCredit/nyIt216Under4Expenses/nyIt216TotalExpenses to us.ny.nyc_cdcc and use its answer.\n\nNYC SCHOOL TAX CREDIT fixed (line 69, income \u2264 $250,000): $63 single/MFS/HOH, $125 MFJ/QSS; plus the line 69a rate-reduction amount (\u2264 $500,000): HOH 0.171% of city taxable income to $14,400 then $25 + 0.228% of excess (single/MFS threshold $12,000/$21; MFJ $21,600/$37). IT-214 REAL PROPERTY TAX CREDIT (Form IT-214 (2025), read directly from the printed form \u2014 SECOND CORRECTION, July 2026: line 12 uses a FIXED PERCENTAGE MENU, not actual utility dollar subtraction): line 8 FAGI \u2264 $18,000 stop; line 12 adjusted rent = line 11 total rent x the printed menu \u2014 rent includes heat+gas+electricity+furnishings+board \u2192 50%; heat+gas+electricity+furnishings \u2192 75%; heat+gas+electricity \u2192 80%; heat (or heat+gas) \u2192 85%; none \u2192 100%; line 13 average monthly adjusted rent (line 12 \xF7 months paid) \u2264 $450 stop; line 14 = 25% x line 12; homeowners line 17 = property taxes + special assessments; line 18 (renters' 14 / homeowners' 17) must be positive and must EXCEED line 19 = FAGI x Table 1 rate (0.035 to 0.065 by FAGI bracket). THE CREDIT (line 20) is a FLAT Table A/B lookup by FAGI. ALL of this arithmetic is COMPUTED by us.ny.it214 \u2014 feed it nyIt214Fagi, nyIt214TotalRent, nyIt214RentPercent (50/75/80/85/100 from the checkbox menu), nyIt214MonthsPaid, nyIt214HomeownerTaxes, isAge65OrOlder (any household member 65+ routes to Table A: $375/$330/$300/$260/$230/$200/$150), and nyIt214Eligible (Step 2 attestations only). Pass its answer into refundableCredits; never hand-compute any IT-214 line.\n\nIT-272 COLLEGE TUITION CREDIT (2025 IT-272-I): full-year NYS RESIDENTS only (part-year and nonresidents cannot claim it); qualified tuition expenses CAPPED at $10,000 per eligible student; credit = the LESSER of $400 or 4% of qualified expenses PER STUDENT (so the $400 max binds at $10,000 of expenses \u2014 confirm the 4% literal rate on the current worksheet before treating it as fixed across years); taxpayers may instead claim the college tuition ITEMIZED DEDUCTION on IT-201-D \u2014 mutually exclusive per student, elect whichever is larger.\n\nIT-229 REAL PROPERTY TAX RELIEF CREDIT: EXPIRED after TY2023 \u2014 DOES NOT EXIST for TY2025 or TY2026; do not encode or claim it. IT-214 (circuit breaker: FAGI \u2264 $18,000, property value \u2264 $85,000, rent \u2264 $450/month) is the live real-property-tax credit for TY2025.\n\n529 COLLEGE SAVINGS SUBTRACTION (Tax Law \xA7 612(c)(32)): up to $5,000 single/HOH/MFS, $10,000 MFJ, of contributions to a New York 529 account per year (recapture applies to nonqualified withdrawals of previously subtracted amounts). PENSION AND ANNUITY EXCLUSION (Tax Law \xA7 612(c)(3-a)): up to $20,000 EACH for taxpayer and spouse (no spousal sharing of unused exclusion) once age 59\xBD, for private/out-of-state pensions and annuities; NYS/local government and federal government pensions are excluded IN FULL regardless of age (a separate, unlimited subtraction, not capped at $20,000).\n\n\xA7 685(c) ESTIMATED TAX: no addition to tax if the amount owed after withholding is under $300 (de minimis); safe harbor is the LESSER of 90% of the current year's tax or 100% of the prior year's tax (110% of prior year's tax if the prior year's NYAGI exceeded $150,000).\n\nIT-196 ITEMIZED CHARITABLE LIMITATION (Tax Law \xA7 615, active through TY2029 per current law): for taxpayers with NYAGI over $1,000,000, the itemized charitable contribution deduction is limited to 50% of the federal amount; over $10,000,000 NYAGI, limited to 25% of the federal amount. UNRESOLVED (do NOT encode): the broader \xA7 615(f) general itemized-deduction phase-down for high-NYAGI filers could not be reconciled against the 2025 IT-196 instructions worksheet during this web-verification \u2014 pull the actual worksheet before modeling it; only the $1M/$10M charitable-specific limitation above is confirmed active.\n\nIT-225 MODIFICATIONS: \xA7 414(h) public-employee pickup = addition on IT-201 line 21 directly (code A-104, NOT via IT-225); NYC IRC \xA7 125 flexible-benefits = addition A-101; ALIMONY (NY did NOT conform to TCJA \xA7 11051): post-2018-instrument alimony PAID = subtraction S-136, alimony RECEIVED = addition A-119. S-120 \u2014 New York Higher Education Loan Program (HELP) loan interest (2025 IT-225-I p.6 subtraction-modifications chart and detail section, applicable to IT-201/IT-203 filers, not IT-204/IT-205), quoted verbatim: chart entry 'S-120  New York Higher Education Loan Program (HELP)'; detail text 'S-120: New York Higher Education Loan Program (HELP) \u2014 Enter any interest you paid in 2025 on loans made to you under HELP.' STATUTE CITE UNRESOLVED: the IT-225-I chart/detail text does not itself cite a Tax Law \xA7 612(c) paragraph number for S-120 (unlike some neighboring codes), and a web search of the codified \xA7 612 (nysenate.gov) did not turn up a matching paragraph as of this July 2026 verification \u2014 treat the CODE as confirmed-current for TY2025 via the 2025 IT-225-I form itself (the authority cited here), with the enabling paragraph number unresolved rather than guessed. Do not confuse with the separate, prospective, general student-loan-interest deduction tracked for a future Tax Law \xA7 612(c) paragraph (TY2026+, following 26 U.S.C. \xA7 221) \u2014 that is a different, not-yet-effective provision and does not affect the TY2025 S-120 answer.\n\nTY2026 ENACTED CHANGES (not encoded this pass \u2014 informational only, so a 2026 asOf refusal has useful context): a new subtraction for tips (up to $25,000, OBBBA conformity) begins TY2026; a new 'POWER credit' applies for TY2026 ONLY; a new REFUNDABLE CDCC under \xA7 606(c-2) replaces the nonrefundable IT-216 coupling for TY2026+ (55% of federal CDCC at NYAGI \u2264 $15,000, phasing down to a 4% floor, reduced further above $750,000 NYAGI); the Empire State Child Credit's $330 per-child (age 4-16) amount rises to $500 for TY2026-2027. Brackets, IT-272, the 529 subtraction, the household-credit tables, and IT-214 are UNCHANGED for TY2026 per the sources reviewed. Tax computation \u2192 us.ny.income_tax; NYC resident tax \u2192 us.ny.nyc_income_tax."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      standardDeductionSingle: { value: "800000", type: "money" },
      // $8,000
      standardDeductionJoint: { value: "1605000", type: "money" },
      // $16,050
      standardDeductionHoh: { value: "1120000", type: "money" },
      // $11,200
      dependentExemption: { value: "100000", type: "money" },
      // $1,000
      nysEicPctOfFederal: { value: "30", type: "int" },
      esccPerChildUnder4: { value: "100000", type: "money" },
      // $1,000 (2025)
      esccPerChild4to16: { value: "33000", type: "money" },
      // $330 (2025; $500 for 2026-27)
      esccReductionPer1000: { value: "1650", type: "money" },
      // $16.50
      esccThresholdJoint: { value: "11000000", type: "money" },
      // $110,000
      esccThresholdSingleHohQss: { value: "7500000", type: "money" },
      // $75,000
      esccThresholdMfs: { value: "5500000", type: "money" },
      // $55,000
      it216LowIncomeFactorPct: { value: "110", type: "int" },
      // NYAGI ≤ $25,000 (flat segment only; see excerpt for the full sawtooth)
      schoolTaxCreditFixedSingle: { value: "6300", type: "money" },
      // $63
      schoolTaxCreditFixedJoint: { value: "12500", type: "money" },
      // $125
      it214MaxUnder65: { value: "7500", type: "money" },
      // $75
      it214Max65Plus: { value: "37500", type: "money" },
      // $375
      it214FagiLimit: { value: "1800000", type: "money" },
      // $18,000
      it272MaxCreditPerStudent: { value: "40000", type: "money" },
      // $400
      it272ExpenseCapPerStudent: { value: "1000000", type: "money" },
      // $10,000
      section529SubtractionSingle: { value: "500000", type: "money" },
      // $5,000
      section529SubtractionJoint: { value: "1000000", type: "money" },
      // $10,000
      pensionExclusionPerPerson: { value: "2000000", type: "money" },
      // $20,000 each, age 59½+
      estimatedTaxDeMinimis: { value: "30000", type: "money" },
      // $300
      estimatedTaxSafeHarborPctCurrent: { value: "90", type: "int" },
      estimatedTaxSafeHarborPctPrior: { value: "100", type: "int" },
      estimatedTaxSafeHarborPctPriorHighIncome: { value: "110", type: "int" },
      // prior NYAGI > $150,000
      estimatedTaxHighIncomeThreshold: { value: "15000000", type: "money" },
      // $150,000
      it196CharitableThreshold1M: { value: "100000000", type: "money" },
      // $1,000,000 NYAGI
      it196CharitableThreshold10M: { value: "1000000000", type: "money" },
      // $10,000,000 NYAGI
      it196CharitablePctOver1M: { value: "50", type: "int" },
      it196CharitablePctOver10M: { value: "25", type: "int" }
    },
    formula: {
      kind: "unsupported",
      reason: "parameters-only rule: use lookup_tax_parameter for the NY amounts; NYS tax \u2192 us.ny.income_tax, NYC resident tax \u2192 us.ny.nyc_income_tax; credits are agent-composed from the cited tables"
    }
  },
  {
    id: "us.ny.it214",
    version: 2,
    // v1 modeled lines 9-19 as agent-attested only, and wrongly claimed line 12 subtracts ACTUAL utility dollar charges; v2 (2025 Form IT-214 read directly, July 2026) encodes the printed line-12 FIXED PERCENTAGE menu and computes the whole Step 4/Step 5 gate chain (lines 8-19) in the engine
    jurisdiction: "us.ny",
    title: "New York IT-214 Real Property Tax Credit \u2014 full Step 3-5 computation with the printed line-12 percentage menu (Form IT-214 line 20, 2025)",
    citation: {
      source: "Form IT-214 (2025); Form IT-214-I (2025 instructions); Tax Law \xA7 606(e)",
      section: "IT-214 lines 8-20; Table 1, Table A, Table B",
      url: "https://www.tax.ny.gov/pdf/current_forms/it/it214_fill_in.pdf",
      excerpt: "2025 Form IT-214, transcribed verbatim from the printed form (July 2026 \u2014 CORRECTS the earlier claim that line 12 subtracts actual utility dollar charges; the printed form uses a FIXED PERCENTAGE MENU): Step 4, line 11 'Enter the total amount of rent you paid during 2025. (Do not include any subsidized part of your rental charge.)'; line 12 'Adjusted rent - If line 11 includes charges for: heat, gas, electricity, furnishings, and board ... 50% (0.5) of line 11; heat, gas, electricity, and furnishings ... 75% (0.75) of line 11; heat, gas, and electricity ... 80% (0.8) of line 11; heat or heat and gas ... 85% (0.85) of line 11; none of the above ... 100% of line 11'; line 13 'Average monthly adjusted rent (divide line 12 by the number of months you paid rent) ... If line 13 is more than $450, stop; you do not qualify for this credit'; line 14 'Multiply line 12 by 25% (0.25); enter here and on line 18'; homeowners: line 15 real property taxes paid + line 16 special assessments = line 17. Step 5: line 18 = renters' line 14 / homeowners' line 17 ('If line 18 is zero or less, stop; no credit is allowed'); line 19 = line 10 = line 8 FAGI x the Table 1 rate ('If line 19 is equal to or more than line 18, stop; you do not qualify'); line 8 gate 'If line 8 is more than $18,000, stop'. TABLE 1 (rate by FAGI): $0-3,000 \u2192 0.035; 3,001-5,000 \u2192 0.040; 5,001-7,000 \u2192 0.045; 7,001-9,000 \u2192 0.050; 9,001-11,000 \u2192 0.055; 11,001-14,000 \u2192 0.060; 14,001-18,000 \u2192 0.065. LINE 20 is a FLAT DOLLAR LOOKUP: TABLE A (line 7 entry \u2014 ANY filer/spouse/dependent age 65 or older routes the WHOLE household to Table A): FAGI $0-3,000 \u2192 $375; 3,001-5,000 \u2192 $330; 5,001-7,000 \u2192 $300; 7,001-9,000 \u2192 $260; 9,001-11,000 \u2192 $230; 11,001-14,000 \u2192 $200; 14,001-18,000 \u2192 $150. TABLE B (no line 7 entry): $0-5,000 \u2192 $75; 5,001-9,000 \u2192 $70; 9,001-14,000 \u2192 $60; 14,001-18,000 \u2192 $50. THIS RULE COMPUTES lines 8-20 from: nyIt214Fagi (line 8), nyIt214TotalRent (line 11), nyIt214RentPercent (the printed 50/75/80/85/100 menu choice from the included-services checkboxes), nyIt214MonthsPaid (line 13 divisor), nyIt214HomeownerTaxes (line 17 = taxes + special assessments), isAge65OrOlder (Table A/B routing), and nyIt214Eligible (the NON-ARITHMETIC gates only: 6+ month same-residence occupancy, full-year NYS residency, not claimable as a dependent, market value \u2264 $85,000, not completely tax-exempt housing/public housing, nursing-home disclosure). A part-year owner/renter adds both buckets into line 18 (1999-lineage instruction: 'add 25% of your adjusted rent paid to the prorated part' of homeowner charges). Pass this rule's answer into the state composer's refundableCredits (composeNY)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      fagiLimit: { value: "1800000", type: "money" },
      // $18,000
      monthlyRentCap: { value: "45000", type: "money" },
      // $450 line-13 average monthly adjusted rent cap
      tableAThreshold1: { value: "300000", type: "money" },
      // $3,000
      tableAValue1: { value: "37500", type: "money" },
      // $375
      tableAThreshold2: { value: "500000", type: "money" },
      // $5,000
      tableAValue2: { value: "33000", type: "money" },
      // $330
      tableAThreshold3: { value: "700000", type: "money" },
      // $7,000
      tableAValue3: { value: "30000", type: "money" },
      // $300
      tableAThreshold4: { value: "900000", type: "money" },
      // $9,000
      tableAValue4: { value: "26000", type: "money" },
      // $260
      tableAThreshold5: { value: "1100000", type: "money" },
      // $11,000
      tableAValue5: { value: "23000", type: "money" },
      // $230
      tableAThreshold6: { value: "1400000", type: "money" },
      // $14,000
      tableAValue6: { value: "20000", type: "money" },
      // $200
      tableAValue7: { value: "15000", type: "money" },
      // $150 ($14,001-$18,000, uses fagiLimit as its threshold)
      tableBThreshold1: { value: "500000", type: "money" },
      // $5,000
      tableBValue1: { value: "7500", type: "money" },
      // $75
      tableBThreshold2: { value: "900000", type: "money" },
      // $9,000
      tableBValue2: { value: "7000", type: "money" },
      // $70
      tableBThreshold3: { value: "1400000", type: "money" },
      // $14,000
      tableBValue3: { value: "6000", type: "money" },
      // $60
      tableBValue4: { value: "5000", type: "money" }
      // $50 ($14,001-$18,000, uses fagiLimit as its threshold)
    },
    formula: (() => {
      const param22 = (name) => ({ kind: "param", name });
      const fagi = fact36("nyIt214Fagi");
      const adjustedRent = {
        kind: "mulDiv",
        a: { kind: "max0", arg: fact36("nyIt214TotalRent") },
        b: { kind: "mulInt", base: money33("1"), count: fact36("nyIt214RentPercent") },
        c: money33("100"),
        round: "half-up"
      };
      const avgMonthlyRent = {
        kind: "mulDiv",
        a: adjustedRent,
        b: money33("1"),
        c: { kind: "max", args: [money33("1"), { kind: "mulInt", base: money33("1"), count: fact36("nyIt214MonthsPaid") }] },
        round: "half-up"
      };
      const line14 = {
        kind: "mulRate",
        base: adjustedRent,
        rate: { num: "25", den: "100" },
        round: "half-up"
      };
      const line18 = {
        kind: "add",
        args: [line14, { kind: "max0", arg: fact36("nyIt214HomeownerTaxes") }]
      };
      const table1Rate = (perMille, threshold2, next) => ({
        kind: "if",
        cond: { kind: "cmp", op: "le", left: fagi, right: money33(threshold2) },
        then: { kind: "mulDiv", a: fagi, b: money33(perMille), c: money33("1000"), round: "half-up" },
        else: next
      });
      const line19 = table1Rate("35", "300000", table1Rate("40", "500000", table1Rate("45", "700000", table1Rate("50", "900000", table1Rate("55", "1100000", table1Rate("60", "1400000", { kind: "mulDiv", a: fagi, b: money33("65"), c: money33("1000"), round: "half-up" }))))));
      const tableA = {
        kind: "if",
        cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableAThreshold1") },
        then: param22("tableAValue1"),
        else: {
          kind: "if",
          cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableAThreshold2") },
          then: param22("tableAValue2"),
          else: {
            kind: "if",
            cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableAThreshold3") },
            then: param22("tableAValue3"),
            else: {
              kind: "if",
              cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableAThreshold4") },
              then: param22("tableAValue4"),
              else: {
                kind: "if",
                cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableAThreshold5") },
                then: param22("tableAValue5"),
                else: {
                  kind: "if",
                  cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableAThreshold6") },
                  then: param22("tableAValue6"),
                  else: {
                    kind: "if",
                    cond: { kind: "cmp", op: "le", left: fagi, right: param22("fagiLimit") },
                    then: param22("tableAValue7"),
                    else: money33("0")
                  }
                }
              }
            }
          }
        }
      };
      const tableB = {
        kind: "if",
        cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableBThreshold1") },
        then: param22("tableBValue1"),
        else: {
          kind: "if",
          cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableBThreshold2") },
          then: param22("tableBValue2"),
          else: {
            kind: "if",
            cond: { kind: "cmp", op: "le", left: fagi, right: param22("tableBThreshold3") },
            then: param22("tableBValue3"),
            else: {
              kind: "if",
              cond: { kind: "cmp", op: "le", left: fagi, right: param22("fagiLimit") },
              then: param22("tableBValue4"),
              else: money33("0")
            }
          }
        }
      };
      return {
        kind: "if",
        cond: {
          kind: "or",
          args: [
            { kind: "not", arg: fact36("nyIt214Eligible") },
            { kind: "cmp", op: "gt", left: fagi, right: param22("fagiLimit") },
            // line 8 stop
            { kind: "cmp", op: "gt", left: avgMonthlyRent, right: param22("monthlyRentCap") },
            // line 13 stop
            { kind: "cmp", op: "le", left: line18, right: money33("0") },
            // line 18 stop
            { kind: "cmp", op: "ge", left: line19, right: line18 }
            // line 19 stop
          ]
        },
        then: money33("0"),
        else: {
          kind: "if",
          cond: fact36("isAge65OrOlder"),
          then: tableA,
          else: tableB
        }
      };
    })()
  },
  {
    id: "us.ny.nyc_cdcc",
    version: 1,
    jurisdiction: "us.ny",
    title: "New York City child and dependent care credit \u2014 IT-216 Worksheet 2 (Form IT-216 line 24, 2025)",
    citation: {
      source: "N.Y.C. Admin. Code \xA7 11-1706(d); Form IT-216-I (2025 instructions), Worksheet 2 (p.6) and the New York City child and dependent care credit limitation table (p.7)",
      section: "IT-216-I Worksheet 2; IT-216 lines 23-24",
      url: "https://www.tax.ny.gov/pdf/current_forms/it/it216i.pdf",
      excerpt: "2025 IT-216-I Worksheet 2, verbatim: 'Caution: If your FAGI is over $30,000 ... or you have no children under 4 years old, stop; you do not qualify for the New York City child and dependent care credit.' Line 1 'Amount from line 14 on Form IT-216' [the NYS child and dependent care credit \u2014 NOT the raw expenses]; line 2 'Amount from line 23 on Form IT-216' [expenses for qualifying persons under 4]; line 3 'Amount from line 3a on Form IT-216' [total qualified expenses]; line 4 'Divide line 2 by line 3 (round the result to the fourth decimal place). This amount cannot exceed 100% (1.0000)'; line 5 'Multiply line 1 by line 4'; line 6 'Enter decimal amount as shown in the New York City child and dependent care credit limitation table' [FAGI-keyed, from IT-201 line 19: 0.750 for FAGI up to $25,000, then descending 0.030 per $200 row \u2014 $25,000-25,200 \u2192 0.735 ... $29,800-30,000 \u2192 0.015; over $30,000 \u2192 0.000]; line 7 'Multiply line 5 by line 6. Full-year New York City residents: Enter the line 7 amount on Form IT-216, line 24.' Line-6 instruction note: 'The New York City child and dependent care credit can be as much as 75% of the New York State child and dependent care credit.' THE PRORATION APPLIES TO THE STATE CREDIT (line 14), NEVER TO THE EXPENSES \u2014 computing under-4 expenses x the line-10/line-13 factors directly understates the credit whenever the state factors differ from a pure expense scaling. This rule evaluates lines 4-5 as an exact credit x under4 / total product (single cent rounding) rather than a pre-rounded 4-decimal ratio \u2014 divergence from the printed 4-decimal intermediate is under one cent per $200 of state credit, sub-dollar in all realistic cases. Part-year NYC residents (Worksheet 2 lines 8-13, IT-360.1 proration) are NOT modeled \u2014 full-year NYC residents only; the part-year split must be worked by hand per the worksheet. Validity: TY2025 instructions (rev. Oct 2025), web-verified July 2026; the 0.750-to-0 factor table and $30,000 cap are unchanged from prior years."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      fagiFlatCeiling: { value: "2500000", type: "money" },
      // $25,000 — flat 0.750 at or below
      fagiCutoff: { value: "3000000", type: "money" },
      // $30,000 — no credit above
      rowWidthCents: { value: "20000", type: "int" },
      // $200 factor-table rows
      flatFactorPerMille: { value: "750", type: "int" },
      // 0.750
      anchorPerMille: { value: "765", type: "int" },
      // 0.765 - 0.030 x row index reproduces 0.735, 0.705, ... 0.015
      stepPerMille: { value: "30", type: "int" }
      // 0.030 per $200 row
    },
    formula: (() => {
      const stateCredit = { kind: "max0", arg: fact36("nyIt216StateCredit") };
      const under4 = { kind: "max0", arg: fact36("nyIt216Under4Expenses") };
      const total = { kind: "max0", arg: fact36("nyIt216TotalExpenses") };
      const fagi = ruleRef30("us.federal.agi");
      const line5 = {
        kind: "min",
        args: [
          { kind: "mulDiv", a: stateCredit, b: under4, c: total, round: "half-up" },
          stateCredit
        ]
      };
      const rowIndex = {
        kind: "stepUnits",
        value: { kind: "max0", arg: { kind: "sub", left: fagi, right: { kind: "param", name: "fagiFlatCeiling" } } },
        unitCents: "20000",
        mode: "ceil"
      };
      const factorDeclining = {
        kind: "max0",
        arg: {
          kind: "sub",
          left: money33("765"),
          right: { kind: "mulInt", base: money33("30"), count: rowIndex }
        }
      };
      const line7 = (factor) => ({
        kind: "mulDiv",
        a: line5,
        b: factor,
        c: money33("1000"),
        round: "half-up"
      });
      return {
        kind: "if",
        cond: {
          kind: "or",
          args: [
            { kind: "cmp", op: "gt", left: fagi, right: { kind: "param", name: "fagiCutoff" } },
            { kind: "cmp", op: "le", left: under4, right: money33("0") },
            { kind: "cmp", op: "le", left: total, right: money33("0") }
          ]
        },
        then: money33("0"),
        else: {
          kind: "roundToDollar",
          mode: "half-up",
          value: {
            kind: "if",
            cond: { kind: "cmp", op: "le", left: fagi, right: { kind: "param", name: "fagiFlatCeiling" } },
            then: line7(money33("750")),
            else: line7(factorDeclining)
          }
        }
      };
    })()
  },
  {
    id: "us.ny.income_tax",
    version: 2,
    // v1 used the $50-bracket-midpoint table unconditionally below $65,000; v2 adds the useFormulaMethod toggle (raw-schedule evaluation, bypassing the table) and documents the DOM-verified table match / third-party raw-schedule divergence
    jurisdiction: "us.ny",
    title: "New York State income tax \u2014 2025 rate schedules with the NYS Tax Table convention (IT-201 line 39)",
    citation: {
      source: "N.Y. Tax Law \xA7 601; 2025 IT-201-I pp.33-39 (rate schedules, tax table, worksheets)",
      section: "Tax Law \xA7 601; IT-201-I line 39",
      url: "https://www.tax.ny.gov/pdf/current_forms/it/it201i.pdf",
      excerpt: "2025 NYS rate schedules (IT-201-I p.33): SINGLE/MFS 4% to $8,500; $340+4.5% to $11,700; $484+5.25% to $13,900; $600+5.5% to $80,650; $4,271+6% to $215,400; $12,356+6.85% to $1,077,550; 9.65/10.3/10.9% above. MFJ/QSS 4% to $17,150; $686+4.5% to $23,600; $976+5.25% to $27,900; $1,202+5.5% to $161,550; $8,553+6% to $323,200; $18,252+6.85% above. HOH 4% to $12,800; $512+4.5% to $17,650; $730+5.25% to $20,900; $901+5.5% to $107,650; $5,672+6% to $269,300; $15,371+6.85% above. METHOD (line 39 instructions): taxable income under $65,000 uses the NYS TAX TABLE ($50 brackets at the midpoint \u2014 inferred from published rows, e.g. $13\u2013$25 \u2192 $1 = 4% \xD7 $19 rounded; rows under $50 are narrower, divergence \u2264 $1); $65,000+ uses the rate schedule (whole-dollar). THIS RULE REFUSES when taxable income exceeds $107,650 \u2014 and CAUTION: the supplemental-tax RECAPTURE worksheets key on NYAGI (line 33) > $107,650 even when taxable income is lower; if NYAGI > $107,650, compute via the IT-201-I pp.34-39 worksheets yourself and disclose.\n\nTABLE-VS-FORMULA DIVERGENCE NOTE (July 2026 DOM-verification): this rule's $50-bracket-midpoint TABLE method reproduces the OFFICIAL 2025 NYS Tax Table EXACTLY across all 1,302 rows (all three filing-status columns, $0-$65,000 \u2014 parsed directly from tax.ny.gov/pit/file/tax-tables/it201i-2025.htm, 'the 2025 New York State Tax Table'). Some third-party reference implementations instead evaluate the continuous rate schedule at the RAW income value (not the bracket midpoint) below $65,000, which can differ from the official table by up to $1 (e.g. NYTI $1,949: raw-schedule 4%\xD71,949=$77.96\u2192$78 vs. this rule's/the table's $77; NYTI $10,001: raw-schedule $407.545\u2192$408 vs. $409; NYTI $55,961 HOH: raw-schedule $2,829.355\u2192$2,829 vs. $2,830) \u2014 those three figures do NOT appear anywhere in the official table at their brackets (checked against all 1,302 rows: zero hits). The 2025 IT-201 instructions MANDATE the table below $65,000 ('If your New York taxable income is less than $65,000, you must use the tax table') \u2014 this rule's table-midpoint default is what agrees with the official table; useFormulaMethod=true instead evaluates the raw rate schedule at the exact income value below $65,000 (matching a raw-schedule reference, for comparison only)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      tableThreshold: { value: "6500000", type: "money" },
      // $65,000
      recaptureThreshold: { value: "10765000", type: "money" }
      // $107,650 (NYAGI-keyed)
    },
    formula: (() => {
      const base = { kind: "max0", arg: fact36("stateTaxableIncome") };
      const scheduleFor = (b, status) => {
        const rows = {
          single: [
            { thresholdCents: "0", fixedCents: "0", rate: { num: "4", den: "100" } },
            { thresholdCents: "850000", fixedCents: "34000", rate: { num: "45", den: "1000" } },
            { thresholdCents: "1170000", fixedCents: "48400", rate: { num: "525", den: "10000" } },
            { thresholdCents: "1390000", fixedCents: "60000", rate: { num: "55", den: "1000" } },
            { thresholdCents: "8065000", fixedCents: "427100", rate: { num: "6", den: "100" } }
          ],
          mfj: [
            { thresholdCents: "0", fixedCents: "0", rate: { num: "4", den: "100" } },
            { thresholdCents: "1715000", fixedCents: "68600", rate: { num: "45", den: "1000" } },
            { thresholdCents: "2360000", fixedCents: "97600", rate: { num: "525", den: "10000" } },
            { thresholdCents: "2790000", fixedCents: "120200", rate: { num: "55", den: "1000" } }
          ],
          hoh: [
            { thresholdCents: "0", fixedCents: "0", rate: { num: "4", den: "100" } },
            { thresholdCents: "1280000", fixedCents: "51200", rate: { num: "45", den: "1000" } },
            { thresholdCents: "1765000", fixedCents: "73000", rate: { num: "525", den: "10000" } },
            { thresholdCents: "2090000", fixedCents: "90100", rate: { num: "55", den: "1000" } }
          ]
        };
        return printedSchedule(b, rows[status]);
      };
      const mid50 = {
        kind: "add",
        args: [
          {
            kind: "mulInt",
            base: money33("5000"),
            count: { kind: "stepUnits", value: base, unitCents: "5000", mode: "floor" }
          },
          money33("2500")
        ]
      };
      const byStatus = (b) => ({
        kind: "if",
        cond: { kind: "or", args: [isStatus10("mfj"), isStatus10("qss")] },
        then: scheduleFor(b, "mfj"),
        else: {
          kind: "if",
          cond: isStatus10("hoh"),
          then: scheduleFor(b, "hoh"),
          else: scheduleFor(b, "single")
        }
      });
      return {
        kind: "if",
        cond: { kind: "cmp", op: "gt", left: base, right: { kind: "param", name: "recaptureThreshold" } },
        then: {
          kind: "unsupported",
          reason: "NY taxable income above $107,650: the supplemental-tax recapture worksheets (IT-201-I pp.34-39, keyed to NYAGI) apply \u2014 compute via the worksheet for your filing status and disclose"
        },
        else: {
          kind: "if",
          // table method is the default below $65,000 (matches the official
          // 1,302-row table exactly, see the excerpt); useFormulaMethod=true
          // opts back to the raw rate schedule at the exact income value
          cond: {
            kind: "and",
            args: [
              { kind: "cmp", op: "lt", left: base, right: { kind: "param", name: "tableThreshold" } },
              { kind: "not", arg: fact36("useFormulaMethod") }
            ]
          },
          then: {
            // printed tax-table bottom row: $0-$13 -> $0 (the generic $50-midpoint
            // fiction would otherwise charge $1 on zero income)
            kind: "if",
            cond: { kind: "cmp", op: "lt", left: base, right: money33("1300") },
            then: money33("0"),
            else: { kind: "roundToDollar", value: byStatus(mid50), mode: "half-up" }
          },
          else: { kind: "roundToDollar", value: byStatus(base), mode: "half-up" }
        }
      };
    })()
  },
  {
    id: "us.ny.nyc_income_tax",
    version: 1,
    jurisdiction: "us.ny",
    title: "New York City resident income tax \u2014 2025 rate schedule (IT-201 line 47 base)",
    citation: {
      source: "N.Y. Tax Law \xA7\xA7 1304, 1304-B; 2025 IT-201-I p.40 (NYC tax rate schedule)",
      section: "Tax Law Art. 30; IT-201-I p.40",
      url: "https://www.tax.ny.gov/pdf/current_forms/it/it201i.pdf",
      excerpt: "2025 NYC resident rate schedule (IT-201-I p.40, \xA7 1304 base + \xA7 1304-B additional combined): MFJ/QSS 3.078% to $21,600; $665+3.762% to $45,000; $1,545+3.819% to $90,000; $3,264+3.876% above. SINGLE/MFS 3.078% to $12,000; $369+3.762% to $25,000; $858+3.819% to $50,000; $1,813+3.876% above. HOH 3.078% to $14,400; $443+3.762% to $30,000; $1,030+3.819% to $60,000; $2,176+3.876% above. NYC taxable income under $65,000 uses the NYC tax table ($50-bracket midpoint convention, same caveat as the NYS table); $65,000+ uses this schedule. Input: NYC taxable income (IT-201 line 47) via stateTaxableIncome \u2014 run this target SEPARATELY from the NYS computation."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      tableThreshold: { value: "6500000", type: "money" }
      // $65,000
    },
    formula: (() => {
      const base = { kind: "max0", arg: fact36("stateTaxableIncome") };
      const bracketsFor = (b, status) => {
        const rows = {
          single: [
            { thresholdCents: "0", fixedCents: "0", rate: { num: "3078", den: "100000" } },
            { thresholdCents: "1200000", fixedCents: "36900", rate: { num: "3762", den: "100000" } },
            { thresholdCents: "2500000", fixedCents: "85800", rate: { num: "3819", den: "100000" } },
            { thresholdCents: "5000000", fixedCents: "181300", rate: { num: "3876", den: "100000" } }
          ],
          mfj: [
            { thresholdCents: "0", fixedCents: "0", rate: { num: "3078", den: "100000" } },
            { thresholdCents: "2160000", fixedCents: "66500", rate: { num: "3762", den: "100000" } },
            { thresholdCents: "4500000", fixedCents: "154500", rate: { num: "3819", den: "100000" } },
            { thresholdCents: "9000000", fixedCents: "326400", rate: { num: "3876", den: "100000" } }
          ],
          hoh: [
            { thresholdCents: "0", fixedCents: "0", rate: { num: "3078", den: "100000" } },
            { thresholdCents: "1440000", fixedCents: "44300", rate: { num: "3762", den: "100000" } },
            { thresholdCents: "3000000", fixedCents: "103000", rate: { num: "3819", den: "100000" } },
            { thresholdCents: "6000000", fixedCents: "217600", rate: { num: "3876", den: "100000" } }
          ]
        };
        return printedSchedule(b, rows[status]);
      };
      const mid50 = {
        kind: "add",
        args: [
          {
            kind: "mulInt",
            base: money33("5000"),
            count: { kind: "stepUnits", value: base, unitCents: "5000", mode: "floor" }
          },
          money33("2500")
        ]
      };
      const byStatus = (b) => ({
        kind: "if",
        cond: { kind: "or", args: [isStatus10("mfj"), isStatus10("qss")] },
        then: bracketsFor(b, "mfj"),
        else: {
          kind: "if",
          cond: isStatus10("hoh"),
          then: bracketsFor(b, "hoh"),
          else: bracketsFor(b, "single")
        }
      });
      return {
        kind: "if",
        cond: { kind: "cmp", op: "lt", left: base, right: { kind: "param", name: "tableThreshold" } },
        then: { kind: "roundToDollar", value: byStatus(mid50), mode: "half-up" },
        else: { kind: "roundToDollar", value: byStatus(base), mode: "half-up" }
      };
    })()
  },
  {
    id: "us.ny.yonkers_surcharge",
    version: 1,
    jurisdiction: "us.ny",
    title: "Yonkers resident income tax surcharge \u2014 16.75% (IT-201 line 55, from the Yonkers worksheet)",
    citation: {
      source: "N.Y. Tax Law \xA7 1310; 2025 IT-201-I pp.17-18 (Yonkers worksheet)",
      section: "IT-201-I p.17-18, 'Yonkers worksheet'; IT-201 line 55",
      url: "https://www.tax.ny.gov/pdf/2025/inc/it201i_2025.pdf",
      excerpt: "LINE NUMBER: the surcharge is entered on IT-201 LINE 55, not line 54 (line 54 is the MCTMT). Rate, quoted verbatim from the worksheet: 'n Yonkers resident tax rate (16.75%) ... o Multiply line m by line n. Enter this amount on Form IT-201, line 55.' The base (worksheet line m) is NOT simply line 46 ('net state tax') \u2014 it is a NARROWED figure, per the worksheet's own chain, quoted verbatim: 'a Amount from line 46 ... b Amount from Form IT-213 (Empire State Child Credit), line 9 ... c Amount from Form IT-214 (Real Property Tax Credit), line 20 ... d Amount from Form IT-216 (Child and Dependent Care Credit), line 14 ... e Amount from Form IT-215 (Earned Income Credit), line 16 ... f Amount from Form IT-209 (Noncustodial Parent EIC), line 32 (or 42) ... g College tuition credit (IT-272 line 5 or 7) ... h Total from lines 69 and 69a from Form IT-201 ... i Amount from Form IT-201-ATT, line 13 ... j Add lines b through i ... k STAR reconciliation amount (Form IT-119, line 3) ... l Subtract line k from line j ... m Subtract line l from line a.' In words: base = (IT-201 line 46, Total New York State taxes) MINUS (Empire State child credit + real property tax credit + child/dependent care credit + EIC + noncustodial-parent EIC + college tuition credit + IT-201 lines 69/69a + IT-201-ATT line 13 other credits, netted against the STAR reconciliation add-back). This rule takes that already-netted base as an agent-supplied fact (nyYonkersBase) \u2014 computing the full a-through-m chain from the underlying credits is out of scope here (agent-composed per the quoted worksheet). TRIGGER (quoted): 'If you were a resident of Yonkers, did you enter an amount that is more than 0 on line 46? If No, go to line 56. If Yes, complete the Yonkers worksheet below' \u2014 the surcharge is SKIPPED ENTIRELY (not merely computed as $0) when line 46 is $0; do not invoke this rule in that case. JOINT-RETURN MIXED RESIDENCY (quoted): 'If you are filing jointly ... and only one spouse was a Yonkers resident and the other spouse was a Yonkers nonresident for all of 2025: 1. Enter special condition code Y1 on Form IT-201, Item G. 2. Calculate on a separate sheet the Yonkers resident income tax surcharge on the New York State tax of the Yonkers resident as if you had filed separate federal returns... 3. Enter the amount calculated on line 55.' \u2014 nyYonkersBase must already reflect that separate-return recomputation in a mixed-residency case; not modeled further here. NOT the same as: Y-203 (Yonkers NONRESIDENT earnings tax, a different rate/base, feeds line 56) or IT-360.1 (part-year Yonkers residents, feeds line 57). Validity window: TY2025; the 16.75% rate has been stable for several years \u2014 no legislated TY2026 change found as of this web-verification (July 2026)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      ratePct: { value: "1675", type: "int" },
      // 16.75% (x100 to keep as int; see rateDen)
      rateDen: { value: "10000", type: "int" }
    },
    formula: {
      kind: "mulRate",
      base: { kind: "max0", arg: fact36("nyYonkersBase") },
      rate: { num: "1675", den: "10000" },
      round: "half-up"
    }
  }
];

// ../corpus-us-federal/dist/rules/state-pa.js
var max03 = (arg) => ({ kind: "max0", arg });
var add2 = (...args) => ({ kind: "add", args });
var sub2 = (left, right) => ({ kind: "sub", left, right });
var rd = (value) => ({ kind: "roundToDollar", value, mode: "half-up" });
var isMarried = {
  kind: "or",
  args: [
    { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "mfj" } },
    { kind: "cmp", op: "eq", left: fact36("filingStatus"), right: { kind: "enum", value: "mfs" } }
  ]
};
var spForgiveness = (netTax, ei, t100) => {
  let expr = money33("0");
  for (let k = 9; k >= 0; k--) {
    expr = {
      kind: "if",
      cond: {
        kind: "cmp",
        op: "le",
        left: ei,
        right: k === 0 ? t100 : add2(t100, money33(String(k * 25e3)))
      },
      then: {
        kind: "mulRate",
        base: netTax,
        rate: { num: String(100 - 10 * k), den: "100" },
        round: "half-up"
      },
      else: expr
    };
  }
  return expr;
};
var taxableIncomeRule = (version, from, to) => ({
  id: "us.pa.taxable_income",
  version,
  jurisdiction: "us.pa",
  title: "Pennsylvania total taxable income (PA-40 line 9) \u2014 positive class amounts only",
  citation: {
    source: "72 P.S. \xA7 7303(a)(1)-(8); 2025 PA-40 Instructions (rev 04-25) pp. 8-20",
    section: "72 P.S. \xA7 7303; PA-40 lines 1a-9",
    url: "https://www.pa.gov/content/dam/copapwp-pagov/en/revenue/documents/formsandpublications/formsforindividuals/pit/documents/2025/2025_pa-40in.pdf",
    excerpt: "Line 9 (2025 PA-40, verbatim): 'Add only the positive income amounts from Lines 1c, 2, 3, 4, 5, 6, 7 and 8. DO NOT ADD any losses.' Eight classes per \xA7 7303(a): compensation; interest; dividends (mutual-fund capital gains DISTRIBUTIONS are dividends, Line 3); net business/profession/farm; net gains from disposition of property; net rents/royalties/patents/copyrights; estate or trust income (cannot be negative); gambling and lottery winnings (noncash PA Lottery prizes exempt; cash prizes taxable, ticket costs for tickets bought on/after 1/1/2016 deductible from winnings). NO cross-class offset, NO carryforward/carryback, and NO spousal sharing: on a joint return, per class, one spouse's loss NEVER offsets the other's income (both-positive add; one-income-one-loss counts ONLY the income) \u2014 encoded as max0(taxpayer net) + max0(spouse net) for each loss-capable class (lines 4/5/6; PA-40 IN p.15, Mary & Ben examples). COMPENSATION: PA compensation is W-2 BOX 16, not Box 1 \u2014 401(k)/elective deferrals ARE taxable compensation; IRA contributions are NOT deductible; supply the Box16-Box1 difference via paCompensationAdjustment (default 0 assumes Box 16 = Box 1). Retirement income is NOT compensation when paid by an eligible employer plan after meeting the plan's age/years-of-service conditions AND retiring (code 7), and Social Security / RR / military pensions / Civil Service annuities / SERS / PSERS are always exempt; early distributions (codes 1-2) and pre-59\xBD IRA distributions are taxable compensation under the COST RECOVERY method (distribution minus previously-taxed contributions; PA has NO federal-style early-withdrawal exceptions) \u2014 cost-recovery basis math is agent-composed into the class facts, disclosed. Unemployment compensation, workers' comp, third-party sick pay, child support, alimony, and W-2 box-10 dependent care up to $5,000 are not taxable. APPROXIMATIONS (disclosed): compensation and its unreimbursed-expense offset (Sch UE, line 1b) are modeled at the return level, not per spouse \u2014 a spouse whose UE exceeds own compensation on a joint return would be overstated (rare; compose per-spouse and supply via paCompensationAdjustment if it matters). Reciprocal compensation states (IN, MD, NJ, OH, VA, WV) and part-year/nonresident apportionment are out of scope for this resident-return target."
  },
  effectiveFrom: from,
  effectiveTo: to,
  output: { type: "money" },
  formula: add2(
    // line 1c: net compensation (never negative on the printed form)
    max03(sub2(add2(fact36("wages"), fact36("paCompensationAdjustment")), fact36("paUnreimbursedBusinessExpenses"))),
    fact36("paInterestIncome"),
    // line 2 (gross class)
    fact36("paDividendIncome"),
    // line 3 (gross class)
    max03(fact36("paBusinessNetIncome")),
    // line 4, taxpayer
    max03(fact36("paSpouseBusinessNetIncome")),
    // line 4, spouse
    max03(fact36("paPropertyGainNet")),
    // line 5, taxpayer
    max03(fact36("paSpousePropertyGainNet")),
    // line 5, spouse
    max03(fact36("paRentRoyaltyNet")),
    // line 6, taxpayer
    max03(fact36("paSpouseRentRoyaltyNet")),
    // line 6, spouse
    fact36("paEstateTrustIncome"),
    // line 7 (cannot be negative)
    fact36("paGamblingWinnings")
  )
});
var otherDeductionsRule = (version, from, to) => ({
  id: "us.pa.other_deductions",
  version,
  jurisdiction: "us.pa",
  title: "Pennsylvania other deductions (PA-40 line 10, Schedule O) \u2014 the exhaustive five",
  citation: {
    source: "2025 PA-40 Instructions (rev 04-25) pp. 9, 20-21; 2025 PA Schedule O + instructions",
    section: "PA-40 line 10; Schedule O",
    url: "https://www.pa.gov/content/dam/copapwp-pagov/en/revenue/documents/formsandpublications/formsforindividuals/pit/documents/2025/2025_pa-40o.pdf",
    excerpt: "'PA law does not allow standard deductions, deductions for personal exemptions, itemized deductions, or deductions for personal expenses' (PA-40 IN p.9). Exactly FIVE line-10 deductions exist for 2025: (M) Medical Savings Account contributions and (H) Health Savings Account contributions, each limited to the amount 'allowed for federal income tax purposes'; (S) STUDENT LOAN INTEREST \u2014 NEW for 2025 \u2014 up to $2,500 per taxable year (no PA income phase-out; the federal studentLoanInterest fact is reused as the interest-paid input and capped here); (T) IRC \xA7 529 qualified tuition program contributions \u2014 maximum $19,000 PER BENEFICIARY, PER TAXPAYER-SPOUSE (2025; no deduction for 529-to-529 rollovers or beneficiary changes); (A) \xA7 529A PA ABLE contributions capped at the annual federal gift-tax exclusion ($19,000 for 2025). The per-beneficiary 529 cap and the ABLE cap CANNOT be enforced without per-beneficiary detail \u2014 the caller must supply already-capped totals (disclosed). Line 10 cannot exceed line 9; the Schedule O Section III PER-SPOUSE cap (each spouse limited to own line-9 income) is approximated at the return level (disclosed). Unreimbursed employee business expenses are a line-1b class expense (Schedule UE), never a line-10 deduction. The 2026 federal gift-exclusion amount for the ABLE cap must be re-verified at 2026 encoding."
  },
  effectiveFrom: from,
  effectiveTo: to,
  output: { type: "money" },
  parameters: {
    studentLoanInterestCap: { value: "250000", type: "money" },
    // $2,500 (2025 PA cap)
    per529BeneficiaryCap: { value: "1900000", type: "money" },
    // $19,000 (disclosure)
    ableGiftExclusionCap: { value: "1900000", type: "money" }
    // $19,000 (2025)
  },
  formula: {
    kind: "min",
    args: [
      add2({ kind: "min", args: [fact36("studentLoanInterest"), { kind: "param", name: "studentLoanInterestCap" }] }, fact36("pa529Contributions"), fact36("paAbleContributions"), fact36("paMsaHsaContributions")),
      ruleRef30("us.pa.taxable_income")
    ]
  }
});
var incomeTaxRule2 = (version, from, to, yearLabel) => ({
  id: "us.pa.income_tax",
  version,
  jurisdiction: "us.pa",
  title: `Pennsylvania income tax \u2014 3.07% flat on adjusted PA taxable income (PA-40 line 12, ${yearLabel})`,
  citation: {
    source: "72 P.S. \xA7 7302 (current as of Jan 1, 2026); 2025 PA-40 line 12",
    section: "72 P.S. \xA7 7302",
    url: "https://codes.findlaw.com/pa/title-72-ps-taxation-and-fiscal-affairs/pa-st-sect-72-7302/",
    excerpt: "\xA7 7302 imposes 'three and seven hundredths per cent' (3.07%) on residents' taxable income; PA-40 line 12: 'Multiply Line 11 by 3.07 percent (0.0307)', where line 11 = line 9 total PA taxable income minus line 10 other deductions. Rate verified unchanged for TY2026 (statute text current as of Jan 1, 2026; the Senate's proposed 2.8% cut was NOT enacted). This target computes GROSS tax (line 12); Tax Forgiveness (Schedule SP) is the separate us.pa.tax_forgiveness target, and the resident credit / WPTC / PA CDCC net against tax in the return composition. Local earned income tax (Act 32) is filed separately with local collectors and is out of scope. Rounding: PA-40 uses whole dollars (drop <$0.50, round up \u2265$0.50 = half-up). Filing threshold: PA gross taxable income over $33 (or any loss transaction) requires a return."
  },
  effectiveFrom: from,
  effectiveTo: to,
  output: { type: "money" },
  parameters: {
    filingThreshold: { value: "3300", type: "money" }
    // $33 gross-income filing trigger
  },
  formula: rd({
    kind: "mulRate",
    base: max03(sub2(ruleRef30("us.pa.taxable_income"), ruleRef30("us.pa.other_deductions"))),
    rate: { num: "307", den: "10000" },
    round: "half-up"
  })
});
var paRules = [
  taxableIncomeRule(1, "2025-01-01", "2027-01-01"),
  otherDeductionsRule(1, "2025-01-01", "2027-01-01"),
  incomeTaxRule2(2, "2025-01-01", "2026-01-01", "TY2025"),
  incomeTaxRule2(3, "2026-01-01", "2027-01-01", "TY2026 \u2014 rate unchanged"),
  {
    id: "us.pa.tax_forgiveness",
    version: 1,
    jurisdiction: "us.pa",
    title: "Pennsylvania Tax Forgiveness credit (Schedule SP, 72 P.S. \xA7 7304) \u2014 PA-40 line 21",
    citation: {
      source: "72 P.S. \xA7 7304; 2025 PA Schedule SP + instructions (PA-40 SP IN 04-25); 2025 PA-40 Instructions p. 36-39 (eligibility income tables)",
      section: "Schedule SP Sections III-IV; PA-40 lines 19a-21",
      url: "https://www.pa.gov/content/dam/copapwp-pagov/en/revenue/documents/formsandpublications/formsforindividuals/pit/documents/2025/2025_pa-40sp.pdf",
      excerpt: "Printed 2025 eligibility-income tables (statutory, NOT indexed \u2014 structure verified against every printed cell): unmarried/separated/deceased base $6,500 at 100% forgiveness; MARRIED (even filing separately, using JOINT eligibility income) base $13,000; PLUS $9,500 per dependent child on every column; each additional $250 of eligibility income drops the forgiveness percentage 10 points (100/90/.../10%), so income beyond base+dependents+$2,250 gets nothing. Forgiveness applies to line 12 tax MINUS the line 22 resident credit (SP Section IV: the resident credit subtracts BEFORE forgiveness), never below zero. ELIGIBILITY INCOME = PA-40 line 9 taxable income PLUS nontaxable add-backs supplied via paEligibilityIncomeAddbacks: nontaxable interest/dividends/gains, alimony received, insurance proceeds and inheritances (incl. 1099-R code-4 box 1), gifts/awards/prizes (incl. noncash PA Lottery), non-PA income, nontaxable military pay (not combat), excluded home-sale gain, nontaxable educational assistance, and outside cash support (foster payments, spousal support from outside the household, cafeteria-plan benefits). NOT added back: Social Security/RRB, eligible retirement benefits, child support, military pensions, workers' comp, personal-injury damages, sick/disability pay. DEPENDENT = a CHILD (natural/adopted/step; grandchild of a grandparent; foster child of a foster parent \u2014 never aunts/uncles/unrelated persons) claimable as a federal dependent; adult qualifying children count. A claimant who is someone else's federal dependent is ineligible unless that person also qualifies (not modeled \u2014 disclose). 'Separated' (apart the last six months of the year or under written separation agreement) uses the unmarried table \u2014 supply via filingStatus; QSS/widowed is NOT married (Table 1); MFS IS married (Table 2, joint eligibility income \u2014 return-level approximation disclosed). Deceased-claimant annualization not modeled (refuse-by-disclosure). Defaults are CONSERVATIVE: paSpDependentChildren defaults to 0 (understates the threshold, never overstates the credit)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2027-01-01",
    output: { type: "money" },
    parameters: {
      baseUnmarried: { value: "650000", type: "money" },
      // $6,500
      baseMarried: { value: "1300000", type: "money" },
      // $13,000
      perDependentChild: { value: "950000", type: "money" },
      // $9,500
      stepWidth: { value: "25000", type: "money" }
      // $250 per 10-point drop
    },
    formula: rd(spForgiveness(
      // net tax: line 12 minus resident credit, floored at zero
      max03(sub2(ruleRef30("us.pa.income_tax"), fact36("paResidentCredit"))),
      // eligibility income: line 9 + add-backs
      add2(ruleRef30("us.pa.taxable_income"), fact36("paEligibilityIncomeAddbacks")),
      // 100% threshold: base + $9,500 x dependent children
      add2({ kind: "if", cond: isMarried, then: { kind: "param", name: "baseMarried" }, else: { kind: "param", name: "baseUnmarried" } }, { kind: "mulInt", base: { kind: "param", name: "perDependentChild" }, count: fact36("paSpDependentChildren") })
    ))
  },
  {
    id: "us.pa.cdcc",
    version: 1,
    jurisdiction: "us.pa",
    title: "Pennsylvania Child and Dependent Care Enhancement Tax Credit (Schedule DC) \u2014 100% of the federal \xA7 21 credit, refundable (TY2025)",
    citation: {
      source: "2025 PA Schedule DC + instructions (PA-40 DC IN 04-25); 2025 PA-40 Instructions pp. 22-23",
      section: "Schedule DC; PA-40 line 23",
      url: "https://www.pa.gov/content/dam/copapwp-pagov/en/revenue/documents/formsandpublications/formsforindividuals/pit/documents/2025/2025_pa-40dc.pdf",
      excerpt: "2025 Schedule DC: for PA residents receiving the federal IRC \xA7 21 credit, 'the credit is 100 percent of that amount' \u2014 the federal Form 2441 LINE 9a tentative credit (expenses capped $3,000 one / $6,000 two-or-more qualifying persons at the 35%\u219220% AGI slide), i.e. the PRE-liability-limit federal amount, because the PA credit is REFUNDABLE. Encoded as 100% of us.federal.cdcc.tentative (which already carries the \xA7 21 expense caps, earned-income limits, AGI percentage slide, and the MFS disallowance). The DC chart's printed caps ($1,050/$600 one dependent, $2,100/$1,200 two-plus at the $43,000 household-income break) are the federal formula's own outputs, not separate limits. Federal Form 2441 + Schedule 3 must be attached or DOR removes the credit (procedural, not modeled). TY2025 window only: the TY2026 federal \xA7 21 percentages change under OBBBA \xA7 70405 and PA's 100%-match treatment for 2026 was not yet verified at encoding \u2014 extend only after re-verification (refuses loudly for 2026 rather than guessing)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    formula: rd(ruleRef30("us.federal.cdcc.tentative"))
  },
  {
    id: "us.pa.wptc",
    version: 1,
    jurisdiction: "us.pa",
    title: "Working Pennsylvanians Tax Credit \u2014 10% of the federal EITC, max $805, refundable (first PA state EITC, TY2025+)",
    citation: {
      source: "2025-26 Pennsylvania budget (signed Nov 12, 2025); PA DOR Working Pennsylvanians Tax Credit page; PA DOR press release Jan 27, 2026 (web-verified 2026-07-24)",
      section: "PA DOR WPTC guidance",
      url: "https://www.pa.gov/agencies/revenue/resources/tax-types-and-information/personal-income-tax/working-pennsylvanians-tax-credit",
      excerpt: "DOR: 'If you receive the federal EITC, you automatically qualify' \u2014 the credit is 10 percent of the federal Earned Income Tax Credit, maximum $805, REFUNDABLE, and 'the Department of Revenue will automatically calculate your WPTC' from the attached federal 1040. Effective beginning with the 2026 filing season (TY2025 returns, per the DOR's Jan 27, 2026 press release promoting the credit for the then-current season). NOTE: the printed 2025 PA-40 (rev 04-25, which predates the November 2025 budget) has NO WPTC line \u2014 DOR applies it in processing; composers should report it as a DOR-applied credit note. CAVEAT (disclosed): the statutory text's effective-tax-year language should be re-verified against the enrolled act when published; this encoding follows the agency's own filing-season guidance. The $805 cap can bind only via rounding in TY2025 (max federal EITC $8,046 \u2192 10% = $804.60)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2027-01-01",
    output: { type: "money" },
    parameters: {
      pctOfFederal: { value: "10", type: "int" },
      cap: { value: "80500", type: "money" }
      // $805
    },
    formula: rd({
      kind: "min",
      args: [
        { kind: "mulRate", base: ruleRef30("us.federal.eitc"), rate: { num: "10", den: "100" }, round: "half-up" },
        { kind: "param", name: "cap" }
      ]
    })
  }
];

// ../corpus-us-federal/dist/rules/state-other.js
var flatBase = { kind: "max0", arg: fact36("stateTaxableIncome") };
function flatTax(args) {
  return {
    id: `us.${args.st}.income_tax`,
    version: args.version ?? 1,
    jurisdiction: `us.${args.st}`,
    title: `${args.name} income tax \u2014 TY2025 flat rate on state taxable income`,
    citation: { source: args.source, section: args.section, url: args.url, excerpt: args.excerpt },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: args.parameters ?? {},
    formula: {
      kind: "roundToDollar",
      value: {
        kind: "brackets",
        base: flatBase,
        table: [{ threshold: "0", rate: { num: args.ratePctTimes100, den: "10000" } }]
      },
      mode: "half-up"
    }
  };
}
function noIncomeTax(st, name, excerpt, url) {
  return {
    id: `us.${st}.income_tax`,
    version: 1,
    jurisdiction: `us.${st}`,
    title: `${name} \u2014 no individual income tax (TY2025)`,
    citation: {
      source: `${name} law (no individual income tax)`,
      section: "n/a",
      url,
      excerpt
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    formula: { kind: "money", cents: "0" }
  };
}
var otherStateRules = [
  // ---- no-individual-income-tax states (TY2025) --------------------------
  noIncomeTax("ak", "Alaska", "Alaska levies no individual income tax (repealed 1980). No state return exists for wage earners.", "https://tax.alaska.gov/"),
  noIncomeTax("fl", "Florida", "Florida's constitution (Art. VII \xA7 5) prohibits an individual income tax. No state individual return.", "https://floridarevenue.com/"),
  noIncomeTax("nv", "Nevada", "Nevada levies no individual income tax. No state individual return.", "https://tax.nv.gov/"),
  noIncomeTax("sd", "South Dakota", "South Dakota levies no individual income tax. No state individual return.", "https://dor.sd.gov/"),
  noIncomeTax("tn", "Tennessee", "Tennessee levies no individual income tax (the Hall tax on interest/dividends was fully repealed effective 1/1/2021).", "https://www.tn.gov/revenue.html"),
  noIncomeTax("tx", "Texas", "Texas levies no individual income tax (Tex. Const. art. VIII \xA7 24-a requires voter approval to create one). No state individual return.", "https://comptroller.texas.gov/"),
  noIncomeTax("wy", "Wyoming", "Wyoming levies no individual income tax. No state individual return.", "https://revenue.wyo.gov/"),
  {
    id: "us.nh.income_tax",
    version: 1,
    jurisdiction: "us.nh",
    title: "New Hampshire \u2014 no individual income tax (TY2025; interest & dividends tax REPEALED)",
    citation: {
      source: "N.H. RSA 77 (repealed); 2021 N.H. Laws ch. 91",
      section: "RSA 77:1 (repeal effective for tax periods beginning after 12/31/2024)",
      url: "https://www.revenue.nh.gov/",
      excerpt: "New Hampshire's 3% interest-and-dividends tax was phased out and fully REPEALED for taxable periods beginning on or after January 1, 2025 \u2014 TY2025 has NO individual income tax of any kind. For TY2024 and earlier the I&D tax existed; an asOf before 2025 must not use this rule."
    },
    effectiveFrom: "2025-01-01",
    // the I&D tax existed before this date
    output: { type: "money" },
    formula: { kind: "money", cents: "0" }
  },
  {
    id: "us.wa.income_tax",
    version: 1,
    jurisdiction: "us.wa",
    title: "Washington \u2014 no individual income tax on wages; 7% capital gains excise NOT MODELED (refuses)",
    citation: {
      source: "RCW 82.87 (capital gains excise); no general income tax",
      section: "RCW 82.87.040",
      url: "https://dor.wa.gov/taxes-rates/other-taxes/capital-gains-tax",
      excerpt: "Washington levies NO tax on wages or ordinary income. It DOES levy a 7% excise on an individual's Washington-allocated net long-term capital gains above an inflation-indexed standard deduction (~$270,000 for 2024; 2025 amount indexed), plus an additional 2.9% on gains over $1 million (2025 legislation) \u2014 with real-estate and retirement-account exemptions. THIS RULE COVERS THE WAGE ANSWER ONLY ($0); a filer with large WA-allocated long-term gains needs the RCW 82.87 excise computed separately \u2014 compute and disclose, this corpus does not model it."
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "gt", left: fact36("longTermCapitalGains"), right: { kind: "money", cents: "0" } },
      then: {
        kind: "unsupported",
        reason: "Washington taxes no wages, but long-term capital gains are present \u2014 the RCW 82.87 7% capital-gains excise (indexed ~$270k deduction, real-estate/retirement exemptions) is not modeled; compute it separately and disclose"
      },
      else: { kind: "money", cents: "0" }
    }
  },
  // ---- flat-rate states (TY2025, web-verified) ---------------------------
  // (PA graduated to the deep pack: see state-pa.ts — us.pa.income_tax v2+
  //  computes the class-netted base itself; the thin v1 is superseded.)
  flatTax({
    st: "in",
    name: "Indiana",
    ratePctTimes100: "300",
    source: "Ind. Code \xA7 6-3-2-1 (2025 rate 3.00%); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "IC 6-3-2-1(b)",
    url: "https://www.in.gov/dor/",
    excerpt: "Indiana TY2025: 3.00% flat on state AGI (2026 drops to 2.95%). No standard deduction; exemptions $1,000 per taxpayer/spouse (plus $1,500 per dependent child, $1,000 other dependents \u2014 compose into the base). COUNTY income taxes (0.5%-3%+, county-specific) apply ON TOP and are NOT modeled \u2014 name the county and compute separately with disclosure.",
    parameters: {
      exemptionPerFiler: { value: "100000", type: "money" },
      exemptionPerDependentChild: { value: "150000", type: "money" }
    }
  }),
  flatTax({
    st: "mi",
    name: "Michigan",
    ratePctTimes100: "425",
    source: "Mich. Comp. Laws \xA7 206.51 (4.25% for 2025); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "MCL 206.51",
    url: "https://www.michigan.gov/taxes",
    excerpt: "Michigan TY2025: 4.25% flat on Michigan taxable income. Personal exemption $5,800 per filer/dependent (2025, indexed); no standard deduction. City income taxes (Detroit 2.4% resident etc.) NOT modeled \u2014 disclose. Retirement/pension subtraction phase-in (2023 PA 4) not modeled \u2014 disclose for pension filers.",
    parameters: { personalExemption: { value: "580000", type: "money" } }
  }),
  flatTax({
    st: "co",
    name: "Colorado",
    ratePctTimes100: "440",
    source: "Colo. Rev. Stat. \xA7 39-22-104 (4.40%); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "C.R.S. \xA7 39-22-104(1.7)",
    url: "https://tax.colorado.gov/",
    excerpt: "Colorado TY2025: 4.40% flat on FEDERAL TAXABLE INCOME (Form 1040 line 15) plus/minus Colorado modifications \u2014 Colorado is the rare state starting from federal TAXABLE income, so the federal standard/itemized deduction is already embedded; do NOT subtract a separate state deduction. A TABOR-triggered temporary rate reduction may apply in refund years (2024 was 4.25%) \u2014 2025 statutory rate 4.40%; verify any TABOR adjustment for the filing year and disclose. State addback for large itemizers and the pension/annuity subtraction are named but not modeled."
  }),
  flatTax({
    st: "ut",
    name: "Utah",
    ratePctTimes100: "450",
    source: "Utah Code \xA7 59-10-104 as amended by 2025 HB 106 (4.50% RETROACTIVE to 1/1/2025 \u2014 pre-HB 106 surveys still print 4.55%); web-verified July 2026",
    section: "Utah Code \xA7 59-10-104; 2025 Utah Laws ch. (HB 106)",
    url: "https://tax.utah.gov/",
    excerpt: "Utah TY2025: 4.50% flat on Utah taxable income (HB 106, signed March 2025, cut the 4.55% rate retroactively to 1/1/2025; withholding tables only caught up June 2025 \u2014 the ANNUAL return rate is 4.50%). Federal AGI + modifications; Utah has NO standard deduction \u2014 instead a nonrefundable TAXPAYER TAX CREDIT of roughly 6% of federal deductions that PHASES OUT with income, which this rule does not model; compute the credit per Utah TC-40 and disclose."
  }),
  flatTax({
    st: "nc",
    name: "North Carolina",
    ratePctTimes100: "425",
    source: "N.C. Gen. Stat. \xA7 105-153.7 (4.25% for 2025); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "N.C.G.S. \xA7 105-153.7",
    url: "https://www.ncdor.gov/",
    excerpt: "North Carolina TY2025: 4.25% flat on NC taxable income (2026 drops to 3.99%). NC standard deduction $12,750 single/MFS, $25,500 MFJ, $19,125 HOH; NO personal exemptions; a $3,000-per-child deduction (income-tiered) exists for CTC-eligible children \u2014 compose into the base and disclose.",
    parameters: {
      standardDeductionSingle: { value: "1275000", type: "money" },
      standardDeductionJoint: { value: "2550000", type: "money" },
      standardDeductionHoh: { value: "1912500", type: "money" }
    }
  }),
  flatTax({
    st: "az",
    name: "Arizona",
    ratePctTimes100: "250",
    source: "Ariz. Rev. Stat. \xA7 43-1011 (2.5% flat); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "A.R.S. \xA7 43-1011",
    url: "https://azdor.gov/",
    excerpt: "Arizona TY2025: 2.5% flat on Arizona taxable income. Standard deduction mirrors the FEDERAL OBBBA amounts \u2014 $15,750 single/MFS, $31,500 MFJ, $23,625 HOH per the 2025 Form 140 as directed by Executive Order 2025-15 (the legislature's conformity bills were vetoed; H.B. 4168 later moved the conformity date to 1/1/2026 \u2014 the DOR-printed 2025 form amounts are the filing reality). Dependent tax credit $100 per dependent under 17 / $25 for 17+ (income-phased) named but not modeled.",
    parameters: {
      standardDeductionSingle: { value: "1575000", type: "money" },
      standardDeductionJoint: { value: "3150000", type: "money" },
      standardDeductionHoh: { value: "2362500", type: "money" }
    }
  }),
  flatTax({
    st: "ky",
    name: "Kentucky",
    ratePctTimes100: "400",
    source: "Ky. Rev. Stat. \xA7 141.020 (4.0% for 2025; 3.5% for 2026 per 2025 HB 1); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "KRS 141.020",
    url: "https://revenue.ky.gov/",
    excerpt: "Kentucky TY2025: 4.0% flat on KY taxable income (2026 drops to 3.5%). Standard deduction $3,270 PER TAXPAYER (each spouse gets one on a joint return: $6,540 MFJ); no personal exemptions. Pension exclusion up to $31,110 per person named but not modeled \u2014 disclose for retirees.",
    parameters: { standardDeductionPerTaxpayer: { value: "327000", type: "money" } }
  }),
  flatTax({
    st: "ga",
    name: "Georgia",
    ratePctTimes100: "519",
    source: "Ga. Code \xA7 48-7-20 as amended by 2025 HB 111 (5.19% RETROACTIVE to 1/1/2025 \u2014 some 2025 surveys still print the pre-HB 111 5.39%); Tax Foundation survey cross-checked (web-verified July 2026)",
    section: "O.C.G.A. \xA7 48-7-20; 2025 Ga. Laws Act (HB 111)",
    url: "https://dor.georgia.gov/",
    excerpt: "Georgia TY2025: 5.19% flat (HB 111, signed April 2025, cut the scheduled 5.39% retroactively to 1/1/2025; the rate continues stepping toward 4.99%). Standard deduction $12,000 single/MFS/HOH, $24,000 MFJ; dependent exemption $4,000 per dependent (unborn-child rule included); no personal exemption for filers.",
    parameters: {
      standardDeductionSingle: { value: "1200000", type: "money" },
      standardDeductionJoint: { value: "2400000", type: "money" },
      dependentExemption: { value: "400000", type: "money" }
    }
  }),
  flatTax({
    st: "ia",
    name: "Iowa",
    ratePctTimes100: "380",
    source: "Iowa Code \xA7 422.5A (3.8% flat effective 1/1/2025, 2024 SF 2442); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "Iowa Code \xA7 422.5A",
    url: "https://revenue.iowa.gov/",
    excerpt: "Iowa TY2025: 3.8% flat (consolidated from three brackets effective 1/1/2025). Iowa taxable income couples to FEDERAL TAXABLE INCOME with Iowa modifications (federal standard/itemized deduction embedded); retirement income is fully EXEMPT for 55+ filers (2023 HF 2317) \u2014 compose and disclose. A $40/$80 personal exemption CREDIT applies against tax (not modeled)."
  }),
  {
    id: "us.ms.income_tax",
    version: 1,
    jurisdiction: "us.ms",
    title: "Mississippi income tax \u2014 TY2025: 4.4% on taxable income over $10,000",
    citation: {
      source: "Miss. Code \xA7 27-7-5 (4.4% for 2025 under the 2022 phase-down as amended by 2025 HB 1); Tax Foundation 2025 survey (web-verified July 2026)",
      section: "Miss. Code \xA7 27-7-5",
      url: "https://www.dor.ms.gov/",
      excerpt: "Mississippi TY2025: 0% on the first $10,000 of taxable income, 4.4% on the excess (rate steps down annually toward eventual elimination under 2025 HB 1). Standard deduction $2,300 single / $4,600 MFJ; personal exemption $6,000 single / $12,000 MFJ + $1,500 per dependent \u2014 compose into the base."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      standardDeductionSingle: { value: "230000", type: "money" },
      standardDeductionJoint: { value: "460000", type: "money" },
      personalExemptionSingle: { value: "600000", type: "money" },
      personalExemptionJoint: { value: "1200000", type: "money" }
    },
    formula: {
      kind: "roundToDollar",
      value: {
        kind: "brackets",
        base: flatBase,
        table: [
          { threshold: "0", rate: { num: "0", den: "100" } },
          { threshold: "1000000", rate: { num: "440", den: "10000" } }
        ]
      },
      mode: "half-up"
    }
  },
  flatTax({
    st: "la",
    name: "Louisiana",
    ratePctTimes100: "300",
    source: "La. Rev. Stat. \xA7 47:32 as amended by 2024 3d Ex. Sess. Act 11 (3.0% flat effective 1/1/2025); Tax Foundation 2025 survey (web-verified July 2026)",
    section: "La. R.S. 47:32",
    url: "https://revenue.louisiana.gov/",
    excerpt: "Louisiana TY2025: 3.0% flat (replaced the graduated 1.85/3.5/4.25% schedule effective 1/1/2025). Standard deduction $12,500 single/MFS, $25,000 MFJ/HOH/QSS (replaced the combined personal exemption); retirement-income exemptions named but not modeled.",
    parameters: {
      standardDeductionSingle: { value: "1250000", type: "money" },
      standardDeductionJoint: { value: "2500000", type: "money" }
    }
  }),
  {
    id: "us.ma.income_tax",
    version: 1,
    jurisdiction: "us.ma",
    title: "Massachusetts income tax \u2014 TY2025: 5% Part B rate + 4% millionaire surtax",
    citation: {
      source: "Mass. Gen. Laws ch. 62 \xA7 4; Mass. Const. amend. art. CXXI (4% surtax); 2025 surtax threshold $1,083,150 (indexed); Tax Foundation 2025 survey (web-verified July 2026)",
      section: "M.G.L. c. 62 \xA7 4; Const. amend. CXXI",
      url: "https://www.mass.gov/orgs/massachusetts-department-of-revenue",
      excerpt: "Massachusetts TY2025: 5.0% on Part B taxable income (wages/business), PLUS a 4% surtax on total taxable income over $1,083,150 (2025 indexed threshold) \u2014 top effective 9%. Short-term capital gains are taxed at 8.5% and Part A interest/dividends at 5% (compose separately when present \u2014 this rule applies the 5%/9% schedule to the single stateTaxableIncome input and REFUSES nothing silently: disclose ST-gain composition). Personal exemption $4,400 single / $8,800 MFJ; no standard deduction. The 2025 threshold amount should be re-verified from the DOR circular for precise millionaire-surtax returns."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    parameters: {
      personalExemptionSingle: { value: "440000", type: "money" },
      personalExemptionJoint: { value: "880000", type: "money" },
      surtaxThreshold: { value: "108315000", type: "money" }
    },
    formula: {
      kind: "roundToDollar",
      value: {
        kind: "brackets",
        base: flatBase,
        table: [
          { threshold: "0", rate: { num: "500", den: "10000" } },
          { threshold: "108315000", rate: { num: "900", den: "10000" } }
        ]
      },
      mode: "half-up"
    }
  }
];

// ../corpus-us-federal/dist/rules/state-parameters.js
var stateParameterRules = [
  ...ilRules,
  ...vaRules,
  ...caRules,
  ...nyRules,
  ...paRules,
  ...otherStateRules
];

// ../corpus-us-federal/dist/rules/qbi.js
var fact37 = (factId) => ({ kind: "fact", factId });
var money34 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef31 = (ruleId) => ({ kind: "rule", ruleId });
var param17 = (name) => ({ kind: "param", name });
var zero24 = money34("0");
var isStatus11 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact37("filingStatus"),
  right: { kind: "enum", value: status }
});
var pctOf2 = (num, den, base) => ({
  kind: "mulRate",
  base,
  rate: { num, den },
  round: "half-up"
});
var qbi = {
  kind: "max0",
  arg: {
    kind: "sub",
    left: {
      kind: "add",
      args: [
        {
          kind: "max0",
          arg: {
            kind: "sub",
            left: fact37("selfEmploymentNetProfit"),
            right: {
              kind: "add",
              args: [
                ruleRef31("us.federal.se_tax_half_deduction"),
                ruleRef31("us.federal.sep_deduction"),
                ruleRef31("us.federal.sehi_deduction")
              ]
            }
          }
        },
        fact37("k1OrdinaryBusinessIncome")
      ]
    },
    right: fact37("qbiLossOffset")
  }
};
var hasBusinessIncome = {
  kind: "or",
  args: [
    { kind: "cmp", op: "gt", left: fact37("selfEmploymentNetProfit"), right: zero24 },
    { kind: "cmp", op: "gt", left: fact37("k1OrdinaryBusinessIncome"), right: zero24 }
  ]
};
var taxableBeforeQbi = ruleRef31("us.federal.taxable_income_before_qbi");
var wageLimitOf = (w2, ubia) => ({
  kind: "max",
  args: [
    pctOf2("50", "100", w2),
    { kind: "add", args: [pctOf2("25", "100", w2), pctOf2("25", "1000", ubia)] }
    // 2.5%
  ]
});
function phasedReduction(tentative, wageLimit, excess, band) {
  return {
    kind: "sub",
    left: tentative,
    right: {
      kind: "mulDiv",
      a: { kind: "max0", arg: { kind: "sub", left: tentative, right: wageLimit } },
      b: excess,
      c: band,
      round: "half-up"
    }
  };
}
function qbiRule(version, effectiveFrom, effectiveTo, yearLabel, threshold2, bandSingleCents, bandJointCents, source, withMinimum) {
  const band = {
    kind: "if",
    cond: isStatus11("mfj"),
    then: param17("bandJoint"),
    else: param17("band")
  };
  const excess = {
    kind: "max0",
    arg: { kind: "sub", left: taxableBeforeQbi, right: threshold2 }
  };
  const excessCapped = { kind: "min", args: [excess, band] };
  const tentative = pctOf2("20", "100", qbi);
  const w2 = fact37("qbiW2Wages");
  const ubia = fact37("qbiUBIA");
  const nonSstb = {
    kind: "if",
    cond: { kind: "cmp", op: "ge", left: excess, right: band },
    then: { kind: "min", args: [tentative, wageLimitOf(w2, ubia)] },
    else: phasedReduction(tentative, wageLimitOf(w2, ubia), excessCapped, band)
  };
  const remaining = { kind: "max0", arg: { kind: "sub", left: band, right: excess } };
  const scale = (e) => ({
    kind: "mulDiv",
    a: e,
    b: remaining,
    c: band,
    round: "half-up"
  });
  const sstb = {
    kind: "if",
    cond: { kind: "cmp", op: "ge", left: excess, right: band },
    then: zero24,
    else: phasedReduction(pctOf2("20", "100", scale(qbi)), wageLimitOf(scale(w2), scale(ubia)), excessCapped, band)
  };
  const businessPart = {
    kind: "if",
    cond: { kind: "cmp", op: "eq", left: excess, right: zero24 },
    then: tentative,
    // at or below the threshold: no SSTB question, no limits
    else: { kind: "if", cond: fact37("businessIsSSTB"), then: sstb, else: nonSstb }
  };
  const overall = pctOf2("20", "100", {
    kind: "max0",
    arg: {
      kind: "sub",
      left: taxableBeforeQbi,
      right: {
        kind: "add",
        args: [ruleRef31("us.federal.schedule_d.preferential_lt_gain"), fact37("qualifiedDividends")]
      }
    }
  });
  const limited = { kind: "min", args: [businessPart, overall] };
  const withFloor = withMinimum ? {
    // OBBBA § 70105: $400 minimum when QBI ≥ $1,000 (TY2026+)
    kind: "if",
    cond: { kind: "cmp", op: "ge", left: qbi, right: param17("minQbiFloor") },
    then: { kind: "max", args: [limited, param17("minDeduction")] },
    else: limited
  } : limited;
  return {
    id: "us.federal.qbi_deduction",
    version,
    jurisdiction: "us.federal",
    title: `Qualified business income deduction (TY${yearLabel}; full W-2/UBIA + SSTB mechanics)`,
    citation: {
      source: `26 U.S.C. \xA7 199A; ${source}`,
      section: "\xA7 199A(a)\u2013(d)",
      url: "https://www.law.cornell.edu/uscode/text/26/199A",
      excerpt: `20 percent of qualified business income \u2014 QBI reduced by the attributable \xBD-SE-tax, SEP, and SE-health-insurance deductions (Reg. \xA7 1.199A-3(b)(1)(vi)) \u2014 limited to 20 percent of taxable income (without this deduction) less net capital gain. Above the \xA7 199A(e)(2) threshold the deduction is limited to the greater of 50 percent of the business's W-2 wages or 25 percent of W-2 wages plus 2.5 percent of UBIA, phased in over the ${yearLabel === "2025" ? "$50,000 ($100,000 joint) band" : "OBBBA-widened $75,000 ($150,000 joint) band (\xA7 70105, taxable years after 2025)"}; a specified service trade or business scales its items by the applicable percentage and gets nothing past the band.${withMinimum ? " OBBBA \xA7 70105 $400 minimum deduction with at least $1,000 of QBI (active participation assumed \u2014 disclosed)." : ""} [Single trade or business \u2014 no aggregation, no REIT/PTP component; W-2 wages and UBIA default to $0, which is conservative; exact ratios where Form 8995-A rounds percentages \u2014 disclosed.]`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      band: { value: bandSingleCents, type: "money" },
      bandJoint: { value: bandJointCents, type: "money" },
      ...withMinimum ? {
        minDeduction: { value: "40000", type: "money" },
        // $400
        minQbiFloor: { value: "100000", type: "money" }
        // $1,000
      } : {}
    },
    formula: {
      kind: "if",
      cond: hasBusinessIncome,
      then: withFloor,
      else: zero24
    }
  };
}
var qbiRules = [
  qbiRule(
    9,
    // v7 counted qualifiedDividends in the § 199A(a)(2)/(e)(3) overall limit; v9 takes the schedule_d netted LT gain there
    "2025-01-01",
    "2026-01-01",
    "2025",
    {
      kind: "if",
      cond: isStatus11("mfj"),
      then: money34("39460000"),
      // $394,600
      else: money34("19730000")
      // $197,300 (incl. MFS)
    },
    "5000000",
    "10000000",
    // $50,000 / $100,000 band (TCJA)
    "Rev. Proc. 2024-40 \xA7 2.27",
    false
  ),
  qbiRule(
    10,
    // v8 counted qualifiedDividends in the overall limit; v10 takes the schedule_d netted LT gain there
    "2026-01-01",
    "2027-01-01",
    "2026",
    {
      kind: "match",
      on: fact37("filingStatus"),
      cases: [
        { when: "mfj", value: money34("40350000") },
        // $403,500
        { when: "mfs", value: money34("20177500") },
        // $201,775
        { when: "single", value: money34("20175000") },
        // $201,750
        { when: "hoh", value: money34("20175000") },
        { when: "qss", value: money34("20175000") }
      ]
    },
    "7500000",
    "15000000",
    // $75,000 / $150,000 band (OBBBA § 70105)
    "Rev. Proc. 2025-32 \xA7 4.26; OBBBA \xA7 70105 (band + $400 minimum)",
    true
  )
];

// ../corpus-us-federal/dist/rules/self-employment.js
var fact38 = (factId) => ({ kind: "fact", factId });
var money35 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef32 = (ruleId) => ({ kind: "rule", ruleId });
var param18 = (name) => ({ kind: "param", name });
var zero25 = money35("0");
var seNetEarnings = ruleRef32("us.federal.se_net_earnings");
function seTaxRule(version, effectiveFrom, effectiveTo, wageBaseCents, yearLabel) {
  return {
    id: "us.federal.se_tax",
    version,
    jurisdiction: "us.federal",
    title: `Self-employment tax (TY${yearLabel}; Schedule SE line 8a uses socialSecurityWages when given, else box-1 wages)`,
    citation: {
      source: `26 U.S.C. \xA7 1401; \xA7 1402(b); SSA ${yearLabel} contribution and benefit base`,
      section: "\xA7 1401(a) 12.4% OASDI; \xA7 1401(b) 2.9% Medicare",
      url: "https://www.ssa.gov/oact/cola/cbb.html",
      excerpt: `12.4 percent of self-employment income up to the contribution and benefit base (${yearLabel}: as announced by SSA) reduced by W-2 social security wages (Schedule SE line 8a = box 3; approximated by box 1 when socialSecurityWages is not supplied), plus 2.9 percent hospital insurance on all self-employment income. No tax if net earnings are under $400 (\xA7 1402(b)(2)). DEFAULT method matches the printed Schedule SE: line 10 'Multiply the smaller of line 9 or line 10 by 12.4%' and line 11 'Multiply line 6 by 2.9% (0.029)' are each rounded to whole dollars before line 12 'Self-employment tax. Add lines 10 and 11' \u2014 useFormulaMethod opts back to exact-cents arithmetic. The 0.9% Additional Medicare Tax (\xA7 1401(b)(2)) is computed separately.`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      wageBase: { value: wageBaseCents, type: "money" },
      seFloor: { value: "40000", type: "money" }
      // $400
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "lt", left: seNetEarnings, right: param18("seFloor") },
      then: zero25,
      else: {
        kind: "if",
        cond: fact38("useFormulaMethod"),
        then: { kind: "add", args: [oasdiComponent, medicareComponent] },
        else: {
          // printed Schedule SE: lines 10 and 11 each entered in whole dollars
          kind: "add",
          args: [
            { kind: "roundToDollar", value: oasdiComponent, mode: "half-up" },
            { kind: "roundToDollar", value: medicareComponent, mode: "half-up" }
          ]
        }
      }
    }
  };
}
var wagesForBase = {
  kind: "if",
  cond: { kind: "cmp", op: "gt", left: fact38("socialSecurityWages"), right: zero25 },
  then: fact38("socialSecurityWages"),
  else: fact38("wages")
};
var oasdiComponent = {
  kind: "mulRate",
  base: {
    kind: "min",
    args: [
      seNetEarnings,
      {
        kind: "max0",
        arg: {
          kind: "sub",
          left: param18("wageBase"),
          right: wagesForBase
        }
      }
    ]
  },
  rate: { num: "124", den: "1000" },
  round: "half-up"
};
var medicareComponent = {
  kind: "mulRate",
  base: seNetEarnings,
  rate: { num: "29", den: "1000" },
  round: "half-up"
};
var selfEmploymentRules = [
  {
    id: "us.federal.se_net_earnings",
    version: 1,
    jurisdiction: "us.federal",
    title: "Net earnings from self-employment (92.35% of net profit)",
    citation: {
      source: "26 U.S.C. \xA7 1402(a)(12)",
      section: "\xA7 1402(a)(12)",
      url: "https://www.law.cornell.edu/uscode/text/26/1402",
      excerpt: "\u2026reduced by an amount equal to the product of such net earnings and one-half of the [15.3%] rate \u2014 i.e., net profit \xD7 92.35%."
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    formula: {
      kind: "mulRate",
      base: fact38("selfEmploymentNetProfit"),
      rate: { num: "9235", den: "10000" },
      round: "half-up"
    }
  },
  // v5/v6: box-3 socialSecurityWages coordination (v3/v4 added the printed
  // Schedule SE per-line whole-dollar rounding)
  seTaxRule(5, "2025-01-01", "2026-01-01", "17610000", "2025"),
  // $176,100
  seTaxRule(6, "2026-01-01", "2027-01-01", "18450000", "2026"),
  // $184,500
  {
    id: "us.federal.se_tax_half_deduction",
    version: 2,
    // v1 kept exact cents; v2 follows Schedule SE line 13 (whole dollars)
    jurisdiction: "us.federal",
    title: "Deduction for one-half of self-employment tax",
    citation: {
      source: "26 U.S.C. \xA7 164(f); Schedule SE (Form 1040) line 13",
      section: "\xA7 164(f)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/164",
      excerpt: "\u2026there shall be allowed as a deduction\u2026 an amount equal to one-half of the taxes imposed by section 1401 (other than the taxes imposed by section 1401(b)(2)) for such taxable year. [Schedule SE line 13: 'Multiply line 12 by 50% (0.50). Enter here and on Schedule 1 (Form 1040), line 15' \u2014 entered in whole dollars by DEFAULT; useFormulaMethod keeps exact cents.]"
    },
    effectiveFrom: "2025-01-01",
    output: { type: "money" },
    formula: {
      kind: "if",
      cond: fact38("useFormulaMethod"),
      then: {
        kind: "mulRate",
        base: ruleRef32("us.federal.se_tax"),
        rate: { num: "50", den: "100" },
        round: "half-up"
      },
      else: {
        kind: "roundToDollar",
        value: {
          kind: "mulRate",
          base: ruleRef32("us.federal.se_tax"),
          rate: { num: "50", den: "100" },
          round: "half-up"
        },
        mode: "half-up"
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/senior-deduction.js
var fact39 = (factId) => ({ kind: "fact", factId });
var money36 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef33 = (ruleId) => ({ kind: "rule", ruleId });
var param19 = (name) => ({ kind: "param", name });
var isStatus12 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact39("filingStatus"),
  right: { kind: "enum", value: status }
});
var zero26 = money36("0");
function perSeniorNet() {
  return {
    kind: "max0",
    arg: {
      kind: "sub",
      left: param19("perSenior"),
      right: {
        kind: "mulRate",
        base: {
          kind: "max0",
          arg: {
            kind: "sub",
            left: ruleRef33("us.federal.agi"),
            right: ruleRef33("us.federal.senior_deduction.magi_threshold")
          }
        },
        rate: { num: "6", den: "100" },
        round: "half-up"
      }
    }
  };
}
var seniorDeductionRules = [
  {
    id: "us.federal.senior_deduction",
    version: 2,
    // v1 wrongly applied the 6% reduction ONCE to the combined amount;
    // § 151(d)(5)(C) reduces "the $6,000 amount" — i.e., EACH qualified
    // individual's amount — so a joint couple phases out at 12%/dollar
    // (found by the differential harness vs PolicyEngine)
    jurisdiction: "us.federal",
    title: "Temporary deduction for seniors (OBBBA; simplified: MAGI approximated as AGI)",
    citation: {
      source: "26 U.S.C. \xA7 151(d)(5), added by Pub. L. 119-21 (OBBBA) \xA7 70103",
      section: "\xA7 151(d)(5)",
      url: "https://www.law.cornell.edu/uscode/text/26/151",
      excerpt: "There shall be allowed a deduction in an amount equal to $6,000 for each qualified individual\u2026 the $6,000 amount\u2026 shall be reduced (but not below zero) by 6 percent of so much of the taxpayer's modified adjusted gross income as exceeds $75,000 ($150,000 in the case of a joint return). [Taxable years beginning before January 1, 2029. Married taxpayers must file jointly (\xA7 151(d)(5)(C)(v)) \u2014 MFS receives $0. SSN of the qualified individual required on the return \u2014 this corpus assumes that condition is met.]"
    },
    // statutory window: taxable years 2025 through 2028
    effectiveFrom: "2025-01-01",
    effectiveTo: "2029-01-01",
    output: { type: "money" },
    parameters: {
      perSenior: { value: "600000", type: "money" }
      // $6,000
    },
    formula: {
      // § 151(d)(5)(C)(v): married taxpayers must file jointly — MFS gets $0.
      kind: "if",
      cond: isStatus12("mfs"),
      then: zero26,
      else: {
        // Only compute (and only demand the threshold) when a senior exists.
        kind: "if",
        cond: {
          kind: "or",
          args: [
            fact39("isAge65OrOlder"),
            {
              kind: "and",
              args: [isStatus12("mfj"), fact39("spouseIsAge65OrOlder")]
            }
          ]
        },
        then: {
          // EACH qualified individual's $6,000 is reduced by 6% of the
          // excess MAGI (so a joint couple loses 12%/dollar in aggregate,
          // fully phased out at $250,000 joint / $175,000 single)
          kind: "add",
          args: [
            {
              kind: "if",
              cond: fact39("isAge65OrOlder"),
              then: perSeniorNet(),
              else: zero26
            },
            {
              kind: "if",
              cond: {
                kind: "and",
                args: [isStatus12("mfj"), fact39("spouseIsAge65OrOlder")]
              },
              then: perSeniorNet(),
              else: zero26
            }
          ]
        },
        else: zero26
      }
    }
  },
  {
    id: "us.federal.senior_deduction.magi_threshold",
    version: 1,
    jurisdiction: "us.federal",
    title: "Senior deduction MAGI phase-out threshold by filing status",
    citation: {
      source: "26 U.S.C. \xA7 151(d)(5)(C), added by Pub. L. 119-21 (OBBBA) \xA7 70103",
      section: "\xA7 151(d)(5)(C)",
      url: "https://www.law.cornell.edu/uscode/text/26/151",
      excerpt: "\u2026reduced (but not below zero) by 6 percent of so much of the taxpayer's modified adjusted gross income as exceeds $75,000 ($150,000 in the case of a joint return)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2029-01-01",
    output: { type: "money" },
    formula: {
      kind: "match",
      on: fact39("filingStatus"),
      cases: [
        // $150,000 only "in the case of a joint return"; QSS is not a joint
        // return. MFS is excluded from the deduction entirely (never reached).
        { when: "mfj", value: money36("15000000") },
        // $150,000
        { when: "single", value: money36("7500000") },
        // $75,000
        { when: "hoh", value: money36("7500000") },
        // $75,000
        { when: "qss", value: money36("7500000") }
        // $75,000
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/social-security.js
var fact40 = (factId) => ({ kind: "fact", factId });
var money37 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef34 = (ruleId) => ({ kind: "rule", ruleId });
var param20 = (name) => ({ kind: "param", name });
var pct2 = (num, base) => ({
  kind: "mulRate",
  base,
  rate: { num, den: "100" },
  round: "half-up"
});
var magiBase = {
  kind: "sub",
  left: {
    kind: "sub",
    left: {
      kind: "add",
      args: [
        fact40("wages"),
        fact40("taxableInterest"),
        fact40("taxExemptInterest"),
        // § 86(b)(2)(B): "increased by the amount of interest… exempt from tax"
        ruleRef34("us.federal.schedule_d.preferential_lt_gain"),
        fact40("qualifiedDividends"),
        ruleRef34("us.federal.schedule_d.ordinary_st_gain"),
        fact40("ordinaryDividends"),
        fact40("selfEmploymentNetProfit"),
        fact40("k1OrdinaryBusinessIncome"),
        fact40("taxableIraDistributions"),
        fact40("taxablePensionsAndAnnuities"),
        ruleRef34("us.federal.pension.simplified_method_taxable"),
        fact40("unemploymentCompensation"),
        fact40("otherOrdinaryIncome"),
        fact40("scheduleENetIncome"),
        ruleRef34("us.federal.rental.net_income")
      ]
    },
    right: {
      kind: "add",
      args: [
        ruleRef34("us.federal.capital_loss_ordinary_offset"),
        fact40("nonpassiveScheduleELoss")
      ]
    }
  },
  right: {
    kind: "add",
    args: [
      ruleRef34("us.federal.above_the_line_adjustments"),
      ruleRef34("us.federal.ira_deduction")
    ]
  }
};
var ss = fact40("socialSecurityBenefits");
var provisional = { kind: "add", args: [magiBase, pct2("50", ss)] };
var isMfs2 = {
  kind: "cmp",
  op: "eq",
  left: fact40("filingStatus"),
  right: { kind: "enum", value: "mfs" }
};
var isMfj = {
  kind: "cmp",
  op: "eq",
  left: fact40("filingStatus"),
  right: { kind: "enum", value: "mfj" }
};
var byJoint = (joint, other) => ({
  kind: "if",
  cond: isMfj,
  then: param20(joint),
  else: param20(other)
});
var socialSecurityRules = [
  {
    id: "us.federal.taxable_social_security",
    version: 8,
    // v7 read the raw gain facts; the § 86 income measure now takes the schedule_d netted results, the simplified-method pension taxable amount, and computed net rental income
    jurisdiction: "us.federal",
    title: "Taxable Social Security benefits (\xA7 86: the 0/50/85% worksheet)",
    citation: {
      source: "26 U.S.C. \xA7 86",
      section: "\xA7 86(a)\u2013(c)",
      url: "https://www.law.cornell.edu/uscode/text/26/86",
      excerpt: "Gross income includes social security benefits per the \xA7 86(a) two-tier formula: up to 50 percent of benefits as provisional income (MAGI + \xBD of benefits) exceeds the base amount ($25,000; $32,000 joint \u2014 statutory, unindexed, unchanged by the OBBBA), and up to 85 percent above the adjusted base amount ($34,000; $44,000 joint), never more than 85 percent of the benefits. A married individual filing separately who does not live apart the whole year has base amounts of zero \u2014 living-apart status is not modeled, so MFS gets the zero thresholds (conservative, disclosed). [MAGI adds back the \xA7 221 student-loan deduction and includes tax-exempt interest (\xA7 86(b)(2)(B), the taxExemptInterest fact).]"
    },
    effectiveFrom: "2025-01-01",
    // statutory, unindexed: open-ended
    output: { type: "money" },
    parameters: {
      base: { value: "2500000", type: "money" },
      // $25,000
      baseJoint: { value: "3200000", type: "money" },
      // $32,000
      tier2: { value: "3400000", type: "money" },
      // $34,000
      tier2Joint: { value: "4400000", type: "money" },
      // $44,000
      halfGap: { value: "450000", type: "money" },
      // $4,500
      halfGapJoint: { value: "600000", type: "money" }
      // $6,000
    },
    formula: {
      kind: "if",
      cond: { kind: "cmp", op: "eq", left: ss, right: money37("0") },
      then: money37("0"),
      else: {
        kind: "if",
        cond: isMfs2,
        then: {
          kind: "min",
          args: [pct2("85", ss), pct2("85", { kind: "max0", arg: provisional })]
        },
        else: {
          kind: "min",
          args: [
            pct2("85", ss),
            {
              kind: "add",
              args: [
                pct2("85", {
                  kind: "max0",
                  arg: {
                    kind: "sub",
                    left: provisional,
                    right: byJoint("tier2Joint", "tier2")
                  }
                }),
                {
                  kind: "min",
                  args: [
                    {
                      kind: "min",
                      args: [
                        pct2("50", ss),
                        pct2("50", {
                          kind: "max0",
                          arg: {
                            kind: "sub",
                            left: provisional,
                            right: byJoint("baseJoint", "base")
                          }
                        })
                      ]
                    },
                    byJoint("halfGapJoint", "halfGap")
                  ]
                }
              ]
            }
          ]
        }
      }
    }
  }
];

// ../corpus-us-federal/dist/rules/standard-deduction.js
var J27 = "us.federal";
var fact41 = (factId) => ({ kind: "fact", factId });
var money38 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef35 = (ruleId) => ({ kind: "rule", ruleId });
var isStatus13 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact41("filingStatus"),
  right: { kind: "enum", value: status }
});
var standardDeductionRules = [
  {
    id: "us.federal.standard_deduction.base_amount",
    version: 2,
    // v1 held pre-OBBBA Rev. Proc. 2024-40 amounts ($15,000/$30,000/$22,500)
    jurisdiction: J27,
    title: "Basic standard deduction amount by filing status (TY2025, as amended by OBBBA)",
    citation: {
      source: "26 U.S.C. \xA7 63(c), as amended by Pub. L. 119-21 (OBBBA) \xA7 70102",
      section: "\xA7 63(c)(7); OBBBA \xA7 70102; Rev. Proc. 2025-32 \xA7 3.01",
      url: "https://www.irs.gov/pub/irs-drop/rp-25-32.pdf",
      excerpt: "Section 63(c)(7) as amended by the OBBBA provides the standard deduction amounts\u2026 for any taxable year beginning in 2025 as follows: $31,500 (married filing jointly), $23,625 (head of household), $15,750 (single), superseding Rev. Proc. 2024-40 \xA7 2.15(1)."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2026-01-01",
    output: { type: "money" },
    formula: {
      kind: "match",
      on: fact41("filingStatus"),
      cases: [
        { when: "single", value: money38("1575000") },
        // $15,750
        { when: "mfs", value: money38("1575000") },
        // $15,750 (same as single)
        { when: "mfj", value: money38("3150000") },
        // $31,500
        { when: "qss", value: money38("3150000") },
        // $31,500 (§ 1(j)(2)(A): "…and Surviving Spouses")
        { when: "hoh", value: money38("2362500") }
        // $23,625
      ]
    }
  },
  {
    id: "us.federal.standard_deduction.base_amount",
    version: 3,
    jurisdiction: J27,
    title: "Basic standard deduction amount by filing status (TY2026)",
    citation: {
      source: "26 U.S.C. \xA7 63(c); Rev. Proc. 2025-32",
      section: "Rev. Proc. 2025-32 \xA7 4.14(1)",
      url: "https://www.irs.gov/pub/irs-drop/rp-25-32.pdf",
      excerpt: "For taxable years beginning in 2026, the standard deduction amounts under \xA7 63(c)(2) are as follows: $32,200 (married filing jointly or surviving spouse), $24,150 (head of household), $16,100 (single or married filing separately)."
    },
    effectiveFrom: "2026-01-01",
    effectiveTo: "2027-01-01",
    output: { type: "money" },
    formula: {
      kind: "match",
      on: fact41("filingStatus"),
      cases: [
        { when: "single", value: money38("1610000") },
        // $16,100
        { when: "mfs", value: money38("1610000") },
        // $16,100 ("single or married filing separately")
        { when: "mfj", value: money38("3220000") },
        // $32,200
        { when: "qss", value: money38("3220000") },
        // $32,200 (surviving spouse)
        { when: "hoh", value: money38("2415000") }
        // $24,150
      ]
    }
  },
  {
    id: "us.federal.standard_deduction.base",
    version: 1,
    jurisdiction: J27,
    title: "Basic standard deduction",
    citation: {
      source: "26 U.S.C. \xA7 63(c)(2)",
      section: "\xA7 63(c)(2)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "the term 'basic standard deduction' means\u2026"
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    formula: ruleRef35("us.federal.standard_deduction.base_amount")
  },
  {
    id: "us.federal.standard_deduction.dependent_limit",
    version: 1,
    jurisdiction: J27,
    title: "Limitation on basic standard deduction of a dependent",
    citation: {
      source: "26 U.S.C. \xA7 63(c)(5); Rev. Proc. 2024-40 \xA7 2.15(2); Rev. Proc. 2025-32 \xA7 4.14(2)",
      section: "\xA7 63(c)(5)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "For taxable years beginning in 2025 and 2026, the standard deduction amount under \xA7 63(c)(5) for an individual who may be claimed as a dependent by another taxpayer cannot exceed the greater of (1) $1,350, or (2) the sum of $450 and the individual's earned income."
    },
    effectiveFrom: "2025-01-01",
    effectiveTo: "2027-01-01",
    // same dollar amounts apply in 2025 and 2026
    output: { type: "money" },
    applicability: fact41("isClaimedAsDependent"),
    // min(normal base amount, max($1,350, earned income + $450))
    // simplified: earned income approximated as wages
    formula: {
      kind: "min",
      args: [
        ruleRef35("us.federal.standard_deduction.base_amount"),
        {
          kind: "max",
          args: [
            money38("135000"),
            // $1,350
            { kind: "add", args: [fact41("wages"), money38("45000")] }
            // + $450
          ]
        }
      ]
    },
    overrides: { ruleId: "us.federal.standard_deduction.base", priority: 10 }
  },
  {
    id: "us.federal.standard_deduction.mfs_spouse_itemizes",
    version: 1,
    jurisdiction: J27,
    title: "MFS standard deduction is zero when the spouse itemizes",
    citation: {
      source: "26 U.S.C. \xA7 63(c)(6)(A)",
      section: "\xA7 63(c)(6)(A)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "In the case of\u2026 a married individual filing a separate return where either spouse itemizes deductions\u2026 the standard deduction shall be zero."
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    // if-form (not and-form) so the spouse-itemizes question is only ever
    // asked once the filing status is actually known to be MFS
    applicability: {
      kind: "if",
      cond: isStatus13("mfs"),
      then: fact41("spouseItemizes"),
      else: { kind: "bool", value: false }
    },
    formula: money38("0"),
    // § 63(c)(6) zeroes the ENTIRE standard deduction (basic + additional),
    // so this overrides the sum rule, not just the basic component.
    overrides: { ruleId: "us.federal.standard_deduction", priority: 10 }
  },
  additionalRule(1, "2025-01-01", "2026-01-01", "160000", "200000", {
    sourceRevProc: "Rev. Proc. 2024-40 \xA7 2.15(3)",
    year: "2025",
    married: "$1,600",
    unmarried: "$2,000"
  }),
  additionalRule(2, "2026-01-01", "2027-01-01", "165000", "205000", {
    sourceRevProc: "Rev. Proc. 2025-32 \xA7 4.14(3)",
    year: "2026",
    married: "$1,650",
    unmarried: "$2,050"
  }),
  {
    id: "us.federal.standard_deduction",
    version: 1,
    jurisdiction: J27,
    title: "Standard deduction",
    citation: {
      source: "26 U.S.C. \xA7 63(c)(1)",
      section: "\xA7 63(c)(1)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: "the term 'standard deduction' means the sum of (A) the basic standard deduction, and (B) the additional standard deduction."
    },
    effectiveFrom: "2025-01-01",
    // structural: open-ended
    output: { type: "money" },
    formula: {
      kind: "add",
      args: [
        ruleRef35("us.federal.standard_deduction.base"),
        ruleRef35("us.federal.standard_deduction.additional")
      ]
    }
  }
];
function additionalRule(version, effectiveFrom, effectiveTo, marriedCents, unmarriedCents, label) {
  const perStatusAmount = {
    kind: "match",
    on: fact41("filingStatus"),
    cases: [
      // the higher amount is for "unmarried AND not a surviving spouse" —
      // MFS and QSS get the married amount (read from both Rev. Procs.)
      { when: "mfj", value: { kind: "param", name: "amountMarried" } },
      { when: "mfs", value: { kind: "param", name: "amountMarried" } },
      { when: "qss", value: { kind: "param", name: "amountMarried" } },
      { when: "single", value: { kind: "param", name: "amountUnmarried" } },
      { when: "hoh", value: { kind: "param", name: "amountUnmarried" } }
    ]
  };
  const addIf = (cond) => ({
    kind: "if",
    cond,
    then: perStatusAmount,
    else: money38("0")
  });
  return {
    id: "us.federal.standard_deduction.additional",
    version,
    jurisdiction: J27,
    title: `Additional standard deduction for the aged and blind (TY${label.year})`,
    citation: {
      source: `26 U.S.C. \xA7 63(f); ${label.sourceRevProc}`,
      section: "\xA7 63(f)",
      url: "https://www.law.cornell.edu/uscode/text/26/63",
      excerpt: `For taxable years beginning in ${label.year}, the additional standard deduction amount under \xA7 63(f) for the aged or the blind is ${label.married}; increased to ${label.unmarried} if the individual is also unmarried and not a surviving spouse (spouse's items counted on a joint return).`
    },
    effectiveFrom,
    effectiveTo,
    output: { type: "money" },
    parameters: {
      amountMarried: { value: marriedCents, type: "money" },
      amountUnmarried: { value: unmarriedCents, type: "money" }
    },
    formula: {
      kind: "add",
      args: [
        addIf(fact41("isAge65OrOlder")),
        addIf(fact41("isBlind")),
        addIf({
          kind: "and",
          args: [isStatus13("mfj"), fact41("spouseIsAge65OrOlder")]
        }),
        addIf({
          kind: "and",
          args: [isStatus13("mfj"), fact41("spouseIsBlind")]
        })
      ]
    }
  };
}

// ../corpus-us-federal/dist/rules/tips-eligibility.js
var fact42 = (factId) => ({ kind: "fact", factId });
var boolLit = (value) => ({ kind: "bool", value });
var isStatus14 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact42("filingStatus"),
  right: { kind: "enum", value: status }
});
var WINDOW = { effectiveFrom: "2025-01-01", effectiveTo: "2029-01-01" };
var tipsEligibilityRules = [
  {
    id: "us.federal.eligible.tips_occupation",
    version: 1,
    jurisdiction: "us.federal",
    title: "Occupation is on the Treasury Tipped Occupation list (\xA7 224 'customarily and regularly received tips')",
    citation: {
      source: "Treas. Reg. \xA7 1.224-1 (final rule, IR-2026-49, Apr. 10, 2026; proposed 90 Fed. Reg. 45340)",
      section: "\xA7 1.224-1 occupation list (TTC 101\u2013809 + final-rule additions)",
      url: "https://www.irs.gov/newsroom/treasury-irs-issue-final-regulations-listing-occupations-where-workers-customarily-and-regularly-receive-tips-under-the-one-big-beautiful-bill",
      excerpt: "The final regulations list the occupations that customarily and regularly received tips on or before December 31, 2024 \u2014 68 proposed occupations across eight categories (Beverage & Food Service through Transportation & Delivery), with visual artists, floral designers, and gas pump attendants added in the final rule."
    },
    ...WINDOW,
    output: { type: "bool" },
    formula: {
      kind: "match",
      on: fact42("occupation"),
      cases: [
        ...TIPPED_OCCUPATIONS.map((o) => ({ when: o.slug, value: boolLit(true) })),
        { when: "other", value: boolLit(false) }
      ]
    }
  },
  {
    id: "us.federal.eligible.tips_deduction",
    version: 1,
    jurisdiction: "us.federal",
    title: "Eligible to claim the qualified-tips deduction (\xA7 224)",
    citation: {
      source: "26 U.S.C. \xA7 224; Treas. Reg. \xA7 1.224-1 (final, Apr. 10, 2026)",
      section: "\xA7 224(a), (d)",
      url: "https://www.law.cornell.edu/uscode/text/26/224",
      excerpt: "Qualified tips must be received in a listed occupation, paid voluntarily by the customer and not subject to negotiation (service charges excluded), and not received in the course of a \xA7 199A(d)(2) specified service trade or business; married taxpayers must file a joint return. [SSN-on-return condition assumed satisfied \u2014 disclosed.]"
    },
    ...WINDOW,
    output: { type: "bool" },
    formula: {
      // Ordered so the no-new-facts conditions short-circuit first:
      // an MFS filer gets a definitive "false" without being asked their job.
      kind: "and",
      args: [
        { kind: "not", arg: isStatus14("mfs") },
        { kind: "rule", ruleId: "us.federal.eligible.tips_occupation" },
        fact42("tipsWereVoluntary"),
        { kind: "not", arg: fact42("employerIsSSTB") }
      ]
    }
  }
];

// ../corpus-us-federal/dist/rules/tips-overtime.js
var fact43 = (factId) => ({ kind: "fact", factId });
var money39 = (cents2) => ({ kind: "money", cents: cents2 });
var ruleRef36 = (ruleId) => ({ kind: "rule", ruleId });
var param21 = (name) => ({ kind: "param", name });
var zero27 = money39("0");
var isStatus15 = (status) => ({
  kind: "cmp",
  op: "eq",
  left: fact43("filingStatus"),
  right: { kind: "enum", value: status }
});
function cappedPhasedDeduction(qualifiedFactId, cap, ineligible = isStatus15("mfs")) {
  return {
    kind: "if",
    // LAZY FIRST: with no qualified amount, no eligibility facts are ever
    // demanded — a taxpayer without tips is never asked their occupation
    cond: { kind: "cmp", op: "gt", left: fact43(qualifiedFactId), right: zero27 },
    then: {
      kind: "if",
      cond: ineligible,
      then: zero27,
      else: {
        kind: "max0",
        arg: {
          kind: "sub",
          left: {
            kind: "min",
            args: [fact43(qualifiedFactId), cap]
          },
          right: {
            kind: "mulInt",
            base: money39("10000"),
            // $100 per step
            count: {
              kind: "stepUnits",
              value: {
                kind: "max0",
                arg: {
                  kind: "sub",
                  left: ruleRef36("us.federal.agi"),
                  right: {
                    kind: "if",
                    cond: isStatus15("mfj"),
                    then: param21("magiThresholdJoint"),
                    else: param21("magiThreshold")
                  }
                }
              },
              unitCents: "100000",
              // $1,000
              mode: "floor"
              // whole $1,000s only — IRS Schedule 1-A: "round down"
            }
          }
        }
      }
    },
    else: zero27
  };
}
var WINDOW2 = { effectiveFrom: "2025-01-01", effectiveTo: "2029-01-01" };
var THRESHOLD_PARAMS = {
  magiThreshold: { value: "15000000", type: "money" },
  // $150,000
  magiThresholdJoint: { value: "30000000", type: "money" }
  // $300,000
};
var tipsOvertimeRules = [
  {
    id: "us.federal.tips_deduction",
    version: 2,
    // v1 assumed occupation-list qualification; now determined by the eligibility predicate
    jurisdiction: "us.federal",
    title: "Deduction for qualified tips (OBBBA; eligibility fully determined; MAGI \u2248 AGI)",
    citation: {
      source: "26 U.S.C. \xA7 224; Treas. Reg. \xA7 1.224-1 (final, IR-2026-49, Apr. 10, 2026)",
      section: "\xA7 224",
      url: "https://www.law.cornell.edu/uscode/text/26/224",
      excerpt: "The amount allowed as a deduction under this section for any taxable year shall not exceed $25,000\u2026 reduced (but not below zero) by $100 for each $1,000 by which the taxpayer's modified adjusted gross income exceeds $150,000 ($300,000 in the case of a joint return). Eligibility (Treasury occupation list, voluntary non-negotiated tips, no SSTB, joint return if married) is determined by us.federal.eligible.tips_deduction. [Whole $1,000s per IRS Schedule 1-A instructions; SSN assumed satisfied.]"
    },
    ...WINDOW2,
    output: { type: "money" },
    parameters: {
      cap: { value: "2500000", type: "money" },
      // $25,000 (all eligible statuses)
      ...THRESHOLD_PARAMS
    },
    formula: cappedPhasedDeduction("qualifiedTips", param21("cap"), {
      kind: "not",
      arg: { kind: "rule", ruleId: "us.federal.eligible.tips_deduction" }
    })
  },
  {
    id: "us.federal.overtime_deduction",
    version: 1,
    jurisdiction: "us.federal",
    title: "Deduction for qualified overtime compensation (OBBBA; simplified: MAGI approximated as AGI)",
    citation: {
      source: "26 U.S.C. \xA7 225, added by Pub. L. 119-21 (OBBBA)",
      section: "\xA7 225",
      url: "https://www.law.cornell.edu/uscode/text/26/225",
      excerpt: "\u2026shall not exceed $12,500 ($25,000 in the case of a joint return)\u2026 reduced (but not below zero) by $100 for each $1,000 by which the taxpayer's modified adjusted gross income exceeds $150,000 ($300,000 in the case of a joint return). Qualified overtime compensation means the FLSA \xA7 7 premium in excess of the regular rate. [Married taxpayers must file jointly \u2014 MFS gets $0. SSN assumed satisfied.]"
    },
    ...WINDOW2,
    output: { type: "money" },
    parameters: {
      cap: { value: "1250000", type: "money" },
      // $12,500
      capJoint: { value: "2500000", type: "money" },
      // $25,000 (joint return)
      ...THRESHOLD_PARAMS
    },
    formula: cappedPhasedDeduction("qualifiedOvertimePremium", {
      kind: "if",
      cond: isStatus15("mfj"),
      then: param21("capJoint"),
      else: param21("cap")
    })
  }
];

// ../corpus-us-federal/dist/index.js
var corpusInput = {
  name: "@invaro/opentax-corpus-us-federal",
  version: "0.37.0",
  rules: [
    ...corporateRules,
    ...corporate1120Rules,
    ...corporatePenaltyTaxRules,
    ...corporateAnnualizedRules,
    ...internationalAndRemainderRules,
    ...qsbsRules,
    ...standardDeductionRules,
    ...amtRules,
    ...itemizedRules,
    ...educationCreditRules,
    ...iraRules,
    ...ira8606Rules,
    ...scheduleDRules,
    ...rentalRules,
    ...pensionRules,
    ...capitalLossRules,
    ...kiddieRules,
    ...incomeRules,
    ...incomeTaxRules,
    ...ctcRules,
    ...creditBlockRules,
    ...ptcRules,
    ...feieRules,
    ...fiduciaryRules,
    ...residualRules,
    ...seniorDeductionRules,
    ...socialSecurityRules,
    ...tipsOvertimeRules,
    ...tipsEligibilityRules,
    ...carLoanRules,
    ...charityRules,
    ...eitcRules,
    ...employerRules,
    ...selfEmploymentRules,
    ...passiveLossRules,
    ...scheduleRRules,
    ...stateParameterRules,
    ...mileageRules,
    ...adjustmentRules,
    ...qbiRules,
    ...investmentTaxRules,
    ...netTaxRules,
    ...estimatedTaxRules,
    ...annualizedInstallmentRules,
    ...dependentRules
  ],
  facts
};
var cached = null;
function getCorpus() {
  if (!cached)
    cached = loadCorpus(corpusInput);
  return cached;
}
var DEFAULT_TARGET = "us.federal.net_tax";

// dist/flags.js
var STATUS_ALIASES = {
  single: "single",
  mfj: "mfj",
  married: "mfj",
  marriedjoint: "mfj",
  marriedfilingjointly: "mfj",
  joint: "mfj",
  mfs: "mfs",
  marriedseparate: "mfs",
  marriedseparately: "mfs",
  marriedfilingseparately: "mfs",
  separate: "mfs",
  hoh: "hoh",
  headofhousehold: "hoh",
  qss: "qss",
  survivingspouse: "qss",
  qualifyingsurvivingspouse: "qss",
  widow: "qss",
  widower: "qss"
};
function normalizeStatus(raw) {
  const key = raw.toLowerCase().replace(/[^a-z]/g, "");
  return STATUS_ALIASES[key] ?? raw;
}
var FACT_FLAGS = [
  {
    factId: "filingStatus",
    option: "--status <status>",
    key: "status",
    group: "Filing & family:",
    help: "single | married (mfj) | married-separate (mfs) | head-of-household (hoh) | surviving-spouse (qss)",
    boolean: false
  },
  {
    factId: "wages",
    option: "--wages <dollars>",
    key: "wages",
    group: "Income:",
    help: 'wages in dollars, e.g. 50000 or "1234.56"',
    boolean: false
  },
  {
    factId: "taxableInterest",
    option: "--interest <dollars>",
    key: "interest",
    group: "Income:",
    help: "taxable interest in dollars",
    boolean: false
  },
  {
    factId: "qualifyingChildren",
    option: "--kids <n>",
    key: "kids",
    group: "Filing & family:",
    help: "qualifying children under 17 (CTC)",
    boolean: false
  },
  {
    factId: "otherDependents",
    option: "--other-dependents <n>",
    key: "otherDependents",
    group: "Filing & family:",
    help: "dependents other than CTC children ($500 ODC, \xA7 24(h)(4))",
    boolean: false
  },
  {
    factId: "dependentCareExpenses",
    option: "--care-expenses <dollars>",
    key: "careExpenses",
    group: "Filing & family:",
    help: "employment-related dependent care expenses (\xA7 21, net of FSA)",
    boolean: false
  },
  {
    factId: "careQualifyingIndividuals",
    option: "--care-persons <n>",
    key: "carePersons",
    group: "Filing & family:",
    help: "care-credit qualifying individuals (under-13 dependents etc.)",
    boolean: false
  },
  {
    factId: "secondaryEarnedIncome",
    option: "--spouse-earned <dollars>",
    key: "spouseEarned",
    group: "Filing & family:",
    help: "lower-earning spouse's earned income (\xA7 21(d) joint limit)",
    boolean: false
  },
  {
    factId: "saversContributions",
    option: "--savers-contributions <dollars>",
    key: "saversContributions",
    group: "Deductions & credits:",
    help: "taxpayer retirement contributions for the \xA7 25B saver's credit",
    boolean: false
  },
  {
    factId: "saversContributionsSpouse",
    option: "--savers-contributions-spouse <dollars>",
    key: "saversContributionsSpouse",
    group: "Deductions & credits:",
    help: "spouse retirement contributions for the \xA7 25B saver's credit",
    boolean: false
  },
  {
    factId: "isFullTimeStudent",
    option: "--student",
    key: "student",
    group: "Filing & family:",
    help: "taxpayer is a full-time student (denies \xA7 25B)",
    boolean: true
  },
  {
    factId: "spouseIsFullTimeStudent",
    option: "--spouse-student",
    key: "spouseStudent",
    group: "Filing & family:",
    help: "spouse is a full-time student (denies spouse's \xA7 25B)",
    boolean: true
  },
  {
    factId: "qualifiedAdoptionExpenses",
    option: "--adoption-expenses <dollars>",
    key: "adoptionExpenses",
    group: "Deductions & credits:",
    help: "qualified adoption expenses (\xA7 23; up to $5,000/$5,120 refundable)",
    boolean: false
  },
  {
    factId: "adoptionIsSpecialNeeds",
    option: "--adoption-special-needs",
    key: "adoptionSpecialNeeds",
    group: "Deductions & credits:",
    help: "special-needs adoption finalized this year (deemed max credit)",
    boolean: true
  },
  {
    factId: "taxExemptInterest",
    option: "--tax-exempt-interest <dollars>",
    key: "taxExemptInterest",
    group: "Income:",
    help: "tax-exempt interest (counts in \xA7 86 and \xA7 36B MAGI)",
    boolean: false
  },
  {
    factId: "slcspAnnualPremium",
    option: "--slcsp <dollars>",
    key: "slcsp",
    group: "Health coverage (\xA7 36B):",
    help: "annual SLCSP benchmark premium (Form 1095-A line 33B)",
    boolean: false
  },
  {
    factId: "marketplacePremiumsPaid",
    option: "--marketplace-premiums <dollars>",
    key: "marketplacePremiums",
    group: "Health coverage (\xA7 36B):",
    help: "annual Marketplace plan premiums (Form 1095-A line 33A)",
    boolean: false
  },
  {
    factId: "advancePTC",
    option: "--aptc <dollars>",
    key: "aptc",
    group: "Health coverage (\xA7 36B):",
    help: "advance premium tax credit received (Form 1095-A line 33C)",
    boolean: false
  },
  {
    factId: "ptcHouseholdSize",
    option: "--household-size <n>",
    key: "householdSize",
    group: "Health coverage (\xA7 36B):",
    help: "tax family size for the poverty-line lookup (\xA7 36B(d)(1))",
    boolean: false
  },
  {
    factId: "foreignEarnedIncome",
    option: "--foreign-earned <dollars>",
    key: "foreignEarned",
    group: "Income:",
    help: "\xA7 911-qualified foreign earned income (also include in wages/SE)",
    boolean: false
  },
  {
    factId: "fiduciaryType",
    option: "--fiduciary <type>",
    key: "fiduciary",
    group: "Estates & trusts (Form 1041):",
    help: "estate | simple-trust | complex-trust (\xA7 642(b) exemption)",
    boolean: false
  },
  {
    factId: "fiduciaryIncomeBeforeExemption",
    option: "--fiduciary-income <dollars>",
    key: "fiduciaryIncome",
    group: "Estates & trusts (Form 1041):",
    help: "1041 taxable income before the exemption, after distributions",
    boolean: false
  },
  {
    factId: "fiduciaryLongTermGains",
    option: "--fiduciary-gains <dollars>",
    key: "fiduciaryGains",
    group: "Estates & trusts (Form 1041):",
    help: "retained trust/estate capital gains (refuses if positive)",
    boolean: false
  },
  {
    factId: "householdEmployeeCashWages",
    option: "--household-wages <dollars>",
    key: "householdWages",
    group: "Business owner:",
    help: "cash wages to one household employee (Schedule H)",
    boolean: false
  },
  {
    factId: "householdFutaTestMet",
    option: "--household-futa",
    key: "householdFuta",
    group: "Business owner:",
    help: "$1,000+/quarter household wages \u2014 FUTA applies (attested)",
    boolean: true
  },
  {
    factId: "isFarmerOrFisherman",
    option: "--farmer",
    key: "farmer",
    group: "Business owner:",
    help: "\u2154 of gross income from farming/fishing (\xA7 6654(i) safe harbor)",
    boolean: true
  },
  {
    factId: "homeOfficeSquareFeet",
    option: "--home-office-sqft <n>",
    key: "homeOfficeSqft",
    group: "Business owner:",
    help: "home office square feet (simplified method target)",
    boolean: false
  },
  {
    factId: "corpIsREITorRIC",
    option: "--corp-reit-ric",
    key: "corpReitRic",
    group: "Corporate & entity (Form 1120):",
    help: "corporation is a REIT/RIC (refuses \u2014 \xA7\xA7 857/852)",
    boolean: true
  },
  {
    factId: "isAge65OrOlder",
    option: "--age65",
    key: "age65",
    group: "Filing & family:",
    help: "taxpayer is 65 or older",
    boolean: true
  },
  {
    factId: "isBlind",
    option: "--blind",
    key: "blind",
    group: "Filing & family:",
    help: "taxpayer is blind",
    boolean: true
  },
  {
    factId: "spouseIsAge65OrOlder",
    option: "--spouse-age65",
    key: "spouseAge65",
    group: "Filing & family:",
    help: "spouse is 65 or older (mfj)",
    boolean: true
  },
  {
    factId: "spouseIsBlind",
    option: "--spouse-blind",
    key: "spouseBlind",
    group: "Filing & family:",
    help: "spouse is blind (mfj)",
    boolean: true
  },
  {
    factId: "isClaimedAsDependent",
    option: "--dependent",
    key: "dependent",
    group: "Filing & family:",
    help: "taxpayer can be claimed as a dependent",
    boolean: true
  },
  {
    factId: "qualifiedTips",
    option: "--tips <dollars>",
    key: "tips",
    group: "Income:",
    help: "qualified tips included in wages (\xA7 224)",
    boolean: false
  },
  {
    factId: "qualifiedOvertimePremium",
    option: "--overtime <dollars>",
    key: "overtime",
    group: "Income:",
    help: "FLSA overtime premium included in wages (\xA7 225)",
    boolean: false
  },
  {
    factId: "longTermCapitalGains",
    option: "--gains <dollars>",
    key: "gains",
    group: "Income:",
    help: "long-term capital gains + qualified dividends",
    boolean: false
  },
  {
    factId: "qualifiedDividends",
    option: "--qualified-dividends <dollars>",
    key: "qualifiedDividends",
    group: "Income:",
    help: "qualified dividends (\xA7 1(h)(11)); use this standalone when a net capital loss also exists",
    boolean: false
  },
  {
    factId: "shortTermCapitalGains",
    option: "--short-term-gains <dollars>",
    key: "shortTermGains",
    group: "Income:",
    help: "net short-term capital gain (\xA7 1222(5)), ordinary rates; mutually exclusive with netCapitalLoss",
    boolean: false
  },
  {
    factId: "ordinaryDividends",
    option: "--ordinary-dividends <dollars>",
    key: "ordinaryDividends",
    group: "Income:",
    help: "non-qualified dividends (1099-DIV box 1a minus box 1b), ordinary rates",
    boolean: false
  },
  {
    factId: "netCapitalLoss",
    option: "--capital-loss <dollars>",
    key: "capitalLoss",
    group: "Income:",
    help: "OVERALL net capital loss from Schedule D, positive (\xA7 1211(b)); for mixed years use the per-bucket loss flags instead",
    boolean: false
  },
  {
    factId: "shortTermCapitalLoss",
    option: "--short-term-loss <dollars>",
    key: "shortTermLoss",
    group: "Income:",
    help: "net short-term capital LOSS (Schedule D line 7 as a loss, positive); nets against --long-term-gains per \xA7 1222(11)",
    boolean: false
  },
  {
    factId: "longTermCapitalLoss",
    option: "--long-term-loss <dollars>",
    key: "longTermLoss",
    group: "Income:",
    help: "net long-term capital LOSS (Schedule D line 15 as a loss, positive); nets against --short-term-gains per \xA7 1222(9)",
    boolean: false
  },
  {
    factId: "pensionGrossPayments",
    option: "--pension-gross <dollars>",
    key: "pensionGross",
    group: "Retirement:",
    help: "gross annuity payments this year (1099-R box 1); the \xA7 72(d) Simplified Method computes the taxable part",
    boolean: false
  },
  {
    factId: "pensionCostBasis",
    option: "--pension-basis <dollars>",
    key: "pensionBasis",
    group: "Retirement:",
    help: "after-tax investment in the contract at the annuity starting date (1099-R box 9b)",
    boolean: false
  },
  {
    factId: "pensionAgeAtStart",
    option: "--pension-age-at-start <age>",
    key: "pensionAgeAtStart",
    group: "Retirement:",
    help: "age at the ANNUITY STARTING DATE (single-life \xA7 72(d) table: \u226455:360 \u226460:310 \u226465:260 \u226470:210 71+:160)",
    boolean: false
  },
  {
    factId: "pensionIsJointAndSurvivor",
    option: "--pension-joint",
    key: "pensionJoint",
    group: "Retirement:",
    help: "joint-and-survivor annuity (post-1997 start): the combined-ages table applies",
    boolean: true
  },
  {
    factId: "pensionCombinedAgesAtStart",
    option: "--pension-combined-ages <n>",
    key: "pensionCombinedAges",
    group: "Retirement:",
    help: "combined ages at the starting date for --pension-joint (\u2264110:410 \u2264120:360 \u2264130:310 \u2264140:260 141+:210)",
    boolean: false
  },
  {
    factId: "pensionMonthsThisYear",
    option: "--pension-months <1-12>",
    key: "pensionMonths",
    group: "Retirement:",
    help: "monthly payments received this year (default 12)",
    boolean: false
  },
  {
    factId: "pensionBasisPreviouslyRecovered",
    option: "--pension-basis-recovered <dollars>",
    key: "pensionBasisRecovered",
    group: "Retirement:",
    help: "basis recovered tax-free in prior years (lifetime recovery caps at the cost basis)",
    boolean: false
  },
  {
    factId: "rentalIncomeBeforeDepreciation",
    option: "--rental-income-pre-depreciation <dollars>",
    key: "rentalIncomePreDepreciation",
    group: "Income:",
    help: "Schedule E rental result BEFORE depreciation (rents minus cash expenses); the engine subtracts \xA7 168 depreciation",
    boolean: false
  },
  {
    factId: "rentalDepreciableBasis",
    option: "--rental-basis <dollars>",
    key: "rentalBasis",
    group: "Income:",
    help: "depreciable basis of the rental BUILDING (excluding land) \u2014 \xA7 168 GDS 27.5-yr straight line, mid-month",
    boolean: false
  },
  {
    factId: "rentalPlacedInServiceMonth",
    option: "--rental-in-service-month <1-12>",
    key: "rentalInServiceMonth",
    group: "Income:",
    help: "month placed in service THIS year (Pub 946 Table A-6 first-year row); omit for property in service all year (3.636%)",
    boolean: false
  },
  {
    factId: "selfEmploymentNetProfit",
    option: "--se-profit <dollars>",
    key: "seProfit",
    group: "Income:",
    help: "self-employment net profit (Schedule C)",
    boolean: false
  },
  {
    factId: "scheduleCNetLoss",
    option: "--schedule-c-loss <dollars>",
    key: "scheduleCLoss",
    group: "Income:",
    help: "Schedule C/F net loss as a positive number (reduces gross and earned income; no SE tax)",
    boolean: false
  },
  {
    factId: "qbiW2Wages",
    option: "--qbi-w2-wages <dollars>",
    key: "qbiW2Wages",
    group: "Business owner:",
    help: "W-2 wages paid by the business (\xA7 199A above-threshold wage limit)",
    boolean: false
  },
  {
    factId: "qbiUBIA",
    option: "--qbi-ubia <dollars>",
    key: "qbiUbia",
    group: "Business owner:",
    help: "UBIA of the business's qualified property (\xA7 199A 2.5% prong)",
    boolean: false
  },
  {
    factId: "businessIsSSTB",
    option: "--qbi-sstb",
    key: "qbiSstb",
    group: "Business owner:",
    help: "the business is a specified service trade or business (\xA7 199A(d)(2))",
    boolean: true
  },
  {
    factId: "businessIsSSTB",
    option: "--qbi-not-sstb",
    key: "qbiNotSstb",
    group: "Business owner:",
    help: "the business is NOT an SSTB",
    boolean: true,
    setValue: false
  },
  {
    factId: "sepContribution",
    option: "--sep <dollars>",
    key: "sep",
    group: "Deductions & savings:",
    help: "self-employed SEP/solo-401(k) employer contribution (\xA7 404(h))",
    boolean: false
  },
  {
    factId: "selfEmployedHealthPremiums",
    option: "--se-health <dollars>",
    key: "seHealth",
    group: "Deductions & savings:",
    help: "self-employed health insurance premiums (\xA7 162(l))",
    boolean: false
  },
  {
    factId: "employeeAnnualWages",
    option: "--employee-wages <dollars>",
    key: "employeeWages",
    group: "Business owner:",
    help: "one employee's annual wages (employer payroll-tax target)",
    boolean: false
  },
  {
    factId: "qreCurrentYear",
    option: "--qre <dollars>",
    key: "qre",
    group: "Business owner:",
    help: "qualified research expenses this year (\xA7 41 ASC target)",
    boolean: false
  },
  {
    factId: "qreAvgPrior3Years",
    option: "--qre-prior-avg <dollars>",
    key: "qrePriorAvg",
    group: "Business owner:",
    help: "average QREs of the 3 prior years (0 = the 6% startup rate)",
    boolean: false
  },
  {
    factId: "k1OrdinaryBusinessIncome",
    option: "--k1 <dollars>",
    key: "k1",
    group: "Income:",
    help: "K-1 box 1 pass-through business income (S-corp/partnership; QBI-eligible, not SE-taxed)",
    boolean: false
  },
  {
    factId: "hsaContribution",
    option: "--hsa <dollars>",
    key: "hsa",
    group: "Deductions & savings:",
    help: "personal HSA contributions (\xA7 223)",
    boolean: false
  },
  {
    factId: "hsaCoverage",
    option: "--hsa-coverage <type>",
    key: "hsaCoverage",
    group: "Deductions & savings:",
    help: "HDHP coverage: self | family",
    boolean: false
  },
  {
    factId: "isAge55OrOlder",
    option: "--age55",
    key: "age55",
    group: "Deductions & savings:",
    help: "taxpayer is 55+ (HSA catch-up)",
    boolean: true
  },
  {
    factId: "studentLoanInterest",
    option: "--student-loan <dollars>",
    key: "studentLoan",
    group: "Deductions & savings:",
    help: "student loan interest paid (\xA7 221)",
    boolean: false
  },
  {
    factId: "carLoanInterest",
    option: "--car-loan <dollars>",
    key: "carLoan",
    group: "Deductions & savings:",
    help: "qualified vehicle loan interest paid (\xA7 163(h)(4))",
    boolean: false
  },
  {
    factId: "charitableCashContributions",
    option: "--charity <dollars>",
    key: "charity",
    group: "Deductions & savings:",
    help: "cash charitable contributions (\xA7 170(p) non-itemizer, or Schedule A when itemizing)",
    boolean: false
  },
  {
    factId: "stateAndLocalTaxesPaid",
    option: "--salt <dollars>",
    key: "salt",
    group: "Deductions & savings:",
    help: "state and local income/sales + property taxes paid (\xA7 164, OBBBA cap)",
    boolean: false
  },
  {
    factId: "forceItemized",
    option: "--force-itemized",
    key: "forceItemized",
    group: "Deductions & savings:",
    help: "honor an explicit \xA7 63(e) election to itemize even when the standard deduction is larger",
    boolean: true,
    setValue: true
  },
  {
    factId: "casualtyFederalDisasterLosses",
    option: "--casualty-disaster <dollars>",
    key: "casualtyDisaster",
    group: "Deductions & savings:",
    help: "federally-declared-disaster casualty losses after per-event $100 floors (\xA7 165(h); 10% AGI floor applied by the engine)",
    boolean: false
  },
  {
    factId: "casualtyQualifiedDisasterLosses",
    option: "--casualty-qualified <dollars>",
    key: "casualtyQualified",
    group: "Deductions & savings:",
    help: "net QUALIFIED disaster losses after per-event $500 floors (no AGI floor; adds to the standard deduction when not itemizing)",
    boolean: false
  },
  {
    factId: "socialSecurityWages",
    option: "--ss-wages <dollars>",
    key: "ssWages",
    group: "Income:",
    help: "W-2 box 3 social security wages (Schedule SE line 8a coordination; defaults to box-1 wages)",
    boolean: false
  },
  {
    factId: "medicareWages",
    option: "--medicare-wages <dollars>",
    key: "medicareWages",
    group: "Income:",
    help: "W-2 box 5 Medicare wages (Form 8959 base; defaults to box-1 wages)",
    boolean: false
  },
  {
    factId: "scheduleENetIncome",
    option: "--schedule-e <dollars>",
    key: "scheduleE",
    group: "Income:",
    help: "net Schedule E rental/royalty income after \xA7 469, non-QBI (QBI-eligible goes in --k1)",
    boolean: false
  },
  {
    factId: "passiveActivityIncome",
    option: "--passive-income <dollars>",
    key: "passiveIncome",
    group: "Income:",
    help: "net passive-activity income (absorbs passive losses; target us.federal.passive_loss_allowed)",
    boolean: false
  },
  {
    factId: "selfRentalNetIncome",
    option: "--self-rental-income <dollars>",
    key: "selfRentalIncome",
    group: "Income:",
    help: "self-rental subset of --passive-income, nonpassive per Reg. 1.469-2(f)(6) (cannot absorb losses)",
    boolean: false
  },
  {
    factId: "passiveActivityLosses",
    option: "--passive-losses <dollars>",
    key: "passiveLosses",
    group: "Income:",
    help: "total passive-activity losses incl. carryovers, positive number (\xA7 469 / Form 8582)",
    boolean: false
  },
  {
    factId: "rentalActiveParticipationLosses",
    option: "--rental-active-losses <dollars>",
    key: "rentalActiveLosses",
    group: "Income:",
    help: "portion of passive losses from active-participation rental real estate (\xA7 469(i) allowance)",
    boolean: false
  },
  {
    factId: "mfsLivedApartAllYear",
    option: "--mfs-lived-apart",
    key: "mfsLivedApart",
    group: "Filing & family:",
    help: "MFS filer lived apart from spouse at ALL times (\xA7 469(i)(5): $12,500 allowance instead of $0)",
    boolean: true,
    setValue: true
  },
  {
    factId: "isRetiredOnTotalDisability",
    option: "--retired-disabled",
    key: "retiredDisabled",
    group: "Filing & family:",
    help: "retired on permanent/total disability with physician's statement (Schedule R, \xA7 22(b))",
    boolean: true,
    setValue: true
  },
  {
    factId: "scheduleRDisabilityIncome",
    option: "--disability-income <dollars>",
    key: "disabilityIncome",
    group: "Income:",
    help: "\xA7\xA7 72/105(a) taxable disability income (caps the under-65 Schedule R initial amount)",
    boolean: false
  },
  {
    factId: "nontaxableBenefitsForScheduleR",
    option: "--schr-nontaxable <dollars>",
    key: "schrNontaxable",
    group: "Income:",
    help: "nontaxable SS/RRB/VA benefits reducing the \xA7 22 amount (Schedule R)",
    boolean: false
  },
  {
    factId: "qbiLossOffset",
    option: "--qbi-loss-offset <dollars>",
    key: "qbiLossOffset",
    group: "Business owner:",
    help: "QBI-eligible losses outside --se-profit/--k1 (e.g. nonpassive rental loss) reducing the \xA7 199A base",
    boolean: false
  },
  {
    factId: "nonpassiveScheduleELoss",
    option: "--schedule-e-loss <dollars>",
    key: "scheduleELoss",
    group: "Income:",
    help: "nonpassive Schedule E loss (\xA7 469(c)(7) RE professional), positive number",
    boolean: false
  },
  {
    factId: "stateTaxableIncome",
    option: "--state-taxable <dollars>",
    key: "stateTaxable",
    group: "Income:",
    help: "state taxable income for the state tax targets (us.ca/us.va/us.il/us.ny income_tax)",
    boolean: false
  },
  {
    factId: "paCompensationAdjustment",
    option: "--pa-comp-adjustment <dollars>",
    key: "paCompAdjustment",
    group: "Income:",
    help: "PA W-2 Box 16 minus Box 1 (401(k) deferrals are PA-taxable; may be negative; also taxable cost-recovery early distributions) (us.pa.taxable_income)",
    boolean: false
  },
  {
    factId: "paUnreimbursedBusinessExpenses",
    option: "--pa-ue-expenses <dollars>",
    key: "paUeExpenses",
    group: "Income:",
    help: "PA Schedule UE unreimbursed employee business expenses (PA-40 line 1b)",
    boolean: false
  },
  {
    factId: "paInterestIncome",
    option: "--pa-interest <dollars>",
    key: "paInterest",
    group: "Income:",
    help: "PA-taxable interest (PA-40 line 2, gross class \u2014 includes commercial-annuity interest)",
    boolean: false
  },
  {
    factId: "paDividendIncome",
    option: "--pa-dividends <dollars>",
    key: "paDividends",
    group: "Income:",
    help: "PA-taxable dividends incl. mutual-fund capital-gain distributions (PA-40 line 3)",
    boolean: false
  },
  {
    factId: "paBusinessNetIncome",
    option: "--pa-business-net <dollars>",
    key: "paBusinessNet",
    group: "Income:",
    help: "taxpayer's own PA business/profession/farm net income or loss (PA-40 line 4; loss never crosses classes or spouses)",
    boolean: false
  },
  {
    factId: "paSpouseBusinessNetIncome",
    option: "--pa-spouse-business-net <dollars>",
    key: "paSpouseBusinessNet",
    group: "Income:",
    help: "spouse's own PA business net income or loss (PA-40 line 4)",
    boolean: false
  },
  {
    factId: "paPropertyGainNet",
    option: "--pa-property-gain <dollars>",
    key: "paPropertyGain",
    group: "Income:",
    help: "taxpayer's own PA net gain/loss from disposition of property (PA-40 line 5; no carryover)",
    boolean: false
  },
  {
    factId: "paSpousePropertyGainNet",
    option: "--pa-spouse-property-gain <dollars>",
    key: "paSpousePropertyGain",
    group: "Income:",
    help: "spouse's own PA net gain/loss from disposition of property (PA-40 line 5)",
    boolean: false
  },
  {
    factId: "paRentRoyaltyNet",
    option: "--pa-rent-royalty <dollars>",
    key: "paRentRoyalty",
    group: "Income:",
    help: "taxpayer's own PA net rents/royalties/patents/copyrights (PA-40 line 6; <30-day rentals are business)",
    boolean: false
  },
  {
    factId: "paSpouseRentRoyaltyNet",
    option: "--pa-spouse-rent-royalty <dollars>",
    key: "paSpouseRentRoyalty",
    group: "Income:",
    help: "spouse's own PA net rents/royalties/patents/copyrights (PA-40 line 6)",
    boolean: false
  },
  {
    factId: "paEstateTrustIncome",
    option: "--pa-estate-trust <dollars>",
    key: "paEstateTrust",
    group: "Income:",
    help: "PA estate/trust income (PA-40 line 7, Schedule J; never negative)",
    boolean: false
  },
  {
    factId: "paGamblingWinnings",
    option: "--pa-gambling <dollars>",
    key: "paGambling",
    group: "Income:",
    help: "PA gambling and lottery winnings net of wager costs (PA-40 line 8; noncash PA Lottery prizes exempt)",
    boolean: false
  },
  {
    factId: "pa529Contributions",
    option: "--pa-529 <dollars>",
    key: "pa529",
    group: "Adjustments:",
    help: "PA Schedule O 529 deduction, caller-capped at $19,000 per beneficiary per taxpayer-spouse (2025)",
    boolean: false
  },
  {
    factId: "paAbleContributions",
    option: "--pa-able <dollars>",
    key: "paAble",
    group: "Adjustments:",
    help: "PA Schedule O ABLE deduction, capped at the federal gift-tax exclusion ($19,000 in 2025)",
    boolean: false
  },
  {
    factId: "paMsaHsaContributions",
    option: "--pa-msa-hsa <dollars>",
    key: "paMsaHsa",
    group: "Adjustments:",
    help: "PA Schedule O MSA + HSA deduction (federal-allowed amounts)",
    boolean: false
  },
  {
    factId: "paSpDependentChildren",
    option: "--pa-sp-dependents <count>",
    key: "paSpDependents",
    group: "Credits:",
    help: "PA Schedule SP dependent children count (+$9,500 each on the Tax Forgiveness threshold) (us.pa.tax_forgiveness)",
    boolean: false
  },
  {
    factId: "paEligibilityIncomeAddbacks",
    option: "--pa-eligibility-addbacks <dollars>",
    key: "paEligibilityAddbacks",
    group: "Credits:",
    help: "PA Schedule SP nontaxable eligibility-income add-backs (gifts, inheritances, non-PA income, ...) (us.pa.tax_forgiveness)",
    boolean: false
  },
  {
    factId: "paResidentCredit",
    option: "--pa-resident-credit <dollars>",
    key: "paResidentCredit",
    group: "Credits:",
    help: "PA resident credit for tax paid other states (PA-40 line 22; subtracts before Tax Forgiveness)",
    boolean: false
  },
  {
    factId: "mfsAbuseOrAbandonmentException",
    option: "--mfs-abuse-abandonment-relief",
    key: "mfsAbuseAbandonmentRelief",
    group: "Health coverage (\xA7 36B):",
    help: "Reg. 1.36B-2(b)(2) abuse/abandonment relief (MFS can claim PTC)",
    boolean: true
  },
  {
    factId: "hsaDistributions",
    option: "--hsa-distributions <dollars>",
    key: "hsaDistributions",
    group: "Common situations:",
    help: "HSA distributions received (1099-SA box 1)",
    boolean: false
  },
  {
    factId: "hsaQualifiedMedicalExpenses",
    option: "--hsa-medical <dollars>",
    key: "hsaMedical",
    group: "Common situations:",
    help: "qualified medical expenses paid from HSA distributions",
    boolean: false
  },
  {
    factId: "hsaDistributionPenaltyExempt",
    option: "--hsa-penalty-exempt",
    key: "hsaPenaltyExempt",
    group: "Common situations:",
    help: "65+/death/disability exception to the 20% HSA additional tax",
    boolean: true
  },
  {
    factId: "mfsConsideredUnmarried",
    option: "--mfs-considered-unmarried",
    key: "mfsConsideredUnmarried",
    group: "Filing & family:",
    help: "MFS filer meets the 32(d)(2) separation tests (unlocks EITC)",
    boolean: true
  },
  {
    factId: "spouseEducatorExpenses",
    option: "--spouse-educator-expenses <dollars>",
    key: "spouseEducatorExpenses",
    group: "Deductions & credits:",
    help: "spouse's eligible educator expenses (separate $300 cap)",
    boolean: false
  },
  {
    factId: "alimonyPaidPre2019",
    option: "--alimony-paid-pre2019 <dollars>",
    key: "alimonyPaidPre2019",
    group: "Deductions & credits:",
    help: "alimony paid under a pre-2019 instrument (deductible)",
    boolean: false
  },
  {
    factId: "alimonyReceivedPre2019",
    option: "--alimony-received-pre2019 <dollars>",
    key: "alimonyReceivedPre2019",
    group: "Income:",
    help: "alimony received under a pre-2019 instrument (taxable)",
    boolean: false
  },
  {
    factId: "federalEstimatedPayments",
    option: "--estimated-payments <dollars>",
    key: "estimatedPayments",
    group: "Common situations:",
    help: "federal estimated payments + prior-year overpayment applied (line 26)",
    boolean: false
  },
  {
    factId: "federalExtensionPayment",
    option: "--extension-payment <dollars>",
    key: "extensionPayment",
    group: "Common situations:",
    help: "payment made with Form 4868 extension (line 31)",
    boolean: false
  },
  {
    factId: "hasChildUnderSix",
    option: "--child-under-six",
    key: "childUnderSix",
    group: "Income:",
    help: "at least one qualifying child under age 6 at year end (CA Young Child Tax Credit)",
    boolean: true
  },
  {
    factId: "caRentedPrincipalResidence",
    option: "--ca-renter",
    key: "caRenter",
    group: "Income:",
    help: "rented a CA principal residence for at least half the year, not a dependent, property not tax-exempt (CA renter's credit)",
    boolean: true
  },
  {
    factId: "caAmti",
    option: "--ca-amti <dollars>",
    key: "caAmti",
    group: "Income:",
    help: "CA alternative minimum taxable income, agent-composed per R&TC \xA7\xA7 17062(c)-(e)/17062.1 (us.ca.amt)",
    boolean: false
  },
  {
    factId: "caRegularTax",
    option: "--ca-regular-tax <dollars>",
    key: "caRegularTax",
    group: "Income:",
    help: "CA 'regular tax' (Form 540 line 31 net of exemption credits) for the Schedule P TMT comparison (us.ca.amt)",
    boolean: false
  },
  {
    factId: "nyYonkersBase",
    option: "--ny-yonkers-base <dollars>",
    key: "nyYonkersBase",
    group: "Income:",
    help: "NY Yonkers surcharge base (IT-201-I Yonkers worksheet line m = line 46 minus the ESCC/IT-214/IT-216/EIC/IT-209/IT-272/69-69a/ATT-13 credit bundle) for us.ny.yonkers_surcharge (16.75%, IT-201 line 55)",
    boolean: false
  },
  {
    factId: "nyIt214Fagi",
    option: "--ny-it214-fagi <dollars>",
    key: "nyIt214Fagi",
    group: "Income:",
    help: "federal AGI for the NY IT-214 Real Property Tax Credit (Form IT-214 line 8); over $18,000 disqualifies (us.ny.it214)",
    boolean: false
  },
  {
    factId: "nyIt214Eligible",
    option: "--ny-it214-eligible",
    key: "nyIt214Eligible",
    group: "Income:",
    help: "attests the NY IT-214 non-income eligibility gates: 6+ month residency, not a dependent, assessed value <= $85,000, average monthly adjusted rent <= $450 (us.ny.it214)",
    boolean: true
  },
  {
    factId: "nyIt214TotalRent",
    option: "--ny-it214-total-rent <dollars>",
    key: "nyIt214TotalRent",
    group: "Income:",
    help: "IT-214 line 11 total rent paid (excluding subsidized portions); the engine applies the printed line-12 percentage menu and $450 monthly cap (us.ny.it214)",
    boolean: false
  },
  {
    factId: "nyIt214RentPercent",
    option: "--ny-it214-rent-percent <pct>",
    key: "nyIt214RentPercent",
    group: "Income:",
    help: "IT-214 line 12 printed percentage menu: 50 (heat+gas+electricity+furnishings+board), 75 (+furnishings), 80 (heat+gas+electricity), 85 (heat or heat+gas), 100 (none)",
    boolean: false
  },
  {
    factId: "nyIt214MonthsPaid",
    option: "--ny-it214-months <n>",
    key: "nyIt214MonthsPaid",
    group: "Income:",
    help: "IT-214 line 13 divisor: months rent was paid during the year (us.ny.it214)",
    boolean: false
  },
  {
    factId: "nyIt214HomeownerTaxes",
    option: "--ny-it214-homeowner-taxes <dollars>",
    key: "nyIt214HomeownerTaxes",
    group: "Income:",
    help: "IT-214 line 17: homeowners' real property taxes plus special assessments (us.ny.it214)",
    boolean: false
  },
  {
    factId: "nyIt216StateCredit",
    option: "--ny-it216-state-credit <dollars>",
    key: "nyIt216StateCredit",
    group: "Income:",
    help: "NY State child and dependent care credit (Form IT-216 line 14) \u2014 Worksheet 2 line 1 starting amount for the NYC credit (us.ny.nyc_cdcc)",
    boolean: false
  },
  {
    factId: "nyIt216Under4Expenses",
    option: "--ny-it216-under4-expenses <dollars>",
    key: "nyIt216Under4Expenses",
    group: "Income:",
    help: "IT-216 line 23 qualified care expenses for children under 4 \u2014 numerator of the NYC credit proration (us.ny.nyc_cdcc)",
    boolean: false
  },
  {
    factId: "nyIt216TotalExpenses",
    option: "--ny-it216-total-expenses <dollars>",
    key: "nyIt216TotalExpenses",
    group: "Income:",
    help: "IT-216 line 3a total qualified care expenses \u2014 denominator of the NYC credit proration, capped at 100% (us.ny.nyc_cdcc)",
    boolean: false
  },
  {
    factId: "businessMilesDriven",
    option: "--business-miles <n>",
    key: "businessMiles",
    group: "Business owner:",
    help: "business miles for the standard mileage rate (70\xA2 2025 / 72.5\xA2 2026; standalone target)",
    boolean: false
  },
  {
    factId: "niitInvestmentExpenses",
    option: "--niit-expenses <dollars>",
    key: "niitExpenses",
    group: "Investor (AMT / QSBS):",
    help: "Form 8960 lines 9-10 deductions properly allocable to investment income (\xA7 1411(c)(1)(B))",
    boolean: false
  },
  {
    factId: "investmentInterestDeduction",
    option: "--investment-interest <dollars>",
    key: "investmentInterest",
    group: "Deductions & savings:",
    help: "deductible investment interest (\xA7 163(d), post-Form-4952 limit attested)",
    boolean: false
  },
  {
    factId: "gamblingLossesItemized",
    option: "--gambling-losses <dollars>",
    key: "gamblingLosses",
    group: "Deductions & savings:",
    help: "gambling losses (\xA7 165(d), Schedule A line 16) \u2014 already limited to winnings by the preparer; 100% for TY2025, OBBBA 90% haircut begins TY2026",
    boolean: false
  },
  {
    factId: "otherItemizedDeductions",
    option: "--other-itemized <dollars>",
    key: "otherItemized",
    group: "Deductions & savings:",
    help: "Schedule A line 16 write-ins (impairment-related work expenses, estate tax on IRD, bond premium), attested total",
    boolean: false
  },
  {
    factId: "noncashCharitableContributions",
    option: "--charity-noncash <dollars>",
    key: "charityNoncash",
    group: "Deductions & savings:",
    help: "noncash charitable contributions allowed this year (Form 8283; AGI limits attested)",
    boolean: false
  },
  {
    factId: "otherOrdinaryIncome",
    option: "--other-income <dollars>",
    key: "otherIncome",
    group: "Income:",
    help: "Schedule 1 line 8 other income (gambling W-2G, prizes, Alaska PFD, jury duty)",
    boolean: false
  },
  {
    factId: "eitcAdditionalQualifyingChildren",
    option: "--eitc-extra-children <n>",
    key: "eitcExtraChildren",
    group: "Filing & family:",
    help: "EIC-only qualifying children beyond the CTC count (under 19, students under 24, disabled any age)",
    boolean: false
  },
  {
    factId: "mortgageInterestPaid",
    option: "--mortgage-interest <dollars>",
    key: "mortgageInterest",
    group: "Deductions & savings:",
    help: "home mortgage interest on acquisition debt (\xA7 163(h)(3))",
    boolean: false
  },
  {
    factId: "mortgageAverageBalance",
    option: "--mortgage-balance <dollars>",
    key: "mortgageBalance",
    group: "Deductions & savings:",
    help: "average mortgage balance for the year ($750k limit proration)",
    boolean: false
  },
  {
    factId: "medicalExpenses",
    option: "--medical <dollars>",
    key: "medical",
    group: "Deductions & savings:",
    help: "unreimbursed medical expenses (\xA7 213, over 7.5% of AGI)",
    boolean: false
  },
  {
    factId: "educatorExpenses",
    option: "--educator-expenses <dollars>",
    key: "educatorExpenses",
    group: "Deductions & savings:",
    help: "eligible educator classroom expenses (\xA7 62(a)(2)(D); engine caps at $300/educator)",
    boolean: false
  },
  {
    factId: "otherAdjustments",
    option: "--other-adjustments <dollars>",
    key: "otherAdjustments",
    group: "Deductions & savings:",
    help: "Schedule 1 lines 24a-z write-in adjustments (jury pay to employer, reforestation, SUB repayment, 501(c)(18)(D), discrimination/whistleblower attorney fees), attested total",
    boolean: false
  },
  {
    factId: "earlyWithdrawalPenaltyPaid",
    option: "--early-withdrawal-penalty <dollars>",
    key: "earlyWithdrawalPenalty",
    group: "Deductions & savings:",
    help: "penalty on early withdrawal of savings (1099-INT box 2; \xA7 62(a)(9))",
    boolean: false
  },
  {
    factId: "iraContribution",
    option: "--ira <dollars>",
    key: "ira",
    group: "Deductions & savings:",
    help: "traditional IRA contributions (\xA7 219)",
    boolean: false
  },
  {
    factId: "isActivePlanParticipant",
    option: "--covered-by-plan",
    key: "coveredByPlan",
    group: "Deductions & savings:",
    help: "covered by an employer retirement plan (W-2 box 13)",
    boolean: true
  },
  {
    factId: "isActivePlanParticipant",
    option: "--not-covered-by-plan",
    key: "notCoveredByPlan",
    group: "Deductions & savings:",
    help: "NOT covered by an employer retirement plan",
    boolean: true,
    setValue: false
  },
  {
    factId: "spouseIsActivePlanParticipant",
    option: "--spouse-covered-by-plan",
    key: "spouseCoveredByPlan",
    group: "Deductions & savings:",
    help: "spouse is covered by an employer retirement plan",
    boolean: true
  },
  {
    factId: "spouseIsActivePlanParticipant",
    option: "--spouse-not-covered-by-plan",
    key: "spouseNotCoveredByPlan",
    group: "Deductions & savings:",
    help: "spouse is NOT covered by an employer retirement plan",
    boolean: true,
    setValue: false
  },
  {
    factId: "isAge50OrOlder",
    option: "--age50",
    key: "age50",
    group: "Deductions & savings:",
    help: "taxpayer is 50+ (IRA catch-up)",
    boolean: true
  },
  {
    factId: "aotcExpensesStudent1",
    option: "--aotc <dollars>",
    key: "aotc",
    group: "Education credits:",
    help: "qualified expenses, first AOTC student (\xA7 25A(b))",
    boolean: false
  },
  {
    factId: "aotcExpensesStudent2",
    option: "--aotc2 <dollars>",
    key: "aotc2",
    group: "Education credits:",
    help: "qualified expenses, second AOTC student",
    boolean: false
  },
  {
    factId: "aotcExpensesStudent3",
    option: "--aotc3 <dollars>",
    key: "aotc3",
    group: "Education credits:",
    help: "qualified expenses, third AOTC student",
    boolean: false
  },
  {
    factId: "llcQualifiedExpenses",
    option: "--llc <dollars>",
    key: "llc",
    group: "Education credits:",
    help: "Lifetime Learning Credit expenses, per return (\xA7 25A(c))",
    boolean: false
  },
  {
    factId: "isSubjectToKiddieTax",
    option: "--kiddie-tax-child",
    key: "kiddieTaxChild",
    group: "Filing & family:",
    help: "taxpayer is a \xA7 1(g) kiddie-tax child (Form 8615 tax; no refundable AOTC)",
    boolean: true
  },
  {
    factId: "parentTaxableIncome",
    option: "--parent-taxable-income <dollars>",
    key: "parentTaxableIncome",
    group: "Filing & family:",
    help: "the parent's taxable income (Form 8615 line 6)",
    boolean: false
  },
  {
    factId: "parentFilingStatus",
    option: "--parent-status <status>",
    key: "parentStatus",
    group: "Filing & family:",
    help: "the parent's filing status (Form 8615)",
    boolean: false
  },
  {
    factId: "parentHasPreferentialIncome",
    option: "--parent-has-gains",
    key: "parentHasGains",
    group: "Filing & family:",
    help: "parent has capital gains/qualified dividends (Form 8615 refuses \u2014 worksheet not modeled)",
    boolean: true
  },
  {
    factId: "parentHasPreferentialIncome",
    option: "--parent-no-gains",
    key: "parentNoGains",
    group: "Filing & family:",
    help: "parent has NO preferential-rate income",
    boolean: true,
    setValue: false
  },
  {
    factId: "socialSecurityBenefits",
    option: "--social-security <dollars>",
    key: "socialSecurity",
    group: "Income:",
    help: "social security benefits received (SSA-1099 box 5, \xA7 86)",
    boolean: false
  },
  {
    factId: "taxableIraDistributions",
    option: "--ira-distributions <dollars>",
    key: "iraDistributions",
    group: "Income:",
    help: "taxable IRA distributions (1099-R)",
    boolean: false
  },
  {
    factId: "taxablePensionsAndAnnuities",
    option: "--pensions <dollars>",
    key: "pensions",
    group: "Income:",
    help: "taxable pensions and annuities (1099-R box 2a)",
    boolean: false
  },
  {
    factId: "unemploymentCompensation",
    option: "--unemployment <dollars>",
    key: "unemployment",
    group: "Income:",
    help: "unemployment compensation (\xA7 85)",
    boolean: false
  },
  {
    factId: "earlyDistributionSubjectToPenalty",
    option: "--early-distribution <dollars>",
    key: "earlyDistribution",
    group: "Income:",
    help: "early retirement distribution subject to the 10% \xA7 72(t) tax",
    boolean: false
  },
  {
    factId: "ira8606Basis",
    option: "--ira8606-basis <dollars>",
    key: "ira8606Basis",
    group: "Income:",
    help: "Form 8606 line 2 prior-years IRA basis, per individual (use with target us.federal.ira8606.taxable_amount)",
    boolean: false
  },
  {
    factId: "ira8606NondeductibleContributions",
    option: "--ira8606-nondeductible <dollars>",
    key: "ira8606Nondeductible",
    group: "Income:",
    help: "Form 8606 line 1 nondeductible IRA contributions for the year",
    boolean: false
  },
  {
    factId: "ira8606YearEndValue",
    option: "--ira8606-value <dollars>",
    key: "ira8606Value",
    group: "Income:",
    help: "Form 8606 line 6 Dec-31 value of ALL this individual's trad/SEP/SIMPLE IRAs + outstanding rollovers",
    boolean: false
  },
  {
    factId: "ira8606Distributions",
    option: "--ira8606-distributions <dollars>",
    key: "ira8606Distributions",
    group: "Income:",
    help: "Form 8606 line 7 distributions (excluding rollovers, conversions, QCDs)",
    boolean: false
  },
  {
    factId: "ira8606Conversions",
    option: "--ira8606-conversions <dollars>",
    key: "ira8606Conversions",
    group: "Income:",
    help: "Form 8606 line 8 net amount converted to Roth IRAs",
    boolean: false
  },
  {
    factId: "amtIsoExerciseSpread",
    option: "--iso-spread <dollars>",
    key: "isoSpread",
    group: "Investor (AMT / QSBS):",
    help: "ISO exercise bargain element held past year-end (AMT, \xA7 56(b)(3))",
    boolean: false
  },
  {
    factId: "amtOtherPreferences",
    option: "--amt-preferences <dollars>",
    key: "amtPreferences",
    group: "Investor (AMT / QSBS):",
    help: "other Form 6251 adjustments/preferences (PAB interest, depreciation, ...)",
    boolean: false
  },
  {
    factId: "federalTaxWithheld",
    option: "--withheld <dollars>",
    key: "withheld",
    group: "Payments & estimated tax:",
    help: "federal tax withheld so far \u2014 switches the answer to balance due / refund expected",
    boolean: false
  },
  {
    factId: "isAtLeastAge25",
    option: "--age25plus",
    key: "age25plus",
    group: "Filing & family:",
    help: "taxpayer is at least 25 (childless EITC age floor)",
    boolean: true
  },
  {
    factId: "spouseItemizes",
    option: "--spouse-itemizes",
    key: "spouseItemizes",
    group: "Filing & family:",
    help: "spouse itemizes on a separate return (MFS)",
    boolean: true
  },
  {
    factId: "occupation",
    option: "--occupation <job>",
    key: "occupation",
    group: "Income:",
    help: 'tipped occupation (fuzzy-matched to the Treasury list), or "other"',
    boolean: false
  },
  {
    factId: "tipsWereVoluntary",
    option: "--service-charges",
    key: "serviceCharges",
    group: "Income:",
    help: "tips were mandatory service charges (NOT voluntary) \u2014 disqualifies \xA7 224",
    boolean: true,
    setValue: false
  },
  {
    factId: "employerIsSSTB",
    option: "--sstb",
    key: "sstb",
    group: "Income:",
    help: "tips received from a specified service trade or business \u2014 disqualifies \xA7 224",
    boolean: true
  },
  {
    factId: "useFormulaMethod",
    option: "--formula",
    key: "formula",
    group: "Expert:",
    help: "exact \xA7 1 formula instead of the Tax Table under $100k (analysis mode)",
    boolean: true
  },
  {
    factId: "entityLegalForm",
    option: "--entity <form>",
    key: "entity",
    group: "Corporate & entity (Form 1120):",
    help: "business legal form: llc | corporation (classification determinations)",
    boolean: false
  },
  {
    factId: "llcMemberCount",
    option: "--members <n>",
    key: "members",
    group: "Corporate & entity (Form 1120):",
    help: "number of LLC members",
    boolean: false
  },
  {
    factId: "filedForm8832CorpElection",
    option: "--form-8832",
    key: "form8832",
    group: "Corporate & entity (Form 1120):",
    help: "entity elected corporate classification (Form 8832)",
    boolean: true
  },
  {
    factId: "filedForm2553SElection",
    option: "--s-election",
    key: "sElection",
    group: "Corporate & entity (Form 1120):",
    help: "entity filed a timely S election (Form 2553)",
    boolean: true
  },
  {
    factId: "sCorpShareholderCount",
    option: "--shareholders <n>",
    key: "shareholders",
    group: "Corporate & entity (Form 1120):",
    help: "shareholder count, families aggregated (\xA7 1361(b): max 100)",
    boolean: false
  },
  {
    factId: "sCorpHasIneligibleShareholder",
    option: "--ineligible-shareholder",
    key: "ineligibleShareholder",
    group: "Corporate & entity (Form 1120):",
    help: "a shareholder is ineligible for S status (e.g. nonresident alien)",
    boolean: true
  },
  {
    factId: "sCorpHasMultipleStockClasses",
    option: "--multiple-stock-classes",
    key: "multipleStockClasses",
    group: "Corporate & entity (Form 1120):",
    help: "more than one class of stock (disqualifies S status)",
    boolean: true
  },
  {
    factId: "corpTaxableIncome",
    option: "--corp-taxable-income <dollars>",
    key: "corpTaxableIncome",
    group: "Corporate & entity (Form 1120):",
    help: "C corporation taxable income, Form 1120 line 30 (\xA7 11 flat 21%)",
    boolean: false
  },
  {
    factId: "corpAvgAdjustedFinancialStatementIncome",
    option: "--corp-afsi <dollars>",
    key: "corpAfsi",
    group: "Corporate & entity (Form 1120):",
    help: "3-yr average adjusted financial statement income (over $1B refuses \u2014 CAMT)",
    boolean: false
  },
  {
    factId: "corpGrossIncome",
    option: "--corp-gross-income <dollars>",
    key: "corpGrossIncome",
    group: "Corporate & entity (Form 1120):",
    help: "corporation's gross income, incl. dividends received",
    boolean: false
  },
  {
    factId: "corpOrdinaryDeductions",
    option: "--corp-deductions <dollars>",
    key: "corpDeductions",
    group: "Corporate & entity (Form 1120):",
    help: "ordinary business deductions (not charitable/DRD/NOL)",
    boolean: false
  },
  {
    factId: "corpCharitableContributions",
    option: "--corp-charity <dollars>",
    key: "corpCharity",
    group: "Corporate & entity (Form 1120):",
    help: "corporate charitable contributions (10% ceiling; 1% floor from 2026)",
    boolean: false
  },
  {
    factId: "corpDividendsReceived",
    option: "--corp-dividends <dollars>",
    key: "corpDividends",
    group: "Corporate & entity (Form 1120):",
    help: "intercorporate dividends received (\xA7 243 DRD)",
    boolean: false
  },
  {
    factId: "corpDrdOwnershipTier",
    option: "--corp-drd-tier <tier>",
    key: "corpDrdTier",
    group: "Corporate & entity (Form 1120):",
    help: "ownership of the payer: under-20 | 20-to-80 | 80-plus",
    boolean: false
  },
  {
    factId: "corpEquipmentPurchases",
    option: "--corp-equipment <dollars>",
    key: "corpEquipment",
    group: "Corporate & entity (Form 1120):",
    help: "qualified property placed in service (\xA7 168(k) 100% bonus)",
    boolean: false
  },
  {
    factId: "corpDomesticResearch",
    option: "--corp-domestic-rd <dollars>",
    key: "corpDomesticRd",
    group: "Corporate & entity (Form 1120):",
    help: "domestic R&E, expensed currently (\xA7 174A)",
    boolean: false
  },
  {
    factId: "corpForeignResearch",
    option: "--corp-foreign-rd <dollars>",
    key: "corpForeignRd",
    group: "Corporate & entity (Form 1120):",
    help: "foreign R&E, 15-year amortization (\xA7 174)",
    boolean: false
  },
  {
    factId: "corpBusinessInterestExpense",
    option: "--corp-interest <dollars>",
    key: "corpInterest",
    group: "Corporate & entity (Form 1120):",
    help: "business interest expense (\xA7 163(j) 30% ATI limit)",
    boolean: false
  },
  {
    factId: "corpAvgGrossReceipts3yr",
    option: "--corp-gross-receipts <dollars>",
    key: "corpGrossReceipts",
    group: "Corporate & entity (Form 1120):",
    help: "3-yr average gross receipts (\xA7 448(c): $31M/$32M exemption)",
    boolean: false
  },
  {
    factId: "qsbsGain",
    option: "--qsbs-gain <dollars>",
    key: "qsbsGain",
    group: "Investor (AMT / QSBS):",
    help: "gain on qualified small business stock (\xA7 1202)",
    boolean: false
  },
  {
    factId: "qsbsHoldingPeriodYears",
    option: "--qsbs-years <n>",
    key: "qsbsYears",
    group: "Investor (AMT / QSBS):",
    help: "whole years the QSBS was held",
    boolean: false
  },
  {
    factId: "qsbsAcquiredAfterJuly2025",
    option: "--qsbs-post-obbba",
    key: "qsbsPostObbba",
    group: "Investor (AMT / QSBS):",
    help: "stock acquired after July 4, 2025 (tiered 50/75/100% regime)",
    boolean: true
  },
  {
    factId: "qsbsAcquiredAfterJuly2025",
    option: "--qsbs-pre-obbba",
    key: "qsbsPreObbba",
    group: "Investor (AMT / QSBS):",
    help: "stock acquired on or before July 4, 2025 ($10M, 100% at 5 years)",
    boolean: true,
    setValue: false
  },
  {
    factId: "qsbsAcquiredAfterSept2010",
    option: "--qsbs-post-2010",
    key: "qsbsPost2010",
    group: "Investor (AMT / QSBS):",
    help: "pre-OBBBA stock acquired after September 27, 2010",
    boolean: true
  },
  {
    factId: "corpNOLCarryforward",
    option: "--corp-nol <dollars>",
    key: "corpNol",
    group: "Corporate & entity (Form 1120):",
    help: "NOL carryforward available (\xA7 172, 80% limit)",
    boolean: false
  },
  {
    factId: "corpFDDEI",
    option: "--corp-fddei <dollars>",
    key: "corpFddei",
    group: "Corporate & entity (Form 1120):",
    help: "FDII (2025) / FDDEI (2026+) for the \xA7 250 deduction",
    boolean: false
  },
  {
    factId: "corpNCTI",
    option: "--corp-ncti <dollars>",
    key: "corpNcti",
    group: "Corporate & entity (Form 1120):",
    help: "GILTI inclusion + \xA7 78 gross-up (2025) / NCTI (2026+)",
    boolean: false
  },
  {
    factId: "generalBusinessCredits",
    option: "--gbc <dollars>",
    key: "gbc",
    group: "Corporate & entity (Form 1120):",
    help: "current-year \xA7 38(b) general business credits",
    boolean: false
  },
  {
    factId: "corpSection179Cost",
    option: "--corp-179 <dollars>",
    key: "corp179",
    group: "Corporate & entity (Form 1120):",
    help: "\xA7 179-elected property cost (incl. qualified real property; no vehicles)",
    boolean: false
  },
  {
    factId: "corpFiscalYearFiler",
    option: "--corp-fiscal-year",
    key: "corpFiscalYear",
    group: "Corporate & entity (Form 1120):",
    help: "corporation uses a fiscal/short taxable year (refuses \u2014 \xA7\xA7 15/443)",
    boolean: true
  },
  {
    factId: "corpIsCoveredCorporation",
    option: "--corp-listed",
    key: "corpListed",
    group: "Corporate & entity (Form 1120):",
    help: "domestic corporation with stock on an established market (\xA7 4501)",
    boolean: true
  },
  {
    factId: "corpStockRepurchasedFMV",
    option: "--corp-buybacks <dollars>",
    key: "corpBuybacks",
    group: "Corporate & entity (Form 1120):",
    help: "FMV of own stock repurchased this year (\xA7 4501 1% excise)",
    boolean: false
  },
  {
    factId: "corpStockIssuedFMV",
    option: "--corp-stock-issued <dollars>",
    key: "corpStockIssued",
    group: "Corporate & entity (Form 1120):",
    help: "FMV of stock issued this year (\xA7 4501(c)(3) netting)",
    boolean: false
  },
  {
    factId: "corpSection245ADividends",
    option: "--corp-foreign-dividends <dollars>",
    key: "corpForeignDividends",
    group: "Corporate & entity (Form 1120):",
    help: "\xA7 245A-eligible foreign-sub dividends (100% DRD; attested conditions)",
    boolean: false
  },
  {
    factId: "aggregateBusinessLoss",
    option: "--business-loss <dollars>",
    key: "businessLoss",
    group: "Business owner:",
    help: "net aggregate business loss, positive number (\xA7 461(l) target)",
    boolean: false
  },
  {
    factId: "corpForeignTaxesGeneral",
    option: "--corp-foreign-taxes <dollars>",
    key: "corpForeignTaxes",
    group: "Corporate & entity (Form 1120):",
    help: "general-basket foreign taxes (\xA7 904 limitation)",
    boolean: false
  },
  {
    factId: "corpForeignTaxesNCTI",
    option: "--corp-ncti-foreign-taxes <dollars>",
    key: "corpNctiForeignTaxes",
    group: "Corporate & entity (Form 1120):",
    help: "CFC-basket foreign taxes (\xA7 960(d): 80%/90% allowance)",
    boolean: false
  },
  {
    factId: "corpCapitalGains",
    option: "--corp-capital-gains <dollars>",
    key: "corpCapitalGains",
    group: "Corporate & entity (Form 1120):",
    help: "corporate capital gains (\xA7 1211(a))",
    boolean: false
  },
  {
    factId: "corpCapitalLosses",
    option: "--corp-capital-losses <dollars>",
    key: "corpCapitalLosses",
    group: "Corporate & entity (Form 1120):",
    help: "corporate capital losses incl. carryovers used (gains-only offset)",
    boolean: false
  },
  {
    factId: "corpPriorYearTax",
    option: "--corp-prior-tax <dollars>",
    key: "corpPriorTax",
    group: "Corporate & entity (Form 1120):",
    help: "prior-year corporate tax (\xA7 6655 safe harbor)",
    boolean: false
  },
  {
    factId: "corpIsLargeCorporation",
    option: "--large-corporation",
    key: "largeCorporation",
    group: "Corporate & entity (Form 1120):",
    help: "$1M+ taxable income in any of the 3 preceding years (\xA7 6655(g)(2))",
    boolean: true
  },
  {
    factId: "corpIsLargeCorporation",
    option: "--not-large-corporation",
    key: "notLargeCorporation",
    group: "Corporate & entity (Form 1120):",
    help: "NOT a \xA7 6655(g)(2) large corporation",
    boolean: true,
    setValue: false
  },
  {
    factId: "priorYearTax",
    option: "--prior-tax <dollars>",
    key: "priorTax",
    group: "Payments & estimated tax:",
    help: "total tax on last year's return (\xA7 6654 safe harbor)",
    boolean: false
  },
  {
    factId: "priorYearAGI",
    option: "--prior-agi <dollars>",
    key: "priorAgi",
    group: "Payments & estimated tax:",
    help: "AGI on last year's return (110% prong over $150k)",
    boolean: false
  }
];
var BY_FACT = /* @__PURE__ */ new Map();
for (const f of FACT_FLAGS) {
  if (!BY_FACT.has(f.factId))
    BY_FACT.set(f.factId, f);
}
function flagFor(factId) {
  return BY_FACT.get(factId)?.option;
}
function factsFromOptions(opts) {
  const out = {};
  for (const f of FACT_FLAGS) {
    const v = opts[f.key];
    if (f.boolean) {
      if (v === true)
        out[f.factId] = f.setValue ?? true;
    } else if (v !== void 0) {
      let value = v;
      if (typeof v === "string") {
        if (f.factId === "filingStatus" || f.factId === "parentFilingStatus") {
          value = normalizeStatus(v);
        }
        if (f.factId === "occupation") {
          const match = matchOccupation(v);
          if ("slug" in match)
            value = match.slug;
          else {
            throw new FactValidationError(match.candidates.length ? `"${v}" matches several listed occupations: ${match.candidates.join(", ")} \u2014 pick one` : `"${v}" is not on the Treasury Tipped Occupation list \u2014 pass one of the listed slugs, or "other" if the job is not listed (run \`opentax facts\` for details)`);
          }
        }
      }
      out[f.factId] = value;
    }
  }
  return out;
}

// dist/render/format.js
function formatMoney(cents2) {
  let n = typeof cents2 === "bigint" ? cents2 : BigInt(cents2);
  const sign = n < 0n ? "-" : "";
  if (n < 0n)
    n = -n;
  const dollars2 = n / 100n;
  const rem = (n % 100n).toString().padStart(2, "0");
  const grouped = dollars2.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
  return `${sign}$${grouped}.${rem}`;
}
function formatValue(tv) {
  switch (tv.type) {
    case "money":
      return formatMoney(tv.value);
    case "int":
      return String(tv.value);
    case "bool":
      return tv.value ? "true" : "false";
    case "enum":
      return String(tv.value);
  }
}
function formatAnswer(tv) {
  if (tv.type === "money" && BigInt(tv.value) < 0n) {
    return `${formatMoney(tv.value)} (refund)`;
  }
  return formatValue(tv);
}

// dist/render/output.js
var import_picocolors = __toESM(require_picocolors(), 1);
var EXIT = {
  OK: 0,
  ERROR: 1,
  NEEDS_FACTS: 2,
  NOT_COVERED: 3
};
function hintFor(err) {
  switch (err.code) {
    case "NEEDS_FACTS": {
      const flags = err.missing.map((m) => flagFor(m.factId)).filter(Boolean).join(" ");
      return flags ? `Add ${flags} (or put the facts in your --facts file), then re-run. \`opentax facts\` lists every input.` : "Provide the listed facts and re-run. `opentax facts` lists every input.";
    }
    case "NO_APPLICABLE_RULE":
      return "The corpus has no rule for this date or case. Check `opentax corpus list`, or see CONTRIBUTING.md to add coverage.";
    case "UNHANDLED_ENUM_CASE":
      return "This situation isn't encoded yet, so the engine refuses instead of guessing. See the coverage table in the README, or CONTRIBUTING.md to add it.";
    case "NOT_MODELED":
      return "The corpus deliberately does not model this region of the law yet (the rule says why). The refusal is the correct answer \u2014 do not approximate around it.";
    case "FACT_INVALID":
      return "Fix the input value. `opentax facts` shows the expected type of every fact.";
    default:
      return "This looks like invalid input or a bug \u2014 please open an issue with the command you ran.";
  }
}
function exitCodeFor(err) {
  if (err instanceof NeedsFactsError)
    return EXIT.NEEDS_FACTS;
  if (err instanceof NoApplicableRuleError)
    return EXIT.NOT_COVERED;
  if (err instanceof UnhandledEnumCaseError)
    return EXIT.NOT_COVERED;
  if (err instanceof NotModeledError)
    return EXIT.NOT_COVERED;
  return EXIT.ERROR;
}
function emitError(err, json) {
  const code = exitCodeFor(err);
  if (!(err instanceof OpenTaxError)) {
    const message = err instanceof Error ? err.message : String(err);
    if (json) {
      print({ ok: false, error: { code: "ERROR", message, data: {}, hint: hintForGeneric() } });
    } else {
      console.error(import_picocolors.default.red(`
error: ${message}
`));
    }
    return code;
  }
  if (json) {
    print({ ok: false, error: { ...err.toJSON(), hint: hintFor(err) } });
    return code;
  }
  if (err instanceof NeedsFactsError) {
    console.error(import_picocolors.default.red("\nCannot answer without these facts:"));
    for (const m of err.missing) {
      const kind = m.type === "enum" && m.enumValues ? `enum: ${m.enumValues.join("|")}` : m.type === "money" ? "dollars" : m.type;
      const flag = flagFor(m.factId);
      console.error(`  ${import_picocolors.default.red("\u2717")} ${import_picocolors.default.bold(m.factId)} (${kind})${flag ? import_picocolors.default.dim(`  \u2192 ${flag}`) : ""}`);
      console.error(`     ${import_picocolors.default.dim(m.description)}`);
    }
    console.error(import_picocolors.default.dim(`
${hintFor(err)}
`));
    return code;
  }
  console.error(import_picocolors.default.red(`
${err.code}: ${err.message}`));
  console.error(import_picocolors.default.dim(`${hintFor(err)}
`));
  return code;
}
function hintForGeneric() {
  return "This looks like invalid input or a bug \u2014 please open an issue with the command you ran.";
}
function print(obj) {
  console.log(JSON.stringify(obj, null, 2));
}

// dist/commands/check.js
function runCheck(flags) {
  const corpus = getCorpus();
  const asOf = flags.asOf ?? (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  let plain = {};
  if (flags.facts) {
    try {
      plain = JSON.parse(readFileSync(flags.facts, "utf8"));
    } catch (err) {
      return emitError(new Error(`cannot read facts file ${flags.facts}: ${err.message}`), flags.json);
    }
  }
  try {
    Object.assign(plain, factsFromOptions(flags));
    const target = flags.target ?? (plain.federalTaxWithheld !== void 0 ? "us.federal.balance_due" : DEFAULT_TARGET);
    const expected = parseDollars(flags.expect);
    const tolerance = parseDollars(flags.tolerance ?? "1");
    const facts2 = coerceFacts(corpus, plain);
    const { value, proof } = evaluate(corpus, facts2, { asOf, target });
    if (value.type !== "money") {
      return emitError(new Error(`check requires a money-valued target, got ${value.type}`), flags.json);
    }
    const actual = value.cents;
    const diff = actual - expected;
    const abs = diff < 0n ? -diff : diff;
    const verified = abs <= tolerance;
    if (flags.proof) {
      writeFileSync(flags.proof, JSON.stringify(proof, null, 2) + "\n");
    }
    if (flags.json) {
      print({
        ok: true,
        verdict: verified ? "verified" : "refuted",
        target,
        asOf,
        claimed: expected.toString(),
        actual: actual.toString(),
        formattedClaimed: formatMoney(expected),
        formattedActual: formatMoney(actual),
        differenceCents: diff.toString(),
        toleranceCents: tolerance.toString(),
        assumptions: proof.assumptions,
        corpusMerkleRoot: proof.corpus.merkleRoot,
        artifactHash: proof.artifactHash
      });
      return verified ? EXIT.OK : EXIT.ERROR;
    }
    console.log();
    if (verified) {
      console.log(`${import_picocolors2.default.green("\u2713 VERIFIED")}  claimed ${import_picocolors2.default.bold(formatMoney(expected))} \u2014 the corpus derives ${import_picocolors2.default.bold(formatMoney(actual))}${abs > 0n ? import_picocolors2.default.dim(` (within $${(Number(tolerance) / 100).toFixed(2)} tolerance)`) : ""}`);
    } else {
      console.log(`${import_picocolors2.default.red("\u2717 REFUTED")}   claimed ${import_picocolors2.default.bold(formatMoney(expected))}, but the law derives ${import_picocolors2.default.bold(import_picocolors2.default.red(formatMoney(actual)))} ${import_picocolors2.default.dim(`(off by ${formatMoney(abs)})`)}`);
      console.log(import_picocolors2.default.dim(`  run \`opentax eval\` with the same inputs for the full cited derivation`));
    }
    if (proof.assumptions.length > 0) {
      console.log(import_picocolors2.default.dim(`  ${proof.assumptions.length} assumption(s) \u2014 if the taxpayer's situation differs, pass the real facts`));
    }
    console.log(import_picocolors2.default.dim(`  corpus ${proof.corpus.merkleRoot.slice(0, 23)}\u2026`));
    console.log();
    return verified ? EXIT.OK : EXIT.ERROR;
  } catch (err) {
    return emitError(err, flags.json);
  }
}

// dist/commands/corpus.js
var import_picocolors3 = __toESM(require_picocolors(), 1);
function runCorpusExport() {
  const corpus = getCorpus();
  print({
    name: corpusInput.name,
    version: corpusInput.version,
    merkleRoot: corpus.merkleRoot,
    rules: corpusInput.rules,
    facts: corpusInput.facts
  });
  return EXIT.OK;
}
function runCorpusHash(flags = {}) {
  const corpus = getCorpus();
  if (flags.json) {
    print({
      ok: true,
      name: corpus.name,
      version: corpus.version,
      ruleCount: corpus.rules.length,
      factCount: corpus.facts.length,
      merkleRoot: corpus.merkleRoot
    });
    return EXIT.OK;
  }
  console.log(`${corpus.name}@${corpus.version}`);
  console.log(`${corpus.rules.length} rules, ${corpus.facts.length} facts`);
  console.log(corpus.merkleRoot);
  return EXIT.OK;
}
function runCorpusList(flags) {
  const corpus = getCorpus();
  const rules = flags.asOf ? corpus.rules.filter((r) => r.effectiveFrom <= flags.asOf && (r.effectiveTo === void 0 || flags.asOf < r.effectiveTo)) : corpus.rules;
  const sorted = [...rules].sort((a, b) => a.id.localeCompare(b.id) || a.version - b.version);
  if (flags.json) {
    print({
      ok: true,
      merkleRoot: corpus.merkleRoot,
      rules: sorted.map((r) => ({
        id: r.id,
        version: r.version,
        title: r.title,
        citation: r.citation,
        effectiveFrom: r.effectiveFrom,
        ...r.effectiveTo ? { effectiveTo: r.effectiveTo } : {},
        ...r.overrides ? { overrides: r.overrides } : {},
        hash: corpus.ruleHashes.get(`${r.id}@${r.version}`)
      }))
    });
    return EXIT.OK;
  }
  console.log();
  for (const r of sorted) {
    console.log(`${import_picocolors3.default.bold(r.id)} ${import_picocolors3.default.dim(`v${r.version}`)}  ${import_picocolors3.default.dim(`[${r.effectiveFrom}, ${r.effectiveTo ?? "open"})`)}`);
    console.log(`  ${r.title} ${import_picocolors3.default.dim(`\u2014 ${r.citation.source}`)}`);
  }
  console.log();
  console.log(import_picocolors3.default.dim(`${sorted.length} rules \xB7 ${corpus.merkleRoot}`));
  return EXIT.OK;
}

// dist/commands/eval.js
var import_picocolors6 = __toESM(require_picocolors(), 1);
import { readFileSync as readFileSync2, writeFileSync as writeFileSync2 } from "node:fs";

// dist/render/summary.js
var import_picocolors4 = __toESM(require_picocolors(), 1);
function findNode(root, ruleId) {
  if (root.ruleId === ruleId || root.resolvedFrom === ruleId)
    return root;
  for (const child of root.children ?? []) {
    const hit = findNode(child, ruleId);
    if (hit)
      return hit;
  }
  return null;
}
function centsOf(root, ruleId) {
  const node = findNode(root, ruleId);
  if (!node || node.value.type !== "money")
    return null;
  return BigInt(node.value.value);
}
var STATUS_LABEL = {
  single: "Single",
  mfj: "Married filing jointly",
  mfs: "Married filing separately",
  hoh: "Head of household",
  qss: "Qualifying surviving spouse"
};
function renderSummary(proof) {
  const withholdingMode = proof.target === "us.federal.balance_due";
  if (proof.target !== "us.federal.net_tax" && !withholdingMode)
    return null;
  const root = proof.root;
  const agi2 = centsOf(root, "us.federal.agi");
  const stdDed = centsOf(root, "us.federal.standard_deduction");
  const senior = centsOf(root, "us.federal.senior_deduction") ?? 0n;
  const tips = centsOf(root, "us.federal.tips_deduction") ?? 0n;
  const overtime = centsOf(root, "us.federal.overtime_deduction") ?? 0n;
  const carLoan = centsOf(root, "us.federal.car_loan_interest_deduction") ?? 0n;
  const charity = centsOf(root, "us.federal.charitable_deduction_nonitemizer") ?? 0n;
  const qbi2 = centsOf(root, "us.federal.qbi_deduction") ?? 0n;
  const taxable3 = centsOf(root, "us.federal.taxable_income");
  const before = centsOf(root, "us.federal.income_tax_before_credits");
  const amt = centsOf(root, "us.federal.amt") ?? 0n;
  const ctc = centsOf(root, "us.federal.ctc") ?? 0n;
  const education = centsOf(root, "us.federal.education.nonrefundable") ?? 0n;
  const refundable = centsOf(root, "us.federal.refundable_credits") ?? 0n;
  const otherTaxes = centsOf(root, "us.federal.other_taxes") ?? 0n;
  const net = withholdingMode ? centsOf(root, "us.federal.net_tax") ?? 0n : BigInt(root.value.value);
  if (agi2 === null || stdDed === null || taxable3 === null || before === null) {
    return null;
  }
  const elected = centsOf(root, "us.federal.deduction_election");
  const itemized = centsOf(root, "us.federal.itemized_deductions") ?? 0n;
  const standardSide = stdDed + charity;
  const itemizing = elected !== null && itemized > standardSide;
  const deductions = (elected ?? standardSide) + senior + tips + overtime + carLoan + qbi2;
  const dedParts = [
    itemizing ? "itemized deductions" : null,
    !itemizing && stdDed > 0n ? "standard deduction" : null,
    senior > 0n ? "senior deduction" : null,
    tips > 0n ? "tips deduction" : null,
    overtime > 0n ? "overtime deduction" : null,
    carLoan > 0n ? "car loan interest" : null,
    !itemizing && charity > 0n ? "charitable deduction" : null,
    qbi2 > 0n ? "QBI deduction" : null
  ].filter(Boolean);
  const creditParts = [
    education > 0n ? "education credits" : null,
    ctc > 0n ? "child tax credit" : null,
    refundable > 0n ? "refundable credits" : null
  ].filter(Boolean);
  const statusNode = findNode(root, "us.federal.income_tax_before_credits");
  const statusTag = statusNode?.inputs?.filingStatus?.value !== void 0 ? STATUS_LABEL[String(statusNode.inputs.filingStatus.value)] : null;
  const year = proof.asOf.slice(0, 4);
  const money40 = (c2) => formatMoney(c2).padStart(14);
  const lines = [];
  lines.push(import_picocolors4.default.bold(`${statusTag ?? "Taxpayer"} \xB7 tax year ${year}`));
  lines.push("");
  lines.push(`  Income               ${money40(agi2)}`);
  lines.push(`  \u2212 Deductions         ${money40(deductions)}${dedParts.length ? import_picocolors4.default.dim(`   (${dedParts.join(" + ")})`) : ""}`);
  lines.push(`  = Taxable income     ${money40(taxable3)}`);
  lines.push(`  Tax before credits   ${money40(before)}`);
  if (amt > 0n) {
    lines.push(`  + Alternative min tax${money40(amt)}${import_picocolors4.default.dim("   (Form 6251)")}`);
  }
  lines.push(`  \u2212 Credits            ${money40(education + ctc + refundable)}${creditParts.length ? import_picocolors4.default.dim(`   (${creditParts.join(" + ")})`) : ""}`);
  if (otherTaxes > 0n) {
    lines.push(`  + Other taxes        ${money40(otherTaxes)}${import_picocolors4.default.dim("   (SE tax / Add'l Medicare / NIIT)")}`);
  }
  const row = (label, amount, paint = (s) => s) => `  ${paint(label.padEnd(21))}${paint(money40(amount))}`;
  lines.push(import_picocolors4.default.dim("  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500"));
  if (net < 0n) {
    lines.push(row("Your refund", -net, (s) => import_picocolors4.default.bold(import_picocolors4.default.green(s))));
  } else {
    const rateLabel = agi2 > 0n ? import_picocolors4.default.dim(`   (${(Number(net * 1000n / agi2) / 10).toFixed(1)}% of income)`) : "";
    lines.push(row(withholdingMode ? "Tax for the year" : "You owe", net, import_picocolors4.default.bold) + rateLabel);
  }
  if (withholdingMode) {
    const balance = BigInt(root.value.value);
    const withheld = net - balance;
    const aprilOf = String(Number(year) + 1);
    lines.push(row("\u2212 Withheld so far", withheld));
    lines.push(import_picocolors4.default.dim("  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500"));
    if (balance < 0n) {
      lines.push(row("Refund expected", -balance, (s) => import_picocolors4.default.bold(import_picocolors4.default.green(s))) + import_picocolors4.default.dim(`   (April ${aprilOf})`));
    } else {
      lines.push(row("Balance due", balance, (s) => import_picocolors4.default.bold(import_picocolors4.default.yellow(s))) + import_picocolors4.default.dim(`   (April ${aprilOf})`));
    }
  }
  return lines.join("\n");
}

// dist/render/tree.js
var import_picocolors5 = __toESM(require_picocolors(), 1);
function renderProof(proof, opts) {
  const lines = [];
  const seen = /* @__PURE__ */ new Set();
  lines.push(`${import_picocolors5.default.bold(proof.target)}  ${import_picocolors5.default.bold(import_picocolors5.default.green(formatAnswer(proof.root.value)))}`);
  lines.push(import_picocolors5.default.dim(`as of ${proof.asOf}`));
  renderChildrenOf(proof.root, "", lines, seen, true);
  if (proof.assumptions.length > 0) {
    lines.push("");
    const boring = (v) => v.type === "money" && v.value === "0" || v.type === "int" && v.value === "0" || v.type === "bool" && v.value === false;
    const interesting = proof.assumptions.filter((a) => !boring(a.value));
    const folded = proof.assumptions.length - interesting.length;
    const showAll = opts?.allAssumptions || interesting.length === proof.assumptions.length;
    const shown = showAll ? proof.assumptions : interesting;
    if (!showAll && interesting.length === 0) {
      lines.push(import_picocolors5.default.yellow(`Assumptions (${proof.assumptions.length}): `) + import_picocolors5.default.dim("every default was $0/none \u2014 pass the real facts if your situation differs (--assumptions lists them)"));
    } else {
      lines.push(import_picocolors5.default.yellow(`Assumptions (${proof.assumptions.length}):`));
      for (const a of shown) {
        lines.push(`  ${import_picocolors5.default.yellow("\u2022")} ${a.factId} = ${formatValue(a.value)} ${import_picocolors5.default.dim(`\u2014 ${a.rationale}`)}`);
      }
      if (!showAll && folded > 0) {
        lines.push(import_picocolors5.default.dim(`  \u2026 and ${folded} more defaults, all $0/none (--assumptions lists them)`));
      }
    }
  }
  lines.push("");
  lines.push(import_picocolors5.default.dim(`corpus ${proof.corpus.name}@${proof.corpus.version}  ${proof.corpus.merkleRoot.slice(0, 23)}\u2026`));
  lines.push(import_picocolors5.default.dim(`artifact ${proof.artifactHash.slice(0, 23)}\u2026`));
  return lines.join("\n");
}
function renderChildrenOf(node, prefix, lines, seen, isRoot = false) {
  if (node.kind === "rule" && node.title) {
    const cite = node.citation ? `  [${node.citation.source}]` : "";
    lines.push(`${prefix}${isRoot ? "" : ""}${import_picocolors5.default.dim(`\xB7 ${node.title}${cite}`)}`);
  }
  const kids = node.children ?? [];
  kids.forEach((child, i) => {
    renderNode(child, prefix, i === kids.length - 1, lines, seen);
  });
}
function renderNode(node, prefix, isLast, lines, seen) {
  const connector = isLast ? "\u2514\u2500 " : "\u251C\u2500 ";
  const childPrefix = prefix + (isLast ? "   " : "\u2502  ");
  if (node.kind === "fact") {
    lines.push(`${prefix}${connector}${import_picocolors5.default.cyan("fact")} ${node.factId} = ${formatValue(node.value)}`);
    return;
  }
  if (node.kind === "assumption") {
    lines.push(`${prefix}${connector}${import_picocolors5.default.yellow("assumed")} ${node.factId} = ${formatValue(node.value)} ${import_picocolors5.default.dim("(default)")}`);
    return;
  }
  const already = node.ruleId && seen.has(node.ruleId);
  const overrideTag = node.resolvedFrom ? import_picocolors5.default.magenta(` \u2937 overrides ${node.resolvedFrom}`) : "";
  lines.push(`${prefix}${connector}${node.ruleId}  ${import_picocolors5.default.green(formatValue(node.value))}${overrideTag}${already ? import_picocolors5.default.dim("  (derived above)") : ""}`);
  if (already)
    return;
  if (node.ruleId)
    seen.add(node.ruleId);
  renderChildrenOf(node, childPrefix, lines, seen);
}

// dist/commands/eval.js
function runEval(flags) {
  const corpus = getCorpus();
  const asOf = flags.asOf ?? (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  let plain = {};
  if (flags.facts) {
    try {
      plain = JSON.parse(readFileSync2(flags.facts, "utf8"));
    } catch (err) {
      return emitError(new Error(`cannot read facts file ${flags.facts}: ${err.message}`), flags.json);
    }
  }
  try {
    Object.assign(plain, factsFromOptions(flags));
    const target = flags.target ?? (plain.federalTaxWithheld !== void 0 ? "us.federal.balance_due" : DEFAULT_TARGET);
    const facts2 = coerceFacts(corpus, plain);
    const { value, proof } = evaluate(corpus, facts2, { asOf, target });
    if (flags.proof) {
      writeFileSync2(flags.proof, JSON.stringify(proof, null, 2) + "\n");
    }
    if (flags.json) {
      print({
        ok: true,
        target,
        asOf,
        value: proof.root.value,
        formatted: formatValue(proof.root.value),
        assumptions: proof.assumptions,
        corpusMerkleRoot: proof.corpus.merkleRoot,
        artifactHash: proof.artifactHash,
        proof
      });
    } else {
      const summary = renderSummary(proof);
      console.log();
      if (summary) {
        console.log(summary);
        console.log();
      }
      if (flags.brief && summary) {
        console.log(import_picocolors6.default.dim(`every number above is a node of a verified derivation \u2014 run without --brief for the full proof tree with citations`));
      } else {
        console.log(renderProof(proof, { allAssumptions: flags.assumptions }));
      }
      if (flags.proof)
        console.log(import_picocolors6.default.dim(`proof written to ${flags.proof}`));
      console.log();
    }
    return EXIT.OK;
  } catch (err) {
    return emitError(err, flags.json);
  }
}

// dist/commands/explain.js
var import_picocolors7 = __toESM(require_picocolors(), 1);
function runExplain(ruleId, flags) {
  const corpus = getCorpus();
  const versions = corpus.byId.get(ruleId);
  if (!versions) {
    const near = [...corpus.byId.keys()].filter((id) => id.includes(ruleId));
    if (flags.json) {
      print({
        ok: false,
        error: {
          code: "UNKNOWN_RULE",
          message: `unknown rule "${ruleId}"`,
          data: { ruleId, suggestions: near.slice(0, 5) },
          hint: "Run `opentax corpus list --json` for every rule id."
        }
      });
      return EXIT.ERROR;
    }
    console.error(import_picocolors7.default.red(`unknown rule "${ruleId}"`));
    if (near.length) {
      console.error(import_picocolors7.default.dim(`did you mean: ${near.slice(0, 5).join(", ")}`));
    }
    return EXIT.ERROR;
  }
  const shown = flags.asOf ? versions.filter((r) => r.effectiveFrom <= flags.asOf && (r.effectiveTo === void 0 || flags.asOf < r.effectiveTo)) : versions;
  if (flags.json) {
    print({
      ok: true,
      versions: shown.map((rule) => ({
        ...rule,
        hash: corpus.ruleHashes.get(`${rule.id}@${rule.version}`),
        dependencies: dependencies(rule)
      }))
    });
    return EXIT.OK;
  }
  for (const rule of shown) {
    console.log();
    console.log(`${import_picocolors7.default.bold(rule.id)} ${import_picocolors7.default.dim(`v${rule.version}`)}`);
    console.log(`  ${rule.title}`);
    console.log(`  ${import_picocolors7.default.dim("citation")}   ${rule.citation.source} ${rule.citation.section}`);
    if (rule.citation.url)
      console.log(`  ${import_picocolors7.default.dim("url")}        ${rule.citation.url}`);
    if (rule.citation.excerpt) {
      console.log(`  ${import_picocolors7.default.dim("excerpt")}    ${import_picocolors7.default.italic(`"${rule.citation.excerpt}"`)}`);
    }
    console.log(`  ${import_picocolors7.default.dim("effective")}  [${rule.effectiveFrom}, ${rule.effectiveTo ?? "open"})`);
    console.log(`  ${import_picocolors7.default.dim("hash")}       ${corpus.ruleHashes.get(`${rule.id}@${rule.version}`)}`);
    if (rule.overrides) {
      console.log(`  ${import_picocolors7.default.dim("overrides")}  ${rule.overrides.ruleId} ${import_picocolors7.default.dim(`(priority ${rule.overrides.priority})`)}`);
    }
    if (rule.parameters) {
      console.log(`  ${import_picocolors7.default.dim("parameters")}`);
      for (const [name, p] of Object.entries(rule.parameters)) {
        const shownValue = p.type === "money" ? formatMoney(String(p.value)) : String(p.value);
        console.log(`    ${name} = ${shownValue}`);
      }
    }
    const { ruleRefs, factRefs } = dependencies(rule);
    if (ruleRefs.length) {
      console.log(`  ${import_picocolors7.default.dim("depends on")} ${ruleRefs.join(", ")}`);
    }
    if (factRefs.length) {
      console.log(`  ${import_picocolors7.default.dim("facts used")} ${factRefs.join(", ")}`);
    }
  }
  console.log();
  return EXIT.OK;
}
function dependencies(rule) {
  const ruleRefs = /* @__PURE__ */ new Set();
  const factRefs = /* @__PURE__ */ new Set();
  const roots = [rule.formula];
  if (rule.applicability)
    roots.push(rule.applicability);
  for (const root of roots) {
    walkExpr(root, (e) => {
      if (e.kind === "rule")
        ruleRefs.add(e.ruleId);
      if (e.kind === "fact")
        factRefs.add(e.factId);
    });
  }
  return { ruleRefs: [...ruleRefs].sort(), factRefs: [...factRefs].sort() };
}

// dist/commands/facts.js
var import_picocolors8 = __toESM(require_picocolors(), 1);
function runFacts(flags = {}) {
  const corpus = getCorpus();
  if (flags.json) {
    print({
      ok: true,
      moneyFormat: 'dollars: 50000, "$50,000", or "1234.56" (strings for cent precision)',
      facts: corpus.facts.map((f) => ({
        id: f.id,
        type: f.type,
        ...f.enumValues ? { enumValues: f.enumValues } : {},
        required: f.default === void 0,
        ...f.default !== void 0 ? { default: f.default.value, defaultRationale: f.default.rationale } : {},
        description: f.description
      }))
    });
    return EXIT.OK;
  }
  console.log();
  console.log(import_picocolors8.default.bold("Facts the corpus understands"));
  console.log(import_picocolors8.default.dim('Money is written in dollars: 50000, "$50,000", or "1234.56".\n'));
  for (const f of corpus.facts) {
    const kind = f.type === "enum" && f.enumValues ? `enum: ${f.enumValues.join("|")}` : f.type === "money" ? "dollars" : f.type;
    const dflt = f.default !== void 0 ? import_picocolors8.default.dim(` (default: ${String(f.default.value)})`) : import_picocolors8.default.yellow(" (required)");
    console.log(`  ${import_picocolors8.default.bold(f.id)} ${import_picocolors8.default.dim(`[${kind}]`)}${dflt}`);
    console.log(`    ${import_picocolors8.default.dim(f.description)}`);
  }
  console.log();
  console.log(import_picocolors8.default.dim("Example: opentax eval --status mfj --wages 120000 --kids 2"));
  console.log();
  return EXIT.OK;
}

// dist/commands/lookup.js
var import_picocolors9 = __toESM(require_picocolors(), 1);

// ../solve/dist/sweep.js
function evaluateAt(corpus, baseFacts, options, atCents) {
  const facts2 = {
    ...baseFacts,
    [options.vary]: { type: "money", value: atCents.toString() }
  };
  const { value } = evaluate(corpus, facts2, {
    asOf: options.asOf,
    target: options.target
  });
  if (value.type !== "money") {
    throw new TypeError(`solve requires a money-valued target, got ${value.type} from ${options.target}`);
  }
  return value.cents;
}
function sweep(corpus, baseFacts, options) {
  if (options.stepCents <= 0n)
    throw new RangeError("stepCents must be positive");
  if (options.toCents < options.fromCents) {
    throw new RangeError("toCents must be >= fromCents");
  }
  const points = [];
  for (let x = options.fromCents; x <= options.toCents; x += options.stepCents) {
    points.push({
      inputCents: x,
      valueCents: evaluateAt(corpus, baseFacts, options, x)
    });
  }
  return {
    points,
    corpusMerkleRoot: corpus.merkleRoot,
    target: options.target,
    asOf: options.asOf,
    vary: options.vary
  };
}

// ../solve/dist/marginal.js
function marginal(corpus, baseFacts, options) {
  const delta = options.deltaCents ?? 10000n;
  if (delta <= 0n)
    throw new RangeError("deltaCents must be positive");
  const before = evaluateAt(corpus, baseFacts, options, options.atCents);
  const after = evaluateAt(corpus, baseFacts, options, options.atCents + delta);
  const marginalCents = after - before;
  const num = marginalCents * 10000n * 2n + delta;
  const rateBps = num / (delta * 2n);
  return {
    atCents: options.atCents,
    deltaCents: delta,
    valueAtCents: before,
    valueAfterCents: after,
    marginalCents,
    rateBps,
    corpusMerkleRoot: corpus.merkleRoot
  };
}

// ../solve/dist/cliffs.js
function findCliffs(corpus, baseFacts, options) {
  const step = options.stepCents ?? 100000n;
  if (step <= 0n)
    throw new RangeError("stepCents must be positive");
  const at = (x) => evaluateAt(corpus, baseFacts, options, x);
  const cliffs = [];
  let prevX = options.fromCents;
  let prevV = at(prevX);
  for (let x = options.fromCents + step; x <= options.toCents; x += step) {
    const v = at(x);
    let dv = v - prevV;
    if ((dv < 0n ? -dv : dv) > x - prevX) {
      collectCliffs(at, prevX, prevV, x, v, cliffs);
    }
    prevX = x;
    prevV = v;
  }
  cliffs.sort((a, b) => a.atCents < b.atCents ? -1 : a.atCents > b.atCents ? 1 : 0);
  return {
    cliffs,
    corpusMerkleRoot: corpus.merkleRoot,
    scanned: {
      fromCents: options.fromCents,
      toCents: options.toCents,
      stepCents: step
    }
  };
}
function collectCliffs(at, lo, loV, hi, hiV, out) {
  if (hi - lo <= 1n) {
    out.push({ atCents: lo, jumpCents: hiV - loV });
    return;
  }
  const mid = (lo + hi) / 2n;
  const midV = at(mid);
  const leftJump = midV - loV;
  const rightJump = hiV - midV;
  if ((leftJump < 0n ? -leftJump : leftJump) > mid - lo) {
    collectCliffs(at, lo, loV, mid, midV, out);
  }
  if ((rightJump < 0n ? -rightJump : rightJump) > hi - mid) {
    collectCliffs(at, mid, midV, hi, hiV, out);
  }
}

// ../solve/dist/invert.js
function invert(corpus, baseFacts, options) {
  const at = (x) => evaluateAt(corpus, baseFacts, options, x);
  const { loCents, hiCents, goalCents } = options;
  if (hiCents <= loCents) {
    return { ok: false, reason: "goal-out-of-range", message: "empty range" };
  }
  const n = BigInt(Math.max(2, options.samples ?? 8));
  let prev = at(loCents);
  for (let i = 1n; i <= n; i++) {
    const x = loCents + (hiCents - loCents) * i / n;
    const v = at(x);
    if (v < prev) {
      return {
        ok: false,
        reason: "not-monotone",
        message: `target decreases between samples near input ${x} \u2014 bisection would be unsound here (try a narrower range)`
      };
    }
    prev = v;
  }
  const loV = at(loCents);
  const hiV = at(hiCents);
  if (loV >= goalCents) {
    return { ok: true, inputCents: loCents, valueCents: loV, corpusMerkleRoot: corpus.merkleRoot };
  }
  if (hiV < goalCents) {
    return {
      ok: false,
      reason: "goal-out-of-range",
      message: `target only reaches ${hiV} at the top of the range (goal ${goalCents})`
    };
  }
  let lo = loCents;
  let hi = hiCents;
  while (hi - lo > 1n) {
    const mid = (lo + hi) / 2n;
    if (at(mid) >= goalCents)
      hi = mid;
    else
      lo = mid;
  }
  return {
    ok: true,
    inputCents: hi,
    valueCents: at(hi),
    corpusMerkleRoot: corpus.merkleRoot
  };
}

// ../solve/dist/compare.js
function compareAcross(corpus, baseFacts, options) {
  const spec = corpus.factById.get(options.vary);
  if (!spec || spec.type !== "enum") {
    throw new TypeError(`compare requires an enum fact, got "${options.vary}"`);
  }
  const values = options.values ?? spec.enumValues ?? [];
  const scenarios = values.map((value) => {
    const facts2 = {
      ...baseFacts,
      [options.vary]: { type: "enum", value }
    };
    try {
      const { value: result } = evaluate(corpus, facts2, {
        asOf: options.asOf,
        target: options.target
      });
      if (result.type !== "money") {
        throw new TypeError(`compare requires a money target, got ${result.type}`);
      }
      return { value, ok: true, valueCents: result.cents };
    } catch (err) {
      if (err instanceof OpenTaxError) {
        return { value, ok: false, errorCode: err.code, errorMessage: err.message };
      }
      throw err;
    }
  });
  return { corpusMerkleRoot: corpus.merkleRoot, scenarios };
}

// ../solve/dist/lookup.js
var tokenize = (s) => s.toLowerCase().replace(/[^a-z0-9]+/g, " ").split(" ").filter((t) => t.length > 2);
function extractByStatus(rule) {
  const out = {};
  walkExpr(rule.formula, (e) => {
    if (e.kind === "match" && e.on.kind === "fact" && e.on.factId === "filingStatus") {
      for (const c2 of e.cases) {
        if (c2.value.kind === "money")
          out[c2.when] = c2.value.cents;
      }
    }
  });
  return out;
}
var JURISDICTION_NAMES = {
  "us.il": "illinois il il-1040",
  "us.ca": "california ca ftb form 540",
  "us.ny": "new york ny nyc it-201",
  "us.va": "virginia va form 760"
};
function lookupParameters(corpus, query, asOf) {
  const tokens = tokenize(query);
  if (tokens.length === 0)
    return [];
  const hits = [];
  for (const rule of corpus.rules) {
    if (asOf && !(rule.effectiveFrom <= asOf && (rule.effectiveTo === void 0 || asOf < rule.effectiveTo))) {
      continue;
    }
    const paramWords = Object.keys(rule.parameters ?? {}).map((n) => n.replace(/([A-Z])/g, " $1").toLowerCase()).join(" ");
    const haystackStrong = `${rule.id} ${rule.title} ${paramWords} ${JURISDICTION_NAMES[rule.jurisdiction] ?? ""}`.toLowerCase();
    const haystackWeak = `${rule.citation.source} ${rule.citation.section} ${rule.citation.excerpt}`.toLowerCase();
    let score = 0;
    for (const t of tokens) {
      if (haystackStrong.includes(t))
        score += 2;
      else if (haystackWeak.includes(t))
        score += 1;
    }
    if (score === 0)
      continue;
    const parameters = {};
    for (const [name, p] of Object.entries(rule.parameters ?? {})) {
      if (p.type === "money")
        parameters[name] = String(p.value);
    }
    const byFilingStatus = extractByStatus(rule);
    if (Object.keys(parameters).length === 0 && Object.keys(byFilingStatus).length === 0) {
      score -= 1;
      if (score <= 0)
        continue;
    }
    hits.push({
      ruleId: rule.id,
      version: rule.version,
      title: rule.title,
      citation: rule.citation,
      effectiveFrom: rule.effectiveFrom,
      effectiveTo: rule.effectiveTo,
      parameters,
      byFilingStatus,
      score
    });
  }
  hits.sort((a, b) => b.score - a.score || a.ruleId.localeCompare(b.ruleId));
  return hits.slice(0, 5);
}
var SNIPPET_LEN = 280;
function makeSnippet(excerpt, tokens) {
  if (excerpt.length <= SNIPPET_LEN)
    return excerpt;
  const lower = excerpt.toLowerCase();
  let at = -1;
  for (const t of tokens) {
    const i = lower.indexOf(t);
    if (i !== -1 && (at === -1 || i < at))
      at = i;
  }
  const start = Math.max(0, Math.min(at === -1 ? 0 : at - 60, excerpt.length - SNIPPET_LEN));
  const cut = excerpt.slice(start, start + SNIPPET_LEN);
  return `${start > 0 ? "\u2026" : ""}${cut}${start + SNIPPET_LEN < excerpt.length ? "\u2026" : ""}`;
}
function searchRules(corpus, query, asOf, limit = 8) {
  const allTokens = tokenize(query);
  if (allTokens.length === 0)
    return [];
  const haystacks = corpus.rules.map((rule) => {
    const paramWords = Object.keys(rule.parameters ?? {}).map((n) => n.replace(/([A-Z])/g, " $1").toLowerCase()).join(" ");
    return {
      rule,
      // "_" is a regex word character — split snake_case so \b-anchored
      // tokens ("tax") still reach id segments ("kiddie_tax").
      id: rule.id.toLowerCase().replace(/[._]/g, " "),
      title: rule.title.toLowerCase(),
      meta: `${paramWords} ${JURISDICTION_NAMES[rule.jurisdiction] ?? ""}`.toLowerCase(),
      weak: `${rule.citation.source} ${rule.citation.section} ${rule.citation.excerpt ?? ""}`.toLowerCase()
    };
  });
  const matchers = new Map(allTokens.map((t) => [t, new RegExp(`\\b${t}`)]));
  const has = (hay, t) => matchers.get(t).test(hay);
  const df = (t) => haystacks.filter((h) => has(h.id, t) || has(h.title, t) || has(h.meta, t) || has(h.weak, t)).length;
  const rare = allTokens.filter((t) => df(t) <= haystacks.length * 0.35);
  const tokens = rare.length > 0 ? rare : allTokens;
  const boost = new Map(tokens.map((t) => [t, df(t) <= haystacks.length * 0.05 ? 2 : 1]));
  const minScore = tokens.length >= 2 ? 2 : 1;
  const hits = [];
  for (const { rule, id, title, meta, weak } of haystacks) {
    if (asOf && !(rule.effectiveFrom <= asOf && (rule.effectiveTo === void 0 || asOf < rule.effectiveTo))) {
      continue;
    }
    let raw = 0;
    let score = 0;
    let matched = 0;
    let sawStrong = false;
    for (const t of tokens) {
      let s = 0;
      if (has(id, t))
        s += 2;
      if (has(title, t))
        s += 2;
      if (has(meta, t))
        s += 1;
      if (s > 0)
        sawStrong = true;
      else if (has(weak, t))
        s = 1;
      if (s > 0)
        matched += 1;
      raw += s;
      score += s * (boost.get(t) ?? 1);
    }
    if (raw < minScore || matched < Math.ceil(tokens.length / 2))
      continue;
    if (tokens.length >= 2 && !sawStrong)
      continue;
    hits.push({
      ruleId: rule.id,
      version: rule.version,
      title: rule.title,
      jurisdiction: rule.jurisdiction,
      citation: rule.citation,
      effectiveFrom: rule.effectiveFrom,
      effectiveTo: rule.effectiveTo,
      snippet: makeSnippet(rule.citation.excerpt ?? "", tokens),
      hasParameters: Object.keys(rule.parameters ?? {}).length > 0,
      score
    });
  }
  hits.sort((a, b) => b.score - a.score || a.ruleId.localeCompare(b.ruleId));
  return hits.slice(0, Math.max(1, Math.min(limit, 20)));
}
function factCheck(corpus, query, claimedCents, asOf, filingStatus) {
  const hits = lookupParameters(corpus, query, asOf);
  if (hits.length === 0) {
    return {
      verdict: "unknown",
      message: `no corpus rule matches "${query}" \u2014 the claim cannot be checked against this corpus`,
      alternatives: []
    };
  }
  const claimed = claimedCents.toString();
  const candidatesOf = (hit) => {
    const out = [];
    if (filingStatus && hit.byFilingStatus[filingStatus] !== void 0) {
      out.push([`byFilingStatus.${filingStatus}`, hit.byFilingStatus[filingStatus]]);
    } else {
      out.push(...Object.entries(hit.byFilingStatus).map(([k, v]) => [`byFilingStatus.${k}`, v]));
    }
    out.push(...Object.entries(hit.parameters));
    return out;
  };
  for (const hit of hits) {
    const exact = candidatesOf(hit).find(([, v]) => v === claimed);
    if (exact) {
      return {
        verdict: "verified",
        matchedRuleId: hit.ruleId,
        matchedField: exact[0],
        actualCents: exact[1],
        claimedCents: claimed,
        citation: hit.citation,
        alternatives: hits.filter((h) => h !== hit)
      };
    }
  }
  const best = hits[0];
  const bestCandidates = candidatesOf(best);
  if (bestCandidates.length === 0) {
    return {
      verdict: "unknown",
      message: `best match "${best.ruleId}" carries no dollar parameters to compare`,
      alternatives: hits
    };
  }
  const [field, actual] = bestCandidates[0];
  return {
    verdict: "refuted",
    matchedRuleId: best.ruleId,
    matchedField: field,
    actualCents: actual,
    claimedCents: claimed,
    citation: best.citation,
    alternatives: hits.slice(1)
  };
}

// dist/commands/lookup.js
function runLookup(queryWords, flags) {
  const corpus = getCorpus();
  const query = queryWords.join(" ");
  const asOf = flags.asOf ?? (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  if (flags.expect !== void 0) {
    const claimed = parseDollars(flags.expect);
    const result = factCheck(corpus, query, claimed, asOf, flags.filingStatus);
    if (result.verdict === "unknown") {
      if (flags.json) {
        print({ ok: true, query, asOf, claimed: claimed.toString(), verdict: "unknown", message: result.message });
        return EXIT.NOT_COVERED;
      }
      console.log();
      console.log(`${import_picocolors9.default.yellow("unknown")} \u2014 ${result.message}`);
      console.log();
      return EXIT.NOT_COVERED;
    }
    if (flags.json) {
      print({
        ok: true,
        query,
        asOf,
        claimed: claimed.toString(),
        verdict: result.verdict,
        rule: result.matchedRuleId,
        field: result.matchedField,
        actual: result.actualCents,
        citation: result.citation,
        corpusMerkleRoot: corpus.merkleRoot
      });
      return result.verdict === "verified" ? EXIT.OK : EXIT.ERROR;
    }
    console.log();
    if (result.verdict === "verified") {
      console.log(`${import_picocolors9.default.green("\u2713 VERIFIED")} \u2014 ${formatMoney(result.actualCents)} is ${import_picocolors9.default.bold(result.matchedField)} of ${result.matchedRuleId}`);
      console.log(import_picocolors9.default.dim(`  ${result.citation.source} ${result.citation.section ?? ""}`));
      console.log();
      return EXIT.OK;
    }
    console.log(`${import_picocolors9.default.red("\u2717 REFUTED")} \u2014 claimed ${formatMoney(claimed.toString())}, but ${result.matchedRuleId}.${result.matchedField} is ${import_picocolors9.default.bold(formatMoney(result.actualCents))}`);
    console.log(import_picocolors9.default.dim(`  ${result.citation.source} ${result.citation.section ?? ""}`));
    console.log();
    return EXIT.ERROR;
  }
  const hits = lookupParameters(corpus, query, asOf);
  if (flags.json) {
    print({
      ok: true,
      query,
      asOf,
      corpusMerkleRoot: corpus.merkleRoot,
      hits: hits.map((h) => ({
        ruleId: h.ruleId,
        title: h.title,
        citation: h.citation,
        effective: `[${h.effectiveFrom}, ${h.effectiveTo ?? "open"})`,
        parameters: h.parameters,
        byFilingStatus: h.byFilingStatus
      }))
    });
    return EXIT.OK;
  }
  console.log();
  if (hits.length === 0) {
    console.log(`${import_picocolors9.default.yellow("no matches")} for "${query}" \u2014 try \`opentax corpus list\` for rule titles`);
    console.log();
    return EXIT.ERROR;
  }
  console.log(import_picocolors9.default.bold(`"${query}" \u2014 as of ${asOf}:`));
  for (const h of hits) {
    console.log();
    console.log(`  ${import_picocolors9.default.bold(h.title)}`);
    console.log(import_picocolors9.default.dim(`  ${h.citation.source} \xB7 [${h.effectiveFrom}, ${h.effectiveTo ?? "open"}) \xB7 ${h.ruleId}`));
    for (const [status, cents2] of Object.entries(h.byFilingStatus)) {
      console.log(`    ${status.padEnd(8)} ${formatMoney(cents2)}`);
    }
    for (const [name, cents2] of Object.entries(h.parameters)) {
      console.log(`    ${name.padEnd(24)} ${formatMoney(cents2)}`);
    }
  }
  console.log();
  return EXIT.OK;
}

// dist/commands/occupation.js
var import_picocolors10 = __toESM(require_picocolors(), 1);
function runOccupation(words, flags) {
  const input = words.join(" ");
  const result = matchOccupation(input);
  if ("slug" in result) {
    const listed = result.slug !== "other";
    const entry = TIPPED_OCCUPATIONS.find((o) => o.slug === result.slug);
    if (flags.json) {
      print({
        ok: true,
        input,
        tipped: listed,
        occupation: listed ? result.slug : null,
        label: entry?.name ?? null
      });
      return EXIT.OK;
    }
    console.log();
    if (listed) {
      console.log(`${import_picocolors10.default.green("tipped occupation")} \u2014 "${input}" matches ${import_picocolors10.default.bold(entry?.name ?? result.slug)} (\xA7 224 deduction eligible; pass --occupation ${result.slug})`);
    } else {
      console.log(`${import_picocolors10.default.yellow("not a listed occupation")} \u2014 "${input}" is not on the Treasury \xA7 224 tipped-occupation list; tips are still income but the deduction does not apply`);
    }
    console.log();
    return EXIT.OK;
  }
  if (flags.json) {
    print({ ok: false, input, ambiguous: true, candidates: result.candidates });
    return EXIT.NEEDS_FACTS;
  }
  console.log();
  console.log(`${import_picocolors10.default.yellow("ambiguous")} \u2014 "${input}" could be any of:`);
  for (const c2 of result.candidates)
    console.log(`  ${c2}`);
  console.log();
  return EXIT.NEEDS_FACTS;
}

// dist/commands/search.js
var import_picocolors11 = __toESM(require_picocolors(), 1);
function runSearch(queryWords, flags) {
  const corpus = getCorpus();
  const query = queryWords.join(" ");
  const asOf = flags.asOf ?? (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  const limit = flags.limit ? Number(flags.limit) : 8;
  const hits = searchRules(corpus, query, asOf, limit);
  if (flags.json) {
    print({
      ok: true,
      query,
      asOf,
      covered: hits.length > 0,
      corpusMerkleRoot: corpus.merkleRoot,
      hits: hits.map((h) => ({
        ruleId: h.ruleId,
        title: h.title,
        jurisdiction: h.jurisdiction,
        citation: `${h.citation.source} ${h.citation.section ?? ""}`.trim(),
        effective: `[${h.effectiveFrom}, ${h.effectiveTo ?? "open"})`,
        excerpt: h.snippet,
        hasDollarParameters: h.hasParameters
      }))
    });
    return EXIT.OK;
  }
  console.log();
  if (hits.length === 0) {
    console.log(`${import_picocolors11.default.yellow("not covered")} \u2014 no corpus rule matches "${query}" (the engine would refuse rather than guess here)`);
    console.log();
    return EXIT.NOT_COVERED;
  }
  console.log(import_picocolors11.default.bold(`"${query}" \u2014 ${hits.length} rule${hits.length === 1 ? "" : "s"}, as of ${asOf}:`));
  for (const h of hits) {
    console.log();
    console.log(`  ${import_picocolors11.default.bold(h.title)}`);
    console.log(import_picocolors11.default.dim(`  ${h.citation.source} \xB7 [${h.effectiveFrom}, ${h.effectiveTo ?? "open"}) \xB7 ${h.ruleId}`));
    console.log(`  ${import_picocolors11.default.dim("\u201C")}${h.snippet}${import_picocolors11.default.dim("\u201D")}`);
  }
  console.log(import_picocolors11.default.dim(`
next: \`opentax explain <ruleId>\` for a formula \xB7 \`opentax lookup ${query}\` for dollar amounts
`));
  return EXIT.OK;
}

// dist/commands/solve.js
var import_picocolors12 = __toESM(require_picocolors(), 1);
import { readFileSync as readFileSync3 } from "node:fs";
function setup(flags) {
  const corpus = getCorpus();
  let plain = {};
  if (flags.facts)
    plain = JSON.parse(readFileSync3(flags.facts, "utf8"));
  Object.assign(plain, factsFromOptions(flags));
  const base = coerceFacts(corpus, plain);
  return {
    corpus,
    base,
    vary: flags.vary ?? "wages",
    asOf: flags.asOf ?? (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
    target: flags.target ?? DEFAULT_TARGET
  };
}
var cents = (dollars2) => parseDollars(dollars2);
function runSweep(flags) {
  try {
    const { corpus, base, vary, asOf, target } = setup(flags);
    const result = sweep(corpus, base, {
      vary,
      fromCents: cents(flags.from),
      toCents: cents(flags.to),
      stepCents: cents(flags.step ?? "1000"),
      asOf,
      target
    });
    if (flags.json) {
      print({
        ok: true,
        vary,
        target,
        asOf,
        corpusMerkleRoot: result.corpusMerkleRoot,
        points: result.points.map((p) => ({
          input: p.inputCents.toString(),
          value: p.valueCents.toString()
        }))
      });
      return EXIT.OK;
    }
    if (flags.csv) {
      console.log(`${vary}_cents,${target.replaceAll(".", "_")}_cents`);
      for (const p of result.points) {
        console.log(`${p.inputCents},${p.valueCents}`);
      }
      return EXIT.OK;
    }
    console.log();
    console.log(import_picocolors12.default.bold(`${target} as ${vary} varies (as of ${asOf})`));
    for (const p of result.points) {
      console.log(`  ${formatMoney(p.inputCents).padStart(14)}  \u2192  ${formatMoney(p.valueCents)}`);
    }
    console.log(import_picocolors12.default.dim(`
${result.points.length} exact evaluations \xB7 corpus ${result.corpusMerkleRoot.slice(0, 23)}\u2026
`));
    return EXIT.OK;
  } catch (err) {
    return emitError(err, flags.json);
  }
}
function runMarginal(flags) {
  try {
    const { corpus, base, vary, asOf, target } = setup(flags);
    const m = marginal(corpus, base, {
      vary,
      atCents: cents(flags.at),
      deltaCents: flags.delta ? cents(flags.delta) : void 0,
      asOf,
      target
    });
    if (flags.json) {
      print({
        ok: true,
        vary,
        target,
        asOf,
        at: m.atCents.toString(),
        delta: m.deltaCents.toString(),
        valueAt: m.valueAtCents.toString(),
        valueAfter: m.valueAfterCents.toString(),
        marginal: m.marginalCents.toString(),
        rateBps: m.rateBps.toString(),
        corpusMerkleRoot: m.corpusMerkleRoot
      });
      return EXIT.OK;
    }
    const pct3 = (Number(m.rateBps) / 100).toFixed(2);
    console.log();
    console.log(`${import_picocolors12.default.bold("marginal rate")} at ${vary} = ${formatMoney(m.atCents)}: ${import_picocolors12.default.bold(import_picocolors12.default.green(`${pct3}%`))}`);
    console.log(import_picocolors12.default.dim(`  next ${formatMoney(m.deltaCents)} of ${vary} \u2192 ${formatMoney(m.marginalCents)} more ${m.marginalCents < 0n ? "refund" : "tax"} (${formatMoney(m.valueAtCents)} \u2192 ${formatMoney(m.valueAfterCents)})`));
    console.log();
    return EXIT.OK;
  } catch (err) {
    return emitError(err, flags.json);
  }
}
function runCliffs(flags) {
  try {
    const { corpus, base, vary, asOf, target } = setup(flags);
    const result = findCliffs(corpus, base, {
      vary,
      fromCents: cents(flags.from),
      toCents: cents(flags.to),
      stepCents: flags.step ? cents(flags.step) : void 0,
      asOf,
      target
    });
    if (flags.json) {
      print({
        ok: true,
        vary,
        target,
        asOf,
        corpusMerkleRoot: result.corpusMerkleRoot,
        cliffs: result.cliffs.map((c2) => ({
          at: c2.atCents.toString(),
          jump: c2.jumpCents.toString()
        }))
      });
      return EXIT.OK;
    }
    console.log();
    if (result.cliffs.length === 0) {
      console.log(`${import_picocolors12.default.green("no cliffs")} in ${vary} \u2208 [${formatMoney(result.scanned.fromCents)}, ${formatMoney(result.scanned.toCents)}] \u2014 every marginal rate \u2264 100%`);
    } else {
      console.log(import_picocolors12.default.bold(`${result.cliffs.length} cliff(s) found:`));
      for (const c2 of result.cliffs) {
        console.log(`  ${import_picocolors12.default.red("\u25AE")} at ${import_picocolors12.default.bold(formatMoney(c2.atCents))}: one more cent of ${vary} costs ${import_picocolors12.default.red(formatMoney(c2.jumpCents))}`);
      }
    }
    console.log(import_picocolors12.default.dim(`
every probe is a real corpus evaluation \xB7 ${result.corpusMerkleRoot.slice(0, 23)}\u2026
`));
    return EXIT.OK;
  } catch (err) {
    return emitError(err, flags.json);
  }
}
function runInvert(flags) {
  try {
    const { corpus, base, vary, asOf, target } = setup(flags);
    const result = invert(corpus, base, {
      vary,
      goalCents: cents(flags.goal),
      loCents: cents(flags.lo),
      hiCents: cents(flags.hi),
      asOf,
      target
    });
    if (flags.json) {
      print(result.ok ? {
        ok: true,
        vary,
        target,
        asOf,
        input: result.inputCents.toString(),
        value: result.valueCents.toString(),
        corpusMerkleRoot: result.corpusMerkleRoot
      } : { ok: false, reason: result.reason, message: result.message });
      return result.ok ? EXIT.OK : EXIT.NOT_COVERED;
    }
    console.log();
    if (!result.ok) {
      console.log(`${import_picocolors12.default.yellow(result.reason)} \u2014 ${result.message}`);
      console.log();
      return EXIT.NOT_COVERED;
    }
    console.log(`${import_picocolors12.default.bold(formatMoney(result.inputCents))} is the smallest ${import_picocolors12.default.bold(flags.vary)} where ${target} first reaches ${formatMoney(cents(flags.goal))}`);
    console.log(import_picocolors12.default.dim(`  exact value there: ${formatMoney(result.valueCents)} \xB7 corpus ${result.corpusMerkleRoot.slice(0, 23)}\u2026`));
    console.log();
    return EXIT.OK;
  } catch (err) {
    return emitError(err, flags.json);
  }
}
function runCompare(flags) {
  try {
    const { corpus, base, asOf, target } = setup(flags);
    const vary = flags.vary ?? "filingStatus";
    let mfsAssumed = false;
    if (vary === "filingStatus" && base.spouseItemizes === void 0) {
      base.spouseItemizes = { type: "bool", value: false };
      mfsAssumed = true;
    }
    const result = compareAcross(corpus, base, { vary, asOf, target });
    if (flags.json) {
      print({
        ok: true,
        vary,
        target,
        asOf,
        corpusMerkleRoot: result.corpusMerkleRoot,
        scenarios: result.scenarios.map((s) => ({
          value: s.value,
          ok: s.ok,
          ...s.ok ? { result: s.valueCents.toString() } : { errorCode: s.errorCode, errorMessage: s.errorMessage }
        }))
      });
      return EXIT.OK;
    }
    console.log();
    console.log(import_picocolors12.default.bold(`${target} by ${vary} (as of ${asOf})`));
    const best = result.scenarios.filter((s) => s.ok).reduce((min, s) => min === null || s.valueCents < min ? s.valueCents : min, null);
    for (const s of result.scenarios) {
      if (s.ok) {
        const mark = s.valueCents === best ? import_picocolors12.default.green(" \u2190 lowest") : "";
        console.log(`  ${s.value.padEnd(8)} ${formatMoney(s.valueCents)}${mark}`);
      } else {
        console.log(`  ${s.value.padEnd(8)} ${import_picocolors12.default.dim(`${s.errorCode}: ${s.errorMessage?.slice(0, 60)}`)}`);
      }
    }
    console.log();
    if (mfsAssumed) {
      console.log(import_picocolors12.default.dim("  (mfs column assumes the spouse does not itemize \u2014 pass --spouse-itemizes if they do)"));
    }
    return EXIT.OK;
  } catch (err) {
    return emitError(err, flags.json);
  }
}

// dist/commands/signup.js
var import_picocolors13 = __toESM(require_picocolors(), 1);
async function runSignup(email, flags) {
  try {
    const res = await fetch("https://opentax.invaro.ai/api/waitlist", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email, source: "cli" })
    });
    const body = await res.json();
    if (flags.json) {
      print({ ok: Boolean(body.ok), ...body.error ? { error: body.error } : {} });
      return body.ok ? EXIT.OK : EXIT.ERROR;
    }
    if (body.ok) {
      console.log(import_picocolors13.default.bold("Check your inbox \u2014 one click on the confirmation link and you're in."));
      return EXIT.OK;
    }
    console.error(import_picocolors13.default.red(`signup failed: ${body.error ?? "unknown error"}`));
    return EXIT.ERROR;
  } catch {
    if (flags.json)
      print({ ok: false, error: "network" });
    else
      console.error(import_picocolors13.default.red("could not reach opentax.invaro.ai \u2014 check your connection"));
    return EXIT.ERROR;
  }
}

// dist/commands/state.js
var import_picocolors14 = __toESM(require_picocolors(), 1);
import { readFileSync as readFileSync4 } from "node:fs";

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/external.js
var external_exports = {};
__export(external_exports, {
  BRAND: () => BRAND,
  DIRTY: () => DIRTY,
  EMPTY_PATH: () => EMPTY_PATH,
  INVALID: () => INVALID,
  NEVER: () => NEVER,
  OK: () => OK,
  ParseStatus: () => ParseStatus,
  Schema: () => ZodType,
  ZodAny: () => ZodAny,
  ZodArray: () => ZodArray,
  ZodBigInt: () => ZodBigInt,
  ZodBoolean: () => ZodBoolean,
  ZodBranded: () => ZodBranded,
  ZodCatch: () => ZodCatch,
  ZodDate: () => ZodDate,
  ZodDefault: () => ZodDefault,
  ZodDiscriminatedUnion: () => ZodDiscriminatedUnion,
  ZodEffects: () => ZodEffects,
  ZodEnum: () => ZodEnum,
  ZodError: () => ZodError,
  ZodFirstPartyTypeKind: () => ZodFirstPartyTypeKind,
  ZodFunction: () => ZodFunction,
  ZodIntersection: () => ZodIntersection,
  ZodIssueCode: () => ZodIssueCode,
  ZodLazy: () => ZodLazy,
  ZodLiteral: () => ZodLiteral,
  ZodMap: () => ZodMap,
  ZodNaN: () => ZodNaN,
  ZodNativeEnum: () => ZodNativeEnum,
  ZodNever: () => ZodNever,
  ZodNull: () => ZodNull,
  ZodNullable: () => ZodNullable,
  ZodNumber: () => ZodNumber,
  ZodObject: () => ZodObject,
  ZodOptional: () => ZodOptional,
  ZodParsedType: () => ZodParsedType,
  ZodPipeline: () => ZodPipeline,
  ZodPromise: () => ZodPromise,
  ZodReadonly: () => ZodReadonly,
  ZodRecord: () => ZodRecord,
  ZodSchema: () => ZodType,
  ZodSet: () => ZodSet,
  ZodString: () => ZodString,
  ZodSymbol: () => ZodSymbol,
  ZodTransformer: () => ZodEffects,
  ZodTuple: () => ZodTuple,
  ZodType: () => ZodType,
  ZodUndefined: () => ZodUndefined,
  ZodUnion: () => ZodUnion,
  ZodUnknown: () => ZodUnknown,
  ZodVoid: () => ZodVoid,
  addIssueToContext: () => addIssueToContext,
  any: () => anyType,
  array: () => arrayType,
  bigint: () => bigIntType,
  boolean: () => booleanType,
  coerce: () => coerce,
  custom: () => custom,
  date: () => dateType,
  datetimeRegex: () => datetimeRegex,
  defaultErrorMap: () => en_default,
  discriminatedUnion: () => discriminatedUnionType,
  effect: () => effectsType,
  enum: () => enumType,
  function: () => functionType,
  getErrorMap: () => getErrorMap,
  getParsedType: () => getParsedType,
  instanceof: () => instanceOfType,
  intersection: () => intersectionType,
  isAborted: () => isAborted,
  isAsync: () => isAsync,
  isDirty: () => isDirty,
  isValid: () => isValid,
  late: () => late,
  lazy: () => lazyType,
  literal: () => literalType,
  makeIssue: () => makeIssue,
  map: () => mapType,
  nan: () => nanType,
  nativeEnum: () => nativeEnumType,
  never: () => neverType,
  null: () => nullType,
  nullable: () => nullableType,
  number: () => numberType,
  object: () => objectType,
  objectUtil: () => objectUtil,
  oboolean: () => oboolean,
  onumber: () => onumber,
  optional: () => optionalType,
  ostring: () => ostring,
  pipeline: () => pipelineType,
  preprocess: () => preprocessType,
  promise: () => promiseType,
  quotelessJson: () => quotelessJson,
  record: () => recordType,
  set: () => setType,
  setErrorMap: () => setErrorMap,
  strictObject: () => strictObjectType,
  string: () => stringType,
  symbol: () => symbolType,
  transformer: () => effectsType,
  tuple: () => tupleType,
  undefined: () => undefinedType,
  union: () => unionType,
  unknown: () => unknownType,
  util: () => util,
  void: () => voidType
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/util.js
var util;
(function(util2) {
  util2.assertEqual = (_) => {
  };
  function assertIs(_arg) {
  }
  util2.assertIs = assertIs;
  function assertNever(_x) {
    throw new Error();
  }
  util2.assertNever = assertNever;
  util2.arrayToEnum = (items) => {
    const obj = {};
    for (const item of items) {
      obj[item] = item;
    }
    return obj;
  };
  util2.getValidEnumValues = (obj) => {
    const validKeys = util2.objectKeys(obj).filter((k) => typeof obj[obj[k]] !== "number");
    const filtered = {};
    for (const k of validKeys) {
      filtered[k] = obj[k];
    }
    return util2.objectValues(filtered);
  };
  util2.objectValues = (obj) => {
    return util2.objectKeys(obj).map(function(e) {
      return obj[e];
    });
  };
  util2.objectKeys = typeof Object.keys === "function" ? (obj) => Object.keys(obj) : (object) => {
    const keys = [];
    for (const key in object) {
      if (Object.prototype.hasOwnProperty.call(object, key)) {
        keys.push(key);
      }
    }
    return keys;
  };
  util2.find = (arr, checker) => {
    for (const item of arr) {
      if (checker(item))
        return item;
    }
    return void 0;
  };
  util2.isInteger = typeof Number.isInteger === "function" ? (val) => Number.isInteger(val) : (val) => typeof val === "number" && Number.isFinite(val) && Math.floor(val) === val;
  function joinValues(array, separator = " | ") {
    return array.map((val) => typeof val === "string" ? `'${val}'` : val).join(separator);
  }
  util2.joinValues = joinValues;
  util2.jsonStringifyReplacer = (_, value) => {
    if (typeof value === "bigint") {
      return value.toString();
    }
    return value;
  };
})(util || (util = {}));
var objectUtil;
(function(objectUtil2) {
  objectUtil2.mergeShapes = (first, second) => {
    return {
      ...first,
      ...second
      // second overwrites first
    };
  };
})(objectUtil || (objectUtil = {}));
var ZodParsedType = util.arrayToEnum([
  "string",
  "nan",
  "number",
  "integer",
  "float",
  "boolean",
  "date",
  "bigint",
  "symbol",
  "function",
  "undefined",
  "null",
  "array",
  "object",
  "unknown",
  "promise",
  "void",
  "never",
  "map",
  "set"
]);
var getParsedType = (data) => {
  const t = typeof data;
  switch (t) {
    case "undefined":
      return ZodParsedType.undefined;
    case "string":
      return ZodParsedType.string;
    case "number":
      return Number.isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
    case "boolean":
      return ZodParsedType.boolean;
    case "function":
      return ZodParsedType.function;
    case "bigint":
      return ZodParsedType.bigint;
    case "symbol":
      return ZodParsedType.symbol;
    case "object":
      if (Array.isArray(data)) {
        return ZodParsedType.array;
      }
      if (data === null) {
        return ZodParsedType.null;
      }
      if (data.then && typeof data.then === "function" && data.catch && typeof data.catch === "function") {
        return ZodParsedType.promise;
      }
      if (typeof Map !== "undefined" && data instanceof Map) {
        return ZodParsedType.map;
      }
      if (typeof Set !== "undefined" && data instanceof Set) {
        return ZodParsedType.set;
      }
      if (typeof Date !== "undefined" && data instanceof Date) {
        return ZodParsedType.date;
      }
      return ZodParsedType.object;
    default:
      return ZodParsedType.unknown;
  }
};

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/ZodError.js
var ZodIssueCode = util.arrayToEnum([
  "invalid_type",
  "invalid_literal",
  "custom",
  "invalid_union",
  "invalid_union_discriminator",
  "invalid_enum_value",
  "unrecognized_keys",
  "invalid_arguments",
  "invalid_return_type",
  "invalid_date",
  "invalid_string",
  "too_small",
  "too_big",
  "invalid_intersection_types",
  "not_multiple_of",
  "not_finite"
]);
var quotelessJson = (obj) => {
  const json = JSON.stringify(obj, null, 2);
  return json.replace(/"([^"]+)":/g, "$1:");
};
var ZodError = class _ZodError extends Error {
  get errors() {
    return this.issues;
  }
  constructor(issues) {
    super();
    this.issues = [];
    this.addIssue = (sub3) => {
      this.issues = [...this.issues, sub3];
    };
    this.addIssues = (subs = []) => {
      this.issues = [...this.issues, ...subs];
    };
    const actualProto = new.target.prototype;
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(this, actualProto);
    } else {
      this.__proto__ = actualProto;
    }
    this.name = "ZodError";
    this.issues = issues;
  }
  format(_mapper) {
    const mapper = _mapper || function(issue) {
      return issue.message;
    };
    const fieldErrors = { _errors: [] };
    const processError = (error) => {
      for (const issue of error.issues) {
        if (issue.code === "invalid_union") {
          issue.unionErrors.map(processError);
        } else if (issue.code === "invalid_return_type") {
          processError(issue.returnTypeError);
        } else if (issue.code === "invalid_arguments") {
          processError(issue.argumentsError);
        } else if (issue.path.length === 0) {
          fieldErrors._errors.push(mapper(issue));
        } else {
          let curr = fieldErrors;
          let i = 0;
          while (i < issue.path.length) {
            const el = issue.path[i];
            const terminal = i === issue.path.length - 1;
            if (!terminal) {
              curr[el] = curr[el] || { _errors: [] };
            } else {
              curr[el] = curr[el] || { _errors: [] };
              curr[el]._errors.push(mapper(issue));
            }
            curr = curr[el];
            i++;
          }
        }
      }
    };
    processError(this);
    return fieldErrors;
  }
  static assert(value) {
    if (!(value instanceof _ZodError)) {
      throw new Error(`Not a ZodError: ${value}`);
    }
  }
  toString() {
    return this.message;
  }
  get message() {
    return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
  }
  get isEmpty() {
    return this.issues.length === 0;
  }
  flatten(mapper = (issue) => issue.message) {
    const fieldErrors = {};
    const formErrors = [];
    for (const sub3 of this.issues) {
      if (sub3.path.length > 0) {
        const firstEl = sub3.path[0];
        fieldErrors[firstEl] = fieldErrors[firstEl] || [];
        fieldErrors[firstEl].push(mapper(sub3));
      } else {
        formErrors.push(mapper(sub3));
      }
    }
    return { formErrors, fieldErrors };
  }
  get formErrors() {
    return this.flatten();
  }
};
ZodError.create = (issues) => {
  const error = new ZodError(issues);
  return error;
};

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/locales/en.js
var errorMap = (issue, _ctx) => {
  let message;
  switch (issue.code) {
    case ZodIssueCode.invalid_type:
      if (issue.received === ZodParsedType.undefined) {
        message = "Required";
      } else {
        message = `Expected ${issue.expected}, received ${issue.received}`;
      }
      break;
    case ZodIssueCode.invalid_literal:
      message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
      break;
    case ZodIssueCode.unrecognized_keys:
      message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
      break;
    case ZodIssueCode.invalid_union:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_union_discriminator:
      message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
      break;
    case ZodIssueCode.invalid_enum_value:
      message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
      break;
    case ZodIssueCode.invalid_arguments:
      message = `Invalid function arguments`;
      break;
    case ZodIssueCode.invalid_return_type:
      message = `Invalid function return type`;
      break;
    case ZodIssueCode.invalid_date:
      message = `Invalid date`;
      break;
    case ZodIssueCode.invalid_string:
      if (typeof issue.validation === "object") {
        if ("includes" in issue.validation) {
          message = `Invalid input: must include "${issue.validation.includes}"`;
          if (typeof issue.validation.position === "number") {
            message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`;
          }
        } else if ("startsWith" in issue.validation) {
          message = `Invalid input: must start with "${issue.validation.startsWith}"`;
        } else if ("endsWith" in issue.validation) {
          message = `Invalid input: must end with "${issue.validation.endsWith}"`;
        } else {
          util.assertNever(issue.validation);
        }
      } else if (issue.validation !== "regex") {
        message = `Invalid ${issue.validation}`;
      } else {
        message = "Invalid";
      }
      break;
    case ZodIssueCode.too_small:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `more than`} ${issue.minimum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `over`} ${issue.minimum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "bigint")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(issue.minimum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.too_big:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `less than`} ${issue.maximum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `under`} ${issue.maximum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "bigint")
        message = `BigInt must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly` : issue.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(issue.maximum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.custom:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_intersection_types:
      message = `Intersection results could not be merged`;
      break;
    case ZodIssueCode.not_multiple_of:
      message = `Number must be a multiple of ${issue.multipleOf}`;
      break;
    case ZodIssueCode.not_finite:
      message = "Number must be finite";
      break;
    default:
      message = _ctx.defaultError;
      util.assertNever(issue);
  }
  return { message };
};
var en_default = errorMap;

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/errors.js
var overrideErrorMap = en_default;
function setErrorMap(map) {
  overrideErrorMap = map;
}
function getErrorMap() {
  return overrideErrorMap;
}

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/parseUtil.js
var makeIssue = (params) => {
  const { data, path, errorMaps, issueData } = params;
  const fullPath = [...path, ...issueData.path || []];
  const fullIssue = {
    ...issueData,
    path: fullPath
  };
  if (issueData.message !== void 0) {
    return {
      ...issueData,
      path: fullPath,
      message: issueData.message
    };
  }
  let errorMessage = "";
  const maps = errorMaps.filter((m) => !!m).slice().reverse();
  for (const map of maps) {
    errorMessage = map(fullIssue, { data, defaultError: errorMessage }).message;
  }
  return {
    ...issueData,
    path: fullPath,
    message: errorMessage
  };
};
var EMPTY_PATH = [];
function addIssueToContext(ctx, issueData) {
  const overrideMap = getErrorMap();
  const issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      // contextual error map is first priority
      ctx.schemaErrorMap,
      // then schema-bound map if available
      overrideMap,
      // then global override map
      overrideMap === en_default ? void 0 : en_default
      // then global default map
    ].filter((x) => !!x)
  });
  ctx.common.issues.push(issue);
}
var ParseStatus = class _ParseStatus {
  constructor() {
    this.value = "valid";
  }
  dirty() {
    if (this.value === "valid")
      this.value = "dirty";
  }
  abort() {
    if (this.value !== "aborted")
      this.value = "aborted";
  }
  static mergeArray(status, results) {
    const arrayValue = [];
    for (const s of results) {
      if (s.status === "aborted")
        return INVALID;
      if (s.status === "dirty")
        status.dirty();
      arrayValue.push(s.value);
    }
    return { status: status.value, value: arrayValue };
  }
  static async mergeObjectAsync(status, pairs) {
    const syncPairs = [];
    for (const pair of pairs) {
      const key = await pair.key;
      const value = await pair.value;
      syncPairs.push({
        key,
        value
      });
    }
    return _ParseStatus.mergeObjectSync(status, syncPairs);
  }
  static mergeObjectSync(status, pairs) {
    const finalObject = {};
    for (const pair of pairs) {
      const { key, value } = pair;
      if (key.status === "aborted")
        return INVALID;
      if (value.status === "aborted")
        return INVALID;
      if (key.status === "dirty")
        status.dirty();
      if (value.status === "dirty")
        status.dirty();
      if (key.value !== "__proto__" && (typeof value.value !== "undefined" || pair.alwaysSet)) {
        finalObject[key.value] = value.value;
      }
    }
    return { status: status.value, value: finalObject };
  }
};
var INVALID = Object.freeze({
  status: "aborted"
});
var DIRTY = (value) => ({ status: "dirty", value });
var OK = (value) => ({ status: "valid", value });
var isAborted = (x) => x.status === "aborted";
var isDirty = (x) => x.status === "dirty";
var isValid = (x) => x.status === "valid";
var isAsync = (x) => typeof Promise !== "undefined" && x instanceof Promise;

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/errorUtil.js
var errorUtil;
(function(errorUtil2) {
  errorUtil2.errToObj = (message) => typeof message === "string" ? { message } : message || {};
  errorUtil2.toString = (message) => typeof message === "string" ? message : message?.message;
})(errorUtil || (errorUtil = {}));

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/types.js
var ParseInputLazyPath = class {
  constructor(parent, value, path, key) {
    this._cachedPath = [];
    this.parent = parent;
    this.data = value;
    this._path = path;
    this._key = key;
  }
  get path() {
    if (!this._cachedPath.length) {
      if (Array.isArray(this._key)) {
        this._cachedPath.push(...this._path, ...this._key);
      } else {
        this._cachedPath.push(...this._path, this._key);
      }
    }
    return this._cachedPath;
  }
};
var handleResult = (ctx, result) => {
  if (isValid(result)) {
    return { success: true, data: result.value };
  } else {
    if (!ctx.common.issues.length) {
      throw new Error("Validation failed but no issues detected.");
    }
    return {
      success: false,
      get error() {
        if (this._error)
          return this._error;
        const error = new ZodError(ctx.common.issues);
        this._error = error;
        return this._error;
      }
    };
  }
};
function processCreateParams(params) {
  if (!params)
    return {};
  const { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error)) {
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  }
  if (errorMap2)
    return { errorMap: errorMap2, description };
  const customMap = (iss, ctx) => {
    const { message } = params;
    if (iss.code === "invalid_enum_value") {
      return { message: message ?? ctx.defaultError };
    }
    if (typeof ctx.data === "undefined") {
      return { message: message ?? required_error ?? ctx.defaultError };
    }
    if (iss.code !== "invalid_type")
      return { message: ctx.defaultError };
    return { message: message ?? invalid_type_error ?? ctx.defaultError };
  };
  return { errorMap: customMap, description };
}
var ZodType = class {
  get description() {
    return this._def.description;
  }
  _getType(input) {
    return getParsedType(input.data);
  }
  _getOrReturnCtx(input, ctx) {
    return ctx || {
      common: input.parent.common,
      data: input.data,
      parsedType: getParsedType(input.data),
      schemaErrorMap: this._def.errorMap,
      path: input.path,
      parent: input.parent
    };
  }
  _processInputParams(input) {
    return {
      status: new ParseStatus(),
      ctx: {
        common: input.parent.common,
        data: input.data,
        parsedType: getParsedType(input.data),
        schemaErrorMap: this._def.errorMap,
        path: input.path,
        parent: input.parent
      }
    };
  }
  _parseSync(input) {
    const result = this._parse(input);
    if (isAsync(result)) {
      throw new Error("Synchronous parse encountered promise.");
    }
    return result;
  }
  _parseAsync(input) {
    const result = this._parse(input);
    return Promise.resolve(result);
  }
  parse(data, params) {
    const result = this.safeParse(data, params);
    if (result.success)
      return result.data;
    throw result.error;
  }
  safeParse(data, params) {
    const ctx = {
      common: {
        issues: [],
        async: params?.async ?? false,
        contextualErrorMap: params?.errorMap
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const result = this._parseSync({ data, path: ctx.path, parent: ctx });
    return handleResult(ctx, result);
  }
  "~validate"(data) {
    const ctx = {
      common: {
        issues: [],
        async: !!this["~standard"].async
      },
      path: [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    if (!this["~standard"].async) {
      try {
        const result = this._parseSync({ data, path: [], parent: ctx });
        return isValid(result) ? {
          value: result.value
        } : {
          issues: ctx.common.issues
        };
      } catch (err) {
        if (err?.message?.toLowerCase()?.includes("encountered")) {
          this["~standard"].async = true;
        }
        ctx.common = {
          issues: [],
          async: true
        };
      }
    }
    return this._parseAsync({ data, path: [], parent: ctx }).then((result) => isValid(result) ? {
      value: result.value
    } : {
      issues: ctx.common.issues
    });
  }
  async parseAsync(data, params) {
    const result = await this.safeParseAsync(data, params);
    if (result.success)
      return result.data;
    throw result.error;
  }
  async safeParseAsync(data, params) {
    const ctx = {
      common: {
        issues: [],
        contextualErrorMap: params?.errorMap,
        async: true
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx });
    const result = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
    return handleResult(ctx, result);
  }
  refine(check, message) {
    const getIssueProperties = (val) => {
      if (typeof message === "string" || typeof message === "undefined") {
        return { message };
      } else if (typeof message === "function") {
        return message(val);
      } else {
        return message;
      }
    };
    return this._refinement((val, ctx) => {
      const result = check(val);
      const setError = () => ctx.addIssue({
        code: ZodIssueCode.custom,
        ...getIssueProperties(val)
      });
      if (typeof Promise !== "undefined" && result instanceof Promise) {
        return result.then((data) => {
          if (!data) {
            setError();
            return false;
          } else {
            return true;
          }
        });
      }
      if (!result) {
        setError();
        return false;
      } else {
        return true;
      }
    });
  }
  refinement(check, refinementData) {
    return this._refinement((val, ctx) => {
      if (!check(val)) {
        ctx.addIssue(typeof refinementData === "function" ? refinementData(val, ctx) : refinementData);
        return false;
      } else {
        return true;
      }
    });
  }
  _refinement(refinement) {
    return new ZodEffects({
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "refinement", refinement }
    });
  }
  superRefine(refinement) {
    return this._refinement(refinement);
  }
  constructor(def) {
    this.spa = this.safeParseAsync;
    this._def = def;
    this.parse = this.parse.bind(this);
    this.safeParse = this.safeParse.bind(this);
    this.parseAsync = this.parseAsync.bind(this);
    this.safeParseAsync = this.safeParseAsync.bind(this);
    this.spa = this.spa.bind(this);
    this.refine = this.refine.bind(this);
    this.refinement = this.refinement.bind(this);
    this.superRefine = this.superRefine.bind(this);
    this.optional = this.optional.bind(this);
    this.nullable = this.nullable.bind(this);
    this.nullish = this.nullish.bind(this);
    this.array = this.array.bind(this);
    this.promise = this.promise.bind(this);
    this.or = this.or.bind(this);
    this.and = this.and.bind(this);
    this.transform = this.transform.bind(this);
    this.brand = this.brand.bind(this);
    this.default = this.default.bind(this);
    this.catch = this.catch.bind(this);
    this.describe = this.describe.bind(this);
    this.pipe = this.pipe.bind(this);
    this.readonly = this.readonly.bind(this);
    this.isNullable = this.isNullable.bind(this);
    this.isOptional = this.isOptional.bind(this);
    this["~standard"] = {
      version: 1,
      vendor: "zod",
      validate: (data) => this["~validate"](data)
    };
  }
  optional() {
    return ZodOptional.create(this, this._def);
  }
  nullable() {
    return ZodNullable.create(this, this._def);
  }
  nullish() {
    return this.nullable().optional();
  }
  array() {
    return ZodArray.create(this);
  }
  promise() {
    return ZodPromise.create(this, this._def);
  }
  or(option) {
    return ZodUnion.create([this, option], this._def);
  }
  and(incoming) {
    return ZodIntersection.create(this, incoming, this._def);
  }
  transform(transform) {
    return new ZodEffects({
      ...processCreateParams(this._def),
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "transform", transform }
    });
  }
  default(def) {
    const defaultValueFunc = typeof def === "function" ? def : () => def;
    return new ZodDefault({
      ...processCreateParams(this._def),
      innerType: this,
      defaultValue: defaultValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodDefault
    });
  }
  brand() {
    return new ZodBranded({
      typeName: ZodFirstPartyTypeKind.ZodBranded,
      type: this,
      ...processCreateParams(this._def)
    });
  }
  catch(def) {
    const catchValueFunc = typeof def === "function" ? def : () => def;
    return new ZodCatch({
      ...processCreateParams(this._def),
      innerType: this,
      catchValue: catchValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodCatch
    });
  }
  describe(description) {
    const This = this.constructor;
    return new This({
      ...this._def,
      description
    });
  }
  pipe(target) {
    return ZodPipeline.create(this, target);
  }
  readonly() {
    return ZodReadonly.create(this);
  }
  isOptional() {
    return this.safeParse(void 0).success;
  }
  isNullable() {
    return this.safeParse(null).success;
  }
};
var cuidRegex = /^c[^\s-]{8,}$/i;
var cuid2Regex = /^[0-9a-z]+$/;
var ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/i;
var uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i;
var nanoidRegex = /^[a-z0-9_-]{21}$/i;
var jwtRegex = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/;
var durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/;
var emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i;
var _emojiRegex = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`;
var emojiRegex;
var ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/;
var ipv4CidrRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/;
var ipv6Regex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/;
var ipv6CidrRegex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/;
var base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/;
var base64urlRegex = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/;
var dateRegexSource = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`;
var dateRegex = new RegExp(`^${dateRegexSource}$`);
function timeRegexSource(args) {
  let secondsRegexSource = `[0-5]\\d`;
  if (args.precision) {
    secondsRegexSource = `${secondsRegexSource}\\.\\d{${args.precision}}`;
  } else if (args.precision == null) {
    secondsRegexSource = `${secondsRegexSource}(\\.\\d+)?`;
  }
  const secondsQuantifier = args.precision ? "+" : "?";
  return `([01]\\d|2[0-3]):[0-5]\\d(:${secondsRegexSource})${secondsQuantifier}`;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex = `${dateRegexSource}T${timeRegexSource(args)}`;
  const opts = [];
  opts.push(args.local ? `Z?` : `Z`);
  if (args.offset)
    opts.push(`([+-]\\d{2}:?\\d{2})`);
  regex = `${regex}(${opts.join("|")})`;
  return new RegExp(`^${regex}$`);
}
function isValidIP(ip, version) {
  if ((version === "v4" || !version) && ipv4Regex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6Regex.test(ip)) {
    return true;
  }
  return false;
}
function isValidJWT(jwt, alg) {
  if (!jwtRegex.test(jwt))
    return false;
  try {
    const [header] = jwt.split(".");
    if (!header)
      return false;
    const base64 = header.replace(/-/g, "+").replace(/_/g, "/").padEnd(header.length + (4 - header.length % 4) % 4, "=");
    const decoded = JSON.parse(atob(base64));
    if (typeof decoded !== "object" || decoded === null)
      return false;
    if ("typ" in decoded && decoded?.typ !== "JWT")
      return false;
    if (!decoded.alg)
      return false;
    if (alg && decoded.alg !== alg)
      return false;
    return true;
  } catch {
    return false;
  }
}
function isValidCidr(ip, version) {
  if ((version === "v4" || !version) && ipv4CidrRegex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6CidrRegex.test(ip)) {
    return true;
  }
  return false;
}
var ZodString = class _ZodString extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = String(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.string) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.string,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.length < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.length > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "length") {
        const tooBig = input.data.length > check.value;
        const tooSmall = input.data.length < check.value;
        if (tooBig || tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          if (tooBig) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          } else if (tooSmall) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          }
          status.dirty();
        }
      } else if (check.kind === "email") {
        if (!emailRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "email",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "emoji") {
        if (!emojiRegex) {
          emojiRegex = new RegExp(_emojiRegex, "u");
        }
        if (!emojiRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "emoji",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "uuid") {
        if (!uuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "uuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "nanoid") {
        if (!nanoidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "nanoid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid") {
        if (!cuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid2") {
        if (!cuid2Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid2",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ulid") {
        if (!ulidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ulid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "url") {
        try {
          new URL(input.data);
        } catch {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "regex") {
        check.regex.lastIndex = 0;
        const testResult = check.regex.test(input.data);
        if (!testResult) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "regex",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "trim") {
        input.data = input.data.trim();
      } else if (check.kind === "includes") {
        if (!input.data.includes(check.value, check.position)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { includes: check.value, position: check.position },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "toLowerCase") {
        input.data = input.data.toLowerCase();
      } else if (check.kind === "toUpperCase") {
        input.data = input.data.toUpperCase();
      } else if (check.kind === "startsWith") {
        if (!input.data.startsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { startsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "endsWith") {
        if (!input.data.endsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { endsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "datetime") {
        const regex = datetimeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "datetime",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "date") {
        const regex = dateRegex;
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "date",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "time") {
        const regex = timeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "time",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "duration") {
        if (!durationRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "duration",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ip") {
        if (!isValidIP(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ip",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "jwt") {
        if (!isValidJWT(input.data, check.alg)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "jwt",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cidr") {
        if (!isValidCidr(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cidr",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64") {
        if (!base64Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64url") {
        if (!base64urlRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _regex(regex, validation, message) {
    return this.refinement((data) => regex.test(data), {
      validation,
      code: ZodIssueCode.invalid_string,
      ...errorUtil.errToObj(message)
    });
  }
  _addCheck(check) {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  email(message) {
    return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
  }
  url(message) {
    return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
  }
  emoji(message) {
    return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
  }
  uuid(message) {
    return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
  }
  nanoid(message) {
    return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
  }
  cuid(message) {
    return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
  }
  cuid2(message) {
    return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
  }
  ulid(message) {
    return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
  }
  base64(message) {
    return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
  }
  base64url(message) {
    return this._addCheck({
      kind: "base64url",
      ...errorUtil.errToObj(message)
    });
  }
  jwt(options) {
    return this._addCheck({ kind: "jwt", ...errorUtil.errToObj(options) });
  }
  ip(options) {
    return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
  }
  cidr(options) {
    return this._addCheck({ kind: "cidr", ...errorUtil.errToObj(options) });
  }
  datetime(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "datetime",
        precision: null,
        offset: false,
        local: false,
        message: options
      });
    }
    return this._addCheck({
      kind: "datetime",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      offset: options?.offset ?? false,
      local: options?.local ?? false,
      ...errorUtil.errToObj(options?.message)
    });
  }
  date(message) {
    return this._addCheck({ kind: "date", message });
  }
  time(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "time",
        precision: null,
        message: options
      });
    }
    return this._addCheck({
      kind: "time",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      ...errorUtil.errToObj(options?.message)
    });
  }
  duration(message) {
    return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
  }
  regex(regex, message) {
    return this._addCheck({
      kind: "regex",
      regex,
      ...errorUtil.errToObj(message)
    });
  }
  includes(value, options) {
    return this._addCheck({
      kind: "includes",
      value,
      position: options?.position,
      ...errorUtil.errToObj(options?.message)
    });
  }
  startsWith(value, message) {
    return this._addCheck({
      kind: "startsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  endsWith(value, message) {
    return this._addCheck({
      kind: "endsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  min(minLength, message) {
    return this._addCheck({
      kind: "min",
      value: minLength,
      ...errorUtil.errToObj(message)
    });
  }
  max(maxLength, message) {
    return this._addCheck({
      kind: "max",
      value: maxLength,
      ...errorUtil.errToObj(message)
    });
  }
  length(len, message) {
    return this._addCheck({
      kind: "length",
      value: len,
      ...errorUtil.errToObj(message)
    });
  }
  /**
   * Equivalent to `.min(1)`
   */
  nonempty(message) {
    return this.min(1, errorUtil.errToObj(message));
  }
  trim() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "trim" }]
    });
  }
  toLowerCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toLowerCase" }]
    });
  }
  toUpperCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toUpperCase" }]
    });
  }
  get isDatetime() {
    return !!this._def.checks.find((ch) => ch.kind === "datetime");
  }
  get isDate() {
    return !!this._def.checks.find((ch) => ch.kind === "date");
  }
  get isTime() {
    return !!this._def.checks.find((ch) => ch.kind === "time");
  }
  get isDuration() {
    return !!this._def.checks.find((ch) => ch.kind === "duration");
  }
  get isEmail() {
    return !!this._def.checks.find((ch) => ch.kind === "email");
  }
  get isURL() {
    return !!this._def.checks.find((ch) => ch.kind === "url");
  }
  get isEmoji() {
    return !!this._def.checks.find((ch) => ch.kind === "emoji");
  }
  get isUUID() {
    return !!this._def.checks.find((ch) => ch.kind === "uuid");
  }
  get isNANOID() {
    return !!this._def.checks.find((ch) => ch.kind === "nanoid");
  }
  get isCUID() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid");
  }
  get isCUID2() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid2");
  }
  get isULID() {
    return !!this._def.checks.find((ch) => ch.kind === "ulid");
  }
  get isIP() {
    return !!this._def.checks.find((ch) => ch.kind === "ip");
  }
  get isCIDR() {
    return !!this._def.checks.find((ch) => ch.kind === "cidr");
  }
  get isBase64() {
    return !!this._def.checks.find((ch) => ch.kind === "base64");
  }
  get isBase64url() {
    return !!this._def.checks.find((ch) => ch.kind === "base64url");
  }
  get minLength() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxLength() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodString.create = (params) => {
  return new ZodString({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodString,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
function floatSafeRemainder(val, step) {
  const valDecCount = (val.toString().split(".")[1] || "").length;
  const stepDecCount = (step.toString().split(".")[1] || "").length;
  const decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount;
  const valInt = Number.parseInt(val.toFixed(decCount).replace(".", ""));
  const stepInt = Number.parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / 10 ** decCount;
}
var ZodNumber = class _ZodNumber extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
    this.step = this.multipleOf;
  }
  _parse(input) {
    if (this._def.coerce) {
      input.data = Number(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.number) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.number,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "int") {
        if (!util.isInteger(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: "integer",
            received: "float",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (floatSafeRemainder(input.data, check.value) !== 0) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "finite") {
        if (!Number.isFinite(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_finite,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodNumber({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodNumber({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  int(message) {
    return this._addCheck({
      kind: "int",
      message: errorUtil.toString(message)
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  finite(message) {
    return this._addCheck({
      kind: "finite",
      message: errorUtil.toString(message)
    });
  }
  safe(message) {
    return this._addCheck({
      kind: "min",
      inclusive: true,
      value: Number.MIN_SAFE_INTEGER,
      message: errorUtil.toString(message)
    })._addCheck({
      kind: "max",
      inclusive: true,
      value: Number.MAX_SAFE_INTEGER,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
  get isInt() {
    return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
  }
  get isFinite() {
    let max = null;
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf") {
        return true;
      } else if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      } else if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return Number.isFinite(min) && Number.isFinite(max);
  }
};
ZodNumber.create = (params) => {
  return new ZodNumber({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodNumber,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodBigInt = class _ZodBigInt extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
  }
  _parse(input) {
    if (this._def.coerce) {
      try {
        input.data = BigInt(input.data);
      } catch {
        return this._getInvalidInput(input);
      }
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.bigint) {
      return this._getInvalidInput(input);
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            type: "bigint",
            minimum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            type: "bigint",
            maximum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (input.data % check.value !== BigInt(0)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _getInvalidInput(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.bigint,
      received: ctx.parsedType
    });
    return INVALID;
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodBigInt({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodBigInt({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodBigInt.create = (params) => {
  return new ZodBigInt({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodBigInt,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
var ZodBoolean = class extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = Boolean(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.boolean) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.boolean,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodBoolean.create = (params) => {
  return new ZodBoolean({
    typeName: ZodFirstPartyTypeKind.ZodBoolean,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodDate = class _ZodDate extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = new Date(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.date) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.date,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    if (Number.isNaN(input.data.getTime())) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_date
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.getTime() < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            message: check.message,
            inclusive: true,
            exact: false,
            minimum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.getTime() > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            message: check.message,
            inclusive: true,
            exact: false,
            maximum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return {
      status: status.value,
      value: new Date(input.data.getTime())
    };
  }
  _addCheck(check) {
    return new _ZodDate({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  min(minDate, message) {
    return this._addCheck({
      kind: "min",
      value: minDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  max(maxDate, message) {
    return this._addCheck({
      kind: "max",
      value: maxDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  get minDate() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min != null ? new Date(min) : null;
  }
  get maxDate() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max != null ? new Date(max) : null;
  }
};
ZodDate.create = (params) => {
  return new ZodDate({
    checks: [],
    coerce: params?.coerce || false,
    typeName: ZodFirstPartyTypeKind.ZodDate,
    ...processCreateParams(params)
  });
};
var ZodSymbol = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.symbol) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.symbol,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodSymbol.create = (params) => {
  return new ZodSymbol({
    typeName: ZodFirstPartyTypeKind.ZodSymbol,
    ...processCreateParams(params)
  });
};
var ZodUndefined = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.undefined,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodUndefined.create = (params) => {
  return new ZodUndefined({
    typeName: ZodFirstPartyTypeKind.ZodUndefined,
    ...processCreateParams(params)
  });
};
var ZodNull = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.null) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.null,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodNull.create = (params) => {
  return new ZodNull({
    typeName: ZodFirstPartyTypeKind.ZodNull,
    ...processCreateParams(params)
  });
};
var ZodAny = class extends ZodType {
  constructor() {
    super(...arguments);
    this._any = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodAny.create = (params) => {
  return new ZodAny({
    typeName: ZodFirstPartyTypeKind.ZodAny,
    ...processCreateParams(params)
  });
};
var ZodUnknown = class extends ZodType {
  constructor() {
    super(...arguments);
    this._unknown = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodUnknown.create = (params) => {
  return new ZodUnknown({
    typeName: ZodFirstPartyTypeKind.ZodUnknown,
    ...processCreateParams(params)
  });
};
var ZodNever = class extends ZodType {
  _parse(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.never,
      received: ctx.parsedType
    });
    return INVALID;
  }
};
ZodNever.create = (params) => {
  return new ZodNever({
    typeName: ZodFirstPartyTypeKind.ZodNever,
    ...processCreateParams(params)
  });
};
var ZodVoid = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.void,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodVoid.create = (params) => {
  return new ZodVoid({
    typeName: ZodFirstPartyTypeKind.ZodVoid,
    ...processCreateParams(params)
  });
};
var ZodArray = class _ZodArray extends ZodType {
  _parse(input) {
    const { ctx, status } = this._processInputParams(input);
    const def = this._def;
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (def.exactLength !== null) {
      const tooBig = ctx.data.length > def.exactLength.value;
      const tooSmall = ctx.data.length < def.exactLength.value;
      if (tooBig || tooSmall) {
        addIssueToContext(ctx, {
          code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
          minimum: tooSmall ? def.exactLength.value : void 0,
          maximum: tooBig ? def.exactLength.value : void 0,
          type: "array",
          inclusive: true,
          exact: true,
          message: def.exactLength.message
        });
        status.dirty();
      }
    }
    if (def.minLength !== null) {
      if (ctx.data.length < def.minLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.minLength.message
        });
        status.dirty();
      }
    }
    if (def.maxLength !== null) {
      if (ctx.data.length > def.maxLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.maxLength.message
        });
        status.dirty();
      }
    }
    if (ctx.common.async) {
      return Promise.all([...ctx.data].map((item, i) => {
        return def.type._parseAsync(new ParseInputLazyPath(ctx, item, ctx.path, i));
      })).then((result2) => {
        return ParseStatus.mergeArray(status, result2);
      });
    }
    const result = [...ctx.data].map((item, i) => {
      return def.type._parseSync(new ParseInputLazyPath(ctx, item, ctx.path, i));
    });
    return ParseStatus.mergeArray(status, result);
  }
  get element() {
    return this._def.type;
  }
  min(minLength, message) {
    return new _ZodArray({
      ...this._def,
      minLength: { value: minLength, message: errorUtil.toString(message) }
    });
  }
  max(maxLength, message) {
    return new _ZodArray({
      ...this._def,
      maxLength: { value: maxLength, message: errorUtil.toString(message) }
    });
  }
  length(len, message) {
    return new _ZodArray({
      ...this._def,
      exactLength: { value: len, message: errorUtil.toString(message) }
    });
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodArray.create = (schema, params) => {
  return new ZodArray({
    type: schema,
    minLength: null,
    maxLength: null,
    exactLength: null,
    typeName: ZodFirstPartyTypeKind.ZodArray,
    ...processCreateParams(params)
  });
};
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    const newShape = {};
    for (const key in schema.shape) {
      const fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: () => newShape
    });
  } else if (schema instanceof ZodArray) {
    return new ZodArray({
      ...schema._def,
      type: deepPartialify(schema.element)
    });
  } else if (schema instanceof ZodOptional) {
    return ZodOptional.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodNullable) {
    return ZodNullable.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodTuple) {
    return ZodTuple.create(schema.items.map((item) => deepPartialify(item)));
  } else {
    return schema;
  }
}
var ZodObject = class _ZodObject extends ZodType {
  constructor() {
    super(...arguments);
    this._cached = null;
    this.nonstrict = this.passthrough;
    this.augment = this.extend;
  }
  _getCached() {
    if (this._cached !== null)
      return this._cached;
    const shape = this._def.shape();
    const keys = util.objectKeys(shape);
    this._cached = { shape, keys };
    return this._cached;
  }
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.object) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const { status, ctx } = this._processInputParams(input);
    const { shape, keys: shapeKeys } = this._getCached();
    const extraKeys = [];
    if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip")) {
      for (const key in ctx.data) {
        if (!shapeKeys.includes(key)) {
          extraKeys.push(key);
        }
      }
    }
    const pairs = [];
    for (const key of shapeKeys) {
      const keyValidator = shape[key];
      const value = ctx.data[key];
      pairs.push({
        key: { status: "valid", value: key },
        value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (this._def.catchall instanceof ZodNever) {
      const unknownKeys = this._def.unknownKeys;
      if (unknownKeys === "passthrough") {
        for (const key of extraKeys) {
          pairs.push({
            key: { status: "valid", value: key },
            value: { status: "valid", value: ctx.data[key] }
          });
        }
      } else if (unknownKeys === "strict") {
        if (extraKeys.length > 0) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.unrecognized_keys,
            keys: extraKeys
          });
          status.dirty();
        }
      } else if (unknownKeys === "strip") {
      } else {
        throw new Error(`Internal ZodObject error: invalid unknownKeys value.`);
      }
    } else {
      const catchall = this._def.catchall;
      for (const key of extraKeys) {
        const value = ctx.data[key];
        pairs.push({
          key: { status: "valid", value: key },
          value: catchall._parse(
            new ParseInputLazyPath(ctx, value, ctx.path, key)
            //, ctx.child(key), value, getParsedType(value)
          ),
          alwaysSet: key in ctx.data
        });
      }
    }
    if (ctx.common.async) {
      return Promise.resolve().then(async () => {
        const syncPairs = [];
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          syncPairs.push({
            key,
            value,
            alwaysSet: pair.alwaysSet
          });
        }
        return syncPairs;
      }).then((syncPairs) => {
        return ParseStatus.mergeObjectSync(status, syncPairs);
      });
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get shape() {
    return this._def.shape();
  }
  strict(message) {
    errorUtil.errToObj;
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strict",
      ...message !== void 0 ? {
        errorMap: (issue, ctx) => {
          const defaultError = this._def.errorMap?.(issue, ctx).message ?? ctx.defaultError;
          if (issue.code === "unrecognized_keys")
            return {
              message: errorUtil.errToObj(message).message ?? defaultError
            };
          return {
            message: defaultError
          };
        }
      } : {}
    });
  }
  strip() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strip"
    });
  }
  passthrough() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "passthrough"
    });
  }
  // const AugmentFactory =
  //   <Def extends ZodObjectDef>(def: Def) =>
  //   <Augmentation extends ZodRawShape>(
  //     augmentation: Augmentation
  //   ): ZodObject<
  //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
  //     Def["unknownKeys"],
  //     Def["catchall"]
  //   > => {
  //     return new ZodObject({
  //       ...def,
  //       shape: () => ({
  //         ...def.shape(),
  //         ...augmentation,
  //       }),
  //     }) as any;
  //   };
  extend(augmentation) {
    return new _ZodObject({
      ...this._def,
      shape: () => ({
        ...this._def.shape(),
        ...augmentation
      })
    });
  }
  /**
   * Prior to zod@1.0.12 there was a bug in the
   * inferred type of merged objects. Please
   * upgrade if you are experiencing issues.
   */
  merge(merging) {
    const merged = new _ZodObject({
      unknownKeys: merging._def.unknownKeys,
      catchall: merging._def.catchall,
      shape: () => ({
        ...this._def.shape(),
        ...merging._def.shape()
      }),
      typeName: ZodFirstPartyTypeKind.ZodObject
    });
    return merged;
  }
  // merge<
  //   Incoming extends AnyZodObject,
  //   Augmentation extends Incoming["shape"],
  //   NewOutput extends {
  //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
  //       ? Augmentation[k]["_output"]
  //       : k extends keyof Output
  //       ? Output[k]
  //       : never;
  //   },
  //   NewInput extends {
  //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
  //       ? Augmentation[k]["_input"]
  //       : k extends keyof Input
  //       ? Input[k]
  //       : never;
  //   }
  // >(
  //   merging: Incoming
  // ): ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"],
  //   NewOutput,
  //   NewInput
  // > {
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  setKey(key, schema) {
    return this.augment({ [key]: schema });
  }
  // merge<Incoming extends AnyZodObject>(
  //   merging: Incoming
  // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
  // ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"]
  // > {
  //   // const mergedShape = objectUtil.mergeShapes(
  //   //   this._def.shape(),
  //   //   merging._def.shape()
  //   // );
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  catchall(index) {
    return new _ZodObject({
      ...this._def,
      catchall: index
    });
  }
  pick(mask) {
    const shape = {};
    for (const key of util.objectKeys(mask)) {
      if (mask[key] && this.shape[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  omit(mask) {
    const shape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (!mask[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  /**
   * @deprecated
   */
  deepPartial() {
    return deepPartialify(this);
  }
  partial(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      const fieldSchema = this.shape[key];
      if (mask && !mask[key]) {
        newShape[key] = fieldSchema;
      } else {
        newShape[key] = fieldSchema.optional();
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  required(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (mask && !mask[key]) {
        newShape[key] = this.shape[key];
      } else {
        const fieldSchema = this.shape[key];
        let newField = fieldSchema;
        while (newField instanceof ZodOptional) {
          newField = newField._def.innerType;
        }
        newShape[key] = newField;
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  keyof() {
    return createZodEnum(util.objectKeys(this.shape));
  }
};
ZodObject.create = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.strictCreate = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strict",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.lazycreate = (shape, params) => {
  return new ZodObject({
    shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
var ZodUnion = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const options = this._def.options;
    function handleResults(results) {
      for (const result of results) {
        if (result.result.status === "valid") {
          return result.result;
        }
      }
      for (const result of results) {
        if (result.result.status === "dirty") {
          ctx.common.issues.push(...result.ctx.common.issues);
          return result.result;
        }
      }
      const unionErrors = results.map((result) => new ZodError(result.ctx.common.issues));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return Promise.all(options.map(async (option) => {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        return {
          result: await option._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: childCtx
          }),
          ctx: childCtx
        };
      })).then(handleResults);
    } else {
      let dirty = void 0;
      const issues = [];
      for (const option of options) {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        const result = option._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: childCtx
        });
        if (result.status === "valid") {
          return result;
        } else if (result.status === "dirty" && !dirty) {
          dirty = { result, ctx: childCtx };
        }
        if (childCtx.common.issues.length) {
          issues.push(childCtx.common.issues);
        }
      }
      if (dirty) {
        ctx.common.issues.push(...dirty.ctx.common.issues);
        return dirty.result;
      }
      const unionErrors = issues.map((issues2) => new ZodError(issues2));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
  }
  get options() {
    return this._def.options;
  }
};
ZodUnion.create = (types, params) => {
  return new ZodUnion({
    options: types,
    typeName: ZodFirstPartyTypeKind.ZodUnion,
    ...processCreateParams(params)
  });
};
var getDiscriminator = (type) => {
  if (type instanceof ZodLazy) {
    return getDiscriminator(type.schema);
  } else if (type instanceof ZodEffects) {
    return getDiscriminator(type.innerType());
  } else if (type instanceof ZodLiteral) {
    return [type.value];
  } else if (type instanceof ZodEnum) {
    return type.options;
  } else if (type instanceof ZodNativeEnum) {
    return util.objectValues(type.enum);
  } else if (type instanceof ZodDefault) {
    return getDiscriminator(type._def.innerType);
  } else if (type instanceof ZodUndefined) {
    return [void 0];
  } else if (type instanceof ZodNull) {
    return [null];
  } else if (type instanceof ZodOptional) {
    return [void 0, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodNullable) {
    return [null, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodBranded) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodReadonly) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodCatch) {
    return getDiscriminator(type._def.innerType);
  } else {
    return [];
  }
};
var ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const discriminator = this.discriminator;
    const discriminatorValue = ctx.data[discriminator];
    const option = this.optionsMap.get(discriminatorValue);
    if (!option) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union_discriminator,
        options: Array.from(this.optionsMap.keys()),
        path: [discriminator]
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return option._parseAsync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    } else {
      return option._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    }
  }
  get discriminator() {
    return this._def.discriminator;
  }
  get options() {
    return this._def.options;
  }
  get optionsMap() {
    return this._def.optionsMap;
  }
  /**
   * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
   * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
   * have a different value for each object in the union.
   * @param discriminator the name of the discriminator property
   * @param types an array of object schemas
   * @param params
   */
  static create(discriminator, options, params) {
    const optionsMap = /* @__PURE__ */ new Map();
    for (const type of options) {
      const discriminatorValues = getDiscriminator(type.shape[discriminator]);
      if (!discriminatorValues.length) {
        throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
      }
      for (const value of discriminatorValues) {
        if (optionsMap.has(value)) {
          throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
        }
        optionsMap.set(value, type);
      }
    }
    return new _ZodDiscriminatedUnion({
      typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
      discriminator,
      options,
      optionsMap,
      ...processCreateParams(params)
    });
  }
};
function mergeValues(a, b) {
  const aType = getParsedType(a);
  const bType = getParsedType(b);
  if (a === b) {
    return { valid: true, data: a };
  } else if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    const bKeys = util.objectKeys(b);
    const sharedKeys = util.objectKeys(a).filter((key) => bKeys.indexOf(key) !== -1);
    const newObj = { ...a, ...b };
    for (const key of sharedKeys) {
      const sharedValue = mergeValues(a[key], b[key]);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newObj[key] = sharedValue.data;
    }
    return { valid: true, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a.length !== b.length) {
      return { valid: false };
    }
    const newArray = [];
    for (let index = 0; index < a.length; index++) {
      const itemA = a[index];
      const itemB = b[index];
      const sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newArray.push(sharedValue.data);
    }
    return { valid: true, data: newArray };
  } else if (aType === ZodParsedType.date && bType === ZodParsedType.date && +a === +b) {
    return { valid: true, data: a };
  } else {
    return { valid: false };
  }
}
var ZodIntersection = class extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const handleParsed = (parsedLeft, parsedRight) => {
      if (isAborted(parsedLeft) || isAborted(parsedRight)) {
        return INVALID;
      }
      const merged = mergeValues(parsedLeft.value, parsedRight.value);
      if (!merged.valid) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_intersection_types
        });
        return INVALID;
      }
      if (isDirty(parsedLeft) || isDirty(parsedRight)) {
        status.dirty();
      }
      return { status: status.value, value: merged.data };
    };
    if (ctx.common.async) {
      return Promise.all([
        this._def.left._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }),
        this._def.right._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        })
      ]).then(([left, right]) => handleParsed(left, right));
    } else {
      return handleParsed(this._def.left._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }), this._def.right._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }));
    }
  }
};
ZodIntersection.create = (left, right, params) => {
  return new ZodIntersection({
    left,
    right,
    typeName: ZodFirstPartyTypeKind.ZodIntersection,
    ...processCreateParams(params)
  });
};
var ZodTuple = class _ZodTuple extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (ctx.data.length < this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_small,
        minimum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      return INVALID;
    }
    const rest = this._def.rest;
    if (!rest && ctx.data.length > this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_big,
        maximum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      status.dirty();
    }
    const items = [...ctx.data].map((item, itemIndex) => {
      const schema = this._def.items[itemIndex] || this._def.rest;
      if (!schema)
        return null;
      return schema._parse(new ParseInputLazyPath(ctx, item, ctx.path, itemIndex));
    }).filter((x) => !!x);
    if (ctx.common.async) {
      return Promise.all(items).then((results) => {
        return ParseStatus.mergeArray(status, results);
      });
    } else {
      return ParseStatus.mergeArray(status, items);
    }
  }
  get items() {
    return this._def.items;
  }
  rest(rest) {
    return new _ZodTuple({
      ...this._def,
      rest
    });
  }
};
ZodTuple.create = (schemas, params) => {
  if (!Array.isArray(schemas)) {
    throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
  }
  return new ZodTuple({
    items: schemas,
    typeName: ZodFirstPartyTypeKind.ZodTuple,
    rest: null,
    ...processCreateParams(params)
  });
};
var ZodRecord = class _ZodRecord extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const pairs = [];
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    for (const key in ctx.data) {
      pairs.push({
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
        value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (ctx.common.async) {
      return ParseStatus.mergeObjectAsync(status, pairs);
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get element() {
    return this._def.valueType;
  }
  static create(first, second, third) {
    if (second instanceof ZodType) {
      return new _ZodRecord({
        keyType: first,
        valueType: second,
        typeName: ZodFirstPartyTypeKind.ZodRecord,
        ...processCreateParams(third)
      });
    }
    return new _ZodRecord({
      keyType: ZodString.create(),
      valueType: first,
      typeName: ZodFirstPartyTypeKind.ZodRecord,
      ...processCreateParams(second)
    });
  }
};
var ZodMap = class extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.map) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.map,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    const pairs = [...ctx.data.entries()].map(([key, value], index) => {
      return {
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index, "key"])),
        value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index, "value"]))
      };
    });
    if (ctx.common.async) {
      const finalMap = /* @__PURE__ */ new Map();
      return Promise.resolve().then(async () => {
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          if (key.status === "aborted" || value.status === "aborted") {
            return INVALID;
          }
          if (key.status === "dirty" || value.status === "dirty") {
            status.dirty();
          }
          finalMap.set(key.value, value.value);
        }
        return { status: status.value, value: finalMap };
      });
    } else {
      const finalMap = /* @__PURE__ */ new Map();
      for (const pair of pairs) {
        const key = pair.key;
        const value = pair.value;
        if (key.status === "aborted" || value.status === "aborted") {
          return INVALID;
        }
        if (key.status === "dirty" || value.status === "dirty") {
          status.dirty();
        }
        finalMap.set(key.value, value.value);
      }
      return { status: status.value, value: finalMap };
    }
  }
};
ZodMap.create = (keyType, valueType, params) => {
  return new ZodMap({
    valueType,
    keyType,
    typeName: ZodFirstPartyTypeKind.ZodMap,
    ...processCreateParams(params)
  });
};
var ZodSet = class _ZodSet extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.set) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.set,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const def = this._def;
    if (def.minSize !== null) {
      if (ctx.data.size < def.minSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.minSize.message
        });
        status.dirty();
      }
    }
    if (def.maxSize !== null) {
      if (ctx.data.size > def.maxSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.maxSize.message
        });
        status.dirty();
      }
    }
    const valueType = this._def.valueType;
    function finalizeSet(elements2) {
      const parsedSet = /* @__PURE__ */ new Set();
      for (const element of elements2) {
        if (element.status === "aborted")
          return INVALID;
        if (element.status === "dirty")
          status.dirty();
        parsedSet.add(element.value);
      }
      return { status: status.value, value: parsedSet };
    }
    const elements = [...ctx.data.values()].map((item, i) => valueType._parse(new ParseInputLazyPath(ctx, item, ctx.path, i)));
    if (ctx.common.async) {
      return Promise.all(elements).then((elements2) => finalizeSet(elements2));
    } else {
      return finalizeSet(elements);
    }
  }
  min(minSize, message) {
    return new _ZodSet({
      ...this._def,
      minSize: { value: minSize, message: errorUtil.toString(message) }
    });
  }
  max(maxSize, message) {
    return new _ZodSet({
      ...this._def,
      maxSize: { value: maxSize, message: errorUtil.toString(message) }
    });
  }
  size(size, message) {
    return this.min(size, message).max(size, message);
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodSet.create = (valueType, params) => {
  return new ZodSet({
    valueType,
    minSize: null,
    maxSize: null,
    typeName: ZodFirstPartyTypeKind.ZodSet,
    ...processCreateParams(params)
  });
};
var ZodFunction = class _ZodFunction extends ZodType {
  constructor() {
    super(...arguments);
    this.validate = this.implement;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.function) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.function,
        received: ctx.parsedType
      });
      return INVALID;
    }
    function makeArgsIssue(args, error) {
      return makeIssue({
        data: args,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_arguments,
          argumentsError: error
        }
      });
    }
    function makeReturnsIssue(returns, error) {
      return makeIssue({
        data: returns,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_return_type,
          returnTypeError: error
        }
      });
    }
    const params = { errorMap: ctx.common.contextualErrorMap };
    const fn = ctx.data;
    if (this._def.returns instanceof ZodPromise) {
      const me = this;
      return OK(async function(...args) {
        const error = new ZodError([]);
        const parsedArgs = await me._def.args.parseAsync(args, params).catch((e) => {
          error.addIssue(makeArgsIssue(args, e));
          throw error;
        });
        const result = await Reflect.apply(fn, this, parsedArgs);
        const parsedReturns = await me._def.returns._def.type.parseAsync(result, params).catch((e) => {
          error.addIssue(makeReturnsIssue(result, e));
          throw error;
        });
        return parsedReturns;
      });
    } else {
      const me = this;
      return OK(function(...args) {
        const parsedArgs = me._def.args.safeParse(args, params);
        if (!parsedArgs.success) {
          throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
        }
        const result = Reflect.apply(fn, this, parsedArgs.data);
        const parsedReturns = me._def.returns.safeParse(result, params);
        if (!parsedReturns.success) {
          throw new ZodError([makeReturnsIssue(result, parsedReturns.error)]);
        }
        return parsedReturns.data;
      });
    }
  }
  parameters() {
    return this._def.args;
  }
  returnType() {
    return this._def.returns;
  }
  args(...items) {
    return new _ZodFunction({
      ...this._def,
      args: ZodTuple.create(items).rest(ZodUnknown.create())
    });
  }
  returns(returnType) {
    return new _ZodFunction({
      ...this._def,
      returns: returnType
    });
  }
  implement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  strictImplement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  static create(args, returns, params) {
    return new _ZodFunction({
      args: args ? args : ZodTuple.create([]).rest(ZodUnknown.create()),
      returns: returns || ZodUnknown.create(),
      typeName: ZodFirstPartyTypeKind.ZodFunction,
      ...processCreateParams(params)
    });
  }
};
var ZodLazy = class extends ZodType {
  get schema() {
    return this._def.getter();
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const lazySchema = this._def.getter();
    return lazySchema._parse({ data: ctx.data, path: ctx.path, parent: ctx });
  }
};
ZodLazy.create = (getter, params) => {
  return new ZodLazy({
    getter,
    typeName: ZodFirstPartyTypeKind.ZodLazy,
    ...processCreateParams(params)
  });
};
var ZodLiteral = class extends ZodType {
  _parse(input) {
    if (input.data !== this._def.value) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_literal,
        expected: this._def.value
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
  get value() {
    return this._def.value;
  }
};
ZodLiteral.create = (value, params) => {
  return new ZodLiteral({
    value,
    typeName: ZodFirstPartyTypeKind.ZodLiteral,
    ...processCreateParams(params)
  });
};
function createZodEnum(values, params) {
  return new ZodEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
var ZodEnum = class _ZodEnum extends ZodType {
  _parse(input) {
    if (typeof input.data !== "string") {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(this._def.values);
    }
    if (!this._cache.has(input.data)) {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get options() {
    return this._def.values;
  }
  get enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Values() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  extract(values, newDef = this._def) {
    return _ZodEnum.create(values, {
      ...this._def,
      ...newDef
    });
  }
  exclude(values, newDef = this._def) {
    return _ZodEnum.create(this.options.filter((opt) => !values.includes(opt)), {
      ...this._def,
      ...newDef
    });
  }
};
ZodEnum.create = createZodEnum;
var ZodNativeEnum = class extends ZodType {
  _parse(input) {
    const nativeEnumValues = util.getValidEnumValues(this._def.values);
    const ctx = this._getOrReturnCtx(input);
    if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(util.getValidEnumValues(this._def.values));
    }
    if (!this._cache.has(input.data)) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get enum() {
    return this._def.values;
  }
};
ZodNativeEnum.create = (values, params) => {
  return new ZodNativeEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
    ...processCreateParams(params)
  });
};
var ZodPromise = class extends ZodType {
  unwrap() {
    return this._def.type;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === false) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.promise,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
    return OK(promisified.then((data) => {
      return this._def.type.parseAsync(data, {
        path: ctx.path,
        errorMap: ctx.common.contextualErrorMap
      });
    }));
  }
};
ZodPromise.create = (schema, params) => {
  return new ZodPromise({
    type: schema,
    typeName: ZodFirstPartyTypeKind.ZodPromise,
    ...processCreateParams(params)
  });
};
var ZodEffects = class extends ZodType {
  innerType() {
    return this._def.schema;
  }
  sourceType() {
    return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const effect = this._def.effect || null;
    const checkCtx = {
      addIssue: (arg) => {
        addIssueToContext(ctx, arg);
        if (arg.fatal) {
          status.abort();
        } else {
          status.dirty();
        }
      },
      get path() {
        return ctx.path;
      }
    };
    checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx);
    if (effect.type === "preprocess") {
      const processed = effect.transform(ctx.data, checkCtx);
      if (ctx.common.async) {
        return Promise.resolve(processed).then(async (processed2) => {
          if (status.value === "aborted")
            return INVALID;
          const result = await this._def.schema._parseAsync({
            data: processed2,
            path: ctx.path,
            parent: ctx
          });
          if (result.status === "aborted")
            return INVALID;
          if (result.status === "dirty")
            return DIRTY(result.value);
          if (status.value === "dirty")
            return DIRTY(result.value);
          return result;
        });
      } else {
        if (status.value === "aborted")
          return INVALID;
        const result = this._def.schema._parseSync({
          data: processed,
          path: ctx.path,
          parent: ctx
        });
        if (result.status === "aborted")
          return INVALID;
        if (result.status === "dirty")
          return DIRTY(result.value);
        if (status.value === "dirty")
          return DIRTY(result.value);
        return result;
      }
    }
    if (effect.type === "refinement") {
      const executeRefinement = (acc) => {
        const result = effect.refinement(acc, checkCtx);
        if (ctx.common.async) {
          return Promise.resolve(result);
        }
        if (result instanceof Promise) {
          throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
        }
        return acc;
      };
      if (ctx.common.async === false) {
        const inner = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inner.status === "aborted")
          return INVALID;
        if (inner.status === "dirty")
          status.dirty();
        executeRefinement(inner.value);
        return { status: status.value, value: inner.value };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner) => {
          if (inner.status === "aborted")
            return INVALID;
          if (inner.status === "dirty")
            status.dirty();
          return executeRefinement(inner.value).then(() => {
            return { status: status.value, value: inner.value };
          });
        });
      }
    }
    if (effect.type === "transform") {
      if (ctx.common.async === false) {
        const base = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (!isValid(base))
          return INVALID;
        const result = effect.transform(base.value, checkCtx);
        if (result instanceof Promise) {
          throw new Error(`Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`);
        }
        return { status: status.value, value: result };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => {
          if (!isValid(base))
            return INVALID;
          return Promise.resolve(effect.transform(base.value, checkCtx)).then((result) => ({
            status: status.value,
            value: result
          }));
        });
      }
    }
    util.assertNever(effect);
  }
};
ZodEffects.create = (schema, effect, params) => {
  return new ZodEffects({
    schema,
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    effect,
    ...processCreateParams(params)
  });
};
ZodEffects.createWithPreprocess = (preprocess, schema, params) => {
  return new ZodEffects({
    schema,
    effect: { type: "preprocess", transform: preprocess },
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    ...processCreateParams(params)
  });
};
var ZodOptional = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.undefined) {
      return OK(void 0);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodOptional.create = (type, params) => {
  return new ZodOptional({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodOptional,
    ...processCreateParams(params)
  });
};
var ZodNullable = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.null) {
      return OK(null);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodNullable.create = (type, params) => {
  return new ZodNullable({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodNullable,
    ...processCreateParams(params)
  });
};
var ZodDefault = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    let data = ctx.data;
    if (ctx.parsedType === ZodParsedType.undefined) {
      data = this._def.defaultValue();
    }
    return this._def.innerType._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  removeDefault() {
    return this._def.innerType;
  }
};
ZodDefault.create = (type, params) => {
  return new ZodDefault({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodDefault,
    defaultValue: typeof params.default === "function" ? params.default : () => params.default,
    ...processCreateParams(params)
  });
};
var ZodCatch = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const newCtx = {
      ...ctx,
      common: {
        ...ctx.common,
        issues: []
      }
    };
    const result = this._def.innerType._parse({
      data: newCtx.data,
      path: newCtx.path,
      parent: {
        ...newCtx
      }
    });
    if (isAsync(result)) {
      return result.then((result2) => {
        return {
          status: "valid",
          value: result2.status === "valid" ? result2.value : this._def.catchValue({
            get error() {
              return new ZodError(newCtx.common.issues);
            },
            input: newCtx.data
          })
        };
      });
    } else {
      return {
        status: "valid",
        value: result.status === "valid" ? result.value : this._def.catchValue({
          get error() {
            return new ZodError(newCtx.common.issues);
          },
          input: newCtx.data
        })
      };
    }
  }
  removeCatch() {
    return this._def.innerType;
  }
};
ZodCatch.create = (type, params) => {
  return new ZodCatch({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodCatch,
    catchValue: typeof params.catch === "function" ? params.catch : () => params.catch,
    ...processCreateParams(params)
  });
};
var ZodNaN = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.nan) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.nan,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
};
ZodNaN.create = (params) => {
  return new ZodNaN({
    typeName: ZodFirstPartyTypeKind.ZodNaN,
    ...processCreateParams(params)
  });
};
var BRAND = /* @__PURE__ */ Symbol("zod_brand");
var ZodBranded = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const data = ctx.data;
    return this._def.type._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  unwrap() {
    return this._def.type;
  }
};
var ZodPipeline = class _ZodPipeline extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.common.async) {
      const handleAsync = async () => {
        const inResult = await this._def.in._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inResult.status === "aborted")
          return INVALID;
        if (inResult.status === "dirty") {
          status.dirty();
          return DIRTY(inResult.value);
        } else {
          return this._def.out._parseAsync({
            data: inResult.value,
            path: ctx.path,
            parent: ctx
          });
        }
      };
      return handleAsync();
    } else {
      const inResult = this._def.in._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
      if (inResult.status === "aborted")
        return INVALID;
      if (inResult.status === "dirty") {
        status.dirty();
        return {
          status: "dirty",
          value: inResult.value
        };
      } else {
        return this._def.out._parseSync({
          data: inResult.value,
          path: ctx.path,
          parent: ctx
        });
      }
    }
  }
  static create(a, b) {
    return new _ZodPipeline({
      in: a,
      out: b,
      typeName: ZodFirstPartyTypeKind.ZodPipeline
    });
  }
};
var ZodReadonly = class extends ZodType {
  _parse(input) {
    const result = this._def.innerType._parse(input);
    const freeze = (data) => {
      if (isValid(data)) {
        data.value = Object.freeze(data.value);
      }
      return data;
    };
    return isAsync(result) ? result.then((data) => freeze(data)) : freeze(result);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodReadonly.create = (type, params) => {
  return new ZodReadonly({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodReadonly,
    ...processCreateParams(params)
  });
};
function cleanParams(params, data) {
  const p = typeof params === "function" ? params(data) : typeof params === "string" ? { message: params } : params;
  const p2 = typeof p === "string" ? { message: p } : p;
  return p2;
}
function custom(check, _params = {}, fatal) {
  if (check)
    return ZodAny.create().superRefine((data, ctx) => {
      const r = check(data);
      if (r instanceof Promise) {
        return r.then((r2) => {
          if (!r2) {
            const params = cleanParams(_params, data);
            const _fatal = params.fatal ?? fatal ?? true;
            ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
          }
        });
      }
      if (!r) {
        const params = cleanParams(_params, data);
        const _fatal = params.fatal ?? fatal ?? true;
        ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
      }
      return;
    });
  return ZodAny.create();
}
var late = {
  object: ZodObject.lazycreate
};
var ZodFirstPartyTypeKind;
(function(ZodFirstPartyTypeKind2) {
  ZodFirstPartyTypeKind2["ZodString"] = "ZodString";
  ZodFirstPartyTypeKind2["ZodNumber"] = "ZodNumber";
  ZodFirstPartyTypeKind2["ZodNaN"] = "ZodNaN";
  ZodFirstPartyTypeKind2["ZodBigInt"] = "ZodBigInt";
  ZodFirstPartyTypeKind2["ZodBoolean"] = "ZodBoolean";
  ZodFirstPartyTypeKind2["ZodDate"] = "ZodDate";
  ZodFirstPartyTypeKind2["ZodSymbol"] = "ZodSymbol";
  ZodFirstPartyTypeKind2["ZodUndefined"] = "ZodUndefined";
  ZodFirstPartyTypeKind2["ZodNull"] = "ZodNull";
  ZodFirstPartyTypeKind2["ZodAny"] = "ZodAny";
  ZodFirstPartyTypeKind2["ZodUnknown"] = "ZodUnknown";
  ZodFirstPartyTypeKind2["ZodNever"] = "ZodNever";
  ZodFirstPartyTypeKind2["ZodVoid"] = "ZodVoid";
  ZodFirstPartyTypeKind2["ZodArray"] = "ZodArray";
  ZodFirstPartyTypeKind2["ZodObject"] = "ZodObject";
  ZodFirstPartyTypeKind2["ZodUnion"] = "ZodUnion";
  ZodFirstPartyTypeKind2["ZodDiscriminatedUnion"] = "ZodDiscriminatedUnion";
  ZodFirstPartyTypeKind2["ZodIntersection"] = "ZodIntersection";
  ZodFirstPartyTypeKind2["ZodTuple"] = "ZodTuple";
  ZodFirstPartyTypeKind2["ZodRecord"] = "ZodRecord";
  ZodFirstPartyTypeKind2["ZodMap"] = "ZodMap";
  ZodFirstPartyTypeKind2["ZodSet"] = "ZodSet";
  ZodFirstPartyTypeKind2["ZodFunction"] = "ZodFunction";
  ZodFirstPartyTypeKind2["ZodLazy"] = "ZodLazy";
  ZodFirstPartyTypeKind2["ZodLiteral"] = "ZodLiteral";
  ZodFirstPartyTypeKind2["ZodEnum"] = "ZodEnum";
  ZodFirstPartyTypeKind2["ZodEffects"] = "ZodEffects";
  ZodFirstPartyTypeKind2["ZodNativeEnum"] = "ZodNativeEnum";
  ZodFirstPartyTypeKind2["ZodOptional"] = "ZodOptional";
  ZodFirstPartyTypeKind2["ZodNullable"] = "ZodNullable";
  ZodFirstPartyTypeKind2["ZodDefault"] = "ZodDefault";
  ZodFirstPartyTypeKind2["ZodCatch"] = "ZodCatch";
  ZodFirstPartyTypeKind2["ZodPromise"] = "ZodPromise";
  ZodFirstPartyTypeKind2["ZodBranded"] = "ZodBranded";
  ZodFirstPartyTypeKind2["ZodPipeline"] = "ZodPipeline";
  ZodFirstPartyTypeKind2["ZodReadonly"] = "ZodReadonly";
})(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
var instanceOfType = (cls, params = {
  message: `Input not instance of ${cls.name}`
}) => custom((data) => data instanceof cls, params);
var stringType = ZodString.create;
var numberType = ZodNumber.create;
var nanType = ZodNaN.create;
var bigIntType = ZodBigInt.create;
var booleanType = ZodBoolean.create;
var dateType = ZodDate.create;
var symbolType = ZodSymbol.create;
var undefinedType = ZodUndefined.create;
var nullType = ZodNull.create;
var anyType = ZodAny.create;
var unknownType = ZodUnknown.create;
var neverType = ZodNever.create;
var voidType = ZodVoid.create;
var arrayType = ZodArray.create;
var objectType = ZodObject.create;
var strictObjectType = ZodObject.strictCreate;
var unionType = ZodUnion.create;
var discriminatedUnionType = ZodDiscriminatedUnion.create;
var intersectionType = ZodIntersection.create;
var tupleType = ZodTuple.create;
var recordType = ZodRecord.create;
var mapType = ZodMap.create;
var setType = ZodSet.create;
var functionType = ZodFunction.create;
var lazyType = ZodLazy.create;
var literalType = ZodLiteral.create;
var enumType = ZodEnum.create;
var nativeEnumType = ZodNativeEnum.create;
var promiseType = ZodPromise.create;
var effectsType = ZodEffects.create;
var optionalType = ZodOptional.create;
var nullableType = ZodNullable.create;
var preprocessType = ZodEffects.createWithPreprocess;
var pipelineType = ZodPipeline.create;
var ostring = () => stringType().optional();
var onumber = () => numberType().optional();
var oboolean = () => booleanType().optional();
var coerce = {
  string: ((arg) => ZodString.create({ ...arg, coerce: true })),
  number: ((arg) => ZodNumber.create({ ...arg, coerce: true })),
  boolean: ((arg) => ZodBoolean.create({
    ...arg,
    coerce: true
  })),
  bigint: ((arg) => ZodBigInt.create({ ...arg, coerce: true })),
  date: ((arg) => ZodDate.create({ ...arg, coerce: true }))
};
var NEVER = INVALID;

// ../compose/dist/money.js
var c = (d3) => BigInt(Math.round((d3 ?? 0) * 100));
var rd2 = (x) => {
  const neg = x < 0n;
  const a = neg ? -x : x;
  const r = (a + 50n) / 100n * 100n;
  return neg ? -r : r;
};
var max04 = (x) => x > 0n ? x : 0n;
var min2 = (a, b) => a < b ? a : b;
var fmtD = (x) => {
  const neg = x < 0n;
  const a = neg ? -x : x;
  return `${neg ? "-" : ""}$${(a / 100n).toLocaleString()}`;
};

// ../compose/dist/types.js
var isJoint = (input) => input.filingJoint === true;
var isHoh = (input) => input.filingHoh === true;
var isHohOrQss = (input) => input.filingHohOrQss === true;
var isMfs3 = (input) => input.filingStatus === "mfs";

// ../compose/dist/ca.js
var STD_DEDUCTION_SINGLE = 570600n;
var STD_DEDUCTION_JOINT_HOH = 1141200n;
var PERSONAL_EXEMPTION_CREDIT = 15300n;
var DEPENDENT_EXEMPTION_CREDIT = 47500n;
function scheduleCaAdjustments(input, notes) {
  const ssSub = rd2(c(input.taxableSocialSecurity));
  const uiSub = rd2(c(input.unemploymentCompensation));
  const hsaDistSub = rd2(c(input.caHsaTaxableDistribution));
  const educatorAdd = rd2(c(input.caEducatorExpensesDeducted));
  const hsaDedAdd = rd2(c(input.caHsaDeduction));
  const ab5WageAdd = rd2(c(input.caAb5GrossIncomeAddition));
  const ab5LossAdd = rd2(c(input.caAb5NetLossAddition));
  const deprAdd = rd2(c(input.caDepreciationAddition));
  if (ssSub > 0n)
    notes.push(`CA subtraction: federally taxable social security ${fmtD(ssSub)} (Schedule CA line 6 col B)`);
  if (uiSub > 0n)
    notes.push(`CA subtraction: unemployment compensation ${fmtD(uiSub)} (Schedule CA line 7 col B)`);
  if (hsaDistSub > 0n)
    notes.push(`CA subtraction: federally taxed HSA distribution ${fmtD(hsaDistSub)} (no \xA7 223 conformity)`);
  if (educatorAdd > 0n)
    notes.push(`CA addition: federal educator-expense deduction ${fmtD(educatorAdd)} added back (no \xA7 62(a)(2)(D) conformity)`);
  if (hsaDedAdd > 0n)
    notes.push(`CA addition: federal HSA deduction ${fmtD(hsaDedAdd)} added back (no \xA7 223 conformity)`);
  if (ab5WageAdd > 0n)
    notes.push(`CA wage addition ${fmtD(ab5WageAdd)}: business gross income reclassified as employee wages for California (AB 5)`);
  if (ab5LossAdd > 0n)
    notes.push(`CA business addition ${fmtD(ab5LossAdd)}: federal Schedule C net loss disallowed for California (AB 5 employee classification)`);
  if (deprAdd > 0n)
    notes.push(`CA addition ${fmtD(deprAdd)}: depreciation difference (California does not conform to \xA7 168(k) bonus depreciation)`);
  return {
    adds: educatorAdd + hsaDedAdd + ab5WageAdd + ab5LossAdd + deprAdd,
    subs: ssSub + uiSub + hsaDistSub
  };
}
function composeCA(input, evalStateTax, notes) {
  const joint = isJoint(input);
  const l13 = rd2(c(input.federalAGI));
  const { adds, subs } = scheduleCaAdjustments(input, notes);
  const l17 = rd2(l13 + c(input.additions) + adds - c(input.subtractions) - subs);
  const standard = joint || isHohOrQss(input) ? STD_DEDUCTION_JOINT_HOH : STD_DEDUCTION_SINGLE;
  const itemized = rd2(c(input.caItemizedDeductions));
  const l18 = itemized > standard ? itemized : standard;
  if (itemized > standard)
    notes.push(`CA itemized deduction ${fmtD(itemized)} exceeds the standard deduction ${fmtD(standard)} \u2014 line 18 itemizes (Schedule CA Part II, agent-computed and disclosed)`);
  const l19 = max04(l17 - l18);
  const l31 = rd2(evalStateTax("us.ca.income_tax", l19));
  const persons = joint || input.filingStatus === "qss" ? 2n : 1n;
  const l32 = rd2(persons * PERSONAL_EXEMPTION_CREDIT + BigInt(input.dependents ?? 0) * DEPENDENT_EXEMPTION_CREDIT + BigInt(input.ageOrBlindBoxes ?? 0) * PERSONAL_EXEMPTION_CREDIT);
  const renters = rd2(c(input.caRentersCredit));
  const l48 = max04(l31 - l32 - renters);
  if (l31 < l32 + renters && l31 > 0n)
    notes.push("CA exemption credits and/or renter's credit exceed tax \u2014 line 48 floors at $0 (both are nonrefundable)");
  let amt = rd2(c(input.caAmt));
  if (input.caAmt === void 0 && (input.caIsoPreference !== void 0 || input.caAmtTaxesAddback !== void 0)) {
    const addback = itemized > standard ? rd2(c(input.caAmtTaxesAddback)) : standard;
    const amti = l19 + addback + rd2(c(input.caIsoPreference));
    amt = rd2(evalStateTax("us.ca.amt", l19, { caAmti: amti, caRegularTax: l31 }));
    notes.push(`Schedule P: AMTI ${fmtD(amti)} = taxable income ${fmtD(l19)} + ${itemized > standard ? "itemized-taxes addback" : "standard-deduction addback"} ${fmtD(addback)} + ISO preference ${fmtD(rd2(c(input.caIsoPreference)))}; AMT ${fmtD(amt)}`);
  }
  const bhst = rd2(c(input.caBhst));
  const earlyBase = rd2(c(input.caTaxableEarlyDistribution));
  const l63 = rd2(earlyBase * 25n / 1000n);
  if (l63 > 0n)
    notes.push(`CA line 63: 2.5% early-distribution additional tax ${fmtD(l63)} on ${fmtD(earlyBase)} (R&TC \xA7 17085(c)(1))`);
  const l64 = l48 + amt + bhst + l63;
  const l71 = c(input.stateWithholding);
  const l72 = c(input.estimatedPayments) + rd2(c(input.extensionPayment)) + rd2(c(input.priorYearOverpaymentCredited));
  const l75 = rd2(c(input.caCalEITC));
  const l76 = rd2(c(input.caYCTC));
  const l78 = l71 + l72 + l75 + l76 + rd2(c(input.refundableCredits));
  const due = l64 + c(input.useTax);
  const l97 = max04(l78 - due);
  const l111 = max04(due - l78);
  return {
    "13_federal_agi": fmtD(l13),
    "17_ca_agi": fmtD(l17),
    "18_ca_deduction": fmtD(l18),
    "19_ca_taxable_income": fmtD(l19),
    "31_tax": fmtD(l31),
    "32_exemption_credits": fmtD(l32),
    "46_renters_credit": fmtD(renters),
    "48_tax_after_credits": fmtD(l48),
    ...amt !== 0n ? { "61_amt": fmtD(amt) } : {},
    ...bhst !== 0n ? { "62_bhst": fmtD(bhst) } : {},
    ...l63 !== 0n ? { "63_other_taxes": fmtD(l63) } : {},
    "64_total_tax": fmtD(l64),
    "71_ca_withholding": fmtD(l71),
    "72_estimated_payments": fmtD(l72),
    "75_caleitc": fmtD(l75),
    "76_yctc": fmtD(l76),
    "78_total_payments": fmtD(l78),
    "97_overpaid": fmtD(l97),
    "111_amount_owed": fmtD(l111),
    "115_refund": fmtD(l97)
  };
}

// ../compose/dist/il.js
var EXEMPTION = 285000n;
var AGE_BLIND_BOX = 100000n;
function composeIL(input, evalStateTax, notes) {
  const fagi = c(input.federalAGI);
  const fedEITC = c(input.federalEITC);
  const wh = c(input.stateWithholding);
  const est = c(input.estimatedPayments);
  const l1 = rd2(fagi);
  const l4 = rd2(l1 + c(input.additions));
  const l9 = max04(rd2(l4 - c(input.subtractions)));
  const nExemptions = input.exemptions ?? 1;
  let l10 = rd2(BigInt(nExemptions) * EXEMPTION + BigInt(input.ageOrBlindBoxes ?? 0) * AGE_BLIND_BOX);
  if (input.claimedAsDependent === true && l9 > EXEMPTION) {
    l10 = 0n;
    notes.push("IL exemption allowance $0: taxpayer is claimable as a dependent on another return and base income exceeds $2,850");
  }
  const l11 = max04(l9 - l10);
  const l12 = rd2(evalStateTax("us.il.income_tax", l11));
  const l14 = l12;
  const l18 = ilNonrefundableCredits(input, l14, notes);
  const l21 = rd2(c(input.useTax));
  const l23 = l14 - l18 + l21;
  const l29 = input.ilEitcOverride !== void 0 ? rd2(c(input.ilEitcOverride)) : rd2(fedEITC * 20n / 100n);
  const l30 = input.ilChildUnder12 ? rd2(l29 * 40n / 100n) : 0n;
  if (!input.ilChildUnder12 && l29 > 0n)
    notes.push("IL CTC $0: no dependent child under 12 indicated");
  const l31 = wh + est + l29 + l30;
  const l32 = max04(l31 - l23);
  const l41 = max04(l23 - l31);
  return {
    "1_federal_agi": fmtD(l1),
    "4_total_income": fmtD(l4),
    "9_base_income": fmtD(l9),
    "10_exemption_allowance": fmtD(l10),
    "11_net_income": fmtD(l11),
    "12_tax": fmtD(l12),
    "14_income_tax": fmtD(l14),
    "18_nonrefundable_credits": fmtD(l18),
    "21_use_tax": fmtD(l21),
    "23_total_tax": fmtD(l23),
    "29_il_eitc": fmtD(l29),
    "30_il_ctc": fmtD(l30),
    "31_payments_refundable": fmtD(l31),
    "32_overpayment": fmtD(l32),
    "38_refund": fmtD(l32),
    "41_amount_owed": fmtD(l41)
  };
}
function ilNonrefundableCredits(input, taxDue, notes) {
  const propertyTax = rd2(c(input.ilPropertyTaxPaid) * 5n / 100n);
  const k12 = (() => {
    const over = max04(c(input.ilK12Expenses) - 25000n);
    const credit = rd2(over * 25n / 100n);
    return credit > 75000n ? 75000n : credit;
  })();
  const teacher = (() => {
    const t = rd2(c(input.ilTeacherExpenses));
    const cap = isJoint(input) ? 100000n : 50000n;
    return t > cap ? cap : t;
  })();
  const available = propertyTax + k12 + teacher + rd2(c(input.nonrefundableCredits));
  const allowed = available > taxDue ? taxDue : available;
  if (available > allowed)
    notes.push(`IL credits available ${fmtD(available)} capped at tax due \u2014 line 18 reports the ALLOWED amount`);
  return allowed;
}

// ../compose/dist/ny.js
var STD_DEDUCTION_JOINT = 1605000n;
var STD_DEDUCTION_HOH = 1120000n;
var STD_DEDUCTION_SINGLE2 = 800000n;
var DEPENDENT_EXEMPTION = 100000n;
function composeNY(input, evalStateTax, notes) {
  const l1 = rd2(c(input.wages));
  const l19 = rd2(c(input.federalAGI));
  const l24 = rd2(l19 + c(input.additions));
  const ssSub = rd2(c(input.taxableSocialSecurity));
  if (ssSub > 0n)
    notes.push(`NY subtraction: federally taxable social security ${fmtD(ssSub)} (IT-201 line 27)`);
  const l32 = rd2(c(input.subtractions)) + ssSub;
  const l33 = l24 - l32;
  const l34 = isJoint(input) ? STD_DEDUCTION_JOINT : isHoh(input) ? STD_DEDUCTION_HOH : STD_DEDUCTION_SINGLE2;
  const l37 = max04(l33 - l34 - BigInt(input.dependents ?? 0) * DEPENDENT_EXEMPTION);
  const l39 = rd2(evalStateTax("us.ny.income_tax", l37, { useFormulaMethod: true }));
  const l43 = rd2(c(input.nyHouseholdCredit));
  const l44 = max04(l39 - l43);
  let nycNet = 0n;
  if (input.nycTaxableIncome !== void 0 && c(input.nycTaxableIncome) > 0n) {
    const nycTax = rd2(evalStateTax("us.ny.nyc_income_tax", rd2(c(input.nycTaxableIncome)), { useFormulaMethod: true }));
    nycNet = max04(nycTax - rd2(c(input.nycHouseholdCredit)));
  }
  const yonkers = rd2(c(input.yonkersSurcharge));
  const l62 = l44 + nycNet + yonkers + c(input.useTax);
  const l72 = c(input.stateWithholding);
  const l73 = c(input.cityWithholding);
  const l74 = c(input.yonkersWithholding);
  const l75 = c(input.estimatedPayments) + rd2(c(input.extensionPayment));
  const l76 = l72 + l73 + l74 + l75 + rd2(c(input.refundableCredits));
  const l77 = max04(l76 - l62);
  const owed = max04(l62 - l76);
  if (owed > 0n)
    notes.push(`balance due ${fmtD(owed)} (IT-201 line 80)`);
  return {
    "1_wages": fmtD(l1),
    "19_federal_agi": fmtD(l19),
    "24_fagi_plus_additions": fmtD(l24),
    "32_subtractions": fmtD(l32),
    "33_ny_agi": fmtD(l33),
    "34_deduction": fmtD(l34),
    "37_taxable_income": fmtD(l37),
    "39_nys_tax": fmtD(l39),
    "43_nonrefundable_credits": fmtD(l43),
    "44_nys_tax_after_credits": fmtD(l44),
    ...yonkers !== 0n ? { "55_yonkers_surcharge": fmtD(yonkers) } : {},
    "62_total_tax": fmtD(l62),
    "72_nys_withholding": fmtD(l72),
    "73_nyc_withholding": fmtD(l73),
    "74_yonkers_withholding": fmtD(l74),
    "75_estimated_payments": fmtD(l75),
    "76_total_payments": fmtD(l76),
    "77_overpaid": fmtD(l77),
    "78_refund": fmtD(l77)
  };
}

// ../compose/dist/pa.js
var classLine = (p, s) => p > 0n || s > 0n ? max04(p) + max04(s) : p + s;
function composePA(input, evalStateTax, notes) {
  const box16 = input.paGrossCompensation !== void 0 ? c(input.paGrossCompensation) : c(input.wages);
  if (input.paGrossCompensation === void 0 && input.wages !== void 0) {
    notes.push("PA line 1a: W-2 Box 16 total not transcribed \u2014 federal Box 1 wages used (401(k)/elective deferrals are PA-taxable; pass paGrossCompensation when Box 16 differs)");
  }
  const l1a = rd2(box16);
  const l1b = rd2(c(input.paUnreimbursedExpenses));
  const l1c = max04(l1a - l1b);
  const l2 = rd2(c(input.paInterest));
  const l3 = rd2(c(input.paDividends));
  const bizP = rd2(c(input.paBusinessNet));
  const bizS = rd2(c(input.paSpouseBusinessNet));
  const propP = rd2(c(input.paPropertyNet));
  const propS = rd2(c(input.paSpousePropertyNet));
  const rentP = rd2(c(input.paRentRoyaltyNet));
  const rentS = rd2(c(input.paSpouseRentRoyaltyNet));
  const l4 = classLine(bizP, bizS);
  const l5 = classLine(propP, propS);
  const l6 = classLine(rentP, rentS);
  const l7 = rd2(c(input.paEstateTrust));
  const l8 = rd2(c(input.paGambling));
  const extra = {
    wages: box16,
    paUnreimbursedBusinessExpenses: c(input.paUnreimbursedExpenses),
    paInterestIncome: c(input.paInterest),
    paDividendIncome: c(input.paDividends),
    paBusinessNetIncome: c(input.paBusinessNet),
    paSpouseBusinessNetIncome: c(input.paSpouseBusinessNet),
    paPropertyGainNet: c(input.paPropertyNet),
    paSpousePropertyGainNet: c(input.paSpousePropertyNet),
    paRentRoyaltyNet: c(input.paRentRoyaltyNet),
    paSpouseRentRoyaltyNet: c(input.paSpouseRentRoyaltyNet),
    paEstateTrustIncome: c(input.paEstateTrust),
    paGamblingWinnings: c(input.paGambling),
    studentLoanInterest: c(input.paStudentLoanInterest),
    pa529Contributions: c(input.pa529Contributions),
    paAbleContributions: c(input.paAbleContributions),
    paMsaHsaContributions: c(input.paMsaHsaContributions)
  };
  const l9 = rd2(evalStateTax("us.pa.taxable_income", 0n, extra));
  const l10 = rd2(evalStateTax("us.pa.other_deductions", 0n, extra));
  const l11 = max04(l9 - l10);
  const l12 = rd2(evalStateTax("us.pa.income_tax", 0n, extra));
  const l13 = rd2(c(input.stateWithholding));
  const l14 = rd2(c(input.priorYearOverpaymentCredited));
  const l15 = rd2(c(input.estimatedPayments));
  const l16 = rd2(c(input.extensionPayment));
  const l17 = rd2(c(input.paNrk1Withholding));
  const l18 = l14 + l15 + l16 + l17;
  const spDeps = input.paSpDependentChildren ?? 0;
  const l21 = rd2(evalStateTax("us.pa.tax_forgiveness", 0n, {
    ...extra,
    paSpDependentChildren: spDeps,
    paEligibilityIncomeAddbacks: c(input.paEligibilityAddbacks),
    paResidentCredit: c(input.paResidentCredit)
  }));
  const l20 = l9 + rd2(c(input.paEligibilityAddbacks));
  const l22 = rd2(c(input.paResidentCredit));
  const l23 = rd2(c(input.paScheduleDcCredit)) + rd2(c(input.paScheduleOcCredits));
  const l24 = l13 + l18 + l21 + l22 + l23;
  const l25 = rd2(c(input.useTax));
  const l26 = max04(l12 + l25 - l24);
  const l27 = rd2(c(input.paPenaltiesInterest));
  const l28 = l26 + l27;
  const l29 = max04(l24 - l12 - l25 - l27);
  const fedEITC = c(input.federalEITC);
  if (fedEITC > 0n) {
    const wptc = min2(rd2(fedEITC * 10n / 100n), 80500n);
    notes.push(`PA Working Pennsylvanians Tax Credit ${fmtD(wptc)} (10% of federal EITC, max $805, refundable) \u2014 NO line on the printed 2025 PA-40; DOR applies it in processing from the attached federal return`);
  }
  notes.push("PA local earned income tax (Act 32) is filed separately with the local collector \u2014 out of scope for the PA-40");
  if (l29 > 0n && l29 <= 100n)
    notes.push("PA does not issue refunds of $1.00 or less");
  if (l28 > 0n && l28 <= 100n)
    notes.push("PA does not require payment of $1.00 or less");
  return {
    "1a_gross_compensation": fmtD(l1a),
    "1b_unreimbursed_expenses": fmtD(l1b),
    "1c_net_compensation": fmtD(l1c),
    "2_interest": fmtD(l2),
    "3_dividends": fmtD(l3),
    "4_business_net": fmtD(l4),
    "5_property_net": fmtD(l5),
    "6_rent_royalty_net": fmtD(l6),
    "7_estate_trust": fmtD(l7),
    "8_gambling": fmtD(l8),
    "9_total_taxable_income": fmtD(l9),
    "10_other_deductions": fmtD(l10),
    "11_adjusted_taxable_income": fmtD(l11),
    "12_tax": fmtD(l12),
    "13_withholding": fmtD(l13),
    "14_prior_year_credit": fmtD(l14),
    "15_estimated_payments": fmtD(l15),
    "16_extension_payment": fmtD(l16),
    "17_nonresident_withholding": fmtD(l17),
    "18_total_estimated_and_credits": fmtD(l18),
    "19b_sp_dependents": String(spDeps),
    "20_eligibility_income": fmtD(l20),
    "21_tax_forgiveness": fmtD(l21),
    "22_resident_credit": fmtD(l22),
    "23_other_credits": fmtD(l23),
    "24_total_payments_credits": fmtD(l24),
    "25_use_tax": fmtD(l25),
    "26_tax_due": fmtD(l26),
    "27_penalties_interest": fmtD(l27),
    "28_total_due": fmtD(l28),
    "29_overpayment": fmtD(l29),
    "30_refund": fmtD(l29)
  };
}

// ../compose/dist/va-worksheets.js
var VA_STD_DEDUCTION_JOINT = 1750000n;
var VA_STD_DEDUCTION_OTHER = 875000n;
var VA_PERSONAL_EXEMPTION = 93000n;
var VA_AGE_BLIND_EXEMPTION = 80000n;
var AGE_DEDUCTION_EACH = 1200000n;
var STA_CAP = 25900n;
function vaAgeDeduction(args, notes) {
  const full = BigInt(args.fullCount) * AGE_DEDUCTION_EACH;
  const testedMax = BigInt(args.testedCount) * AGE_DEDUCTION_EACH;
  const afagi = args.fagi - args.taxableSS;
  const threshold2 = args.joint ? 7500000n : 5000000n;
  const reduction = max04(afagi - threshold2);
  const deduction = rd2(full + max04(testedMax - reduction));
  if (deduction > 0n || args.testedCount > 0) {
    notes.push(`VA age deduction ${fmtD(deduction)} (AFAGI ${fmtD(afagi)} = FAGI minus taxable social security; reduction ${fmtD(min2(reduction, testedMax))} against ${fmtD(testedMax)} income-tested)`);
  }
  return deduction;
}
function vaScheduleA(a, notes) {
  const med = max04(a.medical - rd2(a.fagi * 10n / 100n));
  const vaSaltCap = a.mfs ? 2000000n : 4000000n;
  const salesElected = a.salesTaxes > 0n;
  const line5a = salesElected ? min2(a.salesTaxes, vaSaltCap) : a.incomeTaxes;
  const taxes = line5a + a.realEstateTaxes + a.personalPropertyTaxes + a.otherTaxes;
  const protectedDed = med + a.investmentInterest + a.casualty + a.gambling;
  const total = med + taxes + a.mortgageInterest + a.investmentInterest + a.charitable + a.casualty + a.gambling + a.other;
  const peaseThreshold = a.joint ? 39920000n : a.mfs ? 19960000n : a.hoh ? 36595000n : 33270000n;
  let limited = total;
  if (a.fagi > peaseThreshold && total > protectedDed) {
    const threePct = rd2((a.fagi - peaseThreshold) * 3n / 100n);
    const eightyPct = rd2((total - protectedDed) * 80n / 100n);
    const reduction = min2(threePct, eightyPct);
    limited = total - reduction;
    notes.push(`VA overall itemized limitation: ${fmtD(total)} reduced by ${fmtD(reduction)} (lesser of 3% of FAGI over ${fmtD(peaseThreshold)} or 80% of non-protected deductions) = ${fmtD(limited)}`);
  }
  let line18Reduction = 0n;
  if (!salesElected && line5a > 0n) {
    const capped5a = min2(line5a, vaSaltCap);
    if (limited < total) {
      line18Reduction = rd2(capped5a * limited / total);
      notes.push("VA Sch A line 18 (income-tax reduction) prorated per the Limited Itemized Deduction Worksheet Part B \u2014 verify against the printed worksheet if this return is limited");
    } else {
      line18Reduction = capped5a;
    }
  }
  return max04(limited - line18Reduction);
}
function vaSpouseTaxAdjustment(w, evalStateTax, notes) {
  const exFor = (boxes) => BigInt(boxes) * VA_AGE_BLIND_EXEMPTION + VA_PERSONAL_EXEMPTION;
  const you3 = w.yourVagi - exFor(w.yourAgeBlindBoxes);
  const sp3 = w.spouseVagi - exFor(w.spouseAgeBlindBoxes);
  if (you3 <= 0n || sp3 <= 0n) {
    notes.push("STA $0: one spouse's income net of personal exemptions is zero or less (worksheet Part 1 line 3 stop)");
    return 0n;
  }
  const line4 = w.taxableIncome;
  const line5 = min2(you3, sp3);
  if (line5 > 1700000n && line4 > 3400000n) {
    notes.push("STA = $259 (worksheet line 5 shortcut: smaller income > $17,000 and taxable income > $34,000)");
    return STA_CAP;
  }
  const line6 = max04(line4 - line5);
  const line7 = line4 / 2n;
  const t8 = rd2(evalStateTax("us.va.income_tax", min2(line5, line7)));
  const t9 = rd2(evalStateTax("us.va.income_tax", line6 > line7 ? line6 : line7));
  const sta = w.jointTax - (t8 + t9);
  const capped = sta < 0n ? 0n : sta > STA_CAP ? STA_CAP : sta;
  notes.push(`STA worksheet: joint tax ${fmtD(w.jointTax)} \u2212 split taxes ${fmtD(t8)}+${fmtD(t9)} = ${fmtD(capped)} (cap $259)`);
  return capped;
}
function vaLine23Credit(a, notes) {
  if (a.barred) {
    if (a.fedEITC > 0n)
      notes.push("VA line 23 credit $0: the age deduction / 65+ or blind exemptions claimed on this return bar the Credit for Low-Income Individuals and Virginia EITC (2025 Form 760 instructions)");
    return 0n;
  }
  if (a.fedEITC <= 0n && a.familyVagi === void 0)
    return 0n;
  const refundable = rd2(a.fedEITC * 20n / 100n);
  const familyVagi = a.familyVagi ?? a.vagi;
  const povertyLine2 = 1565000n + BigInt(Math.max(0, a.exemptions - 1)) * 550000n;
  const cli = familyVagi <= povertyLine2 ? BigInt(a.exemptions) * 30000n : 0n;
  const cliUsable = min2(cli, a.netTax);
  if (refundable >= cliUsable) {
    if (refundable > 0n)
      notes.push(`VA line 23 = refundable Virginia EITC ${fmtD(refundable)} (20% of federal EIC ${fmtD(a.fedEITC)}, TY2025 \u2014 refundable, not capped at tax)`);
    return refundable;
  }
  notes.push(`VA line 23 = Credit for Low-Income Individuals ${fmtD(cliUsable)} (nonrefundable, capped at net tax)`);
  return cliUsable;
}

// ../compose/dist/va.js
function composeVA(input, evalStateTax, notes) {
  const joint = isJoint(input);
  const fagi = c(input.federalAGI);
  const l1 = rd2(fagi);
  const ssSub = rd2(c(input.taxableSocialSecurity));
  const uiSub = rd2(c(input.unemploymentCompensation));
  let age = rd2(c(input.vaAgeDeduction));
  if (input.vaAgeDeduction === void 0 && (input.vaAgeQualifyingFull !== void 0 || input.vaAgeQualifyingTested !== void 0)) {
    age = vaAgeDeduction({
      fullCount: input.vaAgeQualifyingFull ?? 0,
      testedCount: input.vaAgeQualifyingTested ?? 0,
      fagi: l1,
      taxableSS: ssSub,
      joint
    }, notes);
  }
  if (ssSub > 0n)
    notes.push(`VA subtraction: federally taxable social security ${fmtD(ssSub)} (760 line 5)`);
  if (uiSub > 0n)
    notes.push(`VA subtraction: unemployment compensation ${fmtD(uiSub)} (Va. Code \xA7 58.1-322.02(9))`);
  const l9 = rd2(l1 + c(input.additions) - age - ssSub - uiSub - c(input.subtractions));
  const n = input.exemptions ?? (joint ? 2 : 1) + (input.dependents ?? 0);
  let l10 = 0n;
  let l11 = joint ? VA_STD_DEDUCTION_JOINT : VA_STD_DEDUCTION_OTHER;
  if (input.vaItemizing === true) {
    l10 = vaScheduleA({
      fagi,
      medical: rd2(c(input.vaItemizedMedical)),
      incomeTaxes: rd2(c(input.vaItemizedStateLocalIncomeTaxes)),
      salesTaxes: rd2(c(input.vaItemizedSalesTaxes)),
      realEstateTaxes: rd2(c(input.vaItemizedRealEstateTaxes)),
      personalPropertyTaxes: rd2(c(input.vaItemizedPersonalPropertyTaxes)),
      otherTaxes: rd2(c(input.vaItemizedOtherTaxes)),
      mortgageInterest: rd2(c(input.vaItemizedMortgageInterest)),
      investmentInterest: rd2(c(input.vaItemizedInvestmentInterest)),
      charitable: rd2(c(input.vaItemizedCharitable)),
      casualty: rd2(c(input.vaItemizedCasualty)),
      gambling: rd2(c(input.vaItemizedGambling)),
      other: rd2(c(input.vaItemizedOther)),
      joint,
      mfs: isMfs3(input),
      hoh: isHoh(input)
    }, notes);
    l11 = 0n;
  } else if (input.claimedAsDependent === true) {
    notes.push("VA standard deduction for a claimable-as-dependent filer is limited to earned income \u2014 pass vaItemizing/earned income facts if this binds");
  }
  const abBoxes = input.ageOrBlindBoxes ?? (input.vaYourAgeBlindBoxes ?? 0) + (input.vaSpouseAgeBlindBoxes ?? 0);
  const l12 = rd2(BigInt(n) * VA_PERSONAL_EXEMPTION + BigInt(abBoxes) * VA_AGE_BLIND_EXEMPTION);
  const l13 = rd2(c(input.vaScheduleAdjDeductions));
  const l14 = l10 + l11 + l12 + l13;
  const l15 = l9 - l14;
  const filingThreshold = joint ? 2390000n : 1195000n;
  let l16 = rd2(evalStateTax("us.va.income_tax", max04(l15)));
  if (l9 < filingThreshold && l16 > 0n) {
    notes.push(`VA tax $0: VAGI ${fmtD(l9)} is below the \xA7 58.1-321 filing threshold ${fmtD(filingThreshold)}`);
    l16 = 0n;
  }
  let l17 = rd2(c(input.vaSpouseTaxAdjustment));
  if (input.vaSpouseTaxAdjustment === void 0 && joint && input.vaYourVagi !== void 0 && input.vaSpouseVagi !== void 0 && l16 > 0n) {
    l17 = vaSpouseTaxAdjustment({
      yourVagi: rd2(c(input.vaYourVagi)),
      spouseVagi: rd2(c(input.vaSpouseVagi)),
      yourAgeBlindBoxes: input.vaYourAgeBlindBoxes ?? 0,
      spouseAgeBlindBoxes: input.vaSpouseAgeBlindBoxes ?? 0,
      taxableIncome: max04(l15),
      jointTax: l16
    }, evalStateTax, notes);
  }
  const l18 = max04(l16 - l17);
  const l19a = c(input.stateWithholding);
  const l19b = c(input.spouseStateWithholding);
  const l20 = c(input.estimatedPayments);
  const l21 = rd2(c(input.priorYearOverpaymentCredited));
  const l22 = rd2(c(input.extensionPayment));
  let l23;
  if (input.vaRefundableEitc !== void 0 || input.nonrefundableCredits !== void 0) {
    const avail = rd2(c(input.nonrefundableCredits));
    l23 = (avail > l18 ? l18 : avail) + rd2(c(input.vaRefundableEitc));
  } else {
    l23 = vaLine23Credit({
      fedEITC: rd2(c(input.federalEITC)),
      netTax: l18,
      vagi: l9,
      familyVagi: input.vaFamilyVagi !== void 0 ? rd2(c(input.vaFamilyVagi)) : void 0,
      exemptions: n,
      barred: age > 0n || (input.ageOrBlindBoxes ?? 0) > 0
    }, notes);
  }
  const l26 = l19a + l19b + l20 + l21 + l22 + l23 + rd2(c(input.refundableCredits));
  const l33 = c(input.useTax);
  const totalDue = l18 + l33;
  const l36 = max04(l26 - totalDue);
  const l35 = max04(totalDue - l26);
  return {
    "1_federal_agi": fmtD(l1),
    "9_vagi": fmtD(l9),
    ...l10 > 0n ? { "10_itemized_deductions": fmtD(l10) } : {},
    "11_standard_deduction": fmtD(l11),
    "12_exemptions": fmtD(l12),
    ...l13 > 0n ? { "13_sch_adj_deductions": fmtD(l13) } : {},
    "14_deductions_subtotal": fmtD(l14),
    "15_va_taxable_income": fmtD(l15),
    "16_tax": fmtD(l16),
    "17_spouse_tax_adjustment": fmtD(l17),
    "18_net_tax": fmtD(l18),
    "19a_your_withholding": fmtD(l19a),
    "19b_spouse_withholding": fmtD(l19b),
    "20_estimated_payments": fmtD(l20),
    "21_prior_year_overpayment_credited": fmtD(l21),
    "22_extension_payment": fmtD(l22),
    "23_low_income_or_eitc_credit": fmtD(l23),
    "26_total_payments_credits": fmtD(l26),
    "33_sales_use_tax": fmtD(l33),
    "35_amount_owed": fmtD(l35),
    "36_refund": fmtD(l36)
  };
}

// ../compose/dist/shape.js
var usd = external_exports.number().finite();
var shared = {
  jurisdiction: external_exports.enum(["il", "va", "ca", "ny", "pa"]),
  filingStatus: external_exports.enum(["single", "mfj", "mfs", "hoh", "qss"]).optional().describe("REQUIRED in practice: the federal filing status \u2014 drives the state bracket schedule, standard deduction column, and exemption structure. The filingJoint/filingHoh/filingHohOrQss booleans are legacy aliases; when filingStatus is present it wins."),
  // federal substrate values, computed by compute_return in the SAME session
  // (pass them verbatim — whole dollars)
  federalAGI: usd.optional().describe("federal Form 1040 line 11 (from compute_return, verbatim). REQUIRED for il/va/ca/ny \u2014 the composer refuses without it. NOT used by PA (class-based: pass the pa* class fields instead)."),
  federalEITC: usd.optional().describe("federal EIC, line 27a (from compute_return)"),
  wages: usd.optional().describe("federal line 1a wages (NY IT-201 line 1)"),
  additions: usd.optional().describe("total state additions to federal AGI (e.g. NY 414(h) A-104 + IRC-125 A-101; VA Schedule ADJ line 2 codes). GATE RULE: coded addition/subtraction line-item arrays sitting under a false 'do you have additions/subtractions' boolean are inactive template rows (especially $1-$4 placeholder amounts) \u2014 transcribe $0 for them and disclose; the gate controls for these arrays"),
  subtractions: usd.optional().describe("total state subtractions OTHER than the automatic ones (taxable social security / unemployment have their own inputs below; e.g. NY S-136 alimony paid, IL retirement subtraction)"),
  exemptions: external_exports.number().int().optional().describe("personal + dependent exemption COUNT (self + spouse + dependents)"),
  ageOrBlindBoxes: external_exports.number().int().optional().describe("count of age-65+/blind boxes checked (taxpayer/spouse, per box)"),
  dependents: external_exports.number().int().optional().describe("dependent count (CA dependent exemption credits; NY $1,000 exemptions)"),
  stateWithholding: usd.optional().describe("state income tax withheld (IL line 25 / VA 19a / CA 71 / NY 72). CONVENTIONS: IL line 25 sums state withholding from EVERY document (W-2s + all 1099s). NY line 72 = W-2 box 17 NYS withholding PLUS NY-coded state withholding from 1099s whose PAYER has an in-state (NY) address; NY-coded withholding printed by an OUT-OF-STATE-addressed payer is NOT included; disclose any excluded amount in notes. VA 19a = the PRIMARY taxpayer's withholding from EVERY document type (W-2, 1099, VK-1 \u2014 Form 760 line 19 instructions name all three; the payer's address does NOT matter for VA, unlike NY); a jointly-issued document's state withholding splits 50/50 between 19a/19b with the odd dollar to the primary."),
  spouseStateWithholding: usd.optional().describe("VA line 19b spouse withholding (spouse's own W-2/1099/VK-1 boxes + spouse's half of jointly-issued documents' withholding, odd dollar to the primary)"),
  cityWithholding: usd.optional().describe("NY line 73 NYC withholding"),
  yonkersWithholding: usd.optional().describe("NY line 74 Yonkers withholding (W-2 box 19 with a Yonkers locality)"),
  estimatedPayments: usd.optional().describe("state estimated payments ONLY (extension payments and prior-year credited overpayments have their own lines where the form provides them)"),
  priorYearOverpaymentCredited: usd.optional().describe("prior-year state overpayment applied toward this year's estimated tax. VA 760 line 21 (its own printed line \u2014 never fold into line 20 estimated payments). Other states: folded into the estimated-payments line."),
  extensionPayment: usd.optional().describe("payment made with an extension request. VA 760 line 22 (its own line, never folded into the estimated-payments line). NY IT-201 line 75 is the COMBINED line \u2014 'estimated tax payments and amount paid with Form IT-370' \u2014 so for NY this is added into the same line as estimatedPayments, not kept separate."),
  taxableSocialSecurity: usd.optional().describe("federally TAXABLE social security (Form 1040 line 6b, from compute_return). REQUIRED whenever nonzero: VA (760 line 5 subtraction), CA (Schedule CA line 6 col B), and NY (IT-201 line 27) all subtract it \u2014 the composer applies the subtraction automatically; do NOT also fold it into the generic subtractions total."),
  unemploymentCompensation: usd.optional().describe("unemployment compensation included in federal AGI (Schedule 1 line 7). REQUIRED whenever nonzero: VA fully subtracts it (Va. Code \xA7 58.1-322.02(9), Schedule ADJ) and CA excludes it (Schedule CA line 7 col B) \u2014 the composer subtracts automatically for those states; do NOT also fold it into the generic subtractions total. IL and NY tax it (no subtraction)."),
  claimedAsDependent: external_exports.boolean().optional().describe("someone else can claim this taxpayer as a dependent (carry the prior-year 1040 'Someone can claim: You as a dependent' checkbox forward as a continuing condition unless the current-year interview contradicts it). IL: zeroes the line 10 exemption allowance when base income exceeds the exemption amount. VA: limits the standard deduction to earned income."),
  useTax: usd.optional().describe("consumer use / sales-use tax owed on the return"),
  nonrefundableCredits: usd.optional().describe("state NONREFUNDABLE credits without an oracle target \u2014 capped at the state tax due by the composer (an excess never creates a refund). VA: do NOT put the low-income credit or any VA EITC election here \u2014 passing it forces a legacy capped path; instead pass federalEITC (+ vaFamilyVagi if testing the low-income credit) and the composer computes and SELECTS the Form 760 line 23 credit itself (TY2025 refundable VA EITC = 20% of federal EIC, uncapped \u2014 it dominates whenever federal EITC > 0). IL: pass ICR raw inputs instead where fields exist."),
  refundableCredits: usd.optional().describe("state refundable credits, e.g. the NY credit block: ESCC + NYS EIC + IT-216 + NYC EIC + NYC school tax + NYC child care (WITHOUT their own oracle target, self-computed per the us.ny.parameters citation and disclosed) PLUS us.ny.it214 (the Real Property Tax Credit, which DOES have an oracle target as of TY2025 v5 \u2014 pass its computed answer here, not a hand-derived percentage of rent)")
};
var il = {
  ilPropertyTaxPaid: usd.optional().describe("IL property tax on principal residence, net of business-use portion"),
  ilK12Expenses: usd.optional().describe("IL qualified K-12 education expenses (before the $250 floor)"),
  ilTeacherExpenses: usd.optional().describe("IL Schedule 1299-C educator materials expenses"),
  ilChildUnder12: external_exports.boolean().optional().describe("IL CTC gate: a QUALIFYING CHILD (\xA7 152(c) lineage \u2014 child/stepchild/foster/sibling or their descendants) under age 12. A qualifying-relative/ODC-only dependent does NOT satisfy this even if under 12; leave false."),
  ilEitcOverride: usd.optional().describe("us.il.eitc oracle target's answer (35 ILCS 5/212(a)(vi), (b-5), (b-10): 20% of the federal EITC recomputed WITHOUT the \xA7 32(c)(1)(A)(ii) childless age gate) \u2014 WINS over the generic 20%-of-federalEITC line-29 computation when present. MUST be used (not merely optional) for a taxpayer age 18-24 or 65+ with NO qualifying children: federalEITC alone is correctly $0 for that population under federal law, so line 29 = 20% x federalEITC would wrongly zero out Illinois' decoupled credit \u2014 pass us.il.eitc's computed answer instead. Safe to pass for every IL EITC claimant (agrees with the generic computation outside the decoupled population).")
};
var va = {
  vaAgeDeduction: usd.optional().describe("OVERRIDE ONLY \u2014 pass vaAgeQualifyingFull/vaAgeQualifyingTested instead and the composer computes the age deduction itself (including the AFAGI social-security exclusion agents routinely miss). When splitting an odd joint total between spouses (Form 760 lines 4a/4b), the odd dollar goes to the SPOUSE."),
  vaAgeQualifyingFull: external_exports.number().int().optional().describe("count of filers (taxpayer/spouse) born ON OR BEFORE January 1, 1939 \u2014 each gets the UNCONDITIONAL $12,000 age deduction (no income test)"),
  vaAgeQualifyingTested: external_exports.number().int().optional().describe("count of filers born January 2, 1939 - January 1, 1961 (65+ for 2025 but income-tested): the composer computes $12,000 each, reduced dollar-for-dollar by AFAGI over $50,000 single / $75,000 married \u2014 where AFAGI = federal AGI MINUS the federally taxable social security (the SS exclusion is the step agents miss; Va. Code \xA7 58.1-322.03(2))"),
  vaRefundableEitc: usd.optional().describe("OVERRIDE ONLY \u2014 the composer now computes the Form 760 line 23 credit itself from federalEITC + the eligibility inputs below. If passed, this refundable amount wins over the computed selection."),
  vaSpouseTaxAdjustment: usd.optional().describe("OVERRIDE ONLY \u2014 the composer now computes the VA Spouse Tax Adjustment worksheet itself when vaYourVagi/vaSpouseVagi are provided. If passed, this amount wins."),
  vaYourVagi: usd.optional().describe("PRIMARY taxpayer's separate VAGI (MFJ only \u2014 the 760 instructions' 'Worksheet for Determining Separate Virginia Adjusted Gross Income': own wages/SE/pensions, own share of joint items 50/50, own age deduction and subtractions). Enables the composer's Spouse Tax Adjustment worksheet (Form 760 line 17). vaYourVagi + vaSpouseVagi must equal line 9 VAGI."),
  vaSpouseVagi: usd.optional().describe("spouse's separate VAGI for the STA worksheet (Form 760 line 17 box; MFJ only)"),
  vaYourAgeBlindBoxes: external_exports.number().int().optional().describe("STA worksheet Part 1 line 2: PRIMARY taxpayer's 65+/blind box count (0-2) \u2014 per-spouse exemption = boxes x $800 + $930"),
  vaSpouseAgeBlindBoxes: external_exports.number().int().optional().describe("STA worksheet Part 1 line 2: spouse's 65+/blind box count (0-2)"),
  vaFamilyVagi: usd.optional().describe("Schedule ADJ line 10 total family VAGI (you + spouse + dependents' VAGI) for the Credit for Low-Income Individuals poverty test; defaults to line 9 VAGI when omitted"),
  // VA Schedule A (itemized) — pass the FEDERAL Schedule A component amounts;
  // the composer applies the Virginia exceptions (10% medical floor, sales-tax
  // cap, no SALT cap on property taxes, the Virginia Pease limitation) itself.
  vaItemizing: external_exports.boolean().optional().describe("taxpayer itemized federally (VA requires the same election, Va. Code \xA7 58.1-322.03(1)) \u2014 enables the VA Schedule A computation from the component inputs below; Form 760 line 10 replaces the line 11 standard deduction"),
  vaItemizedMedical: usd.optional().describe("VA Sch A line 1: total medical/dental expenses BEFORE any floor (VA applies its own 10%-of-FAGI floor \u2014 Virginia deconforms from the federal 7.5% floor)"),
  vaItemizedStateLocalIncomeTaxes: usd.optional().describe("VA Sch A line 5a when INCOME taxes are claimed (mutually exclusive with sales taxes)"),
  vaItemizedSalesTaxes: usd.optional().describe("VA Sch A line 5a when the general SALES tax election was made federally \u2014 capped at the Virginia SALT cap ($40,000; $20,000 MFS for TY2025)"),
  vaItemizedRealEstateTaxes: usd.optional().describe("VA Sch A line 5b real estate taxes \u2014 NOT subject to the SALT cap for Virginia"),
  vaItemizedPersonalPropertyTaxes: usd.optional().describe("VA Sch A line 5c personal property taxes \u2014 NOT subject to the SALT cap for Virginia"),
  vaItemizedOtherTaxes: usd.optional().describe("VA Sch A line 6 other taxes (foreign income tax etc.)"),
  vaItemizedMortgageInterest: usd.optional().describe("VA Sch A home mortgage interest and points (federal Schedule A amount)"),
  vaItemizedInvestmentInterest: usd.optional().describe("VA Sch A investment interest (protected from the overall limitation)"),
  vaItemizedCharitable: usd.optional().describe("VA Sch A charitable contributions (federal Schedule A amount)"),
  vaItemizedCasualty: usd.optional().describe("VA Sch A casualty/theft losses (protected from the overall limitation)"),
  vaItemizedGambling: usd.optional().describe("VA Sch A gambling losses (\xA7 165(d), limited to winnings; protected from the overall limitation)"),
  vaItemizedOther: usd.optional().describe("VA Sch A other itemized deductions"),
  vaScheduleAdjDeductions: usd.optional().describe("Schedule ADJ line 9 total deductions (deduction CODES like 105 continuing-teacher-education, 199 other) \u2014 prints on Form 760 line 13; these are DEDUCTIONS from VAGI, never income subtractions on line 7")
};
var ca = {
  caCalEITC: usd.optional().describe("us.ca.caleitc result (pass the oracle target's answer)"),
  caYCTC: usd.optional().describe("us.ca.yctc result (pass the oracle target's answer)"),
  caItemizedDeductions: usd.optional().describe("CA itemized deduction total (Schedule CA Part II, line 29) \u2014 agent-computed per Schedule CA's own itemized rules WITH disclosure (differs from the federal Schedule A: no SALT cap, mortgage/medical add-backs, etc.). Form 540 line 18 takes the GREATER of this or the CA standard deduction; omit to use the standard deduction only."),
  caRentersCredit: usd.optional().describe("us.ca.renters_credit result (pass the oracle target's answer) \u2014 nonrefundable, joins the exemption credits in the line-48 subtraction from tax."),
  caAmt: usd.optional().describe("OVERRIDE for Form 540 line 61 \u2014 prefer caIsoPreference + caAmtTaxesAddback so the composer builds Schedule P AMTI and evaluates us.ca.amt itself; a passed caAmt wins."),
  caBhst: usd.optional().describe("us.ca.bhst result (pass the oracle target's answer) \u2014 Form 540 line 62 Behavioral Health Services Tax (R&TC \xA7 17043, 1% of CA taxable income over $1,000,000); added into line 64 total tax when nonzero."),
  caEducatorExpensesDeducted: usd.optional().describe("federal educator-expense deduction claimed (\xA7 62(a)(2)(D)) \u2014 California does NOT conform: the composer ADDS it back on Schedule CA (line 11 col C). Pass the federal amount actually deducted (both spouses' combined)."),
  caHsaDeduction: usd.optional().describe("federal HSA deduction (Form 8889 line 13) \u2014 California does not conform to \xA7 223: the composer ADDS it back for CA"),
  caHsaTaxableDistribution: usd.optional().describe("HSA distribution amount taxed federally (Form 8889 line 16) \u2014 not income for California: the composer SUBTRACTS it for CA"),
  caAb5GrossIncomeAddition: usd.optional().describe("gross income from businesses where the worker is classified as an EMPLOYEE for California (AB 5/Dynamex reclassification; the intake's ca_form540_schca.add_gross_income field) \u2014 Schedule CA WAGE addition, col C"),
  caAb5NetLossAddition: usd.optional().describe("net losses from businesses where the worker is an employee for California (intake ca_form540_schca.add_net_loss) \u2014 the federal Schedule C loss is disallowed for CA: Schedule CA BUSINESS addition, col C"),
  caDepreciationAddition: usd.optional().describe("CA depreciation-difference addition: federal depreciation (with \xA7 168(k) bonus, which California NEVER conforms to) minus CA depreciation (plain MACRS on the same asset). Positive = CA income addition (Schedule CA col C on the business/rents line). Compute per-asset and disclose."),
  caTaxableEarlyDistribution: usd.optional().describe("retirement-plan early distribution amount subject to the FEDERAL \xA7 72(t) additional tax \u2014 California imposes its own 2.5% additional tax on the same base (R&TC \xA7 17085(c)(1), FTB 3805P); the composer computes 2.5% and prints it on Form 540 line 63"),
  caIsoPreference: usd.optional().describe("ISO exercise spread AMT preference (\xA7 56(b)(3) as modified by R&TC \xA7 17062) \u2014 with caAmtTaxesAddback this lets the composer BUILD Schedule P AMTI itself (AMTI = line 19 taxable income + taxes deducted in the CA itemized deduction + this preference; standard deduction added back instead when not itemizing) and evaluate us.ca.amt internally; caAmt (a precomputed answer) wins if both are given"),
  caAmtTaxesAddback: usd.optional().describe("taxes actually included in the CA itemized deduction (property taxes etc. surviving the Schedule CA SALT adjustments) \u2014 the Schedule P line 2 addback used when the composer builds AMTI from caIsoPreference; $0 when not itemizing (the composer adds back the standard deduction instead)")
};
var ny = {
  nyHouseholdCredit: usd.optional().describe("NYS household credit from table 2 (us.ny.parameters citation)"),
  nycTaxableIncome: usd.optional().describe("NYC taxable income (IT-201 line 47) if NYC resident"),
  nycHouseholdCredit: usd.optional().describe("NYC household credit from table 5"),
  yonkersSurcharge: usd.optional().describe("us.ny.yonkers_surcharge result (pass the oracle target's answer) \u2014 16.75% of the Yonkers worksheet's netted base (nyYonkersBase). Added into line 62's total and printed on its own line (IT-201 LINE 55, not 54 \u2014 line 54 is MCTMT) when nonzero.")
};
var pa = {
  // PA is CLASS-BASED (eight classes, 72 P.S. § 7303) — federalAGI is NOT the
  // PA base. Transcribe the class amounts below; the composer runs the corpus
  // targets (class netting, Schedule O, 3.07% tax, Schedule SP) itself.
  paGrossCompensation: usd.optional().describe("PA-40 line 1a: W-2 BOX 16 total (NOT Box 1 \u2014 401(k)/elective deferrals are PA-taxable; eligible retirement distributions are exempt and excluded). Falls back to the shared wages input when omitted (composer discloses). Include taxable early-distribution amounts under the cost-recovery method."),
  paUnreimbursedExpenses: usd.optional().describe("PA-40 line 1b: Schedule UE unreimbursed employee business expenses (a compensation-class expense, never a line-10 deduction)"),
  paInterest: usd.optional().describe("PA-40 line 2: PA-taxable interest (gross class \u2014 no expenses; includes commercial-annuity interest taxable as PA interest)"),
  paDividends: usd.optional().describe("PA-40 line 3: PA-taxable dividends INCLUDING mutual-fund capital-gain distributions (PA classifies them as dividends, not gains)"),
  paBusinessNet: usd.optional().describe("PA-40 line 4, TAXPAYER's own net business/profession/farm income or LOSS (negative allowed; within-class netting of the taxpayer's own activities only \u2014 a loss never crosses classes or spouses)"),
  paSpouseBusinessNet: usd.optional().describe("PA-40 line 4, SPOUSE's own net business income or loss (kept separate: PA never nets one spouse's loss against the other's income)"),
  paPropertyNet: usd.optional().describe("PA-40 line 5, taxpayer's own net gain/loss from sale/exchange/disposition of property (negative allowed; no carryover)"),
  paSpousePropertyNet: usd.optional().describe("PA-40 line 5, spouse's own net property gain/loss"),
  paRentRoyaltyNet: usd.optional().describe("PA-40 line 6, taxpayer's own net rents/royalties/patents/copyrights (short-term rentals under 30 days are BUSINESS income, line 4)"),
  paSpouseRentRoyaltyNet: usd.optional().describe("PA-40 line 6, spouse's own net rent/royalty amount"),
  paEstateTrust: usd.optional().describe("PA-40 line 7: estate/trust income (PA Schedule J; an estate or trust cannot distribute a loss \u2014 never negative)"),
  paGambling: usd.optional().describe("PA-40 line 8: gambling and lottery winnings net of wager costs (noncash PA Lottery prizes exempt; cash prizes taxable)"),
  paStudentLoanInterest: usd.optional().describe("Schedule O code S: student loan interest PAID (new deduction for 2025; the composer caps at $2,500 \u2014 pass the uncapped amount)"),
  pa529Contributions: usd.optional().describe("Schedule O code T: \xA7 529 contributions, ALREADY capped at $19,000 per beneficiary per taxpayer-spouse (2025); no deduction for rollovers/beneficiary changes"),
  paAbleContributions: usd.optional().describe("Schedule O code A: PA ABLE contributions, capped at the federal gift-tax exclusion ($19,000 for 2025)"),
  paMsaHsaContributions: usd.optional().describe("Schedule O codes M/H: MSA + HSA contributions at the federally-allowed amounts"),
  paSpDependentChildren: external_exports.number().int().optional().describe("Schedule SP dependent CHILDREN count (child/stepchild/adopted; grandchild of a grandparent; foster child of a foster parent \u2014 never other relatives) claimable as federal dependents; each adds $9,500 to the Tax Forgiveness eligibility-income threshold"),
  paEligibilityAddbacks: usd.optional().describe("Schedule SP Section III nontaxable add-backs (gifts, inheritances, insurance proceeds, non-PA income, nontaxable military pay, excluded home-sale gain, educational assistance, outside cash support). NOT Social Security, eligible retirement benefits, child support, or workers' comp."),
  paResidentCredit: usd.optional().describe("PA-40 line 22: resident credit for tax paid other states (Schedule G-L; not for reciprocal-state compensation: IN/MD/NJ/OH/VA/WV). Subtracts BEFORE Tax Forgiveness \u2014 the composer handles the ordering."),
  paScheduleDcCredit: usd.optional().describe("PA-40 line 23 component: the Child and Dependent Care Enhancement credit \u2014 pass us.pa.cdcc's computed answer (= 100% of the federal Form 2441 line 9a tentative credit; refundable)"),
  paScheduleOcCredits: usd.optional().describe("PA-40 line 23 component: Schedule OC restricted credits total (transcribed; no oracle target)"),
  paNrk1Withholding: usd.optional().describe("PA-40 line 17: nonresident tax withheld from PA Schedule(s) NRK-1"),
  paPenaltiesInterest: usd.optional().describe("PA-40 line 27: penalties and interest incl. estimated-underpayment penalty (REV-1630)")
  // PA line 13 withholding uses the shared stateWithholding input: sum state
  // tax withheld from EVERY document type (W-2 box 17, W-2G box 15, 1099-R
  // box 14, 1099-MISC box 15, 1099-NEC box 5).
};
var stateReturnShape = { ...shared, ...il, ...va, ...ca, ...ny, ...pa };

// ../compose/dist/index.js
function makeStateTaxEvaluator(runTarget, input) {
  return (target, stateTaxableIncomeCents, extraFacts) => {
    const facts2 = {
      stateTaxableIncome: { type: "money", value: String(stateTaxableIncomeCents) },
      filingStatus: {
        type: "enum",
        value: input.filingStatus ?? (input.filingJoint ? "mfj" : input.filingHoh || input.filingHohOrQss ? "hoh" : "single")
      },
      useFormulaMethod: { type: "bool", value: false },
      spouseItemizes: { type: "bool", value: false }
    };
    for (const [k, v] of Object.entries(extraFacts ?? {})) {
      facts2[k] = typeof v === "boolean" ? { type: "bool", value: v } : typeof v === "number" ? { type: "int", value: String(v) } : { type: "money", value: String(v) };
    }
    return runTarget(facts2, target);
  };
}
function composeStateReturn(input, evalStateTax) {
  const notes = [];
  const fs = input.filingStatus;
  if (fs) {
    input.filingJoint = fs === "mfj";
    input.filingHoh = fs === "hoh";
    input.filingHohOrQss = fs === "hoh" || fs === "qss" || fs === "mfj";
  }
  const j = input.jurisdiction;
  if (j !== "pa" && typeof input.federalAGI !== "number") {
    throw new Error("federalAGI is required for il/va/ca/ny state returns \u2014 run compute_return first and pass Form 1040 line 11 verbatim");
  }
  if (j === "il")
    return { lines: composeIL(input, evalStateTax, notes), notes };
  if (j === "va")
    return { lines: composeVA(input, evalStateTax, notes), notes };
  if (j === "ca")
    return { lines: composeCA(input, evalStateTax, notes), notes };
  if (j === "pa")
    return { lines: composePA(input, evalStateTax, notes), notes };
  return { lines: composeNY(input, evalStateTax, notes), notes };
}

// dist/commands/state.js
var inputSchema = external_exports.object({
  ...stateReturnShape,
  filingJoint: external_exports.boolean().optional(),
  filingHoh: external_exports.boolean().optional(),
  filingHohOrQss: external_exports.boolean().optional()
}).strict();
function runState(flags) {
  const corpus = getCorpus();
  const asOf = flags.asOf ?? (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  let args;
  try {
    args = inputSchema.parse(JSON.parse(readFileSync4(flags.facts, "utf8")));
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    if (flags.json) {
      print({ ok: false, error: { code: "BAD_INPUT", message } });
    } else {
      console.error(import_picocolors14.default.red(`bad facts file: ${message}`));
    }
    return EXIT.ERROR;
  }
  try {
    const evalStateTax = makeStateTaxEvaluator((facts2, target) => {
      const { value } = evaluate(corpus, facts2, { asOf, target });
      return value.type === "money" ? value.cents : 0n;
    }, args);
    const { lines, notes } = composeStateReturn(args, evalStateTax);
    if (flags.json) {
      print({
        ok: true,
        asOf,
        jurisdiction: args.jurisdiction,
        corpusMerkleRoot: corpus.merkleRoot,
        lines,
        ...notes.length ? { notes } : {}
      });
      return EXIT.OK;
    }
    console.log();
    console.log(import_picocolors14.default.bold(`${String(args.jurisdiction).toUpperCase()} return \u2014 as of ${asOf}:`));
    const width = Math.max(...Object.keys(lines).map((k) => k.length)) + 2;
    for (const [line, amount] of Object.entries(lines)) {
      console.log(`  ${line.padEnd(width)}${amount}`);
    }
    if (notes.length) {
      console.log();
      for (const n of notes)
        console.log(import_picocolors14.default.dim(`  ${n}`));
    }
    console.log();
    return EXIT.OK;
  } catch (err) {
    if (flags.json) {
      print(err instanceof OpenTaxError ? { ok: false, error: err.toJSON() } : { ok: false, error: { code: "ERROR", message: String(err?.message ?? err) } });
    } else {
      console.error(import_picocolors14.default.red(String(err?.message ?? err)));
    }
    return EXIT.ERROR;
  }
}

// dist/commands/verify.js
var import_picocolors15 = __toESM(require_picocolors(), 1);
import { readFileSync as readFileSync5 } from "node:fs";
function runVerify(proofPath, flags) {
  let artifact;
  try {
    artifact = JSON.parse(readFileSync5(proofPath, "utf8"));
  } catch (err) {
    return emitError(new Error(`cannot read proof ${proofPath}: ${err.message}`), flags.json);
  }
  let result;
  try {
    result = verifyProof(artifact, getCorpus());
  } catch (err) {
    return emitError(err, flags.json);
  }
  if (flags.json) {
    if (result.ok) {
      print({
        ok: true,
        verified: true,
        target: result.target,
        value: artifact.root.value,
        formatted: formatValue(artifact.root.value),
        asOf: artifact.asOf,
        corpusMerkleRoot: result.corpusMerkleRoot,
        artifactHash: artifact.artifactHash
      });
      return EXIT.OK;
    }
    print({
      ok: false,
      verified: false,
      error: {
        code: `VERIFY_${result.reason.toUpperCase().replaceAll("-", "_")}`,
        message: result.message,
        data: {
          reason: result.reason,
          ...result.path ? { path: result.path } : {},
          ...result.expected ? { expected: result.expected } : {},
          ...result.actual ? { actual: result.actual } : {}
        },
        hint: result.reason === "corpus-mismatch" ? "The proof was made against a different corpus version. Re-evaluate with the installed corpus, or install the corpus version it names." : "This proof does not check out. Do not trust its answer; re-run `opentax eval` to derive a fresh one."
      }
    });
    return EXIT.ERROR;
  }
  if (result.ok) {
    console.log();
    console.log(`${import_picocolors15.default.green("\u2713 VERIFIED")}  ${import_picocolors15.default.bold(artifact.target)}`);
    console.log(`  value      ${import_picocolors15.default.green(formatValue(artifact.root.value))} (as of ${artifact.asOf})`);
    console.log(`  corpus     ${result.corpusMerkleRoot}`);
    console.log(`  artifact   ${artifact.artifactHash}`);
    console.log(import_picocolors15.default.dim("  independently re-derived from the corpus; every node matches the recorded derivation"));
    console.log();
    return EXIT.OK;
  }
  console.error();
  console.error(`${import_picocolors15.default.red("\u2717 VERIFICATION FAILED")}  (${result.reason})`);
  console.error(`  ${result.message}`);
  if (result.path)
    console.error(`  divergence at: ${import_picocolors15.default.bold(result.path)}`);
  if (result.expected)
    console.error(`  expected: ${result.expected}`);
  if (result.actual)
    console.error(`  actual:   ${result.actual}`);
  console.error();
  return EXIT.ERROR;
}

// dist/index.js
var COMMON_KEYS = /* @__PURE__ */ new Set([
  "status",
  "wages",
  "kids",
  "age65",
  "interest",
  "gains",
  "seProfit",
  "salt",
  "mortgageInterest",
  "charity",
  "medical",
  "tips",
  "overtime",
  "socialSecurity",
  "pensions",
  "withheld"
]);
function registerFactFlags(cmd) {
  for (const f of FACT_FLAGS) {
    const opt = new Option(f.option, f.help);
    if (!COMMON_KEYS.has(f.key))
      opt.hideHelp();
    cmd.addOption(opt);
  }
  cmd.addHelpText("after", `
These are the everyday flags; ${FACT_FLAGS.length - COMMON_KEYS.size}+ more cover business owners, corporations, investors, education, and retirement \u2014 run \`opentax flags\` for the grouped catalog, or \`opentax facts\` for every input with its default.`);
}
var program2 = new Command();
program2.name("opentax").description(`Verifiable US tax engine: every answer ships with a machine-checkable derivation citing its statutory sources.

Every command accepts --json (single JSON object on stdout, ok: true|false).
Exit codes: 0 ok \xB7 2 needs more facts (retry with them) \xB7 3 not covered by the corpus (don't retry) \xB7 1 other error.`).version("0.1.0");
var evalCmd = program2.command("eval").description("compute a tax answer and print its proof tree").option("--facts <file>", "JSON facts file (inline flags below override it)");
registerFactFlags(evalCmd);
evalCmd.option("--as-of <date>", "evaluate rules as of this ISO date (default: today)").option("--target <ruleId>", "rule to derive (default: income tax after credits)").option("--proof <file>", "write the proof artifact JSON to this file").option("--brief", "plain-English summary only (skip the proof tree)").option("--assumptions", "list every default the engine relied on (folded by default)").option("--json", "print the proof artifact JSON instead of the tree").action((flags) => {
  process.exitCode = runEval(flags);
});
var checkCmd = program2.command("check").description("verify a claimed tax amount (e.g. an AI agent's answer) against the law \u2014 exit 0 verified, 1 refuted").option("--facts <file>", "JSON facts file (inline flags below override it)");
registerFactFlags(checkCmd);
checkCmd.requiredOption("--expect <dollars>", "the claimed amount to verify (negative = refund)").option("--tolerance <dollars>", "allowed absolute difference", "1").option("--as-of <date>", "evaluate rules as of this ISO date (default: today)").option("--target <ruleId>", "rule to check (default: net tax; balance due if --withheld)").option("--proof <file>", "write the proof artifact JSON to this file").option("--json", "machine-readable verdict").action((flags) => {
  process.exitCode = runCheck(flags);
});
program2.command("facts").description("list every input fact the corpus understands").option("--json", "machine-readable output").action((flags) => {
  process.exitCode = runFacts({ json: flags.json });
});
program2.command("flags").description("the full grouped catalog of situation flags for eval/check/sweep/\u2026").action(() => {
  const order = [
    "Filing & family:",
    "Income:",
    "Deductions & savings:",
    "Education credits:",
    "Business owner:",
    "Investor (AMT / QSBS):",
    "Corporate & entity (Form 1120):",
    "Payments & estimated tax:",
    "Expert:"
  ];
  console.log();
  for (const group of order) {
    const flags = FACT_FLAGS.filter((f) => f.group === group);
    if (!flags.length)
      continue;
    console.log(`\x1B[1m${group}\x1B[0m`);
    const width = Math.max(...flags.map((f) => f.option.length)) + 2;
    for (const f of flags)
      console.log(`  ${f.option.padEnd(width)}${f.help}`);
    console.log();
  }
  console.log("All of these work on eval, check, sweep, marginal, cliffs, and compare.");
});
program2.command("lookup").description("find the dollar amounts and citations behind a question, e.g. `opentax lookup standard deduction`").argument("<query...>", "plain-English search terms").option("--as-of <date>", "law in force on this ISO date (default: today)").option("--expect <dollars>", "fact-check a claimed amount: exits 0 verified, 1 refuted, 3 unknown").option("--filing-status <status>", "single | mfj | mfs | hoh | qss (narrows --expect to one status)").option("--json", "machine-readable output").action((queryWords, flags) => {
  process.exitCode = runLookup(queryWords, {
    asOf: flags.asOf,
    json: flags.json,
    expect: flags.expect,
    filingStatus: flags.filingStatus
  });
});
program2.command("search").description('full-text search over the encoded law, e.g. `opentax search kiddie tax` \u2014 zero hits means "outside the corpus"').argument("<query...>", "plain-English search terms").option("--as-of <date>", "law in force on this ISO date (default: today)").option("--limit <n>", "max results (default 8)").option("--json", "machine-readable output").action((queryWords, flags) => {
  process.exitCode = runSearch(queryWords, { asOf: flags.asOf, limit: flags.limit, json: flags.json });
});
program2.command("occupation").description("is this a \xA7 224 tipped occupation? matches the Treasury list, never guesses").argument("<title...>", "job title, e.g. `opentax occupation bartender`").option("--json", "machine-readable output").action((words, flags) => {
  process.exitCode = runOccupation(words, { json: flags.json });
});
program2.command("state").description("compose a state return (IL-1040, VA 760, CA 540, NY IT-201, PA-40) from a composer-facts JSON file").requiredOption("--facts <file>", "composer facts JSON (same shape as the MCP compute_state_return tool)").option("--as-of <date>", "law in force on this ISO date (default: today)").option("--json", "machine-readable output").action((flags) => {
  process.exitCode = runState({ facts: flags.facts, asOf: flags.asOf, json: flags.json });
});
program2.command("signup").description("get OpenTax updates by email (new features and other things worth knowing)").argument("<email>", "your email address").option("--json", "machine-readable output").action(async (email, flags) => {
  process.exitCode = await runSignup(email, { json: flags.json });
});
program2.command("verify").description("re-execute a proof artifact and confirm or refute it").argument("<proof>", "proof artifact JSON file").option("--json", "machine-readable output").action((proofPath, flags) => {
  process.exitCode = runVerify(proofPath, { json: flags.json });
});
program2.command("explain").description("show a rule: citation, effective window, parameters, dependencies").argument("<ruleId>", "rule id, e.g. us.federal.standard_deduction").option("--as-of <date>", "only the version valid on this date").option("--json", "machine-readable output").action((ruleId, flags) => {
  process.exitCode = runExplain(ruleId, { asOf: flags.asOf, json: flags.json });
});
function solverCommand(name, description) {
  const cmd = program2.command(name).description(description);
  cmd.option("--facts <file>", "JSON facts file (inline flags below override it)");
  registerFactFlags(cmd);
  cmd.option("--vary <factId>", "fact to vary", "wages").option("--as-of <date>", "evaluate rules as of this ISO date (default: today)").option("--target <ruleId>", "rule to derive (default: net tax)").option("--json", "machine-readable output");
  return cmd;
}
solverCommand("sweep", "evaluate the target across a range of one input").requiredOption("--from <dollars>", "range start").requiredOption("--to <dollars>", "range end").option("--step <dollars>", "step size", "1000").option("--csv", "CSV output").action((flags) => {
  process.exitCode = runSweep(flags);
});
solverCommand("marginal", "effective marginal rate at a point").requiredOption("--at <dollars>", "input value to differentiate at").option("--delta <dollars>", "finite-difference increment", "100").action((flags) => {
  process.exitCode = runMarginal(flags);
});
solverCommand("cliffs", "find exact dollars where one more cent of input costs more than a cent (marginal > 100%)").requiredOption("--from <dollars>", "range start").requiredOption("--to <dollars>", "range end").option("--step <dollars>", "coarse scan step", "1000").action((flags) => {
  process.exitCode = runCliffs(flags);
});
solverCommand("invert", "smallest input where the target first reaches a goal (e.g. wages that produce a given tax)").requiredOption("--goal <dollars>", "target value to reach").requiredOption("--lo <dollars>", "search range start").requiredOption("--hi <dollars>", "search range end").action((flags) => {
  process.exitCode = runInvert(flags);
});
solverCommand("compare", "compare the answer across an enum fact's values").action((flags) => {
  process.exitCode = runCompare({ ...flags, vary: flags.vary === "wages" ? "filingStatus" : flags.vary });
});
var corpusCmd = program2.command("corpus").description("inspect the installed rule corpus");
corpusCmd.command("hash").description("print the corpus Merkle root").option("--json", "machine-readable output").action((flags) => {
  process.exitCode = runCorpusHash({ json: flags.json });
});
corpusCmd.command("export").description("full corpus as JSON, for independent verifiers (see kernel/)").action(() => {
  process.exitCode = runCorpusExport();
});
corpusCmd.command("list").description("list rules with validity windows and citations").option("--as-of <date>", "only rules valid on this date").option("--json", "machine-readable output").action((flags) => {
  process.exitCode = runCorpusList({ asOf: flags.asOf, json: flags.json });
});
function runCli(argv) {
  program2.parse(argv);
}

// dist/bin.js
runCli();
